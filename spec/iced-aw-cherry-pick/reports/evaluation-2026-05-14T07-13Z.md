---
slug: iced-aw-cherry-pick
kind: evaluation
ts: 2026-05-14T07-13Z
evaluator: read-only fresh context
log_ref: spec/iced-aw-cherry-pick/reports/test-run-2026-05-14T07-13Z.log
log_sha256: 30906659b18700b56bdad2f4e4874bf437ee00a76b3ff41b7df8985b68bf97d2
verdict: PASS
trace_refs: ["REQ-ICED-AW-001", "REQ-ICED-AW-002", "REQ-ICED-AW-003"]
---

# Evaluation — Brief B (iced_aw cherry-pick)

Read-only verdict from the evaluator sub-agent. The evaluator had no
sight of the developer's or ui-designer's diff; grading is strictly
against the raw test-run log
([`test-run-2026-05-14T07-13Z.log`](./test-run-2026-05-14T07-13Z.log))
plus the files the log explicitly cites
([`tasks.md`](../tasks.md), [`spec/trace.toml`](../../trace.toml)).

## Verdict matrix

| # | Criterion | Verdict | Cite (log line / file) |
|---|-----------|---------|------------------------|
| 1 | Compile gates green (cargo fmt + 4× cargo build) | PASS | log:7 (`## exit: 0` for fmt), log:44 (build tests), log:48 (viewer), log:52 (cockpit), log:76 (cockpit_live) |
| 2 | `cargo test -p ui` exits 0 with `N passed; 0 failed` | PASS — N = 267 across 23 binaries | log:518 exit 0; sum of `test result: ok` lines 271, 281, 287, 293, 303, 311, 318, 330, 343, 349, 357, 365, 371, 377, 383, 458, 464, 473, 479, 493, 500, 510, 516 = 154+4+0+0+4+2+1+6+7+0+2+2+0+0+0+69+0+3+0+8+1+4+0 = 267 |
| 3 | Two-run determinism — run 2 count equals run 1 AND `find …*.snap.new` empty | PASS — run 2 N = 267 (matches); find returned zero | log:520-960 second test run exit 0; sum 713, 723, 729, 735, 745, 753, 760, 772, 785, 791, 799, 807, 813, 819, 825, 900, 906, 915, 921, 935, 942, 952, 958 = 267; log:962-963 `find crates/ui -name *.snap.new` returns no rows, exits 0 (H-arch-9 hard gate green) |
| 4 | Fmt + clippy on touched files green — clippy errors CONFINED to documented pre-existing files (`chart.rs`, `window_icon.rs`) | PASS — fmt exit 0 (log:7); all 6 clippy errors at log:1344, 1358, 1367, 1376, 1388, 1413 land in `crates/ui/src/widgets/chart.rs` (lines 1347, 1352, 1356, 1406, 1417) and `crates/ui/src/window_icon.rs:151`. ZERO clippy errors in `iced_widget_catalogs.rs` / `frame.rs` / `widgets/strategies.rs` / `widgets/positions.rs` / `widgets/pnl.rs` / `widgets/agent_feed.rs` / `screens/audit.rs` / `screens/strategies.rs` / `screens/risk.rs` / `viewer.rs` / `bin/viewer.rs` / `theme.rs`. clippy step exits 101 — that exit is the *documented* baseline of pre-existing `expect_used` denials, not a Brief B regression |
| 5 | No NET-NEW rustdoc warnings | PASS — 6 warnings total (log:1008 `ui (lib doc) generated 6 warnings`). All 6 warning sources are pre-existing: `widgets/chart_tooltip.rs:3` (log:967), `widgets/volume_histogram.rs:11` (log:975), `widgets/volume_histogram.rs:13` (log:981), `window_icon.rs:119` (log:989), `test_support.rs:4` (log:997), `test_support.rs:75` (log:1003). NONE in Brief B-touched files (`frame.rs`, `iced_widget_catalogs.rs`, `bin/viewer.rs`, `viewer.rs`, the 8 screens/widgets swap sites) |
| 6 | Transitive dep budget — `iced_aw v0.14.x` + `chrono` + `num-traits` + `once_cell` | PASS — log:1515 `iced_aw v0.14.1`; chrono v0.4.44 (log:1517), num-traits v0.2.19 (log:1492), once_cell v1.21.4 (log:1494). Architect's H-arch-4 prediction of "+3 transitive crates" matches exactly. Note: `iced_fonts v0.3.0` compiles at log:56 — already a workspace dep, not a Brief B addition (already in the tree before Brief B per the architect's tree falsifier in tasks.md T-M0-H4). |
| 7 | Determinism check `CLOCKS PASS` | PASS — log:1534 `CLOCKS PASS  (8 files / 4 patterns)` |
| 8 | Anchors diff empty | PASS — log:1547-1548 `## git diff --stat HEAD spec/anchors.toml` followed by `## exit: 0` with zero stat body — no modification |
| 9 | trace.toml columns filled for REQ-ICED-AW-001/-002/-003 | PASS — all three rows have non-empty `crates` and `tests` (spec/trace.toml:211-219 / 233-249 / 263-271) |
| 10 | Honest ticks — spot-check 3 ticked tasks for file:line + cmd + output | PASS — see "Honest-tick spot-check" below |

## Test counts

| Run | binaries | Passed | Failed |
|---|---|---|---|
| Run 1 (step 6, log:78-518) | 23 | 267 | 0 |
| Run 2 (step 7, log:520-960) | 23 | 267 | 0 |
| Delta | 0 | 0 | 0 |

Test-runner's claim of 267 verified.

Per-binary breakdown (run 1 = run 2 byte-identical):

| Binary | Tests |
|---|---|
| unit (`src/lib.rs`) | 154 |
| `chart_marker_click_opens_modal` | 4 |
| `chart_smoke` | 0 |
| `chart_typography_pin` | 0 |
| `cockpit_cli_smoke` | 4 |
| `cockpit_live_v1_8_handoff` | 2 |
| `equity_curve_smoke` | 1 |
| `incidents_screen_smoke` | 6 |
| `journal_chart_marker_modal_pin` | 7 |
| `lifecycle_keymap` | 0 |
| `live_bin_e2e` | 2 |
| `live_bin_v1_9` | 2 |
| `live_bin_v1_10` | 0 |
| `live_bin_v1_11` | 0 |
| `oms_screen_smoke` | 0 |
| `panel_snapshots` | 69 |
| `panel_snapshots_doc` | 0 |
| `strategies_screen_sparkline_*` | 3 |
| `strategies_screen_v15a_smoke` | 0 |
| `tape_row_click_opens_modal` | 8 |
| `viewer_read_only` | 1 |
| `visual_snapshots` | 4 |
| doc-tests | 0 |
| **Total** | **267** |

## Honest-tick spot-check

Per `AGENT.md ## Process discipline` rule 1, each `[x]` must cite
file:line, test command, and test-output line. Three sampled:

### Tick A — T-M1-3 (panel snapshot for viewer picker)
- Claims (tasks.md:162-179):
  - file:line: `crates/ui/tests/panel_snapshots.rs:1177-1228` + new snap
    `crates/ui/tests/snapshots/panel_snapshots__viewer_picker_default_closed.snap`
  - test cmd: `cargo test -p ui --test panel_snapshots viewer_picker_default_closed`
  - test output: `test viewer_picker_default_closed ... ok`
- Log corroboration: the snap-file listing at log:1544 shows
  `panel_snapshots__viewer_picker_default_closed.snap` (411 bytes, May 13
  22:51). The log's full `panel_snapshots` runs at log:458 and log:900
  both report `69 passed` (developer claim that the new test joined the
  panel-snapshots family without growing failure count is consistent).
- Verdict: **honest tick**.

### Tick B — T-M2-4 (clocks-grep scope check)
- Claims (tasks.md:338-351):
  - file:line: `scripts/check_no_clocks_in_ui_tests.sh:33-42`
  - test cmd: `bash scripts/check_no_clocks_in_ui_tests.sh`
  - test output: `CLOCKS PASS  (8 files / 4 patterns)`
- Log corroboration: log:1533-1534 — exact match for both the command
  invocation and the output string.
- Verdict: **honest tick**.

### Tick C — T-M3-2 (badge swap on strategies widget)
- Claims (tasks.md:442-467):
  - file:line: `crates/ui/src/widgets/strategies.rs:124-131` + new
    `status_badge_cell` at `:332-345`
  - test cmd (a): `cargo build -p ui`
  - test cmd (b): `cargo test -p ui --test panel_snapshots strategies`
  - test output: `test result: ok. 14 passed; 0 failed`
- Log corroboration: log:458 shows the panel_snapshots run with
  `69 passed` (umbrella result; the developer's narrower `--test
  panel_snapshots strategies` filter giving 14 is consistent with the
  panel_snapshots binary having 69 total tests of which 14 are
  strategies-named). The compile gate at log:44 (cargo build tests)
  exits 0, satisfying part (a).
- Verdict: **honest tick** (the umbrella log can't perfectly verify the
  14-test subset because the test-runner used the full `cargo test -p
  ui` rather than the narrower filter, but the umbrella green is
  strictly stronger evidence and there are zero `*.snap.new` files).

All three ticks PASS the citation contract. No untick required.

## Open questions resolved

### Q1 — Brief size discrepancy (test-runner reported ~36k tokens; orchestrator regen reported ~19k)
The discrepancy is real and worth surfacing to the spec-auditor as a
follow-up. The evaluator did not consume the brief and cannot
authoritatively reconcile the numbers, but a ~2× delta between
producer-claimed and consumer-measured size is exactly the signal
spec-auditor exists to investigate. **Not a verdict-blocker.**
Recommendation: file a spec-auditor task to split or trim the
iced-aw-cherry-pick brief in the next pass, and to instrument the
spec-brief skill to emit a consumer-side token count for parity.

### Q2 — `reports/` dir was created via `mkdir -p`
The spec-update SKILL.md exclusion list does not cover `reports/*.log`
files (skill governs `feature.md`, `tasks.md`, structured `.md` reports,
ADR registry — not raw logs). The `mkdir -p` and direct file landing of
the `.log` artifact is consistent with the test-runner contract and is
**NOT a verdict-blocker** or a spec-update bypass.

### Q3 — `## exit:` capture count
`grep -c "^## exit:"` returned **14**, not 13. The discrepancy is
benign: the test-runner numbered 13 commands but step 7 (the two-run
determinism gate) is structurally a *command + a sub-command*: the
second `cargo test -p ui` (with its `## exit: 0` at log:960) and the
follow-up `find crates/ui -name *.snap.new` (with its `## exit: 0` at
log:963). Both are required for the H-arch-9 hard gate and both have
clean exit lines. The log is **NOT corrupted**; the 14-block count is
correct accounting of `<command, exit>` pairs (the test-runner narrated
13 logical steps but step 7 is naturally two shell invocations). Verdict
not affected.

### Q4 — Clippy failures (step 9) confined to documented pre-existing list
Verified by extracting all six `error:` lines and their `-->` follow-ups:

| Error line | File:line | In pre-existing list? |
|---|---|---|
| log:1344 | `crates/ui/src/widgets/chart.rs:1347` | yes (chart.rs) |
| log:1358 | `crates/ui/src/widgets/chart.rs:1352` | yes (chart.rs) |
| log:1367 | `crates/ui/src/widgets/chart.rs:1356` | yes (chart.rs) |
| log:1376 | `crates/ui/src/widgets/chart.rs:1406` | yes (chart.rs) |
| log:1388 | `crates/ui/src/widgets/chart.rs:1417` | yes (chart.rs) |
| log:1413 | `crates/ui/src/window_icon.rs:151` | yes (window_icon.rs) |

All six errors are `clippy::expect_used` (`-D` in `lib.rs:23` per
log:1353). Zero net-new clippy errors. The clippy step's `## exit: 101`
is therefore the pre-existing baseline, not a Brief B regression.

## Architect hypothesis verdicts cross-checked

- **H-arch-4** (iced_aw feature-flag isolation, RESOLVED-PASS in
  feature.md): supported by log:1490-1530 — `cargo tree` shows
  `iced_aw v0.14.1` plus the three predicted transitive crates and no
  surprise additions.
- **H-arch-9** (iced_aw::spinner deterministic render): supported by
  log:962-963 (`*.snap.new` find empty) and log:1534 (`CLOCKS PASS`).
- **H-arch-10** (iced_aw::badge catalog + StyleFn hook): supported
  indirectly via the panel-snapshots strategies tests landing green
  (log:458 `69 passed`) — the badge swap does not regress the
  text-summary strategies snapshots (architectural divergence T-M3-3
  consistent with zero `*.snap.new`).

All three hypotheses stay UNFALSIFIED in the developer pass.

## Verdict

**VERDICT → PASS.** All 10 verdict-matrix criteria green. Two-run
determinism gate (H-arch-9 hard gate) green: 267 → 267 with zero
`*.snap.new` files. Anchors diff empty (REGRESSION not triggered).
Clippy errors confined to the developer's documented pre-existing list
(`chart.rs` + `window_icon.rs`). Three honest-tick spot-checks all
verified. Trace.toml columns filled for all three REQ rows. The
~36k-vs-~19k brief-size discrepancy is surfaced as a spec-auditor
follow-up but is explicitly **not** a verdict-blocker.

Routing: **HANDOFF → presenter** for the
`spec/iced-aw-cherry-pick/presentations/iced-aw-cherry-pick-2026-05-14.md`
assembly (T-M_FINAL-3). The orchestrator (or a follow-up tester pass)
owns ticking `T-M_FINAL-1` and `T-M_FINAL-2`; the evaluator does not
tick.

```toml
[handoff]
from        = "tester-evaluator"
to          = "presenter"
feature     = "iced-aw-cherry-pick"
trace_refs  = ["REQ-ICED-AW-001", "REQ-ICED-AW-002", "REQ-ICED-AW-003"]
verdict     = "PASS"
priority    = "P1"

[inputs]
brief       = "inline"
artifacts   = [
  "spec/iced-aw-cherry-pick/reports/test-run-2026-05-14T07-13Z.log",
  "spec/iced-aw-cherry-pick/tasks.md",
  "spec/iced-aw-cherry-pick/feature.md",
  "spec/trace.toml",
  "AGENT.md",
]

[outputs]
spec_files  = ["spec/iced-aw-cherry-pick/reports/evaluation-2026-05-14T07-13Z.md"]
adrs_added  = []

[open_questions]
items = [
  "Spec-auditor follow-up: the iced-aw-cherry-pick brief reads ~36k tokens at test-runner consumption vs ~19k at orchestrator regen — investigate the ~2x delta and consider a split or trim in the next pass.",
  "Pre-existing clippy expect_used denials in chart.rs + window_icon.rs (6 errors) and pre-existing rustdoc broken-intra-doc-link warnings (6) remain unaddressed across the workspace; not in Brief B scope but worth a clean-up brief.",
]

[assumptions]
items = [
  "The 6 clippy errors at chart.rs + window_icon.rs match the developer's documented pre-existing baseline; the orchestrator brief stated this list explicitly and no net-new errors landed in any Brief B-touched file.",
  "The find … -name *.snap.new step (log:962-963) returning empty body confirms zero new snapshot files; the H-arch-9 hard gate treats that empty body as the green signal.",
  "Test-runner's per-step accounting of 13 logical commands is consistent with grep -c returning 14 exit lines (step 7 is naturally a command + follow-up find sub-command).",
]
```
