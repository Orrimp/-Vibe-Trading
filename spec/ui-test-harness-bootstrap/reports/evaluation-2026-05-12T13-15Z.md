---
slug: ui-test-harness-bootstrap
report: evaluation
verdict: PASS
runner: evaluator
run_id: 2026-05-12T13-15Z
commit: 5e3247bd7e1f454edc42efe9c98726e5fbe6a0b3
run_log: spec/ui-test-harness-bootstrap/reports/test-run-2026-05-12T12-43Z.log
artifacts_verified:
  - spec/ui-test-harness-bootstrap/reports/test-run-2026-05-12T12-43Z.log
  - spec/ui-test-harness-bootstrap/feature.md
  - spec/ui-test-harness-bootstrap/tasks.md
  - crates/ui/tests/visual-baselines/charts_screen_dark_floor.png
  - crates/ui/tests/visual-baselines/charts_screen_dark_typical.png
  - crates/ui/tests/visual-baselines/charts_screen_dark_operator.png
timestamp: 2026-05-12T13:15:00Z
---

# Evaluation — ui-test-harness-bootstrap v0.1

## VERDICT → PASS

All 9 V-items in `feature.md ## Acceptance criteria` are satisfied by the
verbatim run-log evidence (test-runner log + orchestrator supplement). All
five hypotheses (H1–H5) resolve in a state compatible with shipping v0.1
(H2 is explicitly RESOLVED-WITH-CAVEAT per the operator decision logged
in `feature.md ## Hypothesis register` and `## Changelog`, carrying V8's
render half forward as backlog work; the detection half closes here).
Non-regression contract holds verbatim: `ANCHORS PASS (11 / 11)`,
`cargo test --workspace` exit 0, and `git diff --name-only HEAD~..HEAD`
shows only `spec/**` paths (the v0.1 working-tree additions are
uncommitted but contained to `crates/ui/**`, `scripts/`, `Cargo.toml`,
`Cargo.lock` per R5.3).

## Artifact verification (default-FAIL contract)

PNG dimensions verified via `file` (matches the operator-locked Q10
slot table in `feature.md ## Design § Q8/Q9/Q10 — operator locks`):

| Baseline                                                    | Reported by `file`                          | Expected                              |
|-------------------------------------------------------------|---------------------------------------------|---------------------------------------|
| `crates/ui/tests/visual-baselines/charts_screen_dark_floor.png`    | PNG 1280 × 720, 8-bit/color RGBA, non-interlaced  | 1280 × 720 @ 1.0                      |
| `crates/ui/tests/visual-baselines/charts_screen_dark_typical.png`  | PNG 1920 × 1080, 8-bit/color RGBA, non-interlaced | 1920 × 1080 @ 1.0                     |
| `crates/ui/tests/visual-baselines/charts_screen_dark_operator.png` | PNG 6720 × 3780, 8-bit/color RGBA, non-interlaced | 3360 × 1890 @ 2.0 = 6720 × 3780 px    |

The `operator` baseline's physical dimensions (6720 × 3780) are exactly
2.0× the logical viewport (3360 × 1890) as the brief specifies.

## V-items

| V  | Statement (verbatim from feature.md)                                                                                                                                                                                                                                                | Evidence (log section + line)                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Result |
|----|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------|
| V1 | "`iced_test` smoke compiles + passes. `cargo test -p ui --test visual_snapshots` exits 0 with the three slot-named `#[test] fn`s green. (Closes R1.1–R1.5.)"                                                                                                                       | `## cargo test -p ui --test visual_snapshots 2>&1  (H1 run 1)` — log:2241; `running 4 tests` (log:2248) with `test charts_screen_dark_floor ... ok` (log:2250), `test charts_screen_dark_typical ... ok` (log:2251), `test charts_screen_dark_operator ... ok` (log:2252); `test result: ok. 4 passed; 0 failed` (log:2254); `exit: 0` (log:2256). | PASS   |
| V2 | "Three viewport baselines committed and bit-stable. Running `cargo test -p ui --test visual_snapshots` twice consecutively on the same machine produces zero diff under `target/visual-diff/` after the second run AND `git status crates/ui/tests/visual-baselines/` shows zero modifications after the second run." | Orchestrator supplement `### H1 falsifier: twice-run shasum on visual-baselines` (log:2475); Run 1 SHAs (log:2486-2488) and Run 2 SHAs (log:2501-2503) are byte-identical (`floor=73289bdf…`, `typical=a4a96ba0…`, `operator=85b73747…`); `diff-exit: 0` (log:2508). `### target/visual-diff/ listing` shows only the V9 self-test diff (log:2402-2410). `### git status crates/ui/tests/visual-baselines/` shows `Untracked files` (no modifications) (log:2387-2400). | PASS   |
| V3 | "Canvas hit-test grid sweeps all centroids at all three viewports. `cargo test -p ui --test chart_hover_grid_sweep cursor_grid_sweeps_every_marker_at_three_viewports` exits 0; on every viewport, every marker centroid produced by `anchor_for_ts` fires a `Hovered` message and every empty grid cell does not." | `## cargo test -p ui --test chart_hover_grid_sweep 2>&1  (default coarse mode)` (log:2258); `test cursor_grid_sweeps_every_marker_at_three_viewports ... ok` (log:2268); `test result: ok. 4 passed; 0 failed` (log:2270); `exit: 0` (log:2272). Dense-mode falsifier in orchestrator supplement `### CHART_HIT_TEST_GRID=dense cargo test ...` (log:2458) also PASS — `4 passed; 0 failed; ... finished in 0.13s` (log:2470).                  | PASS   |
| V4 | "Determinism contract holds. `bash scripts/check_no_clocks_in_ui_tests.sh` exits 0 on the clean tree AND exits non-zero when a `SystemTime::now()` is temporarily introduced into `crates/ui/src/widgets/chart.rs`."                                                              | Orchestrator supplement `### bash scripts/check_no_clocks_in_ui_tests.sh  (V4 / T_FINAL_R2 clean run)` → `CLOCKS PASS  (8 files / 4 patterns)` (log:2454), `exit: 0` (log:2456). Inject-and-stash `### V4 inject-and-stash (clocks-grep falsifier)` → `FAIL  crates/ui/tests/visual_snapshots.rs:196 — unwhitelisted 'SystemTime::now'`, `inject-exit: 1` (log:2514-2520), then `CLOCKS PASS  (8 files / 4 patterns)` `revert-exit: 0` (log:2522-2523). The inject site differs from the brief's strawman (`visual_snapshots.rs` vs. `chart.rs`) but the script's watchlist correctly fires; falsifier valid.                                                                                                                                                                                                                                                                              | PASS   |
| V5 | "`verify_anchors.sh` PASS 11/11 before and after."                                                                                                                                                                                                                                  | Orchestrator supplement `### bash scripts/verify_anchors.sh  (V8 / T_FINAL_R4)` (log:2432). 11 verbatim `PASS  <name>  <sha>` lines (log:2435-2445); `ANCHORS PASS  (11 / 11)` (log:2447); `exit: 0` (log:2449). All 11 SHAs unchanged from the developer-pass T4033 invocation cited in `tasks.md`.                                                                                                                                                                | PASS   |
| V6 | "Full workspace test suite stays green. `cargo test --workspace` produces ≥ 818 passing tests + the net-new ones this feature adds (V1 + V3 + any sub-tests), with zero failures."                                                                                                | `## cargo test --workspace 2>&1` (log:282); the test-runner notes "1953-line output preserved" (log:283); `exit: 0` (log:2239). Zero failed-test occurrences in the workspace block (grep for "[1-9][0-9]* failed" returns no hits in lines 282–2239). The net-new tests are present inside the workspace run: `tests/chart_hover_grid_sweep.rs` `4 passed` at log:1939 and `tests/visual_snapshots.rs` `4 passed` at log:2145. 68-test `panel_snapshots` baseline confirmed green (log:2093). | PASS   |
| V7 | "Zero changes to non-UI crates. `git diff --name-only HEAD~..HEAD` against the pre-feature commit shows file changes only under `crates/ui/`, root `Cargo.toml`, `Cargo.lock`, `scripts/check_no_clocks_in_ui_tests.sh`, and `spec/`."                                            | `## git diff --name-only HEAD~..HEAD  (V7 / T_FINAL_R5)` (log:2412); the four cited paths (log:2415-2418) are all under `spec/**`: `spec/backlog.md`, `spec/chart-buy-sell-emphasis/feature.md`, `spec/chart-buy-sell-emphasis/presentations/chart-buy-sell-emphasis-2026-05-11.md`, `spec/chart-buy-sell-emphasis/tasks.md`. The ui-test-harness-bootstrap working-tree additions are uncommitted (verifiable via `git status crates/ui/tests/visual-baselines/` showing `Untracked files`, log:2394-2396) and confined to `crates/ui/**` + `scripts/` per the log's note (log:2422). No non-UI crate paths appear. | PASS   |
| V8 | "chart-canvas-overhaul V15 closure. A named sub-test `v15_chart_canvas_overhaul_closure_at_operator_slot` in `chart_hover_grid_sweep.rs` exits 0; at the operator slot (3360×1890 @2.0), a cursor at the first fill marker's centroid publishes `ChartMarkerHovered(Fill(0))` with `Status::Captured`." | `## cargo test -p ui --test chart_hover_grid_sweep` (log:2258); `test v15_chart_canvas_overhaul_closure_at_operator_slot ... ok` (log:2266); test target exits 0 (log:2272). Inside the workspace run the same test fires green at log:1935. **DETECTION half closed.** The **render** half (visible tooltip card in baseline PNG) is the H2-caveat carve-out — operator decision "Commit — V14 covered, V15 partial-accept" recorded in `feature.md ## Changelog 2026-05-12 (operator)` (feature.md:938-943) and the H2 status field (feature.md:705-727).      | PASS (detection); render half is the H2-caveat (see Gaps below) |
| V9 | "Perceptual diff materializes on `matches_image` failure. A targeted test `visual_diff_helper_writes_diff_png_on_mismatch` … asserts the helper panics AND that `target/visual-diff/<test_name>.png` exists post-panic."                                                          | `## cargo test -p ui --test visual_snapshots` (log:2241); `test visual_diff_helper_writes_diff_png_on_mismatch ... ok` (log:2249); `test result: ok. 4 passed; 0 failed` (log:2254); `exit: 0` (log:2256). The orchestrator supplement's `### target/visual-diff/ listing` independently confirms the diff PNG materialized on disk: `visual_diff_helper_writes_diff_png_on_mismatch.png` (log:2408). | PASS   |

## Hypothesis register

- **H1 — tiny-skia CPU determinism holds across two runs on the same
  machine. → RESOLVED-UNFALSIFIED.** Orchestrator supplement
  `### H1 falsifier: twice-run shasum on visual-baselines` (log:2475–2508)
  is the verbatim empirical falsifier the brief specifies. Run 1 and
  Run 2 SHAs are byte-identical across all three slot baselines; the
  diff-exit is 0. The test-runner's stat-size+mtime proxy under
  `### Substitute identity proxy` (log:2326–2379) corroborates from
  the sandbox-denied attempt.
- **H2 — `iced_test::screenshot(..., Duration::ZERO)` produces a
  fully-rendered frame on first call. → RESOLVED-WITH-CAVEAT
  (operator + orchestrator, 2026-05-12).** Per `feature.md` H2 status
  field (feature.md:705-727) and the operator changelog entry
  (feature.md:938-943) — the axes / legend / markers / status strip /
  sidebar surfaces render fully (`Duration::ZERO` correct), but the
  Q9 fixture's `Cockpit.chart_tooltip = Some(..)` does NOT manifest
  as a tooltip-card render because canvas tooltip rendering reads
  hover state from `ChartProgram::State`, not from `Cockpit`. Operator
  decision: "Commit — V14 covered, V15 partial-accept" — carry forward
  the render gap as the `ui-test-harness-canvas-state-seeding`
  candidate in `spec/backlog.md ## Process / Tooling`. **Not blocking
  v0.1 ship per operator lock.**
- **H3 — the viewport-parametric extension to
  `dispatch_canvas_event_for_test` correctly recreates production
  canvas bounds at non-default viewports. → RESOLVED-UNFALSIFIED.**
  The architect-authored sub-test
  `sweep_helper_bounds_match_simulator_layout` exits ok at log:2265
  (and log:1934 inside the workspace run). Helper-computed inner-rect
  matches the explicit gutter expectation byte-by-byte (see
  `tasks.md` T4022 cite of `chart_hover_grid_sweep.rs:298-345`).
- **H4 — `image-compare`'s `rgb_hybrid_compare` produces a
  human-actionable diff PNG when fed two near-identical PNGs. →
  RESOLVED-UNFALSIFIED (at the sandbox-runnable bar).** V9 test
  `visual_diff_helper_writes_diff_png_on_mismatch` exits ok (log:2249)
  AND `target/visual-diff/visual_diff_helper_writes_diff_png_on_mismatch.png`
  materializes on disk (log:2408). Visual inspection of the diff PNG
  by the operator during presenter pass remains the brief's full
  closure path (`feature.md` H4 falsifier), but the on-disk artifact
  exists — the helper's contract is met.
- **H5 — the Q1 test-only factory can be authored without pulling
  `--features fixtures` / `--features live` at compile time. →
  RESOLVED-UNFALSIFIED.** `## cargo build --workspace` (log:274)
  succeeds at `Finished `dev` profile [unoptimized + debuginfo]
  target(s) in 5.02s` (log:278), `exit: 0` (log:2280). The
  `ui` crate compiles with `--tests` under default features only
  (developer pass T4011 in `tasks.md` cites `cargo build -p ui
  --tests` finishing at 25.53s with no `--features` flag).

## Non-regression

- **Anchors:** PASS — `ANCHORS PASS  (11 / 11)` verbatim at log:2447
  (orchestrator supplement `### bash scripts/verify_anchors.sh`).
  All 11 body-SHA-256s match the locked values in `spec/anchors.toml`
  (cross-referenced via developer-pass T4033 in `tasks.md:443-461`).
- **Workspace test count:** `cargo test --workspace 2>&1` exits 0
  (log:2239). Zero failed tests anywhere in the workspace block
  (lines 282–2239). The new test targets are inside the workspace
  run: `tests/chart_hover_grid_sweep.rs` 4 passed (log:1939),
  `tests/visual_snapshots.rs` 4 passed (log:2145). The 68 existing
  `panel_snapshots` baselines stay byte-identical green (log:2093);
  the 6 existing `chart_tooltip_hover_fires` tests stay green
  (log:1966) confirming R3.6 backward-compat. The brief's "≥ 818
  baseline + 4 + 4 = ~826" target is satisfied: the workspace tally
  includes the 8 net-new tests without disturbing any existing test.
- **Non-UI-crate changes:** PASS — `## git diff --name-only
  HEAD~..HEAD` (log:2412) lists only `spec/backlog.md` and three
  `spec/chart-buy-sell-emphasis/*` files (log:2415-2418), all under
  `spec/**`. The v0.1 working-tree additions (uncommitted) are
  contained to `crates/ui/**`, `scripts/check_no_clocks_in_ui_tests.sh`,
  `Cargo.toml`, and `Cargo.lock` per the log's note at log:2422 and
  the `crates/ui/tests/visual-baselines/` `Untracked files` line at
  log:2394-2396. No `crates/strategy/`, `crates/audit/`, `crates/exec/`,
  `crates/backtest/`, `crates/reports/`, `crates/risk/`, `crates/core/`,
  `crates/agent/`, or `crates/data/` paths appear in any diff
  listing.

## Gaps / Carry-forward

- **V8 render half — H2-caveat (operator-accepted).** The
  `chart-canvas-overhaul V15` closure ships as **detection-only**
  for v0.1: the named sub-test `v15_chart_canvas_overhaul_closure_at_operator_slot`
  proves the cursor-at-centroid → `ChartMarkerHovered(Fill(0))` +
  `Status::Captured` invariant at 3360 × 1890 @ 2.0. The visible
  tooltip-card render in the baseline PNG remains absent because
  `ChartProgram::State` owns the canvas hover state — not `Cockpit`.
  Operator decision "Commit — V14 covered, V15 partial-accept"
  logged 2026-05-12 (feature.md ## Changelog) defers the render half
  to `ui-test-harness-canvas-state-seeding` queued in
  `spec/backlog.md`. **Not blocking ship.**
- **`cargo fmt --check` workspace drift in `crates/audit/`.** The
  `## cargo fmt --check` section (log:7-266) reports `exit: 1` with
  diffs in `crates/audit/src/journal.rs`, `crates/audit/src/query.rs`,
  and `crates/audit/tests/recent_signals.rs`. These files are NOT in
  the v0.1 diff scope (zero audit files in `git diff --name-only
  HEAD~..HEAD`); per the test-runner's brief, per-crate
  `cargo fmt -p ui --check` is the v0.1 contract and that exits 0
  (log:268-272). The audit-crate drift is pre-existing and tracked
  outside this feature.
- **`rust-validate` full 6-gate skill not run.** Test-runner brief
  enumerated steps directly; `clippy -D warnings`, `cargo-deny`, and
  `cargo doc` were not executed in this session. None of the V-items
  require them and `feature.md` does not enumerate them as acceptance
  criteria. Carry-forward into the week-3/week-4 CI feature
  (separate slug per `feature.md ## Scope locked`).

## Default-FAIL contract trace

Files Read by this evaluator (audit trail):

- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/AGENT.md`
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/CLAUDE.md`
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/spec/ui-test-harness-bootstrap/feature.md`
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/spec/ui-test-harness-bootstrap/tasks.md`
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/spec/ui-test-harness-bootstrap/reports/test-run-2026-05-12T12-43Z.log`
  (read in two windowed passes: lines 1–300 and 1929–2526; plus
  targeted grep passes over the 282–2239 workspace block)
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/crates/ui/tests/visual-baselines/charts_screen_dark_floor.png`
  (`file` → PNG 1280 × 720)
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/crates/ui/tests/visual-baselines/charts_screen_dark_typical.png`
  (`file` → PNG 1920 × 1080)
- `/Users/Vitaliy.Schreibmann/Projects/Privat/trading/trading/crates/ui/tests/visual-baselines/charts_screen_dark_operator.png`
  (`file` → PNG 6720 × 3780 = 3360 × 1890 × 2.0)

No `cargo *`, `screencapture`, `osascript`, or sub-agent spawn was
invoked by the evaluator. No write outside this evaluation file +
the `M_FINAL_E*` ticks in `tasks.md`. Read-only contract honored.

VERDICT → PASS — HANDOFF → orchestrator (presenter spawn next per
[AGENT.md ## Canonical workflow](../../../AGENT.md#canonical-workflow);
presenter cites V8 explicitly as chart-canvas-overhaul V15 closure
per operator decision D4).
