# Tasks

One file per feature slug, e.g. `spec/tasks/btc-momentum-v1.md`.

Owned by the architect (initial breakdown) then the developer (execution). Use
checkbox lists so progress is scannable at a glance. Each task must include a
one-line acceptance criterion so the tester knows what "done" means.
