---
slug: cockpit-render-regression
kind: evaluation
ts: 2026-05-14T17-15Z
evaluator: read-only fresh context
log_ref: spec/cockpit-render-regression/reports/test-run-2026-05-14T17-15Z.log
log_sha256: 1d7a305a6e3f89673906072cee22407861db08099252413038301ef4170dc847
verdict: PASS
trace_refs: ["REQ-COCKPIT-PANIC-001"]
---

# Evaluation — F1 fix (cockpit-render-regression M0-FIX-1)

Read-only evaluator pass. Sole input: the raw test-run log + cited
tasks.md / trace.toml / feature.md sections. No source-diff inspection
performed (per AGENT.md test-runner / evaluator split).

The log is 1 568 lines, 13 commands, 15 `## exit:` blocks (cmd 7 splits
into cargo-test + find sub-step; cmd 12 splits into `ls` + `grep -c`
sub-step). All splits accounted for.

## Verdict matrix

| # | Criterion                                                                                | Result | Cite                                                                                  |
| - | ---------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------- |
| 1 | Compile gates green (cmds 2-5 cargo build x4)                                            | PASS   | log L4-L53; all four `## exit: 0`                                                     |
| 2 | Test suite green; N >= 267 (cmd 6)                                                       | PASS   | log L495 `## exit: 0`; sum of 23 `test result: ok` lines L248-L493 = **267 passed, 0 failed** |
| 3 | Two-run determinism: cmd-6 N == cmd-7 N AND zero `*.snap.new` files                      | PASS   | run-2 sum L690-L935 = **267 passed**; cmd-7 `find ... -name *.snap.new` L939-L940 emitted no output, exit 0 |
| 4 | Fmt clean (cmd 1)                                                                        | PASS   | log L1-L2 `cargo fmt -p ui --check` `## exit: 0`                                      |
| 5 | Clippy: zero NET-NEW errors in F1-touched files (theme.rs / widgets/strategies.rs)       | PASS   | 6 errors L1417-L1486, all in `widgets/chart.rs` (5) + `window_icon.rs` (1) — documented pre-existing; **0 errors in theme.rs or strategies.rs**; cmd 9 exit 101 permitted by criterion |
| 6 | Rustdoc: zero NET-NEW warnings on F1-touched files (cmd 8)                               | PASS   | 6 rustdoc warnings L943-L985 cite `chart_tooltip.rs`, `volume_histogram.rs`, `window_icon.rs`, `test_support.rs` — all unrelated to F1; theme.rs / strategies.rs absent |
| 7 | Clocks-determinism gate green (cmd 10)                                                   | PASS   | log L1549 `CLOCKS PASS  (8 files / 4 patterns)`, L1550 `## exit: 0`                   |
| 8 | Anchor diff empty (cmd 11)                                                               | PASS   | log L1552-L1553 `git diff --stat HEAD spec/anchors.toml` produced **no output**, exit 0 |
| 9 | Cockpit smoke clean — cmd 12 grep returns 0                                              | PASS   | log L1559-L1561 `grep -c 'panicked at\|non-unwinding panic' /tmp/cockpit-postrefactor.log` printed **`0`** (exit 1 is grep's standard "no matches" signal with `-c`, not a failure of the criterion which asks for the printed count) |
| 10 | Named constant landed in both files (cmd 13)                                            | PASS   | log L1563-L1568 shows `theme.rs:619 pub const STRATEGY_RULE_HEIGHT_PX: f32 = 24.0` (definition) + **3 hits in `strategies.rs` (L222 comment, L228 + L231 use sites)** — exceeds developer's note of "2 hits" but the use-site count of 2 holds; criterion satisfied |
| 11 | Trace.toml columns filled for REQ-COCKPIT-PANIC-001                                     | PASS   | `spec/trace.toml` L350-L369: `crates = [crates/ui/src/widgets/strategies.rs, crates/ui/src/theme.rs]`; `tests` = 4 entries (panel_snapshots / home_strategies_row_cross_link / strategies_screen_sparkline_replaces_placeholder / visual_snapshots) — both non-empty |
| 12 | Honest ticks — T-FIX-1 and T-M0-FIX-VERIFY each cite (a) file:line, (b) test cmd, (c) test-output line | PASS   | tasks.md L148-L160 (T-FIX-1) + L322-L350 (T-M0-FIX-VERIFY) — both provide (a)/(b)/(c). T-M0-FIX-VERIFY's (b)+(c) cite orchestrator's `/tmp/cockpit-f1-falsifier.log` which is permitted per AGENT.md (cargo-run is orchestrator-only) |

12 / 12 PASS.

## Test counts

| Run                            | N   | Source                                                       |
| ------------------------------ | --- | ------------------------------------------------------------ |
| cmd 6 (`cargo test -p ui` #1)  | 267 | Sum of 23 `test result: ok` lines L248-L493                  |
| cmd 7 (`cargo test -p ui` #2)  | 267 | Sum of 23 `test result: ok` lines L690-L935                  |
| Delta                          | 0   | No regression; deterministic across two back-to-back runs    |

Breakdown (identical both runs): lib 154 + 22 integration tests (4, 0,
0, 4, 2, 1, 6, 7, 0, 2, 2, 0, 0, 0, 69, 0, 3, 0, 8, 1, 4) + 0 doc-tests
= **267 passed; 0 failed; 0 ignored**.

## Cockpit smoke

Verbatim from log L1555-L1561:

```
## ls -la /tmp/cockpit-postrefactor.log
-rw-r--r--  1 Vitaliy.Schreibmann  wheel  108 May 14 19:14 /tmp/cockpit-postrefactor.log
## exit: 0

## grep -c panicked at|non-unwinding panic /tmp/cockpit-postrefactor.log
0
## exit: 1
```

Panic count: **0**. The post-refactor orchestrator-produced
`/tmp/cockpit-postrefactor.log` (108 bytes, generated 19:14 on
2026-05-14) contains zero matches for `panicked at` or
`non-unwinding panic`. Exit code 1 from `grep -c` is the standard "no
matches found" signal — NOT a criterion failure. The criterion text
explicitly anchors on the printed count, which is `0`.

Note: tasks.md T-M0-FIX-VERIFY cites a different orchestrator log
(`/tmp/cockpit-f1-falsifier.log`) but with the same `0` panic count.
Two independent orchestrator-produced cockpit-smoke logs both confirm
the panic is gone. The discrepancy in path is not a contract violation
— both are orchestrator-only per AGENT.md ## Capability boundaries and
both report `panic count = 0`.

## Honest-tick spot-check

**T-FIX-1** (tasks.md L127-L172, marked `[x]`):

- (a) file:line: `crates/ui/src/theme.rs:619` + `crates/ui/src/widgets/strategies.rs:228` + `:231`. **Verified against cmd 13 grep:** exact line numbers and constant name match (`STRATEGY_RULE_HEIGHT_PX` at theme.rs:619; use sites at strategies.rs:228 and :231). PASS.
- (b) test command: `cargo build -p ui --bin cockpit --features fixtures` + `cargo run` + `grep -c 'panicked at' /tmp/cockpit-f1-falsifier.log`. Cited as orchestrator-only per AGENT.md. **Cmd 4 in the log (`cargo build -p ui --bin cockpit --features fixtures`) exited 0** at L46-L48 — the buildable part is corroborated by this evaluator-visible log. The `cargo run` part is orchestrator-only and properly attributed. PASS.
- (c) test-output line: `panic count: 0`. Cmd 12 in this log corroborates with the post-refactor counterpart log (`/tmp/cockpit-postrefactor.log` → `0`). PASS.

**T-M0-FIX-VERIFY** (tasks.md L320-L359, marked `[x]`):

- (a) file:line: same three citations as T-FIX-1. **Verified against cmd 13 grep.** PASS.
- (b) test commands: `cargo build -p ui` + `cargo fmt -p ui --check` + `cargo test -p ui` + `cargo clippy -p ui --no-deps --lib --tests` + `cargo doc -p ui --no-deps`. **All five appear in this log as cmds 2/1/6/9/8** — exact commands. PASS.
- (c) test-output lines: claims "267 tests pass, 0 fail (identical to the pre-fix baseline)". **Verified: both runs in this log report 267 passed, 0 failed.** Clippy citation matches the actual log breakdown (6 errors confined to chart.rs + window_icon.rs; theme.rs and strategies.rs clean). PASS.

Both ticks are honest. No spot-check concerns.

## Notable but non-blocking observations

These are NOT verdict-blockers — surfaced per evaluator discipline:

1. **Pre-existing clippy errors will block any future `clippy -D warnings` gate.** 6 errors in `widgets/chart.rs` + `window_icon.rs` (all `expect_used` / `unwrap_used` violations) currently fail clippy with exit 101. This is documented as pre-existing in tasks.md L341-L344 and is out of scope for F1 (no NET-NEW errors introduced in F1-touched files). M1's quality-gate overhaul should address.
2. **Pre-existing rustdoc broken-intra-doc-links (6 warnings).** Same disposition — pre-existing, unrelated to F1, queued for M1.
3. **5 unused-import warnings** in `tests/strategies_screen_sparkline_replaces_placeholder.rs`. Pre-existing; orthogonal to F1.
4. **Spec hygiene:** `spec/cockpit-render-regression/reports/` directory was created on the fly by the test-runner (per the prior pass's open-question), and the log itself + this evaluation now reside under it. Per spec-update SKILL exclusions, log writes here are out of contract — NOT a violation.
5. **Cmd 13 finds 3 strategies.rs hits, not 2** — line 222 is a doc-comment reference, lines 228 + 231 are the two use sites. The developer's note ("expect 2 hits there") was conservative; actual hit count (2 use sites + 1 doc reference) still satisfies the criterion.

## Verdict

**VERDICT → PASS.** All 12 criteria green. F1 (`Length::Fixed(layout::STRATEGY_RULE_HEIGHT_PX)` swap + named constant in `theme.rs`) compiles clean across all four binary configurations, leaves 267/267 tests passing deterministically across two consecutive runs, introduces zero NET-NEW clippy or rustdoc warnings in the F1-touched files, leaves `spec/anchors.toml` untouched, and the orchestrator-produced cockpit-smoke log shows zero panics post-refactor (matching the pre-developer-refactor orchestrator falsifier log). The named constant landed in both `theme.rs` (definition at L619) and `strategies.rs` (use sites at L228 and L231). Trace row REQ-COCKPIT-PANIC-001 has both `crates` and `tests` columns filled. Both T-FIX-1 and T-M0-FIX-VERIFY ticks carry honest (a)/(b)/(c) citations that reconcile against this log.

Routing: PASS → presenter (assemble operator approval block per AGENT.md verdict matrix).
