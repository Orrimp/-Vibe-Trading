---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-05-25
---

# Spec Audit — 2026-05-25

## Headline

The spec tree is **healthier** than at 2026-05-22: lint dropped from 82
violations in 2 categories to **61 in 1 category** (dead-link only;
`shipped-no-tests` cleared), and every P1 from the last audit is resolved —
the ADR-0035 number collision is gone (one canonical 0035 file), the
v0/v05/v1 trace-state mismatches are flipped to `shipped`, the
`v25-tcn-overlay` parent landed as `shipped` (Option A), and `product.md`
now carries the DL-programme retirement narrative. The new drift this week
is three **trace-state vs feature.md mismatches** introduced by the v3
volatility-forecaster retirement (trace still says `shipped`, feature.md
says `retired`) plus the in-flight `lab-end-to-end-v2`. Anchors are in
perfect balance (34 declared = 34 cited, zero diff both directions).

## Run metadata

- Date: 2026-05-25 (Monday weekly cadence).
- Auditor: spec-auditor (scheduled run; executed directly, not delegated).
- Baseline: `spec/dev-notes/audit-2026-05-22.md` (3 days prior).
- Python: sandbox `python3` is **3.10.12** — `tomllib` is NOT available
  natively (it is stdlib only from 3.11+). The previous audit ran on 3.14.
  To run `spec_lint.py` unmodified this run, I installed `tomli` and
  exposed it via a `PYTHONPATH` shim module `tomllib.py` (`from tomli import *`).
  No repo file was modified to achieve this; the shim lives outside the repo.
  Auditor note for operator: the lint's PEP-723 header requires 3.11+; the
  CI/host that runs this weekly should pin a 3.11+ interpreter or the lint
  will hard-fail with `ModuleNotFoundError: No module named 'tomllib'`.
- Commits since last audit (since 2026-05-22 06:02): **53**. Dominated by
  two new lab features (`lab-yahoo-realdata` shipped, `lab-end-to-end-v2`
  in flight), the `v3-llm-forecaster` shipped-partial sprint, and the
  `v3-volatility-forecaster` retire/noop-fix triplet.

## Mechanical lint

`scripts/spec_lint.py --all` → `spec-lint: FAIL (61 violations in 1 categories)`.
Exit code **1** (the script returns the count of failing categories; 1 category
flagged → exit 1; previous run flagged 2 categories → exit 2).

| Category             | This run | Previous (2026-05-22) | Δ        |
|----------------------|----------|-----------------------|----------|
| dead-link            | 61       | 81                    | **−20**  |
| missing-frontmatter  | 0        | 0                     | 0        |
| trace-broken-path    | 0        | 0                     | 0        |
| shipped-no-tests     | 0        | 1                     | **−1**   |
| **TOTAL**            | **61**   | **82**                | **−21**  |

### Notable movements

- **dead-link −20**: the partial dead-link sweep landed this week
  (`b4e930c chore(cleanup): P1.1 partial dead-link sweep (145→63)`) knocked
  out a cluster. The commit message references an internal 145→63 count; the
  lint's own count moved 81→61. Remaining 61 are the same three pre-existing
  clusters flagged last audit (ADR-0027 Kronos slug, `/tmp/` screenshot
  artefacts, stale report cross-links) plus a few new ones from this sprint's
  reports (see sample below). None are regressions in shipped strategy code.
- **shipped-no-tests cleared (−1)**: `v25-tcn-horizon-bump-or-retire` no
  longer flags — it now has report coverage, so the last audit's P2
  decision-record gap is resolved.

### Sample violations (top dead-link offenders this run)

- `spec/architecture/adr/0027-kronos-onnx-tract-integration.md` → 5 refs to
  the removed `../../v25-kronos-forecast-overlay/feature.md` slug (Kronos was
  dropped for candle; superseded ADR never updated). **Carry-over** from
  2026-05-22.
- `spec/chart-canvas-overhaul/feature.md` → 6 refs to `/tmp/orch-diag/cockpit-*.png`
  (uncommitted screenshot artefacts). **Carry-over.**
- `spec/journal-transactions-metadata/feature.md` + `tasks.md` → 4 refs to a
  `test-2026-05-03-1608-journal-transactions-metadata-final.md` report that
  does not exist at the cited relative path. **Carry-over.**
- `spec/v0-paper-sma/reports/screenshots/README.md` → 6 refs to legacy
  `../../features/…` and `../../tasks/…` paths (old flat layout pre-`spec/<slug>/`).
  **Carry-over.**
- `spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-20260522.md`
  → dead-link to `../architecture/adr/0038-vol-forecast-verdict-shape.md#…`
  (one extra `../` walks out of `spec/`). **NEW** this sprint. NOTE: this is
  an **anchored report file** — per CLAUDE.md non-negotiable and ADR-0038 § D6,
  these are byte-immutable; a raw link-fix would mutate the body-SHA and break
  the regression gate. Any fix must route through the § D6.b re-emission
  protocol, not a mechanical edit.
- `spec/v3-llm-forecaster/feature.md` + `decomp.md` → 3 refs to
  `../../crates/llm/tests/fixtures/` and `crates/reflection/src/lib.rs:69`
  (crate paths not yet materialised; the workspace is still mid-`crates/`
  migration). **NEW** this sprint, expected pattern.
- `spec/lab-yahoo-realdata/dev-notes/v0_1_1-fetch-plan-2026-05-25.md` →
  `../../architecture/adr/0040-yahoo-realdata-cache-and-revision-pin.md`
  (ADR-0040 referenced but the relative path resolves wrong — likely
  `../../../architecture/...` from a `dev-notes/` subdir). **NEW** this sprint.

## Feature inventory (64 feature.md files)

Excludes `design/`, `dev-notes/`, `runbooks/`, `archive/`, `architecture/`.
Includes the 6 `lumen-design-adoption/phase-*` sub-features.

### By status

| Status          | Count | Δ vs 2026-05-22 | Notable new/changed entrants                                   |
|-----------------|-------|-----------------|----------------------------------------------------------------|
| shipped         | 50    | +13             | lab-yahoo-realdata; v3-volatility-forecaster-noop-fix; v25-tcn-overlay (was in-progress → shipped); v25-tcn-recalibrate/-threshold/-horizon; v25a-patchtst |
| deprecated      | 4     | 0               | _probe_lint_test, v25-dl-forecast-overlay, v25b-transformer-overlay, v26-forecast-bakeoff |
| retired         | 2     | +2 (new status) | v3-volatility-forecaster, v3-volatility-forecaster-rebaseline  |
| shipped-partial | 1     | +1 (new status) | v3-llm-forecaster (first-of-kind shipped-partial precedent)    |
| draft           | 2     | +1              | ui-gallery-table-cell, v3-regime-classifier                    |
| candidate       | 2     | 0               | cockpit-app-bundle, iced-ecosystem-evaluation                  |
| proposed        | 1     | +1 (new status) | lab-end-to-end-v2                                              |
| roadmap         | 1     | 0               | lumen-design-adoption                                          |
| reserved        | 1     | 0               | lumen-phase-6-assistant-slot                                  |

Total: 64 (the prior audit counted 48 top-level + 6 lumen sub = 54; the count
grew with the v3 lane + lab features and because this run enumerates all
`feature.md` under `spec/*/` and `spec/*/*/` uniformly).

Two new status vocabularies appeared this sprint: **`retired`** (distinct from
`deprecated` — "shipped then invalidated with real evidence") and
**`shipped-partial`** (analytical phase shipped, empirical phase paused). Both
are operator-blessed precedents documented in their feature frontmatter. The
spec-lint and trace contract should be checked to confirm they accept these
two new states without flagging (lint currently does NOT flag them, so the
grammar is tolerant — confirm this is intended, not accidental).

## Stale features

**`status: proposed` or `in-progress`, updated > 30 days ago:** **NONE.**

The only `proposed` feature is `lab-end-to-end-v2` (updated 2026-05-24, 1 day).
There are no `in-progress` feature.md files at all this run — `v25-tcn-overlay`,
the last audit's lone persistent `in-progress`, was promoted to `shipped`
2026-05-22 (operator chose Option A from the prior audit's parent-status triage).

Oldest shipped features (informational, not flagged — only proposed/in-progress
are stale-gated): `v15a-mean-reversion-pairs` (2026-04-29, 26d),
`operator-success-reports` (2026-05-01, 24d), `live-cockpit-unified` /
`real-mtm-unrealized-pnl` (2026-05-02, 23d). All terminal/shipped; no action.

**`shipped` but no test report (`shipped-no-tests`):** NONE — cleared this run.

## Orphans

### Folders missing feature.md or tasks.md

- `cockpit-app-bundle` — missing `tasks.md`. Status `candidate` (pre-task
  stage); omission is expected for a candidate that has not entered the
  developer phase. No action.
- `lumen-design-adoption` — missing `tasks.md`. Status `roadmap`; this is an
  umbrella whose work is decomposed into `phase-*` sub-folders that carry
  their own task files. Omission is expected. No action.

No folder is missing `feature.md`.

### Test reports for missing slugs

None found — every `reports/` directory sits under an extant feature folder.

### Crates not mentioned in any spec

Crate-orphan check still deferred: the `crates/` virtual-workspace migration
is mid-flight (multiple dead-links this run are forward-references to
`crates/llm/…`, `crates/reflection/…`, `crates/forecast/…` paths that do not
yet exist on disk). Same rationale as 2026-05-22 — re-enable this check once
the workspace split lands.

### `_probe_lint_test/` still present

Status `deprecated`, pending delete since 2026-05-13 (12 days). The folder's
own README marks it safe to `rm -rf`. **Carry-over P3** — sandbox-blocked
delete still awaits an operator clear.

## Decay markers

`TODO | FIXME | TBD | XXX | ???` per top-level spec folder, excluding
`spec/archive/` and `spec/dev-notes/`. Decay-heavy threshold: > 5.

| Folder                                       | Count | Flag        | Δ vs 2026-05-22 |
|----------------------------------------------|-------|-------------|------------------|
| lumen-design-adoption                        | 11    | DECAY-HEAVY | 0                |
| trace.toml                                   | 8     | DECAY-HEAVY | +1               |
| cockpit-performance-and-input-responsiveness | 6     | DECAY-HEAVY | 0                |
| ui-rethink-phase-b-lab-run                   | 4     | —           | 0                |
| v3-volatility-forecaster-noop-fix            | 4     | —           | new              |
| tape-row-audit-modal                         | 3     | —           | 0                |
| v05-composed-strategies                      | 3     | —           | 0                |
| ui-rethink-phase-a-lab                       | 3     | —           | 0                |
| lab-end-to-end-v2                            | 3     | —           | new              |
| ui-headless-emulator                         | 3     | —           | 0                |
| v3-llm-forecaster                            | 2     | —           | new              |
| v15a-mean-reversion-pairs                    | 2     | —           | 0                |
| v0-paper-sma                                 | 2     | —           | 0                |
| architecture                                 | 2     | —           | +1               |
| operator-success-reports                     | 2     | —           | 0                |
| ui-quality-gate-overhaul                     | 2     | —           | 0                |
| (1-marker folders)                           | 1 ea  | —           | —                |

Three folders exceed threshold — all carry-overs, no new decay-heavy folders:

- **`lumen-design-adoption` (11, status `roadmap`).** Flat vs last audit.
  TBDs are live design questions in an active roadmap umbrella; legitimate
  open work, not stale deferral. Owner: analyst. Cleanup before phase-6
  begins.
- **`trace.toml` (8, +1).** Still the `tests = [] # TODO developer` cluster on
  early-era REQs (v0/v05/v1/v1.5b/v2 rows) written before the test-column
  convention. +1 this sprint (a new row carries the same stub). Cosmetic on
  shipped REQs. Owner: developer (batch clear).
- **`cockpit-performance-and-input-responsiveness` (6, status `shipped`).**
  Persistent post-ship Q&A TBDs. Same as last two audits. Not a regression
  risk. Owner: architect.

`v3-volatility-forecaster-noop-fix` (4, new) and `lab-end-to-end-v2` (3, new)
are just-landed/just-started features; markers are deferred-follow-on trackers,
below threshold. Watch next audit.

## Soft contradictions (LLM-judged — verify before acting)

Five cross-cutting topics checked against `spec/product.md`,
`spec/architecture.md`, the `INV-*` invariant rows in `trace.toml`, and
relevant `feature.md` files.

**1. Trace-state vs feature.md status drift — v3 volatility-forecaster retirement.**
`spec/v3-volatility-forecaster/feature.md` line 3 → `status: retired`
(with full `retired_2026_05_22:` / `disposition_2026_05_22:` provenance).
`spec/v3-volatility-forecaster-rebaseline/feature.md` line 3 → `status: retired`.
But `spec/trace.toml`:
- `REQ-V3-VOL-FORECASTER-001` → `state = "shipped"`.
- `REQ-V3-VOL-FORECASTER-REBASELINE-001` → `state = "shipped"`.
The trace.toml header contract ("Status states match feature.md frontmatter")
is violated by both. The retirement was real (operator routing R-O1 = (a)
RETIRE C1, MODEL-BROKEN / NO-ALPHA / NEGATIVE-NET-DELTA confirmed under the
real-wired overlay), so `feature.md` is the source of truth and the two trace
rows should flip `"shipped"` → `"retired"`. **Confidence: high** — direct
field mismatch. Owner: developer (two one-line edits). NOTE: `retired` is a new
trace state; confirm the lint/trace grammar accepts it (it does not currently
flag it).

**2. Trace-state vs feature.md status drift — lab-end-to-end-v2.**
`feature.md` → `status: proposed`; `trace.toml REQ-LAB-E2E-V2-001` →
`state = "in-progress"`. The feature is in active development (Wave D-3/D-4
commits this sprint), so `in-progress` is arguably the more accurate state and
`feature.md` is the lagging document here (opposite of #1). **Confidence:
medium** — this may simply be the analyst not having bumped `proposed` →
`in-progress` after the architect/developer picked it up. Owner: analyst (bump
feature.md) OR developer (align trace). Operator picks the direction.

**3. DL/RL models "(planned)" sub-section vs DL programme retirement.**
`spec/product.md` line 183 was **fixed** since last audit — it now reads
"DL forecaster portfolio (**RETIRED 2026-05-22**)" with full retrospective
links. GOOD — last audit's P2 is resolved. HOWEVER, `spec/product.md` lines
219–220 (`### DL/RL models (planned)`) still say: "Forecaster: lightweight
Transformer or TCN on multivariate features." This is the exact paradigm the
v2.5 programme empirically retired (no +0.10 Sharpe-delta at 1h/24h). The
"(planned)" forward-looking framing contradicts the line-183 retirement.
**Confidence: high** that this is a residual omission — the line-183 fix did
not propagate to the "(planned)" capability list. Owner: analyst (annotate
line 220 as "attempted & retired — see v2.5 row" or remove the forecaster
bullet). Note line 221's regime-classifier bullet ("HMM or small LSTM")
overlaps the new `v3-regime-classifier` (draft) lane — these should be
cross-linked rather than left as orphan "planned" prose.

**4. RNG seeding invariant — consistent.** `INV-DETERMINISM-RNG-001`
("All RNG must be seeded with ChaCha20 from a config-supplied seed") aligns
with `architecture.md` line 45 + ADR-0002. No opposing claim found in
product.md or any sampled feature.md. The recent `424853a fix(backtest):
F11 — mix symbol into synthetic_bars_minute seed` strengthens (does not
contradict) the determinism invariant. **No contradiction.**

**5. Money-math invariant — consistent.** `INV-MONEY-DECIMAL-001`
("Money math uses Decimal — never f64") aligns with `architecture.md`
lines 33–36 + ADR-0003 (Decimal money math, accepted 2026-04-17). No
`f64`-for-money claim found in the sampled cross-cutting docs.
**No contradiction.** (Timestamps invariant `INV-TIMESTAMPS-FRACSEC-001`
also present and unchallenged; the "no clocks in UI tests" topic returned no
matching prose in `ui-design-principles.md`/`architecture.md` this run —
either the rule lives elsewhere or is enforced only in test code; flag as
LOW-confidence not-found rather than a contradiction.)

## Anchor coverage

`spec/anchors.toml` declares **34** locked scenarios (up from 30 on
2026-05-22; **+4**). New anchors are the v3 volatility-target family:
`vol-verdict-bs1-realdata`, `top10-2023-fy-vol-target-overlay-realdata`,
`sharpe-comparison-vol-target-bs1-realdata`,
`sharpe-comparison-vol-target-bs1-realbaseline`.

**Cross-reference with `trace.toml` `[[req]]` anchor citations is PERFECT:**
- 34 distinct anchors cited across trace rows.
- 34 declared in anchors.toml.
- Declared-but-not-cited: **none**.
- Cited-but-not-declared: **none**.

This closes the last audit's loose-end concerns
(`top10-2024-fy-patchtst-overlay-realdata` and
`sharpe-comparison-patchtst-bs1-realdata` were never locked and are correctly
not cited — no orphan, no broken path).

### Shipped strategy features vs anchor coverage

| Strategy feature                | Status          | Anchors cited in trace | Assessment |
|---------------------------------|-----------------|------------------------|------------|
| v0-paper-sma                    | shipped         | 2                      | covered    |
| v05-composed-strategies         | shipped         | 3                      | covered    |
| v1-cross-sectional-momentum     | shipped         | 2                      | covered    |
| v15a-mean-reversion-pairs       | shipped         | 2                      | covered    |
| v1-5b-multi-venue               | shipped         | 0                      | infra/routing — no alpha scope; deliberate (matches last audit) |
| v2-llm-strategy                 | shipped         | 0                      | **GAP (likely by-design)** — see note |
| v25-tcn-overlay (+ family)      | shipped         | 4 (+ children 3/4/2)   | covered    |
| v25a-patchtst-overlay           | shipped         | 2                      | covered    |
| backtest-real-binance-data      | shipped         | 4                      | covered    |
| v3-volatility-forecaster        | retired         | 3                      | covered (then retired) |
| v3-vol-forecaster-rebaseline    | retired         | 1                      | covered    |
| v3-vol-forecaster-noop-fix      | shipped         | 3                      | covered    |
| v3-llm-forecaster               | shipped-partial | 0                      | **GAP (by-design)** — empirical phase paused (API-key blocked); analytical-only ship has no deterministic backtest to anchor |
| v3-regime-classifier            | draft           | 0                      | not yet shipped — no anchor expected |

**Anchor-coverage gap notes (none are broken-path; all are zero-citation):**
- `v2-llm-strategy` (shipped) cites **0** anchors. The prior audit credited it
  with `report-sample-7d`/`report-sample-90d`, but those anchors are actually
  owned by `REQ-OPERATOR-REPORTS-001` (operator-success-reports) — they anchor
  the deterministic *report shape*, not the LLM strategy. The v2 LLM strategy
  itself is non-deterministic (LLM directives), so a byte-stable backtest
  anchor is likely infeasible by design — but this is currently *undocumented*.
  Recommend either an explicit "not-anchorable — non-deterministic LLM output"
  note in the feature.md / trace row, or a deterministic replay-cache anchor
  (the `RecordingProvider`/`ReplayProvider` infra exists per the v3-llm-forecaster
  decomp). Owner: tester → architect.
- `v3-llm-forecaster` (shipped-partial) cites **0** anchors — consistent with
  its shipped-partial status (Wave D empirical paused on API key). Deliberate;
  re-evaluate when/if the empirical phase resumes.

**No shipped *deterministic* strategy feature is missing anchor coverage.**

## Resolved since last audit (P1/P2 closure check)

| 2026-05-22 finding                                            | Status now | Evidence |
|---------------------------------------------------------------|------------|----------|
| P1 — ADR-0035 number collision (two files share 0035)         | **RESOLVED** | Only `0035-tcn-sigma-train-recalibration.md` remains; the Phase B ADR was renumbered. |
| P1 — v25-tcn-overlay parent stuck `in-progress`               | **RESOLVED** | feature.md + `REQ-V25-TCN-001` both `shipped` (Option A). |
| P1 — v0/v05/v1 trace `state = "in-progress"` vs feature shipped | **RESOLVED** | `REQ-V0-PAPER-SMA-001`, `REQ-V05-COMPOSED-001`, `REQ-V1-MOMENTUM-001` all `state = "shipped"`. |
| P2 — product.md DL retirement narrative missing               | **RESOLVED (partial)** | product.md line 183 now "RETIRED 2026-05-22"; residual at line 220 "(planned)" forecaster bullet — see Soft Contradiction §3. |
| P2 — v25-tcn-horizon-bump-or-retire shipped-no-tests          | **RESOLVED** | lint `shipped-no-tests` now 0. |
| P2 — dead-link cleanup (81)                                   | **PROGRESSED** | 81 → 61 (−20) via the P1.1 sweep; three carry-over clusters remain. |
| P3 — `_probe_lint_test/` not deleted                          | **OPEN** | Still present (12 days). |
| P3 — trace `tests = [] # TODO` on early REQs                  | **OPEN** | Still 8 markers in trace.toml. |

## Recommended triage

- **P1 — v3 volatility-forecaster trace-state mismatch.** Two `[[req]]` rows
  (`REQ-V3-VOL-FORECASTER-001`, `REQ-V3-VOL-FORECASTER-REBASELINE-001`) carry
  `state = "shipped"` while both feature.md files say `status: retired`. The
  retirement is evidence-backed and is the source of truth. Flip both trace
  states to `"retired"`. Confirm the trace grammar/lint accepts the `retired`
  state. Owner: developer. (Two one-line edits.)
- **P1 — Confirm `retired` and `shipped-partial` are first-class states.**
  Two new status vocabularies entered the tree this sprint (`retired` ×2,
  `shipped-partial` ×1). The lint does not flag them, but it is unclear whether
  that tolerance is intentional or accidental (the lint may simply not validate
  the status enum). If unintended, the lint should be extended to recognise
  them so a future typo'd state is still caught. Owner: architect → (lint owner).
- **P2 — product.md "(planned)" forecaster bullet vs DL retirement.** Line 220
  still lists "lightweight Transformer or TCN" forecaster as a planned model
  despite the v2.5 programme's empirical retirement (line 183). Annotate or
  remove; cross-link line 221's regime-classifier bullet to the new
  `v3-regime-classifier` (draft) lane. Owner: analyst.
- **P2 — lab-end-to-end-v2 status alignment.** feature.md `proposed` vs trace
  `in-progress`. Pick the accurate state (likely `in-progress` given active
  Wave D commits) and align both. Owner: analyst/developer.
- **P2 — Document v2-llm-strategy anchor disposition.** Shipped strategy with
  zero anchor coverage and no recorded rationale. Either add a
  "not-anchorable — non-deterministic" note or wire a replay-cache anchor.
  Owner: tester → architect.
- **P2 — Anchored-report dead-link in v3-volatility-forecaster.**
  `reports/vol-verdict-bs1-realdata-20260522.md` has a dead `../architecture/adr/0038…`
  link, but the file is **byte-immutable** (ADR-0038 § D6). Do NOT mechanically
  edit — route through the § D6.b re-emission protocol or log as
  accepted-immutable-noise. Owner: architect.
- **P3 — Pin a 3.11+ interpreter for the weekly lint.** The scheduled host's
  default `python3` is 3.10 (no `tomllib`); the lint hard-fails without a shim.
  Owner: orchestrator/ops.
- **P3 — `_probe_lint_test/` delete (carry-over).** Pending since 2026-05-13.
  Owner: orchestrator.
- **P3 — trace.toml `tests = [] # TODO developer` batch-clear (carry-over).**
  8 cosmetic markers on shipped REQs. Owner: developer.
- **P3 — Residual dead-link clusters (carry-over).** ADR-0027 Kronos slug (5),
  `/tmp/` screenshots in chart-canvas-overhaul (6), legacy `v0-paper-sma`
  README paths (6), journal-tx-metadata missing report (4). None are shipped-code
  regressions. Owner: developer.

## Changelog
- 2026-05-25 (spec-auditor): weekly scheduled audit. Lint 82→61 (−21), now 1
  category (dead-link only); shipped-no-tests cleared. All three 2026-05-22 P1s
  resolved (ADR-0035 collision, v25-tcn-overlay parent status, v0/v05/v1 trace
  states). New P1: two v3-vol-forecaster trace-state mismatches (shipped vs
  retired) + new `retired`/`shipped-partial` state vocabularies need lint
  confirmation. Anchors balanced 34/34. product.md DL-retirement gap mostly
  closed; residual "(planned)" forecaster bullet at line 220. No stale
  (proposed/in-progress >30d) features.
