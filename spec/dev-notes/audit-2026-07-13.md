---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-07-13
---

# Spec Audit — 2026-07-13 (weekly cadence)

## Headline

**The healthiest week in the audit series on process hygiene — every P1/P2 from the
2026-07-06 audit was triaged and closed — but the v3 scorecard-fix opened one genuine NEW
contradiction.** `spec_lint.py` is PASS (0 violations), anchors hold 119/119, and the
feature.md↔trace.toml lifecycle drift that dominated the last two audits **collapsed 25 → 0**
(Class-A reconciled 2026-07-08; Class-B resolved structurally via ADR-0082 making feature.md
the single source of truth). SS-WIRE (`advisor-short-selling tests=[]`) is **wired** (now 8
tests). Trace coverage gaps fell 21 → 16. The one new finding (**SC-6, P1**): the scorecard
NaN-clamp fix (`9e8cd05`) retracted the flagship P2 verdict's "16/19 crowns clear DSR" down to
**"0/19"**, and the errata that records this **explicitly names editable spec surfaces
(feature.md, product.md, README.md, CHANGELOG.md, do-not-build-register.md) that still carry
the superseded number.** The anchored report was correctly left byte-immutable; the *editable*
propagation is the open gap. Zero strict-stale, no decay-heavy folder, no hard contradiction.

## Run metadata

- Date: 2026-07-13 (scheduled Monday cadence).
- Baseline: `spec/dev-notes/audit-2026-07-06.md` (most recent audit on disk).
- Commits since baseline: **38** (`git log --since='2026-07-06 09:12'`). Dominant theme: the
  **v3 remediation phase (P0–P8) shipped to complete** — 6 new v3 features, the scorecard
  DSR/MinBTL correctness fix, the 3-OS CI matrix activation (P7), and the ship-passive
  era-qualification (operator-ratified Option B, `61887c8`). ADRs **0082–0088** registered.
- Tooling: `spec_lint.py` requires Python 3.11+ (`tomllib`); this workspace ships 3.10, and
  `uv` cannot fetch a standalone interpreter (network-restricted). Ran the lint under a
  `tomli`-as-`tomllib` shim — identical logic, result unchanged. Same run-environment note as
  the last five audits; not a finding.

## Mechanical lint

`scripts/spec_lint.py --all` — **PASS (0 violations).** Exit code 0.

| Category                | This run | Previous (2026-07-06) | Δ  |
|-------------------------|----------|-----------------------|----|
| dead-link               | 0        | 0                     | 0  |
| missing-frontmatter     | 0        | 0                     | 0  |
| orphan-feature          | 0        | 0                     | 0  |
| anchor-coverage         | 0        | 0                     | 0  |
| trace-row-validity      | 0        | 0                     | 0  |
| pipeline-status-drift   | 0        | 0                     | 0  |
| **TOTAL**               | **0**    | **0**                 | 0  |

The self-guarding lint rule ADR-0082 added last week (feature.md = lifecycle source of truth)
is now the reason the drift finding below reads 0 — a structural fix, not a coincidence.

## Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged from baseline.
`anchors.toml` holds 119 `[[anchors]]` entries (count confirmed). The 6 v3 features shipped
this week run the advisor/verdict path with `write_report=false` (anchor-safety-by-construction),
so no anchored report body was re-emitted and the SHA set is byte-stable. The 9 backtest
scenario families and their SHA-256 bodies are untouched — audit-only, per instruction.

## Feature inventory

**155** `feature.md` files in scope (excluding `design/`, `dev-notes/`, `runbooks/`,
`archive/`, `architecture/`) — up from 148 last audit (**Δ +7**, all the new `spec/v3/`
remediation features). Layout: 119 in `spec/v1/`, 13 in `spec/v2/`, **6 in `spec/v3/`** (new
this week), 17 top-level. No duplicate slugs.

Status distribution:

| Status          | This run | Previous | Δ    |
|-----------------|----------|----------|------|
| shipped         | 124      | 117      | +7   |
| presenter-done  | 7        | 7        | 0    |
| retired         | 5        | 5        | 0    |
| tester-done     | 5        | 5        | 0    |
| deprecated      | 4        | 4        | 0    |
| dev-done        | 2        | 2        | 0    |
| candidate       | 2        | 2        | 0    |
| shipped-partial | 1        | 1        | 0    |
| in-progress     | 1        | 1        | 0    |
| roadmap         | 1        | 1        | 0    |
| reserved        | 1        | 1        | 0    |
| arch-done       | 1        | 1        | 0    |
| draft           | 1        | 1        | 0    |

All +7 landed as `shipped` (the v3 P1–P5 wave-through, `43606c6`). Distribution otherwise flat.

## New this week — v3 remediation phase (P0–P8)

Six `spec/v3/` features, all `status: shipped`, each with exactly one `[[req]]` trace row:

| Slug                       | Nature                        | Divergence-e2e gate |
|----------------------------|-------------------------------|---------------------|
| advisor-calibrate-stage    | DATA→CALIBRATE→ANALYZE stepper (UI) | N/A (no equity)     |
| advisor-corpus-expansion   | Coinbase 2nd-venue corpus + rerun harness (data) | N/A |
| advisor-crown-credibility  | crown Passes/WeakEvidence co-presentation (UI) | N/A |
| advisor-handoff-export     | deterministic offline plan export (P5) | N/A (recorded, not skipped) |
| advisor-lot-realism        | **lot-size/min-notional sizing modifier** | **✓ `lot_realism_divergence_end_to_end.rs` cited** |
| advisor-pit-discipline     | look-ahead lint + PitSeries publication lag | N/A (data timing) |

**CLAUDE.md non-negotiable held:** `advisor-lot-realism` is the only new sizing-modifier and it
ships the mandated day-1 baseline-equity-divergence e2e (`crates/backtest/tests/lot_realism_divergence_end_to_end.rs`,
cited in trace). Its money math is Decimal-exact per ADR-0003 (`feature.md:132-134`: rounding
"entirely in `Decimal` — never `f64`", round-DOWN only). The other five introduce no
overlay/sizing/decision-variable, so the divergence gate is correctly N/A (handoff-export
records it "N/A, not skipped" per the P1/P3 precedent).

## Stale features

**Zero** features match the strict staleness rule (`status: proposed` OR `in-progress`, not
updated 30+ days). The single `in-progress` feature is `cockpit-cross-platform` (updated
2026-06-15 → **28 days**) — still under the 30-day bar, but **will cross it next audit** if
untouched. See P1 carry-over.

### Aging watch items (NOT strict-stale — carry-over, all +7d)

| Slug                       | Status    | Updated    | Age  | Δ vs last audit |
|----------------------------|-----------|------------|------|-----------------|
| lumen-design-adoption      | roadmap   | 2026-05-04 | 70d  | +7d, no change  |
| lumen-phase-6-assistant-slot | reserved | 2026-05-04 | 70d | +7d, no change  |
| cockpit-app-bundle         | candidate | 2026-05-11 | 63d  | +7d, no change  |
| iced-ecosystem-evaluation  | candidate | 2026-05-13 | 61d  | +7d, no change  |
| ui-gallery-table-cell      | draft     | 2026-05-16 | 58d  | +7d, no change  |

All aged +7d with no state change (5th consecutive audit for the top four). Park or advance.

## PRIMARY FINDING RESOLVED — feature.md ↔ trace.toml drift collapsed 25 → 0

Last audit's headline (25 features `feature.md status: shipped` with `trace.toml state=`
something else) is **fully cleared.** Recomputed this run: **0 drift rows** across 144 `[[req]]`
feature rows. Two mechanisms, both landed this week:

- **Class-A (3 genuine mid-pipeline lag rows) — RECONCILED 2026-07-08** (`066fa46`
  "reconcile 4 lagging rows to shipped"). `advisor-param-promotion` (was design-complete),
  `advisor-param-tuning` (was in-progress), `advisor-signal-library-expansion` (was
  implemented) all now read `state="shipped"`, each stamped `reconciled→feature.md=shipped
  2026-07-08 (audit-2026-07-06 Class-A P1 lag)` — the auditor finding was actioned by name.
- **Class-B (22 tester-terminal `verified`/`passed` rows) — RESOLVED STRUCTURALLY** via
  **ADR-0082** (`b2a51fe` "feature.md is the single source of truth for lifecycle + self-guarding
  lint") plus the `verified→shipped` alignment sweep (`2fd968f`). The trace `state=` vocabulary
  no longer carries `verified`/`passed` at all.

Current `trace.toml state=` distribution (144 rows): `shipped` ×116, `tester-done` ×7,
`tested` ×6, `deprecated` ×4, `retired` ×4, `dev-done` ×2, `candidate` ×2, `roadmap` ×1,
`shipped-partial` ×1, `design-complete` ×1. The lone `design-complete` is
`advisor-reflection-decision-loop` (trace :3296), whose `feature.md status: arch-done` — a
legitimately-parked mid-pipeline feature, NOT drift (neither side claims `shipped`).

**Residual note (LOW):** `tested` ×6 and `tester-done` ×7 are still de-facto post-tester states
not enumerated in the trace legend, but ADR-0082 made feature.md authoritative, so the lint no
longer treats them as drift. No action unless the architect wants the legend to enumerate them.

## SS-WIRE RESOLVED — advisor-short-selling tests wired

Last audit's carry-over P2 (`REQ-ADVISOR-SHORT-SELLING-001` shipped but `tests=[]` despite
`short_selling_divergence_e2e.rs` on disk) is **closed.** The row now cites **8 tests**,
including `crates/backtest/tests/short_selling_divergence_e2e.rs`, `..._byte_identity.rs`, and
`..._bakeoff_bear_bull.rs`. The two other reconciled rows also carry their tests now
(`advisor-param-tuning` = 4, `advisor-param-promotion` = 3). No shipped-strategy feature is
left with an on-disk-but-uncited day-1 divergence test.

## Orphans

- **Feature folders missing feature.md / tasks.md:** none (lint `orphan-feature=0`).
- **Test reports for missing slugs:** none.
- **Orphan trace rows:** **0 genuine.** A naive slug-vs-feature.md scan surfaces `live`,
  `realdata`, `yahoo` — these are **false positives**: my regex matched `feature = "yahoo"`
  inside `#[cfg(feature = "realdata")]` / `#[cfg(feature="yahoo")]` cargo-feature-gate strings
  quoted in trace comments (e.g. `:564`, `:3283`), not real `[[req]] feature=` keys. Every real
  trace `feature=` slug maps to an existing `feature.md`.

## Trace coverage gaps (21 → 16)

16 in-scope `feature.md` slugs have no `[[req]]` row (was 21, **Δ −5**). The 6 v3 features all
got rows on landing; the previously-uncovered `v5-latency-slippage-sim*` and
`cockpit-toast-queue*` rows were added. Remaining 16:

```
UI-infra / rollup (likely intentional, 8):
  lumen-phase-1-foundation … lumen-phase-6-assistant-slot (6, UI-infra convention)
  phase-2c-overlays  phase-2d  (2 v2 rollup/umbrella folders)

v1 carry-over (8):
  chart-fixture-line-clipping   leaderboard-timeframe-capital
  paper-soak-longevity          ui-drop-iced-aw
  ui-gallery-bin                ui-gallery-table-cell (draft)
  ui-headless-emulator          ui-session-journal-iced-tester
```

The 6 `lumen-phase-*` and 2 rollup folders are the spec-compression convention's historically
un-traced UI infrastructure — low severity, add one-line REQ rows only if full cross-reference
is wanted. No lint violation. Owner: analyst.

## Legacy test-citation gap (informational, low)

23 `shipped` trace rows cite **0 tests** — but these are legacy v1/v2 features
(`v0-paper-sma`, `v05-composed-strategies`, `v1-cross-sectional-momentum`, `v2-llm-strategy`,
`reflection-memory`, `chart-canvas-overhaul`, `ui-quality-gate-overhaul`, etc.), a
long-standing pattern predating the `tests=`-citation convention, NOT a new regression. Lint
`shipped-no-tests` reads these via report-on-disk (which they have) so they pass. Flag only if
the operator wants retroactive back-fill. Owner: analyst (low).

## Decay markers (TODO / FIXME / TBD / XXX / ???)

**No feature folder exceeds the 5-marker threshold** (unchanged). Counts (excluding
`spec/archive/`):

| Folder                         | Count | Flag                                             |
|--------------------------------|-------|--------------------------------------------------|
| dev-notes (NOT a feature)      | 64    | self-reference: audit notes quote the grep patterns; exclude (was 63) |
| lumen-design-adoption          | 4     | below threshold (unchanged)                      |
| v1/ui-rethink-phase-a-lab      | 2     | benign                                           |
| v1/ui-headless-emulator        | 1     | benign                                           |
| v1/operator-ledger-schema-lint | 1     | benign                                           |
| v1/live-equity-history-durable | 1     | benign                                           |
| v1/advisor-llm-narration       | 1     | single TBD                                       |
| architecture / adr             | 2     | phantom-`models` carry-over context (SC-5)       |
| ui-design-principles.md        | 1     | FALSE POSITIVE — `XXXL` spacing token matched `XXX` |

The 64 in `dev-notes` are the audit notes spelling out the marker patterns (rose from 63 as
this note accrues) — not real decay. No feature is decay-heavy.

## Soft contradictions (LLM-judged — verify before acting)

Swept 5 cross-cutting topics from CLAUDE.md non-negotiables against `product.md`,
`spec/architecture/`, and the relevant v3 `feature.md` files.

### SC-1 — Money math (`Decimal`, never `f64`) — CONSISTENT (HIGH)

The new `advisor-lot-realism` sizing modifier is the only new money surface and it is
Decimal-exact: `feature.md:132-134` "rounding is `(qty / step).floor() * step` **entirely in
`Decimal`** — never `f64`", round-DOWN only, ADR-0003. No opposing claim. No action.

### SC-2 — Determinism / seeded reproducibility — CONSISTENT (HIGH)

`advisor-handoff-export` is a deterministic offline serialiser (stamped with run seed +
`last_bar_ts`, "no wall-clock", no LLM in path). `advisor-pit-discipline` adds a look-ahead
lint + explicit publication lag on `PitSeries` (ADR-0086) — both reinforce the determinism
posture. No contradiction.

### SC-3 — UI render-pixel verification — CONSISTENT (HIGH)

The v3 UI surfaces (`advisor-calibrate-stage` stepper, `advisor-crown-credibility`
co-presentation, `advisor-handoff-export` Export button "pixel-proven", `5a533e5`) land
alongside the existing render-snapshot harnesses. Not exhaustively re-verified this run —
flagged as an area, not a finding.

### SC-4 — `anchors.toml` header says `87/87` — actual 119/119 — STALE COMMENT (HIGH, carry-over)

`spec/anchors.toml:22` still reads "expects 87/87 PASS — 2026-05-31". Actual count is 119 and
`verify_anchors.sh` reports `119/119`. Same SC-4 the last four audits flagged and the
orchestrator DEFERRED ("fix on the next anchor-touching commit"). Cosmetic — `verify_anchors.sh`
does not read the comment. No functional impact.

### SC-5 — Phantom `crates/models` in architecture files — CARRY-OVER (HIGH, low severity)

`crates/models` does not exist (`ls -d` confirms), yet it is referenced in
`spec/architecture/12-forecast-overlay.md`, `spec/architecture/00-current-state.md`, and
`spec/architecture.md`. Pre-registered architect doc-sweep item, DEFERRED at prior audits.
Documentation-only, no code discrepancy. No new movement.

### SC-6 — NEW — DSR-retraction not propagated to editable surfaces (MEDIUM-HIGH)

**This is the week's substantive finding.** The scorecard NaN-clamp fix (`9e8cd05`
"exclude non-finite Sharpes from N_eff + DSR variance — the NaN clamp was disarming MinBTL AND
over-crediting DSR on every run") **retracted the flagship P2 verdict number**: the original
report claimed "**16 of 19** `ActiveWins` crowns clear DSR ≥ 0.95 (84%)"; post-fix the correct
figure is "**0/19 clear DSR**" (`crown_clears_dsr` flips true→false on 17/32 rows; outcomes +
crowned arms byte-identical). This is recorded in the append-only errata
`spec/v3/advisor-corpus-expansion/reports/backtest-2026-07-10-p2-verdict-rerun-errata.md`.

The anchored original report was correctly left **byte-immutable** (ADR-0038 §D6 — the errata
is the right pattern). **The problem is the editable surfaces:** the errata's own § 5
explicitly enumerates files that carried the now-superseded "16/19 clear DSR" number —
`spec/product.md`, `README.md`, `CHANGELOG.md`, `spec/dev-notes/do-not-build-register.md`, and
`spec/dev-notes/p2-wobble-thesis-analysis-2026-07-10.md`. Confirmed still stale this run:

- `spec/v3/advisor-corpus-expansion/feature.md:609` — "19/32 primary symbol-runs, **16 of
  which clear the DSR≥0.95** credibility check".
- `spec/v3/advisor-corpus-expansion/tasks.md:282` — same "16 of which clear the DSR≥0.95".
- `spec/v3/advisor-crown-credibility/feature.md:44/176/297/360` — repeated "clears DSR" state
  descriptions (these describe the *UI state machine* — a crown CAN clear DSR → `Passes`
  affordance — so these are plausibly design-semantics, NOT the retracted count; **flagged with
  uncertainty**, analyst to disambiguate).

**Uncertainty (flagged):** the denominators differ (32 all-era symbol-runs vs 19 old-era
`ActiveWins`), so a mechanical grep cannot prove every hit is the retracted claim vs a
correctly-scoped different statistic. But `feature.md:609` and `tasks.md:282` verbatim repeat
the errata's superseded "16 … clear the DSR≥0.95" phrasing — those two are high-confidence
propagation misses. The errata is the authoritative statement; the editable v3 headline should
carry the corrected "gate-crowned, cost-robust, NOT DSR-certified" qualifier or a pointer to
the errata. **Owner: analyst** (propagate the correction to editable files; do NOT touch the
anchored report).

## Resolved / improved since baseline

| Finding (baseline 2026-07-06)                       | Status now       | Evidence                                   |
|-----------------------------------------------------|------------------|--------------------------------------------|
| P1 Class-A 3 lagging trace rows                     | **CLOSED**       | reconciled 2026-07-08 (`066fa46`), all `shipped` + tests wired |
| P2 Class-B verified/passed vocabulary (22 rows)     | **CLOSED**       | ADR-0082 feature.md-as-SoT + `2fd968f` sweep |
| P2 SS-WIRE `advisor-short-selling tests=[]`         | **CLOSED**       | now 8 tests incl. `short_selling_divergence_e2e.rs` |
| P3 legacy `[[requirements]]` table                  | **CLOSED**       | eliminated `d1b3a35` (5 blocks → canonical `[[req]]`) |
| Trace coverage gaps                                 | **IMPROVED**     | 21 → 16 (v5-latency + toast-queue rows added) |
| Lint PASS(0) / anchors 119/119                      | HELD             | unchanged, green                           |
| CLAUDE.md day-1 divergence e2e (new sizing modifier)| HELD             | `advisor-lot-realism` cites `lot_realism_divergence_end_to_end.rs` |

**Every P1 and P2 from the last audit was closed this cycle** — the first audit in the series
with a clean carry-over ledger on the primary findings.

## Recommended triage

- **P1 — SC-6: propagate the scorecard-fix DSR retraction to editable surfaces (NEW).** The
  errata corrected "16/19 crowns clear DSR" → "0/19", but `advisor-corpus-expansion/feature.md:609`,
  `tasks.md:282`, and (per the errata's own § 5) `product.md` / `README.md` / `CHANGELOG.md` /
  `do-not-build-register.md` / `p2-wobble-thesis-analysis-2026-07-10.md` still carry the
  superseded number. Editable files only — the anchored report stays byte-immutable. Owner:
  analyst.

- **P1 — `cockpit-cross-platform` about to cross strict-stale (carry-over, escalating).** Still
  `in-progress`, now **28 days** (was 21) — trips the 30-day strict-stale rule **next audit** if
  untouched. Cross-platform claim unverified off-macOS for a 6th consecutive audit. Resolve or
  explicitly park. Owner: operator → tester.

- **P2 — Approve or return the 7 `presenter-done` features (carry-over).** All carry PASS
  verdicts, accumulating un-ticked operator approvals. Owner: operator.

- **P3 — Trace coverage gaps = 16.** 6 `lumen-phase-*` + 2 rollup folders (`phase-2c-overlays`,
  `phase-2d`) likely intentional; 8 v1 UI/infra carry-overs. Add one-line REQ rows only if full
  cross-reference is wanted. Owner: analyst.

- **P3 — `anchors.toml` header stale (`87/87`→`119/119`, SC-4, carry-over).** Cosmetic; update
  on the next anchor-touching commit. Owner: next tester/dev.

- **P3 — Phantom `crates/models` in architecture files (SC-5, carry-over).** Documentation-only
  sweep. Owner: architect/developer.

- **P3 — Aging watch items (all +7d, 5th cycle).** `lumen-design-adoption` (70d),
  `cockpit-app-bundle` (63d), `iced-ecosystem-evaluation` (61d), `ui-gallery-table-cell` (58d).
  Park or advance. Owner: analyst / operator.

- **P3 — Legacy `tests=0` shipped rows (23, informational).** Predates the citation convention;
  back-fill only if retroactive cross-reference is wanted. Owner: analyst (low).

## Deltas vs baseline (numerical)

| Metric                                | This run | Baseline (2026-07-06) | Δ        |
|---------------------------------------|----------|-----------------------|----------|
| Lint violations (total)               | 0        | 0                     | 0        |
| Anchors PASS                          | 119/119  | 119/119               | 0        |
| Features in scope                     | 155      | 148                   | +7       |
| Shipped features                      | 124      | 117                   | +7       |
| Strict-stale (proposed/in-prog >30d)  | 0        | 0                     | 0        |
| Aging watch items                     | 4        | 4                     | 0 (+7d each) |
| feature.md↔trace status drift         | **0**    | 25                    | **−25**  |
| Trace coverage gaps                   | 16       | 21                    | −5       |
| Orphan trace rows (genuine)           | 0        | 0                     | 0        |
| Decay-heavy feature folders (>5)      | 0        | 0                     | 0        |
| Hard contradictions                   | 0        | 0                     | 0        |
| Open soft contradictions              | 3 (SC-4/5/6) | 2 (SC-4/5)        | +1 (SC-6 new) |

## Changelog
- 2026-07-13 (spec-auditor): Weekly scheduled audit. 38 commits scoped since the 2026-07-06
  baseline — dominated by the v3 remediation phase P0–P8 shipping to complete (6 new v3 features
  + scorecard DSR/MinBTL correctness fix + 3-OS CI matrix activation + ship-passive
  era-qualification; ADR-0082–0088). HEADLINE: healthiest process week in the series — every
  P1/P2 from last audit CLOSED. feature.md↔trace drift collapsed 25→0 (Class-A reconciled
  2026-07-08 `066fa46`; Class-B resolved structurally via ADR-0082 feature.md-as-SoT).
  SS-WIRE wired (advisor-short-selling now 8 tests). Trace coverage gaps 21→16. Legacy
  [[requirements]] table eliminated. CLAUDE.md day-1 divergence non-negotiable HELD (the one new
  sizing modifier advisor-lot-realism cites its e2e; Decimal-exact money math). NEW finding
  SC-6 (P1): the scorecard fix `9e8cd05` retracted the flagship P2 verdict "16/19 crowns clear
  DSR"→"0/19"; the errata correctly left the anchored report immutable but the editable surfaces
  it names (feature.md:609, tasks.md:282, product.md, README.md, CHANGELOG.md,
  do-not-build-register.md, p2-wobble dev-note) still carry the superseded number — analyst to
  propagate. Carry-overs: cockpit-cross-platform in-progress 28d (P1, trips strict-stale next
  week), 7 presenter-done awaiting approval (P2), anchors.toml 87/87 header (P3, SC-4), phantom
  crates/models (P3, SC-5), 4 aging watch items +7d (P3). Lint PASS(0), anchors 119/119, zero
  strict-stale, no decay-heavy folder, no hard contradiction.
