---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-06-22
---

# Spec Audit — 2026-06-22

## Headline

The spec tree is **materially healthier** since `audit-2026-06-15`: a
dead-link/trace cleanup sweep (`50bf4fa`) drove lint from **70 → 1** — the
single survivor is the byte-immutable ADR-0038 report link (the "anchored
floor"; cannot and must not be edited). The 6th-consecutive-audit
`v2-llm-strategy` anchor-disposition gap is **RESOLVED**, decay markers
collapsed (lumen 11→4, cockpit-perf 6→0), and the heavy **67-commit week**
shipped a full new **single-coin investment advisor** epic (2026-06-19 pivot,
F1–F9) with every sizing/ensemble feature carrying its CLAUDE.md day-1
equity-divergence e2e test. Anchors hold at 119/119. The open work is
operational, not structural: one carry-over P1 (cockpit-cross-platform CI
gate, still unverified off-macOS) plus an in-flight 6-feature advisor epic
whose F8/F9 tester loops aren't closed yet.

## Run metadata

- Date: 2026-06-22 (Monday; 7 days since `audit-2026-06-15.md`).
- Auditor: spec-auditor (scheduled weekly, Monday cadence). This run performed
  directly (not delegated).
- Baseline: `spec/dev-notes/audit-2026-06-15.md` (lint 70 / 2-cat).
- Python: 3.10 + `tomli`→`tomllib` shim (carry-over; `uv run` blocked offline
  in sandbox — cannot fetch cpython 3.11+). `spec_lint.py --all` completed via
  the shim. Pin-for-lint remains a P3.
- Commits since baseline (2026-06-15): **67** (heaviest recorded week). Dominant
  arc: the **single-coin investment advisor** pivot (`9a27c21
  docs(product-pivot)`, 2026-06-19) and its F1–F9 build-out
  (`58b55b1`…`1dc9b32`), plus the lint cleanup sweep (`50bf4fa`).

## Mechanical lint

`scripts/spec_lint.py --all` — **FAIL (1 violation in 1 category).** Exit code
= 1.

| Category            | This run | Previous (06-15) | Δ    |
|---------------------|---------:|-----------------:|-----:|
| dead-link           |        1 |               65 | −64  |
| trace-broken-path   |        0 |                5 |  −5  |
| missing-frontmatter |        0 |                0 |   0  |
| shipped-no-tests    |        0 |                0 |   0  |
| orphan-feature      |        0 |                0 |   0  |
| **TOTAL**           |    **1** |           **70** | **−69** |

New all-time low (94 → 71 → 70 → **1**). The cleanup sweep `50bf4fa
chore(spec-lint): clean dead-links + trace-broken-paths — lint 67 → 1 (anchored
floor)` retired all 5 trace-broken-paths and all but one dead-link. The repo is
now at the structural floor.

### dead-link (1) — the byte-immutable floor

- `spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-20260522.md`
  → `../architecture/adr/0038-…#d1-v-verdict-priority-tree-…`. **BYTE-IMMUTABLE
  per ADR-0038 § D6 — DO NOT edit directly.** This is the documented anchored
  floor; the lint cannot reach 0 without an ADR-0038 § D6.b/§ D6.c re-emission
  protocol. Carry-over; excluded from any cleanup sweep. No action.

### Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged from
06-15. No new anchored scenario added this cycle — the entire advisor epic is
un-anchored by construction (see Anchor coverage below).

## Feature inventory (119 top-level feature.md)

Walk of `spec/*/feature.md` excluding `design/`, `dev-notes/`, `runbooks/`,
`archive/`, `architecture/`. Count is **119** top-level (+ 6 nested at depth ≥3).
Baseline reported 115 via a full-recursion walk; this run counts top-level only,
so treat the headline count delta as partly methodological. Net real growth ≈
+4 folders (the advisor epic) offset by status churn.

### Status histogram

| Status          | This run | 06-15 | Δ   | Notes                                          |
|-----------------|---------:|------:|----:|------------------------------------------------|
| shipped         |       86 |    78 | +8  | heavy ship week (advisor F1–F5 + cleanup)      |
| presenter-done  |        7 |     9 | −2  | awaiting operator approval                     |
| in-progress     |        6 |     1 | +5  | advisor F1/F8/F9 + forward-paper/plan + cross-platform |
| retired         |        5 |     4 | +1  |                                                |
| deprecated      |        4 |     4 |  0  |                                                |
| tester-done     |        3 |     4 | −1  |                                                |
| dev-done        |        2 |     5 | −3  | several advanced to presenter/shipped          |
| draft           |        2 |     2 |  0  | incl. `advisor-dynamic-data`                   |
| candidate       |        2 |     3 | −1  |                                                |
| roadmap         |        1 |     1 |  0  | `lumen-design-adoption` umbrella               |
| shipped-partial |        1 |     1 |  0  | `v3-llm-forecaster`                            |
| proposed        |        0 |     1 | −1  | `lab-polish-round-2` → **shipped**             |
| arch-done       |        0 |     1 | −1  |                                                |
| reserved        |        0 |     1 | −1  |                                                |
| **TOTAL**       |  **119** |  **115** | — |                                              |

## Stale features (>30 days, status proposed | in-progress)

**Strict proposed/in-progress stale-rule hits: ZERO.** (Unchanged — 3rd
consecutive clean run.)

All 6 `in-progress` features are fresh:

- `advisor-llm-narration` — in-progress, updated 2026-06-22 (**0d**).
- `advisor-ensemble` — in-progress, 2026-06-21 (**1d**).
- `advisor-forward-paper` — in-progress, 2026-06-21 (**1d**).
- `advisor-forward-plan` — in-progress, 2026-06-21 (**1d**).
- `advisor-bakeoff-ranking` — in-progress, 2026-06-20 (**2d**).
- `cockpit-cross-platform` — in-progress, 2026-06-15 (**7d**). Carry-over; CI
  gate still operator-deferred (see P1).

### Watch items (candidate/draft/roadmap aging ≥25d — not formal stale hits)

- `lumen-design-adoption` — roadmap umbrella, 2026-05-04 (**49d**, was 42d).
  Expected to lag; umbrella node.
- `cockpit-app-bundle` — candidate, 2026-05-11 (**42d**, was 35d). No movement
  in 6+ weeks. Park-or-advance candidate.
- `iced-ecosystem-evaluation` — candidate, 2026-05-13 (**40d**, was 33d). Oldest
  un-advanced evaluation; flagged 3 audits running. Park or kill.
- `ui-gallery-table-cell` — draft, 2026-05-16 (**37d**, was 30d). Aging.
- `advisor-dynamic-data` — draft, 2026-06-21 (**1d**). Fresh — listed for
  completeness only.

## Status drift

A full sweep of `spec/*/` folders containing a `presentations/*.md` deck found
**zero** features below `presenter-done`. The 06-15 enforcement hook
(`971957b`) is holding — **2nd consecutive zero-drift audit.** No action.

## Orphans

- **Crates without spec mention:** none — every `crates/*` is referenced in a
  `feature.md`, `architecture.md`, or `trace.toml`. No regression.
- **Test reports for missing slugs:** none.
- **Folders missing `feature.md`:** none (lint orphan-feature = 0).
- **Folders missing `tasks.md`:** widespread (≈40 folders) but **expected** —
  the lean-tree compression (`79721ce refactor(spec): compress completed
  features`) retires `tasks.md` on shipped features. spec-lint does not flag
  these and reports orphan-feature = 0; treat as convention, not a finding.

## Decay markers (TODO / FIXME / TBD / XXX / ???)

Occurrence counts across `spec/`, excluding `spec/archive/` and
`spec/dev-notes/archive/`:

| Folder                                       | This run | 06-15 | Δ    | Flag                                   |
|----------------------------------------------|---------:|------:|-----:|----------------------------------------|
| dev-notes (active, excl. archive)            |       29 |    20 | +9   | informational (audit notes quote marker tokens by nature) |
| lumen-design-adoption                        |        4 |    11 | −7   | **cleared** (was DECAY-HEAVY)          |
| cockpit-performance-and-input-responsiveness |        0 |     6 | −6   | **cleared** (6-audit DECAY-HEAVY gone) |
| (all other folders ≤ 1 each)                 |    ≤ 1   |   —   |  —   | —                                      |

**No code/spec feature is decay-heavy this cycle** (only `dev-notes` exceeds 5,
and that is the audit notes themselves quoting `TODO`/`TBD`/`FIXME` as findings
— informational, not a code-decay signal). Both prior DECAY-HEAVY carry-overs
(`lumen-design-adoption`, `cockpit-performance…`) are resolved.

## Soft contradictions (LLM-judged — verify before acting)

### SC-A — RNG ChaCha20 vs Murmur3 — **STILL RECONCILED** (PASS)

`spec/architecture/adr/0043-…md` lines 78-91 retain the in-place reconciliation
(`Murmur3-style bit mixer, NOT ChaCha20 — ChaCha20 init was 8–12× over the
target`; lines 280-285 document the shipped 2.28–2.50 ns/call finalizer; the
`ChaCha20Rng` sketch is explicitly retained as design rationale at lines 74,
85-91). No contradiction. CONFIDENCE HIGH. No action.

### SC-B — `product.md` paper-fill `bps: 2` vs canonical `bps: 8` — **STILL RECONCILED** (PASS)

`spec/product.md:574-576` carries the cross-reference: the `bps: 2` paper-fill
is the original Week-2 value; `slippage_bps: 8` is canonical per ADR-0045 § D1 /
ADR-0043 § D3 (citing audit-2026-06-08 SC-C). Reconciled in place. CONFIDENCE
HIGH. No action.

### SC-C — live-trading removal vs advisor pivot — **CONSISTENT** (PASS, new)

The 2026-06-19 single-coin advisor pivot is coherent with the 2026-06-12 live
removal. `product.md:64-66` (`Not a live broker… PAPER / SIM ONLY… live
execution was removed from scope`), `:241` (`live exec removed 2026-06-12`),
and `:452` (paper-only horizon reaffirmed 2026-06-12) all agree with the
PAPER-only advisor framing. No residual "live trading is in-scope" claim found.
CONFIDENCE HIGH (grep of product.md exhaustive; per-feature.md sweep was
spot-checked, not exhaustive — flagging that residual uncertainty).

### SC-D — money math / sizing modifier coverage — **CONSISTENT** (PASS, new)

The advisor F4 budget-aware €200 sizing modifier
(`FixedFractionSizer.budget_cap`) and F8 ensemble both correctly identify
themselves as subject to the CLAUDE.md day-1 baseline-equity-divergence
non-negotiable, and the test files exist (see Anchor coverage). product.md,
the F4/F8 feature.md, and trace.toml agree on the gate's applicability. No
contradiction.

## Anchor coverage

`spec/anchors.toml` — **119 scenarios, PASS 119/119** (unchanged; confirmed via
`verify_anchors.sh`).

### Advisor epic — all features carry explicit anchor dispositions (GOLD STANDARD)

Every advisor trace row documents its `anchors = []` disposition inline, and —
critically — correctly classifies whether the CLAUDE.md day-1 divergence gate
applies:

| Feature (req)                       | Gate applies? | Divergence e2e test                                  | Status |
|-------------------------------------|---------------|------------------------------------------------------|--------|
| `REQ-ADVISOR-BAKEOFF-001` (F1-F3)   | N/A (orchestrator/UI) | n/a                                          | OK     |
| `REQ-ADVISOR-FORWARD-PAPER-001` (F4)| **YES** (sizing modifier) | `crates/risk/tests/budget_sizing_divergence_end_to_end.rs` (exists, wired) | OK |
| `REQ-ADVISOR-FORWARD-PLAN-001` (F6) | N/A (read-only surface) | n/a                                        | OK     |
| `REQ-ADVISOR-DYNAMIC-DATA-001`      | N/A (data loading) | `dynamic_cache_anchor_safety.rs`                | OK     |
| `REQ-ADVISOR-ENSEMBLE-001` (F8)     | **YES** (signal-combiner) | `crates/strategy/tests/ensemble_vote_divergence_end_to_end.rs` (**file exists; trace `tests=[]` not yet wired**) | **WATCH** |
| `REQ-ADVISOR-LLM-NARRATION-001` (F9)| N/A (narration surface) | n/a                                       | OK     |

The required day-1 divergence test FILE for F8 already exists on disk; the
`REQ-ADVISOR-ENSEMBLE-001` trace row still reads `tests = []   # tester fills`.
This is expected for an `in-progress` feature, but is the one open traceability
loop in the advisor epic — see P1.

### Carry-over anchor-disposition gaps

- `v2-llm-strategy` — **RESOLVED.** trace.toml now carries `anchors = []   #
  anchorless by intent — see … § Backtest Scenarios "Anchor disposition" (live
  LLM completions non-deterministic → no stable body-SHA; fixture-replay path
  is the equivalent regression guard)`. This **closes a 6-consecutive-audit P2
  carry-over.**
- `v25-tcn-horizon-bump-or-retire` — decision/retire feature, anchorless by
  intent; cosmetic disposition-note absence persists. P3, unchanged.
- Bare `anchors = []` (no comment) rows at trace.toml L177/660/671/682/697/708/
  719/730/741/763 are all **non-strategy** features (multi-venue, position
  accounts, MTM, journal, cockpit, charts, widgets) — no anchor or disposition
  note is required by the audit rule (which only flags shipped *strategy*
  features). Benign; not a gap.

## Resolved since audit-2026-06-15

| 06-15 finding                                                  | Status now   | Evidence                                                        |
|---------------------------------------------------------------|--------------|----------------------------------------------------------------|
| `v2-llm-strategy` anchor disposition (6th carry-over, P2)     | **RESOLVED** | Inline `anchors = []   # anchorless by intent…` in trace.toml.  |
| Lint total (70)                                               | **IMPROVED** | 70 → 1 via `50bf4fa`; all 5 trace-broken-paths + 64 dead-links gone. |
| `lumen-design-adoption` decay (11, regressed)                 | **RESOLVED** | 11 → 4 (below threshold).                                       |
| `cockpit-performance…` decay (6, flat 6-audit)               | **RESOLVED** | 6 → 0.                                                          |
| `lab-polish-round-2` proposed (watch)                         | **RESOLVED** | now `shipped`.                                                  |
| 3 presenter-done awaiting approval (06-15 P2)                 | **PARTIAL**  | Down to 7 presenter-done total; some advanced to shipped.      |

### Still open (carry-overs)

| Finding                                                        | Severity | Notes                                                |
|---------------------------------------------------------------|----------|------------------------------------------------------|
| `cockpit-cross-platform` CI gate operator-deferred            | **P1**   | in-progress 7d; unverified off-macOS. 2nd audit.     |
| Residual dead-link (1, byte-immutable ADR-0038 floor)         | P3       | Must NOT edit; structural floor.                     |
| Python 3.11+ pin for lint (tomllib shim)                      | P3       | `uv` blocked offline.                                |
| `v25-tcn-horizon-bump-or-retire` disposition note absent      | P3       | Cosmetic.                                            |
| Aging candidate/draft/roadmap watch items (4)                 | P3       | cockpit-app-bundle 42d, iced-eco-eval 40d, ui-gallery 37d, lumen 49d. |

## Recommended triage

- **P1 — `cockpit-cross-platform` CI gate (carry-over).** Still `in-progress`
  (7d), still operator-gated. Locally verified on the macOS canonical box but
  Linux/Windows compile + headless apt-deps remain CI-deferred — the
  cross-platform claim is **unverified off-macOS** for a 2nd consecutive audit.
  Single highest-leverage operator action. Owner: operator → tester.

- **P1 — Close the advisor F8/F9 tester loops before they ship.**
  `advisor-ensemble` (F8) and `advisor-llm-narration` (F9) are `in-progress`
  with `tests = []` ("tester fills") in trace.toml. F8's required day-1
  divergence test file (`ensemble_vote_divergence_end_to_end.rs`) already
  exists on disk but is **not yet wired into the `REQ-ADVISOR-ENSEMBLE-001`
  trace row** — wire it (and the robustness + render tests) before flipping F8
  to shipped, per the CLAUDE.md non-negotiable. Owner: tester → architect.

- **P2 — Approve or return the 7 `presenter-done` features** awaiting operator
  decision. All have PASS verdicts and ready decks; un-ticked approval blocks
  are accumulating. Owner: operator.

- **P3 — Park-or-kill the aging watch items.** `iced-ecosystem-evaluation`
  (40d, 3rd audit), `cockpit-app-bundle` (42d), `ui-gallery-table-cell` (37d)
  have not moved in 5-6 weeks. None are formally stale (not proposed/in-progress)
  but all want an explicit decision. Owner: analyst / operator.

- **P3 — Cosmetic carry-overs:** the byte-immutable ADR-0038 dead-link floor
  (do NOT touch); Python 3.11 lint pin; `v25-tcn-horizon-bump-or-retire`
  disposition note. Owner: developer / ops.

## Changelog

- 2026-06-22 (spec-auditor): Scheduled weekly audit. 67 commits since
  audit-2026-06-15 (heaviest recorded week). HEADLINE: lint collapsed 70 → 1
  via cleanup sweep `50bf4fa` (all 5 trace-broken-paths + 64 dead-links
  retired); the single survivor is the byte-immutable ADR-0038 report-link
  floor (do NOT edit). Anchors 119/119 PASS unchanged. MAJOR CONTEXT:
  single-coin investment advisor pivot (`9a27c21`, 2026-06-19) + F1–F9
  build-out; product.md retitled. All advisor sizing/ensemble features
  (F4 budget modifier, F8 ensemble) correctly carry the CLAUDE.md day-1
  equity-divergence e2e classification; F4 test wired
  (budget_sizing_divergence_end_to_end.rs), F8 test file exists but
  trace `tests=[]` not yet wired (P1, in-progress). RESOLVED:
  v2-llm-strategy anchor disposition (6-consecutive-audit carry-over —
  now inline in trace.toml); lumen decay 11→4 and cockpit-perf decay 6→0
  (both prior DECAY-HEAVY carry-overs cleared); lab-polish-round-2
  proposed→shipped. Zero status-drift (2nd consecutive; hook `971957b`
  holding). Zero proposed/in-progress stale hits (6 in-progress features all
  fresh ≤7d). OPEN: cockpit-cross-platform CI gate operator-deferred
  (P1, 2nd audit); advisor F8/F9 tester loops not closed (P1); 7 presenter-done
  awaiting operator approval (P2); 4 aging candidate/draft watch items (P3);
  Python 3.11 lint pin + ADR-0038 byte-immutable dead-link floor (P3).
