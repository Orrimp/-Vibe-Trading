---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-06-15
---

# Spec Audit — 2026-06-15

## Headline

The spec tree is **materially healthier** since `audit-2026-06-12`: this is the
**first audit in 6 cycles with ZERO status-drift** — the pre-commit
status-enforcement hook (`971957b` / `feat(spec-lint): status-drift
enforcement`) landed and both carry-over drift items
(`cockpit-baseline-panel`, `cockpit-live-dashboard-wiring`) are now
`presenter-done`. Both long-standing soft contradictions (SC-A RNG, SC-B
slippage-bps) now carry in-place reconciliation notes. Lint is at an
all-time low (70, down from 71). Despite a heavy **44-commit week** that added
14 new feature folders, every new strategy/data feature ships with an explicit,
documented anchor disposition. The remaining open items are all P3 cosmetic
carry-overs.

## Run metadata

- Date: 2026-06-15 (Monday; 3 days since `audit-2026-06-12.md`).
- Auditor: spec-auditor (scheduled weekly, Monday cadence).
- Baseline: `spec/dev-notes/audit-2026-06-12.md` (lint 71/2-cat).
- Python: 3.10 + `tomli`→`tomllib` shim (carry-over; `uv run` blocked by
  sandbox network — cannot fetch cpython-3.14 build). `spec_lint.py --all`
  completed via the shim.
- Commits since baseline (2026-06-12): **44**. Dominant arcs: live-trading
  REMOVED (operator decision `c9c4561`, 2026-06-12), the post-live Lab
  strategy-checking arc (`simple-strategies-realdata` shipped), the real-data
  survey → overfit-guard robustness finding, and `cockpit-cross-platform`
  (ADR-0057).

## Mechanical lint

`scripts/spec_lint.py --all` — **FAIL (70 violations in 2 categories).**
Exit code = 2.

| Category            | This run | Previous (06-12) | Δ   |
|---------------------|---------:|-----------------:|----:|
| dead-link           |       65 |               66 | −1  |
| trace-broken-path   |        5 |                5 |  0  |
| missing-frontmatter |        0 |                0 |  0  |
| shipped-no-tests    |        0 |                0 |  0  |
| **TOTAL**           |   **70** |           **71** | **−1** |

Lowest lint total in recorded audit history (94 → 71 → **70**). The −1 is a
single dead-link retirement; no new violations introduced by the 44-commit
week — notable given 14 new feature folders landed.

### dead-link (65) — representative clusters (all pre-existing carry-overs)

- `spec/architecture/adr/0027-kronos-onnx-tract-integration.md` — 5 links to
  removed `../../v25-kronos-forecast-overlay/feature.md`.
- `spec/chart-canvas-overhaul/feature.md` — 7 links to `/tmp/orch-diag/*.png`
  (ephemeral capture paths).
- `spec/journal-transactions-metadata/feature.md` + `tasks.md` — 4 links to a
  test report archived in the 2026-06-11 sweep tar.
- `spec/v0-paper-sma/reports/screenshots/README.md` — 6 links to
  pre-flat-layout paths.
- `spec/v05-composed-strategies/feature.md` — 4 backtest-report links.
- `spec/lumen-design-adoption/phase-*/feature.md` — multiple crate-source and
  sibling-feature links.
- `spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-20260522.md`
  — 1 dead link. **BYTE-IMMUTABLE per ADR-0038 § D6 — do NOT edit directly.**
  Carry-over; excluded from any cleanup sweep.

### trace-broken-path (5) — unchanged from 06-12

- `REQ-LAB-YAHOO-REALDATA-V0-1-4-001` arch → missing
  `spec/lab-yahoo-realdata-v0.1.2-per-ticker-scaling-and-aggregate-cache-state/feature.md`.
- `REQ-VISUAL-FAIL-HTML-REPORTER-001` tests → two bare function names without
  `crates/…rs::` prefix.
- `REQ-QUEUE-STALENESS-RECONCILIATION-001` tests →
  `scripts/queue_staleness_check.py --self-test`.
- `REQ-OPERATOR-LEDGER-SCHEMA-LINT-001` tests →
  `scripts/operator_ledger_check.py --self-test`.

### Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged from
06-12. No new anchors added — all new strategy/data features this cycle are
un-anchored by construction (see Anchor coverage below).

## Feature inventory (115 feature.md files)

Walk of `spec/**/feature.md` excluding `design/`, `dev-notes/`, `runbooks/`,
`archive/`, `architecture/`. Count is **115** at full depth (up from the 103
reported at depth-2 on 06-12; the +12 is the new feature folders this week plus
deeper lumen/ui-rethink sub-phase nodes now counted at full recursion). The
count method differs from the 06-12 depth-2 walk, so treat the delta as
methodological, not pure growth.

### Status histogram

| Status          | Count | Notes                                                          |
|-----------------|------:|----------------------------------------------------------------|
| shipped         |    78 | +7 vs 06-12 (heavy ship week)                                  |
| presenter-done  |     9 | incl. 3 new awaiting operator approval (overfit-guard, corpus, simple-strategies-realdata) |
| dev-done        |     5 | incl. `cockpit-cross-platform` (locally verified, CI-deferred) |
| retired         |     4 |                                                                |
| deprecated      |     4 |                                                                |
| tester-done     |     4 |                                                                |
| candidate       |     3 |                                                                |
| draft           |     2 |                                                                |
| in-progress     |     1 | `cockpit-cross-platform` umbrella (0 days — fresh)             |
| proposed        |     1 | `lab-polish-round-2`                                           |
| arch-done       |     1 |                                                                |
| roadmap         |     1 | `lumen-design-adoption` umbrella                               |
| reserved        |     1 |                                                                |
| shipped-partial |     1 |                                                                |
| **TOTAL**       | **115** |                                                              |

## Stale features (>30 days, status proposed | in-progress)

**Strict proposed/in-progress stale-rule hits: ZERO.** (Unchanged from 06-12.)

- `lab-polish-round-2` — `proposed`, updated 2026-05-25 (**21 days**). Under
  threshold.
- `cockpit-cross-platform` — `in-progress`, updated 2026-06-15 (**0 days**).
  Fresh.

### Watch items (candidate/draft/roadmap aging ≥25d — not formal stale hits)

- `cockpit-app-bundle` — `candidate`, updated 2026-05-11 (**35 days**). Was
  32d at 06-12; continuing to age, no movement.
- `iced-ecosystem-evaluation` — `candidate`, updated 2026-05-13 (**33 days**).
  Was flagged at "30 days exactly" on 06-12 with a prediction it would cross —
  it has. Still not formally stale (not proposed/in-progress) but is the oldest
  un-advanced evaluation. Operator should park or advance.
- `ui-gallery-table-cell` — `draft`, updated 2026-05-16 (**30 days**). New to
  the watch list.
- `lumen-design-adoption` — `roadmap` umbrella, updated 2026-05-04 (**42
  days**). Umbrella node; expected to lag.

## Status drift — RESOLVED (headline)

**Both 06-12 drift items are fixed:**

| Feature                         | 06-12 status | Now              |
|---------------------------------|--------------|------------------|
| `cockpit-baseline-panel`        | `in-progress`  | **`presenter-done`** |
| `cockpit-live-dashboard-wiring` | `tester-done`  | **`presenter-done`** |

A full sweep of all `spec/*/` folders that contain a `presentations/*.md` deck
found **zero** features with `status` below `presenter-done`. The
status-enforcement hook recommended P1 across the last 5 audits
(`971957b feat(spec-lint): status-drift enforcement hook — deck + PASS =>
status >= presenter-done`) has landed. **This closes a 5-consecutive-audit
recurring finding.** No new status-drift instances this cycle.

## Orphans

- **Folders missing `feature.md`/`tasks.md`** (from spec-lint — expected
  carry-overs, no lint category triggered):
  - `spec/cockpit-app-bundle/` — missing `tasks.md` (candidate, pre-developer).
  - `spec/lumen-design-adoption/` — missing `tasks.md` (roadmap umbrella).
- **Test reports for missing slugs:** none found.
- **Crates without spec mention:** none — all `crates/*` referenced in
  `architecture.md`, `trace.toml`, or a `feature.md`. (No regression vs 06-12.)

## Decay markers (TODO / FIXME / TBD / XXX / ???)

Occurrence counts across `spec/`, excluding `spec/archive/` and
`spec/dev-notes/archive/`:

| Folder                                       | Matches | Δ vs 06-12 | Flag                          |
|----------------------------------------------|--------:|-----------:|-------------------------------|
| dev-notes (active, excl. archive)            |      20 | +7         | informational (audit notes accrue marker tokens by nature) |
| lumen-design-adoption                        |      11 | +3         | **DECAY-HEAVY** (regressed from 8) |
| cockpit-performance-and-input-responsiveness |       6 |  0         | **DECAY-HEAVY** (flat — 6th consecutive audit) |
| (all other folders ≤ 2 each)                 |    ≤ 2  | —          | —                             |

`lumen-design-adoption` **regressed** from 8 → 11 markers (was improving last
cycle). `cockpit-performance-and-input-responsiveness` flat at 6 (6th
consecutive). The `dev-notes` rise (13 → 20) is largely the accumulation of
audit-note text that quotes marker tokens (e.g. "TODO", "TBD") as findings —
informational, not a code-decay signal.

## Soft contradictions (LLM-judged — verify before acting)

### SC-A — ADR-0043 § D2 RNG: ChaCha20 vs Murmur3 — **RECONCILED** (was MEDIUM, 5-audit carry-over)

`spec/architecture/adr/0043-simulated-latency-and-slippage.md` now carries an
explicit in-place reconciliation: line 78 — `> Murmur3-style bit mixer, NOT
ChaCha20 — ChaCha20 init was 8–12× over the … target`; lines 80, 280-285 retain
the `ChaCha20Rng` sketch as design rationale and document the shipped Murmur3
finalizer at 2.28-2.50 ns/call. The body and changelog no longer contradict —
the divergence is now annotated. **Closing this as resolved** (confidence HIGH;
the note is unambiguous). Owner sign-off: architect may confirm.

### SC-B — `product.md` Week-2 `bps: 2` vs canonical `bps: 8` — **RECONCILED** (was MEDIUM, 5-audit carry-over)

`spec/product.md:662` still shows the paper-fill `bps: 2`, but lines 665 and
878-880 now ADD the cross-reference: the `bps: 2` paper-fill slippage is the
original Week-2 value; `slippage_bps: 8` is canonical per ADR-0045 § D1 /
ADR-0043 § D3 (explicitly citing audit-2026-06-08 SC-C). The two figures are now
reconciled by an in-place note rather than left as an apparent contradiction.
**Closing as resolved** (confidence HIGH). Owner sign-off: analyst may confirm.

### SC-C — CONSISTENT: live-trading removal coherence (new, PASS)

The operator removed live trading (`c9c4561 revert: remove live trading`,
2026-06-12) AFTER `live-exec-client-binance-spot` (F1) had shipped on 06-12.
Spot-checked: `product.md` boundary and the post-removal backlog
(`3536f79 docs(backlog): live trading removed`) agree — the Lab
strategy-checking arc is now the canonical product surface. No residual
"live trading is in-scope" claim found in `product.md`. CONSISTENT.
(LOW confidence on completeness — a deeper grep of every feature.md for stale
"live" scope claims was not exhaustively performed; flagging the uncertainty.)

### SC-D — CONSISTENT: new ADRs 0054-0057 registered (new, PASS)

ADR-0054 (Mode::Live boundary), 0055 (lab-run persistence + anchor safety),
0056 (Binance corpus timeframe layout), 0057 (cockpit visual-baseline
determinism scope) are all referenced from their owning feature trace rows.
No cross-ADR contradiction found in spot-check. CONSISTENT. Note: ADR-0054's
Mode::Live boundary postdates the live-trading removal — operator may wish to
mark ADR-0054 superseded/historical, but this is a documentation-hygiene
nuance, not a contradiction.

## Anchor coverage

`spec/anchors.toml` — **119 scenarios, PASS 119/119** (unchanged; confirmed via
`verify_anchors.sh`).

### New strategy/data features — all explicitly un-anchored WITH disposition (GOOD)

Every new shippable strategy/data feature this cycle documents its anchor
disposition directly in its `trace.toml` row — the gold standard this audit has
asked for repeatedly:

- `REQ-SIMPLE-STRATEGIES-REALDATA-001` — `anchors = []   # UN-ANCHORED by
  construction; 119/119 verified (AC6).` lab-runs-only, no committed report.
- `REQ-SIMPLE-STRATEGY-OVERFIT-GUARD-001` — `anchors = []   # UN-ANCHORED by
  construction (survey precedent; #[ignore] harness).`
- `REQ-BINANCE-CORPUS-EXPANSION-001` — `anchors = []   # data-corpus feature;
  AC2 asserts existing 3a8b96c4 pin stays byte-identical.`
- `REQ-COCKPIT-CROSS-PLATFORM-001` — `anchors = []   # build/CI/portability;
  AC asserts 119/119 stay byte-identical on canonical box.`

**No anchor-coverage gap among new features.**

### Carry-over anchor-disposition gaps (un-changed)

| Feature                          | State   | Disposition                                                                  |
|----------------------------------|---------|------------------------------------------------------------------------------|
| `v2-llm-strategy`                | shipped | Zero anchors, **no "not-anchorable" disposition note** in trace.toml. LLM non-determinism likely acceptable but still undocumented. 6th consecutive carry-over. Owner: tester → architect. |
| `v25-tcn-horizon-bump-or-retire` | shipped | Decision/retire feature — anchorless by intent, no disposition note. Cosmetic. Carry-over. |

The new features set the pattern these two should follow: one inline
`anchors = []   # <reason>` comment closes each.

## Resolved since audit-2026-06-12

| 06-12 finding                                                          | Status now   | Evidence                                                                 |
|------------------------------------------------------------------------|--------------|--------------------------------------------------------------------------|
| **P1 — add status-enforcement hook (2 drift items)**                   | **RESOLVED** | Hook landed (`971557b`); both items now `presenter-done`; sweep finds 0 drift. |
| **P1 — `backlog.md` `updated:` staleness**                             | **RESOLVED** | Now `updated: 2026-06-13` (was stuck at 06-08 for 4 audits).             |
| **P2 — ADR-0045 dead link to `spec-update/SKILL.md`**                  | **RESOLVED** | Line 159 now `../../../.claude/skills/spec-update/SKILL.md` (correct depth). |
| **P2 — SC-A RNG ChaCha20 vs Murmur3**                                  | **RESOLVED** | In-place reconciliation note in ADR-0043 (lines 78-91, 280-285).         |
| **P2 — SC-B product.md `bps:2` vs canonical `bps:8`**                  | **RESOLVED** | Cross-reference added at product.md:665, 878-880.                        |
| Lint total                                                             | **IMPROVED** | 71 → 70 (−1 dead-link); all-time low.                                    |

### Still open (carry-overs)

| Finding                                                                | Severity | Notes                                                          |
|------------------------------------------------------------------------|----------|----------------------------------------------------------------|
| `v2-llm-strategy` anchor disposition undocumented                      | P2       | 6th consecutive audit.                                         |
| `.claude/skills/rust-coverage/SKILL.md:159` prose citation to a moved dev-note path | P3 | Single textual reference to `testing-strategy-review-2026-05-25.md` (now under `archive/2026-Q2/`). Not a lint hit (not a markdown link). Cosmetic. |
| Residual dead-link clusters (65)                                       | P3       | Includes 1 byte-immutable ADR-0038 report link — must NOT edit. |
| Python 3.11+ pin for lint (tomllib)                                    | P3       | Shim still required; `uv` blocked offline in CI/sandbox.       |
| `lumen-design-adoption` decay markers (11, regressed)                  | P3       | DECAY-HEAVY.                                                   |
| `cockpit-performance-and-input-responsiveness` decay (6, flat)         | P3       | 6th consecutive.                                               |

## Recommended triage

- **P1 — `cockpit-cross-platform` CI gate is the only thing between
  `dev-done` and verification.** The feature is locally verified on the macOS
  canonical box (56 baselines byte-identical, 119/119 anchors, 456 ui tests
  pass) but Linux/Windows compile + headless apt-deps are **CI-deferred**:
  `.github/workflows/ci.yml` is written but the push is operator-gated. Until
  the operator OKs the CI push, the cross-platform claim is **unverified on
  non-macOS**. This is the single highest-leverage operator action this week.
  Owner: operator → tester (T-T1..3 sign-off after first CI run).

- **P2 — Approve or return the 3 `presenter-done` features awaiting operator
  decision:** `simple-strategies-realdata`, `simple-strategy-overfit-guard`,
  `binance-corpus-expansion`. All three have PASS verdicts, ready decks, and
  un-ticked approval blocks. The overfit-guard finding (down-market hedge is
  **path-fragile → ship-passive unqualified**) is a substantive research
  conclusion the operator should consciously ratify. Owner: operator.

- **P2 — Add `v2-llm-strategy` anchor disposition note** (6th consecutive
  carry-over). The new features demonstrate the one-line `anchors = []   #
  <reason>` pattern; apply it. Owner: tester → architect.

- **P3 — Park or advance the aging `candidate`/`draft` watch items.**
  `iced-ecosystem-evaluation` (33d) and `cockpit-app-bundle` (35d) have not
  moved in over a month; `ui-gallery-table-cell` (30d) just crossed. None are
  formally stale (not proposed/in-progress) but all are candidates for an
  explicit park-or-kill decision. Owner: analyst / operator.

- **P3 — `lumen-design-adoption` decay markers regressed (8 → 11).** First
  regression after a cycle of improvement; phase owners should sweep. Owner:
  phase owners.

- **P3 — Cosmetic carry-overs:** `rust-coverage/SKILL.md:159` prose path;
  Python 3.11 pin; residual dead-link clusters (excluding the byte-immutable
  ADR-0038 report link). Owner: developer / ops.

## Changelog

- 2026-06-15 (spec-auditor): Scheduled weekly audit. 44 commits since
  audit-2026-06-12. HEADLINE: first ZERO-status-drift audit in 6 cycles —
  enforcement hook `971957b` landed; both 06-12 drift items
  (cockpit-baseline-panel, cockpit-live-dashboard-wiring) now presenter-done;
  full presentations-deck sweep finds 0 drift. Both soft contradictions
  RESOLVED via in-place reconciliation notes: SC-A (ADR-0043 RNG ChaCha20 vs
  Murmur3, lines 78-91) and SC-B (product.md bps:2 vs canonical bps:8, lines
  665/878-880). Lint at all-time low 70 (65 dead-link + 5 trace-broken-path,
  −1 vs 71). Anchors 119/119 PASS unchanged. 06-12 P1s ALL RESOLVED:
  status hook + 2 flips, backlog.md updated→06-13, ADR-0045 spec-update link
  fixed (../../../). MAJOR CONTEXT: live trading REMOVED (operator c9c4561,
  2026-06-12) — F1 live-exec-client shipped 06-12 then reverted; product
  surface is now the Lab strategy-checking arc. 14 new feature folders;
  4 new ADRs (0054-0057) registered. New strategy/data features
  (simple-strategies-realdata, simple-strategy-overfit-guard,
  binance-corpus-expansion, cockpit-cross-platform) ALL ship with explicit
  documented `anchors = []` dispositions — no anchor-coverage gap. OPEN:
  v2-llm-strategy anchor disposition (6th carry-over, P2); cockpit-cross-platform
  CI gate operator-deferred (P1 verification); lumen decay regressed 8→11 (P3);
  3 presenter-done features awaiting operator approval (P2).
