panel: kill
title: Stop trading
state: halted
banner: AGENT HALTED
hint: Remove .halt and re-arm from the operator runbook before resuming.
runbook_link_label: Open kill-switch runbook
runbook_link_path: spec/runbooks/kill-switch.md
reason: halt_file

notes:
  - "Error" for the kill panel = the halted-sticky state. Banner uses
    `text::DISPLAY` in `color::NEG`; hint and runbook link use muted and
    accent tones.
  - The banner is sticky per R7.3 — only operator action (remove .halt,
    restart) clears it.
  - Runbook link copy comes from `strings::KILL_RUNBOOK_LINK_LABEL` and
    resolves to `strings::KILL_RUNBOOK_LINK_PATH`.
