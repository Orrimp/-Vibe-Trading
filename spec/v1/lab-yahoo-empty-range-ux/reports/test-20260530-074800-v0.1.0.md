---
title: Test Report
feature: lab-yahoo-empty-range-ux
run_id: 2026-05-30-0748-UTC
commit: uncommitted (working tree against HEAD a4a73ad8386591c26a7dd62a9c5ce158646e0bf3)
agent: tester
verdict: FAIL
---

# Test Report — lab-yahoo-empty-range-ux v0.1.0 — 2026-05-30 07:48 UTC

## 1. Scope

- **Feature / change under test:** lab-yahoo-empty-range-ux v0.1.0 — distinguish "Yahoo has no data for range" (future-dated or delisted) from real fetch failures; surface a muted notice instead of a misleading red error. Implements D-ER-1..6 (typed `YahooError::NoDataForRange` variant, sentinel-tagged `preload_notice::classify`, `end_ms.min(now_ms)` clamp, `last_run_notice` field, muted `FG_2` render branch).
- **Spec refs:** `spec/lab-yahoo-empty-range-ux/feature.md` (D-ER-1..6, P-ER-1), `spec/lab-yahoo-empty-range-ux/tasks.md` (M-DEV.1..20)
- **Commit SHA:** `uncommitted` — working tree against HEAD `a4a73ad8`
- **Rust toolchain:** `rustc 1.94.1 (e408947bf 2026-03-25)`
- **OS / arch:** Darwin 25.5.0

## 2. Static Analysis

| Check | Result | Notes |
|---|---|---|
| `cargo fmt -p ui -p data --check` | **PASS** | Zero diff |
| `cargo clippy -p ui -p data --features live,yahoo -- -D warnings` | **FAIL** | 12 errors; 2 are NEW from this feature's changed lines; 10 are pre-existing carry-forward |
| `cargo audit` | not run (not gated for this feature's scope; no new deps added) | n/a |
| `cargo deny` | not run | n/a |

### Clippy detail — attribution

**NEW errors introduced by this feature (2 errors — blocker):**

1. `crates/ui/src/lab/runner.rs:326:8` — `clippy::must_use_candidate` on `pub fn range_to_ms_pair`.
   - Cause: developer deviated from spec by promoting `fn range_to_ms_pair` (private at HEAD) to `pub fn` to enable test access from the integration test crate. The spec noted this deviation (tasks.md M-DEV deviation: "range_to_ms_pair exposed as pub... pub(crate) is insufficient"). The promotion introduced a new clippy hit.
   - Fix: add `#[must_use]` annotation or keep the function private and expose via a test-only re-export.

2. `crates/ui/src/lab/runner.rs:557:17` — `clippy::match_same_arms` on the `NoDataForRange` non-retry arm.
   - Cause: M-DEV.4 added `Err(e @ YahooError::NoDataForRange { .. }) => return Err(e)` as an explicit non-retry early-out in `fetch_with_backoff`, which is identical in body to the catch-all `Err(e) => return Err(e)` arm. Clippy correctly flags the redundancy.
   - Fix: merge the arms: `Err(e @ YahooError::NoDataForRange { .. }) | Err(e) => return Err(e)` (or add `#[allow(clippy::match_same_arms)]` with a note explaining the intentional separation for future divergence).

**Pre-existing errors (10 errors — NOT blocking per gate rules; quoted for visibility):**

| File | Line | Lint | Status |
|---|---|---|---|
| `crates/ui/src/lab/progress.rs:96` | 96 | `must_use_candidate` | Pre-existing (file not in this feature's changed set) |
| `crates/ui/src/lab/runner.rs:744` | 744 | `doc_markdown` (`LabRun` → backticks) | Pre-existing content (existed at HEAD:652, line shifted by feature insertions) |
| `crates/ui/src/lab/runner.rs:1057` | 1057 | `ignored_unit_patterns` (`_ = cancel.cancelled()`) | Pre-existing (existed at HEAD:963) |
| `crates/ui/src/lab/trainer.rs:264` | 264 | `needless_pass_by_value` | Pre-existing (file not in this feature's changed set) |
| `crates/ui/src/lab/training_log.rs:103` | 103 | `must_use_candidate` | Pre-existing (file not in this feature's changed set) |
| `crates/ui/src/widgets/position_curve.rs:111` | 111 | `cast_precision_loss` i64→f64 | Pre-existing |
| `crates/ui/src/widgets/position_curve.rs:134` | 134 | `cast_precision_loss` f64→f32 | Pre-existing |
| `crates/ui/src/widgets/position_curve.rs:134` | 134 | `cast_precision_loss` i64→f64 | Pre-existing |
| `crates/ui/src/live.rs:586` | 586 | `must_use_candidate` | Pre-existing |
| `crates/ui/src/live.rs:720` | 720 | `must_use_candidate` | Pre-existing |

Prior tester baseline (bug-64-d11-attempt-3 final report, 2026-05-29): 10 pre-existing clippy errors confirmed. This feature added 2 new ones.

## 3. Unit & Integration Tests

### Gate 1 (K2 required gate) — empty-vs-error classification

**Command:** `cargo test -p ui --test lab_yahoo_empty_range_classification --no-default-features --features live`

| Test | Result |
|---|---|
| `case_a_empty_source_routes_to_notice` | PASS |
| `case_b_transport_error_routes_to_error` | PASS |
| `k2_empty_vs_error_surfaces_are_distinct` | PASS |

**Result: 3/3 PASS.** Empty-source → `last_run_notice.is_some()` AND `last_run_error.is_none()`. Transport error → `last_run_error.is_some()` AND `last_run_notice.is_none()`. Surfaces are DISTINCT (K2 gate satisfied). Notice contains no `CacheMiss`, `MissingData`, or `Check network`. Includes ticker/range reference.

### Gate 2 (K1 data boundary)

**Command:** `cargo test -p data --features yahoo -- yahoo::tests::no_data_for_range_is_distinct_from_transport_errors`

| Test | Result |
|---|---|
| `yahoo::tests::no_data_for_range_is_distinct_from_transport_errors` | PASS |

**Result: 1/1 PASS.** `NoDataForRange` variant is distinct from `Http`/`RateLimited`/`Parquet` at the data boundary.

### Gate 3 (clamp — H3 + K3)

**Command:** `cargo test -p ui --test lab_yahoo_range_clamp --no-default-features --features "live,yahoo"`

Note: `--no-default-features --features yahoo` alone fails to compile (the `yahoo` feature requires `live` for tokio/agent). Command adjusted to `live,yahoo`.

| Test | Result |
|---|---|
| `h1_2024_byte_identical` | PASS — exact `(1_704_067_200_000, 1_719_792_000_000)` |
| `h2_2024_byte_identical` | PASS — exact `(1_719_792_000_000, 1_735_689_600_000)` |
| `last30d_end_does_not_exceed_now` | PASS |
| `last90d_end_does_not_exceed_now` | PASS |
| `custom_future_end_clamped_to_now` | PASS |
| `custom_past_end_passes_through_unchanged` | PASS |

**Result: 6/6 PASS.** H1_2024/H2_2024 byte-identical (K3 falsifier). Future Custom end clamped. Past Custom end unchanged. Clamp only fires when `end_ms > now_ms`.

### Gate 4 (terminal-state + T5)

**Command:** `cargo test -p ui --test lab_stop_button_gating --no-default-features --features live`

| Test | Result |
|---|---|
| `full_lifecycle_ok_completion_clears_inflight` | PASS |
| `err_completion_clears_inflight` | PASS |
| `stop_requested_mid_run_leaves_inflight_true` | PASS |
| `no_data_notice_completion_clears_inflight_and_progress` (T5) | PASS |

**Result: 4/4 PASS.** T5 confirms: no-data `LabRunCompleted(Err(tagged))` → `lab_run_inflight == false` + `run_progress == None` + `last_run_notice.is_some()` + `last_run_error.is_none()`. No spinner hang. Run button NOT in `Failed` state (R3 / D-ER-3).

### Gate 5 (classify unit tests)

**Command:** `cargo test -p ui --lib --no-default-features --features live -- preload_notice`

| Test | Result |
|---|---|
| `tagged_string_classifies_as_notice_stripped` | PASS |
| `untagged_string_classifies_as_error_verbatim` | PASS |
| `empty_string_classifies_as_error` | PASS |
| `no_data_message_is_tagged_and_readable` | PASS |

**Result: 4/4 PASS.** Tagged → `Notice(stripped)`, untagged → `Error(verbatim)`, empty → `Error`.

### Gate 7 — Regression tests

**Bug #64 regression:**

| Command | Tests | Result |
|---|---|---|
| `cargo test -p ui --test spawn_lab_run_yahoo_harness --no-default-features --features live` | 3/3 | PASS |
| `cargo test -p ui --test lab_runner_preload_callthrough_e2e --no-default-features --features live` | 2/2 | PASS |
| `cargo test -p ui --test lab_runner_cancel_e2e --no-default-features --features live` | 2/2 | PASS |

**Wave C regression:**

| Command | Tests | Result |
|---|---|---|
| `cargo test -p ui --test cockpit_subscription_server_time_always_batched --no-default-features --features live` | 2/2 | PASS |
| `cargo test -p ui --test toast_dismiss_recipe_stream --no-default-features --features live` | 3/3 | PASS |

All regression tests intact. Bug #64 ADR-0050 spawn-site path untouched. Wave C subscription/toast untouched. No REGRESSION.

### Summary table

| Crate/Suite | Passed | Failed | Ignored |
|---|---:|---:|---:|
| K2 classification (`ui` integration) | 3 | 0 | 0 |
| K1 data boundary (`data` unit) | 1 | 0 | 0 |
| Clamp T3/T4 (`ui` integration) | 6 | 0 | 0 |
| Stop-button + T5 (`ui` integration) | 4 | 0 | 0 |
| Classify unit (`ui` lib) | 4 | 0 | 0 |
| Spawn harness regression | 3 | 0 | 0 |
| Preload callthrough e2e | 2 | 0 | 0 |
| Cancel e2e | 2 | 0 | 0 |
| Batched subscription | 2 | 0 | 0 |
| Toast dismiss | 3 | 0 | 0 |
| **Total** | **30** | **0** | **0** |

## 4. Property / Fuzz Tests

_n/a_ — no proptest/cargo-fuzz suites for this feature.

## 5. Backtest Results

_n/a_ — D-ER-6 confirmed: this feature ships no strategy overlay or sizing modifier. It is a data-availability UX path. The CLAUDE.md baseline-equity-divergence overlay-e2e non-negotiable does not apply (R-NR.6). The K2 empty-vs-error classification test is the required gate per the v3-vol-overlay precedent lineage.

## 6. Benchmarks

_n/a_ — no latency-sensitive path changes; the classification path is a one-time string prefix check per run completion event.

## 7. P-ER-1 Falsifier — Edit-then-Edit-back Probe

**Probe performed:** Edited `crates/ui/src/lab/runner.rs` `preload_notice::no_data_message` to omit `NO_DATA_TAG` (returning bare body without sentinel). Ran T5 (`no_data_notice_completion_clears_inflight_and_progress`).

**Result:** T5 FAILED with:
```
thread 'no_data_notice_completion_clears_inflight_and_progress' panicked at crates/ui/tests/lab_stop_button_gating.rs:285:5:
T5 FAIL: last_run_notice must be Some after no-data outcome — muted notice must be set (D-ER-3). Actual: None
```

**Restored:** Edit reverted verbatim. `git status -- crates/` confirmed crates/ byte-identical to pre-probe state (no residue). P-ER-1 gate satisfied: the test discriminates the two surfaces and would catch a sentinel regression.

## 8. Anchor Verification (D-ER-6 / R-NR.1)

**Command:** `bash scripts/verify_anchors.sh`

**Result:** `ANCHORS PASS (84 / 84)` — zero anchor delta.

The synthetic path is wholly untouched. `range_to_ms_pair` is `#[cfg(feature = "yahoo")]` and only reachable from the YahooCache branch. `NoDataForRange` is additive to `YahooError`. `last_run_notice` is `None` on every synthetic run. No backtest-report-emitting code touched. 84/84 anchors byte-identical as expected by D-ER-6.

## 9. Release Build

**Command:** `cargo build --release -p ui --bin cockpit_live --features live,yahoo`

**Result:** `Finished release profile [optimized] target(s) in 11.32s` — CLEAN.

## 10. Spec-lint

**Command:** `python3 scripts/spec_lint.py`

**Result:** `spec-lint: FAIL (147 violations in 4 categories)`

**Category breakdown vs prior baseline (bug-64-d11-attempt-3 final tester, 2026-05-29: 152 violations):**

| Category | Prior baseline | Current | Delta |
|---|---:|---:|---:|
| `dead-link` | ~88 (est.) | 87 | -1 (improvement) |
| `trace-broken-path` | 56 | 55 | -1 (improvement) |
| `shipped-no-tests` | ~2 | 2 | 0 |
| `invalid-frontmatter-value` | ~6 | 5 | -1 (improvement) |
| **Total** | 152 | 147 | **-5 (net improvement)** |

**Assessment:** Count DECREASED from 152 → 147. No new categories. The decrease is consistent with this feature adding two new test files (`lab_yahoo_empty_range_classification.rs`, `lab_yahoo_range_clamp.rs`) that resolved some `trace-broken-path` entries. No new violations attributable to this feature's changes. Pre-existing violations carry forward unchanged per spec-lint gate rules.

**Pre-existing spec debt (quoted for visibility per spec-lint gate rules):**
- 87 `dead-link` violations — carry-forward from prior sessions (architecture ADR refs, screenshot artefacts, legacy flat-layout paths). Owned by analyst/architect.
- 55 `trace-broken-path` violations — carry-forward; spec-lint cannot resolve `::fn` test function notation to on-disk files for many rows. Tests confirmed real by independent run.
- 2 `shipped-no-tests` violations — carry-forward.
- ~5 `invalid-frontmatter-value` violations — carry-forward.

## 11. Operator Visual-verify Recipe (M-FINAL.4)

For when 1Password unlocks and the operator can start `cockpit_live`:

**Command:**
```
cargo run -p ui --bin cockpit_live --features live,yahoo --release
```

**Steps:**
1. In the Lab screen, set Source = YahooCache, Ticker = BTC-USD (or any ticker), Range = Last 30d.
2. Press Run.
3. Observe the run-button-row area below the Run button after ~2s.

**Timing:** Under the 2026 system clock, `Last 30d` computes a future-dated window with no Yahoo data. Preload completes in ~2s (network round-trip returns 0 quotes).

**Expected result (PASS):** A muted, neutral-colored message appears: `ⓘ No Yahoo data for BTC-USD in <window> — the range may be future-dated or the ticker may be delisted.` No red `⚠` glyph. No "Check network connectivity" text. Spinner cleared. Run button NOT in Failed/red state. Re-Run immediately available.

**Failure diagnosis:**
- If red `⚠ CacheMiss` or `⚠ Yahoo auto-fetch failed... Check network` appears → K2 classification is broken; route to developer.
- If spinner hangs (no completion) → R3 terminal-state broken; route to developer.
- If `ⓘ` appears but text contains `CacheMiss`/`MissingData`/`Check network` → R2 copy not applied; route to developer.
- If Range = H1 2024 shows a notice instead of a normal result → regression on clamp (K3 broken); route to developer.

**Cleanup:** None required. The cockpit is stateless between runs.

## 12. Verdict

**`FAIL`**

All 10 functional gates PASS: K2 classification (3/3), K1 data boundary (1/1), clamp (6/6), terminal-state + T5 (4/4), classify unit (4/4), P-ER-1 falsifier confirmed discriminating, 84/84 anchors byte-identical, release build clean, spec-lint count improved (147 < 152 baseline). Zero regressions on Bug #64 or Wave C tests.

The verdict is FAIL on the static-analysis gate: `cargo clippy -- -D warnings` fails with 2 new errors introduced by this feature's changed lines:

1. `clippy::must_use_candidate` on `pub fn range_to_ms_pair` (runner.rs:326) — caused by the developer's deviation from spec (made private fn public for test access without adding `#[must_use]`).
2. `clippy::match_same_arms` on the `NoDataForRange` non-retry arm (runner.rs:557) — the explicit non-retry arm is structurally identical to the catch-all arm; clippy requires they be merged.

These are not pre-existing (confirmed: `range_to_ms_pair` was private at HEAD; `NoDataForRange` did not exist at HEAD). The CLAUDE.md rule "cargo clippy -- -D warnings must pass" is a non-negotiable. Two targeted 1-line fixes resolve both issues without changing any logic.

## 13. Routing

`HANDOFF → developer (2 clippy fixes required; all functional gates PASS)`

**Fix 1 — `runner.rs:326` `#[must_use]`:**
Add `#[must_use]` above `pub fn range_to_ms_pair(...)`. Or: keep the function private and use a `#[cfg(test)] pub use ...` re-export in a test-only module. Either approach resolves the warning.

**Fix 2 — `runner.rs:557` `match_same_arms`:**
Merge the `NoDataForRange` non-retry arm with the catch-all. Example:
```rust
// NoDataForRange is terminal — do not retry. Falls through to catch-all.
Err(e @ YahooError::NoDataForRange { .. }) | Err(e) => return Err(e),
```
Or add `#[allow(clippy::match_same_arms)]` with a comment explaining the intentional separation for future divergence (less preferred; suppression should be last resort).

After both fixes: re-run `cargo clippy -p ui -p data --no-default-features --features "live,yahoo" -- -D warnings` to confirm 0 new errors. All functional tests should remain green (no logic change). Then HANDOFF → tester for re-verification.

---

## M-FINAL task status

- [ ] M-FINAL.1 — Full suite run complete. All 30 tests pass. ✓ (test gate PASS)
- [ ] M-FINAL.2 — Anchors 84/84 byte-identical. ✓ (R-NR.1 PASS)
- [ ] M-FINAL.3 — 4-cell verdict tree: functional PASS; clippy FAIL on 2 new errors → FAIL verdict. Not ticking to PASS.
- [ ] M-FINAL.4 — Operator visual-verify recipe written above (§ 11). Pending operator execution post-1Password unlock.
- [ ] M-FINAL.5 — Report written. `anchors` column NOT populated yet (PASS required first).
- [ ] M-FINAL.6 — HANDOFF → developer (not presenter — clippy fix required).
