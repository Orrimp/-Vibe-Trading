---
slug: architecture-adr-index
status: in-progress
owner: architect
updated: 2026-07-10 (ADR-0082 § Changelog amended — remediation-plan P6a: a SIBLING enforcement of the D1 invariant on the OTHER derived index, `spec_lint.py` category `feature-shipped-changelog-missing` [shipped feature ⇒ a `CHANGELOG.md` reference; slug / REQ-id / folder-path / iteration-suffix-base-slug match + a per-entry-justified `CHANGELOG_ROLLUP_ALLOWLIST`] with its own `--self-test` fixture; closes the R3-4b drift class. Enforcement extension inside D1, not a new decision → amendment, no new ADR number. ALSO ADR-0086 accepted — look-ahead lint + explicit publication lag, the ADR-0058 v0.2 follow-on, remediation-plan P3, feature `advisor-pit-discipline`; the do-not-build register's ONE named open gap + qlib-gap #1. HARDENING + LINT + PROOF, NOT a bug fix — grounding found BOTH DVOL (`v0.dvol_regime`) + macro (`v0.macro_riskon`) exogenous joins ALREADY as-of-correct (both route through the ADR-0058 `PitSeries`; DVOL keyed on the EOD-close instant, macro on `close_ts` ≈ EOD UTC). D1 a STANDALONE `scripts/check_no_raw_asof_join.sh` grep lint (predicate-shape match on a raw `partition_point(t<=q)`/`binary_search*` as-of join outside the sanctioned `core/src/pit.rs` home + a `// PIT-OK:` allowlist marker + `--self-test` + wired into `rust-validate`; NOT a `spec_lint.py` category — avoids the parallel P6a CHANGELOG-lint collision; `cargo-deny` symbol-ban rejected as false-pos/neg). D2 additive first-class `publication_lag_ms` on `PitSeries` (`*_with_lag` ctors; existing ctors = `*_with_lag(_, 0)` = byte-identical default; `as_of` returns `record.ts + lag <= query` via `query − lag`, preserving the `partition_point` one-liner + the ADR-0058 anchor-safe invariant at lag=0; lag table grounded — every current series' lag is 0, a future release-lagged print becomes declarable + demands its own ADR). D3 DVOL + macro retrofit onto `*_with_lag(_, 0)` proven byte-identical (legacy-predicate ≡ `*_with_lag(_,0)` element test); anchor-safe by construction (`write_report=false` arms → 119/119 before AND after). D4 verification floor = lint self-test + byte-identity test + lag-0/positive-lag unit tests + zero anchor delta; equity-divergence e2e N/A (no decision variable — zero divergence is success). FROZEN gate byte-untouched. extends ADR-0058. Took 0086 — read the registry after the sibling P6a ADR; 0086 was free. prior: 2026-07-09 (ADR-0085 accepted — the crown co-presents its overfitting (DSR) verdict, remediation-plan P1, D1=(a) presentation-layer, feature `advisor-crown-credibility`; ADDITIVE UI, NO gate change. D1 the recommendation banner co-presents the credibility verdict inline under the H2 headline for an active crown; D2 a PURE `crown_credibility(outcome, Option<&ScorecardView>)->{Passes|WeakEvidence|NotApplicable}` projection, NO new state field, NO `crates/backtest` change (reads the existing informational `crown_clears_dsr`, never a veto — mirrors ADR-0083 `stage_for`); D3 WeakEvidence (ActiveWins+fails-DSR, the money shot) = an unmissable ⚠ WARN-tier band with non-alarmist "did not survive the overfitting check — treat as weak evidence" copy qualifying the still-true headline, Passes = a muted ✓ line, NotApplicable = banner unchanged; D4 BenchmarkWins/AllFragile carry NO badge (buy-and-hold is gate-exempt per ADR-0066 + the DSR is on the max-Sharpe ACTIVE loser → a badge on a hold pick would mislead); D5 dual-mode + colour-never-only-signal + zero-literal, no new dep; D6 rendered-pixel proof `crown_credibility_render.rs` (WeakEvidence band + Passes control + BenchmarkWins no-band, macOS-gated) + unit tests; D7 anchors 119/119 + FROZEN gate + `scorecard.rs` byte-untouched, veto stays unbuilt (do-not-build E-1). Took 0085 — the sibling P2 ADR had already claimed 0084. prior: 2026-07-09 (ADR-0084 accepted — P2 corpus set + Coinbase second-venue adapter + multi-corpus verdict-rerun harness, remediation-plan P2, feature `advisor-corpus-expansion`. D1 ratifies the 3-corpus Binance set (1718 BTC/ETH/BNB, 2020 the-7-pre-2020-listers, 2526 all-10, last-CLOSED-month clamp); D2 ratifies Coinbase-hourly as the second venue (Kraken REST OHLC hourly deep-history INFEASIBLE — 720-cap, no backward paging) + specifies a NEW `fetch_coinbase_klines` bin + `coinbase_klines.rs` lib (the shipped `coinbase.rs` is a live-WS feed, CANNOT backfill; the ONE seam = the Coinbase→canonical BTCUSDT symbol + [time,low,high,open,close,vol] seconds→millis schema shim into the venue-agnostic parquet) + a BTC-only `data/coinbase` cross-check corpus; D3 fetches DVOL(2021-22+2025-26)+macro(2025-26) additive so the DVOL/macro arms are genuinely evaluable; D4 picks a DEDICATED `p2_verdict_rerun` harness (the shipped runner hardcodes `data/binance` in `preload_bakeoff_binance_bars` → the `null_data_no_crown.rs::run_field_and_rank` per-arm sequence + arbitrary-corpus `ReplayFeed` compose the seam, ZERO runner change, `write_report=false` → anchors 119/119); D5 clamps the 2526/coinbase end to the last fully-closed UTC month; D6 stronger survivor-of-survivors prose for 1718/2020 (no code); D7 opt-in `VolScaledSpread` era-cost annex (E-2; primary verdict on the frozen flat-bps default); D8 anchors 119/119 + FROZEN gate + existing pins byte-immutable BY CONSTRUCTION, the 9 anchors.toml SHAs untouched, no anchor-additive re-emission owed. prior: 2026-07-09 (ADR-0083 accepted — named "Calibrate" stage + the DATA→CALIBRATE→ANALYZE→SUGGEST spine stepper, v3 close-out R3-3a, feature `advisor-calibrate-stage`; formalizes v2 D7 — promote `Screen::Tune` to a sidebar "Calibrate" stage + a visible orientation stepper, DEFER the `agent::AdvisorStage` carrier again; the stepper is an orientation band not a router because DATA+ANALYZE share `Screen::Leaderboard`; render-layer verification mandatory; anchors 119/119 by construction, FROZEN gate untouched. prior: 2026-07-09 (ADR-0082 accepted — process/schema: `feature.md status:` is the single source of truth for `trace.toml state=`; closes the recurring audit lifecycle-drift finding. D2 invariant: a shipped feature's trace row MUST read `state="shipped"`; the tester-terminal aliases (verified/passed/tested/tester-done) are pre-ship-only. Enforced by `scripts/spec_lint.py` category `feature-shipped-trace-drift` (+ `--self-test` fixture); the `trace.toml` legend now enumerates the full vocabulary; the 11 live `passed`-on-shipped rows reconciled to `shipped` (provenance-preserving `PRIOR:`) in the same change. prior: 2026-07-01 (ADR-0064 amended — P2-1 narration faithfulness hardening: `NarrationFacts::allowed_numbers()` returns an owned `HashSet<String>` (was `Vec<String>`, converted inline); `RejectReason::FabricatedNumber(String)`/`::BannedPhrase(String)` now carry the offending token/phrase for the audit trail (were unit variants; `FaithfulnessVerdict::Pass` semantically unchanged); `BANNED_PHRASES` gains prediction verbs/causation clauses/advice phrases (additive, no phrase removed); 27-test adversarial corpus in `crates/agent/tests/narration_faithfulness.rs`; anchor-safe by construction, 119/119. Feature `advisor-narration-faithfulness`, v2-architecture.md §1 P2-1. prior: 2026-07-01 (ADR-0081 accepted — cost-model opt-in VolScaledSpread variant; default unchanged; anchors 119/119; Phase 2D P1-6. prior: 2026-06-30 (ADR-0080 accepted — drawdown-control overlay with static CPPI @ 20% drawdown floor + load-bearing HWM restart (Phase 2C P1-3): `DrawdownControlOverlay<S: Strategy>` generic wrapper homed in `crates/strategy/src/drawdown_control_overlay.rs`, normalised cushion multiplier `M(k) = (d_max − d(k)) / (d_max × (1 − d(k)))` with boundary M(0)=1/M(d_max)=0; floor = `initial × 0.80` NEVER moves (D8 static CPPI, TIPP deferred to v0.2); HWM restart is LOAD-BEARING (BTC benchmark: Sharpe 1.52 max-DD 72%→20% WITH restart; Sharpe −0.04 WITHOUT); composed via `Strategy::quantity_scale` → budget cap NEVER bypassed; 6-test e2e in `drawdown_control_overlay_end_to_end.rs` incl. the mandatory CLAUDE.md divergence gate ≥1bp; anchor-safe by construction (`write_report=false`; 119/119); REQ-V2-P1-3-DRAWDOWN-OVERLAY-001 in trace.toml. prior: ADR-0075/0076 promoted from reserved to accepted — Phase 2A credibility layer shipped: ADR-0075 the overfitting scorecard (P0-1, `Scorecard` on `Recommendation` with closed-form `N_eff = ρ̄+(1−ρ̄)·M` frozen at MAX_SWEEP_CONFIGS=24 + Bailey-LdP DSR + `MinBTL = 2·ln(N_eff)/SR²` + `pbo: None` deferred to Tune surface + `crown_clears_dsr` informational; "How much to trust this" leaderboard panel; FROZEN gate byte-untouched proven by `scorecard_does_not_change_ranking`; anchors 119/119 by construction; feature `advisor-overfitting-scorecard`); ADR-0076 the turnover + coherent-tail KPIs (P1-1 + P1-2, `CandidateKpis.turnover: Decimal` Churn column + `DistributionSummary.{cvar_95,cvar_99,median_terminal_wealth,skew}` reductions over the existing `PathMetrics` vector + `TailSummary` on `Recommendation.crown_tail` + 6-fact "Risk story" leaderboard panel; **CVaR not VaR — coherence is the architectural invariant**; FROZEN gate byte-untouched proven by `turnover_does_not_change_ranking`; anchors 119/119; feature `advisor-turnover-and-tail-metrics`). Operator-approved 2026-06-30. The third Phase 2A feature `advisor-confidence-not-verdict` (P0-3 `ScorecardSummary` projection onto `ForwardPlan.confidence` + "Confidence check" relabel) shipped as a thin projection of ADR-0075 over the ADR-0062 mirror seam — no separate ADR; the scorecard contract carries forward unchanged. ADR-0077 (forward-fidelity coverage, accepted 2026-06-30 in this same registry pass) is the Phase 2B prerequisite refactor. prior: 2026-06-26 (ADR-0072/0073/0074 added — the three #4 fresh-channel-probe architect designs, scoped + built full-fat per the operator: ADR-0072 the DVOL implied-vol exogenous-series probe + the `v0.dvol_regime` arm (Deribit DVOL corpus + `fetch_deribit_dvol` fetcher, a `dvol_data.rs` as-of join DELEGATING to the ADR-0058 `core::pit::PitSeries`, a hand-written `DvolRegimeStrategy` because the composed DSL cannot read a per-bar exogenous series, BTC+ETH universe only; locked W=30 trailing-median cut, NO search; feature `advisor-options-impliedvol-probe`); ADR-0073 the market-calendar layer (`enum MarketCalendar{Crypto24x7,UsEquity}` DERIVED FROM the ticker inside `load_cached` → its signature UNCHANGED → crypto byte-identical, an additive `expected_bars_for_calendar`) + the `v0.macro_riskon` arm (`^GSPC>SMA(50) ∧ DX-Y.NYB<SMA(50) ∧ ^TNX<SMA(20)` ⇒ long/flat, a hand-written `run_macro_gated_buyhold_path` reusing `PitSeries<bool>` for the look-ahead-impossible LOCF daily→hourly join; feature `advisor-crossasset-macro-regime`); ADR-0074 the read-only reflection decision-support surface — the honest C4 (autonomous param/route adaptation is moot on a ship-passive product, so ship a deterministic `recall_decision_lessons` in `crates/trader` reducing the reflection store to a `core`-typed `DecisionMemorySummary` at the bake-off/Tune/forward-plan decision points, narration-only/operator-as-adapter, never re-ranks/predicts; feature `advisor-reflection-decision-loop`). All three anchor-safe by construction (`write_report=false`/read-only → 119/119), the robustness gate + ADR-0066 benchmark BYTE-FROZEN, each with mandatory day-1 divergence/leak gates (the two alpha probes EXPECTED Fragile/null — a valid honest-coverage result, NOT an alpha claim). PAPER/SIM ONLY. Leans on ADR-0058 / ADR-0059 / ADR-0040 / ADR-0041 / ADR-0064 / ADR-0066 / ADR-0057.) prior: 2026-06-26 (ADR-0071 added — OBV DSL primitive (`obv` arity-0 + `obv_avg(N)` arity-1, each a new `IndicatorState` variant mirroring `Sma`/`RollingAvg`; recurrence `OBV_t = OBV_{t-1} + sign(close_t−close_{t-1})·volume_t`, `OBV_0=Some(0)`, Decimal-only; `avg(obv,N)` REJECTED as impossible — the rolling family's `field_arg` accepts only `Expr::BarField`; spelled `obv()`, the FIRST 0-arity indicator) + the operator-ratified FIXED 5-arm signal-library expansion (4 DSL-only — donchian_break/donchian_floor/vol_breakout/roc_momentum — + `v0.obv`, the one new primitive) scored by the BYTE-FROZEN robustness gate + the ADR-0066 buy-and-hold benchmark via the `v0.5.macd` arm seam; field 13→18; anchor-safe by construction (`write_report=false` → 119/119, NO anchor-additive amendment owed); the OBV identity/round-trip guard (textbook OBV vs a hand-computed reference, all 3 sign branches) + the CLAUDE.md day-1 divergence e2e on the REAL TOMLs + a leaderboard render-pixel guard at ~18 rows; honest COVERAGE + a richer decorrelation MENU, NOT an alpha claim (the new signals very likely ALSO Fragile; a null all-Fragile result is valid + shippable); feature `advisor-signal-library-expansion`. Leans on ADR-0067 / ADR-0068 / ADR-0066 / ADR-0069 / ADR-0063 / ADR-0010 / ADR-0057. prior: 2026-06-25 (ADR-0070 added — promotion wiring, carry a robustness-gated Tune config into the forward €200 paper-trade (feature `advisor-param-promotion`, the ADR-0069 §D5 deferred v0.2 carry-forward: wire the "Use this config" affordance — today a visual pill with NO message — so a PROMOTABLE [non-FRAGILE] config carries the TUNED strategy into the existing F5 paper loop + F6 forward plan). D1 a single optional `param_override: Option<ForwardParamOverride>` on `ForwardRunConfig` honored by BOTH forward resolvers (`build_registry_for` + `build_forward_plan_from_registry`); `None` = the existing crowned-pick path byte-identical (anchor-safe 119/119); `ForwardParamOverride` an agent-owned closed enum (NOT `backtest::SweptParams` — keeps the UI carrier `core`-types-only) one-per-family. D2 composed overrides REUSE the sweep's `macd_toml`/`rsi_toml`/`bbands_toml` generators promoted to `pub` + the SAME `ComposedStrategyConfig::from_str(&toml,stem)` identity guard the sweep SCORED with → byte-identical fidelity, in-memory (no disk file), one DSL source of truth. D3 the ONE override reaches F5 (registry) AND F6 (plan) in the supervisor `Launch` arm, both from the SAME generator → identity is STRUCTURAL; the F6 plan describes the tuned rules automatically (the `PlanRuleKind`→`PlanRuleView` mirror). D4 `PromoteSweptConfig(PromoteParams)` message; UI-owned `PromoteParams` on `SweepCellRow` populated at the ONE engine→UI boundary `from_report`/`cell_to_row`; pure handler preseeds `pending_forward_promotion` + navigates `Screen::ForwardPlan` (the `open_strategy_in_lab` precedent); binary dispatch maps `PromoteParams→ForwardParamOverride` + the `BakeoffRunCompleted` crowned-launch precedent + the F7/ADR-0065 €→USDT; TWO boundaries each crossed ONCE. D5 ONLY promotable promotes; FRAGILE lock + frozen gate UNTOUCHED; Marginal stays promotable; promotion READS the verdict never recomputes. D6 honest "you tuned this, not a bake-off winner; robust-on-THIS-window ≠ guarantee; not advice" framing on the forward-plan header, distinct from the crowned provenance; the not-advice footer stays. D7 day-1 divergence+fidelity+plan-reflects-tuned gate `forward_promotion_divergence.rs` (FAIL-before, the v3-vol-overlay-noop non-negotiable). D8 anchor-safe (`None` byte-identical → 119/119) + render-PIXEL proofs (enabled accent button vs the FRAGILE locked-label negative control; the preseeded TUNED forward plan). D9 NO new dependency, `cargo tree -p ui` UNCHANGED. PAPER/SIM ONLY. Leans on ADR-0069 / ADR-0060 / ADR-0062 / ADR-0065 / ADR-0059 / ADR-0057). prior: 2026-06-24 (ADR-0069 added — gate-tied hyperparameter SWEEP seam (feature `advisor-param-tuning`, leaderboard-epic item #3, the operator-chosen gate-tied option NOT a naive single-config editor): a parameter-grid SIBLING of the bake-off — `run_param_sweep` loops N parameterised configs of ONE family (SMA/MACD/RSI/Bollinger) and scores EACH through the SAME byte-frozen robustness gate so overfit configs read FRAGILE by construction. D1 NEW lib seam `backtest::bakeoff::sweep::run_param_sweep` (the `param_robustness_sweep` BIN is bin-only AND wrong-universe [10-symbol momentum `ThetaCell` grid] → NOT reused; reuse the already-extracted `classify_verdict`+`compute_robustness_flag` lib primitives + the `run_bakeoff` orchestration/mirror/progress template; homed in `backtest` per ADR-0059 §D1 → ZERO new `ui` dep edge). D2 surface the bootstrap DISTRIBUTION not just the flag via an ADDITIVE behaviour-preserving `compute_robustness_distribution` sibling (`compute_robustness_flag` delegates + drops the summary, proven bit-identical) — the p5/p50/p95 spread IS the anti-overfit affordance; verdict_bands+seed-rule BYTE-FROZEN (reaffirms ADR-0059 §D4/ADR-0063 §D4/ADR-0066 D3). D3 per-family param injection: SMA via the existing typed `sma_fast_len/slow_len` override; MACD/RSI/Bollinger via a GENERATED `signal`-DSL TOML string built in memory + `ComposedStrategyConfig::from_str` (the engine GAP — composed families have NO runtime param override; ship SMA-first, then add the others behind a round-trip-through-`from_str` test). D4 grid CAPPED `MAX_SWEEP_CONFIGS=24` + HONEST truncation (no silent drop; invalid `fast≥slow` cells dropped+reported). D5 FRAGILE prominent + promotion-DISABLED on fragile rows (the leaderboard "Fragile cannot be crowned" lock); shipped-config baseline row + buy-and-hold always in view; promotion-into-F4/F5 OUT OF SCOPE v0.1. D6 a Lab SUB-VIEW reached by a "Tune…" drill-down (the `InspectStrategyFromLeaderboard` precedent) NOT a 13th nav screen; `SweepReportMirror::from_report` single boundary preserves the `ui→strategy` non-edge. D7 async mirrors the bake-off arms 1:1 (`BakeoffProgress` determinate shape + cancellation + the `spawn_bakeoff` fixtures-Err pattern). D8 the CLAUDE.md day-1 DIVERGENCE e2e `param_sweep_divergence_end_to_end.rs` (swept equity ≠ shipped-config baseline ≥1bp + cells not all identical + a concrete SMA `(10,20)≠(20,50)` pin; the v3-vol-overlay-noop precedent). D9 anchor-safe BY CONSTRUCTION 119/119 (`write_report=false`, frozen bands, 9 anchor SHAs UNMUTATED → no anchor-mutation ADR) + render-PIXEL verification (`param_sweep_render.rs` populated grid + FRAGILE badge + distribution cols + Empty negative control + promotion-disabled discriminator, `#![cfg(target_os="macos")]` per ADR-0057, planning around the cosmic-text font-mutex deadlock). D10 NO new dependency. PAPER/SIM ONLY. Leans on ADR-0059 / ADR-0063 / ADR-0066 / ADR-0057 / ADR-0003. prior: 2026-06-23 (ADR-0068 added — single-coin directional SHORT-selling, paper/sim (feature `advisor-short-selling`, the operator-directed 2026-06-23 "do the expensive short selling"): PORT the proven `montecarlo.rs::run_path` signed-position model (open/cover/maintenance-margin-liquidation/per-bar-funding) into the long-only single-coin path, gated on a single `short_enabled` flag → long-only byte-identical (D1/D2). D3 (Q-SS-1) reuse `Buy`/`Sell` via the position-aware INTERPRETATION route, NO new `SignalKind`. D4 (Q-SS-4) a `core::funding::FundingRate` `FxRate`-style constant, per-bar accrual. D5 (Q-SS-5) inherit the 0.5-floor liquidation, cash + €200 P/L may print NEGATIVE (NOT clamped). D6 (Q-SS-6) ONE shared `backtest::short_exec` helper for bake-off ‖ forward-loop parity (the 4th clamp site the brief missed — runtime.rs:1809). D7 (Q-SS-3, THE CRUX) relax the audit READER `open_positions_at` to emit SIGNED positions — the writer `post_fill_with_signal` is already sign-agnostic (reconciler UNAFFECTED) + the bake-off never journals → reader-only, `audit` no-sibling-imports INTACT. D8 render-pixel-verified SHORT-badge/signed-qty/short-P&L UI + disclaimers. D9 the FIXED 5-arm slate {sma_cross_ls, macd_ls, rsi_ls, bbands_ls, always_short}, long-only arms UNTOUCHED. D10 119/119 + long-only byte-identity + frozen gate/bands/benchmark (ADR-0066/0063) BY CONSTRUCTION, NO anchor SHA mutates. D11 honest null-result-is-shippable framing + the day-1 downtrend-profit-with-correct-sign e2e on bear corpus 4f390622. PAPER/SIM ONLY. Leans on ADR-0051 §D6.10 / ADR-0065 / ADR-0066 / ADR-0063 / ADR-0024 / ADR-0016 / ADR-0059. prior: 2026-06-23 (ADR-0067 added — pre-registered combination-slate expansion of the advisor field (feature `advisor-combination-search`, the operator-requested 2026-06-23 "calculate the combination of multiple strategies" scoped pre-registered-ONLY): adds 6 new pre-registered `EnsembleStrategy` arms to the live field (4 singles + 8 ensembles + buy-and-hold = 13 arms) — 3 `Unanimous{n:2}` decorrelation pairs incl. the `trend_pair` predicted-null CONTROL + the COMPLETE k∈{1,2,3}-of-4 ladder — each scored through the BYTE-FROZEN `RobustnessMode::Bootstrap` gate + the ADR-0066 buy-and-hold benchmark. D1 FIXED code-declared FALSIFIER set (no search = overfit-safe by construction; principled-not-exhaustive; the control predicts little p5 lift → falsifiable). D2 LITERAL `build_ensemble` ids not a parser (engine already dispatches generically; an id grammar edges toward search → REJECTED; ZERO new arbitration math). D3 `default_ensemble_field()` single source of truth (+6→8; `advisor_field()` auto-flows; one runner field-count test 6→12; ~+86% wall-clock within budget on the determinate-progress on-demand path). D4 gate+bands+`rank_candidates`+ADR-0066 exemption FROZEN — "more candidates face the same bar," NOT a B2/B3 band proposal; `BenchmarkWins`/`AllFragile` reachability UNCHANGED, a null all-Fragile result is the expected + shippable finding. D5 honest mostly-Flat rendering of the strict-consensus pairs (no special-case). D6 anchor-safe BY CONSTRUCTION 119/119 (`write_report=false`, 9 anchor SHAs UNMUTATED → no anchor-mutation ADR) + the CLAUDE.md day-1 divergence e2e `combination_slate_divergence_end_to_end.rs` (≥1bp vs a member AND vs buy-and-hold + no-duplicate) + the 13-row leaderboard render-snapshot. DEFERS weighted/inverse-vol/regime blends to a v0.2 + quarantines the combination-SEARCH engine to a guarded follow-on. Leans on ADR-0063 / ADR-0066 / ADR-0059. prior: 2026-06-22 (ADR-0066 added — benchmark exemption from the `AllFragile` outcome determination (feature `advisor-benchmark-robustness`, the operator-approved B1 root fix for the always-`AllFragile`-on-real-crypto finding): the F8 robustness gate wrongly judged the buy-and-hold BENCHMARK by the candidate curve-fit ruler → `AllFragile` on every real-crypto run, masking the honest `BenchmarkWins`. TWO coordinated `rank.rs` edits (classifier byte-FROZEN): D1 `all_fragile`→`all_active_fragile` (filter `!is_benchmark` from the outcome determination — the benchmark is the null hypothesis the candidates are scored AGAINST, framed "benchmark-is-not-a-candidate," NOT a threshold relaxation, no band moved); D2 `is_eligible` returns true for the benchmark regardless of its own flag (the REQUIRED second edit — D1 alone is a WORSE bug: the comparator still partitions a Fragile benchmark ineligible → crown lands on a Fragile active arm → `ActiveWins` on a Fragile crown); D3 `classify_verdict`+`verdict_bands` BYTE-UNCHANGED (the benchmark's flag stays computed + DISPLAYED, only its CONSUMPTION in `rank_candidates` changes — reaffirms ADR-0059 §D4/ADR-0063 §D4 + the 2026-05-30 pre-registration); D4 anchor-safe BY CONSTRUCTION 119/119 (advisor path `write_report=false`, classifier frozen, `default()==Skip`; the ≤18 block-bootstrap θ-surface anchors hash the SWEEP-BIN `classify_verdict` output NOT `rank_candidates`); D5 the day-1 BenchmarkWins-REACHABILITY gate (all ACTIVE arms Fragile + benchmark top-Sharpe → `BenchmarkWins` AND `crowned.is_benchmark` — finally implementing the ADR-0063 §D7/R4.4 promise the `robustness_bootstrap_bites.rs:9` doc comment DECLARED but no test body asserted) + the `t65_all_fragile` amendment + the residual `AllFragile` dual; D6 determinism unchanged (pure/total, no new f64 boundary, no RNG). AMENDS ADR-0059 §D5 (comparator outcome rule) + ADR-0063 §D7 (R4.4 reachability); LEAVES the classifier freeze ADR-0059 §D4 / ADR-0063 §D4 UNCHANGED. REJECTS B2/B3 (band loosening — ≤18 anchor breaks + pre-registration moat forfeiture). prior: 2026-06-22 (ADR-0065 added — EUR→USDT budget-conversion seam (feature `advisor-eur-fx`, single-coin-advisor pivot F7, the LAST v0.2 item): a `FxRate {rate,source,as_of}` value object in a new `crates/core::fx` (no new dep, no new `ui` edge — the ADR-0058 home-in-`core` precedent; rate VALUE from advisor config `.unwrap_or(DEFAULT_EUR_USD_RATE=1.08)`, operator-LOCKED configurable-static, live fetch a v0.3 upgrade reusing this constant as its fallback) + ONE pure `convert_eur_to_usdt(eur:Decimal)->Money<Usdt>` fn + a `BudgetConversion{eur,rate,usdt}` carrier computed ONCE so the ENGINE (`ForwardRunConfig.budget`) and DISPLAY read the SAME converted value (the ADR-0062 anti-drift discipline), EUR kept a labelled `Decimal` at input (no `core::Eur` → F4 stays unit-agnostic, byte-unchanged); REPLACES the 1:1 `from_decimal` stamp at `cockpit_live.rs:1431-1437`; deterministic + anchor-safe BY CONSTRUCTION (anchored CLI is USDT-denominated, never reads the rate → 119/119, `cargo tree -p ui` unchanged) + the DAY-1 conversion-applied gate `crates/core/tests/eur_fx_conversion_applied.rs` (converted≠1:1 AND it's the value F4 caps against AND display==engine; FAIL-before/PASS-after per the `v3-vol-overlay-noop` non-negotiable) + the three "FX not modelled" literals → honest "€X ≈ $Y (at R EUR/USD, source as-of)". prior: 2026-06-22 (ADR-0064 added — advisor LLM "why this one" NARRATION seam (`agent::narration` generator owning the boot-built `Arc<dyn LlmProvider>` + the `backtest::Recommendation` facts) + the FROZEN two-layer faithfulness guard (role-locked cache-marked prompt + the LOAD-BEARING pure deterministic post-check: P1 wrong-crown / P2 contradicted-`RecommendationOutcome` / P3 fabricated-number-exact-string-vs-`num`-formatter / P4 a frozen predict/advise banned-phrase list → reject → `FellBack`) + a TRIGGERED opt-in "Explain" second async step (`BakeoffNarrationRequested`→`InFlight`→`BakeoffNarrationCompleted`, NOT auto) + the plain `ui`-owned `NarrationState{NotRequested|InFlight|Ready(SmolStr)|FellBack}` mirror (no `llm` type through `view`, the one `#[cfg(feature="live")]` adapter) + the fake-`LlmProvider` seam (faithful + unfaithful fixtures, no network) + caching (`CacheBreakpoint::Ephemeral`) + the `BudgetedProvider` path; EPHEMERAL (not persisted) + anchor-neutral 119/119 + the day-1 equity-divergence e2e N/A (read-only narration, like F6), for feature `advisor-llm-narration`, single-coin-advisor pivot F9 — the first in-app LLM consumer. prior: 2026-06-21 (ADR-0063 added — ensemble signal-vote seam (`EnsembleStrategy: Strategy` + pure arbiter + abstention-quorum warmup rule) + activation of the inert robustness gate (`RobustnessMode::Bootstrap` compute-and-feed reusing Politis–White block length + ADR-0051 sub-seed determinism + the FROZEN `classify_verdict`) + `PlanRuleShape::Ensemble` + the anchor-safety-by-construction additive contract (119/119) + the two day-1 divergence/anti-`Skip` e2e gates, for feature `advisor-ensemble`, single-coin-advisor pivot F8. prior: ADR-0062 added — forward-plan read seam for feature `advisor-forward-plan` (single-coin-advisor pivot F6, the CONDITIONAL rule-driven plan between the crowned pick and the F5 paper-trade): D1 a NEW read-only SIBLING trait `strategy::PlanDescribe::describe_plan(&self, &PlanContext)->StrategyPlan` (non-mutating; scoped to the F6 candidate engines — NOT a `Strategy` method, honouring the ADR-0005 trait freeze; engine emits STRUCTURED rule data {closed PlanStance/PlanSignal/PlanRuleShape + ProjectedSizing}, NOT a copy String — the ADR-0059 Recommendation-not-String precedent). D2 stance-now = a non-mutating read of already-warmed indicators (rejects Clone-and-on_bar). D3 the plan is produced AGENT-SIDE at the `ForwardCommand::Launch` boundary from the SAME `build_registry_for(Some(&cfg))` registry the F5 hot-swap runs ⇒ plan↔engine consistency BY CONSTRUCTION (R7), honestly describing today's SMA-PROXY resolution (build_registry_for proxies MACD/RSI/Bollinger/buy-hold through SmaCrossover until F5b). D4 the plan crosses to `ui` as a `core`-typed `agent::config::ForwardPlan` (Clone+Debug; closed agent-owned PlanStance/PlanSignal/PlanRuleKind enums + StrategyId/Symbol/Price/Timestamp/Money/Quantity + horizon_days; no strategy/exec/forecast/llm type) over a SECOND mpsc `RunHandles.forward_plan_rx` (agent→iced, symmetric with the iced→agent forward_rx); `ui` mirrors it like BakeoffReportMirror ⇒ `cargo tree -p ui` UNCHANGED. D5 buy-and-hold = a first-class PlanRuleKind::BuyAndHold arm + the render negative control. D6 the horizon is DISPLAY-ONLY metadata (horizon_days, default 7, range 1-30) — F5 stays BYTE-IDENTICAL (OQ-C, operator-locked OPEN-ENDED, NO self-terminate). D7 anchor-neutral (verify_anchors.sh 119/119 by construction) + the CLAUDE.md day-1 equity-divergence e2e is N/A (F6 describes, it does not size/run — that gate landed on F4). D8 verification = render-layer PNG (populated plan + buy-and-hold negative control, leaderboard_populated_render.rs pattern) + the anti-drift consistency assertion (describe_plan matches on_bar on the same bar) + cargo tree -p ui unchanged. Rejected the OQ-A fallback (UI-side TOML-AST rule derivation) with a CONCRETE drift proof (the SMA-proxy makes a MACD-AST plan actively wrong vs the SMA the loop runs). Leans on ADR-0060 / ADR-0059 / ADR-0005 / ADR-0023 / ADR-0041 / ADR-0057 §D2. prior: 2026-06-21 ADR-0061 added — dynamic on-demand Binance fetch seam + anchor-safe dynamic-cache separation for feature `advisor-dynamic-data`: extract the bin's fetch into `data::binance_klines::fetch_binance_klines_range` (typed `BinanceFetchError`, mock-fetcher seam, no socket in tests), add a git-ignored `data/binance-dynamic/` cache with NO REVISION pin (`dynamic_cache::load_or_fetch`), hook `resolve_bakeoff_bars` into the bake-off preload to FETCH-THE-WHOLE-WINDOW for ranges the pinned 2021-2024 corpus does not cover. Anchor-safe BY CONSTRUCTION: separate git-ignored root [`/data/*` already ignores it, no `!` exception] + `verify_anchors.sh` only walks `spec/` → 119/119 by construction + the dynamic branch never writes `data/binance/` or any `REVISION.toml`. Determinism boundary: dynamic data is EXPLORATORY, never anchored [no REVISION pin, no anchored report, unreachable from the covered-window anchored CLI path]; same-seed-every-arm holds within a run, non-reproducible across runs by design. Curated 10-symbol picker + 1h interval retained; `ui` imports no new crate [errors → `RunError::Internal` → already-wired `PanelState::Error`, verified at the render-pixel layer]; NO new dependency. Leans on ADR-0055 / ADR-0053 §D6 / ADR-0032 / ADR-0059 / ADR-0056. prior: 2026-06-21 ADR-0060 § D6 launch-lifecycle AMENDMENT — the D3/D5 boot-config `RunHandles.forward` field is structurally INSUFFICIENT for the POST-boot bake-off selection (`run()` consumes `RunHandles` once at boot; the selection arrives in the iced thread post-bake-off → a once-consumed boot field can never carry it — the cause of two FAKE dev passes where the DEFAULT 100k loop kept running and the UI relabelled `equity − 200`). REAL mechanism = a HOT-SWAP of the trading-loop task on the already-running runtime (A): a `core`-typed `ForwardCommand::Launch(ForwardRunConfig)` over a new `mpsc` (`RunHandles.forward_rx`, REPLACES `forward`) → a `paper_loop_supervisor` that aborts-the-current-loop + spawns-a-fresh-`spawn_trading_loop` (selected strategy/coin/€200) on the SAME `EventBus`/ledger/store/boot_id; `spawn_trading_loop` returns its `AbortHandle` (await old drain ⇒ no double equity-writer); the Live subscription SURVIVES because `stream_pnl` is anchored to the `Arc<EventBus>` broadcast receiver NOT the producer task. Rejected (B) whole-runtime relaunch (new bus breaks the iced subscription; new boot_id fragments the audit trail) + deferred (C) durable-store-read to v0.2. `forward_rx=None` byte-identical; render-proof UPGRADED to trace to a real `Some(budget)` loop. prior: 2026-06-20 ADR-0060 added — Budget-aware sizing modifier + forward-paper-run selection seam for feature `advisor-forward-paper`, the 2026-06-19 single-coin-advisor pivot F4+F5 (MVP-closing): D1 F4 budget sizing = an ADDITIVE `budget_cap: Option<Money<Usdt>>` field + `with_budget_cap` ctor on `crates/risk::FixedFractionSizer` + a COMPOSED Decimal-exact budget clamp in `compute_qty` (tighter of {exposure cap, budget} binds); `None` ⇒ byte-identical to today; PERMANENT notional ceiling not an equity re-scale. D2 F4 ships WITH the day-1 baseline-equity-divergence e2e (CLAUDE.md non-negotiable, modelled on `vol_targeting_overlay_end_to_end.rs`): budget vs default-capital arms over a ≥1-fill fixture, assert return paths diverge ≥1bp (FAIL-before/PASS-after). D3 F5 selection→forward seam = a `core`-typed `agent::ForwardRunConfig {strategy,symbol,budget,lookback}` built UI-side from the crowned/picked `LeaderRow` + F3 budget (core types only ⇒ NO `ui→strategy` edge) + a WIDENED `build_registry_for(cfg,&ForwardRunConfig)` resolving the selected `StrategyId` + a `RunHandles.forward` field; the paper branch derives `feed_symbol` from the selection (was hardcoded `BTCUSDT`). D4 MVP = REAL-TIME-ONLY forward run (paper = live WS); replay preview deferred v0.2. D5 the budget becomes the loop's STARTING CAPITAL (new `spawn_trading_loop` budget arg) so the EXISTING per-bar equity publish IS the budget equity (ZERO new plumbing); Live shows P/L = equity − budget; durable store ADR-0052 + reflection loop e9da47f reused. 119/119 anchors byte-identical by construction; OQ-1/OQ-2 carry recommended defaults. prior: 2026-06-19 ADR-0059 added — Bake-off orchestrator home + `BakeoffReport` seam + buy-and-hold `run_scenario` arm for feature `advisor-bakeoff-ranking`, the 2026-06-19 single-coin-advisor pivot F1+F2: D1 orchestrator home = `crates/backtest` (new `bakeoff` module) — it already owns `run_scenario` + `stats` + depends on `data`'s `BlockBootstrapPathGen` + `strategy`, AND `ui` ALREADY imports `backtest`/consumes `run_scenario` → the result is consumable through the IDENTICAL sanctioned seam with ZERO new `ui` dep, preserving ui-never-imports-strategy BY CONSTRUCTION; `agent` home REJECTED (`ui` lists `agent` optional/dev → forces a hard `ui→agent` edge), new `advisor` crate REJECTED for MVP. D2 seam = `backtest::bakeoff::BakeoffReport {request, candidates: Vec<CandidateResult>, ranked, crowned, rationale}` with a STRUCTURED `Recommendation` (outcome enum {ActiveWins,BenchmarkWins,AllFragile} + ReasonCodes + winner/benchmark KPIs) NOT a String — UI owns the not-advice copy + the v0.2 LLM narration is additive; mirrored via the `RunReportMirror` precedent. D3 buy-and-hold = an ANCHOR-ADDITIVE `"v0.buyhold"` `run_scenario` arm (BH is NOT a run_scenario arm today — lives only as `run_buyhold_path` in the sweep bin + a read-only example) reusing the extracted `run_buyhold_path`; additive arm + `write_report=false` ⇒ no anchored body created ⇒ 119/119 byte-identical. D4 `run_buyhold_path` + `classify_verdict` + `ParamRobustnessVerdict` + `P5_SHARPE_FRAGILE=0.0` MOVE from the sweep bin into the `backtest` library (behaviour-preserving). D5 `rank_candidates` is PURE/TOTAL/DETERMINISTIC: eligibility-partition-on-Fragile → Sharpe desc (`f64::total_cmp`) → total_return desc → max_drawdown asc → id lexicographic; crown = order[0], Fragile iff ALL fragile; BH ranked by the same rule, special-cased only in copy when it wins; NO f64 arithmetic introduced. 119/119 anchors byte-identical, no byte-immutable report touched, no `spec/anchors.toml` SHA changed. prior: 2026-06-18 ADR-0058 added — Point-in-time as-of-join primitive for feature `point-in-time-data-discipline`: D1 type-level `PitSeries<T>` + `AsOf<T>` (`as_of`/`as_of_value` return ONLY the most-recent record `ts ≤ query`; `AsOf` has PRIVATE fields + NO public ctor + no `get`/`Index`/future-iterator → look-ahead UNREPRESENTABLE at compile time, not a debug-assert; runtime-guard free fn REJECTED — debug-time-only + bypassable). D2 home = `crates/core::pit` (the BASE crate — backtest/data depend on it, it depends on neither; already carries rust_decimal/time/trybuild → ZERO new dep edge; ADR-0041 layering rule applied — `data` home REJECTED as it'd force a backtest→data edge). D3 key = transparent `TimestampMs(i64)` ms-since-epoch NOT `Timestamp` (loaders join on i64 ms; a `Timestamp` round-trip truncates i128→i64 → anchor-delta risk; `i64` `Ord` preserves `t ≤ q` char-for-char). D4 BEHAVIOUR-PRESERVING migration — `funding_as_of`/`basis_as_of` keep public signatures, bodies build a `PitSeries` + map `as_of_value`; `build_*_at_return` + their `Vec<Vec<Option<Decimal>>>` bootstrap-input shape UNCHANGED; same `partition_point(t≤q)`, same `None` warm-up, `Decimal` MOVED with no f64 round-trip ⇒ identical as-of values ⇒ verify_anchors stays 119/119 (no anchored-report edit, no anchors.toml SHA change → ADR-0038 §D6 + anchor-mutation-ADR untriggered). D5 verification floor = ONE shared `core::pit` falsifier (forward-shift `assert_ne!`) + a `trybuild` compile-fail proving no-future is STRUCTURAL + KEEP the 2 per-loader `no_look_ahead_falsifier` as regression guards; CLAUDE.md day-1 equity-divergence gate N/A (no overlay/sizing — correct outcome is equity UNCHANGED); `scripts/` lint DROPPED (typed-API sufficient; v0.2 follow-on). Flagged a FOURTH as-of copy (`stablecoin_diag.rs:301`) the brief did not enumerate. extends ADR-0041. 119/119 anchors byte-identical, no byte-immutable report touched. prior: 2026-06-15 ADR-0057 added — Cockpit visual-baseline determinism scope is the macOS canonical box for feature `cockpit-cross-platform`: LOCKS the 56 PNG visual baselines as macOS-CANONICAL [Linux/Windows render via cosmic-text `PlatformFallback` against the per-OS system-font DB since the cockpit sets no iced default font + `fira-sans` is off → NOT byte-gated], elevating the ADR-0043 § D5 / ADR-0051 § D5 "Apple-Silicon canonical box" determinism scope to a THIRD artifact class — the UI render snapshot gate; D2 enforcement in SOURCE not YAML via a file-level `#![cfg(target_os="macos")]` inner attr on the four snapshot test files [the source gate IS the CI filter — no `--skip` drift]; D3 Linux/Windows visual regression deferred to a v0.2 follow-on gated on H1 [`fira-sans` + pinned `default_font`] needing its own canonical box, MUST supersede/amend this ADR; 119/119 anchors untouched, no byte-immutable report touched. prior: 2026-06-15 ADR-0056 added — Binance corpus timeframe-layout convention for feature `binance-corpus-expansion`: LOCKS own-root-per-timeframe — each Binance timeframe is its OWN corpus root, flat `<root>/<SYM>/<YEAR>/<MM>.parquet` retained, timeframe encoded by root-dir NAME [`-<tf>` suffix for non-1h, bare roots = 1h by convention, existing pinned roots NEVER renamed]; the Yahoo `/<INTERVAL>/` segment of ADR-0040 § D3 is DELIBERATELY NOT mirrored onto Binance [ReplayFeed globs whatever's under `<root>/<sym>/<year>/` with NO interval arg, vs YahooBarSource taking an explicit interval — a retrofit would re-emit `data/binance` 3a8b96c4 or skew the glob to two shapes]; D3 ZERO `ReplayFeed` change, root-path selection the only knob, the four `-realdata` anchors neutral BY CONSTRUCTION [no code seam to forget, cf. ADR-0055 D2]; v0.1 `data/binance-2122/` is a flat 1h sibling root [the `binance-broaduni` precedent] correct+final under D1; daily v0.2 unblocked as a NEW suffixed sibling root with no reader change; 119/119 anchors byte-identical, no anchor-mutation ADR required. prior: 2026-06-12 ADR-0055 added — lab-run persistence topology & anchor-safety for feature `lab-run-save-compare`: Lab reports live at a git-ignored `lab-runs/<slug>/reports/backtest-*.md` workspace-root home OUTSIDE every `spec/**` anchor glob → `verify_anchors.sh` stays 119/119 BY CONSTRUCTION [the git boundary is the byte-immutability guarantee, not reviewer vigilance]; closes the `run_scenario` Phase-C deferral at `engine.rs:608` via a `reports_dir: Option<PathBuf>` anchor-additive `#[serde(default)]` seam on `ScenarioConfig` gated `write_report`, reusing `report/<family>::write` VERBATIM; the Lab+Compare loaders read the union of two roots [lab-runs first, then spec]; H3 invariant write-root==read-root flips `lab_run_engine.rs` skip→pass; retention keep-last-N=20-per-tuple. 0054 SKIPPED — burned in git history as the removed `Mode::Live` ADR. prior: 2026-06-11 ADR-0053 added — unify the per-bar trading loop across research and paper: ONE feed-parameterized `spawn_trading_loop` + `LiveEquityStore` param persisting loop-direct gated by `Some(store)` (the mode gate moves to the caller — research `None`, paper `Some`) + the bar-stream cadence + the `drop(state_tx)` stub removed; research byte-stability via named guards (paced-replay test, AC2 store=None, anchor independence from `runtime::run`); the baseline-equity-divergence gate's INTENT applies + is satisfied by AC1 data-layer + AC6 render-layer Y-variation, NOT N/A. prior: 2026-06-11 ADR-0052 added — durable-live-equity-series: audit-ledger `equity_snapshots` table behind a `LiveEquityStore` trait + mint-site `mode != Research` gate + the hydration `as_of` contract; additive `013` migration, 19 anchors byte-safe by construction. prior: 2026-06-07 ADR-0051 § D6.10 amendment — perp-basis-mn-spread: the SHORT-SIDE ENGINE, the FIRST `run_path` touch since C2 + a SECOND simultaneous co-resampled sidecar; the 6th anchor-additive instance after MR=D6.5/carry=D6.6/TS=D6.7/horizon=D6.8/long-only-basis=D6.9. The market-neutral basis-spread (long-low / short-high, dollar-neutral, beta-stripped) — the pre-registered follow-on after D6.9 closed FAMILY-UNIFORM-FRAGILE at every fee incl. 0bps gross (a VEHICLE verdict, not a signal verdict). Q-MN-1: the short-side engine = a `k_short`-GATED BRANCH inside the EXISTING `run_path` (keeps the CONCRETE `MomentumStrategy` signature — NO dyn/generic, the §D6.5.2 trap avoided; sibling `run_path_long_short` REJECTED — duplicates solvency logic); the short SOLVENCY model (a short's loss is unbounded above): symmetric dollar-neutral notional + a short INITIAL-MARGIN gate (`max_leverage=dec!(1)`) + a deterministic MAINTENANCE-MARGIN LIQUIDATION rule (`maintenance_margin_frac=dec!(0.5)`); equity math UNCHANGED. Q-MN-2 (THE load-bearing gate): the `run_path` anchor-neutrality RE-PROOF — by-construction (short branches are dead code when `k_short==0`) + a `run_path_k_short_zero_byte_identical_to_head` test + the hard `verify_anchors.sh`→107/107 gate run FIRST + after every seam. Q-MN-3: a SECOND SIMULTANEOUS sidecar (the D6.9 channel-reuse RETIRED — basis + funding BOTH live) — `GeneratedPath.basis_by_symbol` + `BlockBootstrapPathGen.basis_at_return` + `with_basis(…)` + `TcnScenarioInput.basis_override`, co-resampled at the SAME `idx_seq`, ZERO new RNG, `None` for non-MN runs → the 107 hold. Q-MN-4: the basis⊥funding arm is RANK-BASED Decimal-EXACT (`rank(basis) − rank(funding)`, NO division → bit-reproducible; OLS slope REJECTED). Q-MN-5: a NEW namespace `perp-basis-mn-spread`; 3 arms (basis/funding/basisperp) × {0,5}bps × {2023,2024} = 12 surfaces × 400 paths ≈ ~12 min; θ L∈{60,168}, K=3; null = dollar-neutral ≈0 NOT BH (the ONE frozen-rule change, a CORRECTION); + up to 6 anchors, BOTH revision SHAs hashed. run_path NO LONGER byte-untouched but byte-IDENTICAL when k_short==0 by construction. Build ~5–8 dev-days. prior: 2026-06-05 ADR-0051 § D6.9 amendment — perp-basis-signal-robustness: the FIRST LIVE signal of the post-OHLCV program, a perp-spot BASIS-REVERSAL arm (cross-sectional basis rank IC −0.08..−0.11 over L=60-168, NEGATIVE + sign-stable both years; tilt AGAINST = long the lowest-basis names) with a SWEPT FEE AXIS — the 5th anchor-additive instance after MR=D6.5/carry=D6.6/TS=D6.7/horizon=D6.8. A single `ScoreSource::BasisReversal` arm with the SIGN baked into the score (`−trailing_mean(basis)`, one place, guarded by the day-1 sign-assertion falsifier; option ScoreSource::Basis+Direction::Reversion REJECTED) + NO cashflow (the basis is a SELECTION signal — carry's accrual Sub-D does NOT transfer; the `run_path` accrual stays gated `None`, P&L is pure price-of-selection). The sidecar REUSES the `funding_by_symbol` co-resample channel (a generic Option<Decimal> sidecar, funding-specific only in name; basis + funding mutually exclusive in v0.1.0 → ZERO new bootstrap field, smallest blast radius; sibling basis_by_symbol REJECTED for v0.1.0) consuming ZERO new RNG draws (the D6.6 proof transfers); the 8h→1h forward-fill is an IDENTITY at 1h (as-of join = `basis_close[t-1]`). THE LOAD-BEARING fix (R-BR.LOAD, REGRESSION-blocked): a NEW `--taker-fee-bps`/`--slippage-bps` axis modeled on the D6.8 `--horizon` axis — the fee hardcoded at param_robustness_sweep.rs:2409-2410 (slippage_bps:2/taker_fee_bps:4) → a CLI flag defaulting 2/4 (MatchConfig byte-identical for non-basis runs) + a hashed body row GATED `score_source==BasisReversal` (so {0,2,5,10} bps are four DISTINCT anchors); mechanism (i) one-surface-per-fee RATIFIED, (ii) fixed-pin+best-cell REJECTED (argmax the frozen §0 forbids crowning). NEW namespace `perp-basis-signal-robustness` (NOT mc-robustness-2026-06) + the additive verify_anchors.sh elif; + up to 8 anchors (4 fee × 2 regime; {0,5}×{2023,2024}=4 minimum); LOCKED 6-cell grid (L∈{24,60,168} BARS × K/cadence, rebalance default 8h/g3 24h, SKIP L=720) + N=200 + the fee ladder + basis-revision SHA aa72409a… hashed; wall-clock ~16 min for all 8 surfaces — TRACTABLE. run_path/PaperEngine/bootstrap byte-UNTOUCHED; the 99 anchors hold by construction. prior: 2026-06-03 ADR-0051 § D6.8 amendment — horizon-retest-robustness: a HORIZON/DATA-PATH change varied at the LOAD + the CALCULATOR level (4h + daily coarser decision cadence on the SAME 10-symbol coins), NOT a new family — the 4th anchor-additive instance after MR=D6.5/carry=D6.6/TS=D6.7. THE LOAD-BEARING fix (R-HR.LOAD, REGRESSION-blocked, the sharpest anchor-risk in the program): compute_sharpe_hourly/_sortino_hourly/compute_calmar are kept BYTE-VERBATIM + three NEW siblings compute_*_periodic(equity, periods_per_year: f64) annualize 4h/daily by √ppy (4h √2190, daily √365, leap-year-aware) — the 1h fns are NEVER edited so the 91 anchors hold by construction; parameterizing the existing fns REJECTED (ULP-change → 91-anchor REGRESSION). resample_ohlcv = a pure ordered Decimal fold (open=first/high=max/low=min/close=last/vol=Σ, UTC integer-division bucketing) wired post-merge_symbols in load_real_bars, horizon==1h → identity. OQ-CARRY-SEM resolves for free (the funding as-of join keys off the COARSE bar open_ts after the resample-first ordering — no funding_data.rs edit). Bootstrap ladder cosmetically 1h (renderer prints the REAL horizon, a hashed field); rebalance cadence counted in coarse-bars-via-the-1h-ladder (every coarse bar = rebalance_minutes ≤ 60). run_path/PaperEngine/bootstrap byte-UNTOUCHED. NEW namespace horizon-retest-robustness (NOT mc-robustness-2026-06); + up to 8 anchors (TS + carry × 4h + daily × 2023/2024); LOCKED grids (TS-4h {42,180,540}×{0.00,0.02}, TS-daily {5,20,60}×{0.00,0.02}, CARRY-4h L∈{2,6,12}, CARRY-daily L∈{1,3,7}) + N (200 @ 4h, 1000 @ daily) hashed. prior: 2026-06-02 ADR-0051 § D6.7 amendment — time-series-momentum-robustness: the FIRST non-cross-sectional family (per-asset absolute momentum, long/flat), a 2ND SELECTOR (select_above_threshold) via a SelectionMode serde-default enum + entry_threshold, +1/+2 TS θ-surface anchors = 90/91; 2026-05-31 ADR-0051 § D6.6 REAL-MECHANISM amendment — carry-strategy: a 2ND time series (funding) co-resampled under the SAME shared idx_seq, +1 carry anchor = 88; 2026-05-31 ADR-0051 § D6.5 cross-ref amendment — cross-sectional mean-reversion 2nd strategy family, +1 MR anchor = 87; 2026-05-30 ADR-0045 § D6.3 + § D7.1b engine-drift))
---


# Architecture Decision Records (ADR) — Index

Every non-trivial architectural decision lives here as a dated,
numbered, immutable record. ADRs are *cited* by `spec/trace.toml` rows
in their `arch` field, by feature `## Design` sections, and by code
review checklists.

## Format

See [TEMPLATE.md](TEMPLATE.md). The required frontmatter is:

```yaml
---
adr: NNNN
title: <short imperative title>
status: proposed | accepted | superseded | deprecated
date: <YYYY-MM-DD>
supersedes: <NNNN | none>
superseded-by: <NNNN | none>
---
```

Body sections: `Context`, `Decision`, `Alternatives considered`,
`Consequences`. Keep ADRs short — most should fit in 100-200 lines.
Longer ADRs usually mean the decision is actually two decisions in a
trench coat.

## Numbering rules

- Numbers are assigned by the architect when the ADR is filed and never
  reused. A `superseded` ADR keeps its number; the new ADR gets a new
  one and references the old via `supersedes:`.
- Numbers are zero-padded to 4 digits (`0001`, not `1`).
- Filename pattern: `NNNN-kebab-case-title.md`.

## Registry

(Cross-referenced from `spec/architecture.md` § ADR registry. This is
the canonical table; the parent file links here.)

| ID    | Title                                              | Status     | Date       |
|-------|----------------------------------------------------|------------|------------|
| 0001  | Crate names avoid stdlib collisions                | accepted   | 2026-04-17 |
| 0002  | RNG seeded with ChaCha20 from config seed          | accepted   | 2026-04-17 |
| 0003  | Money math uses Decimal, never f64                 | accepted   | 2026-04-17 |
| 0004  | Audit-DB uses 6-digit fractional-second timestamps | accepted   | 2026-04-18 |
| 0005  | v0 — clean strategy trait shape, no hot-load       | accepted   | 2026-04-17 |
| 0006  | v0.5 — config-driven strategy composition (hot-load A) | accepted | 2026-04-19 |
| 0007  | v1+ — WASM plugin hot-load deferred                | accepted   | 2026-04-19 |
| 0008  | v0.5 — strategy-event journal schema (Q1)          | accepted   | 2026-04-19 |
| 0009  | v0.5 — registry concurrency: parking_lot::RwLock (Q2) | accepted | 2026-04-19 |
| 0010  | v0.5 — ComposedStrategy exit policy: signal-flip only (Q3) | accepted | 2026-04-19 |
| 0011  | v0.5 — cockpit Strategies panel: right column (Q4) | accepted   | 2026-04-19 |
| 0012  | v0.5 — strategy broadcast types in trading_core (Q5) | accepted | 2026-04-19 |
| 0013  | v1 — cross-sectional momentum resolutions (Q1–Q6)  | accepted   | 2026-04-29 |
| 0014  | v1.5a — mean-reversion pairs resolutions (Q1–Q10)  | accepted   | 2026-04-30 |
| 0015  | v1+ — Operator success reports (Q1–Q9)              | accepted   | 2026-05-01 |
| 0016  | v1+ — real-mtm unrealized PnL plumbing (Q1–Q8 + R10) | accepted  | 2026-05-02 |
| 0017  | v1.5b — multi-venue execution scaffolding (Q1–Q12)  | accepted   | 2026-05-03 |
| 0018  | Lumen design adoption — Phase 1 foundation (Q1–Q11) | accepted   | 2026-05-04 |
| 0019  | v2 — LLM strategy foundation (Q4–Q11) — ADR-0019 § Changelog 2026-05-29 amendment: v2.1 tracing-Layer redactor M-T1 ratified (REQ-V2-1-TRACING-LAYER-REDACTOR-001 close-of-pass-3-deferred-half) | accepted   | 2026-05-10 |
| 0020  | Chart buy/sell emphasis (v1.9 Q1–Q9)                | accepted   | 2026-05-10 |
| 0021  | RustQuant adopted as helper, not foundation         | accepted   | 2026-04-17 |
| 0022  | Cost telemetry lives in dedicated `cost` crate      | accepted   | 2026-04-17 |
| 0023  | iced is the single UI stack                         | accepted   | 2026-04-17 |
| 0024  | Audit ledger: raw `sqlx` + SQLite, not `sqlx-ledger`| accepted   | 2026-04-19 |
| 0025  | v0 hand-rolled Binance WS behind trait              | accepted   | 2026-04-17 |
| 0026  | v0 simple paper engine; LOB deferred                | accepted   | 2026-04-17 |
| 0027  | v2.5 — Kronos foundation-model forecast overlay (ONNX + tract) | accepted | 2026-05-16 |
| 0028  | v2.5 — DL forecast overlay trained in `candle` (supersedes 0027) | accepted | 2026-05-16 |
| 0029  | v2.5 — Forecast-checkpoint provenance schema + LFS-anchor strategy | accepted | 2026-05-17 |
| 0030  | Cockpit calls the backtest engine in-process via tightened API | accepted | 2026-05-17 |
| 0031  | `AuditTick<Event, Context>` consumer envelope for audit ledger read path | proposed | 2026-05-17 |
| 0032  | Backtest real-Binance-data path + REVISION.toml data-revision pin | accepted | 2026-05-18 |
| 0033  | v2.5 TCN alpha-investigation report shape + F-verdict algorithm | accepted | 2026-05-18 |
| 0034  | Cockpit training control — audit-DB-as-seam, subprocess lifecycle, R6 in-panel curves | accepted | 2026-05-19 |
| 0035  | Post-training σ_train recalibration via metadata overlay (v2.5 cross-phase contract) | accepted | 2026-05-21 |
| 0036  | PatchTST training contract — patch-embed shape, σ_train post-training, candle attention determinism gate, cost tripwire (v2.5a) | proposed | 2026-05-21 |
| 0037  | Phase B scenario-dispatch extraction — renumbered 0035→0037 to resolve number collision with ADR-0035-tcn-sigma-train-recalibration (audit-2026-05-22) | accepted | 2026-05-19 (number reassigned 2026-05-22) |
| 0038  | v3 vol-forecast V-verdict report shape + GARCH(1,1) baseline contract (parallel to ADR-0033, not extension) | accepted | 2026-05-22 |
| 0039  | LLM-forecaster verdict criteria L0-L4 (parallel to ADR-0033 § D3 and ADR-0038 § D1, not extension) | accepted | 2026-05-22 |
| 0040  | Yahoo realdata path + revision pin (Lab dispatch source) — generalises ADR-0032 to a second data source; engine stays source-agnostic per Q1=(b); 34/34 anchors byte-identical. § Changelog 2026-05-30 (lab-yahoo-empty-range-ux v0.1.0): additive `YahooError::NoDataForRange` variant (HTTP-200 + 0-quote only, K1-correct) + future-dated `end_ms.min(now)` clamp + UI typed notice-vs-error decode; no new ADR; ADR-0050 untouched; anchors byte-identical | accepted | 2026-05-30 |
| 0041  | Trader crate split — reflection-memory consumer moves out of strategy into new `crates/trader/`; structurally enforces R8.1 / R10.8 layering invariant; pure package-level refactor (additive-zero anchors) | accepted | 2026-05-26 |
| 0042  | Cockpit activity broadcast — 10th `EventBus` channel + RAII `ActivityHandle` for in-flight-work tape (extends ADR-0012); Q1=(a) broadcast-bus over (b) tracing-layer / (c) per-source polling; capacity-256 lossy ring; 100 ms producer-side throttle; in-memory only (audit ledger remains source of truth); 34/34 anchors byte-identical | accepted | 2026-05-26 |
| 0043  | Simulated network latency + order-book slippage in backtest — D1 always-on code path with default-zero noop (rejects Cargo feature flag); D2 seeded `ChaCha20Rng` sub-stream keyed on `(scenario_seed, order_id)` for replay determinism; D3 linear bps slippage at v0.1.0, defer square-root to v0.2.0; D4 NEW `AuditEvent::SimulatedExecMetrics` variant with skip-when-zero guard; D5 backtest-only scope (live mode untouched); 34/34 anchors byte-identical at v0.1.0 ship via R-NR.1 hard gate | accepted | 2026-05-26 |
| 0044  | Activity-aggregator producer pattern — audit-ledger-writes producer with 100 ms aggregation envelope (extends ADR-0042); D1 placement at `crates/agent/src/activity_audit_aggregator.rs` sibling of `activity.rs` (producer cohesion) over `crates/ui/` (UI is subscriber) / `crates/audit/` (R-NR.1 zero-change contract); D2 internal shape (broadcast::Receiver + AtomicU32 + tokio::time::interval + long-lived ActivityHandle); D3 100 ms cadence verbatim from ADR-0042 § D1.4 (status-bar render budget unchanged); D4 separate-handle Failed emission (NOT main-handle fail() — don't taint successful writes red); D5 idle-end semantics (long-lived handle; drop on first empty window for free "audit active / quiet" boolean); 34/34 anchors byte-identical by construction | accepted | 2026-05-26 |
| 0045  | v5 canonical-config + noop-baseline namespace strategy (v5 v0.2.0 anchor migration) — D1 medium config `LatencySlippageSimConfig { latency_ms_min: 30, latency_ms_max: 80, slippage_bps: 8 }`; D2 two-namespace co-existence (`noop-baseline` historical oracle + `v5-realdata-medium-2026-05` canonical pin); D3 K1-surprise per-scenario flag for operator review; D4 mandatory cross-feature e2e re-check; D5 Sharpe-delta table as permanent regression gate. § Changelog 2026-05-30 amendment (engine-drift PAPERWORK finding, diagnosis `1cbe3d4`): D6 names the TWO anchor systems (FILE-anchors `verify_anchors.sh` + IN-TEST re-run `determinism.rs` `t622`/`t717`/`tt1`) and makes `spec/anchors.toml` the single source of truth — `determinism.rs` constants are a derived projection re-locked to the `v5-realdata-medium-2026-05` SHAs (NOT `noop-baseline`) for the no-feature default binary, and reconciling them is NOT a § D6.b re-emission; D7 closes the engine-drift blind-spot via D7.1 sub-second `check_determinism_anchors.py` static drift-linter (+ `--write` auto-sync), D7.2 run synthetic re-run determinism tests in release inside the `verify-anchors` gate (operator cost flag: < ~1 min release wall-clock on backtest-touching ships), D7.3 document dual model + keep `noop-baseline` (rejects option (c) regenerate-and-hash). § Changelog 2026-05-30 second-pass amendment (§ D6.3 + § D7.1b, engine-drift BLOCKER): the `v5-realdata-medium-2026-05` namespace is provenance-MIXED — the `btc-2023-1m-{macd-trend,rsi-reversion,bbands-mean-revert}` v5 rows were emitted from the REAL-DATA path (17544-bar Binance bodies) while sma/momentum/tt1 v5 rows are synthetic; the `determinism.rs` tests run the synthetic v0-fallback path (CWD=tempdir) so those 6 in-test constants are the SYNTHETIC SHAs held in D7.1's `SYNTHETIC_DETERMINISM_SHAS` override map (NOT `anchors.toml`); D6.3 refines the D6.1 mapping rule to synthetic-override→canonical→HARD-ERROR; D7.1b rejects adding synthetic anchors.toml rows (breaks `verify_anchors.sh`/grows immutable report set) and rejects exempting the 6 (reopens blind spot); no anchors.toml or report-file change, VR-3 stays 86/86 | accepted | 2026-05-30 |
| 0046  | Cockpit toast queue v0.1.0 — `VecDeque<ToastEntry>` cap 5 drop-oldest FIFO; stacked Lumen-card overlay in bottom-right via `iced::widget::Stack` above the 24 px activity tape; dual dismissal (shared 500 ms `ToastDismissRecipe` auto-timeout 5 s + per-card `×` button); clock injection via `Message::ToastTick(Instant)` (no AppState clock field); severity tokens map to existing Lumen palette (FG_2 / UP_500 / INFO_400 / DOWN_500) — zero new tokens; back-compat `toast_message()` method shim retained one cycle; 69/69 anchors byte-identical (UI-only) | accepted | 2026-05-27 |
| 0047  | v5 v0.3.0 full-path wiring + namespace-aware Rust resolver — D1 K2-REACHABLE-CHEAP verdict (~5 LoC `--force-synthetic-bars` CLI flag); D2 per-path plumbing contract for 6 unwired strategies (SmaComposed, TcnOverlay, PatchTstOverlay, Pairs, VolTargetOverlay, GarchVolOverlay; ThresholdSweep deferred — no equity surface); D3 namespace-aware Rust resolver in `crates/reports/tests/strategy_anchors_unchanged.rs` (mirrors `verify_anchors.sh` v0.2.0 pattern); D4 conditional Q1 re-emission (operator chose route (a) revert-to-synthetic); D5 anchor namespace extend `v5-realdata-medium-2026-05` (Q3=(a) same pin); D6 e2e inventory unchanged (3 files) | accepted | 2026-05-27 |
| 0048  | lab-recipe-test-harness — Pattern (d) Combination: Surface 1 boundary-test of `spawn_lab_run` with `MockLabYahooBarSource` (catches sentinel emission + tokio::select! channel survival) + Surface 2 Stop-button gating state-machine test against `model.lab_run_inflight` (catches predicate-gated UI elements); D2 file:line locations at crates/ui/tests/{spawn_lab_run_yahoo_harness,lab_stop_button_gating}.rs; D3 scope 3 regression categories A/B/C; D4 visual regressions NOT caught; D5 per-feature M-FINAL gate for any UI Recipe touch; D6 anchor-additivity (channel-only events; 70/70 byte-identical). § Changelog 2026-05-29 amendments: v0.2.0 cross-surface extension carries D1-D6 verbatim to 7 additional Recipe surfaces (TrainingLogRecipe S1+S2, ActivityAuditAggregator S1, ServerTimeRecipe S2, ToastDismissRecipe S1+S2, TrailMirrorRecipe S2, ActivityRecipe S1, TrainingPoller S1+S2) + visual-fail-html-reporter v0.1.0 inherits forensic-artifact emission pattern from D6 (HTML alongside PNG triple on FAIL only) + ui-test-harness-viewport-matrix v0.1.0 inherits D1-D6 for 22 in-scope visual-fixture viewports + ui-contrast-asserter v0.1.0 inherits D1-D6 boundary-test shape for WCAG 2.1 contrast assertion (Surface 1 = `contrast_ratio` pure-fn over 83-entry PAIRS table; Surface 2 = `ContrastClass { Body, Equity, OptOut(reason) }` dispatch; MIN_PAIRS=60 floor + reference-vector tests; UI_CONTRAST_MODE=warn|gate env var default warn at v0.1.0 → gate at v0.2.0 after operator confirms 2-week WARN observation; 2 GENUINE WCAG-AA defects surfaced by dry-run audit are signal-of-record, not opt-outs). | accepted | 2026-05-28 |
| 0049  | v3-regime-classifier Markov-switching verdict shape — D1 model class Markov-switching regression with operator-set semantic priors {Bull μ>0 σ²low, Bear μ<0 σ²low, Volatile μ=0 σ²high, Calm μ=0 σ²low} + Baum-Welch refinement; D2 4-state K4 contract option (γ) preserve Chop + APPEND Volatile=3, Calm=4 with EmbeddingV1/V2 escape hatch; D3 dispatcher integration with degenerate CashHoldStrategy for Volatile/Calm (SUPPRESSION not LIQUIDATION); D4 verdict shape V-PASS / V-MARGINAL / V-FAIL sibling to ADR-0033; D5 anchor namespace v3.0.0-regime; D6 K-reg-2 mitigation via max_regime_confidence ≥ 0.70 threshold | accepted | 2026-05-28 |
| 0050  | iced-tokio runtime-context contract and cooperative cancellation primitives — D1 (corrected): rt.spawn() for HTTP/transitive tokio; rt.enter() guard only for K8 timer-construction pattern; D2: CancellationToken + abort(); D3 (amended): HTTP/spawn_blocking path test required (not just timers); D4 (new): HTTP/reqwest MUST use rt.spawn() — rt.enter() insufficient for GaiResolver DNS spawn_blocking; codified on Bug #64 D.1.1 recurrence #3; e2e gates: lab_runner_ticker_e2e.rs + lab_runner_cancel_e2e.rs + lab_runner_http_offexecutor_e2e.rs | accepted | 2026-05-29 |
| 0051  | Monte-Carlo robustness — sub-seed derivation, distribution-report shape, and anchor determinism (C1 path-gen + C2 robustness harness). D1 master→path sub-seed rule `path_seed_j = master.wrapping_add(j*0x9E37_79B9)` (reuses the per-symbol idiom on the path axis; bound to index not completion order; ensemble seed orthogonal to fill-tie-break seed which is held constant across paths — resolves direction-note § 8 open-Q2). D2 frozen reduction: index-order sequential mean/two-pass-std + `f64::total_cmp` sort + type-7 linear percentile + NaN-absent assertion (the f64 boundary). D3 distribution-report FM/body split + fixed-precision formatting (`{:.6}` ratios / `{:.2}%` drawdowns) + fixed metric order; every distribution input in the hashed body. D4 anchor unit = ONE summary report under new namespace `mc-robustness-2026-06` (rejects N per-path). D5 determinism scope = Apple-Silicon canonical box, inheriting ADR-0043 f64-conversion-boundary verbatim (cross-platform NOT contracted). Retired-line guard: GARCH/regime as generators-not-alpha; v0.1.0 ships only block bootstrap. § Changelog 2026-05-30 D6 amendment (C3 momentum-parameter-robustness-sweep θ-sweep extension): D6.1 SAME path-set across all θ-cells (`cell_seed_g := ensemble_seed` ⇒ `path_seed_{g,j}` byte-identical to C2 D1 ⇒ C1/C2 determinism unchanged by construction, 85 anchors hold; θ varied at config level not seed level; controlled-experiment anti-confounder; L θ-independent printed once = OQ-3); D6.2 REJECTS naive additive two-axis seed `+(g+j)*0x9E37_79B9` as a collision bug (6 colliding pairs in 4×3) — a future per-cell-independence variant needs its own ADR with a collision-free mix; D6.3 ONE θ-surface report = +1 anchor under `mc-robustness-2026-06` (85→86; cell-list + N + buy-hold-flag are hashed body fields); D6.4 inherits D2/D3/D5 verbatim (rows sorted by θ-cell index before render). Resolves C3 OQ-1/OQ-3/OQ-4 (amendment, not ADR-0052). § Changelog 2026-05-31 D6.5 amendment (cross-sectional-mean-reversion-strategy — 2nd strategy family): the family axis (Momentum vs Reversion) is varied at the config level (a `Direction` field on `CrossSectionalMomentumConfig`), NOT the seed level, so it is the 2nd instance of "vary at config level, seed untouched ⇒ determinism unchanged by construction" and inherits D6.1 SAME-paths verbatim (86 anchors hold by construction); MR = the v1 score negated at one cache-write line, anchored `top_k_long`+`score_vol_adjusted_return` reused verbatim (bottom-K on negated scores); REJECTED a separate MR struct (would force `run_path` generic/`dyn` → anchor risk; `run_path` takes a concrete `MomentumStrategy`) + REJECTED a new namespace; +1 MR θ-surface anchor under `mc-robustness-2026-06` (86→87, scenario `v1-mr-theta-surface-2023-block-bootstrap-real-fy`; grid+N+direction+buy-hold are hashed body fields). § Changelog 2026-05-31 D6.6 amendment (carry-strategy — a 2nd CO-RESAMPLED SERIES, a REAL-mechanism amendment not a pure cross-ref): carry consumes a different input (the funding rate) the bootstrap does not carry, so it co-resamples a SECOND time series under the SAME shared `idx_seq` as the returns — `funding_at_return[s][idx_seq[k]]` gathered at the same index that picks the return ⇒ funding↔price co-movement preserved (FP-C1.5 extended cross-series); the gather consumes ZERO new ChaCha20Rng draws (`idx_seq` already materialized) ⇒ SAME-paths determinism (D1/D6.1) holds trivially, no new determinism surface (the de-risk); a naive timestamp forward-fill onto the SYNTHETIC bar ts would WRONGLY decouple price/funding. D6.6.2 additive/defaults-absent: NEW `GeneratedPath.funding_by_symbol: Option<Vec<Vec<Decimal>>>` + `TcnScenarioInput.funding_override: Option` (mirrors `bars_override`) + `ScoreSource { VolAdjustedReturn (default), FundingCarry }` serde-default enum ⇒ 87 anchors (momentum #86 `0dd989d9…`, MR #87 `a708112e…`) byte-identical by construction; `run_path` stays CONCRETE. D6.6.3 REJECTED extend-`Bar` (option (i), changes bootstrap output shape → anchor risk) + REJECTED separate `CarryStrategy` struct (option (iii), the exact D6.5.2 trap — forces `run_path` generic/`dyn`). D6.6.4 +1 carry θ-surface anchor under `mc-robustness-2026-06` (87→88, scenario `v1-carry-theta-surface-2023-block-bootstrap-real-fy`; grid+N+score_source+funding-revision-SHA `bf1ede44…` are hashed body fields; one additive realized-funding column gated to carry reports; per frame-diagnostic E1 a 2024-FY carry-C3 surface is a day-1 gating read, anchor #89 optional/tester's call). D6.6.5 D2/D3/D5 inherited verbatim; funding cashflow is `Decimal` (ADR-0003). Generalizes the shared-index bootstrap to N co-resampled series under one index. Amendment, NOT ADR-0052. § Changelog 2026-06-02 D6.7 amendment (time-series-momentum-robustness — the FIRST non-cross-sectional family; a 2nd SELECTOR varied at the config level): the 3 cross-sectional families share rank→top-K (`top_k_long`); TS-momentum has NO ranking, so it adds a 2nd selector `select_above_threshold` (long every warmed asset whose OWN raw-trend score > entry_threshold, flat the rest; variable cardinality 0..N) gated by a NEW `SelectionMode { CrossSectionalTopK (default), TimeSeriesLongFlat }` serde-default enum + a `#[serde(default)] entry_threshold: Decimal` (default ZERO); the 3rd "vary-at-config-not-seed ⇒ determinism unchanged by construction" instance (MR=D6.5, carry=D6.6), STRICTLY simpler than carry — NO new data source, NO co-resampled series, NO new RNG. D6.7.1 the `TimeSeriesLongFlat` score = RAW cumulative log-return over L (`score_trailing_log_return`, NO vol-norm — the band is a no-trade threshold on the trend); existing VolAdjustedReturn/FundingCarry branches byte-untouched. D6.7.2 the variable-cardinality long/flat is PURE `on_bar` signal emission — `run_path`/`PaperEngine`/`bootstrap` byte-UNTOUCHED (the engine already sizes 0..N Buys at a fixed fraction + goes to cash on 0 Buys); sizing LOCKED to the existing fixed-fraction-per-name (NOT a 1/N rescale — engine edit + breaks apples-to-apples). D6.7.3 additive/defaults-off ⇒ the 89 anchors (momentum #86 `0dd989d9…`, MR #87 `a708112e…`, carry #88 `f03cd714…`, carry #89 `fd96d5a8…`, all pre-existing) byte-identical by construction; a separate `TimeSeriesMomentumStrategy` struct REJECTED (the D6.5.2 trap). D6.7.4 +1/+2 TS θ-surface anchors under `mc-robustness-2026-06` (90 for 2023 `v1-ts-momentum-theta-surface-2023-block-bootstrap-real-fy`, 91 for 2024 the durable choice); LOCKED 6-cell grid (lookback {24,168,720 bars} × entry_threshold {0.00,0.02}, § D-TSM.3-LOCKED) + N + `selection_mode` + `entry_threshold` are hashed body fields (K3); one additive time-in-market column gated to TS reports. D6.7.5 D1/D2/D3/D5/D6.1 inherited verbatim; no new RNG (the de-risk); `select_above_threshold` is a deterministic `BTreeMap`-ordered pure fn ⇒ two-run byte-identity by construction. Amendment, NOT a new ADR. § Changelog 2026-06-03 D6.8 amendment (horizon-retest-robustness — a HORIZON/DATA-PATH change varied at the LOAD + the CALCULATOR level, NOT a new family; the 4th anchor-additive instance after MR=D6.5/carry=D6.6/TS=D6.7): coarser decision cadence (4h + daily) on the SAME 10-symbol coins. D6.8.1 THE LOAD-BEARING fix (R-HR.LOAD, REGRESSION-blocked, the sharpest anchor-risk in the program): `compute_sharpe_hourly`/`compute_sortino_hourly`/`compute_calmar` (1h-baked √8575 Sharpe + /8760 Calmar; a coarse bar through them inflates Sharpe ≈2.0× @4h / ≈4.9× @daily — a silent false-ROBUST) are kept BYTE-VERBATIM + three NEW siblings `compute_*_periodic(equity, periods_per_year: f64)` annualize 4h/daily by `√ppy`; parameterizing the existing fns REJECTED (ULP-change → 91-anchor REGRESSION) + a `Timeframe`-arg signature REJECTED (hides the year-aware leap-year ppy). `periods_per_year(horizon, year)` YEAR-aware (4h 2190/2196, daily 365/366; F-HR.2 asserts √2190/√365); the sweep picks `if horizon==1h { *_hourly /*VERBATIM*/ } else { *_periodic(ppy) }` at BOTH metric call-sites (per-cell + BH). The 1h path's two inconsistent constants stay verbatim; the periodic fns are mathematically correct (no √8575 quirk propagated — 4h/daily are new, no anchor binds them). D6.8.2 `resample_ohlcv` = a pure ORDERED Decimal fold (open=first/high=max/low=min/close=last/vol=Σ, UTC integer-division bucketing 14_400_000/86_400_000, single pass over `open_ts`-sorted input, NO HashMap) wired post-`merge_symbols` in `load_real_bars`; `horizon==1h` → IDENTITY pass-through (1h load path byte-untouched); the coarse `bar_count` = `1h_count/{6,24}` (exact integers) threads into the UNCHANGED bootstrap. D6.8.3 OQ-CARRY-SEM resolves for free: the timestamp-driven funding as-of join keys off the COARSE bar `open_ts` after the resample-first ordering (apples-to-apples with carry #88/#89, NO `funding_data.rs` edit); L re-picked as a coarse-bar ring count; funding co-resamples under the SAME shared `idx_seq` (D6.6). D6.8.4 OQ-BOOTSTRAP-TF: bootstrap ladder cosmetically 1h (correctness-safe; renderer prints the REAL horizon, a hashed field); CONSEQUENCE — `is_rebalance_bar` measures wall-clock minutes on the 1h ladder, so "every coarse bar" = `rebalance_minutes ≤ 60`, "every 2nd" = 120 (grid-encoded per cell). D6.8.5 additive/defaults-off (`--horizon` defaults `1h`) ⇒ the 91 anchors (momentum #86 `0dd989d9…`, MR #87 `a708112e…`, carry #88 `f03cd714…`, carry #89 `fd96d5a8…`, TS #90/#91, all pre-existing) byte-identical by construction; `run_path`/`PaperEngine`/`bootstrap` byte-UNTOUCHED. D6.8.6 NEW namespace `horizon-retest-robustness` (NOT `mc-robustness-2026-06` — a distinct experiment axis); + up to 8 anchors (TS + carry × 4h + daily × 2023/2024); LOCKED grids (TS-4h {42,180,540}×{0.00,0.02}, TS-daily {5,20,60}×{0.00,0.02}, CARRY-4h L∈{2,6,12}, CARRY-daily L∈{1,3,7}, § D-HR.4-LOCKED) + N (200 @4h, 1000 @daily) + horizon + funding-revision-SHA are hashed body fields (K3). D6.8.7 D1/D2/D3/D5/D6.1 inherited verbatim; no new RNG; the resampler is a deterministic ordered fold ⇒ two-run byte-identity (F-HR.5) by construction. Amendment, NOT a new ADR. § Changelog 2026-06-05 D6.9 amendment (perp-basis-signal-robustness — the FIRST LIVE signal of the post-OHLCV program: a perp-spot BASIS-REVERSAL arm with a SWEPT FEE AXIS; the 5th anchor-additive instance after MR=D6.5/carry=D6.6/TS=D6.7/horizon=D6.8): cross-sectional basis rank (IC −0.08..−0.11 over L=60-168, NEGATIVE + sign-stable both years), tilted AGAINST (long the lowest-basis names). D6.9.1 a single `ScoreSource::BasisReversal` arm (serde-default stays `VolAdjustedReturn`) with the SIGN baked into the score (`basis_reversal_score = −trailing_mean(basis)`, the carry-twin, the minus in ONE place guarded by the day-1 sign-assertion falsifier — a flip = a basis-MOMENTUM payer → RED; option (i) ScoreSource::Basis+Direction::Reversion REJECTED, splits the sign across two fields); NO cashflow — carry's accrual Sub-D does NOT transfer (the basis is a SELECTION signal, the `run_path` accrual block stays gated `None`, P&L is pure price-of-selection; the non-no-op guard is the SELECTION collapse, not a cashflow collapse). D6.9.2 the sidecar REUSES the `funding_by_symbol` co-resample channel (a GENERIC `Option<Decimal>` sidecar in its plumbing, funding-specific only in NAME; basis + funding mutually exclusive in v0.1.0 → ZERO new bootstrap/`GeneratedPath`/`TcnScenarioInput` field, smallest blast radius; a sibling `basis_by_symbol` field REJECTED for v0.1.0 as +~15 default-None sites/+anchor-risk); ZERO new RNG draws (the D6.6 proof transfers — `basis_at_return[s][idx_seq[k]]` at the same index that picks the return); the 8h→1h forward-fill is an IDENTITY at 1h (the as-of join is `basis_close[t-1]`, a one-bar lag). D6.9.3 the NEW `--taker-fee-bps`/`--slippage-bps` axis (THE LOAD-BEARING R-BR.LOAD fee-sweep gate — a reversal arm is the most fee-sensitive class; dies-net-of-realistic-fees IS the verdict, retiring the derivatives-positioning family) modeled on the D6.8 `--horizon` axis: the fee hardcoded at `param_robustness_sweep.rs:2409-2410` (`slippage_bps: 2, taker_fee_bps: 4`) → a CLI flag defaulting `2`/`4` (the `MatchConfig` byte-identical for every non-basis run) threaded into the per-path input, rendered as a hashed body row GATED `score_source == BasisReversal` (every existing body-SHA byte-identical; the fee level part of the basis anchor's hashed identity so {0,2,5,10} bps are four DISTINCT anchors); mechanism (i) one-surface-per-fee RATIFIED, mechanism (ii) fixed-pin + best-cell report REJECTED (reads the fee axis at the argmax the frozen §0 rule forbids crowning). D6.9.4 a NEW namespace `perp-basis-signal-robustness` (NOT `mc-robustness-2026-06`; the horizon D6.8 precedent) + the additive `verify_anchors.sh` `elif` after the horizon branch; + up to 8 anchors (4 fee × 2 regime; tester locks at M-TEST PASS, {0,5}×{2023,2024}=4 minimum); scenario `v1-basis-reversal-fee{NN}bps-theta-surface-{year}-block-bootstrap-real-fy`; the LOCKED 6-cell grid (L∈{24,60,168} BARS × K/cadence, rebalance default 8h, g3 24h, SKIP L=720 noise, §D-BR.2-LOCKED) + N (200) + the fee ladder + the basis-revision SHA `aa72409a…` are hashed body fields (K3); wall-clock ~16 min for all 8 surfaces — TRACTABLE. D6.9.5 D1/D2/D3/D5/D6.1 inherited verbatim; `run_path`/`PaperEngine`/`BlockBootstrapPathGen` byte-UNTOUCHED; the basis loader `basis_data.rs` is a near-mirror of `funding_data.rs` (pin `aa72409a…`, Decimal parse of signed `basis_close`). Amendment, NOT a new ADR. § D6.10 body section + Changelog 2026-06-07 (perp-basis-mn-spread — the SHORT-SIDE ENGINE: the FIRST `run_path` touch since C2 + a SECOND simultaneous co-resampled sidecar; the 6th anchor-additive instance after MR=D6.5/carry=D6.6/TS=D6.7/horizon=D6.8/long-only-basis=D6.9): the market-neutral basis-spread (long-low / short-high, dollar-neutral, beta-stripped) — the pre-registered follow-on after D6.9 closed FAMILY-UNIFORM-FRAGILE at every fee incl. 0bps gross (a VEHICLE verdict: full beta + only the long leg, vs the +1.74 BH bar; fees falsified as the killer). D6.10.1 (Q-MN-1) the short-side engine = a `k_short`-GATED BRANCH inside the EXISTING `run_path` (keeps the CONCRETE `MomentumStrategy` signature — NO dyn/generic, the §D6.5.2 trap avoided; sibling `run_path_long_short` REJECTED — duplicates solvency logic = correctness risk + a 3rd call-site); the short SOLVENCY model (a short's loss is unbounded above, the long Bug-B cap has no analogue): symmetric dollar-neutral notional (½ long/½ short, net ≈0, NO 1/N rescale), a short INITIAL-MARGIN gate (`margin = notional/max_leverage`, `max_leverage=dec!(1)` LOCKED), + a deterministic MAINTENANCE-MARGIN LIQUIDATION rule (equity < `maintenance_margin_frac=dec!(0.5)`·gross-short-notional → force-close all shorts at mark, a `liquidations` counter) bounding the loss; equity math UNCHANGED (`Σ qty·mark` already handles `qty<0`). D6.10.2 (Q-MN-2, THE load-bearing gate) the `run_path` anchor-neutrality RE-PROOF: by-construction (every short statement gated `if k_short>0` or reached only with a `qty<0` position → dead code when `k_short==0`, executed path = byte-for-byte HEAD's `run_path`) + a NEW `run_path_k_short_zero_byte_identical_to_head` unit test (mirrors `funding_override_none` neutrality) + the hard `verify_anchors.sh`→107/107 gate run FIRST (M-DEV-0 floor) and after EVERY seam. D6.10.3 (Q-MN-3) a SECOND SIMULTANEOUS sidecar: the D6.9 channel-reuse is RETIRED (basis + funding BOTH live — signal is basis, short PAYS funding); the sibling `basis_by_symbol` D6.9.2 deferred is now OWED + added (`GeneratedPath.basis_by_symbol` + `BlockBootstrapPathGen.basis_at_return` + `with_basis(…)` + `TcnScenarioInput.basis_override`, all funding-field twins, co-resampled at the SAME `idx_seq` — the three-series shared-index extension D6.6.5 anticipated; ZERO new RNG; `None` for every non-MN run → ~37 default-None sites, the 107 hold by construction). D6.10.4 `SelectionMode::LongShort` (serde-default stays `CrossSectionalTopK`, the D6.7 precedent) + `bottom_k_short` (the `top_k_long` mirror, lowest-K scores = highest-basis = the shorts) + the `config.rs:308` `k_short>0` reject GATED (permitted ONLY under `LongShort`). D6.10.5 (Q-MN-4) the basis⊥funding arm is RANK-BASED Decimal-EXACT (residual = `rank(basis) − rank(funding)`, pure integer arithmetic, NO division → no rounding/float, bit-reproducible; it IS the correct statistic — the −0.10 is a RANK IC); a Decimal OLS slope REJECTED (non-terminating Decimal division = hidden determinism surface, ADR-0003/D2 forbid). D6.10.6 (Q-MN-5) a NEW namespace `perp-basis-mn-spread` + the additive `verify_anchors.sh` elif after the `perp-basis-signal-robustness` branch; scenario `v2-mn-{arm}-fee{NN}bps-theta-surface-{year}-block-bootstrap-real-fy` ({arm}∈{basis,funding,basisperp}; {NN}∈{00,05}); the 3-arm headline on the SAME paths; θ-axis L∈{60,168} bars (L=24 DROPPED, L=720 SKIPPED), K_long=K_short=3 LOCKED; fee axis {0,5}bps ONLY (the R-BR.LOAD minimum, reuses the D6.9.3 flags); null = dollar-neutral ≈0 cash NOT buy-and-hold (the ONE frozen-rule change, a CORRECTION); 12 surfaces × 400 paths = 4,800 runs ≈ ~12 min TRACTABLE; + up to 6 anchors (3 arms × {0,5}bps on 2023; +6 with 2024; min = 3-arm × 0bps × 2023 = 3), tester locks at M-TEST PASS; LOCKED grid + N + fee ladder + margin constants + BOTH revision SHAs (basis `aa72409a…` + funding `bf1ede44…`) hashed. D6.10.7 D1/D2/D3/D5/D6.1 inherited verbatim; no new RNG; two additive MN-gated report columns (net-dollar-exposure ≈0, liquidations) → every existing body-SHA byte-identical. Build ~5–8 dev-days (the short-side engine + the FIRST run_path anchor re-proof are the dominant risk, NOT the cost model — the short-leg funding accrual ALREADY EXISTS at `montecarlo.rs:322-373`, only line 350's `continue` skip gates it). Amendment, NOT a new ADR. | accepted | 2026-06-07 |
| 0052  | Durable live equity series — D1 the series lives in the audit SQLite ledger (new additive `equity_snapshots` table) reached through a `LiveEquityStore` trait (every-external-I/O-behind-a-trait); flat sidecar file + agent-owned JSON REJECTED (second/bespoke writer, weaker retention). D2 the write is MINTED in the agent at the per-bar site (reconciler `after_bar_close` + `spawn_research_trading_loop`), NOT the UI, GATED `config.mode != Research` (the single load-bearing duplication-prevention line — research replays repeating 2023 `bar_ts` so persisting overlaps junk), per-bar FIRE-AND-FORGET (write error logs+continues, never blocks/panics the loop, the `bus = None` backtest tolerance); headless `trading` bin persists too. D3 migration `013_equity_snapshots.sql` is purely additive `CREATE TABLE IF NOT EXISTS` (the `010` precedent) — no ALTER/backfill/UPDATE — columns `(id, ts, bar_ts, as_of, total_equity, cash, realized, unrealized, mode)`, money Decimal-as-TEXT (ADR-0003), ts RFC3339-micros (ADR-0004); the 19 backtest anchors byte-safe BY CONSTRUCTION (backtest reconciler `bus = None`, never touches the table); adds NO `anchors.toml` row. D4 boot hydrate via `audit::query::equity_snapshot_tail` → `Task::perform` → a NEW batch `Message::PnlHydrated(Vec<…>)` arm seeding `live_equity_buffer` through `push_live_equity_point` (x-coord = persisted `bar_ts`); the `as_of` delivery-guard reconciliation SEEDS `live_equity_last_as_of` from the MAX hydrated `as_of` so the first live snapshot (`now()` ≥ max) still lands while a duplicate historical re-delivery is dropped; backwards-clock edge ACCEPTED + documented (bounded, cannot corrupt the `bar_ts`-keyed series; an op fault for host monitoring, not the UI guard); reconciler-replays-tail-through-bus + N×`PnlRefreshed` + leave-guard-None all REJECTED; ≥2 rows → KPI `Ready` (clears the `is_all_absent` ≥2-pt trap); query `LIMIT ≤ 2880`. D5 Total-return = SINCE-INCEPTION (first buffered point) with an honest `LIVE_*` caption (`LIVE_SESSION_RETURN_CAPTION` precedent), no new `core` math; retention = age/row-capped `DELETE WHERE ts < …` purge mirroring the nightly backup, aligned with the 30-day ledger-snapshot horizon. Not a strategy/sizing feature → baseline-divergence e2e gate N/A. THE gate is the render harness `live_equity_render.rs` (AC6 hydrated-boot rasterizes a non-empty ACCENT polyline, zero live snapshots; AC5 post-hydrate live append still lands). Resolves feature Q1–Q7. | accepted | 2026-06-11 |
| 0053  | Unify the per-bar trading loop across research and paper (feature `paper-mode-equity-wiring`). D1 ONE feed-parameterized `spawn_trading_loop(feed, …, equity_store: Option<Arc<dyn LiveEquityStore>>, mode_label, …)` for research AND paper — rename/generalize `spawn_research_trading_loop` (verified mode-agnostic + feed-parameterized at runtime.rs:946, 2 call sites: research arm + paced-replay test); shared-inner-body seam (b) = if-budget-tightens fallback, full duplication (c) REJECTED as the literal CLAUDE.md "computed-but-never-applied" anti-pattern. D2 the loop gains a `LiveEquityStore` param + persists loop-direct per bar, gated by `Some(store)` (the mode gate is STRUCTURALLY at the caller — research passes `None`/the existing `drop(equity_store)` semantic, paper passes `Some` — identical in spirit to ADR-0052 D2), fire-and-forget (A6), reusing `reconciler::build_snapshot_row` promoted to `pub(crate)`; ONE per-bar writer, no double-mint. D3 the live closed-bar stream is the cadence (mark `bar.close`), the `drop(state_tx)` idle-reconciler stub (runtime.rs:655-683) is REMOVED, the paper reconciler's MINT role retires (imbalance-only is a named follow-on, not wired v0.1.0), no free 60 s marking timer, no intra-bar tick marking (a follow-on). D4 research byte-stability discharged by NAMED guards: the paced-replay late-subscriber test (signature update is compile-enforced), the research integration tests, `equity_store_integration.rs` AC2 (research store=None → 0 rows), and the anchor gate's structural independence from `runtime::run` (107 anchors byte-safe by construction, backtest never calls the loop); anchor-ADDITIVE, no `anchors.toml` row, no anchor-SHA mutation. D5 the CLAUDE.md baseline-equity-divergence gate's INTENT APPLIES (this ADDS a strategy/sizing/exec decision into paper mode, unlike ADR-0052's genuine N/A) and is SATISFIED — NOT stamped N/A — by the divergence's two halves: AC1 data-layer (per-bar `total_equity` NOT all equal + persisted rows non-constant) + AC6 render-layer (`(max_y-min_y) ≥ CURVE_Y_VAR_MIN` ACCENT bbox HEIGHT — the ONLY discriminator since the `equity_curve.rs:178` flat-line guard centers a degenerate series as a full-width line that PASSES count/x_span — plus a self-proving FLAT contrast). D6 no live exec client in the paper loop (in-process `PaperEngine` seed `0x00C0_FFEE`, fill ts = `bar.close_ts`, no `SystemTime::now()` in the decision path); paper trades the configured registry `SmaCrossover`. Carries the per-bar equity path forward to the eventual live-money path. Resolves feature Q1–Q6. | accepted | 2026-06-11 |
| 0055  | Lab run persistence topology & anchor-safety (feature `lab-run-save-compare`, closes the `run_scenario` Phase-C deferral at `engine.rs:608`). D1 Lab reports live at a git-ignored `lab-runs/<strategy-slug>/reports/backtest-<stamp>-<scenario>.md` at the WORKSPACE ROOT — the `<slug>/reports/backtest-*.md` shape identical to `spec/` so both existing loaders read it with only a ROOT change. D2 anchor-safety is BY CONSTRUCTION: `/lab-runs/` added to `.gitignore` (the `/data/*` precedent) → `verify_anchors.sh` (which only `find`s under `spec/`) is structurally blind to it → stays 119/119; no `anchors.toml` row, no body-SHA touched (the git boundary IS the byte-immutability guarantee, ADR-0038 § D6). D3 write seam = a `reports_dir: Option<PathBuf>` on `ScenarioConfig`, anchor-additive `#[serde(default)]`/struct-update (the `ScenarioDataSource`/`latency_slippage_sim` precedents → the 34 anchor-generating constructors byte-safe), gated `cfg.write_report`; the Lab caller (`runner.rs`) supplies `Some(default_lab_runs_root())`, CLI/anchor paths pass `write_report=false` → bytes unchanged; tests point `reports_dir` at a tempdir (R3 I/O-behind-a-seam). D4 the body REUSES `report/<family>::write` VERBATIM (momentum/pairs/tcn/sma — the 4 single-symbol composed arms fed from `SmaComposedRunResult.state`+`.strategy_meta`, exactly as `main.rs:2109-2110`); body byte-identical to the CLI for fixed `(strategy,pair,range,seed,data_source)` (ADR-0030/0002); `generated:`/`wall_clock_s:` in frontmatter, stripped before hash; `elapsed_secs` frontmatter-only MAY be 0.0 on the engine path. D5 the Lab+Compare loaders read the UNION of two roots, `lab-runs/` FIRST then `spec/` (operator can diff a fresh run vs a committed anchored reference); within-tuple collision → most-recent `generated:` wins, identical filename across roots → `lab-runs/` wins; filename stamp is sub-second (millis) so fast successive runs never collide (the caller computes the filename stamp, independent of the writer's second-precision `generated:`). D6 H3 invariant write-root==read-root: `lab_run_engine.rs::h3_in_memory_equals_cached_disk` threads its tempdir into `reports_dir`, the engine writes `<tempdir>/<slug>/reports/…`, the test derives the read-root via `report_path.parent().parent().parent()` (==tempdir) → `get_or_load` parses the just-written report → element-by-element `in_memory==cached_disk`; the `report_path=None` skip (`lab_run_engine.rs:110-116`) goes dead. D7 retention bounded: keep last N=20 per `(strategy,symbol,range)` tuple, purge on completion (the audit SQLite `lab_runs` table is the FUTURE home IF queryable run metadata is needed — Markdown does not index well). Alternatives: SQLite-table-now (discards the Markdown loaders, larger build) / committed `spec/lab-runs/` (loaders' filter == anchor glob → cannot be both loader-readable and verifier-invisible under `spec/`) / `ReportSink` trait (more surface than v0.1.0 needs) / single-root (loses diff-vs-committed) — all rejected. Not a strategy/sizing feature → baseline-equity-divergence e2e gate N/A; AC6/H3 IS the persisted-vs-in-memory divergence guarantee. NO live trading. Numbered 0055 — 0054 burned in git history (removed `Mode::Live` ADR, `live-trading-removed-2026-06-12.md`). | accepted | 2026-06-12 |
| 0056  | Binance corpus timeframe-layout convention (feature `binance-corpus-expansion`) — the FORWARD lock so a daily v0.2 doesn't bake a wrong directory shape into a pinned corpus. D1 own-root-per-timeframe: each Binance timeframe is its OWN corpus root, the flat `<root>/<SYMBOL>/<YEAR>/<MM>.parquet` layout RETAINED, timeframe encoded by the root-dir NAME (a `-<tf>` suffix for non-1h roots e.g. `data/binance-2122-1d/`; bare roots `data/binance`/`data/binance-2122` are 1h BY CONVENTION; existing pinned roots are NEVER renamed). The interval stays advisory in `REVISION.toml [revision.metadata].interval` (not hashed). D2 Binance↔Yahoo layouts INTENTIONALLY diverge — the Yahoo `/<INTERVAL>/` segment of ADR-0040 § D3 is NOT mirrored onto Binance and NOT retrofitted either way: `ReplayFeed::parquet_files()` globs whatever lives under `<root>/<sym>/<year>/` with NO interval argument (a root IS a single universe×range×timeframe corpus, selected by root path), whereas `YahooBarSource::load_cached` takes an explicit `interval` and joins the segment (Lab adaptively picks cadence per ADR-0040 § D6). A Binance retrofit would either reshuffle `data/binance` → re-emit `REVISION.toml` → flip 3a8b96c4 → REGRESSION the four `-realdata` anchors, OR skew `ReplayFeed`'s glob to two shapes — both strictly worse. D3 ZERO `ReplayFeed` change; root-path selection is the only knob; the 34/119 anchor-bearing `-realdata` body SHAs are unreachable BY CONSTRUCTION (no code seam to forget, cf. ADR-0055 D2 — the git/path boundary is the byte-immutability guarantee). v0.1 `data/binance-2122/` is a flat 1h sibling root (mirrors the `binance-broaduni` precedent), correct+final under D1 — no rename ever owed. Daily v0.2 is unblocked as a NEW suffixed sibling root, fetched `--interval 1d`, own `REVISION.toml`, own `.gitignore` stanza, with zero reader change. Not a strategy/sizing feature → baseline-equity-divergence e2e gate N/A (no equity curve is produced — a data-fetch+layout feature); the gate's evidence-analogue is the AC1 recomputed-aggregate-SHA match + AC4 re-fetch determinism. 119/119 anchors byte-identical; no anchor SHA in scope → no anchor-mutation ADR required. | accepted | 2026-06-15 |
| 0057  | Cockpit visual-baseline determinism scope is the macOS canonical box (feature `cockpit-cross-platform`) — D1 the 56 PNG visual baselines in `crates/ui/tests/visual-baselines/` are macOS-CANONICAL; Linux/Windows render body text via cosmic-text `PlatformFallback` against the per-OS system-font DB (the cockpit sets NO iced default font + `fira-sans` is disabled → no embedded face, all text resolves per-OS) and are NOT byte-gated; extends the ADR-0043 § D5 / ADR-0051 § D5 "Apple-Silicon canonical box, cross-platform byte-identity NOT contracted" determinism scope to a THIRD artifact class — the UI render snapshot gate. D2 enforcement lives in SOURCE not CI YAML — the four snapshot integration-test files (`visual_snapshots.rs`/`render_snapshots.rs`/`panel_snapshots.rs`/`gallery_snapshots.rs`) each carry a file-level inner attr `#![cfg(target_os="macos")]` so off-macOS the whole file + its `#[path]`-included `fixtures` mod compile to nothing (skipped, NEVER re-baselined); the source gate IS the CI filter (no test-name `--skip` needed → removes the "filter drifts out of sync with the test set" failure mode); per-`#[test]` gating + CI-filter-only + fold-into-ADR-0051-Changelog all rejected. D3 cross-platform visual regression is a v0.2 follow-on gated on H1 (`fira-sans` + a pinned `default_font` making renders font-deterministic) needing its OWN canonical box + baseline set; any such feature MUST supersede/amend this ADR, never re-baseline macOS onto another OS (the H1 enable-now path rejected for v0.1 as a baseline migration violating R-NR.1 byte-identity). Touches no backtest/exec/cost bytes (119 anchors unaffected) + no byte-immutable report (ADR-0038 § D6 safe). | accepted | 2026-06-15 |
| 0059  | Bake-off orchestrator home, `BakeoffReport` seam, and the buy-and-hold `run_scenario` arm (feature `advisor-bakeoff-ranking` — the 2026-06-19 single-coin-advisor pivot F1+F2). D1 orchestrator home = `crates/backtest` (new `bakeoff` module): it already owns `run_scenario` + the `stats` layer + depends on `data`'s `BlockBootstrapPathGen` + `strategy`, AND `ui` ALREADY depends on `backtest` + consumes `run_scenario`/`RunReport` via `crates/ui/src/lab/runner.rs` ⇒ the bake-off result is consumable through the IDENTICAL sanctioned seam with ZERO new `ui` dep, preserving ui-never-imports-strategy/exec/forecast/llm BY CONSTRUCTION; `crates/agent` home REJECTED (`ui` lists `agent` only as optional/dev → would force a hard `ui→agent` edge); a new `crates/advisor` REJECTED for MVP (premature for one loop + one comparator). D2 the seam is `backtest::bakeoff::BakeoffReport` (Debug+Clone): `{request, candidates: Vec<CandidateResult>, ranked: Vec<usize>, crowned: Option<usize>, rationale: Recommendation}`; `CandidateResult` = {strategy id, is_benchmark, CandidateKpis(Sharpe/Sortino/Calmar/total_return/max_dd/trade_count), equity_curve, Option<RobustnessFlag>}; the rationale is a STRUCTURED `Recommendation` data type (RecommendationOutcome enum {ActiveWins,BenchmarkWins,AllFragile} + ordered ReasonCodes + winner/benchmark KPIs) NOT a pre-rendered String — the UI owns the not-advice copy + the v0.2 LLM 'why this one' narration is an additive consumer; the cockpit mirrors it via the `RunReportMirror`/`RunSummary` precedent. D3 buy-and-hold becomes an ANCHOR-ADDITIVE `"v0.buyhold"` `run_scenario` arm (BH is NOT a run_scenario arm today — verified: it lives ONLY as `run_buyhold_path` in `bin/param_robustness_sweep.rs` + a read-only example) reusing the extracted `run_buyhold_path` (single-coin equal-weight = 100% held from bar-0 close, mark-to-market) so the loop is uniform (every candidate = one run_scenario call); additive `match` arm on a new id + `write_report=false` ⇒ no report body created/perturbed ⇒ `verify_anchors.sh` 119/119 byte-identical (run before+after). D4 `run_buyhold_path` + `classify_verdict` + `ParamRobustnessVerdict` + `P5_SHARPE_FRAGILE=0.0` MOVE from the sweep bin INTO the `backtest` library (behaviour-preserving relocation; bin output byte-identical; no threshold re-derived). D5 the comparator `rank_candidates(&[CandidateResult]) -> Ranking` is PURE/TOTAL/DETERMINISTIC: eligibility partition on `Fragile` (ineligible-to-crown iff robustness==Some(Fragile)) → Sharpe desc (`f64::total_cmp`) → total_return desc (Decimal) → max_drawdown asc (Decimal) → strategy id lexicographic (determinism backstop); crown = order[0], Fragile iff ALL fragile; BH ranked by the same rule, special-cased only in COPY (BenchmarkWins) when it wins; NO f64 arithmetic introduced (compares pre-computed anchored-stats metrics only → no new f64 boundary). 119/119 anchors byte-identical, no byte-immutable report touched, no `spec/anchors.toml` SHA changed. | accepted | 2026-06-19 |
| 0058  | Point-in-time as-of-join primitive — type-level `PitSeries` in `crates/core` (feature `point-in-time-data-discipline`) — consolidates the FOUR hand-rolled `partition_point(t≤bar_ts)` as-of joins (`funding_as_of` `funding_data.rs:378`, `basis_as_of` `basis_data.rs:397`, the f64 clones `basis_diag.rs:219` + the brief-missed FOURTH `stablecoin_diag.rs:301`) into ONE guarded API. D1 type-level `PitSeries<T>` + `AsOf<T>`: `as_of(query)→Option<AsOf<T>>` / `as_of_value(query)→Option<T>` return ONLY the most-recent record `ts ≤ query` (`None` warm-up); `AsOf` carries PRIVATE `as_of_ts`+`value`, has NO public ctor, no `get`/`Index`/future-iterator → look-ahead UNREPRESENTABLE at compile-time (runtime-guard `as_of_join`+`debug_assert!` REJECTED — debug-time-only + hand-rolled bypass). D2 home = `crates/core::pit`, the workspace BASE crate (backtest+data depend on it, it depends on neither — no cycle; already carries rust_decimal/time/thiserror/proptest/trybuild → ZERO new dep edge; ADR-0041 layering rule — a `data` home REJECTED as it'd force a wrong-direction backtest→data edge). D3 join key = transparent `TimestampMs(i64)` ms-since-epoch, NOT `core::Timestamp` (loaders join on raw i64 ms; a `Timestamp` round-trip truncates i128→i64 → anchor-delta risk; `i64` `Ord` preserves the `|&(t,_)| t≤q` predicate char-for-char). D4 BEHAVIOUR-PRESERVING migration: `funding_as_of`/`basis_as_of` keep PUBLIC signatures (callers/tests byte-stable), bodies build a `PitSeries` once + map `as_of_value`; `build_*_at_return` + their `Vec<Vec<Option<Decimal>>>` output (the `BlockBootstrapPathGen::with_funding`/`with_basis` input) UNCHANGED; same predicate, same empty-series fast-path, same `None` warm-up, `Decimal` MOVED with NO f64 round-trip ⇒ byte-identical as-of values ⇒ `verify_anchors.sh` stays 119/119 (developer re-verifies; no anchored `spec/*/reports/*.md` edited, no `anchors.toml` SHA changed → ADR-0038 §D6 + the anchor-mutation-requires-an-ADR rule untriggered). D5 verification floor = ONE shared `core::pit` falsifier (query causally, forward-shift the series, `assert_ne!`) + a `trybuild` compile-fail fixture proving no API returns `ts > query` (makes the guarantee STRUCTURAL — removing the guard fails a test) + KEEP the two per-loader `no_look_ahead_falsifier` as thin regression guards; the CLAUDE.md day-1 baseline-equity-divergence e2e gate is N/A (no overlay/sizing modifier — the correct outcome is equity UNCHANGED, so the gate would assert the opposite of the goal); the `scripts/` look-ahead lint DROPPED (type-level sufficient for the core join; an optional grep guard captured as a v0.2 follow-on, not built). extends ADR-0041. 119/119 anchors byte-identical; no byte-immutable report touched. | accepted | 2026-06-18 |
| 0060  | Budget-aware sizing modifier + forward-paper-run selection seam (feature `advisor-forward-paper` — the 2026-06-19 single-coin-advisor pivot F4+F5, MVP-closing). D1 F4 budget sizing lives in `crates/risk::sizing` as an ADDITIVE modifier: a `budget_cap: Option<Money<Usdt>>` field on `FixedFractionSizer` + a `with_budget_cap(fraction, budget)` ctor + a COMPOSED `Decimal`-exact budget clamp in `compute_qty` after the existing exposure-cap clamp (`qty = qty.min(budget/price)` — the tighter of {exposure cap, budget} binds); `new()` sets `None` ⇒ byte-identical to today; `size_and_validate` UNCHANGED; the budget is a PERMANENT notional ceiling ('never deploy >€200' even after equity grows), NOT an equity re-scale (re-scale REJECTED — would compound deployment above budget). D2 F4 ships WITH the day-1 baseline-equity-divergence e2e (CLAUDE.md NON-NEGOTIABLE) `risk/tests/budget_sizing_divergence_end_to_end.rs` modelled on `vol_targeting_overlay_end_to_end.rs`: budget arm (`cash=200`, capped) vs baseline arm (`cash=100_000`, `::new`) over a ≥1-fill fixture → assert RETURN paths diverge ≥1bp (no-op cap ⇒ equal ⇒ FAIL-before/PASS-after forensic gate, same PR). D3 F5 selection→forward seam = a `core`-typed `agent::config::ForwardRunConfig {strategy: StrategyId, symbol: Symbol, budget: Money<Usdt>, lookback: Option<DateRange>}` built UI-side from the crowned/picked `LeaderRow` + the F3 budget (core types only ⇒ NO `ui→strategy` edge; `cargo tree -p ui` unchanged) + a WIDENED `build_registry(cfg)` → `build_registry_for(cfg, &ForwardRunConfig)` resolving the selected `StrategyId` to a concrete strategy (same id set the bake-off field uses; unknown→warn+config-default fallback) + a new `RunHandles.forward` field; the Mode::Paper branch derives `feed_symbol` from the selection (replacing the hardcoded `BTCUSDT` `runtime.rs:490`). `ui`/`advisor`-crate homes REJECTED (force a new dep — `agent` already depends on `strategy`). D4 the MVP forward run is REAL-TIME-ONLY (paper = live Binance WS, soak-confirmed; the Live view filling in over time IS journey step 5); the 'what it would have done' fast-replay preview (cheap `run_scenario` over recent window) is DEFERRED to v0.2 (F5b, OQ-1 — adds a 2nd equity surface, not a gate). D5 the budget becomes the loop's STARTING CAPITAL (a trailing `budget: Option<Money<Usdt>>` arg on `spawn_trading_loop`; `Some(b)` ⇒ `initial_capital = b.amount()` + `with_budget_cap`) so the EXISTING per-bar `equity = cash + position·mark` publish to the EventBus PnL channel + the durable store (ADR-0052) IS the budget equity ⇒ ZERO new equity plumbing; Live shows P/L = equity − budget; `None` ⇒ byte-identical to today (research + legacy paper unaffected, ADR-0053 determinism intact). Durable store (ADR-0052) + reflection-wired loop (commit e9da47f) reused via the existing `Some(...)` args. `compute_qty` stays Decimal-pure (no new f64 boundary); 119/119 anchors byte-identical by construction (no `spec/*/reports/` body written, no `anchors.toml` SHA changed). OQ-1 (real-time-only vs replay) + OQ-2 (default-to-crowned vs force-pick) carry recommended defaults; neither is a build gate. **§ Changelog 2026-06-21 amendment (D6 launch-lifecycle):** the D3/D5 boot-config `RunHandles.forward` field is structurally insufficient for the POST-boot bake-off selection (`run()` consumes `RunHandles` once at boot → the cause of two FAKE dev passes where the default 100k loop kept running and the UI just relabelled `equity − 200`); the REAL launch is a HOT-SWAP of the trading-loop task on the running runtime (mechanism A): a `core`-typed `ForwardCommand::Launch(ForwardRunConfig)` over a new `mpsc` (`RunHandles.forward_rx`, replaces `forward`) → a `paper_loop_supervisor` aborting-current + spawning-fresh `spawn_trading_loop` (selected strategy/coin/€200) on the SAME `EventBus`/ledger/store/boot_id; `spawn_trading_loop` returns its `AbortHandle` (await old drain ⇒ no double equity-writer); the Live subscription survives because it is anchored to the `Arc<EventBus>` broadcast receiver, not the producer task. Rejected (B) relaunch (new bus breaks the iced subscription; new boot_id fragments the audit trail) + deferred (C) durable-store-read to v0.2; `forward_rx=None` byte-identical; render-proof upgraded to trace to a real `Some(budget)` loop; +OQ-4 (warm-up bars) +OQ-5 (forward lesson cards). | accepted (D6 amended 2026-06-21) | 2026-06-20 |
| 0061  | Dynamic on-demand Binance fetch seam + anchor-safe dynamic-cache separation (feature `advisor-dynamic-data` — fetch market data on demand for any picked `(coin, window)` the pinned corpus does not cover, then run the bake-off). D1 EXTRACT the bin's fetch into a reusable `data::binance_klines::fetch_binance_klines_range(symbol, start_ms, end_ms, interval) -> Result<Vec<Bar>, BinanceFetchError>` (move `Kline`/`build_klines_url`/`KlineFetcher`+`HttpKlineFetcher`/`paginate_klines`/`write_parquet`; bin re-exports → CLI byte-unchanged; `KlineFetcher` IS the mock seam, no test hits a socket); `kline_to_bar` sets `local_recv_ts=close_ts` (ADR-0032 §D1 Step 7). D2 typed `BinanceFetchError` {Network, Timeout, RateLimited(429/418, honour Retry-After), UnknownSymbol(400/-1121), NoDataForRange(200+0 bars, NOT `Ok(vec![])`), Other} via a pure `classify_binance_error(status, body)` — NO panic/unwrap. D3 FETCH-THE-WHOLE-WINDOW for not-covered ranges (NOT per-gap stitching) — the apples-to-apples ONE-preload clone makes a boundary-bar seam corrupt all arms silently; miss windows (Last30d/Last90d/post-2024 Custom) are entirely outside the pinned 2021-2024 corpus so gap==whole-window anyway; `resolve_bakeoff_bars` routes covered→pinned read-only path (unchanged), not-covered→`dynamic_cache::load_or_fetch`, Synthetic/Yahoo→unchanged. D4 ANCHOR-SAFE BY CONSTRUCTION (3 guarantees): (a) separate git-ignored root `data/binance-dynamic/` — `/data/*` already ignores it, NO `!` exception added; (b) `verify_anchors.sh` only `find`s under `spec/` → structurally blind → 119/119 by construction, no `anchors.toml`/body-SHA touched; (c) dynamic branch NEVER writes `data/binance/` or any `REVISION.toml` (dynamic root has none — nothing to regenerate, vs `yahoo::fetch_and_cache`); enforced by `dynamic_cache_anchor_safety.rs` (mock fetcher, corpus mtime+SHA snapshot before==after) + the 119/119 gate. D5 DETERMINISM BOUNDARY = dynamic data is EXPLORATORY, NEVER anchored — no REVISION pin (inverse of the pinned corpus's purpose), no anchored report (bake-off writes none per ADR-0059), reachable only from BinanceCache+not-covered (anchored CLI uses covered 2021-2024 windows → never takes it); same-seed-every-arm holds WITHIN a run, non-reproducible ACROSS runs by design. D6 cache fidelity = ReplayFeed parquet round-trip (`<SYM>/<YEAR>/<MM>.parquet` ADR-0056 layout, root-swap only) so a dynamic Bar == a corpus Bar byte-for-byte and cache-hit==cache-miss; partial trailing month re-fetched. D7 curated 10-symbol picker + 1h interval retained (UnknownSymbol → defence-in-depth; picker test relaxes "in pinned corpus"→"in curated set"); `ui` imports NO new crate (errors → `RunError::Internal` → already-wired `PanelState::Error`; copy in `ui::strings`; verified at the render-pixel layer). NO new dependency (reqwest/serde_json/tokio/thiserror/time/async-trait/polars all present). Leans on ADR-0055 (git-ignored-root precedent), ADR-0053 §D6 / ADR-0032 (determinism), ADR-0059 (bake-off seam), ADR-0056 (corpus layout). | accepted | 2026-06-21 |
| 0063  | Ensemble signal-vote seam + robustness-gate activation (feature `advisor-ensemble` — the 2026-06-19 single-coin-advisor pivot F8: a BOUNDED, PRE-REGISTERED set of two deterministic signal-vote candidates `v0.8.vote.majority` (≥2-of-3 over {macd,rsi,bbands}) + `v0.8.vote.unanimous` (4-of-4 over {sma,macd,rsi,bbands}), bundled WITH the activation of the today-inert robustness gate). D1 `strategy::EnsembleStrategy` IMPLEMENTS the FROZEN `Strategy` trait (ADR-0005, NOT modified) by holding `members: Vec<Box<dyn Strategy>>` + a pure `arbitrate(method,&stances)->Stance` arbiter (the `RegimeDispatcher` precedent generalised to N homogeneous members) ⇒ it is the ONE primitive reachable identically from `run_scenario` + `build_registry_for` (F5b) + `StrategyRegistry` with ZERO special-casing at the three id-string seams; members are REUSED via one shared `build_member(id)` constructor (`SmaCrossover` + `ComposedStrategy` from the SAME `config/strategies/*.toml` the bake-off scored), arbiter methods FROZEN-in-code (`Majority{k,n}` / `Unanimous{n}`), edge-triggered emission on the ensemble's OWN stance transition (matching `ComposedStrategy`); the WARMUP/ABSTENTION rule is the honest count — an un-warmed member ABSTAINS (counted in NEITHER `long_count` NOR the denominator), the ensemble is FLAT until its needed quorum is warm (treating `None` as FLAT REJECTED — it manufactures false majorities + hides consensus); `quantity_scale`=default 1.0 (signal combiner, not a sizing modifier). D2 the ensemble carries the ADR-0062 `PlanDescribe` impl (non-mutating read of member stances + arbiter). D3 `PlanRuleShape::Ensemble { method: PlanVoteMethod, members: Vec<PlanRuleShape> }` closed-enum extension (structured data ONLY — `PlanVoteMethod` a closed enum, NOT a string), `ui` exhaustive-match gains an `Ensemble` arm naming METHOD + MEMBERS ("holds when ≥2 of {MACD,RSI,Bollinger} agree…" + live tally, NOT a fabricated single-indicator rule), `agent`↔`ui` mirror in lockstep, no-`String`-crosses-the-seam kept end-to-end (`cargo tree -p ui` unchanged). D4 `RobustnessMode::Bootstrap { paths, seed }` is the MISSING per-candidate compute-and-feed path (default stays `Skip`): map realized equity→returns, block length via the EXISTING `politis_white_block_length` (Politis–White PWSD, NOT a magic constant), resample `paths`=1000 moving-block draws with FROZEN ADR-0051 D1 sub-seed determinism (`path_seed_j = master + j*0x9E3779B9`, one `ChaCha20Rng::seed_from_u64` per path, master DERIVED from the bake-off seed ⇒ reproducible), reduce via EXISTING `DistributionSummary::from_path_metrics`, classify via the FROZEN `classify_verdict` (ADR-0059 §D4 bands NOT touched). MULTIPLE-COMPARISONS: re-evaluated (NOT assumed), decision NOT-YET at a 7-arm field — the gate is an ABSOLUTE per-candidate band rule not a winner-selection p-value, the pre-registration lock removes search DoF, so widening the field does not inflate a per-candidate FP rate; TRIGGER recorded — a set that grows with data or any selection-over-configs step REQUIRES its own ADR (Bonferroni/Šidák or White Reality-Check/SPA). D5 ANCHOR SAFETY 119/119 BY CONSTRUCTION via WHICH call sites change: `RobustnessMode::default()` STAYS `Skip` (every anchored CLI path + every mode-unspecified caller keeps `None` flags ⇒ ranking byte-unchanged; ONLY the advisor `spawn_bakeoff` path — which writes NO report, ADR-0059 §D3 `write_report=false` — opts into `Bootstrap`, computing in-memory flags ONLY, writing no file); the two ensemble arms are OPT-IN field entries (the `v0.buyhold`/dynamic-cache/`budget_cap` additive precedent) — `default_field()` UNCHANGED (4 singles), a new `default_ensemble_field()` composed ONLY by the advisor caller; `run_scenario`'s new ensemble arm writes no report + the existing single-id arms are BYTE-UNTOUCHED; NO anchored report opened/edited/re-emitted, NO `anchors.toml` SHA changed, NO `REVISION.toml` touched, NO new anchored scenario (`verify_anchors.sh` run before+after = STOP-and-route-back on non-119). D6 `run_scenario` (`engine.rs`) + `build_registry_for` (`runtime.rs`) each gain ONE arm for the two ids, building the `EnsembleStrategy` via the shared factory + (forward) members from the SAME TOMLs (F5b fidelity) ⇒ a crowned ensemble forward paper-trades through the unchanged `paper_loop_supervisor` hot-swap. D7 TWO day-1 e2e gates (CLAUDE.md non-negotiable, FAIL-before/PASS-after): (a) the vote COMBINES — `ensemble_vote_divergence_end_to_end.rs` asserts the majority ensemble's equity DIVERGES from EACH member's curve by ≥1bp on a disagreement window (catches a silent passthrough = the v3-vol no-op analogue); (b) the gate BITES (anti-`Skip`-regression) — `robustness_bootstrap_bites.rs` asserts an overfit candidate is flagged `Fragile` + loses the crown to a robust single AND a robust candidate is NOT flagged (no false-positive); plus the F2 reachability regression (BenchmarkWins / AllFragile stay reachable WITH ensembles present). Frozen surfaces honoured: `Strategy` trait (ADR-0005), `classify_verdict`+bands (ADR-0059 §D4), `rank_candidates` (ADR-0059 §D5), F5 supervisor (ADR-0060 §D6), F6 `PlanDescribe` non-mutation (ADR-0062). Leans on ADR-0059 / ADR-0060 / ADR-0062 / ADR-0005 / ADR-0051. | accepted | 2026-06-21 |
| 0062  | Forward-plan read seam — read-only stance/rules trait + `core`-typed `ForwardPlan` mirror (feature `advisor-forward-plan` — the 2026-06-19 single-coin-advisor pivot F6, journey step 4, the CONDITIONAL rule-driven plan shown between the crowned pick and the F5 paper-trade; NOT a price forecast, NOT a dated trade calendar, NOT an implied-return claim). D1 the read surface is a NEW read-only SIBLING trait `strategy::PlanDescribe::describe_plan(&self, &PlanContext) -> StrategyPlan` (non-mutating `&self`; scoped to the F6 candidate engines SMA/MACD/RSI/Bollinger/buy-hold — NOT a `Strategy` method, honouring the ADR-0005 trait-shape freeze + sparing every overlay/pairs/cross-sectional/forecast impl a method the advisor never calls); the engine emits STRUCTURED rule data (closed `PlanStance{Flat,Long}` + `Option<PlanSignal{Buy,Sell,Hold}>` + closed `PlanRuleShape{SmaCross,MacdCross,RsiReversion,BollingerReversion,BuyAndHold}` + `ProjectedSizing{units,capped}`), NOT a pre-rendered copy String — the `ui` owns the words + the not-advice/not-a-prediction disclaimers (the ADR-0059 `Recommendation`-not-`String` precedent). D2 stance-now is a NON-MUTATING read of the engine's already-warmed indicators against `last_close` (rejects Clone-and-`on_bar` — that needs `Strategy: Clone`, an ADR-0005-forbidden shape change, doubles indicator memory, risks divergent warm-up). D3 the plan is produced AGENT-SIDE at the `ForwardCommand::Launch` boundary from the SAME `build_registry_for(Some(&cfg))` registry the F5 hot-swap runs (ADR-0060 §D6.2) ⇒ plan↔engine consistency BY CONSTRUCTION (R7); CRITICALLY this honestly describes today's SMA-PROXY resolution (`build_registry_for` `runtime.rs:288` proxies `v0.5.macd`/`v0.5.rsi`/`v0.5.bbands`/`v0.buyhold` through `SmaCrossover` until the F5b dedicated ctors land) instead of asserting Lab-time rules the loop does not run. D4 the plan crosses to `ui` as a `core`-typed `agent::config::ForwardPlan` (`Clone+Debug`; closed `agent`-owned `PlanStance`/`PlanSignal`/`PlanRuleKind` enums; `StrategyId`/`Symbol`/`Price`/`Timestamp`/`Money<Usdt>`/`Quantity` fields + `horizon_days: u16`; NO `strategy`/`exec`/`forecast`/`llm` type) over a SECOND mpsc `RunHandles.forward_plan_rx` (agent→iced RESULT path, symmetric with the iced→agent `forward_rx` COMMAND path); the `agent` boundary maps `strategy::PlanRuleShape`→`agent::PlanRuleKind` in one place (the supervisor); `ui` then mirrors `ForwardPlan`→a `ui`-side `ForwardPlanView` (closed ui enums + ui-owned copy) EXACTLY as it mirrors `BakeoffReport`→`BakeoffReportMirror` ⇒ `cargo tree -p ui` UNCHANGED. D5 buy-and-hold is a first-class `PlanRuleKind::BuyAndHold` arm (stance Long after first buy / `latest_signal=None` no re-eval / full €200) + the render NEGATIVE CONTROL. D6 the horizon is DISPLAY-ONLY metadata (`horizon_days`, default 7, range 1-30; optionally echoed into `ForwardRunConfig` for the 'planned through <date>' label) — F5 stays BYTE-IDENTICAL (OQ-C, operator-locked OPEN-ENDED: NO `horizon_days` self-terminate path; the `paper_loop_supervisor`/`spawn_trading_loop` lifecycle is untouched, the soak's open-ended forward run unaffected). D7 anchor-neutral (`verify_anchors.sh` 119/119 BY CONSTRUCTION — F6 places no orders, runs no new backtest, writes no `spec/*/reports/` body, changes no `anchors.toml` SHA) + the CLAUDE.md day-1 baseline-equity-divergence e2e is N/A (F6 DESCRIBES sizing, it does not size/run — that gate is for a strategy overlay/sizing modifier and landed on F4/ADR-0060 §D2). D8 verification floor = render-layer PNG (populated active-strategy plan: stance badge + IF/THEN standing rules + €200 projected-sizing number, PLUS the buy-and-hold degenerate plan as the NEGATIVE CONTROL + a no-pick empty tautology guard, via the `crates/ui/tests/leaderboard_populated_render.rs` / `leaderboard_screen_program` + `PanelState::Empty` real-renderer pattern, macOS-canonical per ADR-0057 §D2) + the ANTI-DRIFT consistency assertion (`describe_plan` stance+rule-family matches the engine's actual `on_bar` decision on the same bar, per resolved-registry engine — the testable form of 'the plan cannot drift from the engine') + `cargo tree -p ui` unchanged. Rejected the OQ-A fallback (UI-side `ComposedStrategy` TOML-AST rule derivation) with a CONCRETE drift proof: the SMA-proxy means a MACD-AST-derived plan would be actively WRONG vs the SMA the F5 loop runs — two sources of truth, disqualifying for a product whose credibility rests on 'the plan is exactly what the engine will do'. Leans on ADR-0060 (§D6 launch seam), ADR-0059 (`BakeoffReport` mirror + structured-data-not-String discipline), ADR-0005 (`Strategy` trait freeze), ADR-0023/0041 (ui layering), ADR-0057 §D2 (macOS render-pixel canonicality). | accepted | 2026-06-21 |
| 0065  | EUR→USDT budget-conversion seam — configurable static FX rate (feature `advisor-eur-fx` — the 2026-06-19 single-coin-advisor pivot F7, the LAST v0.2 item; resolves product § D4's deferred fixed EUR→USD rate). The operator enters a budget in euros but the engine is USDT-denominated end-to-end (`ForwardRunConfig.budget`/`FixedFractionSizer.budget_cap`/`final_equity` all `Money<Usdt>`; NO `Eur` type exists); today the parsed euro `Decimal` is stamped 1:1 into `Money<Usdt>` at `cockpit_live.rs:1431-1437` with the label "€200 ≈ 200 USDT — FX not modelled". F7 applies `usdt = eur × rate` at that single boundary + rewrites the label honestly. NON-goals: NOT FX trading/prediction, NOT a first-class `Eur` currency + ledger FX-PnL, NOT a network fetcher; the bake-off ranking is FX-invariant (untouched, anchors 119/119). Operator LOCKED: rate source = a CONFIGURABLE STATIC rate (default `1.08`, config-editable, provenance-labelled); live fetch is a v0.3 upgrade layered on this constant (D6). D1 `FxRate {rate: Decimal (private, ctor-validated >0), source: SmolStr, as_of: SmolStr}` value object in a NEW `crates/core::fx` module (the ADR-0058 §D2 home-the-primitive-in-`core` precedent — `core` already carries rust_decimal/smol_str/serde → ZERO new dep; `ui` already imports `trading_core` → consuming a `core` type adds NO edge → `cargo tree -p ui` unchanged BY CONSTRUCTION; homing in `crates/data` REJECTED — forces a `ui→data` edge); the rate VALUE comes from advisor config (`eur_usd_rate: Option<Decimal>` `#[serde(default)]`) `.unwrap_or(DEFAULT_EUR_USD_RATE = dec!(1.08))`, and that constant is ALSO the fallback a future live fetcher degrades to (the "(b) is a strict superset of (a)" proof). D2 ONE pure conversion fn `FxRate::convert_eur_to_usdt(eur: Decimal) -> Money<Usdt>` (the ONLY EUR→USDT arithmetic) + a `BudgetConversion {eur, rate, usdt}` carrier computed ONCE in its ctor so the ENGINE (`conversion.usdt()` → `ForwardRunConfig.budget`) and the DISPLAY (`.eur()`/`.usdt()`/`.rate()`) read the SAME converted value → cannot drift (the ADR-0062 one-boundary-two-readers anti-drift discipline); EUR kept as a labelled `Decimal` at input — NO `core::Eur` marker (REJECTED: bilingual engine for one multiply) → F4/F5/bake-off stay unit-agnostic + byte-unchanged; the seam REPLACES the 1:1 `from_decimal` stamp at `cockpit_live.rs:1431-1437`. D3 deterministic (config constant; `as_of`/`source` are LABELS not clock reads) + anchor-safe BY CONSTRUCTION — the anchored CLI/headless path (bake-off/`run_scenario`/sweeps) is USDT-denominated + NEVER reads the rate (conversion lives ONLY at the cockpit UI input boundary) → `verify_anchors.sh` 119/119, no `anchors.toml` SHA / report body touched; `cargo tree -p ui` unchanged is a hard gate. D4 THE DAY-1 CONVERSION-APPLIED GATE (CLAUDE.md non-negotiable — APPLICABLE per the `v3-vol-overlay-noop` precedent: F7 modifies the budget the F4 sizing modifier consumes): a dedicated `crates/core/tests/eur_fx_conversion_applied.rs`, FAIL-before/PASS-after against a 1:1 stub — (1) `convert(€200, 1.08) == $216 ≠ $200` (the no-op guard; `rate=1.0` is the negative control == 1:1), (2) the converted value is the one F4 caps against (feed `conversion.usdt()` into `with_budget_cap`, assert cap reflects 216 not 200 → closes the computed-then-dropped hole), (3) display == engine (same `BudgetConversion`). D5 the three "FX not modelled" literals (`LEADERBOARD_BUDGET_HINT`/`FORWARD_PLAN_BUDGET_LINE`/`LIVE_FORWARD_FX_NOTE`) → honest "€X ≈ $Y (at R EUR/USD, source as-of)" driven by the SAME carrier; hard-cap + not-advice copy verbatim; a configured rate is always present → no "unavailable" UI. D6 v0.3 live upgrade NOTED-not-built: a `RateSource` trait + `StaticRateSource` (D1's value = the fallback) + a future `data::FxFetcher` (fetcher in `data` per ADR-0061, NOT `ui`) injected through a fake seam in every test/render (never network) — a STRICT superset of D1 needing D1's constant as fallback → zero rework; requires its own ADR. Leans on ADR-0058 §D2 / ADR-0060 §D1/D3 / ADR-0061 §D1 / ADR-0062 / ADR-0003 / ADR-0023/0041. | accepted | 2026-06-22 |
| 0064  | Advisor LLM "why this one" narration seam (feature `advisor-llm-narration` — the 2026-06-19 single-coin-advisor pivot F9, journey step 3, the FIRST in-app LLM consumer; a READ-ONLY plain-language rendering of the ALREADY-DECIDED structured bake-off `Recommendation` + per-candidate KPIs + `RobustnessFlag`s — why the crowned strategy won, what buy-and-hold did, why the runners-up lost — and NOTHING MORE; the LLM NEVER enters the ranking, reinforcing the F8 narration-only lock; NON-goals: no price prediction, no implied/expected return, no "buy this", no invented number, no will-keep-working claim). D1 the generator lives AGENT-SIDE in a new `agent::narration` module (the `agent::plan`/ADR-0062 twin — `agent` already owns the boot-built `Arc<dyn LlmProvider>` (`main.rs:265`) + hard-deps BOTH `llm` AND `backtest`): owns `NarrationFacts` (the EXACT machine values built from `BakeoffReport` at one boundary, the `BakeoffReportMirror::from_report` precedent), the role-locked cache-marked prompt (layer 1), the pure deterministic faithfulness post-check (layer 2), and `generate_narration(provider, facts) -> NarrationOutcome{Ready(SmolStr)|FellBack}` (`core`-clean, NO `llm` type crosses); `backtest` home REJECTED (would couple the deterministic ranking core to a non-deterministic provider + invert the dep direction), `ui` home REJECTED (crosses the view line), new `crates/advisor` REJECTED (one module, over-scoped). D2 the faithfulness post-check is THE LOAD-BEARING GUARD — a pure `llm`-free `check_faithful(text,&facts)->Pass|Reject` whose predicate set + banned-phrase list are FROZEN/pre-registered here (ADR-locked, change = amendment, the `classify_verdict` discipline): P1 wrong crown (names a non-`facts.winner` near a frozen crown lexeme), P2 contradicted `RecommendationOutcome` (`BenchmarkWins` but claims active beat BH / `ActiveWins` but claims nothing beat BH / `AllFragile` but asserts robust w/o the caveat), P3 fabricated number (every numeric token EXACT-STRING-matches a `num`-formatter canonical rendering of a `facts` KPI — deterministic, NOT float-tolerant), P4 a frozen predict/advise banned-phrase list ("will rise"/"expected return"/"guaranteed"/"you should buy"/"price target"/…); a prompt alone is insufficient — the post-check is the net; ANY hit → `FellBack`. D3 the narration is a TRIGGERED second async step (operator "Explain" action posts `Message::BakeoffNarrationRequested`→`InFlight`→agent runs `generate_narration`→`Message::BakeoffNarrationCompleted(NarrationOutcome)` updates the recommendation block IN PLACE), NOT auto-on-`BakeoffRunCompleted` (operator LOCKED opt-in — the templated `recommendation_block` ships complete + honest without it). D4 the narration crosses `view` as a PLAIN `ui`-owned `NarrationState{NotRequested|InFlight|Ready(SmolStr)|FellBack}` enum on `LeaderboardScreenState` (String/enum only — NO `llm`/`agent` type through `view`, the `RecommendationMirror` discipline; the ONE `#[cfg(feature="live")]` recipe/adapter mapping `agent::NarrationOutcome`→the message is the only `agent`-type edit site); gate = no `llm` type reaches a `view` fn (`grep llm:: crates/ui/src/{screens,state,shell}` stays empty) + `cargo tree -p ui` gains NO new edge (`agent`/`llm` already in the `live` graph). D5 the fake-`LlmProvider` seam injects at `generate_narration`'s boundary (the repo "every external I/O behind a trait" rule the `LlmProvider` trait already satisfies); fixtures provide BOTH a `FaithfulFakeProvider` (drives `Ready`) AND an `UnfaithfulFakeProvider` (drives the post-check `Reject`→`FellBack` anti-hallucination e2e) — NO network in any test/render; prompt caching (`SystemBlock::Cached(…,CacheBreakpoint::Ephemeral)`) + the `BudgetedProvider` budget/auto-degrade path exercised structurally (a `BudgetExceeded` fake → `FellBack`). D6 mandatory HONEST FALLBACK total — disabled (`cfg.llm.enabled=false` default)/unavailable/error/timeout/`BudgetExceeded`/`ReplayMiss`/empty/post-check-reject → the existing templated copy; never blocks/waits-on/breaks the bake-off, never a half-answer; the 4 render states are NotRequested=templated+Explain control, InFlight=templated+spinner, Ready=the LLM prose (disclaimer surrounding), FellBack=templated (silent) — the templated copy is the floor in every arm but `Ready`. D7 EPHEMERAL — the prose is NOT persisted in the bake-off report artifact (operator LOCKED; the structured `Recommendation` is the reproducible artifact, the prose a session-only derived view); anchor-neutral (`verify_anchors.sh` 119/119 BY CONSTRUCTION — no report body written, no `anchors.toml` SHA changed) + the CLAUDE.md day-1 baseline-equity-divergence e2e is N/A (F9 narrates, produces NO equity/signal/fill — like F6, UNLIKE F4 sizing + F8 ensemble where the gate bit). D8 verification = per-predicate post-check UNIT tests (deterministic, no LLM: P1/P2/P3/P4 each REJECT→`FellBack` + a faithful narration ACCEPT→`Ready`) + the anti-hallucination E2E (unfaithful fake→`FellBack` the templated copy NOT the prose; faithful fake→`Ready`; `BudgetExceeded` fake→`FellBack`) + the async-ordering test (structured result renders on `BakeoffRunCompleted` BEFORE the narration; narration updates in place on its own message) + the render-layer PNG (`Ready` prose painted + the `FellBack`/`NotRequested` NEGATIVE CONTROL = templated copy + disclaimer painted, through the fake seam — no network, `leaderboard_populated_render.rs` pattern, macOS-canonical per ADR-0057 §D2) + no `llm` type through `view` + 119/119. Model = the Anthropic default via `BudgetedProvider` (operator LOCKED — don't pre-compromise faithfulness). Consumes `crates/llm` (trait + `BudgetedProvider` + `CachedSystemPromptBuilder` + record/replay) AND the frozen `backtest::bakeoff` types UNCHANGED. Leans on ADR-0062 (§D3–D4 agent→iced return path + one-adapter mirror), ADR-0059 (`Recommendation`-not-`String` + one-boundary `from_report` read), ADR-0019 (the v2 LLM foundation), ADR-0023/0041 (ui layering), ADR-0057 §D2 (macOS render-pixel canonicality), ADR-0051 (frozen-predicate-set discipline). **Amended 2026-07-01 (P2-1 narration faithfulness hardening, feature `advisor-narration-faithfulness`):** `NarrationFacts::allowed_numbers()` now returns an owned `HashSet<String>` (was `Vec<String>`) — the verbatim-number-match discipline is unchanged (still exact-string, never float-tolerant), this closes a representational gap and lets `RejectReason::FabricatedNumber(String)`/`::BannedPhrase(String)` carry the offending token/phrase for the audit trail (were unit variants; `WrongCrown`/`ContradictedOutcome` unchanged, `FaithfulnessVerdict::Pass` semantically identical); `BANNED_PHRASES` gains prediction verbs (`expected to`/`forecast`/`predict`/`probably`/`likely to`/`anticipates`/`projected`), causation clauses (`because of`/`driven by`/`caused by`/`due to`), and advice phrases (`you should`/`we recommend`/`invest in`/`stay away from`) — additive, no phrase removed; 27-test adversarial corpus in `crates/agent/tests/narration_faithfulness.rs` (1 positive/3 number-invention/9 prediction/4 causation/8 recommendation/2 backward-compat) plus the pre-existing 25 `narration.rs` unit tests, all pass; anchor-safe by construction, 119/119 before and after. | accepted | 2026-06-22 |
| 0066  | Benchmark exemption from the `AllFragile` outcome determination (`rank_candidates` amendment; `classify_verdict` UNCHANGED) (feature `advisor-benchmark-robustness` — the operator-approved B1 root fix for the always-`AllFragile`-on-real-crypto finding: F8 activated the robustness gate (ADR-0063) and the complete 7-arm field on real BTCUSDT H1-2024 returns `AllFragile` on EVERY run — even when buy-and-hold is the top-Sharpe arm — because the buy-and-hold BENCHMARK is judged by the candidate curve-fit ruler (its resampled p5-Sharpe < 0 on a 60-70%-vol single asset), a CATEGORY ERROR not a numeric bug; this masks the honest `BenchmarkWins` the product was specced to surface. The fix is TWO coordinated `crates/backtest/src/bakeoff/rank.rs` edits, classifier byte-FROZEN). D1 the benchmark is NOT a candidate for the `AllFragile` determination — `all_fragile` (rank.rs:60-62) → `all_active_fragile` ranging over `!c.is_benchmark` arms only; `AllFragile` fires iff all ACTIVE arms are Fragile AND no benchmark gives a crownable result. The benchmark is the null hypothesis the candidates are scored AGAINST (`passive-baseline.md`), never a candidate that must clear the bar — framed strictly "benchmark-is-not-a-candidate," NEVER "we loosened the gate"; every active/ensemble arm still faces the identical frozen `classify_verdict`, NO band moved, NOT a threshold relaxation. D2 the benchmark is crown-eligible irrespective of its own robustness flag — `is_eligible` (rank.rs:124-126) returns true for `c.is_benchmark` regardless of flag; this is the REQUIRED second edit (D1 alone is a WORSE bug: without D2 a Fragile benchmark still partitions ineligible in the comparator, the crown lands on a Fragile ACTIVE arm, `crowned.is_benchmark` is false, and the outcome falls through to `ActiveWins` on a Fragile crown — strictly worse than today's `AllFragile`); the active-arm anti-overfit eligibility lock (ADR-0059 §D5) is UNCHANGED, only the benchmark gains unconditional crown-eligibility. D3 `classify_verdict` + `verdict_bands` are BYTE-UNCHANGED (reaffirms the ADR-0059 §D4 / ADR-0063 §D4 freeze + the 2026-05-30 pre-registration) — the bootstrap still runs for the benchmark arm and its flag is still COMPUTED + DISPLAYED (informational: the baseline is itself path-dependent), only its CONSUMPTION in `rank_candidates` changes. D4 anchor-safe BY CONSTRUCTION 119/119 — the advisor bake-off path writes NO report (`write_report=false`, ADR-0059 §D3) so the in-memory `outcome` touches no anchored body; the classifier is frozen so the 18 block-bootstrap θ-surface anchors (which hash the SWEEP-BIN `classify_verdict` output, NOT `rank_candidates`) are byte-identical; `RobustnessMode::default()==Skip` keeps every anchored CLI path on `None` flags; no `anchors.toml` SHA / `REVISION.toml` / report body touched (the same additive-equivalent contract as ADR-0059 §D3 / ADR-0063 §D5). D5 the day-1 BenchmarkWins-REACHABILITY gate (CLAUDE.md day-1-divergence discipline applied to an outcome-determination change) — a field where all ACTIVE arms are Fragile AND the benchmark is top-Sharpe must yield `outcome == BenchmarkWins` (reason `BenchmarkUndefeated`) AND `crowned.is_benchmark == true`, NOT `AllFragile` (FAIL-before/PASS-after); plus the residual `AllFragile` dual (all arms Fragile + benchmark NOT top-Sharpe → `AllFragile` on a Fragile active crown) + the `rank.rs::t65_all_fragile` (rank.rs:297-325) amendment. This finally IMPLEMENTS the ADR-0063 §D7 / R4.4 reachability regression the `robustness_bootstrap_bites.rs:9` doc comment DECLARED but no test body asserted. D6 determinism unchanged (a `filter` + one boolean disjunct, pure/total, no new f64 boundary, no RNG). AMENDS ADR-0059 §D5 (the F2 comparator outcome rule) + ADR-0063 §D7 (the R4.4 reachability gate); LEAVES the classifier freeze ADR-0059 §D4 / ADR-0063 §D4 + the 2026-05-30 pre-registration UNCHANGED. REJECTS B2/B3 (band loosening — ≤18 θ-surface anchor body-SHA breaks + per-anchor ADR-0038 §D6 re-emission + pre-registration moat forfeiture; the correct fix is also the anchor-cheap fix, 0 breaks). Leans on ADR-0059, ADR-0063, ADR-0051. Anchor-mutation ADR NOT triggered (no `anchors.toml` SHA change). | accepted | 2026-06-22 |
| 0067  | Pre-registered combination-slate expansion of the advisor field (feature `advisor-combination-search` — the operator-requested 2026-06-23 "calculate the combination of multiple strategies", scoped as a pre-registered-ONLY widening of F8's bounded vote-ensemble slate). Adds **6 new pre-registered `EnsembleStrategy` arms** to the live advisor field (4 singles + 8 ensembles + buy-and-hold = **13 arms**), each scored through the BYTE-FROZEN `RobustnessMode::Bootstrap` gate + the ADR-0066 buy-and-hold benchmark; decorrelation is the LEGITIMATE lever that lifts p5 (tail) Sharpe — the binding `classify_verdict` constraint — WITHOUT touching the gate. D1 the slate is a FIXED, code-declared FALSIFIER set (the overfit defense — no search = no in-sample fitting to leak): 3 decorrelation pairs `v0.8.vote.{trend_pair=Unanimous{n:2}[macd,sma] PREDICTED-NULL CONTROL / tr_mr_macd_rsi=Unanimous{n:2}[macd,rsi] / tr_mr_sma_bb=Unanimous{n:2}[sma,bbands]}` + the COMPLETE k-of-4 ladder `v0.8.vote.{any1of4=Majority{k:1,n:4} / k2of4=Majority{k:2,n:4} / k3of4=Majority{k:3,n:4}}` over the 4 base signals (k=4 is the existing unanimous arm → the WHOLE ladder reported, no "best k" cherry-picked); principled-not-exhaustive (NOT every-subset-of-4 = a search dressed as a slate); the control has a PREDICTED outcome (little p5 lift — if it lifts as much as the mixed pairs the decorrelation thesis is FALSIFIED). D2 LITERAL `build_ensemble` ids, NOT a parser — the `run_scenario` engine dispatch already calls `build_ensemble(strategy_str)` generically, only its `match` pattern widens; an id grammar (`vote.k{K}of{N}`) is REJECTED (a parameterized space edges toward search + forfeits "pre-registered = a finite enum in the diff"); ZERO new arbitration math (`VoteMethod::{Majority{k,n},Unanimous{n}}` + `arbitrate` express every arm). D3 `default_ensemble_field()` is the SINGLE source of truth (+6 ids → 8; `advisor_field()`=`default_field()∪default_ensemble_field()` flows to the cockpit automatically, NO `ui` edit, NO duplicated list; `default_field()` UNCHANGED; one runner field-count test moves in lockstep 6→12); ~+86% bake-off wall-clock is within budget on the determinate-progress, operator-triggered, on-demand path (no real-time SLA → no progress-UX change, only an honest arm-count note). D4 the gate + bands + comparator are FROZEN — `classify_verdict`/`compute_robustness_flag`/`verdict_bands`/`bootstrap.rs` BYTE-UNCHANGED, `rank_candidates`+the ADR-0066 benchmark exemption UNCHANGED (a combination is just another `CandidateResult`, crown-eligible only if `robustness != Some(Fragile)`); framed STRICTLY "more candidates face the same bar," NEVER "we moved the bar"; NOT a B2/B3 band proposal (operator-REJECTED); `BenchmarkWins`/`AllFragile` reachability UNCHANGED — a null all-Fragile field → `BenchmarkWins` stays reachable with the 6 arms in + is the most-likely + a fully shippable honest finding. D5 honest mostly-Flat rendering — `Unanimous{n:2}` trend∧mean-revert pairs (members fire in different regimes → rare simultaneous-Long → mostly Flat → near-zero return → own Fragile flag) is HONEST not a bug, NOT engineered around, rendered truthfully via the existing low-`trade_count` + computed-`Fragile` row + the B1 "sat in cash"/"Nothing beat simply holding…" copy + the `PlanRuleShape::Ensemble` "≥ k of {…} agree" description (no new flag/state/special-case). D6 anchor-safe BY CONSTRUCTION 119/119 (new `v0.8.vote.*` ids run `write_report=false` on the Bootstrap advisor path → no anchored body collision; `verify_anchors.sh` before+after = STOP-and-route-back on non-119; anchors keyed by NAME not filename; no `anchors.toml` SHA / `REVISION.toml` / `spec/*/reports/` body touched, the 9 anchor SHAs UNMUTATED → no anchor-mutation ADR) + the CLAUDE.md DAY-1 baseline-equity-divergence e2e `crates/strategy/tests/combination_slate_divergence_end_to_end.rs` (each new arm diverges ≥1bp from a member AND from buy-and-hold + no two new arms identical — no silent no-op / no accidental duplicate; vote-mechanics via `SmaCrossover` members per the F8 precedent + factory smoke over the real 4 base TOMLs; FAIL-before/PASS-after) + the populated 13-row leaderboard + ensemble-rule render-snapshots (verify-UI-at-render-layer). DEFERS weighted/inverse-vol/conditional-regime blends to a v0.2 of this feature (new `VoteMethod` variant + `arbitrate` branch + `PlanVoteMethod` mirror + `ui` match arm + a free continuous knob = overfit risk unless rule-derived); a combination-SEARCH engine is out of scope (a guarded follow-on — if ever built MUST ship walk-forward/out-of-sample + complexity penalty [deflated-Sharpe/Bonferroni] + pre-registered procedure + loud risk call-out + its own ADR). Leans on ADR-0063 / ADR-0066 / ADR-0059. | accepted | 2026-06-23 |
| 0068  | Single-coin directional SHORT-selling, paper/sim (feature `advisor-short-selling` — the operator-directed 2026-06-23 "do the expensive short selling": give the long-or-flat single-coin advisor the down-half lever via a bounded pre-registered short-capable slate that sell-to-opens a simulated short on the bearish flip + buy-to-covers on the bullish flip, judged by the SAME frozen robustness gate + SAME buy-and-hold benchmark). NOT invent — PORT the proven signed-position model (open/cover/maintenance-margin-liquidation/per-bar-funding) already shipped + tested in `crates/backtest/src/scenarios/montecarlo.rs::run_path` (REQ-PERP-BASIS-MARKET-NEUTRAL-001, FAMILY-UNIFORM-FRAGILE) into the single-coin path, which is long-only TODAY by FOUR clamps (engine.rs:1632-1640/:1713-1715, cli_types.rs:632-635, sma_composed_run.rs:554, AND a fourth the brief missed — the forward paper loop runtime.rs:1809-1813/:1884-1885) while the equity formula `cash + qty·mark` is ALREADY short-correct. D1 port the model, gate every short branch on a single `short_enabled: bool` (`#[serde(default)]` on ScenarioConfig/SmaComposedRunInput, the single-coin analogue of `k_short>0`) → long-only is byte-for-byte HEAD's code; MAX_LEVERAGE=1x + maintenance_margin_frac=0.5 inherited verbatim. D2 in-place-gated NOT a sibling scenario (one engine, one `*_byte_identical_to_head` gate; sibling REJECTED — duplicates the solvency loop). D3 (Q-SS-1) signal model = position-aware INTERPRETATION of the existing `Buy`/`Sell` (Sell-when-flat-or-short→open/extend short, Buy-when-short→cover), NO new `SignalKind` variant — the proven MN route, keeps every exhaustive `match sig.kind` total with ZERO churn; explicit `OpenShort`/`CoverShort` variants REJECTED (edits every signal consumer for no behavioural gain). D4 (Q-SS-4) short cost = a NEW `core::funding::FundingRate` honest-constant (private Decimal + checked ctor, the ADR-0065 `FxRate` precedent; DEFAULT_PERP_FUNDING_RATE≈0.01%/8h, operator-tunable) homed in `core` next to fx.rs; PER-BAR accrual `cash += notional·(−rate_per_bar)` (the real-data single-coin hourly loop shares no synthetic 8h epoch → 8h-grid REJECTED) flowing through equity automatically; live feed DEFERRED v0.2 (layered on the constant as fallback). D5 (Q-SS-5) honest unbounded-loss = inherit the maintenance-margin liquidation (force-cover all shorts at the 0.5 floor, cash MAY go negative — NOT clamped at 0); the displayed €200 P/L is ALLOWED to print negative; "a short can lose MORE than your €200" + not-advice + paper-only disclaimers mandatory on every short surface. D6 (Q-SS-6) paper-sim parity = ONE shared pure/sync/deterministic `backtest::short_exec` helper that BOTH the bake-off (`run_scenario`/`sma_composed_run`) AND the agent forward loop (`spawn_trading_loop`, the 4th clamp site) call → forward run consistent-by-construction with the ranked bake-off (F5/F5b); `agent` already depends on `backtest` → no new crate edge. **D7 (Q-SS-3 — THE CRUX) audit seam = relax the READER only.** `post_fill_with_signal` is ALREADY sign-agnostic (a sell-to-open writes the same balanced Dr-cash/Cr-position legs → the double-entry reconciler Σdebits==Σcredits is UNAFFECTED) + the bake-off path NEVER journals (pure in-memory `BacktestState`) → the `qty>0` reject is a READER invariant (`audit::query::open_positions_at`, query.rs:1872-1881) consumed ONLY by `crates/reports` + the cockpit positions widget, NEVER by the equity curve. Replace the net-negative `LedgerError::Database` raise with emission of a SIGNED `OpenPosition` (qty may be <0; the field type already holds it, only the contract relaxes); writer + reconciler BYTE-UNCHANGED; `audit` no-sibling-imports rule INTACT (change internal to query.rs, still depends only on trading_core). A separate `open_positions_signed_at` variant REJECTED (forks the reader); shorts-out-of-the-audited-table REJECTED (silently lies on the positions panel). D8 UI = SHORT badge + signed/negative `base_qty` + short P&L (positive when price falls) + leaderboard `_ls`/`always_short` markers + plain-language forward-plan short-rule copy + disclaimers, all verified at the RENDER-PIXEL layer (`iced_test::Emulator::screenshot`) with a negative control per CLAUDE.md; `ui` imports NO new crate (live.rs:537 already carries base_qty). D9 the FIXED pre-registered 5-arm slate `{sma_cross_ls, macd_ls, rsi_ls, bbands_ls (symmetric long/short variants reusing the existing indicators, short instead of flat on the bearish flip), always_short (the down-side mirror of buy-and-hold, the honest control)}`; existing long-only arms UNTOUCHED, the `_ls` arms strictly additive. D10 anchor-safety + freeze BY CONSTRUCTION — new short arms run `write_report=false` → `verify_anchors.sh` stays 119/119 (run before+after, keyed by NAME) + the single-coin long-only path re-proven byte-identical; `classify_verdict`/`compute_robustness_flag`/`verdict_bands`/`bootstrap.rs` + `rank_candidates` + the ADR-0066 benchmark exemption FROZEN (NOT a band proposal, "more arms face the same bar"); `BenchmarkWins`/`AllFragile` reachability UNCHANGED; NO anchor SHA mutates → no `anchors.toml` amendment, no ADR-0038 §D6 re-emission (2nd feature to touch the single-coin clamps after the MN `run_path` touch, but writes NO new anchored report). D11 honest framing — shorts very likely ALSO Fragile (the MN long/short precedent was FAMILY-UNIFORM-FRAGILE; single-coin shorts inherit full inverse market beta + a real funding cost); a NULL result ("all short arms also Fragile, hold still stands") is the EXPECTED + valid + shippable outcome; the gate decides. Day-1 e2e (CLAUDE.md non-negotiable): a short arm diverges ≥1bp from its long-only sibling AND from buy-and-hold, AND on the pinned 2021-22 bear corpus `4f390622` the `always_short` arm PROFITS where long/flat sits flat — asserted with the correct SIGN — + funding-non-no-op + unbounded-loss-honesty falsifiers. PAPER/SIM ONLY: NO live trading, NO real orders, NO real margin. Leans on ADR-0051 §D6.10 / ADR-0065 / ADR-0066 / ADR-0063 §D4 / ADR-0024 / ADR-0016 / ADR-0059. | accepted | 2026-06-23 |
| 0069  | Gate-tied hyperparameter SWEEP seam (feature `advisor-param-tuning` — leaderboard-epic item #3, the operator-chosen gate-tied option NOT a naive single-config editor: pick a family + parameter ranges, sweep them, and score EACH config through the SAME byte-frozen robustness gate so overfit configs read FRAGILE by construction). D1 a NEW library entry point `backtest::bakeoff::sweep::run_param_sweep` — a parameter-grid SIBLING of `run_bakeoff` (loops N parameterised configs of ONE family SMA/MACD/RSI/Bollinger, runs each via the existing `run_scenario`, scores via the existing bootstrap, collects `{params, in-sample KPIs, verdict, distribution}`); the `param_robustness_sweep` BIN is bin-only AND wrong-universe (a 10-symbol cross-sectional momentum `ThetaCell` grid, NOTHING to do with single-coin rule-engine params) → NOT reused; reuse the already-LIBRARY primitives `classify_verdict`+`compute_robustness_flag` + the `run_bakeoff` orchestration/`from_report`-mirror/`BakeoffProgress` template; homed in `backtest` per ADR-0059 §D1 → ZERO new `ui` dep edge. D2 surface the bootstrap DISTRIBUTION (p5/p50/p95 + the 5 gate signals) not just the flag, via an ADDITIVE behaviour-preserving `compute_robustness_distribution(&[Decimal],paths,seed)->Option<(DistributionSummary,ParamRobustnessVerdict)>` sibling (`compute_robustness_flag` refactored to delegate + drop the summary, proven bit-identical by a FAIL-before test) — the spread IS the anti-overfit affordance (a gaudy in-sample config with negative p5 Sharpe reads FRAGILE); `verdict_bands`+seed-rule BYTE-FROZEN (reaffirms ADR-0059 §D4 / ADR-0063 §D4 / ADR-0066 D3, no band moved). D3 per-family param injection: SMA via the EXISTING typed `sma_fast_len/slow_len` override (cli_types.rs:683-695, runtime.rs:347); MACD/RSI/Bollinger via a GENERATED `signal`-DSL TOML string built IN MEMORY + `ComposedStrategyConfig::from_str` (composed/config.rs:96 parses a string not just a file) — the real engine GAP: MACD/RSI/Bollinger params are literals inside the `signal` string (`macd_hist(12,26,9)` etc.) with NO runtime override (`build_registry_for` hardcodes the filename per id); a new pure `build_swept_strategy(family,params)`; ship SMA-FIRST (the proven seam) then add the composed families behind a round-trip-through-`from_str` AST test. D4 grid CAPPED `MAX_SWEEP_CONFIGS=24` (~2× the 13-arm bake-off, the interactive-click ceiling) + HONEST truncation (first-24 deterministic axis-major order + `truncated`+`requested_count` → a "Showing 24 of N…" banner, NO silent drop); invalid cells (SMA `fast≥slow`) dropped pre-run + reported. D5 FRAGILE is the prominent promotion-BLOCKING state (reuse the leaderboard `DOWN_50`/`DOWN_500` Fragile-badge pixels; "Use this config →" DISABLED+greyed on fragile rows, the "Fragile cannot be crowned" lock); the in-sample point estimate shown but de-emphasised vs the distribution; shipped-config baseline row + buy-and-hold KPIs always in view; promotion-INTO-F4/F5 OUT OF SCOPE v0.1 (the editor SHOWS the verdict; the disabled-on-fragile affordance ships now). D6 a Lab SUB-VIEW reached by a "Tune…" drill-down (a new `Screen::Tune`, navigable-NOT-default-routed, the `InspectStrategyFromLeaderboard` precedent state.rs:1841) NOT a 13th sidebar screen (proliferation REJECTED); a modal overlay is an acceptable ui-designer substitute; `SweepReportMirror::from_report` single boundary preserves the `ui→strategy` non-edge. D7 async mirrors the bake-off arms 1:1 — `BakeoffProgress` `{done,total,current_id}` determinate shape (as `SweepProgress`) + `SweepRunRequested`/`SweepProgress`/`SweepRunCompleted`/`SweepCancelRequested` + `RunCancelReceiver` checked before each cell + the `spawn_bakeoff` fixtures-Err-no-`live` pattern (runner.rs:180), no iced-thread block. D8 the CLAUDE.md day-1 DIVERGENCE e2e `param_sweep_divergence_end_to_end.rs` (≥1 swept cell's realized equity ≠ `report.baseline` shipped-config ≥1bp + cells not all identical + a concrete SMA `(10,20)≠(20,50)` pin; FAIL-before = `build_swept_strategy` silently returning the shipped config; the v3-vol-overlay-noop precedent, modelled on `vol_targeting_overlay_end_to_end.rs`+`combination_slate_divergence_end_to_end.rs`). D9 anchor-safe BY CONSTRUCTION 119/119 (every cell `write_report=false` → no anchored body, the 9 anchor SHAs UNMUTATED, frozen bands → no anchor-mutation ADR) + render-PIXEL verification `param_sweep_render.rs` (`#![cfg(target_os="macos")]` per ADR-0057 §D2: a populated grid + FRAGILE badge clay + the distribution columns + an Empty NEGATIVE control painting no grid + a promotion-disabled-on-fragile discriminator + the determinate progress bar; the implementer MUST serialise the screenshot harnesses around the macOS cosmic-text font-mutex deadlock per `spec/dev-notes/iced-ui-render-verification.md`). D10 NO new dependency, `cargo tree -p ui` unchanged. PAPER/SIM ONLY. Leans on ADR-0059 / ADR-0063 / ADR-0066 / ADR-0057 / ADR-0003. | accepted | 2026-06-24 |
| 0070  | Promotion wiring — carry a robustness-gated Tune config into the forward €200 paper-trade (feature `advisor-param-promotion`, the ADR-0069 § D5 deferred v0.2 carry-forward: wire the "Use this config" affordance — today a visual pill with NO message, `screens/tune.rs:967-970` — so a PROMOTABLE config carries the TUNED strategy into the existing F5 paper loop + F6 forward plan). D1 a single optional `param_override: Option<ForwardParamOverride>` field on `ForwardRunConfig` (`config.rs:32-42`), honored by BOTH forward resolvers; `None` byte-identical to today (anchor-safe). `ForwardParamOverride` = an agent-owned closed enum (NOT `backtest::SweptParams` — keeps the UI carrier's `core`-types-only invariant) one-per-family `Sma{fast,slow}`/`Macd{fast,slow,signal}`/`Rsi{period,oversold}`/`Bollinger{period,k_tenths}` (k as tenths, the `PlanRuleKind::BollingerReversion` convention config.rs:94, no `Decimal` field); `build_registry_for` (runtime.rs:331) + `build_forward_plan_from_registry` (plan.rs:190) each gain a `Some` branch resolving from the override, the `None` path runs the EXISTING match arms UNCHANGED → crowned-pick forward path byte-for-byte preserved, `forward_run_engine_fidelity.rs` passes unchanged, 119/119. D2 composed-family overrides REUSE the sweep's TOML generators promoted to `pub` (`macd_toml`/`rsi_toml`/`bbands_toml` sweep.rs:570-611; sweep keeps calling them, `agent` already deps `backtest` → no new edge), validated by the SAME `ComposedStrategyConfig::from_str(&toml,stem)` identity guard the sweep used to SCORE the cell → what-paper-trades == what-the-gate-scored; in-memory string (NO new disk file), synthetic `source_path="tuned:<stem>"`; duplicating the DSL agent-side REJECTED (drift). D3 the override reaches F5 (registry, runtime.rs:1096) AND F6 (plan, runtime.rs:1193) in the supervisor `Launch` arm, BOTH built from the SAME generator → identity guarantee is STRUCTURAL (ADR-0062 §D3 plan↔loop consistency preserved); the F6 plan describes the tuned rules automatically (the `PlanRuleKind`→`PlanRuleView`→IF/THEN mirror, adapter.rs:87-118). D4 `PromoteSweptConfig(PromoteParams)` UI message; the UI carries a CLOSED `PromoteParams` enum (UI-owned, four families) added to `SweepCellRow` + populated at the ONE engine→UI boundary `SweepReportMirror::from_report`/`cell_to_row` (tune/state.rs:191, from `cell.params`) → `from_report` stays the ONLY `SweptParams` read; the pure `update` arm (mirroring `open_strategy_in_lab` state.rs:2385) preseeds `pending_forward_promotion` + navigates to `Screen::ForwardPlan`; the binary layer (`cockpit_live.rs`, the `BakeoffRunCompleted` crowned-launch precedent 1531-1593) maps `PromoteParams→agent::ForwardParamOverride` + builds `ForwardRunConfig{param_override:Some}` + reuses the F7/ADR-0065 €→USDT + `try_send(ForwardCommand::Launch)`. TWO boundaries each crossed ONCE (`SweptParams→PromoteParams` from_report; `PromoteParams→ForwardParamOverride` binary). D5 ONLY `promotable` (non-FRAGILE) configs promote; the FRAGILE lock + the frozen gate UNTOUCHED; `promotable==!Fragile` already at the boundary (tune/state.rs:193), the affordance gates `on_press` behind it, a Fragile row keeps its locked label + emits NO message; promotion READS the verdict never recomputes; Marginal stays promotable (reaffirms ADR-0059 §D4 / ADR-0063 §D4 / ADR-0066 D3 BYTE-FROZEN). D6 honest provenance framing `TUNE_PROMOTE_CONFIRM_FMT` (zero literals) on the forward-plan header when promoted: "You tuned this {family} config — survived resampling on {window}, not a guarantee, not advice", DISTINCT from the crowned "best of the bake-off" provenance; `TUNE_DISCLAIMER` footer STAYS. D7 day-1 divergence+fidelity gate (CLAUDE.md non-negotiable, v3-vol-overlay-noop precedent) `forward_promotion_divergence.rs`: (a) divergence — same id `None` vs `Some(tuned)` SAME bars → ≥1 differing signal (FAILS-before); (b) fidelity — agent's generated composed TOML byte-equals the sweep's for identical params + resolved id==stem; (c) plan-reflects-tuned — `build_forward_plan_from_registry` with an SMA override emits the tuned `PlanRuleKind::SmaCross` not 20/50. D8 anchor-safe (`None` path byte-identical → 119/119) + render-PIXEL verified (Proof 1 promotable row = ENABLED accent button vs the FRAGILE greyed-locked-label negative control; Proof 2 post-promote forward-plan = the TUNED IF/THEN + the promote-framing header, crowned header the negative control; font-mutex serialized per the dev-note). D9 NO new dependency, `cargo tree -p ui` UNCHANGED (`PromoteParams` UI-owned; the `PromoteParams→ForwardParamOverride` map binary-side where `agent` is already imported). PAPER/SIM ONLY. Leans on ADR-0069 / ADR-0060 / ADR-0062 / ADR-0065 / ADR-0059 / ADR-0057. | accepted | 2026-06-25 |
| 0071  | OBV DSL primitive + the pre-registered 5-arm signal-library expansion (feature `advisor-signal-library-expansion`, the 3rd + cheapest pre-registered arm-class expansion after combination-search ADR-0067 + short-selling ADR-0068; operator-approved 2026-06-26; PAPER/SIM ONLY). The operator RATIFIED a FIXED 5-arm slate = 4 DSL-only arms + ONE new primitive (OBV — the operator chose to include exactly one genuinely-new indicator, overriding the analyst defer-all lean; ATR/VWAP stay deferred). D1 ship OBV as TWO minimal new DSL indicators `obv` (arity 0) + `obv_avg(N)` (arity 1), each a new `IndicatorState` variant (`node.rs:26`) mirroring `Sma`/`RollingAvg`, with the 6-site evaluator surface added twice (`indicator_arity` parser.rs:32 / `IndicatorState` / `latest()` / `on_bar` / `eval_indicator_expr` + a `find_*` / `add_indicator`); NO `ast.rs` RuleAst variant (OBV is a plain value indicator). REJECTED `avg(obv,N)` (IMPOSSIBLE — the rolling family's `field_arg` accepts ONLY `Expr::BarField` and `obv` is not a bar field → `UnknownParam` parser.rs:357) + a degenerate `obv()>0` single-primitive signal. D2 LOCKED recurrence `OBV_0=Some(0)` (seed prev_close on bar 0 like `Rsi`), `OBV_t = OBV_{t-1} + sign(close_t−close_{t-1})·volume_t`, all `rust_decimal::Decimal` (NO f64); `obv_avg(N)` OWNS its inner `Obv` (Box<IndicatorState>, the MacdLine-owns-EMA pattern) + rolls a window (RollingAvg gating), `None` during warm-up → comparison false; spelled `obv()` (empty parens — the FIRST 0-arity indicator, a bare `obv` errors because call-detection peeks for `(` parser.rs:348); REQUIRED OBV identity/round-trip guard (the t505 + ADR-0069 discipline): `from_str` round-trip + textbook OBV vs a hand-computed reference EXACTLY (Decimal, all 3 sign branches) + `obv_avg`==SMA-of-OBV + a dedicated `obv()` parser test. D3 the FIXED slate LOCKED literals (NO search, the overfit defense): `v0.donchian_break`=`close>max(high,20)`, `v0.donchian_floor`=`close>min(low,20)`, `v0.vol_breakout`=`close>max(high,20) AND volume>2*avg(volume,20)`, `v0.roc_momentum`=`close>avg(close,10)*1.05`, `v0.obv`=`obv()>obv_avg(20) AND close>sma(close,50)` — spanning the breakout/channel + volume-flow axes the existing 4 (price-only) do not cover. D4 each arm is just another candidate via the `v0.5.macd` seam (engine.rs:1234 → `sma_composed_run::run`, `composed_toml_override:None`, a UNIQUE non-anchored `scenario_name`); id in `default_field()` (bakeoff/mod.rs:355); `advisor_field_arm_count` test bump 13→18; the gate (`classify_verdict`/`verdict_bands`/`compute_robustness_flag`) + buy-and-hold benchmark (ADR-0066) BYTE-UNCHANGED — more candidates, SAME bar; NOT a B2/B3 band proposal. D5 anchor-safe by construction (`write_report=false` → `maybe_write_report` no-ops → 119/119 before+after; the existing 4 base TOMLs + their anchored reports byte-identical; OBV edits are ADDITIVE enum variants; the 9 `spec/anchors.toml` SHAs untouched) — NO anchor-additive amendment owed. D6 day-1 baseline-equity-divergence e2e (CLAUDE.md non-negotiable) on the REAL TOMLs (`from_str`→`from_config`): ≥1bp from ≥1 existing arm AND from buy-and-hold; no two new arms identical; factory smoke + FAIL-before/PASS-after; flagged + handled the `donchian_floor ⊂ btc_rsi_reversion` clause overlap (strict superset → diverges). D7 UI = 5 friendly `display_label` (leaderboard.rs:957) + 5 strings + a populated render-PIXEL guard at ~18 rows with a negative control (macOS-canonical, ADR-0057); the new arms do NOT break Tune (ADR-0069 sweeps only the 4 existing families) or forward-plan (`describe_plan` SmaCross fallback node.rs:1358, no panic). HONEST framing LOAD-BEARING: the new signals are very likely ALSO Fragile (modal `BenchmarkWins` per ADR-0066); the deliverable is honest COVERAGE + a richer decorrelation MENU, NOT an alpha claim; a null all-Fragile result is valid + expected + shippable; the gate decides. Follow-ons OUT of v1: ATR/VWAP primitives, combination arms USING the new signals, short `_ls` variants, any param sweep, per-arm forward-plan narration. Leans on ADR-0067 / ADR-0068 / ADR-0066 / ADR-0069 / ADR-0063 / ADR-0059 / ADR-0010 / ADR-0057. | accepted | 2026-06-26 |
| 0072  | DVOL implied-vol exogenous-series probe + the `v0.dvol_regime` arm (feature `advisor-options-impliedvol-probe`, #4 fresh-channel probe — the options/implied-vol angle the on-chain probe could not reach; PAPER/SIM ONLY). Locked causal signal (NO search, the overfit defense): per coin, daily, hold the coin iff `dvol_t < median(last W=30 distinct daily closes as-of t)` else cash (tie→cash, warm-up→hold). D1 a hand-written `DvolRegimeStrategy: Strategy` in `crates/strategy/src/dvol_regime.rs` — the composed DSL CANNOT express a per-bar EXOGENOUS series (its indicators read only the OHLCV bar). D2 a new `crates/backtest/src/dvol_data.rs` (clone of `basis_data.rs`) + `DvolDataSource::load` (SHA-verify vs `EXPECTED_DVOL_REVISION_SHA`) + `dvol_as_of` DELEGATING to the ADR-0058 `core::pit::PitSeries::as_of_value` → look-ahead-IMPOSSIBLE LOCF daily→hourly join, NO new join code. D3 a new `ScenarioConfig.dvol_override: Option<Vec<Option<Decimal>>>` (default `None`, mirrors `funding_override`/`basis_override` at engine.rs:1057). D4 a new `data/deribit-dvol/` corpus (`REVISION.toml` pinned, parquets gitignored) + `crates/data/src/bin/fetch_deribit_dvol.rs` (clone of `fetch_binance_premium.rs`; Deribit `public/get_volatility_index_data`, currency BTC/ETH, resolution=43200, no-auth). D5 registration `v0.dvol_regime` in `default_field()` (bakeoff/mod.rs) + a `run_scenario` match-arm (engine.rs, like `v0.obv`); NOT a ScoreSource/SweepFamily; BTC+ETH universe ONLY via a `dvol_supported(symbol)` predicate (arm ABSENT otherwise, never crash). D6 day-1 gates `dvol_regime_divergence_end_to_end.rs` (≥1bp from buy-and-hold over a regime flip) + `dvol_regime_leak_check.rs` (future-shift → decisions differ). Anchor-safe by construction (`write_report=false` → 119/119), the gate (`classify_verdict`/`verdict_bands`) + ADR-0066 benchmark BYTE-FROZEN. The probe is EXPECTED Fragile/null — a valid honest-coverage result, not an alpha claim. Leans on ADR-0058 / ADR-0059 / ADR-0071 / ADR-0066 / ADR-0057. | accepted | 2026-06-26 |
| 0073  | Market-calendar layer + the macro cross-asset risk-regime arm (feature `advisor-crossasset-macro-regime`, #4 fresh-channel probe — the macro/rates angle WITH a reusable trading-calendar foundation; PAPER/SIM ONLY). D1 a NEW `crates/data/src/calendar.rs` `enum MarketCalendar{Crypto24x7,UsEquity}` + `classify_ticker(&str)` + `trading_days_in_range`; the calendar is DERIVED FROM the ticker INSIDE `load_cached` → its public signature stays UNCHANGED → all 14 call sites + crypto coverage BYTE-IDENTICAL; `expected_bars_for_range` left byte-identical, an additive `expected_bars_for_calendar(cal,…)` added; UsEquity = weekdays − a static `US_MARKET_HOLIDAYS` lower-bound set (the ≥95% coverage floor absorbs imprecision). D2 a locked arm `v0.macro_riskon`: risk-ON ≙ `^GSPC>SMA(50) ∧ DX-Y.NYB<SMA(50) ∧ ^TNX<SMA(20)` ⇒ hold the coin (full budget) else flat; a hand-written `run_macro_gated_buyhold_path` in `bakeoff/buyhold.rs` SIBLING of the pure `run_buyhold_path` (so a ComposedStrategy-has-no-demux case never arises). D3 the LOCF daily→hourly join = `regime.as_of_value(bar.open_ts)` via the ADR-0058 `core::pit::PitSeries<bool>` (look-ahead-impossible); a new `crates/backtest/src/macro_regime.rs::load_macro_regime_series` + a new `ScenarioConfig.macro_regime_series: Option<PitSeries<bool>>`. D4 DX-Y.NYB primary (`^DXY` pre-registered fallback; an M-FETCH-0 dry-run confirms availability); a new `BakeoffConfig::default_macro_field()`. D5 day-1 `macro_regime_overlay_end_to_end.rs` (S1 divergence, S2 no-op control, S3 leak-check, S4 warm-up). Anchor-additive/safe (`write_report=false`; 119/119 re-proved at 3 phase checkpoints), the gate + benchmark BYTE-FROZEN; market-calendar lands + is proven INERT before the arm. EXPECTED Fragile/null — valid. Leans on ADR-0040 / ADR-0058 / ADR-0059 / ADR-0066 / ADR-0057. | accepted | 2026-06-26 |
| 0074  | Read-only reflection decision-support surface — the honest C4 (feature `advisor-reflection-decision-loop`; PAPER/SIM ONLY). Autonomous param/route ADAPTATION is moot AND a footgun on a deliberately ship-passive product, so C4 ships as a DETERMINISTIC read-only MEMORY surface (operator-as-adapter, the F9 narration-only treatment — never re-ranks, never predicts). D1 a new read-only `crates/trader/src/decision_memory.rs::recall_decision_lessons(store: &dyn reflection::ReflectionStore, query) -> Result<DecisionMemorySummary, RetrievalError>` — homed in `crates/trader` per the ADR-0041 t1809/t1810 layering gate (a 2nd `retrieve_top_k` consumer beside `ForecastContext::from_runtime`), reducing `Vec<LessonCard>` to a `core`-typed `DecisionMemorySummary{symbol,strategy,match_count,most_recent,entries}` with trader-owned CLOSED enums — NO reflection type crosses into `ui`. D2 three decision-point surfaces — S1 a Leaderboard chip, S2 a Tune note, S3 forward-plan context (S3 deferrable to v0.2) — each a ui-owned `MemoryNoteState{Absent, Present(MemoryNote)}` hydrated via a per-screen `…MemoryHydrate` message (pure sqlite read, NO agent channel); empty-store = SILENT absence; the adapter per screen behind `#[cfg(feature="live")]` (the forward_plan/adapter.rs precedent → `cargo tree -p ui` UNCHANGED). D3 recording stays FORWARD-ONLY for v1 (lessons only from `crates/exec/src/paper.rs::on_trade_close`; NO bake-off→lesson tap). Anchor-safe (read-only → 119/119), the gate UNTOUCHED; render-pixel proofs `leaderboard_memory_chip_render.rs` (+ an empty-store negative control); a day-1 divergence e2e is N/A (narration-only, like F6/F9). Leans on ADR-0041 / ADR-0064 / ADR-0059 / ADR-0057. | accepted | 2026-06-26 |

| 0075  | Overfitting scorecard (P0-1) — report-only `Scorecard` on `Recommendation`: `n_candidates`, `n_eff = ρ̄+(1−ρ̄)·M` closed-form (D4 frozen at MAX_SWEEP_CONFIGS=24), `deflated_sharpe` (Bailey & López de Prado DSR formula with skew/kurt), `min_btl_years = 2·ln(N_eff)/SR²`, `pbo: None` in v2 (D1 deferred to Tune surface), `crown_clears_dsr: bool` informational. Carried via `BakeoffReportMirror::from_report → ScorecardView` (zero new `ui` dep edge). Leaderboard "How much to trust this" panel (4 facts + glosses; `frame::panel` reuse; zero new widgets/tokens). FROZEN gate byte-untouched (D3 report-only; `scorecard_does_not_change_ranking` is the proof). Anchors 119/119 by construction (advisor bake-off runs `write_report=false`). Feature `advisor-overfitting-scorecard`. | accepted | 2026-06-30 |
| 0076  | Turnover + coherent-tail KPIs (P1-1 + P1-2) — `CandidateKpis.turnover: Decimal = Σ(fill.price·fill.qty)/mean_equity` (Churn column, "N.N×" format); `DistributionSummary.{cvar_95, cvar_99, median_terminal_wealth, skew}: f64` reductions over the existing 1000-path `PathMetrics` vector. New `TailSummary` carried on `Recommendation.crown_tail` (crown-only, one panel per bake-off); mirrored as `TailSummaryView` over `from_report` (zero new `ui` dep edge). "Risk story" leaderboard panel: 6 facts (typical outcome / CVaR-95 / CVaR-99 / skew / Sortino / Calmar) with plain-language glosses. **CVaR not VaR — coherence is the architectural invariant** (sub-additive; non-coherent measures reward concentration). Report-only (D3 carry-over). FROZEN gate byte-untouched (`turnover_does_not_change_ranking` is the proof). Anchors 119/119 by construction. Feature `advisor-turnover-and-tail-metrics`. | accepted | 2026-06-30 |
| 0077  | Forward-buildability contract — every crownable arm is forward-buildable; unknown still bails; `None` path byte-identical. Wires the 14 post-F5b arms (5 ADR-0071 DSL, 6 ADR-0067 ensemble, 1 ADR-0072 DVOL, 1 ADR-0073 macro) into `build_registry_for` (`runtime.rs`) and `build_forward_plan_from_registry` (`plan.rs`). ADR-0071 arms → `load_composed_strategy_from_toml` (SAME TOML as bakeoff). ADR-0067 arms → `build_ensemble` (already knows all 8; one match-arm widening). ADR-0072 DVOL → `DvolRegimeStrategy::new(symbol, vec![], DVOL_REGIME_WINDOW)` (empty as_of = warm-up flat; plan describer = `AlwaysLongStrategy`). ADR-0073 macro → `AlwaysLongStrategy` (graceful degradation; no real-time macro corpus in forward run). `unknown => bail!` STAYS. `None` path byte-identical → anchors 119/119 unchanged. 13 new `r1_*` forward-buildability tests in `forward_run_engine_fidelity.rs`. Amends ADR-0060 § D3. Feature `advisor-forward-fidelity-coverage`. | accepted | 2026-06-30 |
| 0078  | Vol-targeting overlay reposition (P1-4) — reparam the existing `vol_targeting_overlay.rs` as a RISK tool not a Sharpe tool (crypto's leverage effect is reversed, γ≈−0.261). Switch σ̂ to `vol_estimator::ewma_realized_vol` with `LAMBDA_126D_HOURLY` (slow EWMA via a new `VolSource` enum; `Default` keeps GARCH for backward-compat). Add `no_trade_band: Decimal` (default 0.0 on `Default`, 0.05 on `p1_4_defaults()`) — bound turnover. Add `derisk_only: bool` (default `false`/`true` resp.) — never upsize on a vol drop. Add `ReturnVolCorrelation` (Pearson ρ over the window) — operator-readable "is the Sharpe mechanism mechanistically present?" telemetry. Existing e2e stays byte-identical on `Default`. Anchor-safe (advisor path runs `write_report=false`). FROZEN gate byte-untouched. Feature `advisor-vol-overlay-reposition`. | accepted | 2026-06-30 |
| 0079  | Shared σ̂ vol estimator (P1-5) — pure, stateless module `crates/strategy/src/vol_estimator.rs`: `log_returns_from_bars`, `realized_vol_from_returns`, `ewma_realized_vol` (EWMA/RiskMetrics-style recurrence), `har_realized_vol` (Corsi 2009 HAR-RV daily+weekly[5]+monthly[22]); three exported λ constants (LAMBDA_126D_DAILY≈0.9945, LAMBDA_126D_HOURLY≈0.999771, LAMBDA_RISKMETRICS=0.94). Home per operator-ratified D5 (v2-architecture.md §6.0): `crates/strategy` — NOT `forecast` (keeps "vol-for-sizing ≠ return-prediction" clean; `ui` never imports). Zero new dep edge. Anchor-safe by construction (no engine path; 119/119 unchanged). Feature `advisor-vol-estimator`. | accepted | 2026-06-30 |
| 0080  | Drawdown-control overlay with static CPPI @ 20% drawdown floor + load-bearing HWM restart (Phase 2C P1-3): `DrawdownControlOverlay<S: Strategy>` generic wrapper in `crates/strategy/src/drawdown_control_overlay.rs`. Normalised cushion multiplier `M(k) = (d_max − d(k)) / (d_max × (1 − d(k)))` with M(0)=1.0/M(d_max)=0. Static floor = `initial × 0.80` NEVER moves (D8 — TIPP/ratcheting deferred v0.2). HWM restart is LOAD-BEARING (BTC benchmark [Hsieh 2022]: Sharpe 1.52 + max-DD 72%→20% WITH restart; Sharpe −0.04 WITHOUT). Composed via `Strategy::quantity_scale` — budget cap NEVER bypassed (D5). Day-1 divergence gate in `drawdown_control_overlay_end_to_end.rs` (6 tests; mandatory CLAUDE.md non-negotiable). Anchor-safe by construction (write_report=false; 119/119). Feature `advisor-drawdown-control-overlay`. | accepted | 2026-06-30 |
| 0081  | Cost-model opt-in `VolScaledSpread` variant (default unchanged) — Phase 2D P1-6. Adds `SlippageModel::VolScaledSpread { base_bps: u32, vol_multiplier: f64, sigma_window: usize, sigma_lambda: f64 }` to `crates/cost/src/slippage.rs` as an **opt-in-forever** new variant (D6, operator-ratified 2026-06-28). Default `Linear { bps: 8 }` byte-identical — anchors 119/119 by construction. Formula: `effective_bps = base_bps + vol_multiplier · σ̂_ewma(bar_returns) · 10_000`; defaults: base=8, mult=2.0, window=20, λ=0.94 (RiskMetrics). σ̂ EWMA inlined (D1 — `cost → strategy` dep would create a cycle; `strategy` dev-depends on `cost`). Adds `apply_slippage_model_with_returns` full dispatcher + `fee_sensitivity_report` helper (report-only fee-sweep). Removes `Eq` derive (f64 fields). `default_is_linear_bps_8` unit test is the load-bearing backward-compat proof. Feature `advisor-cost-model-opt-in`; venue-trust map in `spec/dev-notes/venue-trust-map-2026-07-01.md`. | accepted | 2026-07-01 |
| 0082  | `feature.md status:` is the single source of truth for `trace.toml state=` (process/schema; closes the recurring audit-2026-06-29/07-06 lifecycle-drift finding). D1 `feature.md` frontmatter `status:` is authoritative; the trace `[[req]] state=` must not contradict it; data-flow is `feature.md → trace`, never reverse. D2 THE INVARIANT — when a feature's `feature.md status:` is `shipped`, every trace row resolving to it MUST read `state="shipped"`. D3 the tester-terminal aliases (`verified`/`passed`/`tested`/`tester-done`) + the other intermediate states (`arch-done`/`design-complete`/`dev-done`/`presenter-done`) are legitimate ONLY pre-ship — never flagged while `feature.md ≠ shipped`. D4 flips preserve the old comment verbatim after a `PRIOR:` marker. D5 mechanically enforced by `scripts/spec_lint.py` category `feature-shipped-trace-drift` (resolves the `feature=` slug across `spec/`+`spec/v1/`+`spec/v2/` per the existing `check_trace`; asserts `state=="shipped"` iff the feature is shipped) with a synthetic pass+fail fixture in `--self-test`. D6 the `trace.toml` header legend now enumerates the full `state` vocabulary + states D2 inline. Reconciled the 11 live `passed`-on-shipped rows to `shipped` in the same change; `spec_lint.py` then reports zero drift. **§ Changelog 2026-07-10 amendment (remediation-plan P6a):** a SIBLING enforcement of the D1 invariant on the OTHER derived index — `spec_lint.py` category `feature-shipped-changelog-missing` (every `status: shipped` feature ⇒ a `CHANGELOG.md` reference; slug / REQ-id / folder-path / iteration-suffix-base-slug match + a per-entry-justified `CHANGELOG_ROLLUP_ALLOWLIST` for the documented thematic-rollup convention) with its own `--self-test` fixture; closes the R3-4b drift class (the v2 tranche + v3 close-out were missing from the canonical index). Enforcement extension inside D1, not a new decision → amendment, no new ADR. | accepted | 2026-07-09 (+ 2026-07-10 P6a amendment) |
| 0083  | Named "Calibrate" stage + the DATA→CALIBRATE→ANALYZE→SUGGEST spine stepper (v3 close-out R3-3a, feature `advisor-calibrate-stage`; formalizes v2 D7). D1 the stepper is an ORIENTATION band (not a router): a horizontal DATA·CALIBRATE·ANALYZE·SUGGEST band pushed at the top of the shell `centre` Column (`crates/ui/src/shell.rs`, above `body`, halted-banner precedent), elided off the advisor journey. D2 resolve the highlighted stage from a pure `stage_for(screen, &leaderboard_state)`: `Leaderboard`+`Empty`→DATA, `Leaderboard`+`Ready`→ANALYZE (DATA+ANALYZE share `Screen::Leaderboard` — the decisive IA finding; discriminator reads the EXISTING `PanelState`, no new field), `Tune`→CALIBRATE, `ForwardPlan`→SUGGEST, else `None`. D3 DEFER the `agent::AdvisorStage` context-carrier AGAIN (D7 still holds — the stepper needs only `current_screen`; no `ui`↔`agent` coupling). D4 promote `Screen::Tune` to a sidebar-visible **"Calibrate"** entry (display label only; variant stays `Screen::Tune`) inserted into BOTH `SIDEBAR_ENTRIES_PHASE_A` and `SIDEBAR_GROUPS_PHASE_C` Work group in lock-step (the flatten-invariant test `sidebar_groups_phase_c__flatten_matches_phase_a` is the guard), between Leaderboard and ForwardPlan; existing `OpenTuneEditor` drill-down preserved. D5 zero literals/hex (`crate::strings`+`crate::theme` tokens, no new token), no new dependency (`cargo tree -p ui` unchanged). D6 verification at the RENDERED-PIXEL layer (CLAUDE.md non-negotiable): `#![cfg(target_os="macos")]` `stage_stepper_render.rs` proving Tune→CALIBRATE highlight + the negative-control ForwardPlan→SUGGEST + the DATA/ANALYZE discriminator, plus `stage_for` unit tests. Anchors 119/119 by construction (UI-only, `write_report=false` untouched); FROZEN gate `bakeoff/{robustness,rank}.rs` not read/changed. | accepted | 2026-07-09 |
| 0084  | P2 corpus set + Coinbase second-venue adapter + multi-corpus verdict-rerun harness (remediation-plan P2, feature `advisor-corpus-expansion`; extends the evidence base behind the 2026-06-08 ship-passive verdict). D1 RATIFY the 3-corpus Binance set unchanged (`data/binance-1718` 2017-08→2018-12 BTC/ETH/BNB; `data/binance-2020` 2020 the-7-pre-2020-listers; `data/binance-2526` 2025-01→last-CLOSED-month all-10; honest per-window subsets driven by uneven listing dates). D2 (THE key decision) Coinbase Exchange hourly RATIFIED as the second venue — Kraken REST OHLC hourly deep-history is INFEASIBLE (720-candle total cap, no backward `since` paging); a NEW `crates/data/src/coinbase_klines.rs` + `fetch_coinbase_klines` bin are required because the shipped `coinbase.rs` is a live-WS feed that CANNOT backfill; the ONE real seam = the Coinbase venue shim (canonical on-disk `BTCUSDT` via `coinbase_symbol_map`→product-id `BTC-USD`; candle order `[time,low,high,open,close,volume]`; `time` seconds→millis; no `trade_count`→0; 300-candle window step, ≥200ms pace) into the SAME venue-agnostic 8-col parquet + REVISION.toml; cross-check corpus `data/coinbase` = BTC-USD hourly 2020→last-closed-month, BTC only; Kraken OUT of P2. D3 FETCH DVOL(2021-22+2025-26)+macro(2025-26) — bounded, additive, existing pinned SHAs byte-identical — so `v0.dvol_regime`/`v0.macro_riskon` are genuinely evaluable on 2122/2526; perp-basis/funding NOT back-filled (stay legitimately ABSENT); the R4 arm×corpus matrix is the harness's arm-selection contract. D4 (re-run seam) a DEDICATED `crates/backtest/tests/p2_verdict_rerun.rs` harness, NOT a `--corpus` selector — `run_bakeoff` hardcodes `BINANCE_CORPUS_ROOT="data/binance"` in the `pub(crate)` `preload_bakeoff_binance_bars`, so the harness composes the two PROVEN pieces (`ReplayFeed::new(<corpus_root>).merge_symbols` per `bear_survey.rs:168` + `null_data_no_crown.rs::run_field_and_rank`'s exact per-arm sequence: `run_scenario` bars_override→`derive_candidate_kpis`→`compute_robustness_flag`→`rank_candidates`→`compute_scorecard`), ZERO shipped-runner change, `write_report=false` on every arm; DVOL/macro threaded via the SAME `resolve_dvol_override`/`load_macro_regime_series`; absent arms simply not-added-to-that-corpus's-field (shown "not evaluable (no <data>)", never a silent drop or warm-up-proxy-as-evaluation); SKIP-safe when parquets absent. D5 clamp `binance-2526`/`coinbase` `--end` to the last fully-closed UTC month (no partial trailing month in the pin — idempotency). D6 stronger survivor-of-survivors caveat for 1718/2020 in the REPORT prose (no code; the shipped survival note already fires). D7 register E-2 (do NOT change the flat-8bps default — frozen for cross-regime comparability) + an opt-in `VolScaledSpread` (ADR-0081) era-cost SENSITIVITY annex for 1718/2020; primary verdict stays on the frozen default. D8 anchors 119/119 + FROZEN gate (`bakeoff/{robustness,rank}.rs`/`classify_verdict`/`verdict_bands`/ADR-0066 benchmark) byte-untouched + existing pins (3a8b96c4/4f390622/broaduni/DVOL/macro/basis/funding) byte-immutable + `ci.yml.deferred` parked + €200 SIMULATED, ALL BY CONSTRUCTION; the 9 anchors.toml SHAs untouched, NO anchor-additive re-emission owed. Report contract = the tester's AC1-AC8 (per-corpus×per-arm verdict table, null-CI `crown_clears_dsr`, MinBTL before/after, wobble list, venue reconciliation, survivorship+era-cost caveats, gate section, top-line HOLDS/WOBBLES/BREAKS sentence). Leans on ADR-0032 / ADR-0059 / ADR-0066 / ADR-0072 / ADR-0073 / ADR-0081. | accepted | 2026-07-09 |
| 0085  | The crown co-presents its overfitting (DSR) verdict (remediation-plan P1, D1=(a) presentation-layer, feature `advisor-crown-credibility`; additive UI, NO gate change). The FROZEN gate crowns pure noise ~1/5 seeds (P2-2); the DSR scorecard catches every one (`crown_clears_dsr=false`) but sits in a panel BELOW the ranked table while the crown is an H2 headline at the top — a user trusting the crown alone is misled at a measurable rate. D1 the crown banner (`recommendation_block`, `crates/ui/src/screens/leaderboard.rs`) CO-PRESENTS the credibility verdict inline, directly under the H2 headline, for an active crown (`ActiveWins`). D2 the state is a PURE projection `crown_credibility(outcome: OutcomeKind, scorecard: Option<&ScorecardView>) -> CrownCredibility{Passes|WeakEvidence|NotApplicable}` — NO new stored state field, NO `crates/backtest` change (reads the EXISTING informational `crown_clears_dsr`, never a veto); mirrors ADR-0083 D2 `stage_for`. D3 three states: `Passes` (ActiveWins+clears) = a muted ✓ ACCENT line; `WeakEvidence` (ActiveWins+fails, the money shot) = an unmissable ⚠ WARN_500-on-WARN_50 bordered band with plain-language non-alarmist copy naming the pick as weak evidence + the mechanism ("with this many strategies tried, an edge this size can appear by chance"), qualifying — not negating — the still-true headline; `NotApplicable` = banner byte-identical to today. D4 `BenchmarkWins`/`AllFragile` carry NO credibility badge — buy-and-hold is exempt from the gate (ADR-0066 D1) and the DSR is computed on the max-Sharpe ACTIVE arm (a loser), so a badge on a hold pick would be actively misleading; the existing headline + scorecard panel already read correctly. D5 dual-mode (`ModeColor::current`, no new token) + colour never the only signal (⚠/✓ glyph + words) + zero literals/hex (new `crate::strings` in `all()`) + no new dep (`cargo tree -p ui` unchanged). D6 verification at the RENDERED-PIXEL layer (CLAUDE.md non-negotiable): `#![cfg(target_os="macos")]` `crown_credibility_render.rs` — the WeakEvidence band paints on the banner (`fake_bakeoff_report_mirror_five_arm`) + a `crown_clears_dsr=true`→Passes negative control + a `BenchmarkWins` no-band case + `crown_credibility` unit tests per D3/D4 row. D7 anchors 119/119 by construction (UI-only, `write_report=false` untouched, scorecard rides `Recommendation` off the anchored path); FROZEN gate `bakeoff/{robustness,rank}.rs`+`scorecard.rs` byte-untouched; the veto stays unbuilt (do-not-build E-1). Numbering: took 0085 — the sibling P2 ADR had already claimed 0084. Leans on ADR-0075 / ADR-0066 / ADR-0083 / ADR-0057. | accepted | 2026-07-09 |
| 0086  | Look-ahead lint + explicit publication lag — the ADR-0058 v0.2 follow-on (remediation-plan P3, feature `advisor-pit-discipline`; the do-not-build register's ONE named open gap + qlib-gap #1). Delivers the two halves ADR-0058 § D5 DEFERRED to "a v0.2 follow-on if a fresh data channel opens" — DVOL (`v0.dvol_regime`, ADR-0072) + macro (`v0.macro_riskon`, ADR-0073) are those channels. GROUNDING (determines hardening-vs-fix): BOTH exogenous joins are ALREADY as-of-correct today — `dvol_as_of` (dvol_data.rs:375-390) builds a `PitSeries` + queries `as_of_value` keyed on `day_close_ts_ms = day_open+86_400_000−1`; `load_macro_regime_series` (macro_regime.rs:209) builds `PitSeries<bool>` keyed on `bar.close_ts` ≈ EOD UTC with trailing SMAs — so P3 is HARDENING + LINT + PROOF, NOT a bug fix (no leak found). D1 a STANDALONE grep lint `scripts/check_no_raw_asof_join.sh` mirroring `check_no_clocks_in_ui_tests.sh` (predicate-SHAPE match on `partition_point(t<=q)`/`binary_search*` outside the sanctioned `crates/core/src/pit.rs` home + a per-line `// PIT-OK:` allowlist marker + `--self-test` negative control + wired into `rust-validate`'s pre-test gate); NOT a `spec_lint.py` category (that linter is docs/spec-only + owned by the parallel P6a CHANGELOG-lint work → a separate script avoids the collision); a `cargo-deny` symbol ban REJECTED (false-pos/neg on legitimate non-temporal `partition_point`). D2 additive first-class `publication_lag_ms: i64` on `PitSeries` (`from_sorted_with_lag`/`from_unsorted_with_lag`; the existing ctors REDEFINED as `*_with_lag(_, 0)` ⇒ zero-lag default is BYTE-IDENTICAL; `as_of` returns the record with `record.ts + lag <= query`, implemented by querying against `query − lag` so the `partition_point(t<=q')` one-liner + the ADR-0058 §D3/D4 anchor-safe invariant are preserved verbatim at lag=0); the lag TABLE is grounded — every CURRENT series' lag is 0 because its key already encodes availability (DVOL EOD-close instant, macro/funding/basis settlement/close), a future release-lagged print (CPI/NFP) is now DECLARABLE + audited + demands its own ADR for a non-zero value. D3 DVOL + macro retrofit onto `*_with_lag(_, 0)`, proven BYTE-IDENTICAL by a legacy-predicate ≡ `*_with_lag(_,0)` element-for-element test (the `out_of_span_filter_via_*_as_of` pattern); anchor-safe BY CONSTRUCTION — both arms run the advisor bake-off path `write_report=false` (engine.rs:302-316) → NO anchored body moves → `verify_anchors.sh` 119/119 before AND after. D4 verification floor = the lint self-test + the byte-identity test + `core::pit` lag-0-reduction + positive-lag unit tests + zero anchor delta; the CLAUDE.md equity-divergence e2e is N/A (no decision variable — zero divergence IS success, per the ADR-0058 §D5 precedent). FROZEN gate `bakeoff/{robustness,rank}.rs` byte-untouched; the two research `f64` `examples/` diags stay allowlisted (unchanged from ADR-0058 § Alternatives). extends ADR-0058. Leans on ADR-0058 / ADR-0072 / ADR-0073 / ADR-0038. | accepted | 2026-07-10 |

All architectural decisions are now extracted. Remaining Phase 1A
work: final monolith compression (Changelog) and section-file body
finalisation.

## Changelog
- 2026-07-09 (architect): ADR-0083 added — named "Calibrate" stage + the
  DATA→CALIBRATE→ANALYZE→SUGGEST spine stepper (v3 close-out R3-3a, feature
  `advisor-calibrate-stage`). Formalizes v2 D7: promote `Screen::Tune` to a
  sidebar-visible "Calibrate" stage (display label only; variant unchanged) +
  add the visible orientation stepper; DEFER the `agent::AdvisorStage`
  context-carrier again. Key IA finding: DATA and ANALYZE share
  `Screen::Leaderboard`, so the stepper is an orientation band (not a router)
  resolving the highlighted stage from `current_screen` + the leaderboard
  panel substate. Render-layer verification is mandatory (CLAUDE.md). Anchors
  119/119 by construction; FROZEN gate untouched. Registered in the table
  above + `0083-calibrate-stage-and-spine-stepper.md`.
- 2026-07-09 (architect): ADR-0082 added — process/schema: `feature.md status:`
  is the single source of truth for `trace.toml state=`. Closes the recurring
  audit-2026-06-29/07-06 lifecycle-drift finding. The shipped invariant (D2: a
  shipped feature's trace row MUST read `state="shipped"`; the tester-terminal
  aliases verified/passed/tested/tester-done are pre-ship-only) is enforced by
  `scripts/spec_lint.py` category `feature-shipped-trace-drift` with a synthetic
  pass+fail fixture in `--self-test`; the `trace.toml` header legend now
  enumerates the full `state` vocabulary. Reconciled the 11 live `passed`-on-
  shipped rows to `shipped` (provenance-preserving `PRIOR:`) in the same change.
  Registered in the table above + `0082-trace-lifecycle-single-source-of-truth.md`.
- 2026-06-23 (architect): ADR-0067 added — pre-registered combination-slate
  expansion of the advisor field (feature `advisor-combination-search`): 6 new
  pre-registered vote arms (3 `Unanimous{n:2}` decorrelation pairs incl. the
  `trend_pair` predicted-null control + the complete k∈{1,2,3}-of-4 ladder) → a
  13-arm field, each scored through the byte-frozen Bootstrap gate. Registered in
  the table above + `0067-pre-registered-combination-slate-expansion.md`.
- 2026-06-21 (architect): ADR-0063 added — ensemble signal-vote seam +
  robustness-gate activation (feature `advisor-ensemble`, single-coin-advisor
  pivot F8). Registered in the table above + `0063-ensemble-vote-seam-and-
  robustness-gate-activation.md`. The F8 "mix" capability ships as
  `strategy::EnsembleStrategy` implementing the FROZEN `Strategy` trait (the
  `RegimeDispatcher` precedent generalised to N homogeneous members + a pure
  vote arbiter) — the ONE primitive reachable identically from `run_scenario`,
  `build_registry_for` (F5b forward-paper), and `StrategyRegistry`, bundled with
  the activation of the today-inert robustness gate via a new
  `RobustnessMode::Bootstrap` compute-and-feed path (reuses the existing
  Politis–White block-length selector + the ADR-0051 sub-seed determinism + the
  frozen `classify_verdict`). Anchor-safe by construction (119/119): `Skip` stays
  the default, the two ensemble arms + the `Bootstrap` mode are opt-in only on the
  no-report advisor bake-off path, the existing single-id `run_scenario` arms and
  `default_field()` are byte-untouched. Multiple-comparisons hardening
  re-evaluated and deferred (not-yet at a fixed 7-arm pre-registered field; ADR-
  trigger recorded). Ships with two day-1 e2e gates (the vote combines; the gate
  bites / anti-`Skip`-regression) per the CLAUDE.md non-negotiable + the
  `v3-volatility-forecaster-noop` precedent. Leans on ADR-0059 / ADR-0060 /
  ADR-0062 / ADR-0005 / ADR-0051.
- 2026-05-13 (architect): index initialised. ADRs 0001-0004 land in the
  same session (Session 1).
- 2026-05-13 (architect): ADRs 0005-0007 added (hot-load decisions
  cluster) during Session 4.
- 2026-05-13 (architect): ADRs 0008-0012 added (v0.5 strategy-registry
  resolution cluster) during Sessions 5+6.
- 2026-05-13 (architect): ADR-0013 added (v1 cross-sectional momentum
  resolution cluster) during Session 7.
- 2026-05-13 (architect): ADR-0014 added (v1.5a mean-reversion pairs
  cluster) during Session 8.
- 2026-05-13 (architect): ADRs 0015-0017 added (operator reports,
  real-mtm, v1.5b multi-venue) during Session 9.
- 2026-05-13 (architect): ADRs 0018-0020 added (Lumen Phase 1, v2 LLM,
  chart buy/sell emphasis) during Session 10. All strategy-registry
  Q&A clusters now extracted.
- 2026-05-16 (architect): ADR-0027 added (v2.5 Kronos forecast
  overlay — ONNX + `tract` integration, signal-level overlay on v1
  momentum). Cross-cutting overlay pattern landed in new section
  file `spec/architecture/12-forecast-overlay.md`.
- 2026-05-16 (orchestrator): ADR-0028 supersedes ADR-0027 after Wave A
  bootstrap surfaced (a) Kronos lives outside `transformers`,
  (b) two-model architecture requires Rust-side sampling-loop
  reimplementation, (c) crypto-fit was never validated. Operator
  pivoted v2.5 to "train small custom Transformer/TCN in `candle`".
  Wave A crates (forecast / replay-cache / core forecast types) are
  model-agnostic and preserved; only Kronos-specific files removed.
- 2026-05-13 (architect): ADRs 0021-0026 added (Foundation libraries
  substantive decisions) during Session 11. iced UI body migrated to
  06-ui-and-cockpit.md. All architectural decisions now in ADRs.
- 2026-05-17 (architect): ADR-0029 added (TCN/forecaster checkpoint
  provenance schema + LFS-anchor strategy — cross-phase contract for
  v2.5 TCN, v2.5a PatchTST, v2.5b vanilla Transformer). Also
  backfilled the registry table row for ADR-0028 (previously only in
  changelog).
- 2026-05-17 (architect): ADR-0030 added (cockpit in-process backtest
  engine API). Opens the `ui → backtest` edge for the Lab Run button
  shipped by `ui-rethink-phase-a-lab` per operator-decision Q-A2.
- 2026-05-17 (orchestrator): ADR-0031 added (status: proposed) —
  `AuditTick<Event, Context>` consumer envelope for the audit ledger
  read path. Borrowed from
  [barter-rs](https://github.com/barter-rs/barter-rs) per the survey at
  `spec/dev-notes/external-code-patterns-2026-05-17.md`. Decouples
  consumer-side state replicas from producer's channel choice via
  generic Iterator. Strictly additive (existing taps + broadcast tee
  coexist); zero hot-path impact; 11 anchors stay byte-identical.
  Implementation queued in `spec/backlog.md ## Queue`.
  Establishes the invocation pattern reused by Phase B / Phase E
  Compare matrix / v3 continuous-paper.
- 2026-05-18 (architect): ADR-0032 added — backtest real-Binance-data
  path + `REVISION.toml` data-revision pin for the four new
  `top10-*-fy-tcn-overlay[-weights]-realdata` scenarios. Locks
  (a) module placement (`crates/backtest/src/realdata.rs`,
  feature-gated `realdata`, reuses `data::ReplayFeed::merge_symbols`),
  (b) `REVISION.toml` schema + aggregate-SHA algorithm,
  (c) orthogonal `ScenarioDataSource` axis on `Scenario` (not new
  `ScenarioStrategy` variants), (d) `data_revision_sha` in both
  frontmatter (forensics, excluded from body hash) and a new
  `## Data source` body section (anchor integrity, covered by body
  hash). Existing 15 anchors stay byte-identical (K6 + K10).
  Closes T-AR-1, T-AR-3 of
  `spec/backtest-real-binance-data/tasks.md`.
- 2026-05-19 (architect): ADR-0034 added — Cockpit training control.
  Locks (a) audit-DB-as-seam (training events flow `train_tcn` writer
  → SQLite `training_events` → 1 Hz cockpit subscription reader) over
  alternatives (IPC, `agent::EventBus` channel) — Q1=(a) operator-
  confirmed 2026-05-19, (b) additive migration
  `010_training_events.sql` (anchor-byte-safe; 19 anchors stay
  identical), (c) subprocess lifecycle (sibling `lab::trainer.rs` to
  `lab::runner.rs`; SIGKILL-immediate on Cancel; PID column for
  orphan-detect; hard-fail on missing `--audit-db` file;
  `catch_unwind` boundary for `kind='failed'` row survivability),
  (d) R6 in-panel `widgets::training_plot` (new module, NOT a
  `ChartKind::TrainingCurves` extension of the main chart widget),
  (e) K5 CLI-drift mitigation via `cargo run --bin train_tcn -- --help`
  golden-snapshot test (NOT runtime `--print-config-schema`
  validation), (f) Q4=(a) status-strip orphan annotation (no
  auto-route). Surfaces a latent WAL-mode gap in
  `Ledger::open` (the `?mode=rwc` URL does not issue
  `PRAGMA journal_mode = WAL;`) as a follow-up backlog item;
  non-blocking for this feature given the 1-write-per-5-30-min cadence.
  Closes T-AR-3 of `spec/cockpit-training-control/tasks.md`.
- 2026-05-18 (architect): ADR-0033 added — v2.5 TCN alpha-investigation
  report shape + F-verdict algorithm. Locks (a) read-path placement
  (new bin `crates/forecast/src/bin/forecast_distribution.rs`, NOT
  extension of `crates/backtest`'s TCN dispatch — preserves anchor
  neutrality against the 4 byte-locked `-realdata` anchors and
  generalises to v2.5a/v2.5b alpha-investigations), (b) report
  frontmatter-vs-body discipline + floating-point canonicalisation
  (`%.6f` / `%.9f` / `(x * 1e6) as i64` for bin edges) following the
  ADR-0032 § D4 precedent, (c) F-verdict decision algorithm
  (deterministic F1/F2/F3/F4 classifier over (abs_p95, std,
  sigma_train, frac_inside_epsilon, confidence_gate_survival) with
  F-MIXED escape hatch for BS-1 vs. BS-2 disagreement), (d) Sharpe /
  Sortino / Calmar formulas (sqrt(24·365) hourly annualisation; new
  helpers in the M-SHARPE bin, NOT reuse of `compute_sharpe` which is
  minute-annualised). Existing 19 anchors stay byte-identical
  (R6 non-regression contract). Closes T-AR-3 of
  `spec/v25-tcn-alpha-investigation/tasks.md`.
- 2026-05-21 (architect): ADR-0035 added — Post-training σ_train
  recalibration via metadata overlay (cross-phase contract for v2.5
  TCN, v2.5a PatchTST, v2.5b vanilla Transformer). Locks (a) σ_train
  must be derived in a frozen-weights post-training forward pass, NOT
  via in-loop accumulation (the `train_tcn.rs:606,676-678,733-741`
  bug pattern is deprecated), (b) overlay-file convention
  (`.metadata.recalibrated.json` co-located with the original; original
  stays byte-identical), (c) on-disk JSON number convention for
  `sigma_train` (intentional divergence from ADR-0029 § 2 rule 5's
  string-encoded form — load-bearing for `.as_f64()` parity at the
  inference read site), (d) additive `--metadata-path` CLI flag on
  consumers (never auto-prefer overlay), (e) σ_train-not-in-safetensors
  invariant codified as test. Does NOT supersede ADR-0033 § D3
  (F-verdict algorithm stays immutable per `v25-tcn-recalibrate`
  Q4 = (a)). Closes T-AR-2 of
  `spec/v25-tcn-recalibrate/tasks.md`.
- 2026-05-21 (architect, M-T1): ADR-0036 added (status: proposed) —
  PatchTST training contract for v2.5a phase-2 ship. Locks (D1) PatchTST
  layer skeleton (patch embed + learnable PE + pre-LN transformer
  encoder + projection head; channel-independence per Nie et al § 3.2;
  PatchTST/42 small config = ~410k params), (D2) ADR-0029 canonical-arch
  descriptor extended additively with PatchTST architecture fields
  (model_family, patch_len, stride, d_model, n_heads, d_ff, n_layers,
  dropout, context_len) + tokenisation.target_horizon_bars; existing
  TCN model_revision SHAs unchanged, (D3) σ_train post-training
  derivation per ADR-0035 § D1 cross-phase contract verbatim — no
  in-loop accumulator; architect's grep-based code-review check codified,
  (D4) cost tripwire (24h hard limit + 3× median multiple) with
  continue-on-fire + escalation policy, (D5) K2 candle-attention
  determinism gate (CPU byte-identity + Metal-vs-CPU drift < 1e-4 per
  ADR-0029 § 4), (D6) anchor strategy under version `v2.5a.0-patchtst`
  (2 anchors additively; 28 predecessor anchors byte-immutable),
  (D7) sibling strategy `patchtst_overlay_momentum.rs` (NOT a model-
  agnostic refactor of `tcn_overlay_momentum.rs` — K6 scope-creep
  guard). Sibling deliverable: `spec/v25a-patchtst-overlay/decomp.md`.
  Closes T-AR-2 of `spec/v25a-patchtst-overlay/tasks.md`.
- 2026-05-24 (architect, M-T1): ADR-0040 added — Yahoo realdata path
- 2026-05-26 (architect, M-T1): ADR-0041 added — trader crate split;
  recovery brief for `reflection-memory-trader-wiring` v0.1.0 (P0
  gate-test red on `main`); locks Q1=(a) new `crates/trader/` +
  Q2=(a) clean-cut move + Q3=(a) inverse-API; corrects analyst
  file-count miscount (9 files / 10 test suites, not 8 / 13).
- 2026-05-26 (architect, M-T1): ADR-0043 added — simulated network
  latency + order-book slippage in backtest; default-zero noop preserves
  34 anchors via R-NR.1 hard gate; v0.2.0 anchor-migration brief
  deferred per Q5.
- 2026-05-26 (architect, M-T1): ADR-0044 added — activity-aggregator
  producer pattern (extends ADR-0042). Codifies the
  broadcast-receiver + `AtomicU32` + `tokio::time::interval` +
  long-lived `ActivityHandle` with idle-end semantics recipe as
  reusable for future high-frequency event sources. Locks D1
  placement at `crates/agent/src/activity_audit_aggregator.rs`
  (producer cohesion with existing `activity.rs`), D2 internal
  shape (50 ns/tick fetch_add hot path), D3 100 ms cadence verbatim
  from ADR-0042 § D1.4, D4 separate-handle Failed emission (NOT
  main-handle `fail()` — don't taint successful writes red), D5
  idle-end semantics. Q1=(b) per-window 100 ms / Q2=(a) redacted
  "Audit: N writes" / Q3=(a) sibling Failed event all locked at
  analyst-recommended defaults under standing Autoapprove.
  Zero changes to `crates/audit/` (R-NR.1 anchor-additive
  contract); 34/34 anchors stay byte-identical. Closes T-AR-4 of
  `spec/cockpit-activity-audit-ledger-producer/tasks.md`.
- 2026-05-26 (architect, post-ship): ADR-0042 added — cockpit activity
  broadcast; codifies the design shipped at
  `cockpit-activity-status-bar v0.1.0` (commits `4248c00` + `ea52057`
  + `49bf342` + `ef6f018` + `0ff402f` + `f728334`). 10th `EventBus`
  channel (`activity_tx`, capacity 256) + RAII `ActivityHandle` with
  panic-aware `Drop` + 100 ms producer-side throttle. Q1=(a)
  broadcast-bus pattern wins over (b) tracing-layer (fragile + slow,
  ties UI to log filtering) and (c) per-source polling (coupling-heavy,
  doesn't extend). Lossiness OK because activity tape is operator-
  eyeball UX, not audit (R-NR.4 in-memory only; audit ledger remains
  source of truth). Deferred to v0.1.1: PII redaction for `LlmCall`
  producer (K4); aggregator design for `AuditLedgerWrite` producer
  (R5.2 / K3). 34/34 anchors stay byte-identical. Closes T-AR-4 of
  `spec/cockpit-activity-status-bar/tasks.md`.
  + revision pin (Lab dispatch source). Generalises ADR-0032's
  revision-pin protocol to a second data source (Yahoo Finance) on
  the Lab dispatch path. Locks D1 (module placement —
  `crates/data/src/yahoo.rs` feature-gated `yahoo`; CLI binary
  co-located at `crates/data/src/bin/fetch_yahoo_klines.rs`), D2
  (external dep `yahoo_finance_api 4.1.x` with CLAUDE.md 6-item
  library-compat checklist all green), D3 (revision-pin protocol —
  cadence subdir on disk + per-fetch `[revision.yahoo_response]`
  table for K2 forensics; aggregate-SHA algorithm verbatim-shared
  with ADR-0032), D4 (engine remains source-agnostic per Q1=(b);
  Lab swaps bars upstream via existing `bars_override` hook; the 4
  cross-sectional arms reject `data_source = YahooCache` with typed
  error), D5 (`YahooBarSource` API surface with `load_cached`
  /`fetch_and_cache` split feature-gated so cockpit doesn't pull
  tokio into iced — K10 mitigation), D6 (adaptive cadence Q4=(c) —
  `Interval::derive_from_range` 10-row truth table), D7 (boundary
  ticker conversion Q6=(a) — `binance_to_yahoo_ticker` 10-entry
  table in `crates/data/src/yahoo.rs` for CLI reuse), D8
  (`Venue::Yahoo` variant cascade — additive; existing rows
  untouched; clippy `-D warnings` drives exhaustive-match
  completion). 34/34 anchors stay byte-identical (anchored body-SHAs
  all originate from CLI `--features realdata` paths that construct
  `ScenarioConfig` without the new fields; `#[serde(default)]`
  preserves byte-identity). Closes T-AR3 + T-AR6 of
  `spec/lab-yahoo-realdata/tasks.md`.

- 2026-05-22 (architect, M-T1): ADR-0039 added — LLM-forecaster verdict
  criteria L0-L4 (parallel to ADR-0033 § D3 and ADR-0038 § D1, NOT
  extension; PARALLEL is the operator-locked precedent per
  retrospective lesson #2 + Q6 operator-pick 2026-05-22). Locks (D1)
  L0-L4 priority tree (L1 bias collapse `hold_frac ≥ 0.95` / L2
  calibration failure `|confidence_outcome_corr| < 0.05` / L3 cost
  overrun `overrun_ratio > 2.0 OR cost_actual > cost_cap` / L4
  reasoning trace degenerate `short_frac > 0.50 OR duplicate_frac >
  0.50` / L0 PASS routes to L_ALPHA gate) — analyst-strawman LOCKED
  per Q6 operator constraint with architect cap "≤2 new priorities
  beyond strawman before re-surface" codified inline at D1.b last
  paragraph, (D1.c) L_ALPHA strategy-side gate (Sharpe-delta
  thresholds inherited from ADR-0038 § D1.c verbatim: L-ALPHA-UNLOCKED
  `≥ +0.10` / L-MARGINAL `[+0.05, +0.10)` / L-NO-ALPHA `< +0.05`) +
  5-cell joint advisory verdict routing table, (D2) verdict section
  shape (frontmatter-vs-body discipline per ADR-0032 § D4 + ADR-0033
  § D2 + ADR-0038 § D2; full body layout delegated to
  `spec/v3-llm-forecaster/decomp.md` § T-AR-6), (D3) L_ALPHA Sharpe-
  comparison bin extension (additive `--scenario llm-forecaster-bs1`
  dispatch arm on `crates/forecast/src/bin/sharpe_comparison.rs`),
  (D4) replay-cache namespace additive extension (dedicated
  `data/llm-forecaster-replay.db` + checked-in compressed fixture <
  50 MB per K5; reuses `crates/llm::RecordingProvider/ReplayProvider`
  schema verbatim), (D5) strategy-side + Phase F Assistant slot
  composition v0.1.0 with Q4=(b) overlay deferred to v0.1.1 and
  Q4=(d) all-three-as-builders deferred to v0.2.0+, (D6) anchor +
  version naming (new `v3.0.0-llm-forecaster` namespace; +2 anchors
  at developer Wave G close; sharpe-comparison NOT anchored at
  v0.1.0; re-emission protocol inherited from ADR-0038 § D6.b
  verbatim). Closes T-AR-9 of
  `spec/v3-llm-forecaster/tasks.md`.
