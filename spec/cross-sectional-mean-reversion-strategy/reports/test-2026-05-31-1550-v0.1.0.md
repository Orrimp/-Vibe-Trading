---
title: Test Report
feature: cross-sectional-mean-reversion-strategy
run_id: 2026-05-31-1550-UTC
commit: 94cd8d4747bec3fbbd17f3c7bd3fb35ab3aee719
agent: tester
verdict: FAIL
---

# Test Report — cross-sectional-mean-reversion-strategy v0.1.0 — 2026-05-31 15:50 UTC

## 1. Scope

- **Feature / change under test:** Cross-sectional mean-reversion θ-surface (MR-C3). `Direction { Momentum, Reversion }` enum added to `CrossSectionalMomentumConfig`; score-inversion at the cache-write boundary in `MomentumStrategy::on_bar`; `MR_TIER1_GRID` (6-cell × N=200, LOCKED § D-MR.2); `--direction` flag on `param_robustness_sweep`; R-MR.1 divergence falsifier (`mr_divergence_e2e.rs`); anchor #87 (`v1-mr-theta-surface-2023-block-bootstrap-real-fy`) added to `spec/anchors.toml`. Family verdict: FAMILY-UNIFORM-FRAGILE.
- **Spec refs:** `spec/cross-sectional-mean-reversion-strategy/feature.md`, `spec/cross-sectional-mean-reversion-strategy/tasks.md`
- **Commit SHA:** `94cd8d4747bec3fbbd17f3c7bd3fb35ab3aee719`
- **Rust toolchain:** `rustc 1.94.1 (e408947bf 2026-03-25)`
- **OS / arch:** Darwin 25.5.0, arm64 (Apple-Silicon canonical box, ADR-0051 D5)

## 2. Static Analysis

| Check | Result | Notes |
|---|---|---|
| `cargo fmt --check -p strategy -p backtest` | **PASS** | Zero diffs in changed files. |
| `cargo clippy -p strategy -p backtest -- -D warnings` | **PASS** | 0 new errors/warnings in changed files. Pre-existing baseline debt in other backtest engine files is unchanged (tracked in prior tester report). |
| `cargo audit` | _n/a_ | Not run per anti-stall protocol; no new `Cargo.lock` dependencies added by this change. |
| `cargo deny` | _n/a_ | Same rationale. |

## 3. Unit & Integration Tests

**All targeted tests PASS.**

| Suite | Command | Passed | Failed | Duration |
|---|---|---|---|---|
| MR e2e divergence + falsifier | `cargo test -p backtest --test mr_divergence_e2e` | **5** | 0 | 0.11s |
| C3 param sweep e2e (regression guard) | `cargo test -p backtest --test param_sweep_e2e` | **8** | 0 | 1.74s |
| Strategy MR unit tests | `cargo test -p strategy mr_dev` | **4** | 0 | <0.01s |
| **Total** | | **17** | **0** | |

### MR e2e divergence test detail (5/5 PASS)

| Test | Result | What it proves |
|---|---|---|
| `r_mr_1a_momentum_vs_reversion_diverge` | PASS | Momentum and Reversion equity curves diverge ≥ ε on the same synthetic path — inversion is not a no-op. |
| `r_mr_1b_degenerate_noop_produces_identical_curves` | PASS | Forcing inversion to a no-op (`Reversion => score`) collapses to identical curves (Δ < ε) — gate is falsifiable and detects a silent no-op. This is the RED-on-revert probe mandated by § D-MR.5-GATE(b) and CLAUDE.md. |
| `fp_mr_3_two_run_byte_identity` | PASS | Two runs of the small-N MR sweep at the same seeds produce identical `report_body_hash` — determinism holds at the outer θ-loop and renderer. |
| `fp_mr_5_family_summary_always_valid_value` | PASS | Family-summary line ∈ `{FAMILY-UNIFORM-FRAGILE, FAMILY-HAS-NON-FRAGILE-CELLS}` — anti-cherry-pick renderer gate. |
| `fp_mr_5_non_fragile_mr_cell_carries_c5_flag` | PASS | Any non-FRAGILE cell carries `→ C5 DEFLATION REQUIRED` — anti-cherry-pick cell gate. |

### C3 regression gate (8/8 PASS, includes `fp_c3_3_two_run_byte_identity`)

All 8 C3 param_sweep_e2e tests pass. Critically, `fp_c3_3_two_run_byte_identity` confirms the momentum θ-surface body-SHA is **unchanged** — the `--direction` flag and the `trades` column gated on `direction == reversion` did not disturb the momentum C3 surface.

### Strategy MR unit tests (4/4 PASS)

| Test | Result |
|---|---|
| `mr_dev1_no_direction_defaults_to_momentum` | PASS |
| `mr_dev1_direction_reversion_parses` | PASS |
| `mr_dev1_config_hash_differs_by_direction` | PASS |
| `mr_dev2_reversion_selects_opposite_symbols` | PASS |

### Failing tests

_none_

## 4. Property / Fuzz Tests

_n/a_ — no proptest/cargo-fuzz suites added by this feature.

## 5. Backtest Results

**Feature:** Cross-sectional mean-reversion θ-surface (MR-C3).
**Universe:** 10 USDT pairs (BTCUSDT ETHUSDT BNBUSDT SOLUSDT XRPUSDT ADAUSDT DOGEUSDT AVAXUSDT DOTUSDT LINKUSDT), data_revision_sha `3a8b96c4…`.
**Period:** 2023-FY (in-sample, same as momentum C3 anchor for apples-to-apples comparability).
**Data source:** `block-bootstrap-real`, `bootstrap_mode: shared-index`, auto-L = 204 (θ-independent).
**Fees:** 6 bps round-trip (2 slippage + 4 taker), inherited from C3 `TcnScenarioInput`.
**N paths per cell:** 200. **Ensemble seed:** 0xC0FFEE. **Wall-clock:** 3087.3s (concurrent with R2 verify run, ~2× nominal; expected ~1800s single-run).

### MR θ-surface (6-cell LOCKED § D-MR.2-LOCKED)

Pre-flight void-if-fail header confirmed: `generator: block-bootstrap-real`, `bootstrap_mode: shared-index`. Condition met — verdict is not void.

| g | role | lookback | k_long | drift | p5_sharpe | p50_sharpe | p95_sharpe | P(loss) | P(Sharpe>1) | p95_maxdd | trades | verdict |
|---|------|---|---|---|---|---|---|---|---|---|---|---|
| 0 | baseline MR θ* | 60 | 3 | 0.10 | −0.038507 | −0.012788 | +0.011077 | 70.5% | 0.0% | 91.74% | 1,347,505 | **FRAGILE** |
| 1 | HIGH-CHURN (24-min lb) | 24 | 3 | 0.10 | −0.039078 | −0.016673 | +0.004031 | 84.0% | 0.0% | 91.59% | 1,963,647 | **FRAGILE** |
| 2 | 1w lookback | 168 | 3 | 0.10 | −0.045424 | −0.004750 | +0.019446 | 57.5% | 0.0% | 90.47% | 799,300 | **FRAGILE** |
| 3 | LOW-CHURN: 1mo × wide band | 720 | 5 | 0.50 | −0.043483 | **+0.006941** | +0.074258 | 42.5% | 0.0% | 85.54% | 379,809 | **FRAGILE** |
| 4 | long lb + medium band | 720 | 3 | 0.30 | −0.067443 | −0.003838 | +0.034500 | 56.0% | 0.0% | 88.75% | 329,321 | **FRAGILE** |
| 5 | MAX-CHURN (24-min + k=5) | 24 | 5 | 0.10 | −0.044642 | −0.010432 | +0.016043 | 72.0% | 0.0% | 93.23% | 2,082,234 | **FRAGILE** |

**Buy-and-hold control (the +1.74 bar MR must clear):** p5=+0.124, p50=+1.735, p95=+3.870, P(loss)=4.5%, P(Sharpe>1)=77.5%, p95_maxdd=51.15%. MR does NOT clear this bar on any cell.

**Family verdict: FAMILY-UNIFORM-FRAGILE.** All 6 cells FRAGILE. Conclusion matches the brief's anticipated outcome (§ Scope → honesty).

### FRAGILE verdict reading against the frozen decision rule (§ 0 bands, `robustness-decision-rule-2026-05-30.md`)

The frozen bands classify FRAGILE on ANY primary signal where: p5 Sharpe < 0, p50 Sharpe < 0.5, P(loss) > 35%, P(Sharpe>1) < 35%, p95 MaxDD > ~70%. Composite = weakest-link.

**g=3 (best-shot cell — 1mo lookback + wide band):** p5=−0.043 (<0 → FRAGILE), p50=+0.007 (<0.5 → FRAGILE), P(loss)=42.5% (>35% → FRAGILE), P(Sharpe>1)=0% (<35% → FRAGILE), p95_maxdd=85.54% (>70% → FRAGILE). All 5 primary signals FRAGILE. Verdict: **FRAGILE**. The brief notes g=3 p50=+0.007 barely above zero — this is correctly read as FRAGILE (p50 must reach ≥ 0.5 for MARGINAL; g=3 is not near-MARGINAL on any primary signal except p50).

**No band was mis-applied.** The +1.74 buy-and-hold control is not in scope for the FRAGILE classification (it carries no verdict) but confirms the bar: no MR cell approaches passive holding.

### R2 binding constraint (architect, non-negotiable)

Momentum anchor #86 body-SHA verified byte-identical **without re-running the 25-min sweep**: the committed report `spec/momentum-parameter-robustness-sweep/reports/robustness-sweep-20260530-180006-v1-momentum-theta-surface-2023-block-bootstrap-real-fy.md` hashes to `0dd989d9dc6f81a8dc722096d104fb7c0db3e7220f319c26b132e54df5f71dd5` = anchors.toml row #86 exactly. This proves the `--direction` flag + the `trades` column (MR-only gate) did NOT leak into the momentum path. The binding constraint is met.

### R-MR.1 surface-level inversion sanity

MR g=0 p50=−0.013 vs momentum g=0 p50=−0.008. Surfaces differ — the inversion is provably not a no-op at the distribution level (in addition to the falsifier test `r_mr_1a`/`r_mr_1b` above).

### Turnover-vs-Sharpe relationship (R-MR.3 legibility — the `trades` column)

The `trades` column is visible in the surface table and confirms the expected pattern: low-churn cells (g=3: 379,809; g=4: 329,321) have materially lower trade counts than high-churn cells (g=5: 2,082,234; g=1: 1,963,647). Even the lowest-churn cell (g=4) remains decisively FRAGILE — the fee trap cannot be tuned away within the MR Tier-1 grid. This answers the R-MR.3 turnover-axis question definitively.

## 6. Benchmarks

_n/a_ — no hot-path changes; `run_path` is byte-identical to the prior C3 anchored run. No criterion benchmarks added.

## 7. Anchor Verification (NON-NEGOTIABLE gate)

`bash scripts/verify_anchors.sh` → **ANCHORS PASS (87 / 87)**

- All 86 prior anchors: byte-identical (no existing anchor disturbed).
- Anchor #87 (`v1-mr-theta-surface-2023-block-bootstrap-real-fy`): **PASS**. Body-SHA `a708112e1e8ddd4e48360b1e9f83d927c65d3d0f05be984e362c76f20be7bb4a` matches the committed report at `spec/cross-sectional-mean-reversion-strategy/reports/robustness-sweep-20260531-153647-v1-mr-theta-surface-2023-block-bootstrap-real-fy.md` (verified independently by computing the body-SHA from the file: matches).
- Anchor #86 (`v1-momentum-theta-surface-2023-block-bootstrap-real-fy`): body-SHA `0dd989d9dc6f81a8dc722096d104fb7c0db3e7220f319c26b132e54df5f71dd5` matches the committed momentum report and the anchors.toml entry — byte-identical, confirming R2 binding constraint.

Determinism: `fp_mr_3_two_run_byte_identity` PASS (unit-level) + 87/87 anchor PASS (anchored-run level) — two-run byte-identity is proven at both levels. Same basis as the C2 v0.1.1 close.

## 8. Spec-lint Gate

`python3 scripts/spec_lint.py` → **FAIL (97 violations in 5 categories)**

### Delta vs prior baseline

Prior tester baseline (C3 v0.1.0 report `test-2026-05-30-1834-v0.1.0.md`): 95 violations in 3 categories (`dead-link` 87, `trace-broken-path` 7, `unreferenced-anchor` 0 post-trace-update).

| Category | Prior baseline (C3 post-trace-update) | Current run | Delta | Attribution |
|---|---|---|---|---|
| dead-link | 87 | 87 | **0** | Stable |
| trace-broken-path | 7 | 7 | **0** | Stable (same 7 pre-existing rows) |
| unreferenced-anchor | 0 | 1 | +1 | Anchor #87 not yet cited by trace.toml `anchors` field — **tester's job to fill after PASS; blocked by other issues below** |
| missing-frontmatter | 0 | **1** | **+1 NEW** | `spec/carry-funding-data-backfill/feature.md` missing `updated` key — introduced by commit `ab815d5` (carry spike) |
| orphan-feature | 0 | **1** | **+1 NEW** | `spec/carry-funding-data-backfill` has no `tasks.md` — introduced by commit `ab815d5` (carry spike, `status: draft`) |

**Two new categories** (`missing-frontmatter` + `orphan-feature`) were introduced by the carry data spike commit `ab815d5`, which preceded the MR commit `94cd8d4` in the same developer session. These block PASS per the spec-lint gate rule.

**The `unreferenced-anchor` for #87 is the tester's responsibility** (fill the `anchors` column in trace.toml row `REQ-XS-MEANREVERSION-001` after PASS). It is not blocking in isolation, but is noted here. It will be filled when PASS is re-emitted after the developer fixes the carry stub.

## 9. Trace.toml Gaps (Developer-Owned)

Trace.toml row `REQ-XS-MEANREVERSION-001`:

```toml
crates  = []   # developer fills — proposed: crates/strategy (cross_sectional), crates/backtest (run_path + param_robustness_sweep reuse)
tests   = []   # developer fills
anchors = []   # tester fills after PASS
```

The `crates` and `tests` fields are empty. The developer changed `crates/strategy` and `crates/backtest` (confirmed by `git diff HEAD~1 HEAD --name-only`). Per the tester's trace-completeness gate, a `[req]` row whose `crates` intersect with the changed-crates set must have a non-empty `tests` array. Both fields are unresolvable (require the developer to specify the file:line resolvable test paths and the exact crate names). This is a trace.toml gap that routes back to the developer.

## 10. Pre-existing Spec Debt (Carry-over, Not Blocking)

The following violations are unchanged from the prior baseline and do NOT block this verdict:
- `dead-link` × 87: residual dead links in anchored reports (byte-immutable per ADR-0038 § D6) and legacy feature docs. Routing: architect (ADR-link stubs), developer (live dead links). Unchanged.
- `trace-broken-path` × 7: same 7 pre-existing rows (`REQ-LAB-YAHOO-REALDATA-V0-1-4-001`, `REQ-VISUAL-FAIL-HTML-REPORTER-001` ×3, `REQ-UI-CONTRAST-ASSERTER-001`, `REQ-QUEUE-STALENESS-RECONCILIATION-001`, `REQ-OPERATOR-LEDGER-SCHEMA-LINT-001`). Unchanged.

## 11. FRAGILE Verdict Scientific Reading

Cross-sectional mean-reversion is **conclusively retired on the robustness axis** as the second family to fail the harness after momentum. Key findings:

1. **The fee trap is not tunable away within the MR Tier-1 grid.** Even the deliberately-low-churn g=3 cell (1mo lookback, k=5, drift=0.50 — the best-a-priori low-churn shot) is FRAGILE on all 5 primary signals: p5=−0.043, p50=+0.007, P(loss)=42.5%, P(Sharpe>1)=0%, p95_maxdd=85.5%. The slow rebalance materially lowers trade count (379,809 vs 2,082,234 for g=5) and somewhat improves P(loss) (42.5% vs 72–84% for high-churn cells), but cannot approach the MARGINAL threshold on any signal.

2. **The +1.74 buy-and-hold bar is not cleared by any MR cell.** The passive equal-weight buy-and-hold of the same 10 coins on the same resampled 2023 paths yields p50=+1.735, P(loss)=4.5%, P(Sharpe>1)=77.5%. The best MR cell (g=3) achieves p50=+0.007, P(loss)=42.5%, P(Sharpe>1)=0%. MR is comprehensively dominated by passive holding on this universe.

3. **The inversion is real and verified.** MR g=0 p50=−0.013 vs momentum g=0 p50=−0.008 — surfaces differ, confirming the sign flip took effect. The R-MR.1 falsifier (both real case PASS and degenerate RED-on-revert PASS) proves the divergence gate is a genuine guard.

4. **Scientific interpretation.** FAMILY-UNIFORM-FRAGILE for both momentum AND mean-reversion is a methodology win: the harness ruled out two families cheaply on the same 2023 Binance universe, establishing a clean baseline that buy-and-hold dominates active price-based strategies with high churn on this data. The carry rotation (data now unblocked by the spike commit) is the scientific next step — the best-structural-answer to the fee trap, per the feature brief § Axis 2.

## 12. Verdict

**`FAIL`**

All test suites pass (17/17), anchors verify at 87/87, the FRAGILE verdict reading is correct and methodologically sound, fmt and clippy are clean, and the R2 binding constraint is confirmed. However, two new spec-lint categories were introduced by the carry data spike commit (`ab815d5`, same developer session): `missing-frontmatter` (carry `feature.md` lacks `updated` key) and `orphan-feature` (carry `spec/carry-funding-data-backfill/` lacks `tasks.md`). Per the spec-lint gate rule, new category regressions block PASS. Additionally, trace.toml row `REQ-XS-MEANREVERSION-001` has empty `crates` and `tests` fields that the developer was to fill.

These are small, mechanical fixes. The MR science, the anchor, and the falsifier are all sound. Expected resolution: developer adds `updated: 2026-05-31` to carry `feature.md`, creates a minimal `spec/carry-funding-data-backfill/tasks.md` (stub is sufficient for `status: draft`), and fills the trace.toml `crates`/`tests` fields with resolvable paths. On re-handoff, tester completes the trace `anchors` fill and emits PASS.

## 13. Routing

`HANDOFF → developer (spec-lint regression + trace.toml incomplete)`

**Required fixes (all small):**

1. **`spec/carry-funding-data-backfill/feature.md`** — add `updated: 2026-05-31` to frontmatter (resolves `missing-frontmatter` × 1).
2. **`spec/carry-funding-data-backfill/tasks.md`** — create a minimal stub (e.g. `# Tasks — carry-funding-data-backfill\n\nstatus: draft — no tasks yet.`) in the directory (resolves `orphan-feature` × 1).
3. **`spec/trace.toml` `REQ-XS-MEANREVERSION-001`** — fill `crates` and `tests` with resolvable file:line paths. Proposed:
   - `crates = ["crates/strategy", "crates/backtest"]`
   - `tests = ["crates/backtest/tests/mr_divergence_e2e.rs::r_mr_1a_momentum_vs_reversion_diverge", "crates/backtest/tests/mr_divergence_e2e.rs::r_mr_1b_degenerate_noop_produces_identical_curves", "crates/backtest/tests/mr_divergence_e2e.rs::fp_mr_3_two_run_byte_identity", "crates/backtest/tests/mr_divergence_e2e.rs::fp_mr_5_family_summary_always_valid_value", "crates/backtest/tests/mr_divergence_e2e.rs::fp_mr_5_non_fragile_mr_cell_carries_c5_flag", "crates/strategy/src/cross_sectional/momentum.rs::tests::mr_dev2_reversion_selects_opposite_symbols"]`

After these fixes, re-handoff to tester. Tester will: fill trace `anchors = ["v1-mr-theta-surface-2023-block-bootstrap-real-fy"]`, tick the `T-MR` tester item in `tasks.md`, set trace state to `tested`, run spec-lint (expect exit 0 or pre-existing-only), re-run `verify_anchors.sh`, and emit `VERDICT → PASS`.

**No MR science work is blocked** — the FRAGILE result is correct, the anchor is locked, the falsifier is sound. This is pure paperwork on the carry stub + trace row.

---

## Watch recipe (R2 re-verify if needed)

```
watch -n 30 'ls -lh spec/momentum-parameter-robustness-sweep/reports/robustness-sweep-20260530-180006-v1-momentum-theta-surface-2023-block-bootstrap-real-fy.md && python3 -c "import hashlib; f=open(\"spec/momentum-parameter-robustness-sweep/reports/robustness-sweep-20260530-180006-v1-momentum-theta-surface-2023-block-bootstrap-real-fy.md\",\"rb\").read(); lines=f.decode().split(chr(10)); d=[i for i,l in enumerate(lines) if l.strip()==\"---\"]; body=\"\\n\".join(lines[d[1]+1:]).encode(); print(hashlib.sha256(body).hexdigest())"'
```

Expected: `0dd989d9dc6f81a8dc722096d104fb7c0db3e7220f319c26b132e54df5f71dd5`

## Watch recipe (MR θ-surface re-run, if needed)

```
watch -n 60 'ls -lht spec/cross-sectional-mean-reversion-strategy/reports/*.md | head -3'
```

Re-run command (do NOT re-run unless the committed report is found byte-corrupt — it is already anchored):
```
cargo run --release -p backtest --features "candle realdata" \
  --bin param_robustness_sweep -- \
  --direction reversion --grid mr-tier1 \
  --generator block-bootstrap-real --paths 200 \
  --ensemble-seed 0xC0FFEE \
  --out-dir spec/cross-sectional-mean-reversion-strategy/reports/
```
