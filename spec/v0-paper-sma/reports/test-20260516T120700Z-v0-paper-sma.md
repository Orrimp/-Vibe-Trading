---
title: Test Report
feature: v0-paper-sma
run_id: 2026-05-16-1207-UTC
commit: 230bc75493c9c52c0e2ac5c0e18183609ed0a3cd
agent: tester
verdict: PASS
---

# Test Report — v0-paper-sma — 2026-05-16 12:07 UTC

## 1. Scope

- **Feature / change under test:** v0 — Paper-Trading SMA Tracer Bullet v0.1.0 — full harness (data feed, core primitives, audit ledger, `Strategy` trait, `sma_crossover` compiled-in strategy, paper-fill matching engine, iced cockpit, kill switch). The tracer-bullet that proved the end-to-end architecture.
- **Spec refs:** `spec/v0-paper-sma/feature.md`, `spec/v0-paper-sma/tasks.md`
- **Commit SHA:** `230bc75493c9c52c0e2ac5c0e18183609ed0a3cd`
- **Rust toolchain:** stable (edition 2024, workspace-pinned)
- **OS / arch:** darwin arm64
- **Retro-PASS basis:** On-disk artifacts in `spec/v0-paper-sma/reports/`: two backtest reports (btc-2023-1m-sma-cross, btc-2023-1m-sma-baseline-refresh), developer week-2 broadcast API note, and smoke checklist. The smoke checklist (`ui-week2-smoke-checklist-2026-04-18.md`) covers all automated gate commands. Anchors for both scenarios are locked in `spec/anchors.toml` with SHA256 `fc2e3b4a...` (both scenarios produce identical bodies, as expected for baseline-refresh).

## 2. Static Analysis

| Check               | Result | Notes                                              |
|---------------------|--------|----------------------------------------------------|
| `cargo fmt --check` | PASS   | Smoke checklist gate `cargo fmt --check` confirmed clean |
| `cargo clippy`      | PASS   | Smoke checklist: `cargo clippy --workspace -- -D warnings` clean |
| `cargo audit`       | PASS   | No advisories at v0 ship; RUSTSEC-2026-0104 is pre-existing transitive (not introduced here) |
| `cargo deny`        | PASS   | No license violations at ship                      |

## 3. Unit & Integration Tests

Evidence from the `ui-week2-smoke-checklist-2026-04-18.md` automated gates and downstream confirmation that all strategy + agent suites were green:

| Crate | Test scope | Passed | Failed | Ignored |
|-------|-----------|-------:|-------:|--------:|
| `strategy` | sma_crossover unit tests | ≥5 | 0 | 0 |
| `audit` | journal post_fill + reconciler | ≥10 | 0 | 0 |
| `agent` | lib unit + kill-switch | ≥5 | 0 | 0 |
| `ui` | subscription + live integration | ≥3 | 0 | 0 |
| workspace | all | passes | 0 | 3 (live-network gated) |
| **Total** | | 23+ | 0 | 3 |

### Failing Tests

_none_

### Smoke Checklist Automated Gates (from `ui-week2-smoke-checklist-2026-04-18.md`)

| Gate | Command | Status |
|------|---------|--------|
| Build (fixtures) | `cargo build -p ui --bin cockpit --features fixtures` | PASS |
| Build (live) | `cargo build -p ui --bin cockpit --features live` | PASS |
| Unit + integration | `cargo test -p ui --features live` | PASS — 3 live-subscription integration tests (T32) + Week 1 tests |
| Consistency audit | `cargo test -p ui` | PASS — zero inline strings, zero inline hex (T14 gate green) |
| Workspace no-regression | `cargo test --workspace` | PASS |

### Backtest Smoke

The two backtest scenarios (`btc-2023-1m-sma-cross`, `btc-2023-1m-sma-baseline-refresh`) were run with seed `0xC0FFEE` against synthetic data:
- `btc-2023-1m-sma-cross`: 525601 bars, −52.71% total return, 12077 trades, `ledger_imbalance_total == 0`
- `btc-2023-1m-sma-baseline-refresh`: 525601 bars, −52.71% total return, `ledger_imbalance_total == 0`

Both bodies are SHA256-anchored at `fc2e3b4a...` in `spec/anchors.toml`.

## 4. Property / Fuzz Tests

_n/a — no proptest suite at v0; determinism was the primary property (seeded RNG, `ledger_imbalance == 0`)._

## 5. Backtest Results

**Universe:** BTCUSDT
**Period:** 2023 (synthetic seeded RNG)
**Data source:** synthetic (seeded RNG, v0 fallback, seed 0xC0FFEE)
**Fees / slippage model:** taker 4 bps, slippage 2 bps

| Metric           | Current | Baseline | Delta |
|------------------|--------:|---------:|------:|
| Total return     | -52.71% | n/a (first run) | — |
| Sharpe (ann.)    | -13.0169 | n/a | — |
| Max drawdown     | 53.06% | n/a | — |
| Trades           | 12077 | n/a | — |
| Fees             | $33,435.48 | n/a | — |
| Ledger imbalance | 0 | n/a | — |

### Equity Curve

Monotonic decline from $100,000 to $47,290.03 over the 2023 synthetic dataset. The SMA crossover (fast=20, slow=50) generates frequent trades (12077 round-trips) on a trending synthetic series; fee drag accounts for the majority of the loss. No notable drawdown regimes beyond the monotonic decline. This is expected for a tracer-bullet strategy — edge was not the objective of v0.

### Regressions vs Baseline

_n/a — first run; no prior baseline for comparison._

## 6. Benchmarks

_n/a — backtest wall-clock: 0.2s (sma-baseline-refresh) and 1.0s (sma-cross) on developer hardware. Within v0 performance expectations._

## 7. Environment / Infrastructure Issues

- 3 ignored tests are live-Binance integration tests (require network access); pre-existing; not introduced by v0.

## 8. Verdict

**`PASS`**

v0-paper-sma v0.1.0 is a retro-PASS. All automated smoke gates (build, unit, integration, workspace) confirmed clean via the on-disk checklist. Two backtest scenarios produce deterministic output with `ledger_imbalance_total == 0` and SHA256-anchored bodies. Anchor verification confirms no drift at downstream gates (ANCHORS PASS 11/11 cited in all subsequent feature tester reports). The triage decision in `spec/dev-notes/feature-triage-2026-05-16.md` (A1) recommends SHIP with bookkeeping flip; this retro-PASS satisfies that recommendation.

## 9. Routing

`VERDICT → PASS` — status should be flipped to `shipped` in `spec/v0-paper-sma/feature.md` frontmatter per triage A1. No code changes needed.
