# Changelog

The canonical **"what's been built"** index for this Rust crypto-trading agent —
one line per implemented feature, grouped by subsystem. The project runs several
parallel version ladders (strategy/engine `v0…v5`, plus independently-versioned
cockpit/UI and infra tracks), so entries are grouped by subsystem and tagged with
their own version rather than forced onto a single release timeline.

Per-feature narrative history lives in **git** (`git log -- spec/<slug>/`). The
immutable backtest evidence lives under **`spec/*/reports/`** (119 byte-SHA-256
regression anchors, gated by `scripts/verify_anchors.sh`). Ratified scope and
design remain in **`spec/product.md`**, **`spec/architecture/`** (ADRs), and
**`spec/runbooks/`**.

> **Program status — single-coin investment advisor (paper), MVP SHIPPED (2026-06-19 pivot).**
> The terminal deliverable is the **advisor loop**: pick a coin + budget (e.g. €200
> XRPUSDT) → bake off *every* strategy over a configurable 2-week-to-4-year window →
> rank & select the best by risk-adjusted Sharpe under a robustness gate → emit a
> forward buy/sell plan → **paper-trade the budget forward** in the Live view with
> real P/L. The 2026-06-08 active-vs-passive negative result (no active strategy beat
> passive buy-and-hold net of cost under the frozen block-bootstrap rule) is REFRAMED,
> not discarded: **buy-and-hold is the always-present bake-off benchmark arm** and the
> **Monte-Carlo robustness machine is the credibility layer** that gates every pick —
> the foundation under the advisor, not the terminal product. PAPER-ONLY (simulated
> fills, simulated €200); not financial advice; single-coin. **No live trading**
> (removed 2026-06-12, out of scope). Full spec: `spec/product.md`.

---

## Single-coin investment advisor (paper) — 2026-06-19 pivot, MVP SHIPPED

The terminal product: pick coin + budget → bake off all strategies → rank the best → forward paper-trade the €200. Buy-and-hold is the always-present benchmark arm; the Monte-Carlo robustness machine gates every pick.

- **advisor-bakeoff F1+F2** (v0.1.0) — strategy bake-off + ranking engine (`crates/backtest/src/bakeoff/`: `run_bakeoff(cfg) -> BakeoffReport`; ranks Fragile-ineligible → Sharpe → return → drawdown → id; buy-and-hold always the benchmark arm; structured `Recommendation`). ADR-0059. `58b55b1`.
- **advisor-leaderboard** (v0.1.0) — cockpit Leaderboard screen rendering the ranked bake-off (advisor journey step 3) — `crates/ui/src/screens/leaderboard.rs`, `crates/ui/src/leaderboard/`. `e0cc34b`.
- **advisor-bakeoff F3** (v0.1.0) — guided coin + budget + lookback input widget (`crates/ui/src/widgets/bakeoff_input.rs`) opening the journey. `acc3789`.
- **advisor-forward-paper F4** (v0.2.0) — budget-aware €200 sizing modifier (`crates/risk` `FixedFractionSizer.budget_cap`) with a day-1 baseline-equity-divergence e2e (`crates/risk/tests/budget_sizing_divergence_end_to_end.rs`). ADR-0060. `d4f4dce`.
- **advisor-forward-paper F5** (v0.2.0) — forward paper-trade of the SELECTED strategy: `crates/agent::runtime` `paper_loop_supervisor` hot-swaps the trading loop on `ForwardCommand::Launch` to run the pick at €200 on the same bus/ledger; Live view shows real P/L. ADR-0060 §D6. `c9dd275`.
- **advisor-dynamic-data** (v0.1.0) — on-demand Binance fetch for any coin + window the pinned corpus doesn't cover (`crates/data` `binance_klines` + `dynamic_cache`, git-ignored cache root, anchor-safe by construction — verify_anchors stays 119/119). ADR-0061. `ee5a904`.
- **advisor-forward-fidelity F5b** (v0.2.0) — the forward run executes the REAL crowned strategy, not an SMA proxy: `build_registry_for` loads the actual `ComposedStrategy` (MACD/RSI/Bollinger from `config/strategies/*.toml`) + a new `strategy::AlwaysLongStrategy` for buy-and-hold, unknown id → typed `Err` (no silent fallback); anti-fake gate `crates/agent/tests/forward_run_engine_fidelity.rs` (registry-identity + MACD≠SMA divergence). `608cf1c`.
- **advisor-forward-plan F6** (v0.1.0) — the honest, conditional forward buy/sell plan (new Plan screen between Leaderboard and Live): read-only `PlanDescribe` seam → core-typed `ForwardPlan` → `ForwardPlanView`; IF/THEN rules faithful to the real TOMLs (flip-to-false exits, no fabricated thresholds) with not-a-prediction/not-advice framing; configurable 1–30d horizon (default 7). ADR-0062. `51aba16`.
- **advisor-ensemble F8** (v0.1.0) — strategy-mix vote ensembles (`v0.8.vote.majority` 2-of-3, `v0.8.vote.unanimous` 4-of-4 via `strategy::EnsembleStrategy`, un-warmed members abstain) **+ activation of the robustness gate** — it was INERT (`RobustnessMode` had only `Skip`, so the advisor ranked on in-sample Sharpe alone). `RobustnessMode::Bootstrap` is now a real moving-block bootstrap (Politis–White block length, frozen `classify_verdict`) wired into the live cockpit bake-off; a fragile candidate is shown but never crowned (a high-Sharpe-but-fragile ensemble is demoted to the robust single). LLM/ML stay narration-only. Anchor-safe additive opt-in. ADR-0063. `194cfa6`.
- **advisor-llm-narration F9** (v0.1.0) — opt-in **"Explain"** on the crowned pick: the LLM renders the *already-computed* `Recommendation` + KPIs + robustness flags into faithful plain-language prose (it never enters the ranking). The load-bearing guard is `agent::narration::check_faithful` — a deterministic, llm-free **faithfulness post-check** that falls back to the templated copy if the LLM crowns a different winner (P1), contradicts the outcome (P2), emits a number not in the inputs (P3), or trips the frozen predict/advise banned-phrase list (P4); 21 tests incl. the anti-hallucination e2e. Core-typed `NarrationOutcome` (no `llm` type crosses `view`); opt-in/Anthropic-default/ephemeral. **Also wires the F6+F9 live last-mile** — the `agent→ui` display recipes (`ForwardPlanRecipe`, `NarrationOutcomeRecipe`) that were never connected, fixing F6's previously-empty live Plan screen; relay tests prove the channel→Message→state chain the fixture renders bypass. ADR-0064. `1dc9b32`.
- **advisor-bakeoff progress + polish** (v0.1.1) — a **determinate progress bar** beneath the bake-off input panel ("Running ⟨id⟩ — X of N", `backtest::BakeoffProgress` → `BakeoffProgressRecipe`, replacing the indeterminate spinner); the F6 ensemble plan now **names its members** ("at least 2 of {MACD trend, RSI reversion, Bollinger reversion}", `PlanRuleKind::Ensemble { members }`); the F5b forward supervisor **builds the new registry before tearing down the old loop** (a build error no longer strands the Live view). `afa5bfb`.
- **advisor-eur-fx F7** (v0.1.0) — honest **EUR→USDT budget conversion**: replaces "€200 ≈ 200 USDT (FX not modelled)" with a real rate (`core::fx` `FxRate` + a single `convert_eur_to_usdt` + a `BudgetConversion` carrier shared by engine + display → no drift) → "€200 ≈ $216.00 (at 1.08 EUR/USD, config)". Configurable static rate (live feed = noted v0.3); EUR stays a labelled `Decimal`; the bake-off ranking is FX-invariant. Day-1 conversion-applied gate proves the converted budget reaches + binds F4 (cap 216, qty > 200). ADR-0065. `a82f750`. The advisor v0.2 roadmap is now complete (F1–F9 + dynamic data + EUR-FX).
- **advisor-benchmark-robustness B1** (v0.1.1) — robustness-honesty fix: exempt the buy-and-hold **benchmark** from the `AllFragile` outcome. F8 activated the gate but judged the benchmark by the same candidate-overfit ruler built for the active arms, so the full real-BTCUSDT field flagged `Fragile` (incl. buy-and-hold) → outcome **always** `AllFragile`, suppressing the honest `BenchmarkWins` ("holding is the least-bad on this window") even when buy-and-hold was the top arm by Sharpe. Category error: the benchmark is the null hypothesis the candidates are scored *against*, not a candidate that must clear the bar. Two coordinated `rank.rs` edits — benchmark dropped from the `AllFragile` determination (`all_fragile` → `all_active_fragile`, D1) + benchmark always crown-eligible (D2) — with `classify_verdict`/`verdict_bands` **byte-frozen** (no robustness band moved; `robustness.rs`/`bootstrap.rs` untouched). Restores `BenchmarkWins` as the modal honest real-crypto outcome: since buy-and-hold is always in the field, all-active-Fragile ⇒ `BenchmarkWins`, never `AllFragile` (which now survives only as the benchmark-less residual). Lands the day-1 reachability gate ADR-0063 § D7 R4.4 specced-but-never-built; the `t7_1` real-data validation flips `AllFragile` → `BenchmarkWins` on BTCUSDT H1-2024 (all 7 Fragile, buy-and-hold crowned Sharpe 1.486 +47.78%). UI: the benchmark renders a muted "baseline is path-dependent" note, not the saturated candidate Fragile pill (render-proven). ADR-0066. `ab13407`.
- **advisor-combination-search** (v0.1.1) — **operator-requested** expansion of the bake-off's combination space ("we need to calculate the combination of multiple strategies"). Adds a FIXED, **pre-registered** slate of 6 new `EnsembleStrategy` vote arms over the 4 base signals → a **13-arm** advisor field (4 singles + 8 ensembles + buy-and-hold), each scored through the **identical frozen** robustness gate + benchmark. Reuses `VoteMethod` k-of-n verbatim (zero new arbitration math); literal ids, **no search** (pre-registration = overfit-safe by construction); bands FROZEN (not a B2/B3 proposal). Slate: 3 `Unanimous{n:2}` decorrelation pairs incl. the `trend_pair` **predicted-null control** + the complete k-of-4 ladder (`any1of4` / `k2of4` / `k3of4`), reported whole. Day-1 divergence e2e (each arm's equity ≠ its members AND ≠ buy-and-hold; 16 tests; FAIL-before/PASS-after); 13-row leaderboard + the crowned-combination named-member plan render-proven at the pixel layer (friendly labels — "Unanimous vote (MACD + RSI)" — and honest "sat in cash" for the mostly-Flat pairs). **Honest null result:** on real BTCUSDT H1-2024 **no combination cleared the gate** — all Fragile → `Outcome: BenchmarkWins`, "just hold" wins; and the **decorrelation thesis was not supported** (the correlated `trend_pair` control out-Sharped the decorrelated pairs, 1.075 vs 0.000 / −2.761). A null result is the valid, expected, shippable finding. ADR-0067. `9420965`.
- **advisor-short-selling** (v0.2.0) — **operator-directed** ("do the expensive short selling"): single-coin directional shorts (PAPER/SIM ONLY), the down-half the long-only program never covered. The financial core is a PORT of the shipped market-neutral perp engine (`montecarlo.rs`) into a shared `backtest::short_exec` helper (1× margin, maintenance-margin liquidation with cash allowed NEGATIVE, per-bar funding via `core::funding_rate::FundingRate`; losses NOT capped) called by BOTH the bake-off AND the agent forward loop (F5b parity). The 4 long-only clamps are GATED on `short_enabled` (not deleted → the long-only path stays byte-identical, anchors 119/119); the audit reader emits a signed `OpenPosition` (writer + reconciler byte-unchanged); the UI shows shorts + honest NEGATIVE P&L + a "can lose more than your €200" disclaimer (render-pixel-verified). A FIXED 5-arm slate (`sma_cross_ls`/`macd_ls`/`rsi_ls`/`bbands_ls` + `always_short`) in a SEPARATE `default_short_field` (the standard 13-arm advisor stays long-only). Day-1 downtrend-profit e2e (a short PROFITS on a fall, correct sign). **Result (real bear 2022-Q2 −56% + bull H1-2024):** the 4 short-TIMING arms are ALL FRAGILE on both windows — short timing fails like long timing. `always_short` comes back ROBUST on the bear (Sharpe 4.62, +56%) but that's the **in-window bootstrap blind spot** — a perpetual directional bet rides a matching trend (the mirror of buy-and-hold; FRAGILE −47% on the bull), not a usable edge (you'd have to time the regime). Honest conclusion, now stress-tested from every angle (long, combinations, shorts): **no active strategy beats holding robustly.** ADR-0068. `7526250`.
- **advisor-leaderboard-inspect** (v0.2.0) — **operator-requested** leaderboard interactivity ("Update the Leaderboard page"). (#1+#2) Clicking any leaderboard row now JUMPS to the Lab preseeded with that row's strategy + the leaderboard's chosen coin & lookback window, runs it FAITHFULLY (F5b is done → byte-identical to the ranked strategy), and shows the buy/sell overlay. New `Message::InspectStrategyFromLeaderboard{strategy,coin,lookback}`, handled purely in `state::update` (shares the nav core with `OpenStrategyInLab` so they can't drift; the coin/window are authoritative and override the cold-start default; `LeaderboardLookback::to_lab_date_range` → fixed presets map to the Lab's named `Preset`). Bundled fixes: (#5) "Open in Lab" was a no-op — the `SelectStrategy`→`SwitchScreen` chain is guarded "only if not already on Strategies" and the registry screen IS `Screen::Strategies`, so it refused to leave; (#6) the Lab buy/sell overlay read as "at the bottom" — the triangles were correct (they ride the price line) but the per-bar VOLUME HISTOGRAM used a different x-origin, fixed via `volume_histogram::view_aligned` reusing `chart::x_for_index` + the chart gutters (27px→3px). Render-pixel-verified. `e5f0002`, `8412ac0`.
- **leaderboard-timeframe-capital** (v0.1.0) — **operator-requested** bake-off "tune" knobs (#4; pairs were already adjustable via the coin chips). A bar-size/timeframe selector (H1/H4/D1 — `run_bakeoff` resamples the 1h corpus ONCE via `resample_ohlcv` to the chosen horizon, same coarser bars to every arm; H1 = identity) that DOES change the ranking, and a start-capital field (the sim's initial equity) that does NOT (returns are %-based — the UI says so honestly). `BakeoffRequest.{timeframe,initial_capital}` + `ScenarioConfig.initial_capital` (`None` → legacy `dec!(100_000)`, byte-identical for every existing path → anchors 119/119). Day-1 capital-divergence e2e (2× capital → ~2× absolute equity, return % unchanged). Bundled: `display_label`/`is_short_capable_id` now match the `v0.`-prefixed short ids the bake-off emits (friendly short-arm labels). `b0aeb17`.
- **advisor-param-tuning** (v0.1.0) — **operator-requested** gate-tied hyperparameter sweep editor (#3 — "strategies have hyperparameters, tune them manually"; the operator chose the GATE-TIED option over a naive editor). New Tune screen (routed `Screen::Tune`, drill-down off the Lab): the operator picks a per-family parameter grid (SMA fast/slow, MACD fast/slow/signal, RSI period/oversold, Bollinger period/k) and `run_param_sweep` scores EACH config through the IDENTICAL frozen robustness gate (1000-path moving-block bootstrap, `classify_verdict`) so an overfit config reads FRAGILE and its "Use this config" promote affordance is LOCKED — the anti-overfit affordance is the surfaced p5/p50/p95 distribution spread, not the in-sample point. `MAX_SWEEP_CONFIGS = 24` + honest truncation. Engine: `compute_robustness_distribution` (additive sibling; `compute_robustness_flag` delegates → gate stays BYTE-FROZEN, 8-case bit-identity proven); composed families generate the signal-DSL TOML IN-MEMORY (no runtime param override exists) loaded via `ComposedStrategyConfig::from_str` → `composed_toml_override` (no disk write, anchor-safe), IDENTITY-GUARDED byte-identical (AST hash + `signal_raw`) to the shipped `config/strategies/*.toml` so a swept config is the SAME strategy that gets ranked. Day-1 divergence e2e per family (the no-op guard now covers composed too). All 4 families render-pixel-verified (the populated grid + the FRAGILE lock). The `param_robustness_sweep` bin is NOT reused (bin-only + wrong universe). ADR-0069. `c22ccc4` (design), `3c297b3` (engine T1–T5), `ac02bcd` (UI T6–T10 + composed engine T7), `+T7b` (composed families ON). **Closes the operator's 7-item leaderboard epic.**
- **advisor-param-promotion** (v0.1.0) — **operator-requested** v0.2 follow-on to advisor-param-tuning: wire the Tune editor's "Use this config" affordance (was a visual label) to launch the forward €200 paper-trade of a TUNED config. Engine: `ForwardRunConfig.param_override: Option<ForwardParamOverride>` (agent-owned closed enum) — `build_registry_for` (F5 paper run) + `build_forward_plan_from_registry` (F6 plan) each gain an EARLY-RETURN `Some`-branch that resolves the tuned strategy (SMA lens, or composed via the now-`pub` `backtest::{macd,rsi,bbands}_toml` + `from_str` — the SAME identity-guarded round-trip the sweep scored); `None` stays byte-identical → anchors 119/119. UI: only `promotable` (non-FRAGILE) rows become a real button firing `Message::PromoteSweptConfig`, which preseeds + navigates to the forward Plan and launches via the crowned-pick `ForwardCommand::Launch` (delta = `param_override: Some`); the FRAGILE lock is untouched (a fragile config can NEVER promote — the anti-overfit gate is final). The forward Plan shows the TUNED rules (e.g. 10/20, not default 20/50) + a distinct honesty header ("you tuned this; survived resampling on THIS window — not a guarantee, not advice"). Day-1 gates: override→divergent signals (no-op guard), agent TOML byte-equals the sweep generator (fidelity), plan-reflects-tuned; pure-state wiring test (the click carries the config, not just paints accent — the v3-vol-overlay-noop lesson); render-pixel-verified (the enabled button vs the locked label; the promoted plan + note). ADR-0070. `bf7e5c7` (design), `69e2c06` (engine A–C), `+UI D–F`.
- **advisor-signal-library-expansion** (v0.1.0) — **operator-requested** ("item 1") expansion of the single-coin signal library with a FIXED, pre-registered slate of 5 new bake-off arms beyond SMA/MACD/RSI/Bollinger — honest coverage + a richer decorrelation menu for the combination feature, NOT an alpha claim. The slate (each scored by the IDENTICAL frozen gate + buy-and-hold benchmark): `v0.donchian_break` (`high >= max(high,20)` — new 20-bar high), `v0.donchian_floor` (`close > min(low,20)`), `v0.vol_breakout` (`high >= max(high,20) AND volume > 2*avg(volume,20)`), `v0.roc_momentum` (`close > avg(close,10)*1.05`), `v0.obv` (`obv() > obv_avg(20) AND close > sma(50)`). **New OBV DSL primitive (ADR-0071):** two `IndicatorState` variants — `obv` (arity-0, the first 0-arity indicator, spelled `obv()`) + `obv_avg(N)` (`avg(obv,N)` is impossible — the rolling family only accepts bar-fields — so `obv_avg` owns an inner `Obv`, the MacdLine-owns-EMA pattern); recurrence `OBV_t = OBV_{t-1} + sign(Δclose)·volume` (all Decimal); proven by a textbook-OBV identity guard (all 3 sign branches + warm-up) + a 0-arity `obv()` parser test. Seam: 5 `run_scenario` dispatch arms + `default_field()` 13→18 (advisor field auto-picks up) + 5 friendly `display_label`s + an 18-row leaderboard render proof; day-1 divergence gate (the breakout arms FIRE on a breakout series + diverge from existing arms + buy-and-hold, with a non-vacuous FAIL-before). Frozen gate/bands/benchmark; new arms `write_report=false` → anchors 119/119. **Result (T14, BTCUSDT H1-2024, 1000-path bootstrap): all 5 new arms FRAGILE → BenchmarkWins** (buy-and-hold +47.78%) — the pre-registered, valid null result; the ship-passive thesis holds, now tested with breakout / volume-flow / OBV signals it had never seen. (Build caught + corrected a scope drift: a dev pass had silently SMA-ified the breakout signals — `close > avg(close,20)` — destroying the decorrelation; restored to real breakouts.) The third pre-registered arm-class expansion (after combination-search + short-selling). ADR-0071. `2a96b69` (analyst+architect+ADR), `c271c26` (build).
- **advisor-options-impliedvol-probe** (v0.2.0) — **operator-requested** ("all 3, full-fat") #4 fresh-channel probe: the **OPTIONS / IMPLIED-VOLATILITY** angle (the channel the gated on-chain corpora couldn't reach — Deribit DVOL is free + no-auth). A new exogenous-series bake-off arm `v0.dvol_regime` — hold the coin while Deribit DVOL (the BTC/ETH 30-day implied-vol index) sits BELOW its own trailing 30-day median (calm), else step to cash; LOCKED W=30 signal, **no search** (pre-registered, overfit-safe). Hand-written `DvolRegimeStrategy` (the composed DSL can't express a per-bar EXOGENOUS series); a new git-ignored `data/deribit-dvol/` corpus + `fetch_deribit_dvol` fetcher (`public/get_volatility_index_data`, BTC+ETH 2023-24, SHA-pinned REVISION.toml); `dvol_data.rs` (`DvolDataSource::load` + `dvol_as_of`) DELEGATES the look-ahead-impossible daily→hourly LOCF join to the ADR-0058 `PitSeries`; `resolve_dvol_override` threads the as-of series into the arm inside `run_bakeoff` (BTC+ETH only via `dvol_supported`; graceful skip — never crash — if the corpus is absent). Frozen gate/bands/benchmark; `write_report=false` → anchors 119/119. Day-1 gates: strategy-layer divergence + leak-check (synthetic), PLUS a bake-off-PATH gate proving the orchestrator actually FEEDS real DVOL (the arm's equity diverges from buy-and-hold ~48k/49k USDT on BTC/ETH — catching the `None`-stub no-op an earlier pass shipped). Friendly `display_label` ("Implied-vol regime (hold when DVOL < 30-day median)"). **Result (1000-path bootstrap, BTCUSDT + ETHUSDT H1-2024): `v0.dvol_regime` FRAGILE on BOTH → `BenchmarkWins`** — the pre-registered, valid null; the implied-vol regime signal does not beat holding. The first of the 3 #4 fresh-channel probes; honest coverage of the options/IV channel + a durable reusable exogenous-series seam, NOT an alpha claim. ADR-0072. `5c103ab` (design), `+build`.
- **advisor-crossasset-macro-regime** (v0.1.0) — **operator-requested** ("all 3, full-fat") #4 fresh-channel probe: the **MACRO / CROSS-ASSET / RATES** angle, delivered WITH a reusable trading-calendar layer the broader system lacked. **Two deliverables. (A) the durable MARKET-CALENDAR layer:** a new `crates/data/src/calendar.rs` — `MarketCalendar{Crypto24x7,UsEquity}` + `classify_ticker` + `expected_bars_for_calendar`; the calendar is DERIVED FROM the ticker INSIDE `load_cached`/`fetch_yahoo_klines` so the public signature stays UNCHANGED → all call sites + crypto coverage byte-identical, while equity/FX/rates tickers (which trade ~68% of calendar days) now pass the ≥95% coverage gate instead of being rejected. **(B) the LOCKED arm `v0.macro_riskon`** (NO search, pre-registered): risk-ON ::= `^GSPC>SMA(50) AND DX-Y.NYB<SMA(50) AND ^TNX<SMA(20)` → hold the coin (full budget) else flat. Hand-written `run_macro_gated_buyhold_path` (sibling of `run_buyhold_path`); the daily→hourly LOCF join = `regime.as_of_value(bar.open_ts)` via the ADR-0058 `core::pit::PitSeries<bool>` (look-ahead impossible); `macro_regime.rs::load_macro_regime_series` + `ScenarioConfig.macro_regime_series` + `BakeoffConfig::default_macro_field`; a new git-ignored `data/yahoo-macro/` corpus (^GSPC/DX-Y.NYB/^TNX 2024, SHA-pinned REVISION.toml). Frozen gate/bands/benchmark; `write_report=false` → anchors 119/119. Day-1 gate `macro_regime_overlay_end_to_end.rs` S1–S4 (divergence ≥1bp / always-ON no-op control / future-shift leak-check / warm-up) PASS. **The build caught that the probe had never actually compiled under the `yahoo` feature** (an earlier pass tested only `realdata`, where the whole macro path cfg's out): the arm dispatch was spuriously `#[cfg(feature="yahoo")]`-gated → `UnknownStrategy("v0.macro_riskon")` on the decisive run; fixed by un-gating it (DVOL precedent — always compiled, graceful-degrade to flat when the corpus is absent) + 3 never-compiled defects (`u64`→`usize` KPI counters, `DateRange`-is-an-enum via `date_range_to_ms_pair`, `dec`-import scoped to tests). **Result (1000-path bootstrap, BTCUSDT H1-2024): `v0.macro_riskon` FRAGILE (Sharpe −0.041, 6 regime flips — meaningfully traded, NOT vacuous) → `BenchmarkWins`** — the pre-registered, valid null; the macro risk-on/off regime signal does not beat holding (matching the literature: macro fails OOS outside rate-cut regimes). The 2nd of the 3 #4 fresh-channel probes; the durable deliverables are the reusable market-calendar layer + the exogenous-regime seam + honest macro-channel coverage, NOT an alpha claim. ADR-0073. `5c103ab` (design), `+build`.

## Strategy & backtest engine

- **v0** — Paper-trading SMA-crossover tracer bullet: end-to-end harness (core types, Binance data, audit ledger, cockpit) proving a trivial strategy round-trips with reconciling double-entry books.
- **v0.5** — Composed strategies (hot-loadable TOML indicator/rule assemblies, atomic swap on file change) plus the multi-indicator rule set (MACD + RSI + Bollinger).
- **v1** — Cross-sectional top-N momentum: first multi-symbol real-edge candidate.
- **v1.5a** — Mean-reversion on z-scored pairs (pairs/portfolio plumbing).
- **v1.5b** — Multi-venue support + 1-second aggregated trades.
- **v2** — LLM news/sentiment strategy overlay (first LLM-in-the-loop strategy; support layer, not the alpha source).
- **v2.1** — tracing-`Layer` secret redactor wired across 17 binaries (`REDACT_LAYER_MODE`, WARN default).
- **v5** — Deterministic latency & slippage simulation closing the backtest-vs-live gap, landed across `v0.1`→`v0.5`: canonical medium-friction model (`slippage_bps: 8`, square-root market impact) with the full anchor-migration chain (v0.2 anchor migration, v0.3 full-path wiring, v0.4 candle/realdata feature-gated re-emit, v0.5 sqrt-impact).
- **backtest-real-binance-data** — Real-Binance-data backtest path (pinned corpus, deterministic seeds).
- **simple-strategies-realdata** — SMA / MACD / RSI / Bollinger runnable on real Binance data in the Lab.

## Robustness program — CONCLUDED 2026-06-08 → ship passive

- **monte-carlo-bootstrap-path-generator** (C1) — stationary-block-bootstrap path generator that resamples real returns (Politis–White auto block length), preserving fat tails and volatility clustering.
- **strategy-robustness-harness** (C2) — distribution-summary backtest mode (Sharpe p5/p50/p95, max-drawdown tail, probability-of-loss) read against the frozen § 0 decision rule (p5 Sharpe < 0 → FRAGILE).
- **momentum-parameter-robustness-sweep** — distribution-per-θ over the momentum family; FAMILY-UNIFORM-FRAGILE.
- **cross-sectional-mean-reversion-strategy** — first pivot family through the harness; FRAGILE.
- **time-series-momentum-robustness** — per-asset absolute momentum (long/flat), the thesis-closing OHLCV test; FRAGILE.
- **horizon-retest-robustness** — coarser 4h/daily decision cadence, the last untested OHLCV axis; FRAGILE.
- **carry-strategy** — cross-sectional funding-carry rotation; FRAGILE.
- **perp-basis-signal-robustness** — perp-spot basis-reversal (long-only), the one LIVE/MEDIUM-HIGH signal the program certified, but fee-gated to fragile in production.
- **perp-basis-mn-spread** — market-neutral basis spread in all three arms; basis ≡ funding byte-identically, residual carries negative median Sharpe — closes the derivatives-positioning channel.
- **simple-strategy-overfit-guard** — overfit/robustness guard proving the down-market trend "hedge" was 2-case noise.
- **simple-strategy-bear-survey** — two-stage bear-market survey over the 2021-22 corpus firming ship-passive on the deepest bear evidence.
- **binance-corpus-expansion** — wider down-market Binance corpus (`data/binance/`, ADR-0032 pins) underpinning the surveys above.
- **passive baseline shipped** — buy-and-hold promoted to canonical production baseline (`spec/runbooks/passive-baseline.md`).

## Retired research lines (negative result; code + anchors retained, not deleted)

- **v2.5 DL forecaster programme** — 4-phase bet (TCN overlay, PatchTST overlay, vanilla Transformer, v2.6 bake-off) plus the TCN alpha-investigation / recalibrate / threshold-tuning / horizon-bump sub-studies: terminal **F4** across two model families, no +0.10 Sharpe-delta on hourly OHLCV. RETIRED 2026-05-22.
- **v3 volatility forecaster** (GARCH-σ position sizing, + noop-fix + re-baseline) — MODEL-BROKEN / NO-ALPHA after the no-op overlay fix. RETIRED 2026-05-22.
- **v3 LLM-forecaster** — reflection-memory + audit-trail-anchored LLM signal; **shipped-partial** (alpha-verdict wave deferred on absent `ANTHROPIC_API_KEY`).
- **v3 regime-classifier / v3 XGBoost cheap-classifier** — OHLCV regime-label predictors; foreclosed when the OHLCV channel was exhausted.
- **vol-killswitch-overlay-noop-fix** — fixed a computed-but-unapplied kill-switch overlay (the precedent behind the day-1 baseline-equity-divergence e2e non-negotiable).

## Cockpit & UI

### Shell, navigation & design system
- **lumen-design-adoption** — master roadmap migrating the cockpit to the Lumen design system + multi-screen sidebar shell (Phase 1 tokens/chrome/status-bar shipped; Phases 2-5 shipped; Phase 6 Assistant slot reserved, gated on v2 LLM).
- **ui-rethink-phase-a-lab** — chart-centric Lab screen.
- **ui-rethink-phase-b-lab-run** — Lab Run button + run plumbing.
- **ui-rethink-phase-c-sidebar-ia** — sidebar IA flip + Live + strategy registry + settings rollup.
- **ui-rethink-phase-d-trail** (+ follow-up) — Trail (audit-journal) view.
- **ui-rethink-phase-e-compare** — Compare matrix.
- **ui-rethink-phase-f-memory-models-assistant** — Memory + Models screens + the Phase-6 Assistant slot.

### Live cockpit & dashboards
- **live-cockpit-unified** — unified single-process cockpit binary.
- **cockpit-live-dashboard-wiring** — equity curve + KPI strip fed from the live agent.
- **live-equity-history-durable** — durable equity history surviving `cockpit_live` restarts.
- **paper-mode-equity-wiring** — real (not flat-line) paper-mode cockpit equity.
- **cockpit-baseline-panel** — surfaces the shipped passive buy-and-hold result.
- **cockpit-reports-viewer** — in-cockpit Reports screen (Library sidebar group): browses the committed `backtest-*.md` corpus and renders the selected report (KPI strip + markdown body + equity curve/drawdown band when the report ships a companion CSV — see `backtest-equity-companion`), reusing the offline viewer's render logic via a shared `crate::reports` loader.
- **backtest-equity-companion** — backtest runs emit a companion `reports/artifacts/<stem>/equity-*.csv` (real per-bar equity) so the Reports screen + offline viewer render a populated equity curve + drawdown band; includes the loader stem-match correctness fix (pair companion→report by file stem, not first-match-any) and 14 committed non-anchored demo reports (real-Binance sma-cross over the 2024 universe). The Reports picker defaults to a **curve-only** filter (with a *show-all* toggle) so switching always lands on a graph — index-safe over the full discovered corpus — and a switch-regression pixel guard proves selecting a different report repaints a distinct curve.
- **cockpit-activity-status-bar** + **-audit-ledger-producer** + **-llm-producer** — continuously-updated "what is the cockpit doing right now" activity bar with audit-ledger and LLM-call producers.
- **cockpit-toast-queue** (+ v0.2 cleanup) — bounded toast queue replacing the single-slot REPLACE semantic.
- **cockpit-training-control** + **cockpit-training-pressed-wiring** — operator-driven `train_tcn` launcher and Train-button wiring.
- **cockpit-performance-and-input-responsiveness** — input-responsiveness + render-performance pass.
- **cockpit-render-regression** — render-regression diagnosis + quality-gate overhaul.

### Charts, tape & journal
- **chart-canvas-overhaul** — chart canvas rewrite.
- **chart-buy-sell-emphasis** — buy/sell marker emphasis from the audit ledger.
- **chart-x-axis-local-time** — local-time x-axis (atomic test-mode override; fixed a `set_var` data race).
- **chart-fixture-line-clipping** — canvas-clip fix (vendored `iced_tiny_skia` fork).
- **tape-row-audit-modal** — click a tape row to open its audit-transaction modal.
- **journal-transactions-metadata** — journal-transactions metadata reader.
- **real-mtm-unrealized-pnl** — real mark-to-market unrealized P&L.
- **per-symbol-position-accounts** — per-symbol position accounts in the ledger.

### Lab (run → save → compare) & data
- **lab-run-save-compare** — real-data strategy checking with durable reports.
- **lab-compare-equity-overlay** — two-run equity overlay on the Compare screen.
- **lab-end-to-end-v2** — closes the Phase A/B gaps, adds a progress bar.
- **lab-polish-round-2** — position curve + param tuning + UI density.
- **lab-recipe-test-harness** (+ v0.2 cross-surface extension) — Lab recipe / subscription test harness.
- **lab-yahoo-realdata** — multi-asset Yahoo data pivot for the Lab (+ v0.1.2 ETH-USD anchor & cache badge, + v0.1.3 REVISION front-matter & Binance ETH H1).
- **lab-yahoo-empty-range-ux** — empty-date-range UX handling.
- **bug-64-d11-attempt-3-yahoo-run-runtime-context** — Yahoo+Run runtime-context + cancellation fix.

### iced platform & UI quality gates
- **iced-native-widgets** + **iced-aw-cherry-pick** + **ui-drop-iced-aw** — migration to native iced widgets, dropping `iced_aw`/`iced_fonts`.
- **ui-gallery-bin** — widget-gallery binary.
- **ui-contrast-asserter** — WCAG contrast asserter (v0.2 enforcing gate, `UI_CONTRAST_MODE`, 6 ratified opt-outs).
- **ui-quality-gate-overhaul** — UI quality-gate overhaul.
- **ui-headless-emulator** + **ui-session-journal-iced-tester** + **ui-test-harness-bootstrap** — headless render adapter, `iced_tester` session-journal adapter, and the panel-snapshot test harness.
- **ui-test-harness-viewport-matrix** — multi-viewport snapshot matrix.
- **visual-fail-html-reporter** — HTML viewer for visual-test failures.

## Core infrastructure

- **reflection-memory** (+ trader-wiring) — persistent lesson-card store with retrieval at decision time, wired through the sanctioned ADR-0041 layering seam.
- **operator-success-reports** — auto-generated "is this working?" reports (equity, Sharpe/Sortino/drawdown, attribution, system health).
- **audit-tick-consumer-envelope** — audit tick consumer with an aggregation envelope.
- **point-in-time-data-discipline** — `core::pit::PitSeries`/`AsOf`: a type-level as-of primitive making look-ahead *unrepresentable* (`AsOf` has private fields + no public ctor; a trybuild compile-fail is the proof), consolidating the hand-rolled as-of joins (funding/basis) behind one guarded API. Behaviour-preserving — anchors 119/119. (ADR-0058, extends ADR-0041.)
- **paper-soak-longevity** (reflection-loop paper-wiring) — wires the reflection writer into the paper trading loop so a lesson card is written on each closed trade (the moat #2 differentiator, previously never wired in the paper path); regime tags are accurate via a BTC-daily-close seed loaded off the async hot path (`spawn_blocking`, no startup hang). Ships the longevity evidence artifact (in-session soak: durable fills, equity movement, restart-continuity, kill-switch, lesson accumulation) + an operator runbook for the real-time 90-day soak. Regression guard: `reflection_wiring_regression.rs` (with a no-writer negative control).

## Tooling & process

- **operator-ledger-schema-lint** — ledger chart-of-accounts schema lint.
- **adr-registry-atomic-lint** — ADR-registry atomicity lint (sibling pre-commit guard).
- **queue-staleness-reconciliation** — backlog-queue staleness reconciliation pass.
- **subscription-pipe-server-time-template** — server-time template closing the Wave-1 subscription-pipe carve-out.
- **regression-anchor gate** — `spec/anchors.toml` + `scripts/verify_anchors.sh` (119 byte-SHA bodies); `scripts/spec_lint.py` structural gate (dead-link, frontmatter, orphan, trace, status-drift).

## Deferred / not built (by decision)

- **cockpit-cross-platform** — Linux/Windows source shipped + macOS-verified; 3-OS CI matrix parked inert (`.github/workflows/ci.yml.deferred`), activation deferred to the near-done milestone.
- **cockpit-app-bundle**, **iced-ecosystem-evaluation**, **ui-gallery-table-cell** — candidate/draft, not built.
- **Out of scope (follow-up project):** real-money execution, KYC, exchange API keys, withdrawals, multi-venue real-money, tax/lot accounting.
