# Papers — Deep Learning & Reinforcement Learning for Trading

Ledger of papers read for this topic. One entry per paper, appended immediately
after reading. Source of truth for resume (skip listed titles, continue numbering).

### [1] FinRL: A Deep Reinforcement Learning Library for Automated Stock Trading in Quantitative Finance
- **Authors / Venue:** Xiao-Yang Liu, Hongyang Yang, Qian Chen, Runjia Zhang, Liuqing Yang, Bowen Xiao, Christina Dan Wang / Deep RL Workshop, NeurIPS 2020
- **Year:** 2020
- **Source:** arXiv:2011.09607
- **% read:** 50%
- **Summary:** Introduces the original FinRL open-source library — a three-layer (environment / agent / application) modular pipeline that turns stock-market datasets into Gym-style environments, trains DRL agents (DQN, DDPG, PPO, SAC, A2C, TD3), and backtests them with transaction costs, liquidity constraints and a risk-aversion (turbulence) control. Demonstrates single-stock, multi-stock and portfolio-allocation tasks across NASDAQ-100, S&P 500, DJIA and several Asian indices. Positioned as a reproducibility/education tool rather than a performance claim — it standardizes baselines and reward functions to lower the DRL learning curve.
- **Relevance to our system:** This is the canonical DRL-for-trading scaffolding everyone cites; its env/agent/backtest separation mirrors our bake-off → forward-plan split. Useful as a reference for *how* DRL practitioners frame the trading MDP (state = prices+indicators+holdings, action = target weights, reward = Δ portfolio value − costs) and for the "turbulence index" risk-off switch. Notably the paper makes NO claim that DRL beats buy-and-hold net of costs — consistent with our validated thesis. Background/reference; not directly deployable in a single-coin advisor without heavy adaptation and out-of-sample discipline.

### [2] Deep Hedging
- **Authors / Venue:** Hans Bühler, Lukas Gonon, Josef Teichmann, Ben Wood / Quantitative Finance 19(8):1271–1291
- **Year:** 2018 (arXiv) / 2019 (journal)
- **Source:** arXiv:1802.03042
- **% read:** 50%
- **Summary:** The foundational "deep hedging" paper. Frames hedging a derivatives portfolio under realistic frictions (transaction costs, market impact, liquidity, risk limits) as a Markov Decision Process and trains a neural-network policy to directly minimize a convex risk measure (OCE / entropic) of terminal P&L — bypassing any pricing model or Greeks. Proves the class of constrained NN strategies is rich enough to ε-approximate the optimal hedge. In synthetic Heston markets with costs it matches/beats the frictionless "complete market" delta and scales to high-dimensional portfolios; performance is driven by the set of available hedging instruments, not portfolio size.
- **Relevance to our system:** Background only for the hedging math, BUT the *methodological pattern* is highly relevant: optimize the policy directly against a risk-aware objective (a convex risk measure of P&L) rather than predicting prices then trading. That "learn the decision, not the forecast" framing and the use of a coherent/convex risk measure as the training loss is a transferable idea if we ever train a sizing policy. The reliance on a known synthetic generator (Heston) for training data also foreshadows our interest in market simulators/GANs for test data. Not deployable as-is (we hold spot, not options).

### [3] DeepLOB: Deep Convolutional Neural Networks for Limit Order Books
- **Authors / Venue:** Zihao Zhang, Stefan Zohren, Stephen Roberts / IEEE Transactions on Signal Processing 67(11):3001–3012
- **Year:** 2018 (arXiv) / 2019 (journal)
- **Source:** arXiv:1808.03668
- **% read:** 50%
- **Summary:** Predicts short-term mid-price direction (up/stationary/down) from raw limit-order-book snapshots. Architecture stacks convolutional filters that learn spatial structure across LOB price levels, an Inception module for multi-scale features, then an LSTM for temporal dependence. Trained/evaluated on the FI-2010 benchmark and one year of London Stock Exchange quotes; reports state-of-the-art out-of-sample classification accuracy beating SVM, MLP and prior CNNs, and — importantly — transfers to instruments NOT in the training set, suggesting it learns "universal" microstructure features.
- **Relevance to our system:** Mostly background — we run on OHLCV bars for a single spot coin, not full L2 order-book tape, so the architecture isn't directly usable. Two transferable lessons: (1) a model can learn *generalizable* microstructure features that transfer across instruments (relevant if we ever go multi-coin), and (2) the paper reports classification accuracy but does NOT convert the signal into a cost-aware P&L or address tradability — a recurring pattern in this literature that our buy-and-hold-benchmarked, cost-net robustness gate is specifically designed to expose. A high-accuracy directional classifier ≠ a profitable strategy.

### [4] A Survey of Forex and Stock Price Prediction Using Deep Learning
- **Authors / Venue:** Zexin Hu, Yiqi Zhao, Matloob Khushi / Applied System Innovation 4(1):9
- **Year:** 2021
- **Source:** arXiv:2103.09750
- **% read:** 35%
- **Summary:** Survey of DL price-prediction work across six families: CNN, LSTM, DNN, RNN, reinforcement learning, and "other" (HAN, NLP-driven, WaveNet). Finds the dominant trend is LSTM-hybrid models (LSTM+CNN, LSTM+DNN) and reports that RL and hybrids "yielded great returns." Catalogs the metric zoo used: RMSE, MAPE, MAE, MSE, accuracy, Sharpe ratio, return rate.
- **Relevance to our system:** Useful mainly as a map of the field and as evidence of a methodological problem we already guard against: the survey aggregates dozens of papers reporting glowing RMSE/return numbers but does NOT critically engage with overfitting, data-snooping, transaction costs, or reproducibility. The mismatched-metric soup (some report regression error, some report accuracy, some report Sharpe) makes cross-paper comparison meaningless — exactly why our app fixes ONE frozen robustness gate with buy-and-hold as the universal benchmark. Skeptical takeaway: treat headline numbers in this literature as upper bounds under data-snooping, not as deployable performance.

### [5] Temporal Fusion Transformers for Interpretable Multi-horizon Time Series Forecasting
- **Authors / Venue:** Bryan Lim, Sercan O. Arik, Nicolas Loeff, Tomas Pfister (Google Cloud AI) / International Journal of Forecasting 37(4):1748–1764
- **Year:** 2019 (arXiv) / 2021 (journal)
- **Source:** arXiv:1912.09363
- **% read:** 50%
- **Summary:** The Temporal Fusion Transformer (TFT) is an attention architecture for multi-horizon forecasting with heterogeneous inputs — static covariates, known-future inputs (e.g. calendar), and observed-only time series. Components: per-input variable-selection networks (which double as feature-importance), an LSTM encoder-decoder for local processing, gating (GRN) layers to skip unused components, and an interpretable multi-head attention layer for long-range dependence; it outputs quantiles (probabilistic forecasts). Reports significant gains over DeepAR/Seq2Seq/MQRNN on retail, electricity, traffic, and volatility benchmarks while remaining inspectable via attention + variable weights.
- **Relevance to our system:** Moderately relevant as a *forecasting* tool but with the same deployability caveat as all forecasters. Two genuinely useful ideas if we ever add a learned component: (1) quantile/probabilistic outputs (gives an honest uncertainty band rather than a point price — fits our risk-aware ethos), and (2) built-in interpretability (variable-selection weights) so a learned signal isn't a black box. BUT TFT needs many related series + rich covariates to shine; a single coin's OHLCV is data-poor for it, and the volatility benchmark in the paper is forecasting *realized vol*, not generating tradable, cost-net alpha. Treat as a "if we ever forecast, forecast a distribution and keep it interpretable" reference.

### [6] Informer: Beyond Efficient Transformer for Long Sequence Time-Series Forecasting
- **Authors / Venue:** Haoyi Zhou, Shanghang Zhang, Jieqi Peng, Shuai Zhang, Jianxin Li, Hui Xiong, Wancai Zhang / AAAI 2021 (Best Paper)
- **Year:** 2020 (arXiv) / 2021 (AAAI)
- **Source:** arXiv:2012.07436
- **% read:** 50%
- **Summary:** Tackles long-sequence time-series forecasting where vanilla Transformers are O(L²) in time/memory. Three innovations: (1) ProbSparse self-attention selecting only the dominant queries for O(L log L) cost; (2) self-attention distilling that halves layer length to handle very long inputs; (3) a generative-style decoder that emits the whole horizon in one forward pass (no slow autoregression, no error accumulation). Beats prior methods on ETT (electricity transformer temperature), ECL, and weather benchmarks for long horizons.
- **Relevance to our system:** Background. Informer is an efficiency story for *very long* horizons/sequences (hundreds–thousands of steps), which is the opposite of a single-coin advisor that decides over modest windows. The benchmarks are physical/utility series, not finance. The real signal for us comes from the *critique* of this whole transformer-forecasting line (see DLinear, next): on many series simple linear baselines match or beat these elaborate models, so importing Informer-class complexity into our advisor would add compute + overfitting surface for little expected edge.

### [7] Autoformer: Decomposition Transformers with Auto-Correlation for Long-Term Series Forecasting
- **Authors / Venue:** Haixu Wu, Jiehui Xu, Jianmin Wang, Mingsheng Long (Tsinghua) / NeurIPS 2021
- **Year:** 2021
- **Source:** arXiv:2106.13008
- **% read:** 50%
- **Summary:** Long-term forecasting Transformer with two ideas: (1) makes seasonal-trend *series decomposition* an inner block repeated through the network (progressively separating trend from seasonal) rather than a one-off preprocessing step; (2) replaces dot-product self-attention with an Auto-Correlation mechanism that uses FFT to find period-based sub-series dependencies and aggregate them, giving O(L log L) cost. Reports a 38% relative error reduction over prior baselines across six benchmarks spanning energy, traffic, economics, weather, and disease.
- **Relevance to our system:** Background as an architecture, but ONE idea is broadly useful and cheap: explicit trend/seasonal decomposition. Decomposing a price/return series into trend + seasonal + residual before any modeling is a classic, low-overfitting-risk preprocessing step that even simple strategies can exploit, and it's far lighter than the full Autoformer. The "economics" benchmark is the standard exchange-rate dataset, not crypto, and there's no cost-aware trading evaluation. Net: borrow the decomposition idea conceptually; skip the Auto-Correlation Transformer itself.

### [8] Are Transformers Effective for Time Series Forecasting?
- **Authors / Venue:** Ailing Zeng, Muxi Chen, Lei Zhang, Qiang Xu / AAAI 2023 (Oral)
- **Year:** 2022 (arXiv) / 2023 (AAAI)
- **Source:** arXiv:2205.13504
- **% read:** 75%
- **Summary:** The key skeptical counterpoint to the whole Transformer-forecasting line. Argues that self-attention is permutation-invariant, so it inherently loses the temporal *ordering* that time-series forecasting depends on (positional encodings only partly compensate). Proposes embarrassingly simple one-layer linear models — DLinear (trend/seasonal decomposition + a linear layer each) and NLinear (subtract-last normalization + linear) — as baselines. Across nine real-world datasets these linear models *outperform* Informer, Autoformer, FEDformer, etc., often by a large margin, implying the elaborate architectures' reported gains have "little to do with" Transformer temporal modeling.
- **Relevance to our system:** HIGHLY relevant as a guardrail. It is direct evidence that complex DL forecasters frequently fail to beat trivial linear baselines on real series — the forecasting analogue of our validated "no active strategy robustly beats buy-and-hold" thesis. The lesson for the advisor: any learned/DL component MUST be benchmarked against a dead-simple baseline (linear extrapolation, last-value, or just buy-and-hold) before it earns its place, and "we used a Transformer" is not evidence of edge. This justifies our frozen-gate + always-benchmark design and our reluctance to add DL forecasters. Strong support for staying simple.

### [9] Quant GANs: Deep Generation of Financial Time Series
- **Authors / Venue:** Magnus Wiese, Robert Knobloch, Ralf Korn, Peter Kretschmer / Quantitative Finance 20(9):1419–1440
- **Year:** 2019 (arXiv) / 2020 (journal)
- **Source:** arXiv:1907.06673
- **% read:** 50%
- **Summary:** A GAN for generating synthetic financial price paths. Both generator and discriminator are Temporal Convolutional Networks (TCNs, dilated causal convs) so the model captures long-range dependence like volatility clustering; it models log-returns and is constructed so the resulting process can be transformed toward a risk-neutral measure. The synthetic series reproduce the classic stylized facts — fat-tailed return distributions, volatility clusters, leverage effect, and slow decay of autocorrelation in squared/absolute returns — with distributional agreement across short and long lags. Evaluated by comparing stylized-fact statistics of real vs. generated paths.
- **Relevance to our system:** Directly relevant to our test-data discipline. This is a concrete recipe for generating realistic synthetic price paths to stress-test strategies and the robustness gate beyond the single historical path we actually observed. Compared to our current moving-block bootstrap (which only reshuffles real returns and can't produce unseen-but-plausible regimes), a QuantGAN-style generator could enrich the test set with novel volatility-clustered scenarios — IF we validate that it reproduces our coin's stylized facts and don't overfit the generator to the same data we test on. Caveats to respect: GANs can mode-collapse, may not capture true non-stationarity/regime shifts, and synthetic data that's too close to training data leaks. Flag: a strong candidate to augment (not replace) the bootstrap. See also diffusion-based generators as a more-stable alternative.

### [10] A Deep Reinforcement Learning Framework for the Financial Portfolio Management Problem
- **Authors / Venue:** Zhengyao Jiang, Dixing Xu, Jinjun Liang / arXiv preprint (widely cited)
- **Year:** 2017
- **Source:** arXiv:1706.10059
- **% read:** 50%
- **Summary:** The canonical model-free DRL crypto-portfolio paper. Introduces the EIIE (Ensemble of Identical Independent Evaluators) topology — a shared network scoring each asset independently — plus a Portfolio-Vector Memory (last weights as input, to handle costs) and Online Stochastic Batch Learning, trained to maximize log return. Tested on Poloniex crypto with 30-min bars and a stated 0.25% commission; reports "at least 4-fold returns in 50 days" across three backtests.
- **Relevance to our system:** Important as a *cautionary* exemplar. The 4x-in-50-days headline is exactly the kind of result our robustness gate exists to debunk: the test period (2017) was an extreme crypto bull run where buy-and-hold itself multiplied several-fold, the asset universe was selected by post-hoc liquidity (survivorship/selection bias), there is no 1000-path bootstrap or weakest-link verdict, and slippage/market-impact beyond a flat commission is ignored. Later independent work repeatedly fails to reproduce such returns out-of-sample. For us: the EIIE/PVM *architecture* is interesting if we ever go multi-coin, but the result should NOT be read as evidence DRL beats holding — benchmark against buy-and-hold over the SAME window and it likely evaporates. Reinforces our thesis and gate design.

### [11] Graph-Based Learning for Stock Movement Prediction with Textual and Relational Data
- **Authors / Venue:** Qinkai Chen, Christian-Yann Robert / arXiv (also Journal of Financial Data Science)
- **Year:** 2021
- **Source:** arXiv:2107.10941
- **% read:** 35%
- **Summary:** Proposes MGRN (Multi-Graph Recurrent Network): combines textual sentiment from financial news with multiple relational graphs among stocks to predict next-day movement. The GNN fuses relation edges with a recurrent temporal encoder. Evaluated on STOXX Europe 600; reports better accuracy AND better trading-simulation metrics than non-relational benchmarks.
- **Relevance to our system:** Largely background — graph/cross-asset methods need MANY assets and rich relational + news data, which a single-coin advisor does not have. It does at least run a trading simulation (better than pure-accuracy papers), but the abstract doesn't confirm transaction-cost/slippage handling, and relational graphs over correlated equities are an overfitting and look-ahead-leakage hazard (the graph can encode future-correlated structure). Filed as a "if we ever go multi-coin, cross-asset relations might add signal, but mind leakage and costs" reference. Not actionable now.

### [12] Deep Reinforcement Learning for Trading — A Critical Survey
- **Authors / Venue:** Adrian Millea / Data (MDPI) 6(11):119
- **Year:** 2021
- **Source:** DOI:10.3390/data6110119 (arXiv-adjacent; MDPI open access)
- **% read:** 40%
- **Summary:** A deliberately *critical* survey of DRL applied to (mostly crypto) trading. Its central message is structural rather than celebratory: the community suffers from a "lack of consistency" — incomparable datasets, benchmarks, reward functions, state representations and risk measures — that "can significantly impede research and the development of DRL agents." Surveys promising directions (hierarchical RL to decompose the problem, model-based RL learning a world model, risk measures as reward-shaping) but pointedly does NOT claim DRL reliably beats baselines, leaving that an open question.
- **Relevance to our system:** Strong meta-support for our design philosophy. An expert survey concludes the DRL-trading field can't even compare results because everyone uses different data/metrics — precisely the problem our FROZEN gate (one bootstrap, one weakest-link verdict, buy-and-hold benchmark, fixed costs) is engineered to avoid. The author's emphasis on risk measures as reward-shaping echoes deep hedging [2]. Takeaway: don't chase DRL state-of-the-art; the field's own critical survey says there isn't a reproducible, baseline-beating standard to chase. Reinforces "stay simple, benchmark honestly."

### [13] Deep Reinforcement Learning for Automated Stock Trading: An Ensemble Strategy
- **Authors / Venue:** Hongyang Yang, Xiao-Yang Liu, Shan Zhong, Anwar Walid / ACM ICAIF 2020
- **Year:** 2020
- **Source:** arXiv:2511.12120 (mirror of ICAIF '20; also SSRN 3690996)
- **% read:** 50%
- **Summary:** The widely-cited FinRL ensemble paper. Trains three actor-critic agents (PPO, A2C, DDPG), then at each rebalancing picks the agent with the best recent validation Sharpe (rolling 3-month window), forming an ensemble that "adjusts to different market situations"; uses a turbulence index to force risk-off in crises. On 30 Dow stocks it reports ensemble Sharpe ≈ 1.12 vs A2C 1.10 / PPO 1.10 / DDPG 0.87, beating the DJIA and a min-variance portfolio.
- **Relevance to our system:** Cautionary, with one genuinely good idea. The good idea = the *validation-Sharpe agent-selection* and *turbulence risk-off* are exactly the kind of regime-adaptive switching logic worth knowing. BUT: it benchmarks against DJIA/min-variance, NOT a clean buy-and-hold-each-asset, costs handling is unclear, and a single ~2-3 year out-of-sample window gives a Sharpe with huge error bars — far from our 1000-path bootstrap weakest-link standard. A Sharpe of 1.1 on one path is not robust evidence. Reinforces: our gate would not crown this; the ensemble-selection idea is reusable, the performance claim is not trustworthy as stated.

### [14] Generative AI for End-to-End Limit Order Book Modelling (Token-Level Autoregressive Message-Flow Model)
- **Authors / Venue:** Peer Nagy, Sascha Frey, Silvia Sapora, Kang Li, Anisoara Calinescu, Stefan Zohren, Jakob Foerster / ACM ICAIF 2023
- **Year:** 2023
- **Source:** arXiv:2309.00638
- **% read:** 40%
- **Summary:** Treats the limit-order-book message stream like language: tokenizes order messages (digit-group tokens, LLM-style) and trains an autoregressive deep *state-space* network (S4-style) to generate the next message; a JAX-LOB simulator replays generated messages to evolve the book state end-to-end. Reports low out-of-sample perplexity and mid-price returns from generated order flow that significantly correlate with real data.
- **Relevance to our system:** Background for us specifically (it's high-frequency message-level; we trade spot bars), but it's an important data point in the "generative market simulator" lane I'm tracking for test data. The transferable insight: an autoregressive / sequence-model generator can produce realistic conditional market dynamics, and state-space models are an efficient backbone. For a single-coin bar-level advisor, a much simpler return-level generator (QuantGAN [9] or a diffusion model) is the right scale — this paper shows the high end of the same idea. Flag: confirms the field is moving from GANs toward autoregressive/diffusion simulators (more stable training).

### [15] Enhancing Time Series Momentum Strategies Using Deep Neural Networks (Deep Momentum Networks)
- **Authors / Venue:** Bryan Lim, Stefan Zohren, Stephen Roberts / Journal of Financial Data Science
- **Year:** 2019
- **Source:** arXiv:1904.04912
- **% read:** 75%
- **Summary:** Embeds deep learning into the classic time-series-momentum (TSMOM) volatility-scaling framework. A single LSTM jointly learns BOTH the trend signal AND the position size, trained by directly optimizing a differentiable **Sharpe-ratio loss** (decision, not forecast). Backtested on 88 continuous futures, the Sharpe-optimized LSTM more than DOUBLES traditional TSMOM Sharpe *with zero costs*. Crucially, they add a turnover-regularization term and show the edge persists only up to ~2–3 bps of transaction cost; beyond that the advantage erodes because the model trades a lot.
- **Relevance to our system:** One of the most relevant and *methodologically honest* papers here. (1) The "directly optimize Sharpe as the loss" idea is the forecasting-world version of deep hedging's risk-measure objective [2] — a clean template if we ever learn a sizing rule. (2) Its explicit cost-sensitivity curve is the discipline our literature mostly lacks and that our cost-net gate enforces: the headline 2x shrinks fast once you pay 2-3 bps, and crypto spot costs (spread + fees + slippage) typically EXCEED that. So even a well-built deep momentum model likely fails our buy-and-hold-net-of-costs bar on a single coin. Strong, concrete support for our thesis: the edge lives in the cost assumption. Also: turnover regularization is a transferable trick to keep any learned policy from overtrading.

### [16] Generation of Synthetic Financial Time Series by Diffusion Models
- **Authors / Venue:** Tomonori Takahashi, Takayuki Mizuno / Quantitative Finance (2025); arXiv 2024
- **Year:** 2024
- **Source:** arXiv:2410.18897
- **% read:** 50%
- **Summary:** Applies denoising diffusion probabilistic models (DDPMs) to generate synthetic financial series. Pipeline: wavelet-transform multiple series (price, volume, spread) into image representations, train a DDPM on the images, then inverse-wavelet back to time series. Reports that the generated data satisfy the key stylized facts (fat tails, volatility clustering, seasonality). Frames the contribution against GANs/VAEs, noting that "no model yet satisfies ALL the stylized facts" and positioning diffusion as a more stable, mode-collapse-resistant alternative.
- **Relevance to our system:** Directly relevant to our synthetic-test-data interest and a likely BETTER choice than a GAN for us. Diffusion models avoid the adversarial training instability and mode collapse that plague GANs like QuantGAN [9], while still reproducing stylized facts — exactly the properties we'd want from a generator that augments the moving-block bootstrap with novel-but-plausible regimes. The wavelet-to-image trick also jointly models price+volume+spread, which could give richer multi-feature synthetic paths. Caveats remain (validate stylized-fact match to OUR coin; the honest admission that no generator nails every stylized fact; guard against test-set leakage). Flag: top candidate generator to prototype for enriching test data.

### [17] (Re-)Imag(in)ing Price Trends
- **Authors / Venue:** Jingwen Jiang, Bryan T. Kelly, Dacheng Xiu / Journal of Finance 78(6):3193–3249
- **Year:** 2021 (SSRN) / 2023 (journal)
- **Source:** SSRN:3756587 (Journal of Finance, DOI:10.1111/jofi.13268)
- **% read:** 40%
- **Summary:** Renders each stock's recent OHLC + volume + moving-average history as a small candlestick *image*, then trains a CNN to classify the direction of future returns. The learned image patterns predict returns more accurately than hand-crafted trend/momentum signals, generate significant long-short portfolio alpha, and — striking — transfer across horizons (short-window patterns work at longer scales) and across markets (US-trained patterns work internationally). Published in a top finance journal (rigorous by the field's standards).
- **Relevance to our system:** Background with caveats. Two reasons it doesn't translate to our advisor: (1) the alpha is realized via a *long-short* portfolio across the cross-section of stocks — a single long-only coin can't take the short leg, and most of the documented edge is in shorting the predicted losers; (2) the headline results are not clearly net of realistic transaction costs, and long-short equity costs are far below crypto spot costs. The *method* (chart-as-image CNN) is elegant and the cross-market transfer is a genuinely robust finding, but for us it's a multi-asset, cost-sensitive technique. Interesting, not actionable for a single-coin paper advisor.

### [18] Deep Reinforcement Learning for Cryptocurrency Trading: Practical Approach to Address Backtest Overfitting
- **Authors / Venue:** Berend Gort, Xiao-Yang Liu, Xinghang Sun, Jiechao Gao, Shuaiyu Chen, Christina Dan Wang / arXiv (FinRL-related, AAAI Bridge 2023)
- **Year:** 2022
- **Source:** arXiv:2209.05559
- **% read:** 60%
- **Summary:** Confronts head-on that DRL-trading papers "optimistically report increased profits in backtesting [that] may suffer from false positives due to overfitting." Formulates overfitting detection as a hypothesis test: estimate the Probability of Backtest Overfitting (PBO, via combinatorially-symmetric cross-validation) for each trained agent and REJECT agents above a threshold before deployment. On 10 cryptos over a 2022 crash period, the less-overfitted (accepted) agents earned higher returns than more-overfitted agents, an equal-weight strategy, and the S&P crypto index.
- **Relevance to our system:** Very relevant — it's a kindred-spirit methodology. PBO / CSCV is a well-known López de Prado tool, and this paper operationalizes exactly the discipline our robustness gate embodies: assume backtest results are overfit until a statistical test says otherwise. We could consider adding a PBO-style overfitting probability as a *complementary* diagnostic alongside our moving-block bootstrap weakest-link verdict. Caveat to stay honest: their "beats equal-weight + index" claim rests on a SHORT 2-month out-of-sample window — directionally encouraging but not the multi-regime robustness we demand. Takeaway: adopt the *mindset and the PBO metric*; don't over-read the thin out-of-sample win.

### [19] Time-series Generative Adversarial Networks (TimeGAN)
- **Authors / Venue:** Jinsung Yoon, Daniel Jarrett, Mihaela van der Schaar / NeurIPS 2019
- **Year:** 2019
- **Source:** NeurIPS 2019 proceedings (paper 8789); arXiv-adjacent
- **% read:** 40%
- **Summary:** A general framework for generating realistic multivariate time series. Combines an autoencoder (embedding + recovery nets, so adversarial learning happens in a learned latent space) with a GAN (generator + discriminator) AND a *stepwise supervised loss* that explicitly rewards matching the real one-step-ahead transition distribution. This supervised term is the key innovation for preserving temporal dynamics. Evaluated on several datasets including stocks; introduces the now-standard discriminative-score and predictive-score (train-on-synthetic/test-on-real) metrics plus t-SNE/PCA overlap; reports better fidelity than prior GANs.
- **Relevance to our system:** Relevant as a generator option for synthetic test data, and as the source of the *evaluation protocol* we'd want regardless of generator choice: (1) discriminative score — can a classifier tell synthetic from real? and (2) predictive score / TSTR — does a model trained on synthetic transfer to real? These are exactly the validation gates we'd impose before trusting any generator (QuantGAN [9], diffusion [16], or TimeGAN) to augment our bootstrap. As for the generator itself, TimeGAN is general-purpose; the finance-specialized QuantGAN and the more-stable diffusion approach are likely better-suited, but TimeGAN's TSTR/discriminative-score evaluation is the durable contribution for us.

### [20] FinRL-Meta: Market Environments and Benchmarks for Data-Driven Financial Reinforcement Learning
- **Authors / Venue:** Xiao-Yang Liu, Ziyi Xia, Jingyang Rui, Jiechao Gao, Hongyang Yang, Ming Zhu, Christina Dan Wang, Zhaoran Wang, Jian Guo / NeurIPS 2022 Datasets & Benchmarks
- **Year:** 2022
- **Source:** arXiv:2211.03107
- **% read:** 40%
- **Summary:** Successor to FinRL focused on *data and benchmarks* for financial RL. Notably names the three core obstacles plainly: (1) the LOW signal-to-noise ratio of financial data, (2) SURVIVORSHIP BIAS in historical data, and (3) MODEL OVERFITTING in backtesting. Provides an automatic pipeline turning real-market data into hundreds of gym-style environments, reproduces popular papers as baselines, and uses cloud-hosted community competitions to expose over-optimized backtests via shared out-of-sample evaluation.
- **Relevance to our system:** Useful mainly because even the *FinRL ecosystem itself* — the group most invested in DRL trading — explicitly lists survivorship bias, low SNR, and backtest overfitting as the field's defining problems. That is third-party validation of our skeptical priors. Concretely, the "low signal-to-noise" framing is why a single-coin advisor should expect tiny, fragile edges, and the survivorship-bias warning matters if we ever expand the coin universe (only-surviving-coins backtests flatter active strategies). Background as software, but a strong citation for "the experts agree the failure modes are real."

### [21] Deep Learning for Bitcoin Price Direction Prediction: Models and Trading Strategies Empirically Compared
- **Authors / Venue:** Oluwadamilare Omole, David Enke / Financial Innovation 10(1) (Springer, open access)
- **Year:** 2024
- **Source:** DOI:10.1186/s40854-024-00643-1 (open mirror: scholarsmine.mst.edu/engman_syseng_facwork/1487)
- **% read:** 50%
- **Summary:** Compares CNN-LSTM, LSTNet, TCN and an ARIMA benchmark for Bitcoin price-DIRECTION prediction from on-chain features, with Boruta / genetic-algorithm / LightGBM feature selection. Best model (CNN-LSTM + Boruta) reaches 82.44% directional accuracy. Converting predictions into a long-AND-short trading strategy, they report an "extraordinary annual return of 6654%" and conclude this is "evidence of the potential profitability of predictive models in Bitcoin trading."
- **Relevance to our system:** A near-perfect CAUTIONARY case — exactly the kind of result our gate exists to reject. Red flags: (1) 82% directional accuracy on daily Bitcoin is implausibly high and screams look-ahead/feature leakage from on-chain data alignment; (2) a 6654% annual return is not a serious net-of-cost figure — at realistic crypto round-trip costs and the high turnover a daily directional signal implies, such a strategy bleeds out, and the paper's cost treatment is not credibly conservative; (3) it's long-AND-short (our advisor is long-only spot) and reports a single backtest path with no bootstrap/robustness test or buy-and-hold-over-same-window comparison stated. This is the literature's failure mode in one paper: high accuracy → astronomical headline return → "potential profitability," with the costs/leakage/robustness questions unaddressed. Strong reinforcement that our frozen, cost-net, buy-and-hold-benchmarked gate is necessary; treat such claims as null until independently reproduced.

### [23] Multi-level Deep Q-Networks for Bitcoin Trading Strategies
- **Authors / Venue:** Sattarov Otabek, Jaeyoung Choi / Scientific Reports 14 (Nature, open access)
- **Year:** 2024
- **Source:** DOI:10.1038/s41598-024-51408-w (open: PMC10774387)
- **% read:** 55%
- **Summary:** A DQN-based Bitcoin trader. Three stacked Q-networks: Trade-DQN (buy/hold/sell from price), Predictive-DQN (forecast price change from price + Twitter sentiment), and Main-DQN (fuses both for the final action). Reward balances profit, low risk (investment-threshold penalty), and trade activity; transaction fees modeled at a flat 1.5%. Reports a 29.93% increase in investment value and a Sharpe > 2.7, claimed to beat prior Bitcoin-trading studies.
- **Relevance to our system:** Directly on the DQN sub-area and crypto-specific, but a textbook example of why we don't trust such results. Critical flaws: (1) the OUT-OF-SAMPLE TEST IS ONLY THE FINAL 30 DAYS (720 hourly steps) — a Sharpe of 2.7 on one month is statistically meaningless (huge error bars), the antithesis of our 1000-path bootstrap weakest-link verdict; (2) NO buy-and-hold comparison over the same window is reported, so the headline gain may simply track Bitcoin's drift in that month; (3) single backtest path, no multiple seeds / walk-forward. To its modest credit it DOES model a (high) 1.5% fee and penalizes overtrading. Net: the M-DQN architecture is a reasonable engineering pattern, but the performance claim fails every robustness criterion our gate enforces. Confirms: short-window single-path Sharpe is the field's most common overstatement.

### [24] Empirical Asset Pricing via Machine Learning
- **Authors / Venue:** Shihao Gu, Bryan Kelly, Dacheng Xiu / Review of Financial Studies 33(5):2223–2273
- **Year:** 2018 (NBER w25398) / 2020 (journal)
- **Source:** NBER w25398; SSRN:3159577 (RFS, DOI:10.1093/rfs/hhaa009)
- **% read:** 50%
- **Summary:** The rigorous, large-scale benchmark for ML in (cross-sectional equity) asset pricing. Compares linear models, penalized regressions, random forests, GBRT, and neural networks on predicting monthly stock risk premia using ~94 firm characteristics. Trees and shallow neural nets win — roughly DOUBLING the out-of-sample Sharpe of regression-based strategies — and the gain is traced specifically to capturing NONLINEAR predictor interactions. All methods agree the dominant signals are variations on momentum, liquidity, and volatility. Crucially, the authors are explicit that absolute out-of-sample R² is TINY (≈0.3–0.4% monthly).
- **Relevance to our system:** The most credible "ML genuinely adds value" result in this whole batch — and it teaches the right lesson for us. (1) Where ML helps, it helps via *nonlinear interactions among MANY features across MANY assets in the cross-section* — precisely the setting a single-coin, few-feature advisor lacks; the edge is cross-sectional (rank assets), not time-series-directional on one coin. (2) Even in this favorable, data-rich setting the absolute predictability is minuscule (sub-1% R²); "doubling the Sharpe" is doubling a small number, realized only by a diversified long-short book over thousands of stocks. (3) The dominant signals (momentum/liquidity/volatility) are the SAME classic factors our simple strategies already encode. Bottom line: rigorous ML evidence says the genuine edge is cross-sectional and tiny — not a reason to add DL to a single-coin advisor, and consistent with "no active single-coin strategy robustly beats holding net of costs."

### [22] A Time Series is Worth 64 Words: Long-term Forecasting with Transformers (PatchTST)
- **Authors / Venue:** Yuqi Nie, Nam H. Nguyen, Phanwadee Sinthong, Jayant Kalagnanam (IBM/Princeton) / ICLR 2023
- **Year:** 2022 (arXiv) / 2023 (ICLR)
- **Source:** arXiv:2211.14730
- **% read:** 50%
- **Summary:** The Transformer "comeback" paper responding to DLinear [8]. Two ideas: (1) *patching* — split each series into subseries-level patches used as tokens (retains local semantics, cuts attention cost quadratically, lets the model see longer history); (2) *channel-independence* — each univariate channel shares one Transformer (no cross-channel attention). Adds masked self-supervised pretraining. Reports ~20% MSE reductions over prior transformers and, importantly, edges out the DLinear linear baseline on standard long-term forecasting benchmarks — restoring (a modest) case that a *properly designed* transformer can beat the trivial baseline.
- **Relevance to our system:** Background, but it refines the DLinear lesson rather than overturning it. The honest reading: simple linear models are a HARD baseline that most transformers fail to beat; only after careful design (patching + channel-independence + pretraining) does a transformer eke out a ~single-digit-percent forecasting-error edge — on physical/utility benchmarks, with no trading P&L or costs. For a single-coin advisor that's nowhere near worth the complexity/compute/overfitting cost. The durable takeaway pair [8]+[22]: (a) always beat the linear baseline first; (b) even when a fancy model wins, the margin is small and the metric is forecasting error, not cost-net return. Stay simple.

### [25] A Review of Reinforcement Learning in Financial Applications
- **Authors / Venue:** Yahui Bai, Yuhe Gao, Runzhe Wan, Sheng Zhang, Rui Song / arXiv (Computational Finance); Annual Review of Statistics and Its Application (under review)
- **Year:** 2024
- **Source:** arXiv:2411.12746
- **% read:** 40%
- **Summary:** A recent survey + meta-analysis of RL across financial decision tasks (portfolio management, optimal execution, option hedging, market making, order routing). It frames the field around three persistent obstacles: explainability (black-box decisions), MDP modeling (how to frame a financial problem as a well-posed MDP — state/action/reward design), and robustness (reliability across market conditions). Rather than claiming RL decisively beats benchmarks, it surveys *why* results are fragile and proposes future directions: standardized benchmarking, contextual RL, multi-agent RL, and model-based RL.
- **Relevance to our system:** Meta-support, consistent with our priors. A 2024 survey still lists benchmarking, robustness, and explainability as UNRESOLVED — i.e., four years after FinRL [1] the field still has no reproducible baseline-beating standard. The explicit call for "standardized benchmarking" is exactly what our frozen gate provides. Useful as an up-to-date citation that RL trading remains "great potential, underutilized in practice" rather than a solved, deployable edge. Background for our single-coin advisor (most RL framing is multi-asset portfolio), but reinforces staying skeptical.

### [26] Arbitrage-free Neural-SDE Market Models
- **Authors / Venue:** Samuel N. Cohen, Christoph Reisinger, Sheng Wang (Oxford) / arXiv q-fin.CP; later Applied Mathematical Finance
- **Year:** 2021
- **Source:** arXiv:2105.11053
- **% read:** 45%
- **Summary:** Builds a neural stochastic differential equation (neural SDE) model for the JOINT dynamics of a liquid European-options book. Neural networks parametrize the drift and diffusion coefficients of the SDE system; the key contribution is enforcing *no-static-arbitrage* by constraining the network outputs to satisfy a set of linear inequalities (so generated option-price surfaces remain arbitrage-free). The state space is constructed to be arbitrage-free by design, and the inference problem (learning the model from discrete time series of stock + option prices) is studied. Validated on synthetic data from a Heston stochastic-local-volatility model.
- **Relevance to our system:** Background for the options-specific machinery (we hold spot, not an options book), but two transferable ideas matter. (1) Neural SDEs are a principled *generative* model class for market dynamics — drift/diffusion learned by NNs — and a follow-on lane to GAN/diffusion generators [9][16][19] for synthetic test data; a spot-price neural SDE could in principle generate stylized-fact-preserving paths with a continuous-time inductive bias. (2) The discipline of baking *hard financial constraints* (no-arbitrage) into the generator architecture is a model for how we'd constrain any synthetic-data generator to stay economically plausible (e.g., bounded returns, positive prices, realistic vol clustering). Not deployable as-is; filed under "neural SDE generative simulators" to track.

### [27] Your Offline Policy is Not Trustworthy: Bilevel Reinforcement Learning for Sequential Portfolio Optimization
- **Authors / Venue:** Haochen Yuan, Minting Pan, Yunbo Wang, Siyu Gao, Philip S. Yu, Xiaokang Yang / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2505.12759
- **% read:** 45%
- **Summary:** Attacks the core failure of OFFLINE RL for trading: an agent trained on a fixed historical dataset "merely memorizes the optimal yet impractical buying/selling behaviors" within that dataset — i.e., it overfits non-repeating patterns and does not generalize under market non-stationarity. Proposes MetaTrader, a bilevel framework that trains simultaneously for in-domain performance (original data) AND out-of-domain robustness (diverse data transformations), plus a "robust TD estimation" that approximates worst-case TD targets from a batch of transformed transitions to fight value overestimation under limited offline data. Reports outperformance vs. prior RL + prediction models on public stock datasets.
- **Relevance to our system:** Conceptually relevant and unusually honest about offline-RL's central trap. The paper's own thesis — that an offline policy "is not trustworthy" because it memorizes a single historical path — is the RL restatement of our backtest-overfitting concern and a direct argument AGAINST naively training an RL agent on one coin's history. The bilevel "train on transformed/augmented data for worst-case robustness" idea rhymes with our bootstrap (perturb the data, demand robustness across perturbations). Caveats: transaction costs are not addressed in the abstract, and "beats prior RL" is again not a buy-and-hold-net-of-costs claim under a frozen multi-path gate. Takeaway: the framing validates our skepticism; the worst-case-over-data-transformations objective is a transferable robustness idea.

### [28] Reinforcement Learning-Based Cryptocurrency Portfolio Management Using Soft Actor–Critic and Deep Deterministic Policy Gradient Algorithms
- **Authors / Venue:** Kamal Paykan / arXiv (q-fin/cs.LG)
- **Year:** 2025
- **Source:** arXiv:2511.20678
- **% read:** 55%
- **Summary:** Compares SAC vs DDPG for continuous portfolio allocation across four cryptos (BTC, ETH, LTC, DOGE) on daily OHLCV with a 50-day rolling window; transaction costs are modeled (rate c subtracted from net return). Trained 2016–2022, tested 2023–2024. SAC reaches final value 2.76 (+176%) vs DDPG 1.97 (−3%) and Markowitz 2.04; SAC has higher Sharpe (0.067 vs 0.013) and lower max drawdown. Attributes SAC's robustness to its stochastic, entropy-regularized policy + double critics (less overestimation) vs DDPG's deterministic policy overfitting noisy markets.
- **Relevance to our system:** A clean, recent illustration of BOTH a useful sub-finding and our core methodological complaint. Useful: the SAC-over-DDPG result (entropy regularization + twin critics → more stable in volatile crypto) is a consistent, repeated lesson — if one ever uses continuous-action RL, SAC/TD3 beat vanilla DDPG for stability. The complaint: the paper benchmarks against Markowitz but OMITS buy-and-hold and equal-weight — a fatal omission for crypto where 2023–2024 BTC/ETH simply rose, so the +176% may be largely market drift, not skill. The reported Sharpe values are also tiny in absolute terms (0.067). Single 2-year out-of-sample path, no bootstrap. Net: confirms our rule that "beats Markowitz" or "+176%" means nothing without buy-and-hold over the same window — exactly the benchmark our gate hard-codes. Cautionary, with one reusable algorithm-choice nugget (prefer SAC over DDPG).

### [29] Deep Reinforcement Learning for Online Optimal Execution Strategies
- **Authors / Venue:** Alessandro Micheli, Mélodie Monod (Imperial College London) / arXiv (q-fin.TR)
- **Year:** 2024
- **Source:** arXiv:2410.13493
- **% read:** 45%
- **Summary:** Tackles optimal *execution* (splitting a large order to minimize market impact) in markets with TRANSIENT price impact — impact that decays via a convex kernel G(t−tₖ), which makes the problem non-Markovian and hard for standard RL. Proposes a DDPG actor-critic with a novel auxiliary Q-function formulation that reduces input dimensionality and "eliminates the need for strict assumptions about the market's price-impact model," enabling online adaptation as parameters drift. Validated on SYNTHETIC experiments only (exponential, power-law, linear-resilience kernels); converges to the optimal strategy across kernels and adapts online.
- **Relevance to our system:** Background — execution micro-optimization is orthogonal to a single-coin bar-level advisor that places at most a few trades over a window (we are not splitting large orders against a decaying impact kernel). Two notes worth keeping: (1) it's an example of RL done *rigorously* — synthetic ground-truth where the optimal answer is known, so "did the agent recover the optimum?" is a clean test (contrast with the crypto papers that have no ground truth). (2) Transient/decaying market impact is a real cost component; if we ever model slippage more richly than a flat bps, a decay-kernel impact model is the standard formalism. Not actionable now; filed under execution RL.

### [30] Optimizing Market Making using Multi-Agent Reinforcement Learning
- **Authors / Venue:** Yagna Patel / arXiv (q-fin.TR)
- **Year:** 2018
- **Source:** arXiv:1812.10252
- **% read:** 30%
- **Summary:** An early multi-agent RL market-making system with a two-tier decomposition: a *macro-agent* makes the directional decision (buy/sell/hold) and a *micro-agent* handles optimal limit-order placement within the order book. Demonstrated on Bitcoin market data as a proof of viability for RL on market-microstructure problems. (Abstract/metadata accessible; specific net-P&L, inventory-risk, and cost handling not surfaced in the available text.)
- **Relevance to our system:** Background. Market making (continuously quoting two-sided limit orders to earn the spread while managing inventory) is a fundamentally different activity from our long-only spot advisor — we are a *taker* deciding when to hold, not a *maker* earning the spread. The hierarchical macro/micro decomposition (decide direction, then decide placement) is a clean engineering pattern but irrelevant to a few-trades-per-window bar advisor. Filed for completeness in the LOB/market-making sub-area; not actionable.

### [31] Modelling Crypto Markets by Multi-Agent Reinforcement Learning
- **Authors / Venue:** Johann Lussange, Stefano Vrizzi, Stefano Palminteri, Boris Gutkin / arXiv (q-fin/cs.MA)
- **Year:** 2024
- **Source:** arXiv:2402.10803
- **% read:** 35%
- **Summary:** An agent-based MARL *market simulator* for crypto. Instead of zero-intelligence agents, each agent is an RL learner that values assets from two information sources — observed market prices and an approximation of the asset's fundamental value — producing bottom-up individual and collective dynamics. Calibrated to real Binance data across 153 cryptocurrencies (2018–2022), it claims to emulate crypto-market microstructure and reproduce both bearish and bullish regimes, including robustness through the COVID-19 period.
- **Relevance to our system:** Interesting for the synthetic-test-data lane, but with a different flavor than GAN/diffusion generators [9][16][19]. This is a *structural / agent-based* simulator: dynamics emerge from interacting learning agents rather than being fit to match a return distribution directly. Potential upside — agent-based sims can in principle produce genuinely NOVEL regimes (the structure, not the historical path, is the prior), which is exactly the gap our moving-block bootstrap has. Potential downside — calibration is hard, validation against precise stylized facts isn't shown in the abstract, and an ABM is far heavier to build/validate than a diffusion model. Filed as a "generative market simulator" alternative to track; the diffusion route [16] remains the more practical first prototype for us.

### [32] Autoencoder Asset Pricing Models
- **Authors / Venue:** Shihao Gu, Bryan T. Kelly, Dacheng Xiu / Journal of Econometrics 222(1):429–450
- **Year:** 2019 (SSRN) / 2021 (journal)
- **Source:** SSRN:3335536 (Journal of Econometrics, DOI:10.1016/j.jeconom.2020.07.009)
- **% read:** 45%
- **Summary:** Retrofits an autoencoder (an unsupervised neural dimension-reduction device) into a *conditional latent-factor* asset-pricing model. Like Kelly-Pruitt-Su (2019), latent factors and factor exposures (betas) depend on observable asset characteristics — but unlike KPS's linearity, the autoencoder makes the exposures a flexible NONLINEAR function of the characteristics. The architecture bakes in the no-arbitrage economic restriction. Reports out-of-sample pricing errors far smaller (and generally statistically insignificant) than leading linear factor models.
- **Relevance to our system:** Background for us, but it's the rigorous "autoencoders for factors" reference and teaches the same lesson as Gu-Kelly-Xiu [24]: the genuine ML edge in finance is CROSS-SECTIONAL (modeling the joint structure of many assets via latent factors and characteristic-conditioned betas), not time-series-directional on one coin. An autoencoder needs a panel of many assets × many characteristics to estimate latent factors; a single coin has no cross-section to compress. The transferable ideas are conceptual: (1) autoencoders as a principled way to learn a low-dimensional latent representation, and (2) embedding economic constraints (no-arbitrage) into the network. Neither is actionable for a single-coin advisor; reinforces "ML's real value is cross-sectional, and we don't have a cross-section."

### [33] Temporal Relational Ranking for Stock Prediction (Relational Stock Ranking / RSR)
- **Authors / Venue:** Fuli Feng, Xiangnan He, Xiang Wang, Cheng Luo, Yiqun Liu, Tat-Seng Chua / ACM Transactions on Information Systems 37(2)
- **Year:** 2018 (arXiv) / 2019 (journal)
- **Source:** arXiv:1809.09441
- **% read:** 40%
- **Summary:** A foundational graph-based stock-prediction paper. Two ideas: (1) reframe prediction as a RANKING task (rank stocks by expected return) rather than classification/regression, to align with the actual investment goal of picking the best names; (2) a "Temporal Graph Convolution" component that jointly models temporal price evolution AND a relation network of stocks (sector membership, supplier–customer links). Back-tested on NYSE/NASDAQ; reports average return ratios of 98% (NYSE) / 71% (NASDAQ) and claims to beat prior stock-prediction methods.
- **Relevance to our system:** Background, and a useful conceptual contrast. The ranking reformulation is genuinely smart — but it is inherently CROSS-SECTIONAL (you can only rank when you have many assets), so it does not apply to a single coin. The headline "98% return" is reported without a clearly stated transaction-cost model or a buy-and-hold-of-the-index benchmark over the same window, so by our standards it's an upper bound, not deployable performance. Relational graphs over equities also risk encoding future-correlated structure (leakage) — see the critique [34]. Filed as a cross-asset/ranking reference for any future multi-coin extension; not actionable for one coin, and the performance claim is untrustworthy as stated.

### [34] Evaluating Financial Relational Graphs: Interpretation Before Prediction
- **Authors / Venue:** Yingjie Niu, Lanxin Lu, Rian Dolphin, Valerio Poti, Ruihai Dong / arXiv (q-fin/cs.LG)
- **Year:** 2024
- **Source:** arXiv:2410.07216
- **% read:** 45%
- **Summary:** A skeptical methodology paper about the GNN-stock-prediction literature [11][33]. Its central critique: existing work builds relationship graphs from predefined static factors and then judges the GRAPH solely by the downstream prediction accuracy of the NN — "leading to confusion and imprecision," because a graph can look good merely because the network is good, obscuring whether the graph structure genuinely matters. Proposes "interpretation before prediction": evaluate a relationship graph INDEPENDENTLY (does it explain historical financial phenomena?) before plugging it into a predictor, plus dynamic graphs that capture changing relationships.
- **Relevance to our system:** Strongly aligned with our skeptical mandate, even though graphs themselves don't apply to a single coin. The deeper lesson generalizes to ANY engineered input/feature: do NOT validate a component by the end-to-end model's accuracy alone — a complex model can mask a useless or leaky feature. This is the feature-level analogue of our no-op-overlay discovery discipline (the project's hard rule that an overlay must demonstrably change equity, not just compute a number). It's also indirect evidence that much of the GNN-stock literature's claimed edge may rest on graphs that aren't actually informative. Takeaway for us: ablate and independently justify every input; "the model did better with feature X" is not proof X carries real signal.

### [35] A Word is Worth A Thousand Dollars: Adversarial Attack on Tweets Fools Stock Prediction
- **Authors / Venue:** Yong Xie, Dakuo Wang, Pin-Yu Chen, Jinjun Xiong, Sijia Liu, Oluwasanmi Koyejo / NAACL 2022 (pp. 587–599)
- **Year:** 2022
- **Source:** arXiv:2205.01094
- **% read:** 40%
- **Summary:** The first probe of adversarial vulnerability in social-media-driven stock predictors. Many models ingest Twitter/Reddit text as a sentiment signal; the authors craft adversarial attacks via combinatorial optimization under semantic-similarity and budget constraints, then simply CONCATENATE a perturbed-but-semantically-similar tweet to fool three victim stock-prediction models. They achieve consistent attack success rates and cause significant simulated monetary loss — by changing only a few words in input text.
- **Relevance to our system:** Background as an attack method (we use no social-media text inputs), but a sharp cautionary lesson for the whole "alternative-data / sentiment / LLM-feature" lane. If a model's signal can be flipped by a semantically-trivial text perturbation, that signal is dangerously brittle and — critically for crypto — an EXPLOITABLE attack surface, since crypto social feeds are heavily manipulated (shills, bots, pump posts). This is a strong argument AGAINST wiring noisy social-sentiment features into a trading decision and a reminder that adversarial/non-stationary inputs can silently break a learned model. Reinforces preferring robust price/volume features over manipulable text. Filed under adversarial robustness; relevant to the cross-topic LLM/sentiment discussion.

### [36] Deep Hedging of Derivatives Using Reinforcement Learning
- **Authors / Venue:** Jay Cao, Jacky Chen, John Hull, Zissis Poulos / Journal of Financial Data Science 3(1), Winter 2021
- **Year:** 2021
- **Source:** arXiv:2103.16409
- **% read:** 50%
- **Summary:** A deep-hedging extension (building on Bühler et al. [2]) that uses RL to learn option-hedging policies UNDER transaction costs. Key trick: track TWO Q-functions — one for the expected hedging cost and one for the expected SQUARED cost — so the agent can optimize a mean-variance objective (expected cost + λ·std-dev), making it risk-aware rather than just cost-minimizing. Finds the learned policy substantially beats classical delta hedging once costs are material (it rehedges less, trading off tracking error against cost). Notably, a hybrid "accounting-P&L" approach with a simple valuation model works well even when that model is mis-specified vs. the true price process.
- **Relevance to our system:** Background for the options application, but the dual-Q mean-variance idea is a clean, transferable template. The lesson that mirrors our thesis: WITH transaction costs, the optimal policy trades LESS — costs push the optimal action toward inaction (here, fewer rehedges; for us, toward holding). That is the same force that makes buy-and-hold hard to beat net of costs. The two-Q-function construction (track mean and variance of cost separately to get a risk-adjusted objective) is a neat pattern if we ever learn a risk-aware sizing/rebalancing policy. Reinforces: costs are the dominant constraint and the right objective is risk-adjusted, not return-maximizing.

### [37] Leakage and the Reproducibility Crisis in ML-based Science
- **Authors / Venue:** Sayash Kapoor, Arvind Narayanan (Princeton) / Patterns (Cell Press) 4(9):100804
- **Year:** 2022 (arXiv) / 2023 (journal)
- **Source:** arXiv:2207.07048
- **% read:** 50%
- **Summary:** The landmark cross-disciplinary study of data leakage as a driver of irreproducible ML research. Surveys 17 scientific fields and finds leakage-induced errors affecting 329 papers, repeatedly producing "wildly overoptimistic" results. Contributes a fine-grained taxonomy of 8 leakage types (from textbook train/test contamination to subtle, open-research-problem forms like temporal leakage and non-independence between train and test sets) and proposes "model info sheets" — a standardized reporting template that forces authors to argue, per leakage type, why their evaluation is clean.
- **Relevance to our system:** Highly relevant as a discipline document, even though it's domain-general. Financial backtesting is a leakage minefield — look-ahead bias, using same-bar information, centered/normalized features computed over the full sample, survivorship, and non-stationarity that violates train/test independence (the temporal-leakage type is exactly our risk). The "model info sheet" idea — a checklist that forces an explicit no-leakage argument before trusting a result — is directly adoptable as a lightweight gate for any future learned component in our advisor: before a model's backtest counts, fill in why each of the 8 leakage types is excluded. This is the methodological backbone behind why our frozen, out-of-sample, cost-net gate exists. Strong supporting citation for our test-data discipline.

### [38] Time-Causal VAE: Robust Financial Time Series Generator
- **Authors / Venue:** Beatrice Acciaio, Stephan Eckstein, Songyan Hou / arXiv (q-fin/math.PR)
- **Year:** 2024
- **Source:** arXiv:2411.02947
- **% read:** 40%
- **Summary:** A variational-autoencoder generator (TC-VAE) for financial time series that enforces a CAUSALITY constraint on both encoder and decoder, so the map from real to generated series is a "causal transport" (information only flows forward in time, respecting the filtration). The authors prove the TC-VAE loss upper-bounds the *causal Wasserstein distance*, which controls how much downstream dynamic stochastic-optimization values can differ between real and synthetic data — a theoretically grounded fidelity guarantee. Generated data reproduce stylized facts of real markets.
- **Relevance to our system:** Relevant to the synthetic-test-data lane and a notable theoretical advance over GAN/diffusion generators [9][16][19]: the causality constraint is precisely what you want for a TRADING test set — a generator that respects time-ordering won't accidentally bake in look-ahead structure, and the causal-Wasserstein bound means strategies optimized on synthetic data behave similarly on real data (directly relevant to using synthetic paths to stress our gate). VAEs are also more stable to train than GANs. Caveat: still needs validation that it matches OUR coin's stylized facts and doesn't leak the held-out path. Filed alongside diffusion [16] as a top-tier generator candidate — arguably the most theoretically appropriate one for trading-strategy stress-testing because of the causal-transport property.

### [39] Spurious Predictability in Financial Machine Learning
- **Authors / Venue:** Sotirios D. Nikolopoulos (University of Peloponnese) / arXiv (q-fin/stat)
- **Year:** 2026
- **Source:** arXiv:2604.15531
- **% read:** 55%
- **Summary:** A rigorous skeptical framework arguing that adaptive specification search produces apparently-significant backtests even when true returns are a martingale difference (ZERO predictability). Three mechanisms combine: data leakage (manufactures signal), selection bias (picking the best of many candidates), and specification error (misreading the inflated stats as alpha). Shows in-sample evidence under the null scales as √(log K_eff) in the effective number of independent trials, while walk-forward evidence stays null-bounded — so the IS/OOS divergence is itself the tell. Highly-correlated strategy families compress effective multiplicity (500 near-identical candidates ≈ 5 independent ones) yet still inflate IS performance. Proposes a two-stage audit: first falsify the pipeline against structural nulls (white noise, regime-switching vol, bid-ask bounce, zero-alpha factors), then quantify selection inflation via a "Backtest Inflation Factor."
- **Relevance to our system:** Directly, powerfully relevant — this is one of the most important papers for our app's philosophy. (1) The FALSIFICATION-against-structural-nulls idea is something we should literally adopt: run our entire bake-off + ranking pipeline on synthetic NULL data (white noise / GARCH with zero drift edge / bid-ask-bounce) and confirm it crowns NOTHING — if any "winning" strategy emerges from pure noise, our pipeline has a leak or selection bug. This is a concrete, high-value test to add. (2) It explains WHY a large bake-off needs a multiplicity correction: testing many strategies guarantees a lucky in-sample winner under the null; our 1000-path bootstrap weakest-link verdict + buy-and-hold benchmark is exactly the OOS discipline that defeats this, but the paper warns even walk-forward isn't enough if timing/leakage is wrong. (3) The "factor exposure masquerading as alpha" point maps to "a strategy that just tracks the coin's drift is not alpha — benchmark vs buy-and-hold." Strongest support yet for our gate, and a clear actionable: add a null-data falsification test.

### [40] TLOB: A Novel Transformer Model with Dual Attention for Price Trend Prediction with Limit Order Book Data
- **Authors / Venue:** Leonardo Berti, Gjergji Kasneci (TU Munich) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2502.15757
- **% read:** 55%
- **Summary:** A modern DeepLOB [3] successor: a dual-attention Transformer (temporal attention across LOB snapshots + spatial attention across price/volume features), with MLP-based feed-forward blocks and bilinear normalization for non-stationarity. On FI-2010 it reaches 92.8% F1 (vs ~92.1% prior SoTA, 77.6% DeepLOB) and improves on Tesla/Intel and Bitcoin datasets, with bigger gains at longer horizons. Crucially HONEST: it shows older Finnish 2010 stocks are far easier to predict than 2015 NASDAQ stocks, and Bitcoin (2023) is hardest of all — and that redefining the trend threshold to the *average spread* (a tradability-realistic bar) sharply degrades performance, explicitly naming "the critical gap between academic performance metrics and practical trading applicability."
- **Relevance to our system:** Background as an architecture (we're not on L2 LOB tape), but methodologically it is one of the GOOD-CITIZEN papers and reinforces two of our core beliefs with hard evidence. (1) The accuracy→profit gap is real and the authors prove it: once the prediction threshold is set to the bid-ask spread (i.e., only count moves big enough to actually trade against costs), the impressive F1 collapses — this is the LOB-microstructure version of "edge dies once you pay the spread," exactly what our cost-net gate enforces. (2) Benchmark difficulty is non-stationary and venue/era-dependent: 2010 Finnish data is "easy," Bitcoin is "hard" — a direct warning that crypto is the hardest setting and that results on stale benchmarks overstate real-world edge. Confirms staying skeptical of high-accuracy LOB classifiers and that crypto specifically resists prediction.

### [41] Can GANs Learn the Stylized Facts of Financial Time Series?
- **Authors / Venue:** Sohyeon Kwon, Yongjae Lee (UNIST) / ACM ICAIF 2024
- **Year:** 2024
- **Source:** arXiv:2410.09850
- **% read:** 45%
- **Summary:** A careful evaluation paper asking whether GAN generators actually reproduce financial stylized facts — random-walk behavior, mean reversion, jumps, time-varying volatility — for univariate and multivariate series. Finding: GANs CAN capture various stylized facts, but performance "varies significantly depending on the choice of generator architecture," and "naively applying GANs might not effectively capture the intricate characteristics" of financial data. The contribution is the skeptical, validation-first stance: fidelity is architecture-sensitive and must be checked, not assumed.
- **Relevance to our system:** Directly relevant to the synthetic-test-data lane and an important counterweight to the optimism of QuantGAN [9] / TimeGAN [19]. It says plainly what we must internalize before trusting ANY generator: stylized-fact fidelity is fragile and architecture-dependent, so a generator that looks fine on one metric can silently miss jumps or vol-clustering. This validates our hard guardrail — before a synthetic generator augments the bootstrap, it MUST be quantitatively validated against OUR coin's specific stylized facts (fat tails, vol clustering, jumps), per-fact, not taken on faith. It also nudges us toward the more-stable, theoretically-grounded alternatives (diffusion [16], time-causal VAE [38]) over vanilla GANs. Strong support for "validate the generator hard, or don't use it."

### [42] Deep Reinforcement Learning Framework for Diversified Portfolio Management Across Global Equity Markets
- **Authors / Venue:** Kamil Kashif, Robert Ślepaczuk (University of Warsaw) / arXiv (q-fin.PM)
- **Year:** 2026
- **Source:** arXiv:2605.17307
- **% read:** 55%
- **Summary:** A rigorously-designed SAC portfolio study and — crucially — a near-NEGATIVE result. Learns continuous portfolio weights with transaction costs (2 bps/turnover), turnover penalties, and diversification constraints baked into the reward; compares five configs (LSTM vs Transformer encoders, flat vs hierarchical policy, different rewards). Evaluation is strong: 16 out-of-sample walk-forward folds spanning 2003–2026 across Nasdaq-100, Nikkei 225, and Euro Stoxx 50 (5y train / 1y val / 1y test, rolling retrain). Headline finding: "no strategy achieves statistically significant excess returns relative to Buy & Hold under HAC-robust inference across all markets." Only Euro Stoxx 50 shows significant abnormal returns; RL adds value mainly in high-uncertainty regimes; ensembling across markets helps diversification.
- **Relevance to our system:** One of the STRONGEST direct corroborations of our validated thesis in the entire library. This is DRL done about as honestly as the field gets — real costs, 16 OOS folds over 23 years, multiple markets, and (the rare part) HAC-robust statistical inference on the excess return vs. buy-and-hold — and the conclusion is essentially "RL does NOT robustly beat buy-and-hold." That is our thesis, reproduced by RL proponents on equities. Two transferable points: (1) the use of HAC-robust statistical inference on the Sharpe/excess-return is a rigor we could echo (our bootstrap weakest-link is a kindred multi-path test); (2) "RL adds value mainly in elevated-uncertainty regimes" is a testable hypothesis but a thin, regime-conditional edge, not a general one. Definitive citation that even well-built DRL portfolios fail to beat passive holding under honest statistics.

### [43] Diffusion-Augmented Reinforcement Learning for Robust Portfolio Optimization under Stress Scenarios
- **Authors / Venue:** Himanshu Choudhary, Arishi Orra, Manoj Thakur (IIT Mandi) / arXiv (q-fin/cs.LG)
- **Year:** 2025
- **Source:** arXiv:2510.07099
- **% read:** 50%
- **Summary:** Combines the two DL lanes that matter for us: a denoising diffusion model (DDPM) generates synthetic market-CRASH sequences conditioned on a "stress intensity" knob, and those synthetic stress scenarios augment the training data of a PPO portfolio agent so it learns tail-risk-resilient policies without needing real crises to have occurred. Reports 59.5% cumulative return (vs 17.9% index), Sharpe 1.91 (vs 0.77), and lower max drawdown (−15.8% vs −20.3% without augmentation), plus faster recovery during an unseen 2025 stress episode.
- **Relevance to our system:** The most directly inspiring paper for a concrete experiment we could run. The core idea — use a generative model to synthesize STRESS/CRASH scenarios that the historical path lacks, then stress-test (or train) against them — is exactly the gap in our moving-block bootstrap (which can only reshuffle realized returns and never invents a worse-than-observed crash). We could adopt the *conditional-stress generator* idea purely for TESTING: generate stress paths at varying intensities and require a crowned strategy to survive them, hardening our robustness gate. Skeptical caveats: the performance numbers compare to a passive index (not labeled clearly as buy-and-hold) on a single recent window and an ablation, not a 1000-path multi-regime gate, so treat the headline returns cautiously. But the *method-as-test-data-generator* is high value and aligns with diffusion [16] and DARL's own stress-conditioning. Top idea to track for hardening our gate with synthetic tail scenarios.

### [44] Towards Realistic Market Simulations: a Generative Adversarial Networks Approach
- **Authors / Venue:** Andrea Coletta, Matteo Prata, et al. (Sapienza Rome) + Aymeric Moulin, Svitlana Vyetrenko, Tucker Balch (J.P. Morgan AI Research) / ACM ICAIF 2021
- **Year:** 2021
- **Source:** arXiv:2110.13287
- **% read:** 50%
- **Summary:** A Conditional GAN that acts as a "world agent" — instead of modeling individual traders, it generates the AGGREGATE order flow conditioned on the current market state (order-book snapshots, recent orders), producing limit orders that react dynamically to an experimental agent placed in the simulation. Validated two ways: distributional match (price/volume/direction/inter-arrival vs. historical) and four canonical stylized facts (no return autocorrelation, volatility clustering, volume–volatility correlation, heavy tails) on TSLA data, outperforming hand-crafted agent pools. Honest about limitations: struggles to match the volume–volatility correlation, volume matching is "intrinsically hard," needs ~4 days on a V100 for 2 days of data, and responsiveness to diverse agent strategies is under-explored.
- **Relevance to our system:** Relevant to the synthetic-test-data lane, and valuable precisely because of its CANDOR about failure modes. Two takeaways: (1) a market simulator that REACTS to the agent (endogenous response / market impact) is the gold standard for testing — our current bootstrap and most generators replay an exogenous path that ignores our trades, which flatters any strategy big enough to move the market (not our concern at €200, but conceptually important). (2) The explicit list of where the GAN falls short (volume–volatility correlation, training cost, generalization) is a realistic expectations-setter: even a J.P. Morgan-backed GAN doesn't nail every stylized fact, echoing [41]. For a €200 single-coin advisor a reactive simulator is overkill; the durable lesson is the validation rigor (per-stylized-fact + distributional) and the honesty about limits. Background-plus; reinforces "validate generators hard, expect gaps."

### [45] Attention-Enhanced Reinforcement Learning for Dynamic Portfolio Optimization
- **Authors / Venue:** (Listed on arXiv) / arXiv (q-fin.PM); also ScienceDirect peer-reviewed
- **Year:** 2025
- **Source:** arXiv:2510.06466
- **% read:** 40%
- **Summary:** A DRL portfolio framework combining a DIRICHLET policy (outputs portfolio weights that are always feasible — non-negative, sum to one — handling tradability constraints naturally and exploring the simplex stably) with cross-sectional ATTENTION (per-asset temporal encoders + a global attention layer to capture sector relationships and cross-asset spillovers). Reward includes transaction costs and a portfolio-variance penalty, tying the objective to mean-variance trade-offs. Reports that the attention-Dirichlet policy beats equal-weight and standard RL benchmarks on terminal wealth and Sharpe while keeping turnover/drawdown realistic.
- **Relevance to our system:** Mostly background (multi-asset, cross-sectional attention is meaningless for one coin), but two reusable design nuggets. (1) The DIRICHLET policy parametrization is a genuinely nice trick for ANY weight-allocation problem: it guarantees valid, normalized weights by construction — if we ever output a continuous "fraction of the €200 to deploy" it's a clean way to keep the action in [0,1] and explore stably. (2) The reward = return − cost − variance-penalty structure is the recurring "right" objective (risk-adjusted, cost-aware), consistent with [2][15][36]. The headline "beats equal-weight + standard RL" is, as always, not a buy-and-hold-net-of-costs claim under a multi-path gate — note equal-weight ≠ buy-and-hold-the-best-asset. Filed as a design-pattern reference (Dirichlet sizing); performance claim untrusted as stated.

### [46] Generative Modelling of Financial Time Series with Structured Noise and MMD-based Signature Learning
- **Authors / Venue:** Chung I Lu, Julian Sester (NUS) / Statistics & Risk Modeling (2025); arXiv 2024
- **Source:** arXiv:2407.19848
- **Year:** 2024
- **% read:** 45%
- **Summary:** A generative model for financial time series trained by minimizing Maximum Mean Discrepancy (MMD) under a SIGNATURE KERNEL — the path-signature transform captures complex temporal/path-dependent structure, giving a principled distance between real and generated paths (no adversarial discriminator). Adds STRUCTURED NOISE: a moving-average model on the noise variance to reproduce volatility clustering. On S&P 500 data it reproduces stylized facts and "outperforms a comparable GAN-based approach"; the authors also use the synthetic data to train RL portfolio agents and propose adjusting the noise for different regimes with minimal retraining.
- **Relevance to our system:** A strong addition to the synthetic-test-data toolkit and arguably the most appealing generator family for us alongside diffusion [16] / time-causal VAE [38]. Why: MMD-with-signature-kernel training is NON-adversarial (no GAN instability/mode-collapse — addresses the [41] fragility complaint head-on) and the signature transform is purpose-built for path-dependent dynamics, which is exactly what return paths are. The structured-noise-for-vol-clustering idea is a clean, interpretable lever, and the "tweak noise to get different regimes" property is precisely the regime-enrichment we want to stress our bootstrap. Has public code (GitHub). Skeptical note: still validate stylized-fact match to our coin; "beats a GAN" is a fidelity claim, not a trading-edge claim. Top-three generator candidate to track.

### [47] Deep Reinforcement Learning that Matters
- **Authors / Venue:** Peter Henderson, Riashat Islam, Philip Bachman, Joelle Pineau, Doina Precup, David Meger (McGill) / AAAI 2018
- **Year:** 2017 (arXiv) / 2018 (AAAI)
- **Source:** arXiv:1709.06560
- **% read:** 35%
- **Summary:** The landmark reproducibility critique of deep RL (general, not finance). Shows that reported results are dominated by non-determinism + intrinsic method variance: holding everything fixed and changing only the RANDOM SEED yields non-overlapping learning curves; and hyperparameters, network architecture, reward scale, environment choice, and even the specific CODEBASE implementation each materially change measured performance. Two groups trained from the same algorithm with different seeds can differ at statistical significance. Concludes that without significance metrics and standardized reporting, you cannot tell whether a "new" method is a genuine advance, and gives evaluation guidelines (report many seeds, use significance tests).
- **Relevance to our system:** Foundational for our skepticism about every RL-trading result in this library. If, in clean simulated control tasks, changing only the seed produces non-overlapping performance, then a single-seed, single-path crypto-trading backtest ([10][21][23][28]) carries essentially no evidential weight — the headline number could be a lucky seed. This is the RL-side justification for our 1000-path bootstrap and weakest-link verdict: demand robustness across many resamples, never trust a point estimate. Directly motivates a rule for any future learned component in our advisor: report performance across multiple seeds AND multiple bootstrap paths, with a significance/dispersion measure, or the result doesn't count. One of the most important methodological citations for the whole topic.

### [48] Model-Free Deep Hedging with Transaction Costs and Light Data Requirements
- **Authors / Venue:** Pierre Brugière, Gabriel Turinici (Université Paris Dauphine-PSL) / arXiv (q-fin.CP)
- **Year:** 2025
- **Source:** arXiv:2505.22836
- **% read:** 45%
- **Summary:** A deep-hedging extension ([2][36] lineage) whose headline contribution is DATA EFFICIENCY: it trains a neural-network hedging policy (discrete time, transaction costs, GBM dynamics) that outperforms both Black-Scholes and the Leland cost-aware model using as few as 256 trajectories — versus the 10⁵–10⁶ trajectories prior deep-hedging work needed. The authors argue this light data requirement makes real-time implementation on actual financial series more practical.
- **Relevance to our system:** Background for the hedging application, but the data-efficiency angle is the interesting bit and a useful counter-data-point. Most of this literature is data-HUNGRY (a core reason DL overfits a single coin's short history); a method that learns a sensible policy from a few hundred trajectories is more compatible with the data-poor single-coin setting. Caveat to stay honest: the 256-trajectory result is on SYNTHETIC GBM data where the target hedge is well-defined and stationary — the easy case. Real crypto is non-stationary with fat tails, so "light data" likely doesn't transfer to learning a robust spot strategy. Still, it's a reminder not to assume every DL method needs millions of samples. Filed under deep-hedging extensions; not actionable, mildly thesis-relevant (data-efficiency matters in our regime).

### [49] Deep Stock Trading: A Hierarchical Reinforcement Learning Framework for Portfolio Optimization and Order Execution
- **Authors / Venue:** Rundong Wang, Hongxin Wei, Bo An, Zhouyan Feng, Jun Yao / AAAI 2021
- **Year:** 2020 (arXiv) / 2021 (AAAI)
- **Source:** arXiv:2012.12620
- **% read:** 40%
- **Summary:** A hierarchical-RL trader that separates two timescales: a HIGH-level policy sets portfolio weights at low frequency (maximize long-term return), and a LOW-level policy executes the implied buys/sells within a short window at higher frequency (minimize trading cost). Explicitly criticizes prior work for assuming instantaneous, frictionless reallocation and ignoring price slippage; tackles execution constraints directly. Uses pre-training + iterative training for data efficiency; reports improvements over state-of-the-art across US and Chinese markets.
- **Relevance to our system:** Background (multi-asset portfolio + intraday execution, neither of which fits a single-coin few-trades advisor), but it gets one thing conceptually right that we should keep in mind: it SEPARATES the allocation decision from the execution/cost decision, and it explicitly refuses to assume frictionless instant rebalancing. That two-level separation (what to hold vs. how to get there cheaply) is good hygiene; for us the "execution" layer is trivial (€200, a few trades) but the principle — never assume costless instantaneous trades — is exactly our cost-net discipline. Performance claim is the usual "beats SoTA" without a clear buy-and-hold-net-of-costs multi-path comparison. Filed as a hierarchical-RL reference; not actionable.

### [50] The Evolution of Reinforcement Learning in Quantitative Finance: A Survey
- **Authors / Venue:** Nikolaos Pippas, Elliot A. Ludvig, Cagatay Turkay (Warwick) / ACM Computing Surveys (accepted 2025)
- **Year:** 2024 (arXiv) / 2025 (journal)
- **Source:** arXiv:2408.10932
- **% read:** 30%
- **Summary:** A large, recent survey (167 publications) of RL across quantitative finance — portfolio optimization, trading strategies, market making, market simulation, risk management — plus methodological threads like multi-agent RL, transfer learning, and meta-learning. Frames finance as a valuable but adversarial RL testbed (complex, multi-agent, information-asymmetric, inherently random) and critiques strengths/weaknesses of existing methods while mapping the gap between theoretical RL frameworks and practical financial deployment.
- **Relevance to our system:** Meta/reference. Its value for us is mainly as an up-to-date (2024/25, ACM CSUR) map confirming the field is broad but still wrestling with the theory↔practice gap — i.e., no settled, deployable, baseline-beating recipe, consistent with [12][25][50]. The explicit emphasis on financial markets' randomness and information asymmetry is the structural reason single-coin directional edges are tiny and fragile. Useful as a survey citation to point at "here is the whole RL-finance landscape"; nothing directly deployable for a single-coin advisor. Background.

### [51] Dynamic Graph Neural Networks for Enhanced Volatility Prediction in Financial Markets
- **Authors / Venue:** Pulikandala Nithish Kumar, Nneka Umeorah, Alex Alochukwu (Cardiff / Albany State) / arXiv (q-fin.ST)
- **Year:** 2024
- **Source:** arXiv:2410.16858
- **% read:** 50%
- **Summary:** A Temporal Graph Attention Network (combining GCN + GAT layers) for forecasting REALIZED VOLATILITY across eight major global equity indices (S&P 500, DAX, CAC 40, FTSE 100, Nifty 50, Nikkei 225, KOSPI, Hang Seng), Nov 2007–Jun 2022. Graphs are built two ways: Pearson-correlation adjacency, and a Diebold-Yilmaz volatility-SPILLOVER index (directional, via VAR) — the spillover graph wins. Reports lower forecast error (MAFE/MSE) than GARCH and an MLP baseline across most indices and horizons (1/5/10/22-day). Honest caveat: performance degrades in high-volatility regimes (MAPE > 16% during the 2008 crisis), i.e., it's worst exactly when volatility matters most.
- **Relevance to our system:** Background, but more relevant than the directional GNN papers [11][33] because it forecasts VOLATILITY, which we actually care about (vol-targeting / sizing). Two honest takeaways: (1) cross-asset spillover graphs add real value for vol forecasting — but only with MANY assets (8 indices here); a single coin has no cross-asset graph, so this doesn't transfer to us directly (though if we ever incorporated BTC→altcoin spillover for an altcoin advisor, the Diebold-Yilmaz spillover-index idea is a clean way to build the graph). (2) The model is WORST in crisis regimes — a recurring, important limitation: vol models trained on calm data underestimate tail risk exactly when it spikes, which is a strong argument for stress-testing our gate with synthetic tail scenarios ([43]) rather than trusting a learned vol forecast. No cost-aware trading evaluation (pure prediction-error metrics). Filed under GNN/volatility; reinforces "models fail in crises."

### [52] Machine Learning-Based Bitcoin Trading Under Transaction Costs: Evidence From Walk-Forward Forecasting
- **Authors / Venue:** Andrei Bysik, Robert Ślepaczuk (University of Warsaw) / arXiv (q-fin.TR/ST)
- **Year:** 2026
- **Source:** arXiv:2606.00060
- **% read:** 60%
- **Summary:** Evaluates XGBoost, LSTM, and iTransformer on ~70,000 hourly BTC/USDT bars (2018–2026) under a rigorous 27-fold walk-forward protocol, with explicit transaction costs and a "cost-aware execution filter" that only trades when the forecast magnitude exceeds a cost-proportional threshold. Central finding: naive sign-based strategies generate positive GROSS returns but "fail once transaction costs of ten basis points are imposed" — some collapse to worse than −80% annualized. The cost-aware filter restores profitability (best XGBoost ~65% annualized, Sharpe > 1) by cutting turnover by an order of magnitude — BUT, crucially, even the filtered ML strategies do NOT achieve statistically significant outperformance versus BUY-AND-HOLD after bootstrap adjustment.
- **Relevance to our system:** Among the SINGLE most relevant papers in the entire library — it is almost a blueprint of our app's thesis and method, on our exact asset (hourly Bitcoin). It independently confirms, with bootstrap-adjusted significance: (1) statistical predictability does NOT translate to economic profit once costs are paid — naive crypto signals go from positive-gross to catastrophic-net at just 10 bps; (2) a cost-aware trade filter (trade only when expected move > cost) is the difference between viable and ruinous — directly validating our cost-net design and suggesting a concrete strategy primitive (forecast-magnitude-vs-cost gating); (3) even after all that engineering, the ML strategies are NOT significantly better than buy-and-hold under bootstrap — i.e., our "no active strategy robustly beats holding net of costs" thesis, reproduced on hourly BTC with the SAME bootstrap-significance discipline we use. Strongest possible external validation. Actionable: the cost-proportional trade-threshold filter is worth testing as a strategy/overlay in our bake-off.

### [53] Painting the Market: Generative Diffusion Models for Financial Limit Order Book Simulation and Forecasting
- **Authors / Venue:** Alfred Backhouse, Kang Li, Jakob Foerster, Anisoara Calinescu, Stefan Zohren (Oxford) / arXiv (q-fin.TR)
- **Year:** 2025
- **Source:** arXiv:2509.05107
- **% read:** 40%
- **Summary:** A diffusion-based market simulator that converts limit-order-book data into a structured IMAGE and uses diffusion with INPAINTING to generate future LOB states. Key advantage over autoregressive LOB generators (e.g. [14]): diffusion generates long sequences in PARALLEL, avoiding the error accumulation that plagues step-by-step autoregression, and exploits spatio-temporal inductive biases. Achieves state-of-the-art on the LOB-Bench benchmark even from lower-fidelity L2 (vs L3) data, prioritizing coherent global structure over local detail.
- **Relevance to our system:** Background for the LOB application (we're on bars, not L2 tape), but it's a clean data-point in the generative-simulator trend and reinforces a lesson relevant to our generator choice: diffusion's parallel generation avoids the autoregressive error-accumulation problem — important if we ever generate LONG synthetic paths to stress the gate, since an autoregressive generator's errors compound over a multi-month horizon. Combined with [16] (diffusion for return series), the message is consistent: diffusion is the stable, error-accumulation-resistant choice for long synthetic sequences. The "coherent global structure over local fidelity" framing is also apt for our use — we want plausible regime shapes for stress-testing, not tick-perfect local detail. Filed under generative simulators; not actionable directly, supports diffusion as our preferred generator family.

### [54] Evaluating Generative Models for Synthetic Financial Data
- **Authors / Venue:** Christophe D. Hounwanou, Pierre Ntakirutimana, Yaé Ulrich Gaba / arXiv (q-fin/cs.LG)
- **Year:** 2025
- **Source:** arXiv:2512.21791
- **% read:** 55%
- **Summary:** A head-to-head EVALUATION of three generators (ARIMA-GARCH, VAE, TimeGAN) on S&P 500 daily returns, with a comprehensive multi-axis metric framework: distributional fidelity (MMD, KS test), temporal coherence (autocorrelation of returns/squared-returns, vol clustering), DOWNSTREAM utility (portfolio Sharpe + allocation similarity, vol-forecast RMSE/MAE), and — distinctively — PRIVACY/LEAKAGE (nearest-neighbor distance + membership-inference attack). Findings: TimeGAN gets the best fidelity (lowest MMD) and lowest memorization (1.8% leakage); VAE trains stably but over-smooths extreme events (kills tail fidelity); ARIMA-GARCH is interpretable but more leakage-prone. Conclusion: no single model dominates — pick by balancing realism, interpretability, cost.
- **Relevance to our system:** Highly relevant as the VALIDATION-PROTOCOL template for our synthetic-test-data work — arguably the single most useful "how to vet a generator" reference. The metric set is exactly what we'd impose before trusting any generator to augment our bootstrap: (1) distributional + temporal fidelity to OUR coin, (2) downstream utility (does a strategy ranked on synthetic behave the same on real?), and crucially (3) LEAKAGE via nearest-neighbor + membership-inference — a direct, quantitative way to enforce our hard guardrail that the generator must not memorize/leak the held-out test path. The VAE-over-smooths-tails finding is a sharp warning: a generator that flattens extreme events would make our gate EASIER (less stress), the opposite of what we want — so tail fidelity must be a gating metric. Actionable: adopt this exact eval suite (fidelity + temporal + downstream + leakage, with explicit tail-event checks) as the acceptance test for any generator prototype.

### [55] Comparative Study of Bitcoin Price Prediction (LSTM vs GRU)
- **Authors / Venue:** Ali Mohammadjafari / University of Louisiana at Lafayette; arXiv (cs.LG)
- **Year:** 2024
- **Source:** arXiv:2405.08089
- **% read:** 60%
- **Summary:** A focused empirical comparison of LSTM vs GRU for daily Bitcoin price prediction on Yahoo Finance data (Dec 2015–Apr 2023; OHLC + adj-close + volume). Uses 5-fold cross-validation and L2 regularization. Finding: GRU outperforms LSTM (GRU MSE 4.67 vs LSTM 6.25) and runs ~30% faster. The study is purely a *prediction-accuracy* exercise — MSE/MAE only; no trading simulation, no transaction costs, no profitability or buy-and-hold analysis. Honest limitations are stated (only two models, untuned hyperparameters, more robustness work needed).
- **Relevance to our system:** A representative data point for the "classical LSTM/GRU crypto" seam, and a clean example of the literature's dominant gap: it reports a price-prediction error metric and stops there. For our advisor the result is doubly inert — (1) it never converts the forecast into a cost-net P&L or compares to buy-and-hold, so it says nothing about *tradable* edge (a lower MSE on price level can easily coexist with zero or negative net return), and (2) predicting the price *level* (which is dominated by the prior close on a near-random-walk) flatters MSE and tells you little about predicting the *return sign* that actually matters. The one transferable nugget is mundane and cost-relevant: GRU ≈ LSTM in accuracy but cheaper/faster — if we ever needed an RNN, GRU is the lighter default. Background; reinforces "accuracy metric ≠ alpha; demand cost-net + buy-and-hold."

### [56] CryptoMamba: Leveraging State Space Models for Accurate Bitcoin Price Prediction
- **Authors / Venue:** Mohammad Shahab Sepehri, Asal Mehradfar, Mahdi Soltanolkotabi, Salman Avestimehr (USC) / IEEE ICBC 2025
- **Year:** 2025
- **Source:** arXiv:2501.01010
- **% read:** 60%
- **Summary:** Applies a Mamba (selective state-space model) backbone to daily Bitcoin price forecasting, arguing SSMs capture long-range dependence with linear-time efficiency. On a held-out year (Sep 2023–Sep 2024), CryptoMamba-v (with volume) beats LSTM/Bi-LSTM and an S-Mamba baseline on RMSE (1598 vs LSTM 2673) and MAE. It adds two trading algorithms — "Vanilla" (binary buy/sell when predicted-vs-actual change exceeds a 0.01 threshold meant to proxy fees) and "Smart" (±2% prediction interval enabling partial sizing) — and reports final net worth from $100: CryptoMamba-v reaches $213–$263 vs LSTM ≈ $62–$104.
- **Relevance to our system:** Directly on the "Mamba/state-space for finance" seam and crypto-specific, but a textbook case of our methodological complaints despite being more thorough than most. Critical flaws by our standard: (1) NO buy-and-hold comparison is reported — and the test year Sep-2023→Sep-2024 was a strong BTC uptrend (roughly a doubling), so a $100→$263 result may simply track market drift, not skill; without the buy-and-hold line the trading numbers are uninterpretable. (2) Transaction costs are only "implicit" via a 0.01 threshold, not an explicit round-trip fee+slippage model — exactly the cost-blindness our gate forbids. (3) It is a SINGLE one-year test path, no bootstrap / multiple seeds / weakest-link verdict. (4) Forecasting the price *level* (RMSE on price) is the easy metric on a near-random walk; lower RMSE ≠ tradable edge. To its credit it at least runs a trading sim and a fee-proxy threshold. Net: an architecture data point (Mamba beats LSTM on price-level RMSE) but the profitability claim fails every robustness criterion — and the missing buy-and-hold benchmark on an uptrending year is the decisive omission our app hard-codes against. Cautionary; reinforces the gate.

### [57] Reinforcement Learning Pair Trading: A Dynamic Scaling Approach
- **Authors / Venue:** Hongshen Yang, Avinash Malik (University of Auckland) / arXiv (q-fin.TR/cs.LG)
- **Year:** 2024
- **Source:** arXiv:2407.16103
- **% read:** 65%
- **Summary:** Trains RL agents to time and size cryptocurrency *pair* trades (BTC-GBP, BTC-EUR, 1-min bars, ~263k points). Two formulations: RL1 (timing only: PPO/DQN/A2C) and RL2 (timing + quantity: PPO/A2C/SAC), with custom reward shaping that penalizes large position changes (a turnover/transaction penalty). Crucially, it DOES model fees (0.02% Binance default, with a sensitivity sweep at 0%/0.01%/0.05%). Reports annualized profit 9.94%–31.53% (A2C best) vs 8.33% for classical rule-based pair trading (Gatev et al.) — but the formation period is Oct–Nov 2023 and the test is only DECEMBER 2023 (one month), no buy-and-hold benchmark, no multiple seeds, and the authors explicitly flag "limited dataset scope" and the need for cross-validation.
- **Relevance to our system:** On the "deep RL for pairs/stat-arb" seam. Pair trading is inherently a TWO-leg, market-neutral, cross-sectional strategy (long one asset / short another on a cointegration spread) — structurally inapplicable to our long-only single-coin spot advisor, which can't take the short leg. Two genuinely useful things, though: (1) it is one of the more cost-honest RL-trading papers — explicit Binance fees plus a turnover penalty in the reward, with a fee-sensitivity sweep — exactly the discipline most of the literature omits; the reward-side turnover penalty echoes [15][45]. (2) Its omissions are instructive for our skepticism: a one-month test with no buy-and-hold and no seed variance means the 31.53% headline is statistically thin (cf. [47]). Net: background for the strategy class (cross-sectional, not ours), but a positive example of cost modeling and a negative example of single-window evaluation. Filed under RL pairs/stat-arb.

### [58] Contrastive Learning of Asset Embeddings from Financial Time Series
- **Authors / Venue:** Rian Dolphin, Barry Smyth, Ruihai Dong (University College Dublin) / arXiv (q-fin/cs.LG)
- **Year:** 2024
- **Source:** arXiv:2407.18645
- **% read:** 40%
- **Summary:** A self-supervised CONTRASTIVE framework that learns vector embeddings for assets from their return time series. Positive/negative training pairs are mined from the similarity of asset returns across many sub-windows, with a statistical-hypothesis-testing sampling strategy to cope with financial noise. The learned embeddings are evaluated on downstream representation-quality tasks — industry/sector classification and portfolio optimization — and reported to beat existing baselines. No trading P&L or cost-aware evaluation; the contribution is the representation itself, learned across MANY assets (cross-sectional).
- **Relevance to our system:** On the "contrastive/self-supervised representation learning for prices" seam, but largely background for us for the now-familiar reason: it is fundamentally CROSS-SECTIONAL — the entire premise is learning how assets relate to *each other* (positives = similarly-behaving assets), which requires a universe of assets a single-coin advisor doesn't have. The downstream wins are sector classification and portfolio optimization, neither of which is a single-coin task, and there is no cost-net trading evaluation. The transferable idea is conceptual: contrastive pretext tasks can extract noise-robust structure from unlabeled return data, and the hypothesis-testing-based positive/negative sampling is a thoughtful way to handle financial noise. But for one coin there is no cross-asset contrast to exploit. Filed under self-supervised representation learning; not actionable.

### [59] Trading through Earnings Seasons using Self-Supervised Contrastive Representation Learning (Contrastive Earnings Transformer)
- **Authors / Venue:** Zhengxin Joseph Ye, Bjoern Schuller (Imperial College London) / arXiv (cs.LG/q-fin)
- **Year:** 2024
- **Source:** arXiv:2409.17392
- **% read:** 35%
- **Summary:** Proposes the Contrastive Earnings Transformer (CET), a self-supervised model built on Contrastive Predictive Coding (CPC) to better extract durable signal from corporate earnings data, which arrives irregularly and decays in predictive value fast. A comparative study across diverse equity sectors claims CET "extrapolates the inherent value of earnings data over time," giving more consistent stock predictions as earnings data ages. Cross-sectional (multiple sectors/stocks); the accessible text does not confirm a cost-aware trading strategy versus pure prediction/representation quality.
- **Relevance to our system:** Background on two counts. (1) The signal is corporate EARNINGS — a fundamental, scheduled-disclosure data type that simply does not exist for a cryptocurrency (no quarterly earnings), so the application is out-of-domain for a single-coin crypto advisor. (2) It is cross-sectional (across sectors/stocks). The only transferable idea is the CPC/self-supervised framing for extracting signal from sparse, fast-decaying events — conceptually interesting but with no clean crypto analogue (the nearest would be sparse on-chain or news events, which carry their own leakage/manipulation hazards per [35]). The "predictive value decays rapidly after the event" observation is a useful general caution: event-driven signals are short-lived, so any event feature must be evaluated for how fast its edge decays. Filed under self-supervised representation learning; not actionable.

### [60] Non-adversarial Training of Neural SDEs with Signature Kernel Scores
- **Authors / Venue:** Zacharia Issa, Blanka Horvath, Maud Lemercier, Cristopher Salvi (Oxford / KCL / Imperial) / NeurIPS 2023
- **Year:** 2023
- **Source:** arXiv:2305.16274
- **% read:** 45%
- **Summary:** Trains Neural SDEs (NN-parametrized drift/diffusion as a generative model for paths) WITHOUT a GAN — replacing the adversarial discriminator with a proper *scoring rule on path space* built from signature kernels. This sidesteps the notorious GAN problems the authors name explicitly (unstable training, mode collapse, weight-clipping/gradient-penalty hacks). The score is strictly proper with a consistent estimator, training is memory-efficient via linear path-dependent PDEs, and it supports conditioning on market scenarios. Reported to outperform on rough-volatility simulation, conditional FX forecasting from observed past trajectories, and LOB generation; public code on GitHub.
- **Relevance to our system:** A strong addition to the synthetic-test-data toolkit and squarely in the "neural SDE / non-adversarial generator" lane we track ([26][38][46]). It directly attacks the [41] complaint (GAN fidelity is fragile/unstable): signature-kernel scoring gives non-adversarial training of a continuous-time path generator with a principled distance on path space — the same appeal as signature-MMD [46], here wired into a Neural SDE so it has a continuous-time inductive bias. The "conditioning on market scenarios" property is exactly the conditional-stress lever we want ([43]) for generating worse-than-observed regimes to harden the gate. Caveats unchanged: validate stylized-fact match to OUR coin and run the leakage suite [54] before trusting it; the paper's benchmarks (rough vol, FX, LOB) aren't crypto spot bars. Filed alongside [38][46] as a top-tier non-adversarial generator candidate; the signature-kernel scoring rule + Neural-SDE combo is one of the most principled options. Public code lowers the prototyping cost.

### [61] Self-Supervised Learning for Time Series: Contrastive or Generative?
- **Authors / Venue:** Ziyu Liu, Azadeh Alavi, Minyi Li, Xiang Zhang / arXiv (cs.LG)
- **Year:** 2024
- **Source:** arXiv:2403.09809
- **% read:** 50%
- **Summary:** A controlled head-to-head of the two self-supervised paradigms for time series: CONTRASTIVE (SimCLR with jittering augmentation) vs GENERATIVE (a masked autoencoder, MAE, with patch reconstruction). Finding is context-dependent: the generative MAE wins in LOW-label regimes (label ratios 0.01–0.1; +5.5% F1 at 1% labels on HAR) and trains ~26% faster, while contrastive SimCLR edges ahead only when labels are abundant (ratio > 0.5); results also vary by domain. Not finance-specific (HAR activity + ECG datasets); evaluation is classification, not forecasting.
- **Relevance to our system:** Background, but it carries a useful design heuristic for the unlikely case we ever pretrain a representation. The practically-relevant finding: in LOW-LABEL / data-poor regimes the generative (masked-reconstruction) approach beats contrastive — and a single coin's tradable "labels" (profitable regime transitions) are extremely scarce and noisy, so by analogy a generative/reconstruction pretext would be the better bet than contrastive over one coin. It also independently confirms the recurring theme that method superiority is regime- and domain-dependent, not universal — i.e., "use SSL technique X" is never automatically right. But the paper is on sensor/medical data with clear labels and a classification metric, nothing like cost-net crypto trading. Filed under self-supervised representation learning; informs method choice only if we ever go there (which the cross-sectional-edge evidence [24][32] argues against). Not actionable.

### [62] Stock Market Prediction via Deep Learning Techniques: A Survey
- **Authors / Venue:** Jinan Zou, Qingying Zhao, Yang Jiao, Haiyao Cao, Yanxi Liu, Qingsen Yan, Ehsan Abbasnejad, Lingqiao Liu, Javen Qinfeng Shi (University of Adelaide) / arXiv (cs.LG)
- **Year:** 2022 (arXiv) / 2023 (rev.)
- **Source:** arXiv:2212.12717
- **% read:** 35%
- **Summary:** A broad survey of 94 deep-learning stock-prediction papers across four subtasks (price prediction, movement prediction, portfolio management, trading strategies) and five architecture families (RNN, CNN, GNN, Transformer, RL). It catalogs the field and notes — but does NOT systematically audit — the recurring problems: it singles out an earlier review (Jiang 2021) as notable precisely *because* it considered implementation/reproducibility (implying most don't), and observes that only one surveyed study (Wang et al. 2021) acknowledged price slippage as a practical constraint, implying the rest ignore real-world trading costs. Overfitting and baseline comparisons are mentioned per-paper but never aggregated into a verdict.
- **Relevance to our system:** Background as a field map, but useful as third-party confirmation (from a neutral survey, not a critic) of the literature's structural blind spots that our gate is built around: reproducibility is the exception not the norm, and essentially one paper out of 94 took trading costs/slippage seriously. That is a damning base rate — it means the overwhelming majority of DL stock-prediction results are cost-blind and therefore say nothing about *tradable* edge, exactly the failure our cost-net, buy-and-hold-benchmarked gate exists to expose. It also confirms the metric soup (price vs movement vs portfolio vs strategy, each with different metrics) that makes the literature non-comparable. Filed as a survey citation supporting "treat headline DL-prediction numbers as cost-blind upper bounds." Not actionable; reinforces skepticism.

### [63] Evaluation of Reinforcement Learning Techniques for Trading on a Diverse Portfolio
- **Authors / Venue:** Ishan Khare, Tarun Martheswaran, Akshana Dassanaike-Perera (Stanford) / arXiv (q-fin/cs.LG)
- **Year:** 2024
- **Source:** arXiv:2309.03202
- **% read:** 55%
- **Summary:** A small but honest study comparing tabular/classical RL (Value Iteration, SARSA — on-policy; Q-Learning — off-policy) trading SPY on 2000–2023 data, with buy-and-hold S&P 500 as the explicit benchmark and two splits (one including COVID in training, one excluding it). Central finding is a NEGATIVE, regime-dependent result: when COVID was *in* the training set the RL agents could beat the baseline, but when COVID was *excluded* from training, buy-and-hold OUTPERFORMED all RL approaches out-of-sample. Q-Learning had the best training performance but the worst test generalization (classic overfit/bias-variance). Honest limitations: single asset, transaction costs NOT modeled, no multiple seeds/significance.
- **Relevance to our system:** Genuinely relevant as a negative result with an explicit buy-and-hold benchmark — the comparison most of the literature omits. Its key lesson is directly on-thesis: RL trading only "beat" holding when the test regime (COVID volatility) was already represented in training; on a genuinely out-of-distribution test, buy-and-hold won. That is the practical face of "RL memorizes the training regime and fails to generalize" ([27]) and a concrete warning about our own gate: a strategy can look good in-sample precisely because the in-sample window contained the regime it exploits. The Q-Learning-overfits-most finding echoes [47]'s seed/variance message. Caveats keep it from being decisive (no costs → if anything RL's apparent wins would shrink further with costs; single asset; no seeds). Net: a useful, honestly-reported corroboration that buy-and-hold beats RL out-of-distribution; supports our gate and the value of testing on regimes NOT in the training window.

### [64] Deep Reinforcement Learning Strategies in Finance: Insights into Asset Holding, Trading Behavior, and Purchase Diversity
- **Authors / Venue:** Alireza Mohammadshafie, Akram Mirzaeinia, Haseebullah Jumakhan, Amir Mirzaeinia / CSCE-ICAI 2024
- **Year:** 2024
- **Source:** arXiv:2407.09557
- **% read:** 45%
- **Summary:** An analysis of the *behavior* (not just performance) of five DRL agents (DDPG, PPO, TD3, SAC, A2C) trading 30 Dow stocks hourly (2022–2024) in the FinRL framework. Finds clear behavioral archetypes: DDPG/A2C/TD3 are "holders" (stay flat for long stretches, cap ~400 shares) while PPO/SAC are "traders" (churn more, up to ~1,700 shares); A2C unexpectedly earned the highest cumulative reward. It explicitly does NOT report a buy-and-hold benchmark, terminal wealth/CAGR, or transaction costs, and runs a single seed.
- **Relevance to our system:** Background — multi-asset equities, no costs, no buy-and-hold, single seed (so the performance content is uninterpretable by our standards, and the paper itself only claims behavioral insight). One observation is mildly interesting for our priors: trained DRL agents spontaneously split into "holders" and "traders," and the holders are not obviously worse — consistent with the broader finding that, net of costs, doing less (holding) is competitive. But because costs are omitted, the "traders" PPO/SAC are flattered (their churn is free here), so the paper can't actually adjudicate hold-vs-trade. Filed as a behavioral-analysis curiosity; reinforces (weakly) that holding-like behavior emerges and isn't dominated, while illustrating yet again the missing-benchmark/missing-cost pattern. Not actionable.

### [65] Gray-box Adversarial Attack of Deep Reinforcement Learning-based Trading Agents
- **Authors / Venue:** Foozhan Ataiefard, Hadi Hemmati (York University) / arXiv (cs.LG/q-fin)
- **Year:** 2023
- **Source:** arXiv:2309.14615
- **% read:** 50%
- **Summary:** Studies the adversarial *robustness* of deep-RL trading agents under a realistic GRAY-BOX threat model: the attacker has no access to the victim's model weights and simply trades in the SAME market to manipulate prices the victim observes (no special access needed beyond a normal trading account). A hybrid CNN+FC adversary policy is trained to harm the victim. Results are dramatic: the attack cut victim reward by 214% on average and slashed profits across a baseline agent (−139%), an ensemble method (−94%), and even a real industrial-partner trading product (−86%) — while spending less budget than the victims. Validated in simulated environments across three market configurations.
- **Relevance to our system:** Directly on the "adversarial robustness" seam and a sharp cautionary lesson, even though we deploy no live RL agent. The transferable insight is about FRAGILITY and attack surface: a DRL trading policy can be degraded catastrophically by an adversary who merely trades against it — and crypto markets are notoriously more manipulable than equities (wash trading, spoofing, low-float pumps), so any learned policy deployed in crypto faces an even worse version of this. This is a strong argument that (1) a black-box learned policy is a liability in an adversarial market, and (2) simple, transparent, hard-to-game rules (and holding) are more robust by construction. It complements [35] (adversarial text attacks on sentiment models): both show learned trading signals are brittle to adversarial inputs. Reinforces our preference for simple robust strategies over opaque learned ones; filed under adversarial robustness. Not actionable as a model, valuable as a risk argument.

### [66] Reinforcement Learning in Agent-Based Market Simulation: Unveiling Realistic Stylized Facts and Behavior
- **Authors / Venue:** Zhiyuan Yao, Zheng Li, Matthew Thomas, Ionut Florescu (Stevens Institute of Technology) / IJCNN 2024
- **Year:** 2024
- **Source:** arXiv:2403.19781
- **% read:** 40%
- **Summary:** An agent-based market *simulator* in which the participants are RL learners rather than zero-intelligence or hand-coded agents. The emergent market is shown to reproduce realistic stylized facts, and the authors probe agent behavior under external shocks such as a flash crash, arguing the RL-agent population adapts to changing conditions and yields more realistic dynamics than simpler agent pools. Validation is against real-market stylized facts (abstract light on specifics).
- **Relevance to our system:** On the "agent-based / generative market simulator" seam ([31][44] lineage), tracked for synthetic test data. Like [31], the appeal of a structural ABM is that dynamics EMERGE from interacting agents, so it can in principle generate genuinely novel regimes (e.g., a flash crash the historical path lacks) — exactly the gap in our moving-block bootstrap, and a route to the stress scenarios we want ([43]). The flash-crash probe is conceptually the kind of tail event we'd want to inject into the gate. Downsides are the usual ABM ones (heavy to build/calibrate, validation against precise stylized facts not fully shown, far more engineering than a diffusion/VAE generator). Filed as an ABM simulator alternative; for a €200 single-coin advisor the non-adversarial generative route ([16][38][46][60]) remains the more practical first prototype, but the flash-crash/shock-injection idea is worth borrowing for stress-testing. Background-plus.

### [67] TSMixer: An All-MLP Architecture for Time Series Forecasting
- **Authors / Venue:** Si-An Chen, Chun-Liang Li, Nate Yoder, Sercan O. Arik, Tomas Pfister (Google Cloud AI) / Transactions on Machine Learning Research
- **Year:** 2023
- **Source:** arXiv:2303.06053
- **% read:** 45%
- **Summary:** A deliberately simple all-MLP forecaster that EXTENDS the DLinear [8] linear-baseline insight rather than refuting it. It explicitly accepts that "simple univariate linear models can outperform [deep] models" on common benchmarks, then adds time-mixing and feature-mixing MLP blocks (with residuals that preserve temporal-linear capacity) to capture CROSS-VARIATE structure. On standard academic benchmarks TSMixer is merely "comparable" to SoTA transformers; its clear win comes on the large-scale M5 retail dataset where rich multivariate/auxiliary information exists. Key stated insight: going beyond univariate linear helps only when the data carries genuine cross-variate signal.
- **Relevance to our system:** Background as an architecture, but it sharpens the DLinear lesson ([8][22]) in a way that matters for us. The honest takeaway: deep models beat the linear baseline only when there is real CROSS-VARIATE / multivariate structure to exploit (M5 retail has it; the standard univariate benchmarks largely don't). A single coin's OHLCV is the data-poor, low-cross-variate case — precisely where TSMixer's own thesis says the MLP machinery adds little over a linear model. This is the same conclusion as the cross-sectional-edge finding [24][32], now from the forecasting side: complexity pays off with multivariate breadth, which a single coin lacks. Reinforces: beat the linear baseline first, and don't expect deep forecasters to help on one thin series. Filed under "simple-baseline" guardrails; not actionable.

### [68] CoST: Contrastive Learning of Disentangled Seasonal-Trend Representations for Time Series Forecasting
- **Authors / Venue:** Gerald Woo, Chenghao Liu, Doyen Sahoo, Akshat Kumar, Steven Hoi (Salesforce Research / SMU) / ICLR 2022
- **Year:** 2022
- **Source:** arXiv:2202.01575
- **% read:** 50%
- **Summary:** A self-supervised representation-learning approach for forecasting that DISENTANGLES trend and seasonal components and learns each with a dedicated contrastive loss — a time-domain loss (MoCo-style) for the trend via a mixture of auto-regressive experts at multiple kernel sizes, and a frequency-domain loss (learnable Fourier layers, separate amplitude/phase) for seasonality. The paradigm is "learn disentangled representations first, then a simple regression head," motivated causally. Reports 18–39% MSE improvements over end-to-end forecasters and ~21% over TS2Vec on ETT/electricity/weather/M5 benchmarks. Supports univariate and multivariate; not finance-specific.
- **Relevance to our system:** On the "contrastive/self-supervised representation learning for prices" seam. The disentangle-trend-from-seasonal idea is the same cheap, robust primitive we already noted ([7][8]) — and CoST's "learn representation, then simple linear regression head" two-stage design is appealingly low-overfitting in spirit. BUT the caveats are decisive for us: (1) the benchmarks are power/weather/retail, never cost-net trading P&L; (2) crypto has weak/unstable seasonality (no reliable diurnal/weekly cycle that survives out-of-sample on a single coin), so the seasonal-disentangling half of the method has little to bite on; (3) the bigger wins are multivariate, and we have one thin series. The transferable conceptual nugget — disentangle trend/seasonal and keep the downstream head simple — is worth remembering, but contrastive pretraining on one coin's history is unlikely to manufacture tradable edge where the cross-sectional evidence [24][32] and the linear-baseline evidence [8][67] say none exists. Filed under self-supervised representation learning; conceptual only.

### [69] Interpretable Hypothesis-Driven Trading: A Rigorous Walk-Forward Validation Framework for Market Microstructure Signals
- **Authors / Venue:** Gagan Deep, Akash Deep, William Lamptey (Texas Tech University) / arXiv (q-fin.TR)
- **Year:** 2025
- **Source:** arXiv:2512.12924
- **% read:** 55%
- **Summary:** A rigor-first framework that pairs interpretable, hypothesis-driven signals with RL and a STRICT walk-forward protocol — 34 independent OOS test periods (2015–2024) on 100 US equities, explicit costs ($1 commission + 0.0005 slippage), and SPY as the passive benchmark. It opens by citing Harvey et al.'s "most claimed findings in financial economics are likely false" once you account for data mining. The honest result: signals are strongly regime-dependent (positive in 2020–2024 high-vol, negative in 2015–2019 calm), and AGGREGATE performance is 0.55% annualized, Sharpe 0.33, statistically INSIGNIFICANT (p = 0.34). SPY crushes it on absolute return (13.2% vs 0.55%) though the strategy has a much smaller drawdown (−2.76% vs −23.8%). Authors candidly note only ~12% statistical power at the observed effect size.
- **Relevance to our system:** Among the more useful negative-result citations of this batch and strongly on-thesis. It does the methodology RIGHT — strict information-set discipline, 34 walk-forward folds across regimes, real costs, an explicit passive benchmark — and reaches the same conclusion our gate is built to deliver: the active strategy does NOT produce statistically significant returns and the passive benchmark wins on absolute performance. The honesty about statistical POWER (12%) is a rare and important point for us: with few independent regime-folds you often *cannot* distinguish a tiny edge from zero — a direct argument for our 1000-path bootstrap (manufacture many resamples to get power) and for humility about thin out-of-sample windows. The regime-dependence (works only in high-vol) echoes [42][63]: any apparent edge is conditional, not general. One nuance worth keeping: the strategy's smaller drawdown vs SPY is the kind of risk-adjusted trade-off our weakest-link verdict should weigh — but lower drawdown at near-zero return is not "beating" buy-and-hold. Strong support for the gate, the multiple-testing caution, and reporting statistical power. Background as a model (equities, RL+rules), but methodologically a kindred paper.

### [70] TS2Vec: Towards Universal Representation of Time Series
- **Authors / Venue:** Zhihan Yue, Yujing Wang, Juanyong Duan, Tianmeng Yang, Congrui Huang, Yunhai Tong, Bixiong Xu (Peking Univ. / Microsoft) / AAAI 2022
- **Year:** 2021 (arXiv) / 2022 (AAAI)
- **Source:** arXiv:2106.10466
- **% read:** 40%
- **Summary:** The canonical universal self-supervised time-series representation method. Learns timestamp-level embeddings via HIERARCHICAL contrastive learning over augmented context views (augmentations = timestamp masking + random cropping), so any sub-sequence representation is an aggregation of its timestamps. Achieves SoTA unsupervised results across 125 UCR + 29 UEA datasets and transfers to forecasting (a simple linear head on the representation) and anomaly detection. General-purpose; no finance/trading evaluation in the paper.
- **Relevance to our system:** On the "contrastive/self-supervised representation learning" seam and the most-cited baseline there (CoST [68] explicitly beats it). Background for us: it is a general representation learner validated on classification/anomaly benchmarks, with no trading or cost-net evaluation, and — like all SSL methods here — its forecasting wins are demonstrated on standard (non-financial, often multivariate) benchmarks. The transferable idea is the "learn a representation with a simple linear downstream head" pattern (low overfitting surface), and the augmentations (masking, cropping) are a clean recipe IF we ever pretrain. But the recurring blockers apply: a single coin's history is data-poor and near-random-walk, the cross-sectional/multivariate setting where representation learning shines is absent ([24][32][67]), and a better representation of a near-efficient price still need not yield cost-net alpha. Filed under self-supervised representation learning as the field-standard baseline; conceptual reference only, not actionable.

### [71] Deep Calibration of Market Simulations using Neural Density Estimators and Embedding Networks
- **Authors / Venue:** Namid R. Stillman, Rory Baggott, Justin Lyon, Jianfei Zhang, Dingqiu Zhu, Tao Chen, Perukrishnen Vytelingum (Simudyne / Tencent) / arXiv (q-fin.CP)
- **Year:** 2023
- **Source:** arXiv:2311.11913
- **% read:** 50%
- **Summary:** Solves the CALIBRATION problem for agent-based market simulators — how to tune ABM parameters so the simulation matches a real trading period — via simulation-based inference. It pairs neural density estimators (neural spline flows / masked autoregressive flows) with embedding networks (MLPs that compress high-dimensional market data into low-dimensional summaries), so calibration does NOT rely on hand-picked stylized-fact metrics. Applied to two ABMs (Zero-Intelligence Trader, extended Chiarella), it infers posterior parameter distributions and — though "blind" to stylized facts during calibration — the calibrated simulators still reproduce volatility clustering, volume-volatility correlation, and heavy tails on Hong Kong exchange data. Honest limitations: some parameters are unidentifiable (posteriors span the whole prior), single-day calibration only, simple embeddings, model-misspecification unaddressed.
- **Relevance to our system:** On the "generative / agent-based market simulator" seam ([31][44][66]), and it fills a specific gap: how to make an ABM actually MATCH our coin's data rather than just look qualitatively market-like. The learned-summary-statistic idea (let an embedding net decide what to match, instead of hand-coding stylized facts) is elegant and could in principle calibrate any simulator to our coin. But the honest limitations are the dealbreakers for us right now: parameter unidentifiability and single-day-only calibration mean it's not yet a reliable way to produce long, varied, well-matched synthetic histories, and ABMs remain far heavier than the non-adversarial generative route ([16][38][46][60]). The durable lesson is methodological: when validating ANY generator against our coin, learned summary statistics + posterior inference is a more principled fit-check than eyeballing a few stylized facts. Filed under simulator calibration; background-plus, not a first prototype.

### [72] Universal Time-Series Representation Learning: A Survey
- **Authors / Venue:** Patara Trirat, Yooju Shin, Junhyeok Kang, et al. (KAIST) / arXiv (cs.LG)
- **Year:** 2024
- **Source:** arXiv:2401.03717
- **% read:** 35%
- **Summary:** The first comprehensive survey of universal time-series representation learning (methods tested on ≥2 downstream tasks), reviewing 127 papers since 2018 from top ML venues. Organizes the field around three pillars — data-centric design (selection, decomposition, augmentation, generation), neural architectures (MLP→RNN→Transformer→GNN→neural ODE), and learning objectives (task-adaptive, non-contrasting/reconstruction-masked, contrasting). Notes self-supervised contrastive objectives now dominate (annotation scarcity), multivariate inter-variable structure is the hard part, and "diverse semantics across domains make universal transfer difficult." Field limitations flagged: no augmentation strategies with theoretical guarantees, weak distribution-shift adaptation, sparse standardized representation-quality protocols.
- **Relevance to our system:** Background/meta map of the SSL-for-time-series field that contextualizes [58][59][61][68][70]. Two takeaways matter for our skepticism. (1) The survey contains NO finance-specific evidence that representation learning improves forecasting/trading — it explicitly stays domain-general; so the entire SSL lane remains *unvalidated for cost-net trading*, exactly the bar our gate enforces. (2) It names the field's own unsolved problems — augmentations lack theoretical guarantees and distribution-shift adaptation is weak — which are precisely the properties a non-stationary single-coin price series would stress most (an augmentation that's invalid for prices, or a representation that breaks under regime shift, manufactures false confidence). Net: confirms SSL is a broad, fast-moving, but finance-unproven area; nothing here changes the "cross-sectional edge is tiny, single-coin SSL is unlikely to help" conclusion ([24][32]). Survey citation; not actionable.

### [73] Bitcoin Transaction Strategy Construction Based on Deep Reinforcement Learning
- **Authors / Venue:** Fengrui Liu, Yang Li, Baitong Li, Jiaxin Li, Huiyang Xie / Applied Soft Computing (Elsevier)
- **Year:** 2021
- **Source:** arXiv:2109.14789
- **% read:** 40%
- **Summary:** A PPO-based Bitcoin trading agent that frames the task as an MDP (actions = transactions, reward = returns, state = prices), built on top of an LSTM price predictor (reported to beat SVM/MLP/TCN/Transformer). Headline claim: 31.67% more return than the best benchmark (a 12.75% improvement). The accessible text does NOT specify transaction-cost/slippage modeling, the names of the competing benchmarks, the test period, or any walk-forward/robustness validation.
- **Relevance to our system:** On the deep-RL-crypto seam, crypto-specific (Bitcoin), but it lands in the same cautionary bucket as [10][21][23][28][56]. The decisive gaps by our standard: (1) transaction costs are not clearly modeled — a PPO agent built on a directional LSTM forecast will trade frequently, and uncosted churn flatters returns exactly as in [52] (where positive-gross collapsed to −80% net at 10 bps); (2) "31.67% more than the best benchmark" does not state whether that benchmark is buy-and-hold over the SAME window, nor the test period — and 2021 was a wild BTC year where drift dominates; (3) no robustness/walk-forward/seed analysis. The PPO+LSTM stack is a standard engineering pattern; the performance claim is untrustworthy as stated. Cautionary; reinforces the gate and the cost-blindness pattern. Background.

### [74] MambaStock: Selective State Space Model for Stock Prediction
- **Authors / Venue:** Zhuangwei Shi (Ji Hua Laboratory) / arXiv (q-fin.ST/cs.LG)
- **Year:** 2024
- **Source:** arXiv:2402.18959
- **% read:** 40%
- **Summary:** Applies the Mamba selective state-space model (S6 selection + scan, N=16 state dims, tanh output for movement-rate in (−1,1)) to predict daily price *movement rate* for four Chinese bank stocks. Benchmarks against Kalman filter, ARIMA, ARIMA-NN, XGBoost, LSTM, BiLSTM, Transformer, and two hybrids; reports best-in-class error (e.g., RMSE 0.045, R² 0.94 on one stock). Purely a prediction-error study — no trading simulation, no transaction costs, no buy-and-hold comparison; limitations not discussed.
- **Relevance to our system:** On the "Mamba/state-space for finance" seam, pairing with CryptoMamba [56]. Background and a textbook example of the prediction-only failure mode: a high R² on movement rate sounds impressive but (1) is never converted into a cost-net P&L or compared to buy-and-hold, so it says nothing about tradable edge, and (2) an R² of 0.94 on next-step movement rate is suspiciously high for a near-efficient daily series and smells of the same level-vs-return / autocorrelation-flattering issue as [55][56] — predicting a quantity dominated by its own persistence inflates fit without implying you can trade it net of costs. The architecture point (Mamba is a viable, linear-time sequence backbone) is the only durable takeaway, and it's already covered by [56]. Filed under state-space/finance; reinforces "prediction metric ≠ alpha; demand cost-net + buy-and-hold." Not actionable.

### [75] KASPER: Kolmogorov-Arnold Networks for Stock Prediction and Explainable Regimes
- **Authors / Venue:** Vidhi Oad, Param Pathak, Nouhaila Innan, Shalini D, Muhammad Shafique (NYU Abu Dhabi et al.) / arXiv (q-fin/cs.LG)
- **Year:** 2025
- **Source:** arXiv:2507.18983
- **% read:** 35%
- **Summary:** Applies the newer Kolmogorov-Arnold Network (KAN) architecture — sparse spline-based learnable activations — to stock prediction, adding Gumbel-Softmax regime detection and symbolic rule extraction (Monte Carlo Shapley values) for interpretability. On Yahoo Finance data it reports R² 0.89, MSE 0.0001, and a Sharpe ratio of **12.02**. The accessible text discloses NO transaction-cost model, NO buy-and-hold benchmark, and NO walk-forward/robustness detail.
- **Relevance to our system:** A near-perfect CAUTIONARY case for the newest architecture fad, on par with [21][23]. A Sharpe of 12.02 is not a plausible real-world figure — professional strategies target ~1–3, and a Sharpe above ~4 on a single asset is essentially a red flag for look-ahead leakage, in-sample fitting, an unrealistic/zero-cost fill assumption, or computing Sharpe on a fitted in-sample curve. Combined with the missing cost model and missing buy-and-hold benchmark, the number carries no evidential weight. The deeper lesson for us: every generation brings a shiny new architecture (KAN here, Mamba [56][74], Transformers [40] before) with the SAME methodological holes — no costs, no passive benchmark, implausible headline metric — so our frozen, cost-net, buy-and-hold-benchmarked, bootstrap gate is exactly the architecture-agnostic filter that protects us regardless of what's trendy. Treat KAN claims as null until reproduced under realistic costs. Strong reinforcement of the gate; the only nugget worth noting is that KAN's symbolic-rule extraction is at least aimed at interpretability. Cautionary; not actionable.

### [76] An Ensemble Method of Deep Reinforcement Learning for Automated Cryptocurrency Trading
- **Authors / Venue:** Shuyang Wang, Diego Klabjan (Northwestern University) / arXiv (q-fin.TR/cs.LG)
- **Year:** 2023
- **Source:** arXiv:2309.00626
- **% read:** 40%
- **Summary:** An ensemble DRL approach for intraday crypto portfolio trading whose two contributions are (1) a model-selection technique using MULTIPLE validation periods (rather than one) and (2) a "mixture distribution policy" for combining the selected models. It explicitly benchmarks against BOTH a single-DRL strategy AND a passive investment strategy, and reports out-of-sample improvement over both, with robustness assessed via the distribution of performance across many granular test periods; non-stationarity handled by periodic retraining.
- **Relevance to our system:** A relatively more disciplined DRL-crypto paper than most — and worth noting for two good practices we already value. (1) It uses MULTIPLE validation periods for model selection, which is a partial defense against the single-validation-set overfitting trap (cf. the FinRL Contests finding [77] that a single validation set overfits); this rhymes with our multi-path bootstrap philosophy. (2) It includes a PASSIVE benchmark and reports the DISTRIBUTION of out-of-sample performance across periods rather than a single number — exactly the dispersion-aware reporting [47] argues for. Caveats keep it cautionary: the accessible text doesn't pin down transaction-cost modeling, the magnitude of the edge over the passive benchmark, or the test period, and intraday crypto portfolio trading is multi-asset and not our setting. Net: a better-than-average methodology (multi-validation, passive benchmark, distributional reporting) but no clear evidence of a robust cost-net edge over holding. Background-plus; the multi-validation-period idea is a reusable diagnostic.

### [77] FinRL Contests: Benchmarking Data-driven Financial Reinforcement Learning Agents
- **Authors / Venue:** Keyi Wang, Nikolaus Holzer, Ziyi Xia, Yupeng Cao, Jiechao Gao, Anwar Walid, Kairong Xiao, Xiao-Yang Liu / arXiv (q-fin/cs.LG)
- **Year:** 2024 (rev. 2025)
- **Source:** arXiv:2504.02281
- **% read:** 45%
- **Summary:** Reports three years (2023–2025) of the FinRL competitions — 200+ participants, 100+ institutions — as a standardized benchmark for financial RL, and the results are a quietly devastating real-world stress test. Headline findings: (1) policy INSTABILITY is the central barrier — small training/environment changes swing performance wildly; (2) GENERALIZATION fails acutely — a 2023 top team hit Sharpe 9.56 on the initial test window then went NEGATIVE with reversed rankings on the very next unseen week; (3) LLM-signal tasks (2024–25) showed 90–335% training-period returns but −50% to −92% drawdowns (severe overfitting); (4) on CRYPTO specifically, the best ensemble managed 0.66% cumulative return vs 0.74% for simply HOLDING Bitcoin (near-identical Sharpe 0.28 vs 0.20). FinRL-Crypto applied a PBO overfitting-rejection at 10% significance. Stock-trading ensembles did beat the DJIA in-contest (62.6% vs 18.95%), but the OOS instability caveats apply.
- **Relevance to our system:** One of the strongest external corroborations of our thesis from a large, neutral, multi-team benchmark — and especially valuable because it is run by the FinRL group itself (DRL proponents). The decisive data point: across many teams and three years, the best CRYPTO RL agent essentially TIED buy-and-hold Bitcoin (0.66% vs 0.74%) — i.e., even a global community of practitioners, competing hard, could not robustly beat holding on crypto, our exact asset and exact thesis. The Sharpe-9.56→negative-next-week result is the single cleanest illustration of why a single-window backtest is worthless and why our 1000-path bootstrap + weakest-link verdict + always-buy-and-hold benchmark exists. The LLM-signal 335%-return-with-−92%-drawdown finding is a direct warning about the LLM-feature lane (cross-topic). And the use of PBO-rejection [18] in FinRL-Crypto reinforces adding an overfitting-probability diagnostic to our gate. Definitive citation: "even the crowd cannot beat buy-and-hold on crypto, and single-window RL results reverse out-of-sample." Strong support for the gate.

### [78] Conditional Sig-Wasserstein GANs for Time Series Generation
- **Authors / Venue:** Shujian Liao, Hao Ni, Lukasz Szpruch, Magnus Wiese, Marc Sabate-Vidales, Baoren Xiao (UCL / Alan Turing Institute) / Mathematical Finance (2024); arXiv 2020
- **Year:** 2020 (arXiv) / 2024 (journal)
- **Source:** arXiv:2006.05421
- **% read:** 45%
- **Summary:** A generative model for (conditional) financial time series that fuses a Wasserstein-GAN with the PATH SIGNATURE. The key move: the signature gives a graded sequence of statistics that universally characterizes a path's law, so the authors define a conditional Sig-W₁ metric and use it as an EXPLICIT discriminator — replacing the expensive, unstable GAN min-max with a closed-form distance and capturing temporal dependence of the joint distribution even for long sequences (where vanilla GANs blow up in dimension). Reports consistent outperformance over prior generators on similarity and predictive-ability measures on synthetic and empirical data; published in Mathematical Finance.
- **Relevance to our system:** A strong synthetic-test-data candidate and a key node in the signature-methods lane ([46] signature-MMD, [60] signature-kernel Neural SDE). Its appeal for us is the same as those: the signature is purpose-built for path-dependent dynamics (return paths are exactly that), and using an explicit Sig-W₁ discriminator removes the adversarial-instability/mode-collapse problem [41] flags — you get a principled distance instead of a fragile min-max. The CONDITIONAL framing is also directly useful: conditioning on recent history lets you generate plausible *continuations* of our coin's path, and conditioning more generally is the lever for the stress/regime enrichment we want ([43]). Caveats unchanged: validate per-stylized-fact against OUR coin and run the leakage suite [54]. Net: belongs in the top tier of generator candidates alongside [16][38][46][60]; the signature family ([46][60][78]) is now well-represented and is arguably the most theoretically apt for path data. Filed under generative simulators (signature methods).

### [79] N-BEATS: Neural Basis Expansion Analysis for Interpretable Time Series Forecasting
- **Authors / Venue:** Boris N. Oreshkin, Dmitri Carpov, Nicolas Chapados, Yoshua Bengio (Element AI / Mila) / ICLR 2020
- **Year:** 2019 (arXiv) / 2020 (ICLR)
- **Source:** arXiv:1905.10437
- **% read:** 45%
- **Summary:** A pure-DL univariate forecaster — deep stacks of fully-connected layers with backward/forward residual links, no time-series-specific components in the generic version. An interpretable variant adds explicit trend and seasonality basis functions with little accuracy loss. On the M3/M4/TOURISM competition datasets it beats the statistical benchmark by ~11% and the prior M4 winner (an NN+statistical hybrid) by ~3%, arguing that generic deep primitives alone can outperform hybrid/statistical methods across diverse domains.
- **Relevance to our system:** Background as an architecture, but a useful counter-balance to the DLinear narrative ([8]) that keeps our skepticism calibrated rather than dogmatic. N-BEATS is real evidence that a well-designed pure-DL forecaster CAN beat strong statistical baselines — but note the margins (3–11%) are modest and, crucially, measured as forecasting ACCURACY across thousands of HETEROGENEOUS series (M4 has 100k series), never as cost-net trading P&L on one asset. That is the recurring pattern: deep forecasters earn their keep with breadth (many series / cross-variate structure [67]), which a single coin lacks, and "better forecast accuracy" still doesn't imply "tradable edge after costs." The genuinely transferable, low-risk idea is N-BEATS's interpretable trend/seasonal basis — same cheap decomposition primitive as [7][8][68]. Net: a fair, honest forecasting paper; it tempers the "linear always wins" overstatement while leaving our conclusion intact (forecast-accuracy gains are modest, breadth-dependent, and not cost-net alpha for one coin). Background.

### [80] Diffusion Factor Models: Generating High-Dimensional Returns with Factor Structure
- **Authors / Venue:** Minshuo Chen, Renyuan Xu, Yumin Xu, Ruixun Zhang / arXiv (q-fin/stat.ML)
- **Year:** 2025
- **Source:** arXiv:2504.06566
- **% read:** 40%
- **Summary:** Bakes a latent FACTOR structure into a diffusion generative model by decomposing the score function via time-varying orthogonal projections, so the generator's effective complexity scales with the intrinsic factor dimension k rather than the asset count d. Designed to generate synthetic high-dimensional asset returns (thousands of assets, limited data) for risk management, portfolio optimization, and scenario simulation. Provides statistical error bounds and shows superior latent-subspace recovery in small-data regimes. Explicitly CROSS-SECTIONAL (many assets).
- **Relevance to our system:** On the generative-simulator lane (diffusion family, [16][43][53]), but its specific contribution — exploiting cross-sectional factor structure to generate many correlated assets from little data — is for a setting we don't have (one coin = no cross-section, no factor structure to exploit). So the method itself is not for us. The transferable, durable ideas are two: (1) it is another data point that diffusion + an economically-motivated structural constraint (here a factor model; cf. no-arbitrage in [26][32]) is the principled way to constrain a generator to stay plausible — reinforcing that any generator we build should bake in structure, not just fit a distribution; (2) the "complexity scales with intrinsic dimension, not raw dimension" insight is a reminder that data-poor settings need strong inductive biases (a single coin is extremely data-poor, so a heavy unconstrained generator would overfit). Filed under generative simulators (cross-sectional/diffusion); conceptual reference, not a single-coin prototype.

### [81] Tail-GAN: Learning to Simulate Tail Risk Scenarios
- **Authors / Venue:** Rama Cont, Mihai Cucuringu, Renyuan Xu, Chao Zhang (Oxford) / arXiv (q-fin.RM/stat.ML)
- **Year:** 2022 (rev. 2025)
- **Source:** arXiv:2203.01664
- **% read:** 45%
- **Summary:** A GAN purpose-built to simulate TAIL-RISK scenarios for portfolios. The clever ingredient: it exploits the joint *elicitability* of Value-at-Risk (VaR) and Expected Shortfall (ES) to design the GAN objective so the generator is trained specifically to reproduce extreme-loss distributions correctly (not just the body of the distribution), with PCA for scalability to many assets. Reported to correctly capture tail risk across a broad class of static AND dynamic trading strategies and to generalize on synthetic + market data; explicitly multi-asset (beyond the usual univariate scenario-generation setting).
- **Relevance to our system:** Directly relevant to the part of our synthetic-data interest that matters MOST — stress/tail scenarios — and a sharp complement to [43] (diffusion stress generator) and [54]'s warning that generators which over-smooth tails make the gate too easy. The core idea is exactly what we want: a generator trained to get the TAIL right (VaR/ES-aware objective), so the synthetic paths we'd feed our robustness gate actually contain the worse-than-observed crashes the moving-block bootstrap can't produce. The VaR/ES-elicitability training objective is a principled, transferable way to ensure tail fidelity is a first-class target rather than an afterthought — and tail fidelity is precisely the gating metric [54] says we must enforce. Caveats: it's a GAN (so the [41] fragility caution applies — validate hard), it's multi-asset/portfolio-oriented (we'd need the single-asset specialization), and it targets risk-measure accuracy, not alpha. Net: a top idea for HARDENING our gate with tail scenarios; the tail-aware objective is the durable lesson even if we pick a different generator backbone. Filed under generative simulators (tail/stress).

### [82] Review of Deep Learning Models for Crypto Price Prediction: Implementation and Evaluation
- **Authors / Venue:** Jingyang Wu, Xinyi Zhang, Fangyixuan Huang, Haochen Zhou, Rohitash Chandra (UNSW Sydney) / arXiv (cs.LG/q-fin)
- **Year:** 2024
- **Source:** arXiv:2405.11431
- **% read:** 45%
- **Summary:** A review-plus-implementation that re-implements and evaluates several DL architectures (LSTM variants, CNN variants, Transformers, ConvLSTM hybrids) for crypto price prediction across four coins, in univariate and multivariate settings, over pre-COVID and COVID-era windows. Main findings: a multivariate ConvLSTM gives the best prediction ACCURACY, and multivariate beats univariate across all four coins. The authors are explicit that the study addresses only prediction-error metrics — NO trading costs, NO portfolio/P&L performance, NO benchmarking against simple baselines.
- **Relevance to our system:** A useful, honestly-scoped crypto-specific data point that, by its own admission, can't speak to tradable edge — and that limitation is the lesson. Two takeaways align with our priors: (1) multivariate > univariate here echoes the cross-variate-structure theme ([67] TSMixer, [24][32]) — adding features can improve forecast accuracy — BUT for our single coin "multivariate" would mean extra channels (volume, on-chain, other coins), each of which adds leakage/manipulation surface ([34][35]) and none of which is shown to survive into cost-net return. (2) The authors' candor that they measure accuracy only, with no costs/baseline/P&L, is exactly the gap our gate fills: a better crypto price forecast is not evidence of a strategy that beats holding net of costs. Net: confirms (again) that the crypto-DL-prediction literature is overwhelmingly accuracy-only and cost-blind; this one is at least honest about it. Background; reinforces "demand cost-net + buy-and-hold."

### [83] TimeMAE: Self-Supervised Representations of Time Series with Decoupled Masked Autoencoders
- **Authors / Venue:** Mingyue Cheng, Qi Liu, Zhiding Liu, Hao Zhang, Rujiao Zhang, Enhong Chen (USTC) / WSDM 2026; arXiv 2023
- **Year:** 2023
- **Source:** arXiv:2303.00320
- **% read:** 35%
- **Summary:** A GENERATIVE (masked-reconstruction) self-supervised representation learner for time series — the masked-autoencoder counterpart to the contrastive methods [70][68]. Two ideas: (1) "semantic unit elevation" — mask non-overlapping SUB-SERIES windows rather than individual time steps, raising semantic density and cutting compute; (2) a DECOUPLED encoder that processes visible and masked regions separately, avoiding the train/inference mismatch caused by injecting artificial mask tokens into the main encoder. General-purpose representation learning; not finance-specific in the accessible text.
- **Relevance to our system:** On the "self-supervised representation learning" seam, and it pairs with the contrastive-vs-generative finding [61] — TimeMAE is the generative (MAE) branch, which [61] found better in LOW-LABEL regimes (i.e., the data-poor single-coin case). So IF we ever pretrained a representation on one coin, a sub-series-masked MAE like this would be the more sensible choice than contrastive. But the now-standard blockers dominate: no finance/trading/cost-net evaluation, the benchmarks are general time-series tasks, and a richer representation of a near-efficient single-coin price still need not produce alpha that survives costs ([24][32][8][67]). The transferable, low-risk nugget is the sub-series (patch) masking idea — semantically denser than point masking and cheaper — same spirit as PatchTST [22]. Filed under self-supervised representation learning (generative branch); conceptual reference only, not actionable.

### [85] FEDformer: Frequency Enhanced Decomposed Transformer for Long-term Series Forecasting
- **Authors / Venue:** Tian Zhou, Ziqing Ma, Qingsong Wen, Xue Wang, Liang Sun, Rong Jin (DAMO Academy, Alibaba) / ICML 2022
- **Year:** 2022
- **Source:** arXiv:2201.12740
- **% read:** 45%
- **Summary:** Completes the Informer [6] / Autoformer [7] long-horizon-transformer trilogy. Two ideas: (1) a seasonal-trend *decomposition* scheme (with a mixture-of-experts moving-average extractor) that separates a series into trend + seasonal so the Transformer handles fine detail while decomposition captures the global profile; (2) a *frequency-enhanced* block that operates in the Fourier (and wavelet) domain — exploiting that most time series are sparse in a Fourier basis, it randomly selects a fixed number of frequency modes, giving linear O(L) complexity. Reports 14.8% (multivariate) / 22.6% (univariate) error reduction over prior baselines across six standard benchmarks (ETT, electricity, traffic, weather, exchange-rate, illness). No financial/trading P&L or transaction-cost evaluation — pure forecasting-accuracy metrics.
- **Relevance to our system:** Background as an architecture, and it lands in exactly the spot the DLinear critique [8] targets: FEDformer is one of the elaborate transformers that DLinear's trivial linear baselines were shown to match or beat on these very benchmarks. So importing it into a single-coin advisor would add Fourier-domain machinery and overfitting surface for a forecasting-accuracy edge that (a) may not survive the linear-baseline comparison [8][22], (b) is measured on physical/utility series not crypto, and (c) is never converted to cost-net tradable return. The one cheap, durable idea — already noted for [7] — is seasonal-trend decomposition as a low-risk preprocessing primitive; the frequency-mode-selection trick is interesting but the "exchange-rate" benchmark is the closest it gets to finance and there's no trading evaluation. Net: filed under transformer-forecasting; skip the architecture, the decomposition idea is the only takeaway. Reinforces "beat the linear baseline first."

### [86] N-HiTS: Neural Hierarchical Interpolation for Time Series Forecasting
- **Authors / Venue:** Cristian Challu, Kin G. Olivares, Boris N. Oreshkin, Federico Garza, Max Mergenthaler-Canseco, Artur Dubrawski (CMU / Nixtla / Element AI) / AAAI 2023
- **Year:** 2022 (arXiv) / 2023 (AAAI)
- **Source:** arXiv:2201.12886
- **% read:** 45%
- **Summary:** The direct successor to N-BEATS [79] for LONG-horizon forecasting. Adds two ideas to the N-BEATS deep-stack-of-MLPs-with-residual-basis design: (1) *multi-rate signal sampling* — each block pools its input at a different rate before its MLP, so different stacks specialize on different frequencies/scales; (2) *hierarchical interpolation* — each block predicts at a different temporal resolution and the forecast is assembled by interpolation, drastically cutting the number of parameters needed for long horizons and reducing prediction volatility. Reports ~20% average accuracy improvement over the latest Transformers (Informer/Autoformer-class) while being ~50× faster, across the standard long-horizon benchmarks. No financial/trading or cost-aware evaluation.
- **Relevance to our system:** Background, and like N-BEATS [79] it is a useful counterweight that keeps our skepticism calibrated: a well-designed pure-MLP forecaster beats the heavy transformers AND is far cheaper — i.e., when DL forecasting genuinely helps, simplicity + good inductive bias (multi-rate, hierarchical interpolation) wins over attention complexity, the same message as DLinear [8] / TSMixer [67]. But the caveats are unchanged and decisive for us: the ~20% gain is forecasting ACCURACY on many heterogeneous/physical series, never cost-net trading P&L on one coin, and N-HiTS's strengths (long-horizon, smoothness, many series) don't map to a single near-random-walk crypto series where we make a few decisions. The transferable, low-risk nugget is again the multi-scale/hierarchical decomposition idea. Net: confirms "simpler-but-well-designed beats heavy transformers" while leaving intact "forecast-accuracy gains aren't tradable alpha for one coin." Filed under transformer-alternative forecasters.

### [84] Replicating Anomalies
- **Authors / Venue:** Kewei Hou, Chen Xue, Lu Zhang / Review of Financial Studies 33(5):2019–2133; NBER w23394
- **Year:** 2017 (NBER) / 2020 (journal)
- **Source:** NBER w23394 (also SSRN:2961979, RFS DOI:10.1093/rfs/hhy131)
- **% read:** 30%
- **Summary:** The landmark large-scale REPLICATION study of the cross-sectional return-predictability literature. The authors assemble 447 published "anomaly" variables and re-test them with a uniform, conservative methodology — NYSE breakpoints (to stop microcap stocks from driving results) and value-weighted returns. Result: **286 of 447 anomalies (64%) — including 93% of liquidity anomalies — are statistically INSIGNIFICANT at the 5% level**, and even the survivors have much smaller effect sizes than originally claimed. The authors attribute the failure to "widespread p-hacking" and conclude markets are more efficient than the literature implied.
- **Relevance to our system:** Not a deep-learning paper, but one of the most important methodological citations for our entire skeptical stance — it is the empirical proof, at scale, of what our gate assumes: that the large majority of published "edges" are artifacts of multiple testing / p-hacking / microcap-and-equal-weighting choices, and evaporate under conservative, consistent re-testing. This is the cross-sectional-equity analogue of running our bake-off and finding that almost nothing survives a frozen, honest gate. Three direct lessons: (1) when a bake-off tries MANY strategies, expect most "winners" to be false positives (the multiplicity problem [39]) — a strong argument for a selection-bias correction (Deflated-Sharpe/PBO) on top of our bootstrap; (2) value-weighting + proper breakpoints mattered enormously — the analogue for us is realistic costs + buy-and-hold benchmark, which similarly kill flattering artifacts; (3) "markets are more efficient than the literature implied" is, for a single liquid coin, essentially our validated thesis. Foundational support for the gate and for adding a multiple-testing correction. Background as a model (equities, no DL); methodologically central.

### [87] TimesNet: Temporal 2D-Variation Modeling for General Time Series Analysis
- **Authors / Venue:** Haixu Wu, Tengge Hu, Yong Liu, Hang Zhou, Jianmin Wang, Mingsheng Long (Tsinghua) / ICLR 2023
- **Year:** 2022 (arXiv) / 2023 (ICLR)
- **Source:** arXiv:2210.02186
- **% read:** 40%
- **Summary:** A task-general time-series backbone that reshapes a 1D series into a set of 2D tensors. It uses FFT to detect the dominant periods, then folds the series so that each 2D tensor's columns hold *intra*-period variation and its rows hold *inter*-period variation; an inception-style convolutional "TimesBlock" then processes these 2D maps (reusing computer-vision backbones). Reports state-of-the-art across FIVE tasks — long/short forecasting, imputation, classification, anomaly detection — against 15+ baselines on the standard benchmarks (ETT, electricity, traffic, weather, etc.). No financial/trading P&L or transaction-cost evaluation.
- **Relevance to our system:** Background as an architecture. TimesNet's whole premise is *multi-periodicity* — it shines on series with strong, stable periodic structure (electricity load, traffic) that FFT can cleanly extract. A single crypto coin's returns have weak, unstable, regime-dependent seasonality (no reliable diurnal/weekly cycle that survives out-of-sample — same point made for CoST [68]), so the 2D period-folding has little real structure to exploit, and the benchmarks are physical/utility series with no cost-net trading evaluation. It belongs to the same family as the other heavy transformers/CNNs the DLinear critique [8] warns about: impressive on periodic physical data, unproven (and likely overfit) on one thin near-random-walk financial series. The "reshape to 2D so vision backbones apply" idea is clever but irrelevant for us. Filed under transformer-era forecasters; not actionable, reinforces "complexity needs structure (periodicity/breadth) a single coin lacks."

### [88] iTransformer: Inverted Transformers Are Effective for Time Series Forecasting
- **Authors / Venue:** Yong Liu, Tengge Hu, Haoran Zhang, Haixu Wu, Shiyu Wang, Lintao Ma, Mingsheng Long (Tsinghua / Ant Group) / ICLR 2024
- **Year:** 2023 (arXiv) / 2024 (ICLR)
- **Source:** arXiv:2310.06625
- **% read:** 40%
- **Summary:** A minimal but influential rethink of transformers for forecasting, responding (like PatchTST [22]) to the DLinear [8] critique. It keeps the vanilla Transformer components but INVERTS the dimensions: instead of one token = all variates at one timestamp, each VARIATE'S whole series becomes one token. Attention then models cross-variate (cross-series) correlations and the feed-forward net learns each series' temporal representation. This fixes two problems the authors blame for transformers' weakness: meaningless attention from fusing unaligned variates into a token, and degradation with long lookbacks. Reports state-of-the-art on real-world multivariate benchmarks with better generalization across variates and lookback lengths. No trading/cost-aware evaluation; the abstract doesn't foreground a head-to-head vs DLinear.
- **Relevance to our system:** Background, and notably its entire advantage is CROSS-VARIATE: inverted attention helps precisely because it models correlations ACROSS many series — which is the multivariate/cross-sectional setting a single-coin advisor does not have ([24][32][67] make the same point from different angles). For one univariate coin, iTransformer's key mechanism (attention over variates) degenerates to a single token and offers nothing over a univariate model — and on univariate benchmarks the linear baseline [8] remains hard to beat. The recurring lesson holds: the deep-forecasting wins live in cross-variate breadth, and they are measured as forecast accuracy on physical data, never as cost-net crypto return. Filed under transformer-forecasting; confirms (again) "no cross-section, no transformer edge."

### [89] Visual Chart Representations for Cryptocurrency Regime Prediction: A Systematic Deep Learning Study
- **Authors / Venue:** Dustin M. Haggett / arXiv (cs.CV/q-fin)
- **Year:** 2026
- **Source:** arXiv:2605.00875
- **% read:** 50%
- **Summary:** A systematic, crypto-specific study of the "price-as-image + CNN" idea (cf. (Re-)Imag(in)ing Price Trends [17], DeepLOB-imaging). It compares three image encodings — raw rendered candlestick charts, Gramian Angular Field (GAF) summation images, and multi-channel GAF — across four vision models (a 4-layer CNN, ResNet18, EfficientNet-B0, ViT-Tiny) for predicting crypto market DIRECTION. Striking finding: the SIMPLEST setup wins — a 4-layer CNN on raw 128×128 candlestick charts with a 30-day lookback hits 0.892 AUC-ROC, while the mathematically "clever" GAF encodings collapse (GAF summation 0.310, multi-channel GAF 0.252 — barely above chance). ImageNet transfer learning helped the larger models by 4–16 points. Explicitly CLASSIFICATION-ONLY: the author openly states the work measures "discrimination ability rather than risk-adjusted returns after accounting for transaction costs and slippage," with full backtesting flagged as future work.
- **Relevance to our system:** A genuinely useful crypto-specific data point on the GAF/price-image seam, valuable for two honest lessons. (1) The much-hyped GAF encoding was DRAMATICALLY WORSE than a plain candlestick render (0.31 vs 0.89 AUC) — a concrete reminder that a fancier input transform is not automatically better and must be validated, the input-level analogue of the [34] "don't trust a component by downstream accuracy alone" warning. (2) Even the BEST result is a 0.89 AUC classification score with NO trading P&L, NO costs, NO buy-and-hold comparison — and the author is commendably explicit that discrimination ≠ profitability. This is exactly the accuracy→tradable-edge gap our cost-net, buy-and-hold-benchmarked gate exists to close: a 0.89 AUC directional classifier on crypto could easily earn nothing (or lose) once round-trip spread+fees and the implied turnover are paid (cf. [40][52]). Net: confirms (a) image-CNNs can discriminate crypto direction in-sample, (b) the GAF transform underperforms raw charts, and (c) none of it is shown to beat holding net of costs. Background-plus; reinforces "high AUC is not alpha — demand cost-net + buy-and-hold."

### [90] An Empirical Evaluation of Generic Convolutional and Recurrent Networks for Sequence Modeling (TCN)
- **Authors / Venue:** Shaojie Bai, J. Zico Kolter, Vladlen Koltun (CMU / Intel Labs) / arXiv (cs.LG); widely cited as the canonical TCN paper
- **Year:** 2018
- **Source:** arXiv:1803.01271
- **% read:** 40%
- **Summary:** The foundational Temporal Convolutional Network (TCN) paper — not finance-specific, but the source the trading literature cites whenever it uses a TCN backbone (e.g. DeepLOB [3], QuantGAN [9]). Defines the modern TCN: stacked DILATED CAUSAL convolutions (causal = no leakage from the future; dilation = exponentially growing receptive field) inside residual blocks. The systematic finding: across a broad suite of standard sequence-modeling benchmarks, this generic CNN OUTPERFORMS canonical RNNs (LSTM/GRU) while showing LONGER effective memory, more stable gradients, and parallelizable training. Concludes that "convolutional networks should be regarded as a natural starting point for sequence modeling," challenging the RNN-by-default habit. No financial/trading or cost-aware evaluation.
- **Relevance to our system:** Background as an architecture, but a useful baseline-discipline data point and the right reference for "why TCN, not LSTM." Two lessons. (1) The CAUSAL-convolution constraint is exactly the no-look-ahead discipline our backtesting demands — a clean architectural embodiment of "information only flows forward" (cf. the causality constraint in [38][60]); any sequence model we ever touched should be causal by construction. (2) The headline result — a generic CNN beats the more-complex/fashionable RNN — rhymes with the recurring theme that the simpler, well-designed model is a hard baseline ([8][67][86]); it's the RNN-era version of "don't assume the trendy architecture wins." But it's a sequence-modeling-ACCURACY result on non-financial benchmarks, with no cost-net trading evaluation, so it says nothing directly about tradable edge on one coin. If we ever needed a sequence model, TCN (causal, parallel, long memory) is a sane default over LSTM/GRU [55]. Filed under RNN/CNN backbones; conceptual reference.

### [91] Double Deep Q-Learning for Optimal Execution
- **Authors / Venue:** Brian Ning, Franco Ho Ting Lin, Sebastian Jaimungal (University of Toronto) / Applied Mathematical Finance (2021); arXiv 2018
- **Year:** 2018 (arXiv) / 2021 (journal)
- **Source:** arXiv:1812.06600
- **% read:** 40%
- **Summary:** A canonical deep-RL OPTIMAL-EXECUTION paper (liquidate/acquire a fixed quantity over a horizon while minimizing implementation shortfall / market impact). Uses a Double DQN (fully-connected net, experience replay, double-Q to curb value overestimation) whose state includes limit-order-book features and trading signals and whose action is how much to trade each step — a model-free alternative to the classical Almgren-Chriss continuous-time stochastic-control solution. Tested on real data for nine stocks; reports out-performance vs benchmark schedules (e.g. TWAP/AC-style) on mean/median shortfall, probability of out-performance, and gain-loss ratios.
- **Relevance to our system:** Background — execution scheduling (slicing a large parent order to minimize impact) is orthogonal to a €200 single-coin advisor that places a handful of taker trades and does not move the market. Two notes worth keeping. (1) Like [29], it's RL done relatively rigorously against a well-defined classical benchmark (Almgren-Chriss), which makes "did RL beat the principled baseline?" a meaningful question — contrast with the crypto-return papers that have no ground-truth optimum and often no benchmark at all. (2) The double-Q trick (two value estimates to fight overestimation bias) is the same stabilizer that makes SAC/TD3 beat vanilla DDPG [28] — a consistent, reusable lesson if continuous/discrete-action RL is ever used. The market-impact/implementation-shortfall objective is also the formal home of the cost component our flat-bps slippage approximates; if we ever modeled impact richly, this is the literature. Filed under execution RL; not actionable, mild cost-modeling relevance.

### [92] Frequency-domain MLPs are More Effective Learners in Time Series Forecasting (FreTS)
- **Authors / Venue:** Kun Yi, Qi Zhang, Wei Fan, Shoujin Wang, Pengyang Wang, Hui He, Defu Lian, Ning An, Longbing Cao, Zhendong Niu / NeurIPS 2023
- **Year:** 2023
- **Source:** arXiv:2311.06184
- **% read:** 40%
- **Summary:** Another entry in the "simple MLP beats heavy transformers" lineage ([8] DLinear, [67] TSMixer, [79][86] N-BEATS/N-HiTS), but operating in the FREQUENCY domain. Pipeline: (1) FFT each series into complex frequency components; (2) apply redesigned MLPs to the real and imaginary parts, at both inter-series (channel-wise) and intra-series (time-wise) scales; (3) inverse-transform back. Motivated by two frequency-domain properties — a "global view" (each frequency coefficient summarizes the whole signal, easing long-range dependence) and "energy compaction" (signal energy concentrates in a few frequencies, so the MLP focuses on the informative part). Reports consistent gains over state-of-the-art across 13 short- and long-term benchmarks. No financial/trading or cost-aware evaluation.
- **Relevance to our system:** Background as an architecture, and it reinforces our central forecasting-side guardrail. FreTS is yet more evidence that a well-designed SIMPLE model (here, MLPs on FFT coefficients) competes with or beats elaborate transformers — the same lesson as DLinear [8] and TSMixer [67], so it further deflates the case for importing transformer complexity into our advisor. The frequency-domain framing rhymes with FEDformer [85] and Autoformer's [7] auto-correlation, all exploiting spectral sparsity — but the "energy compaction" assumption is shakiest exactly where we operate: a near-random-walk crypto return series has a broad, flat-ish spectrum (white-noise-like) with little energy compaction to exploit, so the method's core advantage is weakest on our data. And, as ever, the gains are forecasting ACCURACY on physical/utility benchmarks, never cost-net trading return on one coin. Filed under simple-baseline forecasters; conceptual reference, reinforces "simple beats complex; spectral structure a single coin largely lacks."

### [93] Robust Multivariate Time-Series Forecasting: Adversarial Attacks and Defense Mechanisms
- **Authors / Venue:** Linbo Liu, Youngsuk Park, Trong Nghia Hoang, Hilaf Hasson, Jun Huan (AWS AI Labs) / ICLR 2023
- **Year:** 2022 (arXiv) / 2023 (ICLR)
- **Source:** arXiv:2207.09572
- **% read:** 40%
- **Summary:** Studies the adversarial ROBUSTNESS of (probabilistic) multivariate time-series FORECASTERS — the regression analogue of the adversarial-classifier and adversarial-RL-trading work [35][65]. The headline attack is subtle: an adversary can degrade the forecast of a TARGET series by making strategic, SPARSE, near-imperceptible modifications to the past observations of a small number of OTHER (correlated) series — exploiting the model's cross-series dependence. Proposes two defenses: an extension of randomized smoothing (certified robustness, adapted from classification) and an adversarial-training algorithm that generates adversarial examples while optimizing for robustness. Experiments on real-world datasets show both the attack's potency and the defenses' superiority over baselines.
- **Relevance to our system:** Background as a method (we run no multivariate forecaster), but a sharp, specific cautionary lesson for any move toward multi-feature / multi-series inputs. It demonstrates that the very thing that makes multivariate models powerful — cross-series dependence ([88] iTransformer, [82] multivariate-beats-univariate) — is also an ATTACK SURFACE: perturbing a correlated auxiliary series silently corrupts the target forecast. For crypto specifically this is alarming, because the natural "other series" one would add (other coins, on-chain metrics, sentiment) are cheap to manipulate (wash trades, spoofed on-chain flows, shill posts [35]), so a multivariate crypto forecaster would inherit a manipulable, fragile dependence on exactly the inputs an adversary can move. Combined with [35][65], the message is consistent: learned trading signals are brittle to small adversarial input changes, and adding more (manipulable) input channels increases — not decreases — fragility. Strong argument for preferring few, robust, hard-to-game price/volume features over a rich multivariate input web. Filed under adversarial robustness; reinforces input-parsimony and skepticism of alt-data channels.

### [94] Financial Vision Based Reinforcement Learning Trading Strategy
- **Authors / Venue:** Yun-Cheng Tsai, Fu-Min Szu, Jun-Hao Chen, Samuel Yen-Chi Chen / Applied System Innovation / AI (MDPI), 2022
- **Year:** 2022
- **Source:** arXiv:2202.04115
- **% read:** 35%
- **Summary:** Combines the price-as-image lane ([17][89]) with deep RL. Encodes recent price action as Gramian Angular Field (GAF) candlestick images, uses a CNN for "financial vision" feature extraction, and trains an RL agent to act on detected CURRENT candlestick PATTERNS (engulfing, morning star, etc.) — deliberately framing the task as present-pattern detection + entry/exit timing rather than future-price prediction (to dodge prediction delay), and emphasizing interpretability against the "black box" problem. Trained on 15-minute Ethereum data; reports (per the authors) superior trading performance vs the top-10 ETFs, and an interesting transfer-learning claim that an ETH-trained vision model transfers to US-stock trading. The accessible abstract does not surface an explicit transaction-cost model, a buy-and-hold-of-ETH benchmark, or a robustness/multi-path test.
- **Relevance to our system:** On the GAF/price-image + RL seam, crypto-adjacent (trained on ETH), and notable mainly for two things. The good: its explicit motivation toward INTERPRETABILITY (detecting named candlestick patterns rather than a black-box forecast) is the right instinct — a transparent rule is auditable and harder to silently overfit, aligning with our preference for simple, inspectable strategies over opaque learned ones ([34][65]). The cautionary: the benchmark is the top-10 ETFs, NOT buy-and-hold of the traded asset (ETH) over the same window — the same fatal-omission pattern as [28][56], and especially misleading for crypto where simply holding ETH often dominates; without that line the "superior performance" claim is uninterpretable, and there's no clear cost model or multi-path robustness. The cross-asset transfer claim (ETH→US stocks) is intriguing but unverified and, if real, more likely reflects generic trend/pattern structure than a tradable edge. Net: interpretable framing is a plus, but the evaluation fails our buy-and-hold-net-of-costs bar. Background; reinforces the gate and that candlestick-pattern signals must be benchmarked against holding.

### [95] Cryptocurrency Portfolio Optimization by Neural Networks
- **Authors / Venue:** Quoc Minh Nguyen, Dat Thanh Tran, Juho Kanniainen, Alexandros Iosifidis, Moncef Gabbouj (Tampere University / Aarhus University) / arXiv (q-fin.PM); IEEE-adjacent
- **Year:** 2023
- **Source:** arXiv:2310.01148
- **% read:** 40%
- **Summary:** A neural-network portfolio method tailored to crypto's structure: instead of allocating across many assets, the network constructs a portfolio of a PAIR of negatively-correlated assets and is trained end-to-end to maximize the Sharpe ratio (decision-objective, like Deep Momentum Networks [15]), with a novel regularization term to stop the model collapsing onto a single favored asset. Trained/evaluated on ~19 months of Binance data; the authors report backtest profits across different market situations. The accessible abstract does not pin down the exact architecture, the coin set, a transaction-cost model, or an explicit buy-and-hold/equal-weight benchmark.
- **Relevance to our system:** On the deep-learning crypto-portfolio seam, and conceptually a notch above the typical DRL-crypto paper for two reasons we value, though still not clean by our standard. The good: (1) it directly optimizes SHARPE as the training loss — the risk-adjusted decision-objective pattern that recurs in the better papers ([2][15][36][45]) — and (2) the anti-concentration regularizer is a sensible guard against the model just over-betting one coin (a mild echo of turnover/diversification penalties). The caveats keep it cautionary: it is inherently a PAIR / cross-sectional construction (needs a negatively-correlated counterpart), which a single-coin long-only advisor structurally cannot do — you can't build a negatively-correlated pair from one asset; and the abstract reports profits without a clearly stated cost model, buy-and-hold benchmark, or multi-path robustness over the short 19-month window (which spanned a strong crypto cycle where drift dominates). Net: the max-Sharpe objective + anti-concentration regularizer are reusable ideas, but the method (pairs) and the evaluation (no clear buy-and-hold/costs/bootstrap) don't transfer to or satisfy our single-coin gate. Background-plus.

### [96] DGDNN: Decoupled Graph Diffusion Neural Network for Stock Movement Prediction
- **Authors / Venue:** Zinuo You, Zijian Shi, Hongbo Bo, John Cartlidge, Li Zhang, Yan Ge (University of Bristol) / ICAART 2024
- **Year:** 2024
- **Source:** arXiv:2401.01846
- **% read:** 40%
- **Summary:** A graph-neural-network stock-movement predictor that responds to a key weakness of earlier GNN work ([11][33]): rather than using PREDEFINED static relations (sector, supply chain), it AUTOMATICALLY constructs DYNAMIC stock graphs via entropy-driven edge generation (a signal-processing view of which stocks are informationally linked at a given time), runs a generalized graph-diffusion process to learn task-optimal dependencies, and uses a decoupled scheme to capture hierarchical intra-stock features. Reports substantial improvements over state-of-the-art baselines on real-world datasets, with ablation/sensitivity studies; the accessible text does not confirm a cost-aware trading simulation or a buy-and-hold benchmark (evaluation appears classification-accuracy-oriented).
- **Relevance to our system:** Background — like all GNN work it is fundamentally CROSS-SECTIONAL (it models relationships AMONG many stocks), so it cannot apply to a single coin with no graph to build. But it's worth filing as the "dynamic, data-driven graph" answer to the [34] critique (which argued predefined static graphs are often uninformative and must be justified independently): DGDNN's entropy-driven, time-varying edges are a more principled construction than fixed sector links. Two transferable cautions, though. (1) An adaptively-learned graph is also a larger overfitting/leakage surface — letting the data choose the edges risks encoding future-correlated structure, exactly the hazard [34] warns of, so "dynamic graph" is not automatically safer. (2) As usual the result is movement-classification accuracy with no clearly stated cost-net P&L or buy-and-hold comparison, so it says nothing about tradable edge. Net: a methodologically interesting cross-asset technique for a future multi-coin extension; not actionable for one coin, and the accuracy claim is not a tradability claim. Filed under GNN/cross-asset.

### [97] Deep Reinforcement Learning in Cryptocurrency Market Making
- **Authors / Venue:** Jonathan Sadighian / arXiv (q-fin.TR)
- **Year:** 2019
- **Source:** arXiv:1911.08647
- **% read:** 35%
- **Summary:** An early, crypto-specific deep-RL MARKET-MAKING framework (DRLMM). Two policy-gradient agents (a feed-forward net as function approximator) act on a state built from limit-order-book data plus order-flow arrival statistics, comparing two reward-function designs; the core problem framed is the market maker's stochastic INVENTORY-CONTROL challenge (quote two-sided, earn the spread, avoid accumulating a dangerous directional position). Performance is evaluated by daily and average trade returns on cryptocurrency data. Positioned as a proof that deep RL can address inventory control in crypto market making.
- **Relevance to our system:** Background, and structurally NOT our activity — market making means continuously posting bid/ask limit orders to earn the spread while managing inventory (you profit from the bid-ask spread as a liquidity PROVIDER), whereas our advisor is a long-only liquidity TAKER deciding when to hold spot. So the strategy class is inapplicable (cf. [30]). The one cross-cutting note is conceptual: market-making rewards force an explicit trade-off between capturing spread and carrying inventory RISK, i.e. the objective is intrinsically risk-aware — the same theme as deep hedging [2][36] and the Sharpe-loss papers [15]. The crypto setting also underlines that the spread the maker EARNS is the same spread WE PAY as a taker — a reminder that the bid-ask spread is a first-order cost in crypto (someone is collecting it), reinforcing our cost-net discipline. The abstract offers no robustness caveats. Filed under market-making RL; not actionable, mild cost-intuition relevance.

### [98] TimeMixer: Decomposable Multiscale Mixing for Time Series Forecasting
- **Authors / Venue:** Shiyu Wang, Haixu Wu, Xiaoming Shi, Tengge Hu, Huakun Luo, Lintao Ma, James Y. Zhang, Jun Zhou (Ant Group / Tsinghua) / ICLR 2024
- **Year:** 2024
- **Source:** arXiv:2405.14616
- **% read:** 40%
- **Summary:** A fully MLP-based forecaster (no attention) that extends the simple-model + decomposition lineage ([8] DLinear, [67] TSMixer, [7][85] decomposition) with an explicit MULTISCALE design. Two blocks: Past-Decomposable-Mixing (PDM) decomposes the input at multiple sampling scales into seasonal + trend, then mixes seasonal components fine-to-coarse and trend components coarse-to-fine (microscopic detail at fine scales, macroscopic structure at coarse scales); Future-Multipredictor-Mixing (FMM) ensembles several scale-specific predictors. Reports state-of-the-art on long- and short-term forecasting benchmarks with good runtime efficiency. No financial/trading or cost-aware evaluation.
- **Relevance to our system:** Background as an architecture, and another reinforcement of our forecasting-side guardrail: the field's strongest recent forecasters keep turning out to be SIMPLE MLP + decomposition designs, not heavy transformers — TimeMixer, TSMixer [67], DLinear [8], N-HiTS [86] all make the same point, further deflating any case for transformer complexity in our advisor. The multiscale seasonal/trend mixing is the recurring cheap, low-overfitting primitive (decompose + multi-scale) we've noted repeatedly ([7][68][79]). But the now-familiar blockers are decisive: the gains are forecasting ACCURACY on physical/utility benchmarks with strong multiscale seasonality, never cost-net trading return; a single near-random-walk crypto series has weak, unstable seasonality at every scale, so the multiscale machinery has little to grip; and better forecast error ≠ tradable edge ([8][52][55]). Filed under simple-baseline forecasters; conceptual reference (decompose + multiscale), not actionable.

### [99] LSTM-ARIMA as a Hybrid Approach in Algorithmic Investment Strategies
- **Authors / Venue:** Kamil Kashif, Robert Ślepaczuk (University of Warsaw, Quantitative Finance Research Group) / arXiv (q-fin.TR/ST); later journal
- **Year:** 2024
- **Source:** arXiv:2406.18206
- **% read:** 55%
- **Summary:** A rigorously-built study of an LSTM-ARIMA hybrid (LSTM for long-range dependence, ARIMA for short-term linear structure) for algorithmic strategies on the S&P 500, FTSE 100, and CAC 40 over ~23 years (2000–2023), with buy-and-hold as the explicit benchmark, 0.1% transaction costs modeled, and walk-forward optimization (1000 train / 250 val / 250 test, non-anchored). On a risk-adjusted basis (a Modified Information Ratio combining return, volatility, and max drawdown) the LSTM-ARIMA hybrid APPEARS to beat buy-and-hold across all three indices (and especially in long-short form), with notably smaller drawdowns (e.g. S&P 500 long-only MaxDD 28.95% vs B&H 56.78%). BUT the authors are admirably honest about the catch: a paired t-test finds the hybrid's outperformance is NOT statistically significant in most scenarios (only plain ARIMA long-only reached p < 0.10), ARIMA alone often produced negative returns and lost to B&H, and results were sensitive to hyperparameters.
- **Relevance to our system:** A strong on-thesis citation and a near-model of our own evaluation discipline (it is from the same Warsaw group as [42][52], which produce some of the most benchmark-honest work in the literature). Its true lesson is the gap between an apparently-winning risk-adjusted backtest and STATISTICAL SIGNIFICANCE: the hybrid looks like it beats buy-and-hold, yet a paired t-test cannot reject "no real edge" — exactly the trap our 1000-path bootstrap weakest-link verdict and the multiple-testing/Deflated-Sharpe correction are built to expose ([39][69][84]). Two transferable points: (1) report the SIGNIFICANCE of any excess return, not just the point estimate — an attractive Information Ratio that fails a significance test is not a crownable edge; (2) the strategy's smaller drawdown vs B&H is a genuine risk-adjusted property worth weighing, but lower drawdown with insignificant excess return is still not "robustly beating holding." Also note the headline lift came mostly from the LONG-SHORT variant, which a long-only single-coin advisor can't run. Net: even a carefully-built, cost-aware, walk-forward DL hybrid does NOT significantly beat buy-and-hold — our thesis, reproduced with honest statistics on equity indices. Strong support for the gate and for adding a significance/Deflated-Sharpe layer. Background as a model (equities, long-short); methodologically kindred.

### [100] A Framework for Empowering Reinforcement Learning Agents with Causal Analysis: Enhancing Automated Cryptocurrency Trading (CausalReinforceNet)
- **Authors / Venue:** Rasoul Amirzadeh, Dhananjay Thiruvady, Asef Nazari, Mong Shan Ee (Deakin University) / arXiv (q-fin.TR/cs.LG)
- **Year:** 2023
- **Source:** arXiv:2310.09462
- **% read:** 40%
- **Summary:** Proposes CausalReinforceNet (CRN): rather than feeding an RL trading agent all raw indicators, it first uses a Bayesian network and a dynamic Bayesian network to do CAUSAL feature selection (identify which signals actually drive price moves) and then conditions the RL agent on that causally-filtered state. Two agents (built on two different RL algorithms) trade five "altcoins" (BNB, ETH, LTC, XRP, and USDT — note USDT is a stablecoin). The agents are compared against BOTH a buy-and-hold benchmark AND a baseline RL model; the authors report CRN is "consistently superior" in profitability, while explicitly noting "the level of effectiveness varies across different cryptocurrencies." The accessible abstract does not specify the RL algorithms, the exact margin over buy-and-hold, the transaction-cost model, the test period, or robustness/seed analysis.
- **Relevance to our system:** On the DRL-crypto seam, and a relatively better-behaved example because it DOES include a buy-and-hold benchmark (the comparison most of this literature omits — cf. [28][56][73]). Two genuinely useful angles. (1) The CAUSAL feature-selection idea is on the right side of our skepticism: explicitly testing which inputs carry real (causal, not merely correlational) signal before trading them is the disciplined alternative to the leaky kitchen-sink feature sets that drive spurious backtests ([34][37][39]) — it's a feature-level guard we'd endorse in spirit. (2) The candid admission that effectiveness "varies across cryptocurrencies" is itself on-thesis: an edge that appears on some coins and not others, on a single test window, is exactly the regime/asset-conditional fragility our 1000-path bootstrap weakest-link verdict is designed to catch — a per-coin lucky result is not a robust strategy. The decisive caveats for our standard: without a stated cost model, test period, or multi-path/seed robustness, the "beats buy-and-hold" claim is a single-path point estimate (cf. [47][77]) and untrustworthy as evidence — especially since the coin set includes a stablecoin (USDT barely moves, so "beating buy-and-hold USDT" is trivial and would flatter aggregate numbers). Net: the causal-feature-selection methodology is a transferable, on-thesis idea; the profitability claim does not meet our buy-and-hold-net-of-costs, multi-path bar. Background-plus; reinforces "select features by genuine signal, and one-window per-coin wins aren't robust edges."

