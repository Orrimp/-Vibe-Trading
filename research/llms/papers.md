# Papers — LLMs (likelihood-computing neural nets) in Finance

Ledger for the `llms` topic. One entry per paper, appended immediately after
reading. Source of truth for resume. See `../README.md` for the contract.

Focus — LLMs as likelihood-computing neural nets in finance: LLM trading agents
(FinGPT, FinMem, FinAgent, TradingAgents); LLM financial sentiment / news;
multi-agent LLM trading frameworks; LLM reasoning for finance; financial LLM
benchmarks (BloombergGPT, FinBen, StockBench); retrieval-augmented finance
(RAG). **Special focus (operator's open question):** has anyone trained an
LLM / foundation model directly **on financial (numeric) time series** rather
than text? → the **time-series foundation model** literature (TimeGPT,
Lag-Llama, Chronos, Time-LLM, MOMENT, TimesFM, Moirai, "LLMs are zero-shot
time series forecasters") and whether it is applied to / critiqued on
financial/crypto data. That thread has its own labeled sub-section in
`knowledge.md`.

Skeptical lens (per task): where do LLMs *genuinely* add value (narration,
news/context, research assistance) vs. hype? Any LLM-derived signal is treated
as suspect until validated on genuinely out-of-sample (post-training-cutoff)
data, **net of costs**, against buy-and-hold — abstract NLP accuracy is not
tradeable edge.

> **Provenance:** entries [1]–[14] were **migrated** (2026-06-27) from the
> retired `llm-and-evolution/papers.md` — the LLM-side papers only, renumbered
> from [1]. The genetic/evolutionary papers from that ledger went to
> `evolution/`. New papers ([15]+) are added in subsequent rounds.

---

### [1] FinGPT: Open-Source Financial Large Language Models
- **Authors / Venue:** Hongyang Yang, Xiao-Yang Liu, Christina Dan Wang / FinLLM Workshop @ IJCAI 2023 (Best Presentation Award); AI4Finance
- **Year:** 2023
- **Source:** arXiv:2306.06031
- **% read:** 40% (abstract + framework sections via abstract page)
- **Summary:** Proposes an open-source, *data-centric* financial LLM framework as a democratized alternative to the proprietary BloombergGPT. Core contribution is an automatic data-curation pipeline over Internet-scale financial text plus lightweight LoRA fine-tuning so the model can be cheaply re-tuned as markets/news drift (RLHF/RLSP for personalization). Targets downstream tasks: financial sentiment, NER, robo-advising, and algorithmic-trading signal generation. The paper is framed as infrastructure / a "stepping stone" — it reports the pipeline and tasks, NOT a controlled trading-PnL study.
- **Relevance to our system:** Two distinct uses. (a) The narration seam — a small LoRA-tuned open model could power our "why this one" explanations cheaply and locally. (b) Sentiment/NER as an *exogenous feature*, not a strategy on its own. Key caveat for us: the paper validates NLP capability and data plumbing, NOT that any of it beats buy-and-hold net of costs — exactly the gap our FROZEN gate exists to expose. Use FinGPT-style tooling for *context/narration*, never as an un-gated alpha source.

### [2] FinMem: A Performance-Enhanced LLM Trading Agent with Layered Memory and Character Design
- **Authors / Venue:** Yangyang Yu, Haohang Li, Zhi Chen, et al. / AAAI 2024 Spring Symposium (also arXiv)
- **Year:** 2023
- **Source:** arXiv:2311.13743
- **% read:** 70% (abstract + experiments/results via ar5iv HTML)
- **Summary:** An LLM trading agent with three modules: Profiling (persona + risk preference), a human-like layered Memory (shallow/intermediate/deep layers with different decay rates so daily news fades fast and 10-Ks persist), and Decision-making. Tested on 5 high-news-volume tickers (TSLA, NFLX, MSFT, AMZN, COIN), train Aug-2021→Oct-2022, test Oct-2022→Apr-2023, vs buy-and-hold, A2C/PPO/DQN DRL agents, generic Generative Agents, and FinGPT. Reported strong out-performance, e.g. TSLA +61.8% (Sharpe 2.68) vs B&H −18.6% (Sharpe −0.54); COIN +35.0% vs −30.0%; AMZN/MSFT/NFLX more marginal.
- **Relevance to our system:** The single most important caveat for us: results are on **only 5 tickers, hand-picked for highest news volume** (selection bias), over **one ~6-month bearish test window** where any agent that goes flat/short beats a falling-knife B&H — this is precisely the kind of small-N, single-path, favorable-window result our 1000-path moving-block bootstrap gate is designed to reject. The paper does **not** address look-ahead bias and trains on "future price data" to derive labels (asymmetry vs test). Layered-memory *idea* (decay-weighted context) is a genuinely interesting input-construction trick; the *trading claims* are not gate-credible evidence. Treat as: good agent-architecture inspiration, NOT proof LLMs beat holding.

### [3] TradingAgents: Multi-Agents LLM Financial Trading Framework
- **Authors / Venue:** Yijia Xiao, Edward Sun, Di Luo, Wei Wang / arXiv (Tauric Research; open-source)
- **Year:** 2024
- **Source:** arXiv:2412.20138
- **% read:** 70% (abstract + experiments via HTML v1)
- **Summary:** Models a trading firm as a society of specialized LLM agents — fundamental/sentiment/technical/news analysts, Bull vs Bear *researcher debate*, a trader, and a risk-management team — that communicate in natural language to reach a decision. Tested on AAPL/GOOGL/AMZN over Jun–Nov 2024 vs B&H, MACD, KDJ, RSI, ZMR, SMA. Reports eye-popping numbers: AAPL CR 26.6% (Sharpe 8.21, MDD 0.91%) vs best baseline 2.05%; similar for GOOGL/AMZN.
- **Relevance to our system:** A Sharpe of 8 with <1% max-drawdown is a giant red flag, not a selling point — it is the signature of an over-fit / leaked result, not a deployable edge. The setup has *every* failure mode our gate guards against simultaneously: 3 assets, one 5-month window, **no transaction costs**, and LLM training cutoffs (gpt-4o / o1-preview) that overlap the 2024 test period (the model may "remember" the price path). Useful to us ONLY as a design pattern: the *debate / multi-role decomposition* is a nice way to structure a **narration / research-assistant** layer (bull-case vs bear-case text for the operator). As an alpha engine it is exactly what our 1000-path bootstrap-vs-B&H gate exists to reject.

### [4] FinAgent: A Multimodal Foundation Agent for Financial Trading (Tool-Augmented, Diversified, Generalist)
- **Authors / Venue:** Wentao Zhang, Lingxuan Zhao, Haochong Xia, et al. / ACM SIGKDD 2024
- **Year:** 2024
- **Source:** arXiv:2402.18485
- **% read:** 70% (abstract + experiments via HTML v3)
- **Summary:** A multimodal (numeric + text + visual/chart-image) LLM trading agent with a market-intelligence module, a dual-level reflection module, diversified memory retrieval, and tool augmentation (it can call established indicators/expert strategies). Evaluated on 5 US stocks + ETHUSD, train 2022-06→2023-06, test 2023-06→2024-01, against 12 baselines (rule-based, LGBM/LSTM/Transformer, DQN/SAC/PPO, FinGPT, FinMem). Claims >36% avg profit improvement; headline 92.27% ARR on TSLA; beats B&H on ARR for all 6 assets.
- **Relevance to our system:** More baselines and more assets than TradingAgents — better, but still **single test window, costs only vaguely "considered," and GPT-4V's Apr-2023 cutoff overlaps the test window** (visual chart "analysis" could be memorization). Tellingly, on the one asset closest to ours (crypto, ETHUSD) it *underperforms* baselines and the authors admit their tools are stock-specialized — a direct caution that stock-tuned LLM agents do NOT transfer to crypto. The genuinely reusable idea is **tool-augmentation**: an LLM that *calls* vetted indicators rather than inventing signals. For us that maps to: LLM orchestrates/narrates over our gated strategy library, it does not itself emit ungated alpha.

### [5] BloombergGPT: A Large Language Model for Finance
- **Authors / Venue:** Shijie Wu, Ozan Irsoy, Steven Lu, et al. (Bloomberg) / arXiv
- **Year:** 2023
- **Source:** arXiv:2303.17564
- **% read:** 40% (abstract + corpus/eval framing)
- **Summary:** A 50B-parameter decoder-only LLM trained from scratch on ~708B tokens, ~363B of them proprietary financial text (FinPile) + ~345B general. Outperforms similarly-sized open models on financial NLP by large margins while staying competitive on general benchmarks. Evaluated purely on NLP: sentiment, NER, classification, ConvFinQA, plus standard LM benchmarks. Closed/proprietary (no weights released).
- **Relevance to our system:** Important *negative* data point: the flagship "finance LLM" paper makes **zero trading-PnL claims** — it proves better *language* understanding of finance, not better *returns*. Directly supports our thesis split: LLMs are validated for **text comprehension** (sentiment, extraction, narration — useful to our advisor's context/"why this one" layer) but NOT for alpha. Also a cost/closedness caution: a 50B from-scratch model is irrelevant to our local advisor; a small fine-tuned open model (FinGPT-style, [1]) is the realistic narration substrate.

### [6] Assessing Look-Ahead Bias in Stock Return Predictions Generated by GPT Sentiment Analysis
- **Authors / Venue:** Paul Glasserman, Caden Lin (Columbia) / arXiv (also J. of Financial Data Science)
- **Year:** 2023
- **Source:** arXiv:2309.17322
- **% read:** 50% (abstract + methodology summary)
- **Summary:** Tests whether GPT-based news-sentiment return prediction is contaminated by look-ahead bias (the model "knowing" how the stock later moved, because the headline + outcome were in its training data). Method: compare strategies on *original* headlines vs *anonymized* headlines (company identifiers stripped), in-sample vs out-of-sample. Surprising finding: in-sample, anonymized headlines *outperform* — a "distraction effect" (the model's general knowledge of the named company interferes) dominates raw look-ahead bias. Proposes anonymization as a de-biasing / out-of-sample backtesting procedure.
- **Relevance to our system:** Directly load-bearing for any LLM-sentiment feature we might add. Two concrete protocols for us: (1) if we ever feed news to an LLM for a signal, **test only on data after the model's training cutoff**, or use **anonymized text**, to keep the backtest honest; (2) be aware LLM sentiment is noisy in *both* directions (look-ahead inflation AND name-distraction). Reinforces that our gate must treat any LLM-derived signal as suspect until validated on genuinely out-of-sample (post-cutoff) data — abstract NLP accuracy is not enough.

### [7] FinBen: A Holistic Financial Benchmark for Large Language Models
- **Authors / Venue:** Qianqian Xie, Weiguang Han, Zhengyu Chen, et al. / NeurIPS 2024 Datasets & Benchmarks
- **Year:** 2024
- **Source:** arXiv:2402.12659
- **% read:** 50% (abstract + results framing)
- **Summary:** Largest open financial LLM benchmark at publication: 36 datasets, 24 tasks, 7 aspects — information extraction, textual analysis, QA, text generation, risk management, **forecasting**, and **decision-making / stock trading** (first benchmark to evaluate trading + agent + RAG settings). Headline finding is a *stark divide*: LLMs (incl. GPT-4) excel at IE and textual analysis but **struggle with forecasting and complex reasoning**; they "remain fundamentally constrained in quantitative prediction."
- **Relevance to our system:** This is the cleanest *independent, benchmark-grade* confirmation of our thesis split. The very paper that adds trading/forecasting evaluation concludes LLMs are strong on language, weak on numbers/forecasting. So: use LLMs for the text-shaped jobs in our advisor (extraction, summarization, the "why this one" narration) and **do not expect them to forecast price** — consistent with "no active strategy robustly beats holding." Also a reusable asset: FinBen's task taxonomy is a good menu for scoping *which* LLM jobs are even worth attempting.

### [8] Retrieval-Augmented LLMs for Financial Time Series Forecasting (FinSeer / StockLLM)
- **Authors / Venue:** Mengxi Liu, Yibo Wang, et al. / arXiv
- **Year:** 2025
- **Source:** arXiv:2502.05878
- **% read:** 75% (abstract + method + results + limitations via HTML)
- **Summary:** First RAG framework for financial time-series forecasting. A retriever (FinSeer) is trained via LLM-feedback / knowledge distillation to fetch historically *significant* sequences that help a small fine-tuned 1B model (StockLLM) predict next-day up/down. On ACL18/BIGDATA22/STOCK23 (US stocks), RAG beats bare StockLLM and text-trained retrievers — but the absolute accuracy is **~52–54% (barely above a coin flip)**, and crucially **text-trained retrievers often *degrade* performance**. The authors are honest: no profitability/cost/regime analysis; modest absolute accuracy.
- **Relevance to our system:** A careful, honest RAG-for-forecasting paper that still lands at ~54% directional accuracy with **no claim it translates to profit** — i.e. even "good" LLM forecasting is marginal and unproven on PnL. Two lessons: (1) generic text embeddings are the *wrong* retrieval substrate for numeric time series (they chase surface patterns) — a caution if we ever bolt RAG onto context; (2) directional accuracy ≠ tradeable edge: 54% can still lose to costs, exactly why our gate measures *equity vs B&H net of costs*, not hit-rate. Confirms RAG helps *retrieval/context*, not magically *forecasting profit*.

### [9] Can ChatGPT Forecast Stock Price Movements? Return Predictability and Large Language Models
- **Authors / Venue:** Alejandro Lopez-Lira, Yuehua Tang (Univ. of Florida) / arXiv + SSRN (widely cited)
- **Year:** 2023 (rev. through 2025)
- **Source:** arXiv:2304.07619
- **% read:** 60% (abstract + key-results discussion)
- **Summary:** The seminal "can an LLM read the news and predict returns" study. ChatGPT rates each headline good/bad/irrelevant; scores form a daily long-short portfolio. Finds a positive ChatGPT-score → next-day-return correlation; ChatGPT beats traditional sentiment dictionaries; smaller models (GPT-1/2, BERT) can't do it — return predictability is an *emergent* capability. Crucially, using **post-cutoff** headlines GPT-4 gets ~90% hit rate on the **non-tradable initial reaction** and *also* predicts a *tradable post-publication drift*, concentrated in **small stocks and negative news**. Returns **decline as LLM adoption rises** (price efficiency).
- **Relevance to our system:** The most careful pro-LLM-signal evidence — and even it is heavily hedged in ways that matter for us. The big hit-rate is on the *non-tradable* instantaneous move; the tradable part lives in *illiquid small-caps* (high cost/slippage) and is *decaying with adoption*. For a single-coin crypto advisor that's a triple caution: our asset is liquid+efficient, costs bite, and any edge self-erodes. Net: LLM news-sentiment is a *real but fragile, cost-sensitive, decaying* signal — plausibly worth a gated experiment as an exogenous feature, but absolutely not a standalone alpha, and must be tested post-cutoff + net of costs. Strongly consistent with our "no robust edge over holding" thesis.

### [10] FinLlama: Financial Sentiment Classification for Algorithmic Trading Applications
- **Authors / Venue:** Thanos Konstantinidis, Giorgos Iacovides, et al. / ACM ICAIF 2024 (also arXiv)
- **Year:** 2024
- **Source:** arXiv:2403.12285
- **% read:** 40% (abstract + architecture; trading-backtest specifics not in the excerpt)
- **Summary:** A finance-tuned sentiment model: LoRA fine-tune of Llama-2-7B on 34,180 labeled financial-text samples across 4 datasets, with a generator-discriminator scheme that outputs both sentiment *polarity* (3-class) and *strength*, feeding a small NN decision mechanism. Claims the sentiment signal supports "enhanced portfolio management decisions and increased market returns."
- **Relevance to our system:** Representative of the "fine-tune a small open LLM for finance sentiment" recipe that is the realistic substrate for any sentiment feature we'd add (and for narration, à la FinGPT [1]). The *NLP* contribution (polarity + strength via LoRA) is credible and cheap. But the trading claim is unquantified in the abstract — **no stated transaction costs, no explicit B&H comparison, no post-cutoff OOS framing** — i.e. the same evidentiary gap as the rest of the LLM-trading literature ([2][3][4][9]). Take-away for us: FinLlama-style models are a fine *engineering* choice for a sentiment/narration component, but the "increased returns" claim must be re-proven inside our cost-aware, post-cutoff, bootstrap-vs-B&H gate before we believe it. NLP accuracy ≠ tradeable edge (theme echoing [5][7]).

### [11] From Deep Learning to LLMs: A Survey of AI in Quantitative Investment
- **Authors / Venue:** (quant-AI survey group) / arXiv
- **Year:** 2025
- **Source:** arXiv:2503.21422
- **% read:** 55% (full HTML skim of taxonomy + critical sections)
- **Summary:** A landscape survey organizing AI-in-quant across three eras (statistical → deep learning → LLM agents) along the pipeline Data → Prediction → Portfolio Opt → Execution. Notably *circumspect*: states LLM deployment "is still in its early stages," that LLM-for-portfolio-optimization yields "modest outcomes so far," that DL models risk overfitting and lack transparency, and that "model predictions themselves cannot be directly used as investment decisions." Makes **no claim** that AI/DL/LLM reliably beats passive benchmarks; it is a taxonomy, not a validation study.
- **Relevance to our system:** A useful authoritative, *neutral-skeptical* anchor citation. Even a broad survey of the whole field declines to assert AI beats passive, and explicitly flags overfitting, the prediction→decision gap, and LLM immaturity — all consistent with our thesis and our gate's reason to exist. Two reusable framings for our docs: (1) the four-stage pipeline (Data→Prediction→PortfolioOpt→Execution) is a clean way to scope where our advisor uses AI (mostly *not* in prediction); (2) its honest "modest outcomes so far" is the field's own verdict on LLM alpha — supports keeping LLMs on narration/context, not signal generation. Critique: the survey *under*-examines costs and the backtest-vs-live gap, which our gate foregrounds.

### [12] Alpha-GPT 2.0: Human-in-the-Loop AI for Quantitative Investment
- **Authors / Venue:** Hang Yuan, Saizhuo Wang, Jian Guo, et al. / arXiv (follows Alpha-GPT, arXiv:2308.00016)
- **Year:** 2024
- **Source:** arXiv:2402.09746
- **% read:** 35% (abstract + framework positioning; full empirical detail not in excerpt)
- **Summary:** A multi-agent LLM system spanning the quant pipeline — specialized agents for alpha *mining*, *modeling*, and *analysis*, fronted by an "AlphaBot" LLM layer that translates human research intent into structured tasks and uses RAG over a vector DB of financial literature + historical alphas. The explicit design philosophy is **Human-in-the-Loop**: AI accelerates and orchestrates, the human researcher steers and judges throughout, rather than autonomous alpha generation.
- **Relevance to our system:** The most aligned paper with our actual LLM posture. It frames the LLM as a **research assistant / orchestrator** — interpreting intent, retrieving relevant prior work, driving tools — with a human in the loop to catch nonsense. That is exactly the safe, value-adding role for LLMs in our advisor: narrate, retrieve context, help the *operator* reason, while the **robustness gate (not the LLM)** decides what's real. The human-in-the-loop is, in effect, a manual analogue of our automated gate against spurious alphas. Caveat: the paper still operates in the alpha-mining paradigm and the abstract doesn't quantify whether the human-in-the-loop measurably reduces false discoveries. Reusable design: RAG-over-financial-literature + intent-translation is a clean blueprint if we expand our "why this one" seam into an interactive research helper.

### [13] StockBench: Can LLM Agents Trade Stocks Profitably in Real-world Markets?
- **Authors / Venue:** (StockBench group) / arXiv
- **Year:** 2025
- **Source:** arXiv:2510.02209
- **% read:** 70% (abstract + setup + results via HTML v2)
- **Summary:** A *contamination-clean* reality check: 14 frontier LLMs (GPT-5, Claude-4-Sonnet, o3, DeepSeek-V3, Qwen3, Kimi-K2, …) trade the top-20 DJIA stocks over Mar 3–Jun 30 2025 — a window **deliberately after the models' knowledge cutoffs to prevent leakage**. Agents see prices, fundamentals, and recent news; **costs/slippage are NOT modeled (making trading easier than reality)**. Result: B&H returned 0.4% (MDD −15.2%). Best model Kimi-K2 1.9%; most clustered ~2–2.5%; two models lost money. Crucial nuance: "most models outperform B&H" only in aggregate over a roughly flat/up window — **during downturns ALL LLM agents underperform the passive baseline.** Strong financial-QA ability "does not necessarily translate into effective trading."
- **Relevance to our system:** The single best capstone for our thesis on the LLM side — and the most rigorous LLM-trading evaluation in the set (post-cutoff = no leakage). Even so, the "win" over B&H is tiny (≈1–2%), happens in a benign window *without costs*, and **completely inverts in downturns** — i.e. once you add our transaction costs and a 1000-path bootstrap spanning bad regimes, the apparent edge would almost certainly vanish or go negative. Directly validates: (a) keep LLMs off the alpha rail; (b) our gate's regime-spanning bootstrap + cost-awareness is exactly the right test (StockBench's downturn result is what a single benign backtest hides); (c) QA/benchmark skill ≠ trading skill (echoes [5][7][11]). This is the paper to cite when asked "but can't a frontier LLM just trade?"

### [14] Standard Benchmarks Fail — Auditing LLM Agents in Finance Must Prioritize Risk
- **Authors / Venue:** (LLM-finance-audit group) / arXiv
- **Year:** 2025
- **Source:** arXiv:2502.15865
- **% read:** 45% (abstract + argument + framework)
- **Summary:** A position/audit paper arguing that accuracy- and return-based benchmarks give "an illusion of reliability" for LLM finance agents, masking vulnerabilities: **hallucinated facts, stale data, and adversarial prompt manipulation.** Proposes risk-first, multi-tier auditing (model / workflow / system level) and treating a "safety budget" as the primary success metric rather than performance.
- **Relevance to our system:** The governance/safety complement to the performance critique. For our advisor, an LLM in the loop (narration, news ingestion) introduces non-PnL risks our backtest gate does *not* catch: it can **hallucinate a "why this one" rationale, ingest stale/false news, or be manipulated by adversarial headlines.** Concrete implications: (1) any LLM narration must be *grounded* in the actual gated numbers (templated/constrained, not free-form invention); (2) any news/sentiment feed needs provenance + recency checks; (3) the LLM must never be on the decision path — it explains the gate's verdict, it doesn't make it. This paper justifies a hard architectural boundary: LLM = explanation/UX layer with its own safety checks; robustness gate = sole arbiter of what to trade.

### [15] Chronos: Learning the Language of Time Series
- **Authors / Venue:** Abdul Fatir Ansari, Lorenzo Stella, Caner Turkmen, et al. (Amazon) / arXiv (TMLR 2024)
- **Year:** 2024
- **Source:** arXiv:2403.07815
- **% read:** 70% (full ar5iv read — method, tokenization, both benchmarks, limitations)
- **Summary:** A foundation model that treats forecasting as *language modeling on numbers*: each series is **mean-scaled** (divide by mean absolute value of context) then **uniformly quantized into B=4,094 bins over [−15, +15]** (+ PAD/EOS = 4,096 vocab); standard **T5 encoder-decoder LMs (20M Mini / 46M Small / 200M Base / 710M Large**, plus a 90M GPT-2 variant) are then trained with plain cross-entropy on those tokens, context 512 → predict 64. Pretrained on public datasets + a **synthetic dataset from Gaussian processes (KernelSynth)** for generalization. Headline numbers (aggregated **relative-to-Seasonal-Naive** score, lower=better): in-domain (15 datasets) Chronos-Large WQL ≈ **0.72** / MASE ≈ 0.77 (beats AutoETS/AutoARIMA/DeepAR/PatchTST/TFT); **zero-shot (27 unseen datasets) WQL ≈ 0.87 (2nd overall) / MASE ≈ 0.89 (3rd)** — i.e. only **~13% better than Seasonal-Naive** zero-shot and merely *competitive* with per-dataset N-HiTS/PatchTST. A 1000-step fine-tune lifts even Chronos-Small to 1st. Open (amazon-science/chronos-forecasting).
- **Relevance to our system:** Directly answers the operator's question — yes, transformer "LLM-architecture" models are trained directly on **numeric** series; Chronos is the canonical open example. The full read sharpens the skepticism on three fronts. (a) **The ~13% zero-shot edge is over Seasonal-Naive, NOT a random walk** — and for crypto returns the relevant null *is* the random walk / buy-and-hold drift, which Seasonal-Naive isn't; the paper never benchmarks a random walk. (b) **A stated architectural limitation is decisive for crypto:** the fixed [−15s, +15s] bin range makes it "theoretically infeasible to model time series with a strong trend," it "underestimates trend when context is short," and small-variance series suffer quantization overflow/precision loss — crypto is trend-dominated with regime shifts, exactly the failure zone. (c) Benchmarks are general accuracy (WQL/MASE on energy/transport/weather), **no financial or crypto data in training or eval** (finance named only as an aspirational domain). Net: a real, deployable forecasting input *if* ever gated, but "competitive zero-shot WQL on energy" gives zero assurance on crypto returns net of costs — and its own trend-modeling limitation is a red flag for our asset. See knowledge.md special-focus section.

### [16] Large Language Models Are Zero-Shot Time Series Forecasters (LLMTime)
- **Authors / Venue:** Nate Gruver, Marc Finzi, Shikai Qiu, Andrew Gordon Wilson (NYU) / NeurIPS 2023
- **Year:** 2023
- **Source:** arXiv:2310.07820
- **% read:** 70% (full ar5iv read — encoding, results, GPT-4 finding, mechanism, limitations)
- **Summary:** Shows *general-purpose* LLMs (GPT-3, LLaMA-2-70B), **no fine-tuning**, zero-shot extrapolate series "comparable to or exceeding" purpose-built models. Mechanism (full read): encode the series as **digit strings with spaces forcing per-digit tokenization** (GPT-3: "1 2 3 , 1 2 3 0"; LLaMA-2: no spaces), affine-rescaled to center ~1; the discrete digit distribution becomes a **flexible continuous density** that can fit heavy-tailed/multimodal shapes. On Darts/Monash/Informer it beats **ARIMA, TCN, N-BEATS, N-HiTS, Spectral-Mixture GP, PromptCast** on NLL/CRPS/MAE. Success attributed to an **Occam/simplicity bias + repetition bias** matching periodic/seasonal structure. Two decisive caveats: **GPT-4 *underperforms* GPT-3** (altered tokenization can't force per-digit splits + **RLHF degrades uncertainty calibration**; chat variants underperform base models); and the authors concede **"textual pattern identification doesn't fully connect to numerical extrapolation,"** plus weakness at arithmetic/compositional trends. Validation includes **TSMC stock data post-training-cutoff** (not crypto).
- **Relevance to our system:** The seminal "text LLM forecasts numbers" result — the *opposite* approach to Chronos [15] (no numeric pretraining, just digit-encoding). The full read makes the crypto-skepticism concrete on three points. (1) **The whole mechanism — simplicity + repetition + seasonality bias — is precisely what crypto returns LACK** (near-random-walk, weak/absent seasonality); LLMTime wins *because* the test series are periodic, the property our asset doesn't have. (2) **RLHF/alignment HURTS number forecasting** (GPT-4 < GPT-3, chat < base) — a direct warning that the frontier chat models we'd actually run are *mis-calibrated* for numbers, doubly so for a risk overlay (cf. [83]). (3) The win is **likelihood/CRPS on generic benchmarks, never PnL** — and the authors' own "text patterns don't connect to numeric extrapolation" admission foreshadows the reasoning debunks [78]. Strong support that LLM zero-shot forecasting does NOT transfer to crypto returns; a hypothesis to gate, not assume — and if ever tried, use a *base* (not chat/RLHF) model.

### [17] Lag-Llama: Towards Foundation Models for Probabilistic Time Series Forecasting
- **Authors / Venue:** Kashif Rasul, Arjun Ashok, Andrew Robert Williams, et al. / arXiv (also TS-foundation-models site)
- **Year:** 2023 (v3 Feb 2024)
- **Source:** arXiv:2310.08278
- **% read:** 65% (full ar5iv read — architecture, corpus breakdown, CRPS tables, finance result, limitations)
- **Summary:** The **first open-source TSFM**. A small LLaMA-style **decoder-only transformer (8 layers, 9 heads, 16 dim/head, context 32)** for *univariate probabilistic* forecasting: each token concatenates **lagged values** (at lag set ℒ) + **date-time covariates** (second-of-minute … quarter-of-year), RMSNorm + RoPE, robust median/IQR standardization, and a **Student-t head** (3 params). Pretrained on **27 datasets / 6 domains / 7,965 series / 352M data windows**. Crucially (full read): the "finance" content is **just 4 economics/banking datasets**, of which the financial one is **"Exchange Rate" (8 currencies, daily, 1990–2016) — no crypto, no equities.** Results (CRPS, avg rank lower=better): **zero-shot avg rank 6.714 (worse than the TFT baseline at 5.0)**; only after **fine-tuning** does it lead (avg rank 2.786, SOTA on 3 sets). On the one financial set, Exchange-Rate: fine-tuned **0.009** (best), zero-shot 0.011 (comparable). Open data/models/code.
- **Relevance to our system:** A concrete "yes" to the operator's question — numeric (lags), not text — but the full read materially *lowers* its standing for us. (a) **Zero-shot it is actually WORSE than a supervised TFT baseline (rank 6.7 vs 5.0)** — the "strong zero-shot" framing only holds *after* fine-tuning; off-the-shelf it underperforms, echoing [27][80]. (b) **Its "finance" exposure is one daily FX dataset** — not crypto, not even equities — so any claim it "knows finance" is thin; FX daily is far calmer than crypto. (c) It is *univariate probabilistic* scored on CRPS, never PnL-vs-B&H; and for near-random-walk crypto returns the honest Student-t output is "wide distribution centered near no-change" = exactly what B&H already encodes. Net: historically important as the first open TSFM, but its own numbers (zero-shot < TFT; finance = FX only) argue *against* expecting crypto return-edge — fine-tuning would be mandatory and even then the prior ([19][27][28]) is vol-not-direction.

### [18] A Decoder-Only Foundation Model for Time-Series Forecasting (TimesFM)
- **Authors / Venue:** Abhimanyu Das, Weihao Kong, Rajat Sen, Yichen Zhou (Google Research) / ICML 2024
- **Year:** 2023 (ICML 2024)
- **Source:** arXiv:2310.10688
- **% read:** 70% (full ar5iv read — architecture, corpus, all three benchmark tables, limitations)
- **Summary:** Google's TSFM: a **patched-decoder** transformer (200M params: 20 layers, 16 heads, dim 1280; **input patch 32 → output patch 128** timepoints, so few autoregressive steps), pretrained on ~**100B time-points**. Corpus composition (full read): **~100B Wikipedia pageviews** + ~1B Google-Trends points (22k queries) + M4/Electricity/Traffic/Weather + **3M synthetic series** (ARMA/seasonal/trend/step), mixed **40% real / 60% synthetic**. Zero-shot results: **Monash (18 sets)** top-3, ~10% better than LLMTime(GPT-3); **Darts (8)** within significance of the best (seasonal ARIMA); **ETT** best/tied with PatchTST (ETTh1 h=96 MAE 0.37 vs PatchTST 0.41, LLMTime 0.42). Naive = "predict last value"; scaled-MAE normalizes by it. Open (Apache-2.0).
- **Relevance to our system:** The most *deployable* TSFM (small, open, Apache-2.0) and a confirming "yes" to the operator's question. The full read adds two decisive specifics. (1) **There IS a Bitcoin datapoint** — in the Monash table TimesFM scores MAE **1.97e18 vs naive 5.32e18 on the Monash Bitcoin set**; but this is a *price-level* MAE on a tiny/odd Monash series (note the 1e18 scale), measured only against the last-value naive, **not** a return/PnL or random-walk-on-returns test — so it tells us nothing tradeable (the level-persistence trap, cf. [63]). (2) **The pretraining corpus is web-traffic + synthetic, with NO financial/crypto data**, and the model "cannot incorporate covariates," is "univariate point forecasting" only, and ignores date features — all limits for a crypto risk model. Its selling point is *matching* supervised models zero-shot while small, not beating them. Net: the realistic substrate *if* we ever gate a forecaster, but "competitive zero-shot MASE on weather/Wikipedia" — and a price-level Bitcoin MAE beating last-value — give zero assurance of return-edge net of costs. The finance-applied critiques ([19][27][28][64][83]) remain the decisive evidence.

### [19] Foundation Time-Series AI Model for Realized Volatility Forecasting
- **Authors / Venue:** Anubha Goel, Puneet Pasricha, Martin Magris, Juho Kanniainen / arXiv
- **Year:** 2025
- **Source:** arXiv:2505.11163
- **% read:** 55% (abstract + method + results framing via abstract page)
- **Summary:** Directly applies a **time-series foundation model (TimesFM [18])** to a *financial* forecasting task — **realized volatility** — testing it both zero-shot and with a custom **incremental-learning fine-tune**, against econometric benchmarks (HAR/GARCH-family). Key result: the **pretrained zero-shot model is only "a reasonable baseline"**; the **fine-tuned** variants statistically outperform traditional models (Diebold-Mariano / Giacomini-White tests). The stated conclusion is that **incremental fine-tuning is *essential*** for the model to learn volatility patterns — zero-shot alone is not enough.
- **Relevance to our system:** One of the most decisive papers for the operator's open question. It is a *finance* application of a numeric-pretrained TSFM, and its honest finding is exactly the skeptical one: **zero-shot foundation-model forecasting of a financial series is merely OK; you must fine-tune (with care) to beat plain econometric baselines.** Two lessons for us: (a) don't expect an off-the-shelf TSFM to forecast crypto vol/returns better than simple models without adaptation; (b) even the *win* here is on **volatility** (a genuinely forecastable, persistent quantity) — *not* on directional return / PnL, which is the near-random-walk part that matters for beating buy-and-hold. Supports using such a model, if at all, for a *risk/vol-sizing* input (forecastable) rather than a return/alpha signal — and only after our cost-aware gate vets it.

### [20] Generalisation Bounds of Zero-Shot Economic Forecasting using Time Series Foundation Models
- **Authors / Venue:** Jittarin Jetwiriyanon, Teo Susnjak, Surangika Ranathunga / arXiv
- **Year:** 2025
- **Source:** arXiv:2506.15705
- **% read:** 55% (abstract + findings + caveats via abstract page)
- **Summary:** Evaluates three TSFMs — **Chronos [15], TimeGPT, and Moirai** — zero-shot on *economic* series, focusing on **data-scarce regimes and structural breaks.** Finds that "appropriately engineered" TSFMs can internalize economic dynamics and give well-behaved uncertainty out of the box, **matching or exceeding classical models under stable conditions** with no fine-tuning. The critical caveat: they show **"vulnerability to degradation during periods of rapid shocks"** — i.e. they break exactly when conditions shift abruptly. Verdict: zero-shot TSFM deployment is viable for *stable*-period monitoring but needs caution in volatile episodes.
- **Relevance to our system:** Reinforces [19] with a multi-model economic study and pins down the failure mode that matters most for crypto: **TSFMs are competitive in calm regimes and degrade during shocks/regime breaks.** Crypto is *dominated* by shocks and regime breaks — the precise condition under which these models are weakest, and the precise condition where beating buy-and-hold would actually be valuable (crash avoidance). So a zero-shot TSFM gives you accuracy when you don't need it (calm) and fails when you do (crash). Strong, finance-adjacent support for our thesis. Methodologically the "generalisation under structural breaks" framing maps onto our regime-spanning 1000-path bootstrap — both are ways to stop calm-period accuracy from masquerading as a robust edge.

### [21] MOMENT: A Family of Open Time-series Foundation Models
- **Authors / Venue:** Mononito Goswami, Konrad Szafer, Arjun Choudhry, et al. (CMU AutonLab) / ICML 2024
- **Year:** 2024
- **Source:** arXiv:2402.03885
- **% read:** 70% (full ar5iv read — architecture, corpus, all four task tables, anomaly detail, limitations)
- **Summary:** A family of open foundation models for general-purpose TS **analysis**. Architecture (full read): **T5-style encoder**, **Small ~40M / Base ~125M / Large ~385M**, with **masked-patch reconstruction** pretraining (T=512 → N=64 patches of P=8, mask 30%, learnable [MASK], reversible instance-norm, channel-independent). Corpus = **"Time Series Pile," 100K+ series** from 4+ repos (9 long-horizon sets incl. **Exchange** rates, 58 Monash, 159 UCR classification, 1980 TSB-UAD anomaly). Per-task results: **long-horizon forecasting** near-SOTA via linear probing (2nd to PatchTST); **classification** mean acc **0.794** on UCR (beats GPT4TS 0.567, but TS2Vec 0.852 better); **anomaly detection** adj-F1 **0.679** linear-probe / 0.636 zero-shot (beats GPT4TS 0.444, TimesNet 0.562 — but plain **k-NN marginally better on VUS-ROC**, 0.726 vs 0.715); **imputation** lowest MSE on ETT. Tellingly, **zero-shot short-horizon forecasting underperformed — ARIMA/ETS/Theta were superior.** Open on HuggingFace.
- **Relevance to our system:** Broadens the operator's-question answer beyond forecasting — numeric-pretrained TSFMs also do *anomaly detection*, which is arguably more useful to a risk-aware advisor than point forecasting. The full read sharpens the verdict: (a) the **one genuinely interesting capability for us is anomaly/regime detection** (adj-F1 0.679, beats the LLM-based GPT4TS) — a possible "de-risk on anomalous regime" *risk* signal; **but** even there a trivial **k-NN ties/beats it on VUS-ROC**, so the foundation model earns little over a simple detector. (b) **Zero-shot *forecasting* loses to ARIMA/ETS** — confirming the cluster verdict that off-the-shelf TSFM point-forecasting is weak (and the authors themselves say "fine-tune with in-domain data before high-stakes use"). (c) Only "finance" data is daily Exchange rates; **no crypto.** For us: anomaly detection is the seam worth a *gated* experiment, but the bar (beat k-NN, beat ARIMA, then survive the cost-aware bootstrap-vs-B&H) is high and point-forecasting is the part least likely to help against holding.

### [22] Unified Training of Universal Time Series Forecasting Transformers (Moirai)
- **Authors / Venue:** Gerald Woo, Chenghao Liu, Akshat Kumar, Caiming Xiong, Silvio Savarese, Doyen Sahoo (Salesforce AI) / ICML 2024 (Oral)
- **Year:** 2024
- **Source:** arXiv:2402.02592
- **% read:** 70% (full ar5iv read — architecture, LOTSA domain breakdown, CRPS tables, limitations)
- **Summary:** Salesforce's **Moirai** — a patch-based **masked-encoder** Transformer for *universal* forecasting with **"any-variate attention"** (flattens multivariate series into one sequence + binary same-/cross-variate attention biases + RoPE) and multi-patch-size projections per frequency. Sizes: **Small 14M / Base 91M / Large 311M**. Pretrained on **LOTSA = 27,646,462,733 observations across nine domains.** Decisive full-read detail: the domain split is **Energy 59.2%, Transport 17.7%, Climate 15.2%, CloudOps 5.5%, Web 1.6%, Sales 0.7%, Nature 0.09%, Economics/Finance 0.10%, Healthcare 0.01%** — i.e. **finance is just ~0.10% (≈24.9M of 27.6B obs).** Zero-shot CRPS is competitive (best/2nd on 5/7 sets, e.g. Electricity 0.050 vs TiDE 0.048; Solar 0.406 *beats* TiDE 0.420; but Walmart 0.098 vs PatchTST 0.077 — loses). "Bitcoin" appears only inside the Monash table, with no separate result. Authors note "little to no hyperparameter tuning" and a "heuristic" multi-patch design. Open (uni2ts).
- **Relevance to our system:** The most *multivariate-capable* TSFM — but the full read deflates the "trained on finance" framing: **finance is a rounding-error 0.10% of LOTSA**, and the only crypto presence (Bitcoin) yields no reported result. So while the operator's-question answer is technically "yes, some financial data," Moirai is overwhelmingly an energy/transport/climate model. The any-variate ability is theoretically attractive (BTC + on-chain/funding covariates), but: (a) zero-shot CRPS is competitive-not-dominant and it *loses* to per-dataset PatchTST on the retail set; (b) it is one of the three models [20] found **degrades under shocks** — the crypto-relevant failure; (c) probabilistic CRPS ≠ tradeable edge net of costs; (d) a single-coin advisor rarely needs multivariate machinery. SOTA general TSFM to *know*, not evidence it beats holding crypto.

### [23] Time-LLM: Time Series Forecasting by Reprogramming Large Language Models
- **Authors / Venue:** Ming Jin, Shiyu Wang, Lintao Ma, et al. / ICLR 2024
- **Year:** 2023 (ICLR 2024)
- **Source:** arXiv:2310.01728
- **% read:** 70% (full ar5iv read — reprogramming mechanism, results, the authors' own ablation, limitations)
- **Summary:** Reprograms a **frozen** text LLM (default **Llama-7B**, also GPT-2) for forecasting: a small set of **text prototypes** (linear-probed from the LM's embeddings, e.g. "short up"/"steady down") with **multi-head cross-attention** mapping patches → language cues, plus **Prompt-as-Prefix (PaP)** prepending dataset context + task instructions + input statistics (trend, top-5 lags). Only the input-transform + output-projection train (**<6.6M params, ~0.2% of Llama-7B**); the backbone is frozen. Standard-benchmark wins: averaged over 8 datasets **MSE 0.408 vs GPT4TS 0.465 / DLinear 0.422**; few-shot (10% data) ~5–33% over baselines; zero-shot ~22% over GPT4TS. **Crucially, the authors' OWN ablation (full read) claims the LLM matters:** Llama vs GPT-2 backbone = **14.7% MSE reduction**, Llama-32-layer vs Llama-8-layer = 14.5%, and "the reprogramming layer alone (no LLM) achieves only baseline performance." No finance/crypto data (electricity/weather/traffic/illness/M4 only).
- **Relevance to our system:** The leading example of the **text-LLM-reprogramming** branch (vs numeric-pretrained TSFMs). The full read surfaces the load-bearing *conflict at the heart of the operator's question*: **Time-LLM's own ablation says the frozen language backbone contributes ~15%** (bigger backbone = better) — yet the rigorous independent ablations [25] (NeurIPS-24 Spotlight) and [73] (random-init = pretrained) **directly refute exactly this**, showing the LLM ablates out with no loss once you control properly. The discrepancy is the whole story: a method paper's in-house ablation that *removes* the LLM but keeps it under-powered will "show the LLM helps," whereas a controlled from-scratch-equivalent baseline shows it does not. For us this means Time-LLM is the *least* likely TSFM-branch to help on crypto (no finance eval, debunked premise, and a ~7B frozen Llama is far heavier than a small dedicated model that matches it). Catalog as background; **[25]/[73] are the binding evidence over Time-LLM's own ablation.**

### [24] TimeGPT-1
- **Authors / Venue:** Azul Garza, Cristian Challu, Max Mergenthaler-Canseco (Nixtla) / arXiv
- **Year:** 2023
- **Source:** arXiv:2310.03589
- **% read:** 40% (abstract + framing via abstract page + Nixtla/search summary)
- **Summary:** The first *commercial* time-series foundation model: an **encoder-decoder transformer** (positional encoding + multi-head attention) for **zero-shot** forecasting and anomaly detection, served via API. Per Nixtla it is trained on **>100B time-series data points across domains including finance, healthcare, weather, demographics, and IoT/transport.** Claims zero-shot inference beats established statistical, ML, and DL methods on performance, efficiency, and simplicity. (Closed weights; API/SaaS product.)
- **Relevance to our system:** Directly relevant to the operator's question — TimeGPT's training set *explicitly includes finance*, so the answer is unambiguously "yes, models have been trained on financial numeric series." But it's closed/SaaS (a poor fit for our local Rust advisor vs the open Chronos/TimesFM), and the headline is again *general* zero-shot accuracy, not crypto-trading PnL. Crucially TimeGPT is one of the models scrutinized in the independent benchmark critiques ([20] economic, and the "how foundational" study logged below): those find it competitive in stable regimes but *not* a reliable winner, especially under shocks. So: a real, finance-trained TSFM exists and is purchasable, but there is no gate-credible evidence it beats buy-and-hold on crypto net of costs — and being closed-source it's the wrong substrate for us anyway.

### [25] Are Language Models Actually Useful for Time Series Forecasting?
- **Authors / Venue:** Mingtian Tan, Mike A. Merrill, Vinayak Gupta, Tim Althoff, Thomas Hartvigsen / NeurIPS 2024 (Spotlight)
- **Year:** 2024
- **Source:** arXiv:2406.16964
- **% read:** 60% (abstract + ablation design + headline results via abstract page + search summary)
- **Summary:** A rigorous **ablation** of three popular LLM-for-time-series methods (incl. Time-LLM-style reprogramming). Devastating headline: **removing the LLM component — or replacing it with a trivial attention layer — does NOT degrade forecasting; in most cases it *improves*.** Pretrained LLMs do no better than from-scratch models, do not represent sequential dependencies, and do not help in few-shot settings. Plain patching + attention matches LLM-based forecasters while being **up to 3 orders of magnitude cheaper** in train/inference time.
- **Relevance to our system:** The single most important skeptic paper for the operator's question, on the *text-LLM-reprogramming* branch. It directly debunks the premise that a **language** model contributes anything to numeric forecasting: the gains attributed to LLMs come from the surrounding patch/attention machinery, not the language prior. For our advisor this means: (1) the "reprogram a chat LLM to forecast crypto" idea (Time-LLM [23], LSTPrompt) is almost certainly not worth the cost/complexity — a small dedicated model is cheaper and as good; (2) it sharpens the distinction between *purpose-built numeric TSFMs* (Chronos/TimesFM — these at least learn from numbers) and *text-LLM repurposing* (debunked here). Strongly reinforces keeping LLMs on the *language* jobs (narration/news) and **not** the forecasting/decision rail.

### [26] How Foundational are Foundation Models for Time Series Forecasting?
- **Authors / Venue:** Nouha Karaouli, Denis Coquenet, Elisa Fromont, Martial Mermillod, Marina Reyboz / NeurIPS 2025 Workshop (TS Foundation Models)
- **Year:** 2025
- **Source:** arXiv:2510.00742
- **% read:** 50% (abstract + critical findings via abstract page)
- **Summary:** A critique of how "foundational" TSFMs really are. Two key findings: (1) a TSFM's **zero-shot ability is tightly tied to the domains it was pretrained on** (it generalizes far less than the "foundation model" branding implies); (2) on genuinely unseen data, even **fine-tuned** TSFMs **do not consistently beat smaller dedicated models** once you account for their far larger parameter count / memory. Implication: TSFMs underperform under distribution shift and offer no clear advantage for their cost.
- **Relevance to our system:** The purpose-built-TSFM counterpart to [25]'s LLM-reprogramming debunk — together they bracket the whole field. The "zero-shot is tied to pretraining domains" point is decisive for crypto: unless a TSFM was heavily pretrained on *crypto-like* high-vol, fat-tailed, regime-switching data, its zero-shot crypto forecasts will be weak — and [20] already showed TSFMs degrade under shocks, which dominate crypto. Bottom line for the operator's question: foundation models for numeric series **exist and are real**, but the independent evidence ([20][25][26]) is that they are *competitive-not-dominant*, *domain-bound*, *shock-fragile*, and *not* cost-justified over small dedicated models — so there is no credible basis to expect one to beat buy-and-hold on crypto net of costs. Our gate would be the arbiter, but the prior is strongly negative.

### [27] Re(Visiting) Time Series Foundation Models in Finance
- **Authors / Venue:** Eghbal Rahimikia, Hao Ni, Weiguan Wang / arXiv
- **Year:** 2025
- **Source:** arXiv:2511.18578
- **% read:** 55% (abstract + key findings via abstract page + search summary)
- **Summary:** Billed as the **first comprehensive empirical study of TSFMs in global financial markets**, on a large-scale dataset of **daily excess returns across diverse markets.** The central, stark finding: **off-the-shelf generic pretrained TSFMs perform *poorly* in finance — in both zero-shot AND fine-tuning settings — whereas models pretrained *from scratch on financial data* achieve substantial forecasting *and economic* improvements.** Further gains come from larger data, synthetic augmentation, and hyperparameter tuning. Frames finance data as noisy, non-stationary, heterogeneous.
- **Relevance to our system:** Possibly the single most direct answer to the operator's question. It says, with the biggest financial sample to date: the famous general-purpose TSFMs (Chronos/TimesFM/Moirai/etc.) **do NOT transfer to finance off-the-shelf** — you have to *retrain from scratch on financial data* to get an edge. For our advisor that is a double-edged result: (a) it confirms generic zero-shot TSFM forecasting of crypto is a dead end (consistent with [20][26]); (b) it leaves open that a *finance-specialized* from-scratch model *might* help — but building/retraining one is far beyond a retail single-coin advisor's scope, and the paper's "economic improvements" are still measured as forecasting/portfolio metrics on equities, not as crypto PnL surviving a 1000-path cost-aware bootstrap-vs-B&H gate. Net: don't expect off-the-shelf TSFMs to work on crypto; a bespoke financial TSFM is out of scope and unproven on our exact test.

### [28] Pretrained Time-Series Foundation Models for Financial Return Forecasting
- **Authors / Venue:** Miquel Noguer i Alonso, Rodolfo Pereira Franklin / arXiv
- **Year:** 2026
- **Source:** arXiv:2606.27100
- **% read:** 60% (abstract + setup + DM-test results + conclusion via abstract page + search summary)
- **Summary:** A careful head-to-head of **six TSFMs** (TimeGPT / TimeGPT-LH, TimesFM-2.5, Moirai-2.0, Chronos, Chronos-2) vs **five deep baselines** (NBEATS, NHITS, PatchTST, iTransformer, KAN) on **financial *return* forecasting** for 5 liquid US equities (AAPL, AMZN, GOOG, JPM, META). Explicitly motivates return forecasting as a *hard* TSFM test case (low signal-to-noise, structural breaks, heavy tails, weak persistence). Result: **gains over the random-walk benchmark are "small and sparse"** — Diebold-Mariano rejects equal/inferior accuracy only for **Chronos on AMZN and Moirai-2.0 on GOOG** (i.e. 2 of many model-asset pairs). Conclusion: TSFMs are "useful practical priors that reduce model-development cost in low-data forecasting, but are **not universal engines for statistically reliable alpha generation.**"
- **Relevance to our system:** The most quantitatively decisive entry for the operator's question. On the *exact* quantity that matters for beating buy-and-hold — **financial returns** (not vol, not weather) — the best TSFMs barely beat a **random walk**, and only on 2 of ~30 model-asset combinations with statistical significance. A random walk is, for returns, essentially the buy-and-hold null. This is direct, gate-adjacent evidence that **TSFMs do not produce reliable return-forecasting alpha** even on liquid large-caps (crypto, with worse SNR and heavier tails, would be worse). The authors' own framing — "useful priors, not alpha engines" — is precisely our posture. Strongest single citation for "no, an off-the-shelf TSFM will not beat holding on returns."

### [29] FinCast: A Foundation Model for Financial Time-Series Forecasting
- **Authors / Venue:** Zhuohang Zhu, Haodong Chen, Qiang Qu, Vera Chung / arXiv
- **Year:** 2025
- **Source:** arXiv:2508.19609
- **% read:** 70% (full ar5iv read — architecture, pretraining-mix breakdown, crypto results, metric scope, limitations)
- **Summary:** A foundation model **purpose-built for *financial* series** (the constructive answer to "generic TSFMs don't transfer" [27]). Architecture (full read): **1B-param decoder-only sparse Mixture-of-Experts** (4 experts, top-k=2), patch-based with instance-norm, RMSNorm, learnable frequency embeddings. Pretrained on **20B+ time-points / 2.4M series**, with a concrete asset-class mix: **Stocks 44.5% (9.1B pts), Others 22.5%, Forex 16.0% (3.27B), Crypto 8.69% (1.78B), Futures 8.36% (1.71B), Macro 0.02%**; resolutions seconds→months. Zero-shot (3,632 series, h=10/30/60): **~20% MSE / ~10% MAE reduction** avg, ranks 1st on 23–25 of 36 datasets, vs TimesFM (200M/500M), Chronos-T5, Time-MoE. **Crypto-specific (full read):** tests crypto_1min/1hour/1day — e.g. **crypto_1day h=60 MSE 0.2774 vs TimesFM-500M 0.5730.** Decisive caveats: it does **NOT distinguish return vs price-level forecasting** (uniform instance-norm → scalar point MSE = almost certainly *level*), reports **no volatility breakout**, and uses **no trading/Sharpe/PnL/B&H** — accuracy metrics only.
- **Relevance to our system:** The single most direct answer to the operator's question on the *constructive* side — **yes, someone built a foundation model with substantial crypto in pretraining (8.69%, 1.78B points) and evaluated it on crypto.** It exists, it's a 1B MoE, and it beats generic TSFMs (TimesFM/Chronos) on crypto MSE (~2× lower on crypto_1day). But the full read confirms the skeptical reading that matters: (a) the win is **price-level point-MSE**, the exact **level-persistence trap** ([63] on ETH, [18]'s Bitcoin datapoint) — tomorrow's price ≈ today's, so a random walk scores similarly and "2× lower MSE than TimesFM" says nothing about *return* direction; (b) **no return/PnL/Sharpe/B&H metric at all**, so there is zero evidence of tradeable crypto edge net of costs; (c) it's a 1B-param GPU-trained model — the *opposite* of a lean local Rust advisor. Net: FinCast is the proof that **finance/crypto-specialized TSFMs are real and beat generic ones on accuracy**, yet provides **no gate-credible evidence of crypto return-alpha** — the level-MSE win is precisely what our gate is built to discount. The model to *watch* (and the strongest "it exists" citation), not adopt.

### [30] Profit Mirage: Revisiting Information Leakage in LLM-based Financial Agents
- **Authors / Venue:** Xiangyu Li, Yawen Zeng, Xiaofen Xing, Jin Xu, Xiangmin Xu / arXiv
- **Year:** 2025
- **Source:** arXiv:2510.07920
- **% read:** 55% (abstract + method + findings via abstract page)
- **Summary:** A direct attack on the credibility of LLM-trading backtests. Defines **information leakage** as the LLM having *memorized* historical data + outcomes within its training window, so impressive backtest returns are a **"profit mirage"** that **collapses once you test beyond the training cutoff.** Builds **FinLake-Bench**, a leakage-robust benchmark quantifying the effect across four dimensions, and shows substantial performance decay attributable to memorization rather than genuine prediction. Proposes **FactFin** (counterfactual perturbations + RAG + MCTS + counterfactual simulator) to force causal learning and improve out-of-sample generalization.
- **Relevance to our system:** Direct, named, quantified support for the leakage critique we apply to every LLM-trading paper ([2][3][4]) — and the methodological complement to StockBench [13] (post-cutoff testing). The "profit mirage" framing is exactly the failure our gate and post-cutoff discipline exist to expose: reported LLM-agent profits are largely memorization, not edge. Two concrete takeaways: (1) any LLM signal we ever consider must be evaluated *strictly post-training-cutoff* (or with counterfactual perturbation), or its backtest is worthless; (2) the counterfactual-perturbation idea (does the signal survive when you scramble the memorizable specifics?) is a clever overfitting/leakage detector worth knowing alongside our bootstrap. Strong citation for "LLM-agent trading profits are an artifact."

### [31] CryptoTrade: A Reflective LLM-based Agent to Guide Zero-shot Cryptocurrency Trading
- **Authors / Venue:** Yuan Li, Bingqiao Luo, Qian Wang, Nuo Chen, Xu Liu, Bingsheng He / EMNLP 2024 (main)
- **Year:** 2024
- **Source:** arXiv:2407.09546
- **% read:** 50% (abstract + method via abstract page + search summary; ACL anthology)
- **Summary:** The canonical **crypto-specific** LLM trading agent. Uniquely combines **on-chain** data (transparent, immutable blockchain signals) with **off-chain** data (news), plus a **reflective mechanism** that refines daily decisions by analyzing prior-decision outcomes. Tested zero-shot across various cryptocurrencies and market conditions vs traditional strategies and time-series baselines; claims "superior performance in maximizing returns." Establishes a crypto-trading benchmark. (Abstract gives no numbers, no cost treatment, no explicit limitations.)
- **Relevance to our system:** The most directly on-topic LLM-agent paper for us (crypto, on-chain + off-chain) — but it inherits every evidentiary gap of the LLM-agent genre: no transaction-cost modeling in the abstract, no clear post-cutoff/leakage control (a GPT-class model's cutoff likely overlaps the test window — exactly the "profit mirage" [30]), and "superior returns" stated without a regime-spanning, cost-aware, B&H-net comparison. The **on-chain-data** idea is the genuinely crypto-native contribution worth noting (on-chain flows are a real exogenous feature class), but the agent's trading claims are not gate-credible. For us: catalog CryptoTrade as the reference crypto-LLM-agent and a source of *feature ideas* (on-chain signals), not as evidence LLMs beat holding BTC.

### [32] An Adaptive Multi-Agent Bitcoin Trading System
- **Authors / Venue:** Aadi Singhi / arXiv
- **Year:** 2025
- **Source:** arXiv:2510.08068
- **% read:** 50% (abstract + design + results via abstract page)
- **Summary:** A multi-agent LLM Bitcoin trader with four roles (technical, sentiment, decision, reflection) and a **verbal-feedback** loop: a Reflect agent writes daily/weekly natural-language critiques that are injected into future prompts, so the system "learns" without any weight updates. Backtested on BTC **Jul-2024 → Apr-2025**, claiming >30% higher returns in bullish phases and **+15% overall vs buy-and-hold**, a sentiment agent turning sideways markets into +100%, and weekly feedback adding +31%.
- **Relevance to our system:** Crypto, single-coin (BTC) — superficially our exact setting — but a textbook of the failure modes our gate rejects: **transaction costs not addressed**, a **single ~10-month window** (which included a strong BTC bull run — so beating B&H "in bullish phases" is partly just exposure), and a **GPT-class cutoff that overlaps the 2024–25 test period** (leakage / "profit mirage" [30]). The "+100% from a sentiment agent in a sideways market" claim with no cost accounting is a red flag, not a result. The verbal-reflection-without-fine-tuning mechanism is a mildly interesting *agent-design* idea, but the trading numbers are not credible evidence. For us: a direct illustration that even *crypto single-coin* LLM systems collapse under cost-aware, regime-spanning, post-cutoff scrutiny — i.e. our gate's exact remit. Reinforces the thesis on our own asset class.

### [33] Large Language Model Agent in Financial Trading: A Survey
- **Authors / Venue:** Han Ding, Yinheng Li, Junhao Wang, Hang Chen, Doudou Guo, Yunbai Zhang / arXiv
- **Year:** 2024
- **Source:** arXiv:2408.06361
- **% read:** 40% (abstract + taxonomy framing via abstract page)
- **Summary:** A survey organizing the LLM-trading-agent field along three axes: **agent architectures**, **data inputs**, and **backtesting/performance evaluation.** It frames the open question as whether LLM agents "can outperform professional traders," reviews the common designs (single-agent, multi-agent, reflective/memory), and outlines challenges + future directions — but **declines to declare LLM agents mature or consistently profitable**, positioning the field as still developmental.
- **Relevance to our system:** A neutral anchor/citation for the LLM-trading-agent landscape (complements the broader DL→LLM survey [11]). Its refusal to claim profitability — despite surveying the very papers that *report* big returns — is itself telling: the field's own survey treats out-performance as an open question, not an established fact. Useful for our docs as the "here is the map of LLM-agent designs" reference, while our gate supplies the verdict the survey withholds. Reusable: its architecture/data taxonomy is a clean way to describe *where* an LLM could sit in our advisor (data ingestion / narration), explicitly not as a validated alpha source.

### [34] LLM-Powered Multi-Agent System for Automated Crypto Portfolio Management
- **Authors / Venue:** Yichen Luo, Yebo Feng, Jiahua Xu, Paolo Tasca, Yang Liu / arXiv
- **Year:** 2025
- **Source:** arXiv:2501.00826
- **% read:** 55% (abstract + design + results via abstract page)
- **Summary:** A multi-agent LLM system for **crypto portfolio** management: a Crypto Agent (market dynamics), News Agent (weekly news sentiment), and Trading Agent (fuses + executes), tested under hierarchical/collaborative/debate communication and zero-shot/CoT/RAG/skill-augmented configs. Inputs fuse **price + on-chain time series + news + technical indicators.** On a **52-week 2025 backtest** over the **top-15 L1 cryptos**, the best (Hierarchical-Skill) config reports **133.52% cumulative return, Sharpe 1.50**, beating single-agent, passive benchmarks, and DL baselines; ablating the Crypto Agent costs 42.6 pts.
- **Relevance to our system:** Closer to a *portfolio* than our single-coin advisor, but crypto-native and instructive. Same red flags: **no transaction-cost/slippage modeling**, a **single 52-week 2025 window** (and 2025 was a strong crypto year — a passive top-15 basket also did well, so "beats passive" needs the *net-of-cost, regime-spanning* test), and **cutoff/leakage** unaddressed ("profit mirage" [30]). The genuinely useful bits for us: (a) **on-chain + news + price fusion** as a feature-architecture sketch; (b) the finding that the *market-analysis* agent (numbers) matters far more than the news agent (the 42.6-pt ablation) — consistent with our thesis that *price/structure* dominates *text*. But 133% with no costs over one bull year is not gate-credible alpha. Catalog as design/feature inspiration, not evidence.

### [35] FinBERT: Financial Sentiment Analysis with Pre-trained Language Models
- **Authors / Venue:** Dogu Araci / arXiv (MSc thesis, Univ. of Amsterdam)
- **Year:** 2019
- **Source:** arXiv:1908.10063
- **% read:** 40% (abstract + method via abstract page + search summary)
- **Summary:** The foundational domain-adapted financial NLP model: take **BERT**, *further pre-train* it on a large financial corpus, then fine-tune for **sentiment classification.** Evaluated on Financial PhraseBank and FiQA; reports state-of-the-art on every metric, and shows domain pre-training lets it win **even with a small labeled set and only partial fine-tuning.** (Pure NLP; no trading application in the paper.) The ProsusAI/finbert checkpoint became a widely-used default.
- **Relevance to our system:** The canonical, cheap, *open* financial-sentiment substrate — the realistic baseline for any sentiment feature in our advisor (predates and is lighter than the LLM-era FinGPT [1]/FinLlama [10]). Its honest scope is the point: FinBERT proves you can *classify financial-text sentiment* well, full stop — it makes **no** trading claim. For us it's a ready, encoder-sized tool if we ever add a sentiment *input*, but exactly like the bigger models, its NLP accuracy says nothing about beating buy-and-hold; that requires our cost-aware, post-cutoff, bootstrap-vs-B&H gate. Good "minimal viable sentiment model" reference.

### [36] PIXIU: A Large Language Model, Instruction Data and Evaluation Benchmark for Finance (FinMA / FLARE)
- **Authors / Venue:** Qianqian Xie, Weiguang Han, Xiao Zhang, Yanzhao Lai, Min Peng, Alejandro Lopez-Lira, Jimin Huang / NeurIPS 2023 Datasets & Benchmarks
- **Year:** 2023
- **Source:** arXiv:2306.05443
- **% read:** 40% (abstract + framework via abstract page + search summary)
- **Summary:** A comprehensive open finance-LLM framework: (1) **FinMA**, a LLaMA fine-tuned on (2) **136K** multi-task financial **instruction** samples, evaluated on (3) **FLARE**, a benchmark of 5 financial NLP tasks + 1 financial *prediction* task across 9 datasets (incl. a stock-movement-prediction task). Spans multiple financial document types and modalities. Predecessor/sibling to FinBen [7] from an overlapping group.
- **Relevance to our system:** Important because it's an early benchmark that *includes a numeric financial-prediction (stock-movement) task alongside text tasks* — and, consistent with the later FinBen [7] and StockBench [13] findings, the pattern is that instruction-tuned finance LLMs do well on the *NLP* tasks while the *prediction* task is the weak spot (the broader literature, [7][8], pegs such directional prediction near coin-flip). For us PIXIU/FinMA is (a) a reusable *open* instruction-tuned finance model for text jobs (narration/extraction), and (b) more evidence that adding a "prediction" task to a finance-LLM benchmark exposes weakness, not strength, on the numeric side. NLP-capable, not alpha-capable.

### [37] StockTime: A Time Series Specialized Large Language Model Architecture for Stock Price Prediction
- **Authors / Venue:** Shengkun Wang, Taoran Ji, Linhan Wang, et al. / arXiv (CIKM 2024)
- **Year:** 2024
- **Source:** arXiv:2409.08281
- **% read:** 45% (abstract + architecture via abstract page)
- **Summary:** An LLM architecture specialized for **stock-price time series**: it treats **stock prices as consecutive tokens**, auto-derives textual descriptors (correlations, statistical trends, timestamps) from the price data itself, fuses text + numeric series in a shared embedding space, and predicts autoregressively. Claims to **outperform recent LLM-for-TS methods** with lower memory/runtime. Notably concedes models "produce less accurate predictions when faced with false and redundant information in financial markets" (i.e. noise hurts).
- **Relevance to our system:** Another data point on the operator's question — an LLM adapted to *financial* numeric series (prices-as-tokens), purpose-built for stocks. But the honesty checks: its claimed wins are *relative to other LLM-for-TS methods* (a low bar, given [25] shows the LLM often ablates out) and on prediction-error metrics, **not** tradeable PnL vs B&H net of costs; no crypto eval; and it explicitly admits noise degrades it — and crypto is noisier than equities. So StockTime joins FinCast [29]/StockLLM [8] as "yes, people specialize LLMs on financial series," while adding nothing that suggests crypto return-alpha over holding. The "noise hurts" admission is itself supportive of our thesis (the un-forecastable part dominates). Background/catalog.

### [38] A Review of Large Language Models for Stock Price Forecasting from a Hedge-Fund Perspective
- **Authors / Venue:** Olivia Zhang, Zhilin Zhang / arXiv (accepted IEEE Conf. on AI 2026)
- **Year:** 2026
- **Source:** arXiv:2605.05211
- **% read:** 45% (abstract + pitfalls + assessment via abstract page)
- **Summary:** A practitioner-oriented review of LLMs for stock-price forecasting that is refreshingly **cautionary**. It surveys the usual applications (news/social sentiment, report/earnings-call analysis, price-series tokenization, multi-agent systems) but foregrounds the **practical pitfalls the literature understates**: fragility in sentiment analysis, improper dataset/horizon design, flawed evaluation metrics, **data leakage**, **illiquidity premia**, and the fundamental **limits of stock-price predictability.** Its core recommendation: **stress-test robustness under realistic market frictions before deploying.**
- **Relevance to our system:** Almost a mission statement for our advisor's gate, from the buy-side. The recommended discipline — stress-test under realistic frictions, beware leakage, beware illiquidity, respect predictability limits — is precisely what our 1000-path moving-block bootstrap *with costs* against buy-and-hold operationalizes. It's the ideal neutral citation that the *field's own practitioner reviews* converge on skepticism + frictions-aware testing, not "LLMs forecast prices." Its pitfall list also maps cleanly onto our standing critiques across [2][3][4][30][31][32]. Strong anchor for "even the hedge-fund-perspective review says: test under frictions, don't assume edge."

### [39] Sentiment Trading with Large Language Models
- **Authors / Venue:** Kemal Kirtac, Guido Germano / Finance Research Letters 2024 (also arXiv)
- **Year:** 2024
- **Source:** arXiv:2412.19245
- **% read:** 55% (abstract + results via abstract page + search summary)
- **Summary:** A large, peer-reviewed sentiment-trading study on **965,375 US news articles (Jan-2010 → Jun-2023)**, comparing **OPT (GPT-3-class), BERT, FinBERT, and the Loughran-McDonald dictionary.** OPT wins on sentiment accuracy (74.4% vs ~72% BERT/FinBERT vs 50.1% L-M). A daily **long-short** portfolio on OPT scores reports a striking **Sharpe 3.05** (vs 2.11 BERT, 2.07 FinBERT, 1.23 L-M) and a **+355% gain Aug-2021 → Jul-2023.** Concludes LLM sentiment is significantly associated with subsequent daily returns.
- **Relevance to our system:** The strongest *peer-reviewed pro-LLM-sentiment* result in the ledger — and precisely because it's strong, the unstated caveats are what matter for us. The abstract **does not model transaction costs** and **does not address that OPT/GPT-3's training window overlaps the 2010–2021 sample** (look-ahead/leakage, exactly [6][30]). A long-short *cross-sectional* equity strategy with daily rebalancing is also turnover-heavy and **inapplicable to a single-coin advisor** (we can't go long-short across names). And a Sharpe of 3 from daily news sentiment is the kind of number that historically shrinks hard under realistic costs + post-cutoff testing (cf. Lopez-Lira [9]'s decay finding). So: real evidence that LLM sentiment *correlates* with returns, but not gate-credible tradeable alpha, and structurally not portable to our setup. Reinforces "sentiment = possible exogenous feature, tested net-of-cost post-cutoff; never standalone alpha."

### [40] Fin-R1: A Large Language Model for Financial Reasoning through Reinforcement Learning
- **Authors / Venue:** Zhaowei Liu, Xin Guo, Zhi Yang, et al. (SUFE / Fudan / Rice / LSE) / arXiv
- **Year:** 2025 (v5 2026)
- **Source:** arXiv:2503.16252
- **% read:** 45% (abstract + method + benchmarks via HTML)
- **Summary:** A compact **7B financial-reasoning LLM** built via a two-stage pipeline: SFT on 60K curated **chain-of-thought** financial samples, then **GRPO** RL with format+accuracy rewards. Targets financial *reasoning/QA* benchmarks (FinQA, ConvFinQA, Ant-Finance, TFNS, Finance-Instruct-500k); scores 75.2 avg, second overall, beating all similar-size models and within 3 pts of DeepSeek-R1. **Explicitly makes no trading/forecasting claim** — positioned for compliance checking and robo-advisory *consultation*.
- **Relevance to our system:** A clean, current example of the *right* LLM scope for finance — and a useful contrast to the trading-agent hype. Fin-R1 invests heavily in *reasoning* (CoT + RL) yet deliberately stays on **interpretable QA / advisory consultation**, not price prediction. That is exactly our posture: an LLM can *reason about and explain* financial questions (a strong fit for our "why this one" narration / operator-consultation seam) while the decision/alpha stays with the gate. Also relevant: it shows a small (7B), open-ish reasoning model can be competitive — a realistic substrate for a local narration/explanation layer, no frontier API required. Catalog as: best-practice LLM-for-finance scoping (reasoning/advisory, not trading).

### [41] FinRL-DeepSeek: LLM-Infused Risk-Sensitive Reinforcement Learning for Trading Agents
- **Authors / Venue:** Mostapha Benhenda (LAGA) / arXiv
- **Year:** 2025
- **Source:** arXiv:2502.07393
- **% read:** 45% (abstract + method via abstract page)
- **Summary:** A hybrid that **fuses an LLM signal into an RL trader**: it extends the **CVaR-PPO (CPPO)** risk-sensitive RL algorithm with **risk-assessment + trading-recommendation signals an LLM extracts from financial news.** Tested on the **Nasdaq-100** using the FNSPID news dataset, with three LLMs (DeepSeek-V3, Qwen-2.5, Llama-3.3). Open code/data/agents. (Abstract gives no concrete backtest numbers, cost treatment, or limitations.)
- **Relevance to our system:** A representative *LLM-as-feature-into-RL* design — the LLM produces a news-derived risk/recommendation signal that an RL policy consumes, rather than the LLM trading directly. Architecturally that's the safer pattern (LLM = exogenous signal, not decision-maker). But it stacks two of our least-trusted components (RL trader + LLM news signal), inherits leakage risk (news + cutoff overlap, [30]), and the abstract reports no costs or B&H-net comparison — so no gate-credible evidence. The genuinely useful idea for us is the *risk-sensitive* framing (CVaR objective): if we ever used an exogenous LLM/news signal, wiring it to **risk/sizing** (downside control) rather than directional alpha is the defensible target — consistent with the realized-vol finding [19] and the "B&H + strategy reduces drawdown" pattern. Catalog as design reference for LLM-signal-as-risk-input.

### [42] Market-Derived Financial Sentiment Analysis: Context-Aware Language Models for Crypto Forecasting
- **Authors / Venue:** Hamid Moradi-Kamali, Mohammad-Hossein Rajabi-Ghozlou, Mahdi Ghazavi, et al. / arXiv
- **Year:** 2025
- **Source:** arXiv:2502.14897
- **% read:** 55% (abstract + method + results via abstract page)
- **Summary:** A **crypto-specific** sentiment-forecasting model. Instead of human sentiment labels, it uses **market-derived labels — each tweet is labeled by the *ensuing short-term price move*** — then prompt-tunes a domain LM with market + temporal context. On Bitcoin tweets + 227 curated news events: +11% short-term trend-prediction accuracy over sentiment baselines, **89.6% accuracy** on the curated BTC news set, and **Sharpe up to 5.07** (trending) / 3.73 (neutral). Claims to overturn the assumption that price signals beat sentiment signals.
- **Relevance to our system:** Crypto + Bitcoin — our exact asset — but a near-textbook illustration of the traps our gate exists for. (1) **The labeling scheme — "label the tweet by what the price did next" — bakes the target into the features**; high accuracy then partly measures the label leak, not predictive power. (2) **No transaction costs**, and a Sharpe of 5 on short-term BTC signals is wildly implausible net of crypto fees/slippage. (3) "Curated impactful news events" is selection on outcome. (4) Single-regime-flavored evaluation. So despite being on-topic, its headline numbers are not gate-credible. The one *defensible* idea is *market-derived labeling* as a way to build a sentiment dataset — but it must then be tested on a *separate*, post-event, cost-aware, bootstrap-vs-B&H protocol, not on the same trend-defined labels. Strong example for the operator that "crypto + LLM + sentiment + big Sharpe" usually means leakage + no costs, not edge.

### [43] Time Series Foundation Models for Multivariate Financial Time Series Forecasting
- **Authors / Venue:** Ben A. Marconi / arXiv
- **Year:** 2025
- **Source:** arXiv:2507.07296
- **% read:** 55% (abstract + results + conclusion via abstract page)
- **Summary:** Evaluates two TSFMs — **Tiny Time Mixers (TTM)** and **Chronos [15]** — on three *financial* forecasting tasks: **US 10-yr Treasury yield changes, EUR/USD volatility, equity spread.** Findings are carefully mixed: pretrained TTM beats its un-pretrained version and needs **3–10 fewer years of data** for comparable performance, with **25–50% better** results when fine-tuned on limited data; zero-shot, TTM **beats naive benchmarks on volatility and equity-spread** prediction. **But traditional specialized models still matched or surpassed TTM on 2 of 3 tasks**, and the author concludes that competitive financial results will need **domain-specific pretraining + architecture tailored to financial series.**
- **Relevance to our system:** A balanced, finance-applied TSFM study that lands exactly where the operator's-question evidence converges: TSFMs help most in **noisy, data-constrained** settings and on **volatility/spread** (forecastable targets) — and even then **lose to specialized models on most tasks**, requiring finance-specific pretraining ([27]) to compete. Note the tasks where TSFM *did* beat naive are **volatility and a spread** — *not* directional price/return (echoing [19][28]: the forecastable financial quantity is vol, not return). For us: more support that (a) any TSFM value is in *risk/vol* estimation, not return-alpha; (b) off-the-shelf is not enough; (c) data-efficiency (fewer years needed) is the genuine practical upside — relevant if we ever had short crypto histories, but still gated.

### [44] LLM Agents Do Not Replicate Human Market Traders: Evidence from Experimental Finance
- **Authors / Venue:** Thomas Henning, Siddhartha M. Ojha, Ross Spoon, Jiatong Han, Colin F. Camerer (Caltech et al.) / arXiv
- **Year:** 2025
- **Source:** arXiv:2502.15800
- **% read:** 50% (abstract + setup + findings via abstract page)
- **Summary:** Puts LLM agents into a **classic experimental-finance asset-market paradigm** (traders buy/sell a risky asset with a *known* fundamental value), in single-model and mixed-model markets. Key finding: LLM agents are **"textbook-rational"** — they price near fundamental value and show only a **muted tendency to form bubbles**, with less strategy variance than humans. Conclusion: LLM-only populations **fail to reproduce human market phenomena (bubbles, crashes)** and cannot substitute for human traders in modeling real market dynamics.
- **Relevance to our system:** A distinctive, non-PnL caveat with two implications for our advisor. (1) **For market *simulation*:** if we ever considered LLM agents to generate synthetic market scenarios or stress tests, this says they'd produce *unrealistically calm, rational* dynamics — missing exactly the bubble/crash regimes our 1000-path moving-block bootstrap must span (so our resampling-from-real-data approach is the right one, not LLM-simulated paths). (2) **For trading behavior:** "textbook-rational, near fundamental value, muted bubbles" implies an LLM trader would tend toward... holding near fair value — i.e. it has no special crash-timing or momentum-riding edge, consistent with "no robust edge over buy-and-hold." Useful, original citation on the *limits* of LLMs as market participants/simulators.

### [45] Agentic Retrieval-Augmented Generation for Financial Document Question Answering (FinAgent-RAG)
- **Authors / Venue:** Yang Shu, Yingmin Liu, Zequn Xie / Expert Systems with Applications (submitted)
- **Year:** 2026
- **Source:** arXiv:2605.05409
- **% read:** 50% (abstract + method + results via abstract page)
- **Summary:** An *agentic* RAG framework for QA over financial filings that require multi-step numeric reasoning over tables, narrative text, and footnotes. Three parts: a **Contrastive Financial Retriever** (hard-negative mining), a **Program-of-Thought** module that emits executable Python for arithmetic (so the model computes rather than guesses numbers), and an **Adaptive Strategy Router** that scales compute to question difficulty. On FinQA / ConvFinQA / TAT-QA it reaches 76.8% / 78.5% / 75.0% execution accuracy (+5.6–9.3 pts over the strongest baseline) and cuts API cost 41.3% on FinQA. Strictly a **document-QA accuracy benchmark — no trading, no PnL, no costs/slippage in the market sense.**
- **Relevance to our system:** Adds RAG-for-finance *depth* (a flagged thin seam) and is squarely on the *language* side of our thesis split — it makes financial documents queryable, not markets predictable. Two genuinely reusable ideas for our narration/research-assistant seam: (1) **Program-of-Thought** — have the LLM emit *verifiable code* for any arithmetic in a "why this one" explanation rather than hallucinate figures (directly mitigates the hallucination risk flagged in [14]); (2) the **adaptive router** (cheap path for easy questions) is a concrete cost-control pattern for a local advisor. But this is firmly a context/extraction tool: it answers "what did the 10-K say," never "what will the price do." Catalog as RAG/extraction best-practice, not alpha.

### [46] Metadata-Driven Retrieval-Augmented Generation for Financial Question Answering
- **Authors / Venue:** Michail Dadopoulos, Anestis Ladas, Stratos Moschidis, Ioannis Negkakis / Int. J. of Accounting Information Systems (under revision)
- **Year:** 2025
- **Source:** arXiv:2510.24402
- **% read:** 45% (abstract + method + findings via abstract page)
- **Summary:** A systematic study of how to make RAG work on *long, structured* financial filings where the relevant evidence is **sparse and cross-referenced** — the regime where naive vector retrieval fails. Proposes a multi-stage architecture combining **LLM-generated metadata**, pre-retrieval filtering, post-retrieval reranking, and **contextual embeddings** (metadata embedded *alongside* the chunk text). Benchmarked on **FinanceBench**; the headline finding is that embedding chunk metadata directly with text ("contextual chunks") gives the largest gain, and a custom metadata reranker is more cost-effective than commercial rerankers. Pure document-QA accuracy; no trading.
- **Relevance to our system:** More RAG-for-finance depth, and a practical engineering lesson if we ever add a filing/news retrieval layer behind narration: **plain semantic chunking is insufficient for financial documents** — you need metadata-enriched chunks + reranking, because the evidence is sparse and cross-referenced. Reinforces FinSeer's [8] point from the document side (generic embeddings are weak for finance) and the cost-control theme. Strictly a context/extraction tool — background for the narration seam, not the decision rail.

### [47] Harnessing Earnings Reports for Stock Predictions: A QLoRA-Enhanced LLM Approach
- **Authors / Venue:** Haowei Ni, Shuchen Meng, Xupeng Chen, et al. / IEEE DOCS 2024
- **Year:** 2024
- **Source:** arXiv:2408.06634
- **% read:** 40% (abstract + method via abstract page + search summary)
- **Summary:** Fine-tunes an open LLM (Llama-3-8B-Instruct, 4-bit) via **QLoRA** instruction-tuning to predict post-earnings stock movement, fusing base factors (financial metrics + earnings-call transcripts) with external factors (market indices, analyst grades) into an ~8,556-row dataset (next-day move after earnings). Reports gains in accuracy / weighted-F1 / **Matthews correlation coefficient**, with the 4-bit Llama-3-8B beating GPT-4 on the task. Results are **classification metrics only — no trading PnL, no transaction costs, no stated walk-forward/post-cutoff protocol.**
- **Relevance to our system:** A representative earnings-call → movement-prediction study (fills the flagged earnings/10-K seam). Two honest reads for us: (a) it confirms a *small, 4-bit, QLoRA-tuned open model can outperform a frontier API* on a finance classification task — directly supportive of our "small local model is the realistic substrate" takeaway ([1][40]) for any narration/extraction component; (b) but it stops at classification metrics (MCC/F1) and never crosses into PnL-vs-B&H net of costs, so it says nothing about tradeable edge — and earnings-event trading is anyway inapplicable to a single-coin crypto advisor (no earnings). Catalog as evidence for the *local-model-is-enough* engineering point, not as alpha.

### [48] From Text to Alpha: Can LLMs Track Evolving Signals in Corporate Disclosures?
- **Authors / Venue:** Chanyeol Choi, Yoon Kim, Yu Yu, ..., Alejandro Lopez-Lira, Yongjae Lee / arXiv (cs.CE)
- **Year:** 2025 (rev. 2026)
- **Source:** arXiv:2510.03195
- **% read:** 45% (abstract + method + results via abstract page)
- **Summary:** Tries to extract *alpha* from the **narrative** of corporate disclosures using "LLM as extractor, embedding as ruler": the LLM pulls metric-focused text spans, and embedding-similarity measures **semantic drift** across reporting periods to detect "moving targets" (shifts in which metrics management emphasizes). Claims **>2× the risk-adjusted alpha** of a named-entity-recognition baseline with significantly stronger predictive power. The abstract does **not** address transaction costs and does **not** mention look-ahead / leakage safeguards.
- **Relevance to our system:** Co-authored by Lopez-Lira ([9]) and more methodologically interesting than most ("track *changes* in emphasis," not static sentiment) — but it inherits the genre's evidentiary gaps for our purposes: a "risk-adjusted alpha vs NER baseline" claim with no cost accounting and no leakage control isn't gate-credible (the very look-ahead concern Glasserman & Lin [6] raised for exactly this author's domain). The transferable *idea* — measuring semantic *drift* over time as a feature rather than point sentiment — is a genuinely novel input-construction trick, but it's disclosure-driven (no analogue for a single crypto coin) and would still need post-cutoff, cost-aware, bootstrap-vs-B&H validation. Catalog as a clever feature idea, not evidence of edge.

### [49] BizFinBench: A Business-Driven Real-World Financial Benchmark for Evaluating LLMs
- **Authors / Venue:** Guilong Lu, Xuntao Guo, Rongjunchen Zhang, Wenqiao Zhu, Ji Liu (HiThink Research) / arXiv (cs.AI)
- **Year:** 2025
- **Source:** arXiv:2505.19457
- **% read:** 45% (abstract + structure + findings via abstract page)
- **Summary:** A *business-grounded* financial LLM benchmark: 6,781 annotated queries across five dimensions (numerical calculation, reasoning, information extraction, prediction recognition, knowledge QA) in nine fine-grained categories, with an "IteraJudge" LLM-as-judge method to de-bias evaluation. Benchmarks **25 models** (Claude-3.5-Sonnet, o3, Gemini-2.0, DeepSeek-R1, Qwen). Headline: **no model dominates all tasks; LLMs handle routine finance queries competently but struggle with complex cross-concept reasoning**, and even leaders only reach ~63–64 on numerical calculation. No dedicated trading/stock-prediction task.
- **Relevance to our system:** Fills the "LLM agent/finance benchmarks" seam (a flagged thin area) and adds another independent, benchmark-grade confirmation of our thesis split: LLMs are decent at routine financial *language/knowledge* tasks and weak at the harder *reasoning/quantitative* end. The ~63 ceiling on *numerical calculation* (even for frontier models) is a quiet but important caution for any narration that does arithmetic — reinforcing the Program-of-Thought "make it compute, don't let it guess numbers" lesson from [45]. Background/anchor citation; no trading claim to weigh.

### [50] Golden Touchstone: A Comprehensive Bilingual Benchmark for Evaluating Financial Large Language Models
- **Authors / Venue:** Xiaojun Wu, Junxi Liu, Huanyi Su, ..., Jian Guo (IDEA-FinAI) / Findings of EMNLP 2025
- **Year:** 2024 (rev. 2025)
- **Source:** arXiv:2411.06272
- **% read:** 55% (abstract + task list + stock-prediction results via HTML)
- **Summary:** A bilingual (Chinese-English) financial-LLM benchmark over **eight tasks**: sentiment analysis, classification, entity recognition, relation extraction, multiple-choice, summarization, QA, and **stock-movement prediction.** Compares GPT-4o, Llama3, FinGPT, FinMA, and the authors' open Touchstone-GPT (continual-pretrain + instruction-tune). The decisive data point: **on stock-movement prediction every model is near chance** — GPT-4o weighted-F1 **0.4241**, FinMA-7B 0.3211, Touchstone-GPT 0.4396 (English DJIA) — and the authors state the best result "falls short of practical utility," that "sentiment of news items may not reliably predict stock movements," and that quantitative data would be essential.
- **Relevance to our system:** One of the cleanest, most quotable confirmations of our thesis: a careful bilingual benchmark shows LLMs do fine on the seven *language* tasks and **collapse to ~0.42 weighted-F1 on the one *prediction* task** — i.e. *worse than a coin flip is plausible after costs*, and the authors themselves say it's not practically useful and that text-sentiment doesn't reliably predict moves. This is exactly the "language ≠ alpha" wall ([7][8][36]) seen yet again, and the explicit "you'd need quantitative data" admission echoes our "numbers > text" finding ([34]). Strong anchor citation for "even purpose-built finance LLMs can't predict direction." Touchstone-GPT is also a usable *open* finance model for text jobs.

### [51] FinArena: A Human-Agent Collaboration Framework for Financial Market Analysis and Forecasting
- **Authors / Venue:** Congluo Xu, Zhaobin Liu, Ziyang Li / arXiv (cs.CE)
- **Year:** 2025
- **Source:** arXiv:2503.02692
- **% read:** 40% (abstract + architecture via abstract page)
- **Summary:** A **human-agent collaboration** framework with a mixture-of-experts-inspired machine module: a **time-series agent** forecasts prices from history, a **News Agent** uses **adaptive RAG** over unstructured news (to curb hallucination), and a universal expert agent fuses multimodal features with the *investor's risk preference* to produce decisions. Reports it "surpasses traditional and SOTA benchmarks in stock trend prediction and yields promising results in trading simulations across various risk profiles." The abstract gives **no transaction costs, no B&H-net comparison, no leakage/post-cutoff discipline, and no concrete numbers.**
- **Relevance to our system:** Two reusable design ideas align with our posture: (a) **risk-preference-conditioned output** (the same gated picks framed differently per investor risk appetite) is a clean UX/narration pattern for an advisor; (b) **adaptive RAG specifically to reduce hallucination** on the news leg echoes our grounding requirement [14]. But the *forecasting/trading* claims are qualitative-only with the genre's standard gaps (no costs, no B&H-net, no leakage control), so they're not gate-credible. Catalog as human-in-the-loop + risk-profile-narration design inspiration (consistent with Alpha-GPT 2.0 [12]), not as alpha evidence.

### [52] Coinvisor: An RL-Enhanced Chatbot Agent for Interactive Cryptocurrency Investment Analysis
- **Authors / Venue:** Chong Chen, Ze Liu, Lingfeng Bao, Yanlin Wang, Ting Chen, Daoyuan Wu, Jiachi Chen / arXiv (cs.AI)
- **Year:** 2025
- **Source:** arXiv:2510.17235
- **% read:** 45% (abstract + mechanism + results via abstract page)
- **Summary:** A **crypto-specific conversational analysis agent** whose core contribution is an **RL-based tool-selection mechanism**: it learns to plan multi-step and orchestrate diverse analytical tools / data sources for real-time, interactive crypto investment *analysis*. Crucially, it is explicitly positioned for "accurate and actionable investment **insights**" via a chat UI — **not** automated trading, and it makes **no buy-and-hold / PnL claim.** Evaluated on tool-orchestration quality (+40.7% recall, +26.6% F1 over the base model) and user satisfaction (4.64/5).
- **Relevance to our system:** The single most *posture-aligned* crypto LLM paper in the ledger — it is an LLM doing exactly the job we'd give one: **interactive crypto research/narration with tool orchestration, NOT signal generation**, and it deliberately makes no return claim. Two concrete reusable ideas: (a) **RL-learned tool selection** is a principled way to let a narration agent decide *which* of our vetted (gated) tools/indicators to surface for a given operator question — a more rigorous version of FinAgent's tool-augmentation [4]; (b) evaluating the LLM on *orchestration quality + user satisfaction* (not PnL) is precisely how we'd validate a narration layer. Strong supporting example that the defensible LLM role in crypto is the analysis/UX layer — confirms our architecture, nothing to gate.

### [53] A Time Series is Worth 64 Words: Long-term Forecasting with Transformers (PatchTST)
- **Authors / Venue:** Yuqi Nie, Nam H. Nguyen, Phanwadee Sinthong, Jayant Kalagnanam (IBM/Princeton) / ICLR 2023
- **Year:** 2022 (ICLR 2023)
- **Source:** arXiv:2211.14730
- **% read:** 45% (abstract + method + results framing via abstract page + search summary)
- **Summary:** The influential **patching + channel-independence** transformer for long-horizon forecasting. Two ideas: (1) segment each series into **subseries-level patches** used as tokens (retains local semantics, cuts attention cost quadratically, allows longer look-back); (2) **channel-independence** — every univariate channel shares one embedding + transformer weights. Reports ~**21% MSE / ~17% MAE** reductions over prior transformer SOTA on standard multivariate benchmarks, plus strong **self-supervised pretraining** that beats supervised training and transfers across datasets. Benchmarks are general (weather/traffic/electricity/ILI); **no financial or crypto data, no trading/PnL.**
- **Relevance to our system:** A *primary* (fills the flagged "more TSFM primaries" seam) and important context: PatchTST is one of the **deep baselines** that the financial-return study [28] showed TSFMs only sparsely beat — and the patching idea is the very machinery the ablation paper [25] found does the real work (vs the language prior). So PatchTST matters to us as (a) the architectural ancestor of the patched-decoder TSFMs (TimesFM [18]); (b) a reminder that a *small, dedicated* patch-transformer is the cheaper, often-as-good alternative to a giant TSFM ([26]). Its honest scope — general-forecasting MSE/MAE, never PnL — is the usual caution: good accuracy on weather ≠ edge on crypto returns. Background/architecture catalog; if we ever forecast (vol), a PatchTST-class small model is the realistic candidate, still gated.

### [54] iTransformer: Inverted Transformers Are Effective for Time Series Forecasting
- **Authors / Venue:** Yong Liu, Tengge Hu, Haoran Zhang, Haixu Wu, Shiyu Wang, Lintao Ma, Mingsheng Long (Tsinghua THUML) / ICLR 2024 (Spotlight)
- **Year:** 2023 (ICLR 2024)
- **Source:** arXiv:2310.06625
- **% read:** 45% (abstract + method + framing via abstract page + search summary)
- **Summary:** **Inverts** the transformer for forecasting: instead of one token per timestamp (mixing all variates), it embeds **each whole series as a "variate token,"** so attention captures *cross-variate* correlations and the FFN learns per-variable nonlinear representations. This fixes two standard-transformer failure modes — performance degradation + compute blow-up with longer look-back, and poor variate-centric representations — and yields SOTA on challenging real-world multivariate benchmarks with better generalization across variates. Positioned as "the fundamental backbone" of TS forecasting. General benchmarks; **no financial/crypto eval, no trading.**
- **Relevance to our system:** The other deep baseline in [28]'s financial-return comparison (where TSFMs barely beat it/random-walk), so it's load-bearing context for our "TSFMs aren't alpha engines" conclusion. The inverted/variate-token idea is the multivariate counterpart to Moirai [22] and theoretically attractive *if* we fed BTC alongside on-chain/funding covariates — but the same caveats bind: it's an accuracy method on calm general benchmarks, not a PnL method, and a single-coin advisor rarely benefits from multivariate machinery. Catalog as architecture/baseline; no evidence it helps beat holding on crypto.

### [55] Time-MoE: Billion-Scale Time Series Foundation Models with Mixture of Experts
- **Authors / Venue:** Xiaoming Shi, Shiyu Wang, Yuqi Nie, Dianqi Li, Zhou Ye, Qingsong Wen, Ming Jin / ICLR 2025 (Spotlight)
- **Year:** 2024 (ICLR 2025)
- **Source:** arXiv:2409.16040
- **% read:** 45% (abstract + architecture + results framing via abstract page + search summary)
- **Summary:** Scales TSFMs to **2.4B parameters** using a **sparse mixture-of-experts** decoder-only transformer (auto-regressive, flexible horizons/contexts) that activates only a subset of experts per prediction — so capacity grows while inference cost stays bounded. Pretrained on **Time-300B** (>300B time-points across 9 domains). Reports significantly improved zero-shot and full-shot forecasting, consistently beating dense baselines by large margins. The abstract names **no financial/crypto domain** and reports general forecasting accuracy, not trading.
- **Relevance to our system:** The current high-water-mark for *scale* among numeric-pretrained TSFMs (fills the "more primaries" seam, MoE branch). For our advisor it mostly underscores the cost/skeptic tension: a 2.4B model is the *opposite* of a lean local Rust advisor, and the same independent critiques apply — scale doesn't fix the domain-bound, shock-fragile limits ([20][26]) and the headline is general-accuracy, not crypto PnL. Its MoE efficiency (activate few experts) is an interesting deployment idea in the abstract, but [43]/[28] show *small* models (TTM/PatchTST) are the practical financial choice; a billion-param MoE is not. Background/architecture catalog; no alpha evidence.

### [56] Tiny Time Mixers (TTMs): Fast Pre-trained Models for Enhanced Zero/Few-Shot Forecasting of Multivariate Time Series
- **Authors / Venue:** Vijay Ekambaram, Arindam Jati, Pankaj Dayama, ..., Jayant Kalagnanam (IBM Research) / NeurIPS 2024
- **Year:** 2024
- **Source:** arXiv:2401.03955
- **% read:** 45% (abstract + method + results framing via abstract page + search summary)
- **Summary:** The **anti-scale** TSFM: a compact model **starting from ~1M parameters**, built on the lightweight **TSMixer** (MLP-mixer, not attention) architecture, pretrained in just 4–8 hrs on public data. Innovations: adaptive patching, diverse-resolution sampling, resolution-prefix tuning, multi-level modeling for **cross-channel correlations**, and **exogenous-signal infusion** at fine-tune time. Reports **4–40% zero/few-shot improvement** over popular benchmarks while **running on CPU-only machines.** No financial/crypto evaluation in the paper itself (though [43] later applied TTM to financial tasks).
- **Relevance to our system:** The most *deployment-relevant* TSFM for a lean local advisor — ~1M params, CPU-runnable, fast to fine-tune — and it's the exact model [43] applied to finance (where pretrained TTM beat its un-pretrained version and needed 3–10 fewer years of data, but still **lost to specialized models on 2 of 3 financial tasks** and helped on *volatility/spread*, not direction). So TTM is the realistic candidate *if* we ever add a forecasting input: small, cheap, exogenous-signal-aware. But [43]'s finance result is the binding evidence — useful for *vol/risk* in data-scarce settings, not return-alpha, and only after our gate. The CPU-runnable + data-efficiency angle is the genuine practical upside worth remembering for short crypto histories. Catalog as the "if we must forecast, use a tiny model like this" reference.

### [57] N-HiTS: Neural Hierarchical Interpolation for Time Series Forecasting
- **Authors / Venue:** Cristian Challu, Kin G. Olivares, Boris N. Oreshkin, Federico Garza, Max Mergenthaler-Canseco, Artur Dubrawski / AAAI 2023
- **Year:** 2022 (AAAI 2023)
- **Source:** arXiv:2201.12886
- **% read:** 45% (abstract + method + results framing via abstract page + search summary)
- **Summary:** An MLP-based (N-BEATS-descendant, **no attention**) long-horizon forecaster using **hierarchical interpolation + multi-rate data sampling**: blocks specialize on different frequency bands and assemble the forecast via doubly-residual stacking, controlling both prediction volatility and compute. Reports ~**20% accuracy improvement over the latest transformers while ~50× faster.** Pure general-forecasting benchmarks; **no financial/crypto evaluation, no trading.**
- **Relevance to our system:** Another of the deep baselines [28] used (and that TSFMs only sparsely beat on financial *returns*), and notable for *not* being a transformer — it's a stark reminder that a cheap MLP-stack can match/beat attention models on accuracy ([25]'s broader point: the fancy architecture often isn't what matters). For our advisor: if a forecasting input is ever justified (vol/risk), an N-HiTS/PatchTST-class small model is the pragmatic, fast, CPU-friendly choice over a giant TSFM — but accuracy on generic benchmarks still says nothing about beating B&H on crypto returns. Background/architecture catalog.

### [58] Sundial: A Family of Highly Capable Time Series Foundation Models
- **Authors / Venue:** Yong Liu, Guo Qin, Zhiyuan Shi, Zhi Chen, Caiyin Yang, Xiangdong Huang, Jianmin Wang, Mingsheng Long (Tsinghua THUML) / ICML 2025 (Oral, Top 1%)
- **Year:** 2025 (ICML 2025)
- **Source:** arXiv:2502.00816
- **% read:** 45% (abstract + method + results framing via abstract page + search summary)
- **Summary:** A **generative** TSFM that abandons discrete tokenization: its **TimeFlow Loss** (a flow-matching objective) lets a transformer be pretrained natively on **continuous-valued** series and emit **multiple plausible non-deterministic forecasts** (no parametric density assumed). Pretrained on **TimeBench (~1 trillion time points**, mostly real + some synthetic); 128M-param base. Reports SOTA on **both point and probabilistic** benchmarks with millisecond zero-shot inference. **No financial/crypto evaluation; general forecasting.**
- **Relevance to our system:** The newest, most capable open numeric-pretrained TSFM (fills "more primaries", generative branch) — and methodologically the cleanest contrast to Chronos's [15] *quantize-into-tokens* approach: Sundial models the continuous distribution directly via flow-matching. Its **probabilistic, multi-future** output is the *right shape* for honest crypto forecasting (a wide predictive distribution centered near no-change — which is what B&H already encodes), but exactly therefore unlikely to yield directional alpha. Same binding caveats as the whole cluster: SOTA on calm general benchmarks, no finance/crypto/PnL evidence, domain-bound + shock-fragile per [20][26][27]. A model to watch (open, fast, probabilistic); no basis yet to expect it beats holding crypto. If we ever experiment, its probabilistic output could feed a *risk/sizing* overlay (gated), per the vol-not-direction finding [19][43].

### [59] LiveTradeBench: Seeking Real-World Alpha with Large Language Models
- **Authors / Venue:** Haofei Yu, Fenghai Li, Jiaxuan You / arXiv (q-fin.TR)
- **Year:** 2025
- **Source:** arXiv:2511.03628
- **% read:** 50% (abstract + setup + findings via abstract page)
- **Summary:** A **live, leakage-proof** trading benchmark: instead of offline backtests, it streams *real-time* market prices + news and has 21 LLMs (across families) make sequential portfolio decisions over a **50-day live window** on **US stocks and Polymarket** prediction markets — so by construction there's no train/test overlap. Central finding: **"high LMArena scores do NOT imply superior trading outcomes"** — static chat-benchmark skill fails to predict real-world decision performance; models show distinct risk styles, some adapt to live signals. The abstract does **not** clearly establish that any model achieved *statistically significant* alpha, and does not detail transaction-cost modeling.
- **Relevance to our system:** Methodologically the **gold-standard design** for the operator's skepticism — *live/post-cutoff is the only honest LLM-trading test*, exactly StockBench's [13] and Profit Mirage's [30] logic taken to its limit (no backtest at all). The headline — **chatbot-leaderboard skill ≠ trading skill** — is a fresh, quotable confirmation that financial-QA/reasoning ability doesn't transfer to returns ([5][7][13]). For us it both (a) reinforces "keep the LLM off the alpha rail" and (b) validates our discipline that any LLM signal must be judged on genuinely out-of-sample, sequential, cost-aware performance — not benchmark accuracy. Note it doesn't claim a winner, which is itself telling. Strong anchor citation.

### [60] RiskLabs: Predicting Financial Risk Using Large Language Model based on Multimodal and Multi-Sources Data
- **Authors / Venue:** Yupeng Cao, Zhi Chen, Prashant Kumar, ..., K.P. Subbalakshmi, Papa Momar Ndiaye / arXiv (q-fin.RM)
- **Year:** 2024 (rev. 2025)
- **Source:** arXiv:2404.07452
- **% read:** 45% (abstract + method + results framing via abstract page)
- **Summary:** Applies LLMs to **financial *risk* forecasting** (deliberately, not return/trading), fusing three modalities: **earnings-call text + vocal/audio features**, market **time-series** data, and **news** context, to predict **market volatility and variance.** Reports the multimodal fusion is effective at forecasting volatility/variance and analyzes each source's contribution, finding the LLM plays a crucial role. (Abstract gives no specific numbers / explicit baseline table.)
- **Relevance to our system:** Strongly supportive of our most important nuance: **the forecastable financial target is *volatility/risk*, not direction/return** — RiskLabs deliberately aims LLMs at *risk* and reports success there, mirroring the realized-vol findings [19][43] and the CVaR-RL framing [41]. It is also a clean **multimodal (text + audio + price + news)** example (fills the multimodal seam). For our advisor the takeaway is consistent and twofold: (a) if an LLM/forecasting component is ever justified, point it at **risk/sizing** (a vol estimate to drive a gated overlay), never at return-alpha; (b) the audio/earnings-call modality has no crypto analogue, but the *principle* — fuse numeric + text for a *risk* estimate — is the defensible direction. Catalog as evidence for "LLMs/forecasting help risk, not return."

### [61] One Fits All: Power General Time Series Analysis by Pretrained LM (GPT4TS / FPT)
- **Authors / Venue:** Tian Zhou, Peisong Niu, Xue Wang, Liang Sun, Rong Jin (Alibaba DAMO) / NeurIPS 2023
- **Year:** 2023
- **Source:** arXiv:2302.11939
- **% read:** 70% (full ar5iv read — freezing scheme, all task tables, PCA argument, the pretrained-vs-random ablation, limitations)
- **Summary:** The canonical **"freeze a pretrained LM, fine-tune the edges"** approach (predecessor to Time-LLM [23]). Takes the first 3–6 layers of GPT-2, **freezes self-attention + FFN**, trains only input embedding, layer-norm (+ optionally positional emb) + output (~4.6–6.1% of params). Results: long-term forecasting MSE 0.516 vs TimesNet 0.596 (13.4% better); classification 74.0% vs PatchTST 65.0%; anomaly F1 86.72%; few-shot (10%) 0.371 MSE (33% over CNN); zero-shot 13.1% over DLinear. Headline theoretical claim: **frozen self-attention "behaves similarly to PCA"** (Theorem 1: the attention-reconstruction optimum is the input-covariance eigenvectors), with token-similarity rising to 0.9+ in deep frozen layers vs 0.1–0.2 random. **Decisive full-read detail — the paper's OWN ablation (Table 7) argues pretraining matters:** GPT2(6)-FPT vs random-init, e.g. **ETTh1 0.427 vs 1.326**, ETTh2 0.346 vs 0.666, Weather 0.237 vs 0.277. General benchmarks only; **no finance/crypto, no PnL.**
- **Relevance to our system:** The original evidence that you can repurpose a *frozen LM's* weights for numeric forecasting — and the full read exposes the same conflict as Time-LLM [23]: **GPT4TS's in-house Table-7 ablation claims pretrained ≫ random-init (ETTh1 0.43 vs 1.33), yet [25] and [73] later refute exactly this** with controlled from-scratch-equivalent baselines (LLM ablates out; random-init = pretrained once capacity matches). The discrepancy is again instructive — GPT4TS's "random-init" baseline keeps the model under-trained on small data (the overfitting [73] diagnoses), so it *looks* like pretraining helps. Even the "attention = PCA" argument cuts against the LLM thesis: if frozen attention is just doing PCA, a *cheap* linear method captures it — no language prior needed. For us: more reason not to reach for a frozen chat LLM to forecast crypto, and "comparable to SOTA on weather" says nothing about crypto return-alpha. Background/catalog; **[25]/[73] bind over its own ablation.**

### [62] UniTime: A Language-Empowered Unified Model for Cross-Domain Time Series Forecasting
- **Authors / Venue:** Xu Liu, Junfeng Hu, Yuan Li, Shizhe Diao, Yuxuan Liang, Bryan Hooi, Roger Zimmermann / ACM Web Conference (WWW) 2024
- **Year:** 2023 (WWW 2024)
- **Source:** arXiv:2310.09751
- **% read:** 45% (abstract + method + results framing via abstract page + search summary)
- **Summary:** A *single unified* cross-domain forecaster (vs one model per dataset). Uses **domain instructions** (natural-language tags identifying the source domain) + a **Language-TS Transformer** to let one model disambiguate and serve many domains at once, plus masking to handle different convergence speeds across domains. Improves **37 of 80** benchmark entries to new SOTA vs separately-trained baselines and shows gains in **zero-shot transfer** to unseen domains. Benchmarks are the standard general set; **no financial/crypto evaluation, no trading/PnL claim.**
- **Relevance to our system:** Sits in the text-empowered TSFM branch (with GPT4TS [61], Time-LLM [23]) — the "language" here is *domain-identification text*, not a price-predicting prior. For the operator's question it's another "yes, people bolt language onto numeric forecasting," but the honest reads are familiar: (a) the win is *partial* (37/80, i.e. not even a majority of entries) on calm general benchmarks; (b) no finance/crypto, no PnL; (c) the language component's role is housekeeping (which-domain-is-this), which a single-coin advisor doesn't need — we have exactly one domain. Combined with [25]'s ablation, no basis to expect the language-empowerment helps crypto returns. Background/catalog.

### [63] Ethereum Price Prediction Employing Large Language Models for Short-term and Few-shot Forecasting
- **Authors / Venue:** (LLM-for-crypto-TS group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2503.23190
- **% read:** 60% (abstract + method + datasets + results tables via HTML full text)
- **Summary:** Applies **frozen-backbone LLMs (Llama-3, Llama-2, GPT-2)** *directly to numeric ETH price series* (not sentiment) — the GPT4TS [61]/Time-LLM [23] recipe on our own asset class. For Llama they freeze grouped-query-attention + FFN and fine-tune RMSNorm + rotary embeddings; for GPT-2 they freeze attention + FFN and tune positional embeddings + layer-norm. Univariate forecasting: 7-day input → 1-day-ahead, plus a few-shot variant (10% of training data), on a 2016–2023 daily OHLCV Kaggle set + an Ethereum-node transaction-level set. Headline: **Llama-3 reaches "SOTA"** — short-term RMSE 0.0518 (normalized), beating LSTM/MLP/ANN/PatchTST. **No naive/random-walk baseline, no trading PnL, no Sharpe, no transaction costs.**
- **Relevance to our system:** A directly on-asset entry for the operator's question (frozen LLM → numeric ETH), and a near-textbook illustration of the **price-level-forecasting trap** our gate guards against. Predicting next-day *price level* from a 7-day window yields tiny MSE/RMSE *trivially*, because tomorrow's price ≈ today's price — a **random walk would score almost identically**, yet the paper benchmarks only against other learned models (LSTM/MLP/PatchTST), **never against a naive/persistence baseline**, and never converts accuracy to **return-direction or PnL net of costs.** So "Llama-3 beats LSTM on RMSE" tells us nothing about beating buy-and-hold — the forecastable part (level persistence) is exactly what B&H already captures, and the un-forecastable part (the *return*) is untouched. Combined with [25]'s ablation (the frozen LLM likely contributes little) this is weak evidence dressed as a win. Strong cautionary example for the operator: *low price-level error ≠ tradeable edge*; demand a random-walk benchmark and a return/PnL metric.

### [64] TimeGPT's Potential in Cryptocurrency Forecasting: Efficiency, Accuracy, and Economic Value
- **Authors / Venue:** (crypto-TSFM evaluation group) / Forecasting (MDPI) 2025, 7(3):48
- **Year:** 2025
- **Source:** DOI:10.3390/forecast7030048 (MDPI Forecasting); also ResearchGate
- **% read:** 45% (abstract + RePEc + two search passes cross-checked; full text still 403-blocked — deeper read partially blocked)
- **Summary:** A peer-reviewed, multi-dimensional evaluation of **nine models** (baseline + zero-shot TSFMs + deep-learning) on **21 major cryptocurrencies**, daily and hourly, scored on accuracy (MAE/RMSE/MAPE), *speed*, statistical significance (**Diebold-Mariano**), and *economic value* (**Sharpe** under a long/short strategy). Cross-checked findings: it is **fine-tuned TimeGPT *without* variables** that leads on accuracy on both frequencies (DM-confirmed), with **fine-tuned Chronos** excelling on daily and **TFT** a close second on hourly; zero-shot **TimeGPT and Chronos** are *tens of times faster* than DL models. Economic-value headline: it is **TimeGPT *with* variables** (not the accuracy-leading config) that yields the eye-popping **ETH Sharpe 4.2947 under a long/short strategy**, while zero-shot **Chronos is best on BTC at Sharpe ~1.03.** The authors' stated caveat: "a model's effectiveness is highly dependent on market characteristics" and "variables have asset-specific effects." (Transaction-cost treatment and the explicit B&H benchmark are in the full text, which stays 403-blocked.)
- **Relevance to our system:** One of the **most directly on-point** entries for the operator's question — TSFMs (TimeGPT, Chronos) evaluated on *crypto*, with an *economic* (Sharpe) metric, peer-reviewed. And it is exactly where skepticism earns its keep. (1) The eye-popping **Sharpe 4.29 on ETH** comes from a **long/short** strategy (we are long-only single-coin — inapplicable) with **no stated cost treatment** in the accessible text; a 4+ Sharpe from daily crypto signals is the "profit mirage" pattern [30][42] that historically collapses under fees. (2) **The config that forecasts *best* (fine-tuned TimeGPT *without* variables) is NOT the config that produces the big Sharpe (TimeGPT *with* variables)** — a textbook **error/profit decoupling** [82]: forecasting accuracy and trading profitability come apart, so the headline economic number is not riding on the accuracy win. (3) Tellingly, on **BTC** (most liquid/efficient, closest to our default) the best Sharpe is only **~1.03** — barely distinguishable from buy-and-hold in a bull-tilted sample. (4) TimeGPT is closed/SaaS (wrong substrate for our local Rust advisor). Net: real crypto evidence that TSFMs *can* forecast, but the tradeable claim rests on long/short + an accuracy-decoupled config + (un-stated) costs; under our long-only, cost-aware, 1000-path bootstrap-vs-B&H gate the **BTC ~1.0 Sharpe is the honest signal — no robust edge.** Strongest crypto-specific TSFM citation, read skeptically.

### [65] BreakGPT: Leveraging Large Language Models for Predicting Asset Price Surges
- **Authors / Venue:** (LLM-crypto-breakout group) / arXiv (cs.LG)
- **Year:** 2024
- **Source:** arXiv:2411.06076
- **% read:** 60% (abstract + method + results table + limitations via HTML full text)
- **Summary:** An LLM-based (modified **GPT-2** + Transformer) architecture for **binary classification of price *surges*** — predicting whether the current sequence signals the start of a sharp upward move. Despite the framing, actual experiments are **only on Solana** (5-min OHLC resampled from 1-min, Feb–Aug, with one month held out), with engineered TA features (SMA/EMA/RSI/Bollinger) and a 0.5% move as the "surge" threshold. Results are weak on the class that matters: uptrend **F1 = 0.16** for BreakGPT vs 0.20 for a ConvTransformer and 0.12 for a plain Transformer — while "overall accuracy" is **0.95 simply because non-surge is the majority class.** **No trading PnL, no transaction costs, no buy-and-hold comparison;** authors concede the low F1 implies poor practical utility and an unresolved class-imbalance problem.
- **Relevance to our system:** A genuinely useful *negative* crypto result for the operator's question. An LLM aimed at the alpha-relevant event (catching upside breakouts on a crypto asset) **fails on the meaningful metric** (F1 ≈ 0.16) and is beaten by a small ConvTransformer — i.e. the LLM adds nothing, echoing the ablation [25]. It also illustrates the **accuracy-paradox trap** our gate is immune to: 0.95 "accuracy" is just predicting "no surge" always, which is worthless for timing. For us: direct on-asset evidence that LLM breakout-prediction on crypto is not tradeable, and a reminder to never trust a headline "accuracy" number on an imbalanced up/down task — the bootstrap-vs-B&H net-of-cost equity comparison is the only honest test. Catalog as on-topic negative result.

### [66] Assessing the Operational Viability of Foundation Models for Time Series Forecasting
- **Authors / Venue:** (TSFM operational-evaluation group) / arXiv
- **Year:** 2026
- **Source:** arXiv:2605.24381
- **% read:** 45% (abstract + findings + finance framing via abstract page)
- **Summary:** A deployment-economics evaluation of zero-shot TSFMs vs supervised specialists across **four operational regimes** — periodic systems, physically-constrained processes, **financial markets**, and demand forecasting. Findings: TSFMs excel where there are **transferable periodic structures** and on cold-start/long-tail series, and newer models are "rapidly closing the gap" in finance; but supervised specialists keep an edge on physically-constrained systems, and TSFMs carry real trade-offs in **latency, data-drift adaptation, and deployment cost.** Proposes a **"Complexity Router"** that assigns each series to the cheapest adequate model class, beating "deploy one universal TSFM everywhere" on both accuracy and inference cost.
- **Relevance to our system:** Reinforces the operator's-question verdict from the *operations* angle: a giant TSFM is rarely the cost-justified choice — route easy series to a small model, reserve heavy machinery for where it pays. For a lean local Rust advisor that's decisive — it argues *against* bolting a foundation model onto crypto forecasting and *for* small/cheap models ([56] TTM, [53] PatchTST, [57] N-HiTS), consistent with [26][43]. The finance finding is hedged ("closing the gap," not "winning"), and crypto's defining feature is *weak* periodic structure + heavy drift — exactly where this paper says TSFMs are least advantaged and most costly. The "complexity router" idea is a nice general pattern (match model cost to task difficulty) but doesn't change our conclusion: no operationally-justified case for a TSFM on crypto returns. Background/operations catalog.

### [67] CTBench: Cryptocurrency Time Series Generation Benchmark
- **Authors / Venue:** Yihao Ang, Yifan Bao, et al. / arXiv (ICLR 2026)
- **Year:** 2025 (ICLR 2026)
- **Source:** arXiv:2508.02758
- **% read:** 50% (abstract + framework + key-finding framing via abstract page + search summary)
- **Summary:** The first **crypto-specific time-series *generation* (TSG)** benchmark — i.e. evaluating models that produce *synthetic* crypto series for data augmentation / stress testing / strategy prototyping. Curates 452 tokens; evaluates 8 generators (5 methodological families) over **4 market regimes** on **13 metrics in 5 dimensions** — forecasting accuracy, rank fidelity, **trading performance**, **risk assessment**, computational efficiency. A **dual-task** design separates *predictive utility* (does synthetic data preserve patterns for forecasting?) from *statistical arbitrage* (do reconstructed series support mean-reverting trade signals?). Headline finding: a clear **trade-off between statistical fidelity and real-world profitability** — **high statistical quality does NOT guarantee profitable / tradeable synthetic data.**
- **Relevance to our system:** Tangential to the alpha question but directly **validates a core design choice in our gate**: it's about *generating* synthetic crypto paths, which is the alternative to our **moving-block bootstrap resampling of real data.** Its central result — *statistically realistic synthetic series still fail to support profitable trading* — is strong evidence that fancy generative models (GANs/diffusion/TSFM-sampled paths) are **not** a safe substitute for resampling actual history when stress-testing a strategy. Combined with [44] (LLM-simulated markets are unrealistically calm), this reinforces keeping our 1000-path engine on **real-data block-bootstrap**, not model-generated paths. The "trading performance + risk" evaluation dimensions also mirror our own (don't trust a generator/forecaster on statistical metrics alone — test it on PnL + risk). Catalog as support for the bootstrap-not-generate design, and a caution about synthetic-data shortcuts.

### [68] Exploring LLM Cryptocurrency Trading Through Fact-Subjectivity Aware Reasoning (FS-ReasoningAgent)
- **Authors / Venue:** Qian Wang, Yuchen Gao, Zhenheng Tang, Bingqiao Luo, Bingsheng He / arXiv (also ICLR 2025 workshop)
- **Year:** 2024
- **Source:** arXiv:2410.12464
- **% read:** 65% (abstract + method + full results table + ablations + limitations via HTML)
- **Summary:** A well-designed crypto LLM-trading study (CryptoTrade [31] successor). It first observes that *stronger* LLMs sometimes trade *worse* because they over-weight facts vs sentiment, then builds a 7-agent framework separating **fact** vs **subjectivity** reasoning paths (Statistics / Fact / Subjectivity / two Reasoning agents / Trade / Reflection). Tested on **BTC, ETH, SOL, Nov-2023 → Jul-2024** (deliberately **after** the GPT cutoff to avoid leakage), with **proportional transaction fees** and a full B&H comparison. Crucial honest result: returns are **comparable to — not better than — buy-and-hold** (e.g. BTC bull 76.19% vs **B&H 79.63%**; ETH bull 77.28% vs 78.72%; SOL bull 76.71% vs 77.35%), modestly *limiting losses* in bears (BTC bear −15.91% vs B&H −19.15%). Bull-market gains lean on the *subjectivity* agent; bear-market loss-control leans on the *fact* agent.
- **Relevance to our system:** Among the **most credible crypto LLM-agent papers** for us precisely because it does the discipline right — **post-cutoff window, transaction fees, explicit B&H, our exact assets (BTC/ETH/SOL)** — and the honest verdict is *exactly our thesis*: the agent **tracks B&H in bulls (slightly below) and only mildly cushions bears.** That is "no robust edge over holding," shown on our own coins by a careful study. Two genuinely useful nuances: (a) the bear-market loss-limiting (~3 pts) is the *one* place active management plausibly helps — a **risk/drawdown** angle, not alpha, consistent with [19][41][60]; (b) the fact-vs-subjectivity regime dependence is a real behavioral insight (sentiment leads in rallies, facts steady panics). But even here, beating *some* LLM baselines by 7–10% while *still trailing B&H in bulls* is the point: the benchmark that matters (holding) is not beaten. The Reflection-agent dependency (performance degrades without it) is also a regime-adaptability red flag. Strong, fair citation: "a careful, cost-aware, post-cutoff crypto LLM agent comes out ≈ buy-and-hold."

### [69] From Hypotheses to Factors: Constrained LLM Agents in Cryptocurrency Markets
- **Authors / Venue:** (constrained-LLM-factor group) / arXiv (q-fin)
- **Year:** 2026
- **Source:** arXiv:2604.26747
- **% read:** 60% (abstract + method + OOS results + limitations via HTML)
- **Summary:** A methodologically rigorous LLM-for-alpha design that treats **factor discovery as constrained hypothesis search**: an LLM proposes *falsifiable* factor hypotheses inside a restricted **domain-specific language** (cross-sectional ranks, lags, rolling stats, log/abs, linear combos — **no forward-looking features**), while a **deterministic evaluation engine** enforces fixed splits and selection gates that *the agent cannot modify* ("reasoning separated from evaluation"). Daily CoinMarketCap data 2020–2025; **train 2020–22, validate 2023, pure OOS 2024–26.** 25 factors over 5 rounds; converges on **small, liquidity-scarce tokens with persistent range + positive trend.** Reports **OOS Sharpe 1.55** (equal-weight long-short), 44.6% annualized, **5 bps one-way costs included**, top single-factor Sharpe 2.41. **But: the market-cap-weighted version "performs poorly" — the alpha is concentrated in small tokens; no explicit B&H baseline.**
- **Relevance to our system:** A double-edged, instructive entry. On the plus side it is the **best test-discipline** in the crypto-LLM set — strict train/val/OOS separation, an evaluation engine the LLM *cannot* game, costs included — essentially an LLM-driven analogue of our gate (the agent searches; a frozen rule judges). That architecture (LLM proposes, immutable gate disposes) is *exactly* our posture and worth citing as the right way to let an LLM near alpha. On the skeptical side, the headline Sharpe 1.55 **dissolves under our constraints**: (a) it is **cross-sectional long-short over many small tokens** — structurally impossible for a long-only *single-coin* advisor; (b) the alpha is **concentrated in small, illiquid tokens** (cap-weighted fails), i.e. the classic illiquidity-premium mirage [38] where real costs/slippage on tiny tokens would gut it (5 bps is wildly optimistic for microcaps); (c) **no buy-and-hold benchmark** is reported, so "44% annualized" in the 2024–25 crypto bull is not shown to beat just holding. Net: great *methodology* (constrained search + immutable evaluator), but the *result* is illiquid-small-cap long-short alpha, inapplicable and likely fragile for our liquid single-coin setting. Cite for the design, discount the edge.

### [70] Structured Agentic Workflows for Financial Time-Series Modeling with LLMs and Reflective Feedback (TS-Agent)
- **Authors / Venue:** (TS-Agent group) / arXiv (cs.LG / q-fin)
- **Year:** 2025
- **Source:** arXiv:2508.13915
- **% read:** 55% (abstract + method + results tables + conclusion via HTML)
- **Summary:** An *agentic* framework where an LLM **iteratively builds and refines the forecasting *pipeline*** (not the forecast itself): two stages — case-based model pre-selection, then cycles of code refinement + hyperparameter tuning + execution-feedback — backed by dynamic memory and knowledge banks (case library, refinement tips, model implementations). Evaluated on **hourly prices for 20 crypto pairs (2024)**, FX (1990–2010), and 10 US stocks, on forecasting + synthetic generation. Beats AutoML (AutoGluon/Optuna) and other agent frameworks (DS-Agent/ResearchAgent) on **RMSE** (crypto 0.206–0.277 vs 0.297–0.392) and **robustness** (100% run success vs 40–80%), with ~20% better Sharpe/VaR *differences*. **No actual PnL or buy-and-hold comparison** is reported.
- **Relevance to our system:** A clean example of the **defensible LLM role on the numeric side**: the LLM is an *engineering orchestrator* (it writes/tunes forecasting code reliably), not a price oracle. The genuine, reusable win is **robustness/automation** — 100% successful pipeline runs where generic LLM automation fails 40–60% of the time — which is about *developer productivity*, not alpha. For the operator's question it adds: even when an LLM helps with financial time series, the demonstrated benefit is *better RMSE + reliable tooling*, **not** demonstrated trading edge (the authors themselves say profitability "remains undemonstrated"). It also reports *risk-metric differences* (Sharpe/VaR) but no PnL-vs-B&H — the same accuracy≠edge gap. For us: supports using an LLM to *assist building/validating* our pipeline (orchestration), while the gate decides what trades. Catalog as LLM-as-pipeline-engineer evidence, not alpha.

### [71] AlphaSharpe: LLM-Driven Discovery of Robust Risk-Adjusted Metrics
- **Authors / Venue:** (AlphaSharpe group) / arXiv (q-fin / cs.LG)
- **Year:** 2025
- **Source:** arXiv:2502.00029
- **% read:** 55% (abstract + method + validation + conclusion via HTML v2)
- **Summary:** Uses an LLM in an **evolutionary loop** (crossover + LLM-suggested mutation) to *discover new risk-adjusted performance **metrics*** — alternatives to the Sharpe ratio — rather than to forecast prices. Validated out-of-sample on **15 years × 3,246 US stocks** by how well a metric computed on past data **ranks future Sharpe/returns** (Spearman, Kendall, NDCG@25%). Reports the discovered metrics have **>3× higher rank-correlation with future performance** than traditional Sharpe and ~90% better portfolio outcomes from improved selection. Explicitly claims **better *metrics*, not alpha** — the metrics rank assets better, they don't themselves generate returns; acknowledges deflated-Sharpe/overfitting concerns and uses time-series CV.
- **Relevance to our system:** Unusually **directly relevant to our gate-improvement roadmap** (adding Deflated-Sharpe / PBO). It's the rare LLM-finance paper aimed at *selection robustness* — building a performance metric that *generalizes out-of-sample* — which is exactly the problem Deflated-Sharpe/PBO solve and exactly what our 1000-path bootstrap ranking needs. Two takeaways: (a) it confirms the *right* place for an LLM in a quant pipeline is **meta-level (designing/critiquing the evaluation metric)**, not the alpha rail — consistent with our "LLM as research assistant" posture; (b) but it's a *caution* too — an LLM **evolving metrics to maximize out-of-sample rank-correlation is itself a multiple-testing engine**, so its "3× better" claim needs the very Deflated-Sharpe deflation it cites (search over many candidate metrics inflates the winner). For us: interesting input to the gate-metric discussion, but our **frozen** gate (fixed metric, no per-run metric search) is the safer design precisely because metric-search invites overfitting. Cite in the Deflated-Sharpe/PBO context as "LLMs can help design robustness metrics — but metric-search needs its own overfitting correction."

### [72] FinBERT-BiLSTM: A Deep Learning Model for Predicting Volatile Cryptocurrency Market Prices Using Market Sentiment Dynamics
- **Authors / Venue:** (FinBERT-BiLSTM group) / arXiv (cs.LG / q-fin)
- **Year:** 2024
- **Source:** arXiv:2411.12748
- **% read:** 65% (full ar5iv read — fusion, all result tables, trading-profit tables, cost treatment, limitations)
- **Summary:** Fuses **FinBERT** [35] daily sentiment with a **3-layer BiLSTM (55/25/20 units)** by *concatenating the current-day sentiment score onto the price sequence* before the LSTM. Assets **BTC + ETH**, **Jan–Aug 2023 (585 days)**; prices from Yahoo, news from CoinDesk/Binance. The accuracy tables are a textbook **price-level trap**: e.g. BTC one-day-ahead MAE 1223 / **MAPE 0.019% / "accuracy" 98.07%** (FinBERT-BiLSTM) vs plain LSTM MAE 1453 / 97.68% — the headline "98% accuracy" is just next-day-price ≈ today's price. Full read DID surface a trading section *with costs* (transaction rate 0.0001 BTC / 0.003 ETH): over 88 test days FinBERT-BiLSTM "profit" **$21,580 (BTC) / $35,219 (ETH)** vs FinBERT-LSTM $5,808/$28,696 — **but the benchmark is "perfect prediction" ($41,234/$68,452) and plain LSTM/BiLSTM, which executed *no trades*; there is NO buy-and-hold comparison and no Sharpe/directional-accuracy.**
- **Relevance to our system:** A representative **crypto sentiment-plus-price** hybrid on our exact assets — and on the full read a *cleaner* cautionary example than before. It (commendably) includes transaction costs, yet still falls into the two traps our gate is built for: (a) the **price-level-accuracy illusion** — "98% accuracy / 0.019% MAPE" is the level-persistence trick ([63][18]), where a random walk scores almost identically and which says nothing about return direction; (b) **no buy-and-hold benchmark** — the trading profit is measured against "perfect foresight" and against baselines that *made no trades*, so "beats LSTM" is not "beats holding BTC" (and over Jan–Aug 2023, simply *holding* BTC/ETH returned a great deal). So despite costs, there is **no gate-credible evidence of edge over B&H**. The genuine catalog value: the recurring "add FinBERT sentiment to a price net" recipe ([10][39][42][87]) improves *level-accuracy* but is never shown to beat holding net of costs. Standing rule applies. (Upgraded from 30%: prior note that results weren't retrievable was wrong — the full text has them, and they reinforce the level-trap/no-B&H critique.)

### [73] From Text to Time? Rethinking the Effectiveness of the Large Language Model for Time Series Forecasting
- **Authors / Venue:** (LLM4TS critique group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2504.08818
- **% read:** 55% (abstract + controlled-experiment design + findings via HTML)
- **Summary:** A second rigorous ablation (after [25]) of whether the *language* backbone helps numeric forecasting — with a cleaner controlled design: **three identically-architected models differing only in pretraining** — (A) text-pretrained GPT-2, (B) time-series-pretrained GPT, (C) **randomly-initialized** GPT. Finding: **Model C (no pretrained knowledge at all) matches or exceeds Model A in many zero-shot tests**, and "a Transformer trained from scratch on ~50M time-series samples matches a frozen GPT-2 backbone." Diagnoses *why* prior LLM-for-TS papers looked good: on **small public datasets the encoder/decoder overfit**, masking the backbone's (non-)contribution; as dataset size grows, LLM models **degrade** relative to simpler baselines. Benchmarks general (electricity/traffic/exchange); no finance-specific conclusion.
- **Relevance to our system:** Reinforces [25] with an even more controlled experiment — the "language" in language-model-for-time-series is **inessential**: a random-init transformer of the same shape does as well, so the gains attributed to LLM pretraining are an artifact of small-data encoder/decoder overfitting. For the operator's question this is load-bearing on the *text-LLM-repurposing* branch (GPT4TS [61], Time-LLM [23], TEMPO/aLLM4TS): don't expect the language prior to help forecast crypto. It also delivers a *meta-lesson our gate embodies*: **small-dataset adaptation can masquerade as capability** — exactly the overfitting our 1000-path bootstrap + (planned) Deflated-Sharpe/PBO exist to expose. Strong second citation for "the LLM ablates out of numeric forecasting."

### [74] TEMPO: Prompt-based Generative Pre-trained Transformer for Time Series Forecasting
- **Authors / Venue:** Defu Cao, Furong Jia, Sercan O. Arik, Tomas Pfister, Yixiang Zheng, Wen Ye, Yan Liu / ICLR 2024
- **Year:** 2023 (ICLR 2024)
- **Source:** arXiv:2310.04948
- **% read:** 40% (abstract + method framing via abstract page + search summary)
- **Summary:** A GPT-based TS forecaster combining two inductive biases: (1) explicit **decomposition** of the series into **trend / seasonal / residual** components fed separately, and (2) **soft prompts** that help the model adapt across different time-series distributions. Built on a GPT backbone; reports strong **zero-shot** performance on standard benchmarks (incl. unseen datasets and multi-modal inputs), positioned as a foundation model. Benchmarks are general (ETT/weather/electricity/etc.); **no financial/crypto evaluation, no trading/PnL.**
- **Relevance to our system:** Another entry in the prompt/decomposition-based LLM-for-TS branch (with S2IP-LLM, aLLM4TS, GPT4TS [61]). Its genuinely portable idea is **decomposition** (trend/seasonal/residual) — but note that decomposition is a *classical* signal-processing trick, not an LLM capability, and crypto returns have weak seasonal/trend structure to decompose. For the operator's question TEMPO is in the family the ablations [25][73] target: the GPT backbone is likely doing little beyond the decomposition + prompt machinery. No finance/crypto/PnL evidence. Background/catalog — the decomposition idea is the only transferable part, and it's not LLM-specific.

### [75] FinCon: A Synthesized LLM Multi-Agent System with Conceptual Verbal Reinforcement for Enhanced Financial Decision Making
- **Authors / Venue:** Yangyang Yu, Zhiyuan Yao, Haohang Li, ..., Khaldoun Khashanah, Jordan W. Suchow (FinMem authors) / NeurIPS 2024
- **Year:** 2024
- **Source:** arXiv:2407.06567
- **% read:** 40% (abstract + architecture framing via abstract page)
- **Summary:** A multi-agent LLM system (from the FinMem [2] group) structured as a real investment firm — a **manager-analyst communication hierarchy** — with **"conceptualized beliefs"** acting as *verbal reinforcement*: episodic self-critiques update beliefs and propagate selectively (not full peer-to-peer) to cut communication cost. Applied to **single-stock trading and portfolio management**, claiming "impressive investment returns" and surpassing human teams while reducing communication overhead. (The abstract does not state assets, windows, baseline/B&H gaps, transaction costs, or post-cutoff/leakage controls.)
- **Relevance to our system:** A more sophisticated descendant of the FinMem/TradingAgents lineage [2][3], and inherits their evidentiary gaps for our purposes: "impressive returns / surpasses human teams" with **no stated costs, window, or leakage control** in the abstract — the same setup our gate rejects ([30] profit mirage). The genuinely interesting *architecture* idea is **conceptual verbal reinforcement** (beliefs refined by self-critique and selectively propagated) — a structured way to do reflective memory, mildly relevant to a narration/research layer. But as alpha evidence it's the usual story. For us: catalog as agent-architecture inspiration (belief refinement, manager-analyst roles for *narration*), not as proof LLMs beat holding — verify any return claim through the cost-aware, post-cutoff, bootstrap-vs-B&H gate.

### [76] QuantAgent: Price-Driven Multi-Agent LLMs for High-Frequency Trading
- **Authors / Venue:** (QuantAgent group) / arXiv (q-fin.TR / cs.AI)
- **Year:** 2025
- **Source:** arXiv:2509.09995
- **% read:** 60% (abstract + agent design + results table + limitations via HTML v3)
- **Summary:** A **price-only** (no news/sentiment) multi-agent LLM for higher-frequency trading: IndicatorAgent (RSI/MACD/etc.), PatternAgent (chart shapes), TrendAgent (support/resistance), and a Risk/Decision agent emitting LONG/SHORT with stop/take levels. Notably *excludes* text because it "lags price discovery." Benchmarked on **9 assets incl. BTC/USD**, 1h/4h candles, 100-bar windows with the last 3 bars withheld (proper leakage isolation). Reports **50.7–63.7% directional accuracy** (4h) vs random/linear/XGBoost, best close-to-close returns on 7/8 assets. Honest about failure on ultra-short (1–15 min) candles (noise) and **LLM-call latency exceeding the trade window**. **No transaction costs, and NO buy-and-hold/passive comparison** — baselines are other algorithms, not holding.
- **Relevance to our system:** Two genuinely useful points and one decisive gap. Useful: (a) the authors' deliberate choice to **drop text/sentiment because it lags price** is a direct, independent vote for our **"numbers > text"** theme ([34][50]); (b) the latency honesty (an LLM call can be slower than the opportunity it's chasing) is a real argument against LLMs on the fast decision rail. Decisive gap: **no buy-and-hold benchmark and no costs** — "55% directional accuracy" is meaningless for us without net-of-cost equity vs holding (a coin-flip-plus on direction routinely loses to costs + B&H, exactly what our gate measures). And HFT itself is out of scope for a daily-ish single-coin advisor. Catalog as: supports numbers-over-text + latency caution; not gate-credible alpha (no B&H, no costs).

### [77] Automate Strategy Finding with LLM in Quant Investment (QuantAgent / Alpha-mining pipeline)
- **Authors / Venue:** (LLM-alpha-mining group) / arXiv (q-fin.PM / cs.CE)
- **Year:** 2024 (rev. 2025)
- **Source:** arXiv:2409.06289
- **% read:** 55% (abstract + pipeline + results table + costs/limitations via HTML v4)
- **Summary:** A 3-stage LLM alpha-mining pipeline: (1) GPT-4o reads financial literature and emits **100+ formulaic alpha factors** across 9 categories; (2) multi-agent evaluation filters by Information Coefficient + market conditions; (3) a 3-layer NN maps alpha values to forward yields. On **SSE 50 / CSI 300 (China) + S&P 500**, 2019–2024, with **proper temporal splits** (train 2021–H1'22, val H2'22, test 2023+). Headline (SSE 50, 2023): **53.17% cumulative return, Sharpe 0.287** vs SSE-50 benchmark **−13.22%**, XGBoost 9.53%, FinCon [75] 22.47%; stays positive through 2022 downturns. Turnover capped ~38%/day, **but no slippage/commission/impact modeling** — gross returns only.
- **Relevance to our system:** Better discipline than most (real train/val/test partitioning, an explicit *benchmark index* comparison — rare and welcome). The honest read for us: (a) it does beat its index, but the index fell −13% (a *bear* Chinese-equity year) — "beats a falling benchmark" is the easy case (going flat/defensive wins), exactly what our regime-spanning bootstrap normalizes for; (b) **Sharpe 0.287 is low**, and with **~38% daily turnover and zero cost modeling**, realistic frictions could erase the gross edge (the authors concede this); (c) it's a *cross-sectional* equity factor strategy (top-13/drop-5) — structurally inapplicable to a long-only single coin. The transferable idea — **LLM proposes formulaic factors, a quantitative filter selects** — mirrors [69] and our "LLM proposes, gate disposes" posture. But the result is low-Sharpe, gross-of-cost, cross-sectional equity alpha in a bear window: not evidence for beating B&H on liquid crypto net of costs. Cite for the pipeline pattern, discount the edge.

### [78] Language Models Still Struggle to Zero-shot Reason about Time Series
- **Authors / Venue:** Mike A. Merrill, Mingtian Tan, Vinayak Gupta, Tom Hartvigsen, Tim Althoff / Findings of EMNLP 2024
- **Year:** 2024
- **Source:** arXiv:2404.11757
- **% read:** 55% (abstract + benchmark design + results + verdict via HTML)
- **Summary:** Builds a large benchmark (230k MCQs, 8.7k synthetic series + text captions, 10 domains) to test whether LLMs can *reason* about time series across three tasks: **etiological** (which caption likely produced this series), **QA** (factual questions), and **context-aided forecasting**. Results: LLMs hover **near random** — GPT-4 33.5% on etiology (random 25%, humans 66%); LLaMA ~25% (= random) on two-series QA; and **adding relevant text context yields negligible forecasting improvement, staying near or *below* a simple median-prediction baseline.** GPT-4's apparent competence "dissolves when answers are manually perturbed," indicating memorized artifacts over genuine reasoning. From the same group as the ablation [25].
- **Relevance to our system:** A third independent strike (with [25][73]) against the idea that LLMs *understand* numeric series — here on *reasoning*, not just forecasting. Two points matter for us: (1) **context-aided forecasting barely beats, often loses to, a median baseline** — i.e. feeding an LLM text context (news) to improve a numeric forecast does *not* reliably help, directly undercutting the "LLM reads news → better price forecast" premise behind many trading-agent papers; (2) the "competence dissolves under perturbation" finding is the reasoning-side analogue of the leakage/profit-mirage critique [30][6] — apparent skill is memorized artifact. For the operator's question: LLMs neither forecast nor *reason about* time series reliably, and adding text context doesn't rescue it. Strong skeptic citation; supports keeping LLMs off both the forecasting and the news-into-forecast rails.

### [79] FinRobot: An Open-Source AI Agent Platform for Financial Applications using Large Language Models
- **Authors / Venue:** Hongyang Yang, Boyu Zhang, Neng Wang, et al. (AI4Finance Foundation) / arXiv (NeurIPS 2024 workshop)
- **Year:** 2024
- **Source:** arXiv:2405.14767
- **% read:** 40% (abstract + four-layer architecture via abstract page + search summary)
- **Summary:** An **open-source platform** (not a result paper) for LLM-powered financial *analysis*, structured in four layers: Financial AI Agents (Financial Chain-of-Thought decomposition), Financial LLM Algorithms (per-task model strategy), LLMOps/DataOps (training/fine-tuning + data), and Multi-source LLM Foundation Models (a "Smart Scheduler" picks the best model per task). Explicitly positioned to **democratize** financial AI for analysts and laypeople. Makes **no trading-PnL or beat-the-benchmark claim** — it is infrastructure/tooling (from the same group as FinGPT [1]).
- **Relevance to our system:** A clean *tooling* reference, and a useful contrast to the trading-agent hype: a major open financial-LLM platform frames itself entirely around **analysis / document work / orchestration** and *deliberately makes no alpha claim*. That's the same posture as Coinvisor [52] and Alpha-GPT 2.0 [12] — LLM as research/analysis layer, not signal generator. The **Smart Scheduler** (route each task to the best/cheapest model) is a reusable pattern for a narration layer (cf. the complexity router [66]) and the multi-LLM-behind-a-trait design matches our architecture's "LLM behind an interface." For us: catalog as the canonical open finance-LLM *platform*, evidence that the serious open-source effort targets analysis/tooling, not trading edge. No PnL to gate.

### [80] Revisiting LLMs as Zero-Shot Time-Series Forecasters
- **Authors / Venue:** (LLM-TS re-evaluation group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2506.00457
- **% read:** 40% (abstract confirmed via arXiv abstract page; ar5iv HTML conversion failed + PDF text inaccessible — deeper read blocked)
- **Summary:** A careful re-assessment of the LLMTime [16]-style claim that frozen LLMs zero-shot-forecast numeric series well. Verdict (abstract-confirmed): **no — LLM zero-shot forecasters "often struggle to achieve high accuracy due to their sensitivity to noise, underperforming even simple domain-specific models."** Small input perturbations sharply degrade them, exposing a brittleness prior optimistic studies missed. The paper's recommendation is to abandon zero-shot prompting in favor of **fine-tuning LLMs to better process numerical sequences**. No finance-specific application; methodological focus. (The specific baseline names and per-dataset numbers live in the full PDF, which I could not retrieve — ar5iv returned a fatal conversion error; logged honestly at 40%.)
- **Relevance to our system:** Reinforces the skeptic cluster ([25][73][78][16]) specifically on **noise sensitivity** — the property that matters most for crypto, which is *dominated* by noise. If a frozen LLM falls apart under small perturbations on clean general benchmarks, it has no chance on raw crypto returns (almost pure noise). It also aligns with [19]'s finding that **fine-tuning is essential** (zero-shot isn't enough) — but recall even fine-tuned models only reliably help on *volatility*, not return direction. For the operator's question: another nail in the "off-the-shelf/zero-shot LLM forecasts crypto" coffin. The fine-tuning recommendation is noted but out of scope (and the prior [27][28] says even fine-tuned generic TSFMs barely beat random walk on returns). Skeptic catalog.

### [81] Learning to Generate Explainable Stock Predictions using Self-Reflective Large Language Models (SEP)
- **Authors / Venue:** Kelvin J.L. Koa, Yunshan Ma, Ritchie Ng, Tat-Seng Chua (NUS) / ACM Web Conference (WWW) 2024
- **Year:** 2024
- **Source:** arXiv:2402.03659
- **% read:** 50% (abstract + method + results framing via abstract page + search summary)
- **Summary:** A **Summarize-Explain-Predict (SEP)** framework that teaches an LLM to produce **explainable** stock predictions *without* human-annotated explanations: a self-reflective agent learns to explain past moves via self-reasoning, and a **PPO** trainer fine-tunes the model on self-generated explanations. Reports the fine-tuned LLM **beats traditional DL and other LLM methods on prediction accuracy + Matthews correlation** for stock-movement classification, and can also be used for portfolio construction. Strong emphasis on the *explanation* quality (verbalizing why factors matter), not just the label.
- **Relevance to our system:** Sits at the productive intersection for us — **explainability**, which is exactly our intended LLM job (the "why this one" narration). The self-reflective, no-human-labels mechanism is a clever way to generate rationales, and the *explanation*-centric framing matches how we'd evaluate a narration layer (is the reasoning sound/grounded), echoing Coinvisor [52]. The skeptical caveat is the usual one on the *prediction* half: it reports **classification accuracy / MCC**, not PnL-vs-B&H net of costs, on stock movements (inapplicable to single-coin crypto), so the "beats other methods at prediction" claim is not tradeable-edge evidence ([50][7]: directional classification is near-chance and not practically useful). For us: a good reference for **LLM-generated grounded explanations** (the narration seam), while the prediction claims stay subject to the gate. Catalog as explainability inspiration.

### [82] FinTSB: A Comprehensive and Practical Benchmark for Financial Time Series Forecasting
- **Authors / Venue:** Yifan Hu, Yuante Li, Peiyuan Liu, Yuxia Zhu, Naiqi Li, Tao Dai, Shu-tao Xia, Dawei Cheng, Changjun Jiang (Tongji FinLab) / arXiv
- **Year:** 2025
- **Source:** arXiv:2502.18834
- **% read:** 60% (abstract + framework + key findings + model list via HTML)
- **Summary:** A rigor-first benchmark for **financial** time-series forecasting that explicitly attacks the three flaws that inflate the literature: the **Diversity Gap** (covers 4 movement regimes — up/down/volatile/extreme — over 15 years, multiple markets), the **Standardization Deficit** (one fixed preprocessing + eval pipeline), and the **Real-World Mismatch** (**includes 0.1% transaction costs, short-selling restrictions, trading halts**). Evaluates **11 metrics in 3 families** — *ranking* (IC/RankIC + stability), *portfolio* (annualized return, vol, MDD, **Sharpe**, IR), and *error* (MSE/MAE, "noting low correlation with profitability"). Includes **foundation models / LLM-for-TS** (Timer, Time-MoE multiple sizes, Chronos). Headline findings: **no method wins all three dimensions**; **XGBoost/LightGBM beat many deep models**; and decisively, **lower forecasting error does NOT guarantee trading profit** — execution matters more than prediction accuracy.
- **Relevance to our system:** Possibly the **best benchmark-grade external validation of our entire gate philosophy** in the whole ledger. It independently operationalizes the exact disciplines we built: **regime-spanning evaluation, costs, and — crucially — measuring *trading/portfolio* outcomes separately from *error* metrics, then finding they decouple** ("lower MSE ≠ more profit"). That is our "directional accuracy/RMSE is not tradeable edge" thesis, proven on a careful financial benchmark *that includes the TSFMs/LLMs* (Time-MoE [55], Chronos [15]). The finding that **gradient-boosted trees beat deep/foundation models** echoes [26][43][57]: small dedicated models win. For the operator's question this is strong: even on a benchmark *built to be fair to them*, TSFMs/LLMs don't dominate financial forecasting, and forecasting accuracy poorly predicts profit. Best citation for "measure PnL under costs, not error — and they come apart." (Note: even its portfolio metrics are cross-sectional ranking IC, still a level above our long-only single-coin, but the principle transfers cleanly.)

### [83] Beyond Accuracy: Are Time Series Foundation Models Ready for Real-World Deployment?
- **Authors / Venue:** (TSFM readiness-evaluation group) / arXiv (preprint, under review)
- **Year:** 2025
- **Source:** arXiv:2510.16060
- **% read:** 45% (abstract + critique axes + findings + verdict via PDF text)
- **Summary:** A deployment-readiness critique of TSFMs that looks **past point-forecast accuracy** at the properties that matter operationally: **uncertainty calibration** (do the confidence intervals mean anything?), **robustness under distribution shift**, **computational cost**, and cross-dataset **generalization**. Evaluates Chronos, TimesFM, Moirai, TOTO, TiReX and finds **calibration failures are widespread** — models are frequently *confidently wrong* or under-estimate uncertainty — and performance varies dramatically by dataset. Verdict: **TSFMs are NOT yet production-ready** for high-stakes use; validate calibration before trusting them.
- **Relevance to our system:** Directly relevant to the *one* use we'd ever consider for a TSFM — a **risk/volatility estimate to drive sizing** — because that use stands or falls on **uncertainty calibration**, which this paper shows is exactly where TSFMs fail (confidently-wrong intervals). It also independently flags the *RLHF-hurts-calibration* concern raised for chat LLMs in [16]. So even the defensible "vol-for-risk" application [19][43] inherits a calibration warning: a miscalibrated vol forecast driving de-risking could *increase* drawdown. For the operator's question, this widens the skeptic case from "accuracy is competitive-not-dominant" to "and the *uncertainty* is unreliable" — a TSFM that can't honestly say *how* unsure it is is dangerous on the risk rail. Reinforces: any forecasting overlay must pass our cost-aware bootstrap-vs-B&H gate, which measures realized equity/drawdown, not the model's self-reported confidence. Strong skeptic/operations citation.

### [84] Chronos-2: From Univariate to Universal Forecasting
- **Authors / Venue:** (Amazon Chronos team) / arXiv
- **Year:** 2025
- **Source:** arXiv:2510.15821
- **% read:** 45% (abstract + architecture + results framing via PDF text + search summary)
- **Summary:** The successor to Chronos [15]: an **encoder-only, ~120M-param Mixture-of-Experts** TSFM that unifies **univariate, multivariate, and covariate-informed** forecasting in one model, using **group attention** for efficient *in-context* cross-learning across related series/covariates. Reports strong **zero-shot** generalization, competitive-or-superior to domain-specific methods, with gains on long horizons and covariate-based tasks (per the broader fev-bench it and TabPFN-TS lead covariate forecasting). **No financial/crypto evaluation, no trading.** Explicitly a *time-series* model (LM techniques inform the architecture but it's purpose-built for numbers).
- **Relevance to our system:** Keeps the operator's-question catalog current — Chronos-2 is the 2025 SOTA on the *numeric-pretrained* branch and adds the genuinely useful capabilities (multivariate + **covariate** support + in-context learning) that a single-coin advisor might one day want (feed BTC + on-chain/funding covariates). But the binding caveats are unchanged: no finance/crypto/PnL evidence, and the independent critiques apply to the whole family — domain-bound [26], shock-fragile [20], miscalibrated [83], doesn't transfer to finance off-the-shelf [27], barely beats random walk on *returns* [28]. A single-coin long-only advisor also rarely needs multivariate machinery. So: note Chronos-2 as the current best general TSFM and the most plausible future *experiment* substrate (open, covariate-aware), but there is still **zero gate-credible evidence it beats holding crypto net of costs.** Catalog/watch.

### [85] FinTSBridge: A New Evaluation Suite for Real-world Financial Prediction with Advanced Time Series Models
- **Authors / Venue:** (FinTSBridge group) / arXiv
- **Year:** 2025
- **Source:** arXiv:2503.06928
- **% read:** 40% (abstract + framing via abstract page + search summary)
- **Summary:** An evaluation suite bridging **modern time-series forecasting models** (>10 recent models, including foundation/advanced architectures) to **real financial asset-pricing prediction**, on three financial datasets. Beyond MSE/MAE it adds finance-specific metrics **msIC / msIR** that capture the *correlation structure* the models actually exploit. Framed as infrastructure to test "effectiveness and robustness of advanced forecasting models in financial domains" — the retrievable abstract stops short of a headline "do they beat simple baselines" verdict (that's in the full paper).
- **Relevance to our system:** A companion to FinTSB [82] confirming a *trend* that matters for the operator's question: the field is building **finance-specific, robustness-aware evaluation suites** precisely because generic accuracy metrics mislead on financial data — the same realization our gate is built on. Its IC/IR-style metrics are cross-sectional (signal-vs-future-return correlation), still a step removed from our long-only single-coin equity-vs-B&H, but the *direction* is identical: don't trust MSE, measure financial efficacy under realistic conditions. I logged it at 40% because the abstract withholds the decisive baseline-comparison result; it adds breadth (another finance-TS benchmark) and reinforces the "measure the right thing" theme, but contributes no specific pro/anti-TSFM number. Catalog as benchmark-infrastructure evidence.

### [86] Can LLM-based Financial Investing Strategies Outperform the Market in Long Run? (FINSABER)
- **Authors / Venue:** (FINSABER group) / arXiv (q-fin.TR / cs.LG)
- **Year:** 2025 (rev. 2026)
- **Source:** arXiv:2505.07078
- **% read:** 80% (full ar5iv read — bias controls, universe, per-stock + regime Sharpe tables, costs, verdict)
- **Summary:** The **definitive long-run, bias-controlled refutation** of LLM-trading alpha. **FINSABER** mitigates **survivorship** (includes delisted S&P 500 constituents 2000–2024), **look-ahead** (inputs aligned to info available pre-window), and **data-snooping** (rolling windows over dynamically-changing selections, not cherry-picked periods), then re-tests **FinMem [2] (GPT-4o-mini + GPT-4o) and FinAgent [4]** vs B&H, SMA/WMA/ATR/Bollinger, ARIMA, XGBoost, and 5 RL methods across **2004–2024 (20y)**, **with commission costs** ($0.0049/share, min $0.99/order; rf 3%). Concrete full-read numbers: on the selected 4 stocks **B&H beats both agents on 3 of 4** — TSLA B&H Sharpe **0.631** vs FinMem 0.641 / FinAgent 0.206; NFLX B&H **0.622** vs FinMem 0.293 / FinAgent **−0.419**; AMZN B&H 0.551 vs 0.188/0.364; MSFT B&H 0.461 vs 0.203/0.285. Best composite (volatility-selection): **B&H Sharpe 0.703 / AR 7.9% beats FinAgent 0.241 / FinMem −0.228 and even ARIMA 0.325.** Regime split: bull FinAgent **0.12** vs B&H **0.61** (FinMem −0.19); bear FinAgent −0.38 vs B&H −0.28 (FinMem −0.97) — "overly conservative in bulls, overly aggressive in bears." Verdict: LLM investors offer **"no reliable advantage."**
- **Correction (this deep read):** the prior ledger line "all p > 0.34" is **not supported by the retrievable text** — the full read found **no per-strategy alpha p-values reported**; the refutation rests on the Sharpe/AR tables above (B&H matches/beats the agents) and the regime analysis, not a significance test. Corrected here for honesty.
- **Relevance to our system:** The **single strongest external validation of our entire thesis** on the LLM-agent side — and it does so with *our exact methodology*: a bias-mitigated, multi-decade, cost-aware backtest with B&H as benchmark. It takes the very agents that headline papers ([2][4]) report beating the market and shows that, over a broad universe and long horizon **with costs and bias controls, they fail to beat buy-and-hold on Sharpe (B&H wins on 3 of 4 stocks and on the best composite)** — the apparent edge was selective windows + cherry-picked stocks (echoing [30] profit mirage, [13] StockBench's downturn inversion). The "pathological" bull/bear behavior is exactly why a single benign window flatters them. For the operator: this is the paper to cite for "no, LLM strategies don't beat the market in the long run, net of costs" — it is essentially **our advisor's thesis, proven at scale on equities by an independent bias-controlled study.** Strongest LLM-agent citation in the ledger. (Caveat noted by authors: any residual training-data overlap would *favor* the LLMs, making the negative result even more damning.)

### [87] Enhancing Trading Performance Through Sentiment Analysis with Large Language Models: Evidence from the S&P 500
- **Authors / Venue:** (LLM-sentiment-trading group) / arXiv (q-fin)
- **Year:** 2025
- **Source:** arXiv:2507.09739
- **% read:** 55% (abstract + method + results + explicit cost caveat via HTML)
- **Summary:** Combines **GPT-2 + FinBERT** sentiment over financial news (WSJ/Barron's/Benzinga/MarketWatch/Dow Jones) with technical indicators (MACD variants, SAR) and TS forecasts (ARIMA/Prophet/ETS) into a daily buy/sell/hold signal on the S&P 500. Train Aug-2019→2024, **test May 10–Aug 7 2024** (a short ~3-month window). Best variant ("GPT Dow Jones with VW MACD") reports **5.77% return vs −0.70% buy-and-hold** over the test window. FinBERT sentiment accuracy 75.6%, GPT-2 65.5%. **Critically, the authors explicitly assume *zero transaction costs and perfect execution*** and concede this "likely overstates achievable returns."
- **Relevance to our system:** A candid example of the standard sentiment-trading setup *and its fatal caveat stated by the authors themselves*. The 6.5-pt out-performance over B&H comes from **daily rebalancing across many indicators with zero cost** over a **single 3-month window** — exactly the configuration our gate rejects: short single window + no costs + heavy turnover. The authors' own admission ("zero transaction costs... overstates returns") is the whole point — with realistic friction on daily multi-signal trading, the ~6% gross edge would likely be consumed (cf. [89], where 5 bps cuts a 13.8% sentiment-strategy return to 3.7%). For us: another "sentiment correlates with price but the *trading* claim dies on costs + window" data point ([10][39][42][9]). Reinforces: any sentiment overlay must be proven net-of-cost over regime-spanning paths, not a 3-month zero-cost backtest. Catalog as honest-caveat sentiment example.

### [88] FinSTaR: Towards Financial Reasoning with Time Series Reasoning Models
- **Authors / Venue:** (FinSTaR group) / arXiv (q-fin / cs.LG)
- **Year:** 2026
- **Source:** arXiv:2605.03460
- **% read:** 60% (abstract + taxonomy + dual-CoT + results + market-efficiency caveat via HTML)
- **Summary:** Tackles *why* time-series **reasoning** models (TSRMs) fail in finance, and proposes a **2×2 taxonomy** crossing single-/multi-entity with **assessment vs prediction**. Key design insight: assessment is **deterministic** (computable from observed prices) so it uses **Compute-in-CoT** (extract→compute→classify, **>93% accuracy**); prediction is **inherently stochastic** so it uses **Scenario-Aware CoT** (base/adverse/favorable scenarios before judging). Trained on FinTSR-Bench (S&P 500, 2010–2025), FinSTaR hits **78.9% overall (+27% over comparable LLMs)**. Crucially it does **not forecast price/returns** — it makes classification judgments (recover-or-not, persist-vs-revert) — and **prediction tasks plateau at 65–80%**, which the authors **explicitly attribute to market efficiency**: "prediction accuracy cannot reach 100% because the necessary information is not contained in the price history alone." Positioned as decision-support, *not* an autonomous trader; **no PnL.**
- **Relevance to our system:** A remarkably aligned paper — it independently arrives at our exact worldview via a clean taxonomy. The **assessment/prediction split** = the forecastable-vs-unforecastable split at the heart of our thesis: *describing* what the series did (drawdown, vol, regime — deterministic, >93%) is easy and is what our analytics already do; *predicting* the future return is stochastic and **capped by market efficiency** ("info not in price history"), which is precisely why no active strategy robustly beats holding. Two takeaways: (1) it endorses pointing LLMs at **assessment/explanation** (our narration seam — compute-then-state, à la Program-of-Thought [45]) rather than prediction; (2) its own admission that prediction plateaus *because of efficiency* is a quotable, reasoning-model-era restatement of our thesis. For the operator's question on LLMs/foundation-models for numeric series: even a purpose-built financial *reasoning* model concedes the predictive ceiling is set by efficiency, not by model quality. Strong thesis-aligned citation; also a good template for grounded narration (deterministic compute in the explanation).

### [89] Integrating Large Language Models and Reinforcement Learning for Sentiment-Driven Quantitative Trading
- **Authors / Venue:** (LLM+RL sentiment-trading group) / arXiv (q-fin.TR)
- **Year:** 2025
- **Source:** arXiv:2510.10526
- **% read:** 60% (abstract + method + results + cost/leakage treatment via HTML)
- **Summary:** One of the **more carefully evaluated** sentiment-trading papers: **FinGPT [1]** sentiment + **TD3** reinforcement learning, fusing news sentiment with RSI/VWAP/MACD. Universe **44 mega-cap S&P 500 stocks**, train 2018–2023, **test 2024–2025 — post-FinGPT-cutoff (Nov-2023), with pre-2024 company names masked** to fight leakage. **Costs included (10 bps).** Results: the **RL long-only** variant gets **23.65% annualized vs 17.17% B&H** (Sharpe 1.38 vs 1.20) and survives 10 bps despite 52.3% turnover; but the **rule-based long-short** variant **collapses under costs — 13.84% → 3.66% at just 5 bps.** Authors flag the small universe + single OOS year as limits on generalization.
- **Relevance to our system:** The **most methodologically honest** sentiment-trading entry — post-cutoff window, name-masking, *and* explicit costs — so its split result is genuinely informative. The headline is double-edged: (a) the **rule-based variant's collapse (13.8%→3.7% at 5 bps)** is a crisp, quantified illustration of our central cost-erosion thesis — turnover-driven "edge" evaporates net of fees; (b) the **RL long-only beating B&H by ~6 pts net of 10 bps** is one of the *few* credibly-tested apparent wins in the whole ledger — but it rests on a **single OOS year** (2024, a strong equity year), a **small mega-cap universe**, and a leveraged RL policy, i.e. it has *not* faced a regime-spanning, 1000-path, weakest-link bootstrap, and crypto's costs are far higher than 10 bps equity commissions. For us: cite (a) as direct cost-erosion evidence, and treat (b) as exactly the kind of single-window apparent edge our gate exists to stress — plausible-but-unproven until run across bad regimes net of crypto-realistic costs. A fair, useful, genuinely-mixed result.

### [90] When Agents Trade: Live Multi-Market Trading Benchmark for LLM Agents
- **Authors / Venue:** (live-trading-benchmark group) / arXiv (q-fin.TR / cs.AI)
- **Year:** 2025
- **Source:** arXiv:2510.11695
- **% read:** 45% (abstract + setup + nuances via PDF text + search summary)
- **Summary:** A **live, leakage-proof** benchmark (in the spirit of LiveTradeBench [59] / StockBench [13]): multiple frontier LLM agents (Claude, GPT-4, Gemini, …) trade in **real time for ~two months** across **crypto (BTC, ETH)**, equities, and **prediction markets**. Reports that LLM agents *can* generate profitable returns and *sometimes* outperform Buy & Hold — but results are explicitly **mixed**: large **variance across models**, **degradation during downturns**, and **meaningful cost sensitivity.** The authors are candid that **two months is too short** to draw robust conclusions; they frame the contribution as *methodology*, not proof that agents beat markets.
- **Relevance to our system:** Reinforces the contamination-clean-eval cluster ([13][59][86]) and adds the **crypto (BTC/ETH)** live dimension. Its honest nuances are our thesis in miniature: any apparent "beats B&H" is **model-dependent, cost-sensitive, and reverses in downturns** — exactly the fragility our 1000-path regime-spanning, cost-aware, weakest-link gate is built to catch. And the authors' own caveat — **two months can't establish trading superiority** — is the live-benchmark analogue of why we resample 1000 paths rather than trust one window. For us: cite as further evidence that even *live*, the LLM-vs-B&H result is mixed/short/fragile, never a robust edge. (Methodologically it also validates that *live/post-cutoff* is the only honest LLM-trading test — consistent with our forward paper-trade design.) Catalog as live-benchmark, mixed-result citation.

### [91] Integrating Large Language Models in Financial Investments and Market Analysis: A Survey
- **Authors / Venue:** Sedigheh Mahdavi, Jiating Chen, Pradeep Kumar Joshi, Lina Huertas Guativa, Upmanyu Singh (Blend360 AI Research Lab) / arXiv
- **Year:** 2025
- **Source:** arXiv:2507.01990
- **% read:** 40% (abstract + taxonomy + stance via abstract page + search summary)
- **Summary:** A 2025 survey organizing LLM-in-finance research into **four framework categories** — LLM-based Frameworks/Pipelines, Hybrid Integration Methods, Fine-Tuning/Adaptation, and Agent-Based Architectures — across applications: stock selection, risk assessment, sentiment, trading, and forecasting. Its stance is **measured and exploratory**: it describes how LLMs "enhance analytical capabilities" and decision-making **without asserting they reliably beat the market or generate superior returns**, framing the field as full of potential *and* unresolved challenges.
- **Relevance to our system:** A current, neutral anchor citation (complementing the DL→LLM survey [11], the LLM-trading-agent survey [33], the hedge-fund-perspective review [38]). The consistent pattern across *all* these surveys is the same and worth citing collectively: **even comprehensive surveys decline to claim LLMs beat passive benchmarks**, framing trading alpha as exploratory and challenge-laden. That repeated refusal — from the field's own meta-literature — is itself strong support for our thesis and our gate's reason to exist. Its four-category taxonomy is also a tidy way to describe *where* an LLM sits in our advisor (we use Hybrid/Agent ideas only for narration/analysis, not the alpha rail). Background/anchor; no PnL claim to weigh.

### [92] FinZero: Launching Multi-modal Financial Time Series Forecast with Large Reasoning Model
- **Authors / Venue:** (FinZero group) / arXiv (q-fin / cs.LG)
- **Year:** 2025
- **Source:** arXiv:2509.08742
- **% read:** 55% (abstract + method + results table + limitations via HTML)
- **Summary:** A **3B-param multimodal reasoning model** that **renders the time series as an *image*** (arguing numeric standardization loses information) and fine-tunes via a novel RL objective (**UARPO**, a GRPO variant with uncertainty-adjusted, in-/cross-group advantages). Task: predict **directional movement (rise/fall) + volatility trend**, on the **FVLDB** dataset of 10k+ image-text pairs spanning global indices and **Bitcoin**. Results vs a naive baseline (~49%), GPT-4o (~52%): **FinZero ~58.9%** average directional accuracy (~13% relative lift over GPT-4o in the high-confidence group). **No trading returns, no costs, no buy-and-hold; binary prediction only (no magnitude/sizing).**
- **Relevance to our system:** Directly on-asset (includes BTC) for the operator's question, and a fair example of the *reasoning-model + multimodal* frontier. The honest read: even with charts-as-images and RL, directional accuracy reaches only **~59%** — better than chance, but (a) it's *accuracy, not PnL net of costs*; (b) ~59% on direction routinely loses to costs + B&H once turnover and fees bite (our standing point [50][8]); (c) binary up/down with no magnitude can't size positions, which is where real edge or risk-control would live; (d) single-dataset, no regime-spanning test. So FinZero is a genuine technical step (uncertainty-aware RL, visual encoding) but **not gate-credible evidence of crypto edge** — ~59% directional accuracy is exactly the "looks predictive, isn't tradeable" zone our bootstrap-vs-B&H gate exists to adjudicate. The *uncertainty-quantification* angle (confidence scores) is the mildly interesting bit, relevant to a risk overlay if it were calibrated (cf. [83]'s calibration warning). Catalog as on-asset reasoning-model forecasting: modest accuracy, no PnL.

### [93] A Survey of Large Language Models for Financial Applications: Progress, Prospects and Challenges
- **Authors / Venue:** Yuqi Nie, Yaxuan Kong, Xiaowen Dong, John M. Mulvey, H. Vincent Poor, Qingsong Wen, Stefan Zohren / arXiv (Findings of EMNLP 2024-adjacent)
- **Year:** 2024
- **Source:** arXiv:2406.11903
- **% read:** 40% (abstract + taxonomy + stance via abstract page + search summary)
- **Summary:** A broad, well-cited survey covering LLMs across **six financial domains**: linguistic tasks, sentiment analysis, **financial time-series forecasting**, financial reasoning, **agent-based modeling**, and other (decision support, simulation, data augmentation). Emphasizes LLMs' strengths in context understanding and large-scale data processing while **stopping short of claiming reliable market prediction or alpha**; frames forecasting/trading as an emerging area needing more work before profitability claims can be substantiated, and outlines open challenges.
- **Relevance to our system:** Another authoritative, *measured* anchor (with [11][33][38][91]) that — notably given its strong author list (Poor/Mulvey/Zohren) — **declines to assert LLM trading alpha** and explicitly separates the *language* strengths from the *forecasting/trading* open problem. Its inclusion of **time-series forecasting and agent-based modeling as distinct, still-open domains** is useful framing for the operator's question: the serious survey literature treats LLM-on-numeric-series and LLM-trading as unproven research frontiers, not solved capabilities. For us: cite as part of the "every serious survey hedges on LLM alpha" set that backs keeping LLMs on narration/analysis. Background/anchor; no PnL claim.

### [94] TS-Reasoner: Aligning Time Series Foundation Models with LLM Reasoning
- **Authors / Venue:** (TS-Reasoner group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2510.03519
- **% read:** 40% (abstract + method + verdict via PDF text)
- **Summary:** A hybrid that **bolts an LLM's *reasoning* onto a numeric TSFM's *forecasting***: the TSFM handles the time-series numbers, the LLM provides reasoning chains, explanations, and contextual interpretation of the predictions. Reports the hybrid beats both TSFM-alone and LLM-alone on time-series tasks requiring *both* accuracy and interpretability. **No specific finance/crypto application or trading evaluation** in the retrievable content — general time-series analysis.
- **Relevance to our system:** Architecturally this is the *right division of labor* and it maps cleanly to our posture: **TSFM (or any numeric model) does the numbers; LLM does the *reasoning/explanation* over them** — i.e. the LLM's value is interpretability, not the forecast itself. That's exactly our "LLM = narration/explanation layer, gate/model = decision" boundary, and TS-Reasoner finding the LLM adds value *specifically where reasoning/explainability matter* (not raw accuracy) is consistent with the ablations [25][73] (the LLM doesn't improve the *number*) and with FinSTaR [88] (assessment/explanation is the LLM's lane). For the operator's question: this supports using an LLM to *explain* a forecast/decision, while staying skeptical that it improves the forecast. No finance/PnL evidence. Catalog as "LLM-for-reasoning-over-forecasts" design support.

### [95] Trade in Minutes! Rationality-Driven Agentic System for Quantitative Financial Trading (TiMi)
- **Authors / Venue:** (TiMi group) / arXiv (q-fin.TR / cs.AI)
- **Year:** 2025
- **Source:** arXiv:2510.04787
- **% read:** 55% (abstract + 3-stage method + live results + cost/limitations via HTML)
- **Summary:** An LLM agentic system that **decouples strategy-generation from execution** in three stages: Policy (LLM agents write strategies via semantic + code + math reasoning), Optimization (offline simulation + constraint-based parameter solving), Deployment (minute-level execution, ~137 ms latency). Covers **213+ pairs** incl. **US index futures + BTC/ETH/altcoin crypto futures** (Binance). **Live Jan–Apr 2025**, with **maker/taker fees + funding + slippage checks modeled.** Annualized: index futures 6.4% (Sharpe 0.74), mainstream crypto 8.0% (0.79), altcoins 13.7% (0.86); MDD 20–33%, lower variance than RL baselines; beats RL (DDPG 5–6%), grid (3.2%), TradingAgents [3] (5.4%). **But: no explicit buy-and-hold comparison over the same periods**, and authors flag the optimization stage limits zero-shot transfer + "crypto is risky."
- **Relevance to our system:** A relatively serious crypto-inclusive system (live, with costs, on BTC/ETH/altcoin futures) — and even so it lands in our thesis zone. The two-stage strategy/execution decoupling and constraint-based parameter optimization are decent engineering, but the binding gaps for us are explicit: (a) **no buy-and-hold baseline** over the same window — so "8.0% crypto ARR, Sharpe 0.79" in a 4-month 2025 window is *not shown to beat just holding* (which in early-2025 crypto may well have matched or exceeded it); (b) **Sharpe 0.74–0.86 is modest** and the altcoin "outperformance" is on the riskiest, least-liquid pairs (illiquidity again, cf. [69][38]); (c) **minute-level futures trading with funding/leverage is far outside** a daily-ish long-only single-coin spot advisor. The honest inclusion of fees and live data is commendable and makes the *absence of a B&H benchmark* the telling gap — exactly the comparison our gate centers. Catalog as: live, cost-aware crypto agent that still doesn't demonstrate beating buy-and-hold (no B&H shown), modest Sharpe, illiquid-altcoin-tilted.

### [96] A Survey of Reasoning and Agentic Systems in Time Series with Large Language Models
- **Authors / Venue:** (TS-reasoning-survey group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2509.11575
- **% read:** 40% (abstract + taxonomy + open-challenges via PDF text)
- **Summary:** A survey of the emerging intersection of **LLM reasoning + agentic systems** with time series, organized along: reasoning approaches (chain-of-thought, multi-step inference), agentic paradigms (tool use, planning, reflection, continual learning), TS tasks (forecasting, anomaly detection, classification, segmentation), and integration mechanisms. Notably **withholds a verdict on the central debate — do LLMs genuinely reason about temporal data or just pattern-match** — explicitly listing as open challenges: scalability on long sequences, **validating whether LLMs truly understand temporal dependencies**, lack of standardized agentic-TS benchmarks, and interpretability/performance trade-offs. Domain-general (no finance focus in the retrievable text).
- **Relevance to our system:** A current, *honest* anchor for the reasoning-and-agents thread of the operator's question. Its refusal to claim LLMs genuinely understand time series — and its flagging of "validate whether LLMs understand temporal dependencies" as **still open** — is consistent with the empirical debunks we logged ([25][73][78][80]: the LLM ablates out / hovers near baselines). For us the reusable framing is the **agentic-paradigm taxonomy** (tool use, planning, reflection) — useful vocabulary for describing a *narration/research* agent over our gated tools — while noting the survey itself treats LLM *reasoning over numeric series* as unproven. Background/anchor; reinforces "the field hasn't shown LLMs truly reason about numbers."

### [97] Large Language Models are Few-shot Multivariate Time Series Classifiers (LLMFew)
- **Authors / Venue:** (LLMFew group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2502.00059
- **% read:** 55% (abstract + method + ablation analysis + verdict via HTML)
- **Summary:** A *pro-LLM* result for the operator's question: an LLM-enhanced framework for **few-shot multivariate time-series classification** — patching layer + a Patch-wise Temporal Convolution Encoder (PTCEnc) feeding a **LoRA-fine-tuned pretrained LLM decoder** + MLP head. On **10 UEA datasets** (**no financial data**), reports large 1-shot gains (e.g. **+125% on Handwriting, +50% on EthanolConcentration**) over 11 baselines, attributing success to leveraging the LLM's "pretrained knowledge." The ablation compares full vs frozen-LLM vs no-LLM — but **never trains a from-scratch transformer of equivalent capacity**, so it cannot isolate whether *pretraining* helps or merely *model capacity/architecture*.
- **Relevance to our system:** Included deliberately for *balance* — a paper that claims LLMs *do* help on numeric series — and on inspection it reinforces the skeptic case rather than overturning it. Its headline wins are on **few-shot classification of non-financial UEA datasets** (handwriting, gestures, EEG), tasks with **strong learnable structure** — the *opposite* of near-random-walk crypto returns — and it never measures PnL, costs, or anything tradeable. Crucially, its ablation has the **exact gap [25] and [73] warn about**: no from-scratch-equivalent-capacity control, so "pretrained knowledge helps" is *unsubstantiated* — capacity/architecture may explain the gains. So even the pro-LLM datapoint, read carefully, is (a) on a domain unlike crypto, (b) accuracy-only, and (c) missing the control that would prove the language prior matters. For the operator's question: a fair reminder that *some* LLM-for-TS wins are real on structured-signal tasks, but they neither transfer to noisy financial returns nor isolate the LLM's contribution. Catalog as "pro-LLM claim, but domain-mismatched + ablation-incomplete."

### [98] CryptoBench: A Dynamic Benchmark for Expert-Level Evaluation of LLM Agents in Cryptocurrency
- **Authors / Venue:** (CryptoBench group) / arXiv (cs.AI / q-fin)
- **Year:** 2025
- **Source:** arXiv:2512.00417
- **% read:** 55% (abstract + task taxonomy + key findings + verdict via HTML)
- **Summary:** A **dynamic, contamination-resistant** benchmark for LLM agents on **expert-level crypto tasks** (monthly-refreshed templates to prevent memorization), testing 10 SOTA models on live interaction across **on-chain intelligence, market data, DeFi, DEX analytics, and MEV.** Tasks form a 2×2: simple/complex **Retrieval** vs simple/complex **Prediction.** The decisive finding is a **"retrieval-prediction imbalance": leading models excel at data *retrieval* but show "near-complete failure in *predictive* reasoning,"** and performance *degrades* when wrapped in agentic frameworks vs direct prompting. Verdict: current LLM agents are "significantly short of expert-level" and function "better as sophisticated search engines than as analysts." **No trading PnL** — task accuracy only.
- **Relevance to our system:** A near-perfect crypto-specific capstone for the operator's question. On a **contamination-resistant** crypto benchmark, LLMs are good at *fetching facts* and **fail at *prediction*** — the exact "language/retrieval ≠ forecasting/alpha" split we've seen on equities ([7][50][88]), now shown natively on crypto/on-chain/DeFi. The "better as search engines than analysts" framing is quotable and *precisely* our intended LLM role (retrieval/context/narration) vs the role we deny it (prediction/decision). The bonus finding that **agentic wrapping makes things *worse*** is a caution against the elaborate multi-agent crypto-trading stacks ([31][32][34]) — more agent scaffolding ≠ more skill. For us: strong, current, crypto-native evidence that LLMs retrieve but don't predict — keep them on the retrieval/narration rail, gate everything else. Excellent skeptic citation on our own asset class.

### [99] How Can Time Series Analysis Benefit From Multiple Modalities? A Survey and Outlook
- **Authors / Venue:** (multimodal-TS-survey group) / arXiv (cs.LG)
- **Year:** 2025
- **Source:** arXiv:2503.11835
- **% read:** 45% (abstract + taxonomy + finance findings + honest-assessment via HTML)
- **Summary:** A survey of **multimodal** time-series analysis organized into three approaches: **TimeAsX** (recast a series as text/image/audio to reuse a foundation model from that modality), **Time+X** (fuse the series with complementary modalities — mainly text/news — via early/intermediate/late fusion), and **Time2X / X2Time** (bidirectional translation, e.g. Time2Text captions/explanations). Honest assessment: development is substantial but **evidence that text+numeric fusion genuinely improves *forecasting* is "genuinely mixed,"** hampered by **narrow data domains (mostly stock prediction), coarse modality alignment (irrelevant text dilutes the signal), and inherent data contamination.** Notes isolated finance promise (e.g. text helping *credit rating*) but no systematic proof that multimodal beats unimodal.
- **Relevance to our system:** Directly addresses the *"add news/text to a price model"* idea that underlies most LLM-finance trading claims, and the survey's verdict is exactly our skeptical prior: **the evidence that fusing text with numeric series helps *forecasting* is mixed, contamination-plagued, and narrow.** Its three caveats map onto our concerns precisely — (1) narrow domain (mostly stock, ≈ no crypto), (2) **irrelevant text dilutes the signal** (a real risk for noisy crypto news), (3) **data contamination** (the leakage theme [6][30]). The **Time2Text** branch (caption/explain a series) is, notably, the one multimodal direction that's unambiguously useful *and* matches our posture — explanation/narration, not prediction. For the operator's question: an authoritative "multimodal text+numeric forecasting is unproven/mixed," reinforcing that any news-into-forecast feature must clear our gate, and that the safe multimodal use is *explaining* the series, not predicting it. Background/anchor on the multimodal branch.

### [100] From Knowing to Doing: A Memory-Controlled Benchmark for LLM Trading Agents on Stock Markets (KTD-Fin)
- **Authors / Venue:** (KTD-Fin group) / arXiv (q-fin.TR / cs.AI)
- **Year:** 2026
- **Source:** arXiv:2605.28359
- **% read:** 80% (full ar5iv read — masking + de-anon probe, Barra attribution numbers, decomposition, B&H, costs, verdict)
- **Summary:** The definitive **memorization-vs-skill** dissection of LLM trading agents, and a fitting capstone. It fixes two evaluation failures at once: **memory leakage** (cutoffs overlap backtests, so models trade on memorized tickers/dates/narratives) and **return conflation** (cumulative return hides whether profit is *selection skill* vs passive *factor exposure*). Method: a **4-level masking protocol** (Bright / Stock-blind / Date-blind / Blinded), certified by a **10-attacker de-anon probe (top-1 ticker recovery 3.0% vs 0.3% random; joint ticker-top-5 + date-±7d only 1.5%)**, plus **Barra-style attribution**, **realistic A-share costs (5 bps buy / 15 bps sell, T+1)**. Damning full-read numbers: **stock-selection alpha is negative for 9 of 10 models** — only Claude Opus 4.7 reaches **+0.2%**; the rest run from **−0.7% (Qwen3-Plus, the *top total-return* model) to −77.8% (Kimi-K2)**. The decomposition is the headline: Qwen3-Plus's **+70.3% total = +41.8% market + +29.2% style + (−0.7%) selection**; Claude's +51.8% = +35.0% + 16.6% + 0.2%. **CSI300 buy-and-hold returned +36.92%** (2024-01→2026-04); most agents beat it on *raw* return but **underperform once selection skill is isolated.** Masking shifts rationales "from brand narratives to factor ranks." Verdict: "raw cumulative returns systematically overstate LLM trading skill… gains stem almost entirely from factor loading, not security selection."
- **Relevance to our system:** The single most complete validation of our thesis on the LLM side — it independently builds *both* discipline pillars our advisor stands on: (1) **leakage control** (the masking protocol = the post-cutoff/anonymization rule [6][30][13]) and (2) **benchmark-relative attribution** (its Barra decomposition is the equity-market analogue of our **buy-and-hold-is-always-the-benchmark** rule — strip out passive exposure and ask what's left). The result is exactly "no robust edge over holding": **apparent LLM returns are passive market/style harvesting, selection alpha is negative, and B&H matches/beats them risk-adjusted** — with costs included. The "rationale changes with visible identifiers" finding is also a sharp warning for any **narration** layer: an LLM's *explanation* can be a post-hoc story keyed to memorized names, not the actual (gated) numbers — reinforcing our rule that narration must be **grounded in the gated figures** [14][45]. For the operator: cite [100] as the rigorous proof that LLM-agent trading "success" is knowing-the-story (memorized factor exposure), not doing-the-analysis — and that buy-and-hold, net of costs, remains the bar none of them robustly clears. Strongest capstone citation for the ledger.
