# Papers — Backtesting & Test Data (evaluation methodology that doesn't lie)

Ledger for the `backtesting` topic. One entry per paper, appended immediately
after reading. Source of truth for resume. See `../README.md` for the contract.

Focus: backtest overfitting & multiple testing; deflated Sharpe ratio; PBO;
combinatorial purged cross-validation; walk-forward; data-snooping (White's
Reality Check, Hansen's SPA); transaction-cost & market-impact (square-root
law); slippage; look-ahead / survivorship bias; block bootstrap & resampling
for evaluation (mirrors our FROZEN robustness gate); the "financial
charlatanism" critiques; Sharpe pitfalls; number-of-trials / false discoveries.

---

### [1] The Deflated Sharpe Ratio: Correcting for Selection Bias, Backtest Overfitting and Non-Normality
- **Authors / Venue:** David H. Bailey, Marcos López de Prado / Journal of Portfolio Management 40(5), pp. 94–107
- **Year:** 2014
- **Source:** SSRN:2460551 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551); PDF https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf
- **% read:** 60% (abstract + method + key formulas from full-text PDF; skipped detailed proofs)
- **Summary:** When you screen N strategy candidates and keep the best Sharpe ratio (SR), that max SR is upward-biased even if all strategies are pure noise — the expected maximum SR among N independent trials is centered well above zero and grows with N and shrinks with track-record length T. The Deflated Sharpe Ratio (DSR) subtracts this selection-bias benchmark E[max SR] and re-expresses the result as a probability that the true SR exceeds the inflated threshold. E[max SR] ≈ √(V[SR])·[(1−γ)·Z⁻¹(1−1/N) + γ·Z⁻¹(1−1/(N·e))], where γ≈0.5772 is Euler–Mascheroni and Z⁻¹ is the Gaussian inverse-CDF. Critically, the SR estimator's variance is adjusted for non-normality: V[SR] ≈ (1 + 0.5·SR² − skew·SR + ((kurt−3)/4)·SR²)/(T−1), so negative skew / fat tails inflate the variance and lower the deflated significance. Also defines Minimum Track Record Length (MTRL): the shortest history needed for an observed SR to be significant at a chosen confidence given the number of trials.
- **Relevance to our system:** This is the canonical multiple-testing correction and is DIRECTLY actionable for our bake-off. We screen many strategies (SMA/MACD/RSI/Bollinger/composed families) on one (coin,window) and crown the best — exactly the selection process DSR is built to penalize. We should compute the number of effective trials N our sweep performs and report DSR (or at least E[max SR]) alongside the raw winner so a "winning" SR is haircut for how many configs we tried. The non-normality term matters doubly for crypto (heavy tails, negative skew). MTRL gives a principled answer to "is our forward window long enough to trust the crowned pick?" Complements (does not replace) our bootstrap gate: bootstrap tests path-robustness of ONE strategy; DSR tests selection bias ACROSS the many we compared.

### [2] The Probability of Backtest Overfitting
- **Authors / Venue:** David H. Bailey, Jonathan M. Borwein, Marcos López de Prado, Qiji Jim Zhu / Journal of Computational Finance 20(4), 39–69
- **Year:** 2015 (rev.)
- **Source:** SSRN:2326253 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253); PDF https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf
- **% read:** 55% (abstract, full intro, hold-out critique, formal framework + CSCV definition; skipped numerical test cases and EVT/Monte-Carlo appendices)
- **Summary:** Defines Probability of Backtest Overfitting (PBO) as the probability that the strategy chosen as best IN-SAMPLE ranks BELOW the median OUT-OF-SAMPLE — i.e. the IS optimum is fitting noise. Formally, over a probability space of (IS,OOS) sample pairs, PBO = Σ Prob[r̄ₙ < N/2 | strategy n is IS-optimal]·Prob[n is IS-optimal]. Estimated via Combinatorially Symmetric Cross-Validation (CSCV): split the T×N performance matrix into S disjoint equal time-blocks, form all C(S, S/2) ways to choose half the blocks as IS (the rest OOS), in each split pick the IS-best strategy, find its OOS rank, map that rank to a relative position ω∈(0,1) and logit λ=ln(ω/(1−ω)); PBO is the fraction of splits with λ<0 (OOS-below-median). Model-free, nonparametric, requires only the per-strategy return series, and avoids hold-out's pitfalls. The framework also yields performance degradation (OOS-vs-IS regression slope), probability of loss OOS, and stochastic-dominance plots. Key polemic: hold-out is unreliable (researcher leaks future knowledge; high-variance; fails for <1000 obs), and a 5% false-positive rate only holds for ONE test — billions of MA-crossover configs over many securities make false positives near-certain.
- **Relevance to our system:** Highly actionable and a near-perfect fit for our bake-off. CSCV needs ONLY the per-strategy return/equity series we already produce for every candidate in the sweep, so we could add a PBO number to the ranking output with no new modeling. It would quantify the chance our crowned strategy is an IS artifact — exactly the failure mode behind our validated "nothing robustly beats holding" thesis. The "performance degradation" slope (how much OOS Sharpe drops vs IS) and "probability of loss OOS" are cheap, interpretable companions to our bootstrap verdict. Note CSCV's combinatorial IS/OOS swapping is conceptually adjacent to combinatorial purged CV (Lopez de Prado's later book) — for our single-coin time series we'd want PURGING/embargo between IS and OOS blocks to avoid leakage across the split boundary. Also reinforces a reporting discipline: we should record and surface N (number of configs tried) so the operator can judge overfit risk.

### [3] Pseudo-Mathematics and Financial Charlatanism: The Effects of Backtest Overfitting on Out-of-Sample Performance
- **Authors / Venue:** David H. Bailey, Jonathan M. Borwein, Marcos López de Prado, Qiji Jim Zhu / Notices of the American Mathematical Society 61(5), pp. 458–471
- **Year:** 2014
- **Source:** SSRN:2308659 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2308659); PDF https://www.davidhbailey.com/dhbpapers/backtest-pseudo.pdf (scanned — read via OCR)
- **% read:** 50% (abstract, intro, MinBTL theorem, compensation-effects sections via OCR of scanned PDF; figures not legible)
- **Summary:** The flagship polemic. Proves that high in-sample Sharpe is trivially achievable by trying a modest number of strategy configurations on a FIXED dataset — "backtest overfitting." Derives Minimum Backtest Length (MinBTL): the expected maximum Sharpe among N independent skill-less (zero-mean) trials, E[max_N] ≈ √(V)·[(1−γ)Z⁻¹(1−1/N)+γZ⁻¹(1−1/(Ne))], has the clean upper bound √(2 ln N). Re-scaling gives MinBTL (years) ≈ 2 ln[N] / E[max_N]² — i.e. to keep a skill-less strategy's expected max IS-Sharpe ≤ a target, the backtest must lengthen as 2 ln N. Concrete: with only ~ small N of configs the expected max IS Sharpe hits ~1 on a 2-year backtest, so an SR≈1 over 2 years is consistent with PURE noise once you tried enough configs. Key economics result: in the ABSENCE of memory/compensation effects, overfitting yields ~zero OOS performance; but with serial dependence / "compensation effects" (overcrowding, mean reversion), overfitting induces strictly NEGATIVE OOS performance — overfit strategies don't just fail to work, they actively lose. Also: after ~20 trials at the 95% level you expect one spurious "significant" strategy. Section 7 shows how this can be used to commit fraud (cherry-picked backtests).
- **Relevance to our system:** The intellectual foundation of our entire product thesis ("no active strategy robustly beats holding, net of costs"). MinBTL is a concrete guardrail: given the number of configs our bake-off sweeps (N), MinBTL tells us the MINIMUM history length the (coin,window) must have before any crowned IS-Sharpe is even potentially meaningful — we could refuse to crown / flag low-confidence when window_years < 2 ln(N)/target². The negative-OOS-under-memory result is a strong argument for WHY our forward paper-trade so often underperforms buy-and-hold for the crowned non-trivial strategies. Directly motivates surfacing N and a MinBTL check in the ranking UI. Pairs with [1] (DSR) and [2] (PBO) as the three-part anti-overfitting toolkit we should consider porting.

### [4] The Sharpe Ratio Efficient Frontier
- **Authors / Venue:** David H. Bailey, Marcos López de Prado / Journal of Risk 15(2), pp. 3–44
- **Year:** 2012
- **Source:** SSRN:1821643; PDF https://www.davidhbailey.com/dhbpapers/sharpe-frontier.pdf
- **% read:** 30% (abstract + Probabilistic/Minimum Track Record Length sections; via prior knowledge of this paper confirmed against fetched directory listing)
- **Summary:** Introduces the Probabilistic Sharpe Ratio (PSR) and Minimum Track Record Length (MinTRL) BEFORE the multiple-testing extensions. PSR(SR*) = probability that the true SR exceeds a benchmark SR*, computed as Z[ (SR−SR*)·√(T−1) / √(1 − skew·SR + ((kurt−1)/4)·SR²) ] — i.e. it converts an observed SR into a confidence level while correcting for skewness, kurtosis and sample length. MinTRL is the smallest T such that PSR exceeds a chosen confidence (e.g. 95%) for SR*=0: MinTRL = 1 + [1 − skew·SR + ((kurt−1)/4)·SR²]·(Z_conf / SR)². Defines the "Sharpe ratio efficient frontier" — the set of portfolios maximizing PSR rather than SR — and shows that maximizing SR ≠ maximizing the PROBABILITY that SR is real, because higher moments matter.
- **Relevance to our system:** PSR/MinTRL are the single-strategy building blocks the later DSR/MinBTL papers generalize to the multiple-testing case. For our app, PSR is the right way to report confidence in a single crowned strategy's Sharpe on the forward window (correcting for crypto's fat tails/skew), and MinTRL answers "how long must the paper-trade run before its Sharpe is trustworthy?" Cheap to compute from the equity curve we already store. Good candidate for the per-strategy confidence badge in the ranking output.

### [5] . . . and the Cross-Section of Expected Returns
- **Authors / Venue:** Campbell R. Harvey, Yan Liu, Heqing Zhu / Review of Financial Studies 29(1), pp. 5–68; NBER WP 20592
- **Year:** 2016
- **Source:** SSRN:2249314 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2249314); PDF https://www.nber.org/system/files/working_papers/w20592/w20592.pdf
- **% read:** 55% (abstract, intro, full multiple-testing methodology section, recommended-threshold results via PDF text extraction; skipped per-factor tables)
- **Summary:** Catalogs 316 published "factors" purporting to explain the cross-section of returns and argues most are likely false discoveries from data mining. Because the literature collectively ran a huge number of tests, the conventional t-ratio > 2.0 significance bar is far too lax. Applies three multiple-testing corrections: Bonferroni and Holm (both control the Family-Wise Error Rate, FWER = P(≥1 false positive)) and Benjamini–Hochberg–Yekutieli (BHY, which controls the False Discovery Rate, FDR = expected proportion of false positives among rejections; less stringent — FDR ≤ FWER). Recommended hurdles: Bonferroni t-ratio rises from 1.96 (1967) to 3.78 by 2012 (→4.0 by 2032); Holm similar; BHY stabilizes at ~3.39 (1% FDR) / 3.18 (5% FDR). Bottom line: a NEW factor needs a t-ratio of at least ~3.0 (p≈0.27%), and they argue the practical minimum is ~3.18. They also note 316 understates the true number of trials (file-drawer/unpublished tests), so even these thresholds are optimistic; suggest haircutting toward ~113 "common" factors.
- **Relevance to our system:** The asset-pricing analogue of our bake-off's multiple-testing problem. Our sweep tests many strategy configs on one series and crowns a winner — the same "factor zoo" data-mining setup. The core lesson is directly portable: the t-stat / Sharpe significance bar must RISE with the number of configs tried. The FWER-vs-FDR distinction gives us a design choice for the ranking gate — FWER (Bonferroni/Holm) if we want near-zero tolerance for crowning a false winner (conservative, fits our "nothing beats holding" prior), or BHY/FDR if we tolerate a controlled false-discovery proportion across many (coin,window) runs. The file-drawer point reinforces honest accounting of N (every config in the sweep, not just the survivors). Concretely: convert our crowned strategy's Sharpe to a t-stat (t ≈ SR·√(years)·√(periods/yr)) and require it to clear a Bonferroni/BHY hurdle for the sweep's N before declaring a "beat."

### [6] A Reality Check for Data Snooping
- **Authors / Venue:** Halbert White / Econometrica 68(5), pp. 1097–1126
- **Year:** 2000
- **Source:** DOI:10.1111/1468-0262.00152 (https://onlinelibrary.wiley.com/doi/abs/10.1111/1468-0262.00152); summarized via Corradi-Swanson survey + abstract (primary PDF blocked by TLS/cert error)
- **% read:** 35% (abstract + methodology via secondary survey sources; could not open primary PDF)
- **Summary:** The foundational data-snooping test. When you search over l candidate models/rules and report the best one's performance vs a benchmark, the best's apparent superiority is upward-biased by the search itself. White's Reality Check (RC) tests the single null H0: "the BEST model among all l has NO predictive superiority over the benchmark," i.e. max_k E[f_k] ≤ 0 where f_k is model k's performance differential vs the benchmark. The test statistic is V = max_k √n · f̄_k (the max over models of the standardized average differential). Its sampling distribution under the null is obtained by the stationary bootstrap (Politis–Romano) — resample blocks of the differential series, recompute the max statistic many times, and the RC p-value is the fraction of bootstrap maxima exceeding the observed V. This automatically accounts for the FULL universe of rules tried, so a "great" best rule that's really noise gets a high (insignificant) p-value.
- **Relevance to our system:** This is essentially the academic ancestor of what our app already does — a bootstrap test of whether the BEST strategy genuinely beats a benchmark, where OUR benchmark is buy-and-hold. The mapping is almost 1:1: f_k = (strategy k return − buy&hold return); test whether max_k beats 0 via block bootstrap. We should verify our gate's null is framed as "best beats benchmark accounting for all configs tried" (RC-style) rather than "this one strategy beats benchmark" — the former is the multiple-testing-correct version. The stationary/moving-block bootstrap White uses is the SAME family as our moving-block bootstrap, validating our core method choice. Key caveat carried into [7]: RC is CONSERVATIVE when the search includes many bad rules.

### [7] A Test for Superior Predictive Ability (SPA)
- **Authors / Venue:** Peter Reinhard Hansen / Journal of Business & Economic Statistics 23(4), pp. 365–380
- **Year:** 2005
- **Source:** SSRN:264569 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=264569 — 403 gated); summarized via search abstracts + Corradi-Swanson survey
- **% read:** 30% (abstract + methodology summary from multiple secondary sources; primary gated)
- **Summary:** Improves White's Reality Check on two fronts. (1) STUDENTIZATION: instead of the raw max √n·f̄_k, SPA uses a studentized statistic max_k √n·f̄_k / σ̂_k, which down-weights erratic/high-variance rules so a single noisy rule can't dominate the max. (2) SAMPLE-DEPENDENT NULL: White's RC builds the null under the "least favorable configuration" (all models exactly at the boundary E[f_k]=0), which is overly conservative when many genuinely-poor models (E[f_k]≪0) are in the set — they shouldn't count toward the snooping penalty. SPA instead estimates which models are "relevant" (near the boundary) and excludes obviously-poor ones from the null distribution, recovering power. Result: SPA is more powerful and far less sensitive to the inclusion of irrelevant bad rules than RC, while still controlling size. Empirically validated (Hansen & Lunde, Hsu & Kuan) as more powerful than RC.
- **Relevance to our system:** Directly relevant if our bake-off sweeps a wide config grid that includes many obviously-bad strategies (e.g. degenerate SMA windows). Under a White-style gate, those bad configs inflate the snooping penalty and make it HARDER for a genuinely-good strategy to register as beating buy-and-hold — i.e. our gate could be too conservative. Adopting SPA-style studentization (divide each strategy's excess-return by its bootstrap std) and excluding clearly-dominated configs from the null would give our gate more power to detect a real winner if one exists, WITHOUT inflating false positives. Worth checking whether our current 1000-path gate is RC-style (conservative) or SPA-style; if RC-style, SPA is a power upgrade.

### [8] Stepwise Multiple Testing as Formalized Data Snooping (StepM)
- **Authors / Venue:** Joseph P. Romano, Michael Wolf / Econometrica 73(4), pp. 1237–1282
- **Year:** 2005
- **Source:** DOI:10.1111/j.1468-0262.2005.00615.x; PDF https://users.ssc.wisc.edu/~behansen/718/RomanoWolf2005.pdf
- **% read:** 40% (abstract + StepM algorithm + RC relationship from full-text PDF; skipped asymptotic proofs)
- **Summary:** Generalizes White's RC from "does the single best model beat the benchmark?" to "identify ALL models that beat the benchmark" while still controlling the Family-Wise Error Rate (FWE = P(≥1 false rejection)). The StepM algorithm: Step 1 — compute the max studentized statistic over all remaining hypotheses, get a bootstrap critical value (resampling the data to capture the JOINT dependence of test statistics), reject every hypothesis whose statistic exceeds it; Step 2 — remove the rejected ones and recompute the max critical value over the SURVIVORS; repeat until no further rejections. Because the bootstrap critical value shrinks as strong performers are peeled off, StepM rejects strictly more true-false hypotheses than single-step RC, and far more than Bonferroni/Holm (which ignore dependence). Still controls FWE asymptotically.
- **Relevance to our system:** If we ever want to surface not just THE single crowned strategy but the full SET of strategies that robustly beat buy-and-hold (e.g. "these 3 of 40 configs cleared the gate"), StepM is the principled FWE-controlling way to do it — and it reuses our existing bootstrap machinery. Its dependence-aware critical values are more powerful than the Bonferroni hurdle suggested in [5], because our sweep's configs are HIGHLY correlated (neighboring SMA windows produce near-identical equity curves) — Bonferroni would massively over-penalize that correlation; StepM/bootstrap handles it correctly. Strong candidate for upgrading our ranking from "top-1" to "set of gate-passers" without losing multiple-testing rigor.

### [9] Combinatorial Purged Cross-Validation (CPCV) + Purging/Embargo
- **Authors / Venue:** Marcos López de Prado / "Advances in Financial Machine Learning" (Wiley), ch. 7 & 12; summarized via Wikipedia "Purged cross-validation" + practitioner write-ups
- **Year:** 2018
- **Source:** Book ISBN 978-1119482086; https://en.wikipedia.org/wiki/Purged_cross-validation ; https://www.quantbeckman.com/p/with-code-combinatorial-purged-cross
- **% read:** 45% (full method via Wikipedia + two practitioner explainers with code; not the book chapter verbatim)
- **Summary:** Two ideas that fix leakage in financial CV, plus a combinatorial scheme that yields a DISTRIBUTION of backtest paths. (1) PURGING: drop training observations whose LABEL-formation interval overlaps the test set's time range (financial labels depend on future returns, so naive K-fold leaks). (2) EMBARGO: additionally drop a small percentage of observations immediately AFTER each test fold (e.g. 1–5% / 50 of 1000 obs) to kill serial-correlation leakage that purging alone misses. (3) CPCV: split the series into N sequential non-overlapping groups, then use ALL combinations of k groups as the test set (C(N,k) splits), applying purge+embargo each time. Because each group appears in multiple test sets, you reconstruct φ[N,k] = (k/N)·C(N,k) distinct out-of-sample backtest PATHS — e.g. N=6,k=2 gives 15 splits → 5 paths. This converts a single point-estimate Sharpe into an empirical DISTRIBUTION of Sharpes, enabling percentile-based (e.g. 10th-pct PSR) selection — reframing optimization as ROBUSTNESS identification.
- **Relevance to our system:** CPCV is the gold-standard purged evaluator and is philosophically identical to what our gate is trying to do — produce a DISTRIBUTION of outcomes rather than one number and judge robustness by the downside (weakest-link). Two concrete imports: (a) PURGE + EMBARGO our IS/OOS / bootstrap block boundaries — even though our strategies use point-in-time signals (smaller leakage horizon than ML meta-labels), any indicator with a lookback window (SMA-200, Bollinger) creates feature-overlap at block edges that an embargo would clean. (b) The φ[N,k] combinatorial-path idea is an ALTERNATIVE/complement to our moving-block bootstrap for generating many OOS paths from limited history — both attack the same "one history = one lucky path" problem. Our weakest-link verdict ≈ CPCV's "select by a low percentile of the path distribution." This is probably the single most transferable methodology to our gate.

### [10] The Stationary Bootstrap
- **Authors / Venue:** Dimitris N. Politis, Joseph P. Romano / Journal of the American Statistical Association 89(428), pp. 1303–1313
- **Year:** 1994
- **Source:** DOI:10.1080/01621459.1994.10476870; mechanics via SAS/IML explainer + multiple secondary sources (primary tandfonline/semanticscholar not directly fetchable)
- **% read:** 35% (abstract + full mechanics from secondary explainers; not the JASA PDF verbatim)
- **Summary:** The canonical resampling method for weakly-dependent STATIONARY time series, the parent of the bootstrap used in White's RC [6]. Plain IID bootstrap destroys autocorrelation, so block methods resample contiguous chunks to preserve local dependence. The MOVING-BLOCK bootstrap uses FIXED-length blocks but the resulting series is non-stationary (block boundaries are special). The STATIONARY bootstrap fixes this by drawing each block's length from a GEOMETRIC distribution with parameter p (expected block length = 1/p): pick a random start, take a geometric-length run, concatenate until you reach the series length. Random block lengths make the resampled series itself stationary. The mean block length 1/p is the single tuning knob: small p (long blocks) preserves more serial correlation; p→1 collapses to the IID bootstrap. Wrap-around (circular) handling avoids end effects.
- **Relevance to our system:** This is the theoretical backbone of OUR gate. Our "1000-path moving-block bootstrap" is the FIXED-length sibling of this method; the key design parameter we share is the BLOCK LENGTH, which must be long enough to preserve crypto's autocorrelation/volatility-clustering or the bootstrap will understate risk and over-reward strategies. Two takeaways: (a) consider whether the stationary (random-length) variant would remove block-boundary artifacts our fixed-length version may have; (b) block length is not a free choice — there's an optimal-block-length literature (Politis-White 2004) we should consult to set ours principled rather than ad hoc. Confirms our method is squarely within accepted practice for dependent-data resampling.

### [11] Backtest Overfitting in the Machine Learning Era: A Comparison of Out-of-Sample Testing Methods in a Synthetic Controlled Environment
- **Authors / Venue:** Hamid R. Arian, Daniel Norouzi M., Luis A. Seco / Knowledge-Based Systems 305 (2024)
- **Year:** 2024
- **Source:** DOI:10.1016/j.knosys.2024.112477; SSRN:4686376 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=4686376 — gated); summarized via publisher + search abstracts
- **% read:** 25% (abstract + headline results; full text gated)
- **Summary:** Builds a SYNTHETIC controlled environment (known ground truth, with non-stationarity, autocorrelation, regime shifts injected) to fairly RANK out-of-sample testing methods — K-fold CV, walk-forward, purged K-fold, and Combinatorial Purged CV (CPCV) — by their Probability of Backtest Overfitting (PBO) and Deflated Sharpe Ratio (DSR). Headline result: CPCV is markedly SUPERIOR at mitigating overfitting (lowest PBO, best DSR); WALK-FORWARD is the worst at preventing false discoveries — high temporal variability, weak stationarity, prone to overstating performance. Introduces ensemble/adaptive variants (Bagged CPCV, Adaptive CPCV) that further improve robustness by averaging across many path-distributions and adjusting to regime conditions.
- **Relevance to our system:** Empirical, ground-truth-validated evidence that the path-distribution approach ([9] CPCV / our bootstrap gate) BEATS the single-trajectory walk-forward our forward paper-trade currently resembles. This directly supports keeping/strengthening our bootstrap gate over a naive walk-forward, and is concrete ammunition for porting CPCV-style purged combinatorial paths. It also uses PBO+DSR as the evaluation yardstick — reinforcing the [1][2] recommendation to ADD those two metrics to our ranking output as the standard overfitting scorecard. The synthetic-controlled-environment design is itself a testing-discipline idea: we could validate OUR gate against synthetic series with KNOWN no-alpha ground truth to confirm it correctly returns "no strategy beats hold."

### [12] Walk-Forward Optimization / Analysis (methodology review)
- **Authors / Venue:** Composite of practitioner/methodology references (QuantInsti; Pardo's "Evaluation and Optimization of Trading Strategies" lineage; susanpotter.net)
- **Year:** 2024 (review); concept orig. Pardo 1992/2008
- **Source:** https://blog.quantinsti.com/walk-forward-optimization-introduction/ ; https://www.susanpotter.net/quant/walk-forward-optimization/
- **% read:** 40% (two full methodology articles; not a single primary paper — concept review)
- **Summary:** Walk-Forward Optimization (WFO) replaces a single backtest with a chain of IS-optimize → OOS-test windows that roll forward (e.g. train 2010–2015, test 2016; train 2011–2016, test 2017; …), concatenating the OOS segments into one "walk-forward" equity curve that approximates live re-optimization. ANCHORED windows expand from a fixed start (more stability, but later windows have so much data almost any parameters "look fine," masking overfit); ROLLING windows keep a fixed length (adapts faster to regimes). Walk-Forward Efficiency (WFE = OOS performance / IS performance) is the headline robustness metric: >0.7 robust, <0.4 likely overfit. A strategy that survives WFO across MULTIPLE window configurations is much less likely overfit. Limitations: window-selection bias (window length/start shape results independent of merit), reactive lag to regime changes (adapts after the damage), and heavy compute.
- **Relevance to our system:** Our forward paper-trade is essentially a single-walk WFO (crown on history, run forward on unseen data). Two cautions: (1) WFO/single-walk is exactly the method [11] found WORST at preventing false discoveries vs CPCV — so we should treat the forward run as a confidence check, not the robustness gate (the bootstrap gate is the real test). (2) WFE is a cheap, interpretable companion metric: report OOS-Sharpe/IS-Sharpe for the crowned strategy; a low ratio is a clear overfit flag the operator can read. The "survive across multiple window configs" idea suggests we vary the bake-off window (start/length) and require the crown to be STABLE across them rather than crowned on one lucky window — a direct hardening of our ranking.

### [13] Strict Universality of the Square-Root Law in Price Impact Across Stocks (and the square-root impact law generally)
- **Authors / Venue:** (TSE survey) + Almgren, Thum, Hauptmann, Li lineage / arXiv + J. Risk
- **Year:** 2024 (survey); core law Almgren et al. 2005
- **Source:** arXiv:2411.13965 (https://arxiv.org/pdf/2411.13965); core law: Almgren et al. 2005 "Direct estimation of equity market impact" (Risk 18)
- **% read:** 35% (abstract + impact-law formulation + universality result from full-text PDF; skipped TSE-specific empirics)
- **Summary:** Establishes that market impact follows a SQUARE-ROOT law that is strikingly universal across stocks, asset classes, geographies, and eras. Functional form: Impact ≈ Y·σ·√(Q/V), where Q = order size, V = average daily volume, σ = volatility, and Y is a dimensionless O(1) coefficient. The exponent on participation rate Q/V is ~0.5 (sublinear — doubling size raises impact by only ~√2≈1.41×), confirmed strictly across the entire Tokyo Stock Exchange. Impact scales LINEARLY with volatility. Impact decomposes into TEMPORARY (liquidity-demand cost that decays after the trade) and PERMANENT (lasting, information-driven price shift) components (Almgren–Chriss). The law holds even for option markets.
- **Relevance to our system:** Directly relevant to our cost model. If our backtest charges only fixed/linear fees + a constant spread, it UNDER-states costs for any non-trivial order relative to market depth — and overstated net returns are a prime source of phantom alpha (the very thing our gate exists to reject). For a €200 retail budget on a liquid coin, market impact is genuinely negligible (Q/V ≈ 0), so a fixed fee + spread model is DEFENSIBLE — but we should (a) state this assumption explicitly and (b) ensure that if a strategy's edge depends on high turnover, the per-trade cost (fee+spread) still bites enough that churny strategies can't fake a win. The √-law matters more as a guard for any future "what if I scaled this to $X" feature: impact grows as √(size), so apparent edge erodes with capital. Cost realism is a test-data-discipline lever: too-cheap costs = look-ahead-free but still-fake alpha.

### [14] Survivorship Bias in Performance Studies
- **Authors / Venue:** Stephen J. Brown, William N. Goetzmann, Roger G. Ibbotson, Stephen A. Ross / Review of Financial Studies 5(4), pp. 553–580
- **Year:** 1992
- **Source:** DOI:10.1093/rfs/5.4.553; PDF https://terpconnect.umd.edu/~wermers/ftpsite/FAME/Brown_Goetzmann_Ibbotson_Ross.pdf (read via OCR/text extraction)
- **% read:** 45% (abstract, mechanism, persistence-regression results via PDF text extraction)
- **Summary:** The seminal survivorship-bias paper. Funds/strategies that perform poorly tend to DISAPPEAR from databases, so any analysis on a surviving-only sample overstates returns. The deeper, surprising contribution: analyzing the volatility–return relationship in a sample TRUNCATED by survivorship shows that survivorship ALONE manufactures the APPEARANCE of return predictability and performance persistence ("hot hands") where none exists — the spurious persistence is strong enough to account for much of the published evidence favoring predictability. Their persistence regressions (period-2 Jensen's α on period-1 α) show the apparent persistence is largely driven by the PERSISTENCE OF NEGATIVE performance (losers staying losers before they exit), not genuine winning skill. Related estimates in the literature put survivorship inflation at ~0.2–0.8%/yr in returns and up to ~0.5 in Sharpe.
- **Relevance to our system:** Survivorship is a structural test-data trap. For our single-coin advisor it bites in TWO places: (1) COIN SELECTION — if we only offer / backtest coins that still exist and have long histories, we've implicitly conditioned on survival; dead coins (delisted, collapsed, rug-pulled) are invisible, so any "crypto strategy works" conclusion is survivorship-inflated. A buy-and-hold benchmark on a SURVIVING coin is itself survivor-biased upward, which actually makes our "nothing beats hold" bar HARDER (good) — but cross-coin generalization claims need the dead coins. (2) The spurious-persistence result is a direct warning: apparent "this strategy persistently beats hold on BTC" can be an artifact of conditioning on a surviving asset. Mitigation: include delisted/dead coins in any universe-level study; never claim cross-asset robustness from survivors only.

### [15] LookAheadBench: A Standardized Benchmark of Look-Ahead Bias in Point-in-Time Data
- **Authors / Venue:** (financial-LLM benchmark authors) / arXiv preprint
- **Year:** 2026
- **Source:** arXiv:2601.13770 (https://arxiv.org/pdf/2601.13770)
- **% read:** 40% (abstract + method + findings + recommendations from full-text PDF; skipped detailed tables)
- **Summary:** Introduces a standardized dual-period benchmark to quantify LOOK-AHEAD BIAS — the inadvertent use of information not available at the decision moment, which artificially inflates backtested performance. Method: a training/restricted-info period vs a testing period on genuinely future data, with strict point-in-time temporal boundaries, plus alpha-decay analysis and comparison against simple baselines (buy-and-hold, momentum, mean reversion, moving averages). Applied to ML/LLM trading agents. Key findings: look-ahead bias substantially inflates reported returns; models show significant ALPHA DECAY once temporal discipline is enforced; and many LLM-based trading agents FAIL to beat simple quantitative baselines under honest point-in-time evaluation. Recommends: enforce strict temporal boundaries, validate data availability at each timestamp, test alpha persistence forward, and benchmark against established simple strategies rather than unrealistic ones.
- **Relevance to our system:** Two strong alignments with our design. (1) Its headline empirical finding — sophisticated agents don't beat simple baselines once look-ahead is removed — is a fresh, independent restatement of our validated "nothing robustly beats holding" thesis, and validates our insistence that buy-and-hold ALWAYS be the benchmark. (2) Look-ahead discipline is a concrete checklist for our pipeline: every indicator must use only past bars (no centered moving averages, no full-sample normalization, no using bar t's close to act at bar t's open), and any parameter chosen on the full series leaks. We should audit our indicator implementations (SMA/MACD/RSI/Bollinger) for these exact leaks and add a test that shifts signals by one bar to confirm no future data is consumed. The alpha-decay test (does edge persist forward?) is exactly our forward paper-trade — we should report decay explicitly.

### [16] The Statistics of Sharpe Ratios (+ Sharpe estimation/CI technical note)
- **Authors / Venue:** Andrew W. Lo / Financial Analysts Journal 58(4), pp. 36–52; corroborating formula via Two Sigma "Sharpe Ratio: Estimation, Confidence Intervals, and Hypothesis Testing" (Mertens/Bao-Ullah lineage)
- **Year:** 2002
- **Source:** SSRN:377260 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=377260); formula read from https://www.twosigma.com/wp-content/uploads/sharpe-tr-1.pdf
- **% read:** 35% (Lo abstract + the autocorrelation-annualization + asymptotic-SE results read in detail from the corroborating Two Sigma technical report; Lo's FAJ not read verbatim)
- **Summary:** Treats the Sharpe ratio as a STATISTIC with sampling error, not a fixed truth. Two key results. (1) ANNUALIZATION under serial correlation: the naive monthly→annual factor √q (e.g. √12) is WRONG when returns are autocorrelated; the correct factor depends on the autocorrelations ρ_k. For the stationary/AR(1) case it is approximately √(q / (1 + 2ρ₁)), so POSITIVE autocorrelation (ρ₁>0) makes the naive √q OVERSTATE the annualized Sharpe (an example shows a hedge-fund annual Sharpe overstated by ~65% from monthly serial correlation). (2) The estimated Sharpe has an asymptotic standard error; under IID Gaussian returns SE(SR) ≈ √((1 + SR²/2)/T), and the SE inflates further under non-normality (skew/kurtosis, per Bao–Ullah) and autocorrelation (HAC estimation needed). Implication: Sharpe comparisons and "significance" must carry confidence intervals.
- **Relevance to our system:** Crypto returns exhibit strong volatility clustering and serial correlation, so a naively annualized Sharpe in our bake-off can be materially OVERSTATED — inflating apparent edge precisely where our gate must be skeptical. Two concrete fixes: (a) annualize with the autocorrelation-corrected factor (or just report Sharpe at the native bar frequency with a CI, avoiding annualization spin); (b) attach a confidence interval to every crowned Sharpe using Lo's SE (and the non-normality-adjusted SE from [1]/[4]) so the operator sees that, e.g., a "Sharpe 1.2" might be 1.2 ± 0.9. This is the per-strategy-uncertainty companion to the cross-strategy selection correction (DSR). Also a reason to prefer ratio-free robustness (our bootstrap path distribution) over a single annualized point Sharpe.

### [17] The Seven Sins of Quantitative Investing
- **Authors / Venue:** Yin Luo et al. (Deutsche Bank Quantitative Strategy); popularized in Lopez de Prado & portfolio-optimization texts
- **Year:** 2014
- **Source:** Deutsche Bank research (2014); open summary https://portfoliooptimizationbook.com/book/8.2-seven-sins.html
- **% read:** 50% (full taxonomy via open textbook chapter; not the original DB note)
- **Summary:** A practitioner taxonomy of the seven recurring ways backtests overstate performance. (1) SURVIVORSHIP BIAS — using only assets/funds that still exist, omitting the dead losers. (2) LOOK-AHEAD BIAS — using info not available at decision time (future data, misaligned timestamps, full-sample statistics). (3) STORYTELLING BIAS — inventing post-hoc narratives for random patterns; confusing coincidence with causation. (4) OVERFITTING / DATA-SNOOPING BIAS — tweaking until the model fits history; with enough parameters almost anything looks profitable in-sample. (5) TURNOVER & TRANSACTION COST — ignoring realistic fees/slippage/market impact on rebalancing, overstating NET returns. (6) OUTLIERS — performance riding on rare, non-repeatable events (data errors, liquidity crunches). (7) ASYMMETRIC PATTERN & SHORTING COST — assuming unlimited cheap shorting that doesn't exist, especially in crises.
- **Relevance to our system:** A ready-made AUDIT CHECKLIST for our pipeline — we can literally tick each sin against our app. Survivorship (1): which coins we offer (see [14]). Look-ahead (2): indicator implementations (see [15]). Storytelling (3): resist narrating WHY a crowned strategy "should" work; the gate is the only arbiter. Data-snooping (4): the central one — our sweep IS multiple testing; needs DSR/PBO ([1][2]). Costs (5): our fee+spread model and turnover sensitivity ([13]). Outliers (6): check whether a crowned strategy's edge is concentrated in 1–2 days (our bootstrap partly handles this by resampling, but a "drop the top-k days" stress test would directly expose it). Shorting (7): mostly N/A for a long/flat retail advisor, but relevant if any strategy shorts. This is the cheapest high-value import: a written "seven-sins" self-audit appended to each ranking report.

### [18] Determining Optimal Trading Rules without Backtesting
- **Authors / Venue:** Peter P. Carr, Marcos López de Prado / arXiv working paper (later in financial-ML literature)
- **Year:** 2014
- **Source:** arXiv:1408.1159 (https://arxiv.org/abs/1408.1159); PDF https://arxiv.org/pdf/1408.1159
- **% read:** 40% (abstract + method + Sharpe-surface result from full-text PDF; skipped OU-derivation algebra)
- **Summary:** A constructive alternative to overfitting-prone parameter search. Calibrating profit-taking / stop-loss thresholds by running a backtest grid is a primary overfitting source. Instead, assume the traded series follows a discrete Ornstein–Uhlenbeck (mean-reverting) process; then the optimal entry/exit (profit-take, stop-loss) thresholds can be derived NUMERICALLY from just two OU parameters — the half-life of mean reversion and the volatility — WITHOUT any historical parameter search. They build a Sharpe-ratio SURFACE over (profit-take, stop-loss) pairs implied by the OU dynamics and read off the optimum. Because the thresholds come from the assumed data-generating process rather than data mining, there is no backtest-overfitting loop. They give empirical evidence such optima exist and are computable.
- **Relevance to our system:** A different philosophy from our bake-off (derive rules from a model vs search-and-gate), but two useful imports. (1) If we ever tune exit thresholds (stop/take-profit) for a crowned strategy, doing it by grid-search over history is exactly the overfitting trap our gate exists to catch — Carr–LdP shows a model-based alternative that sidesteps it. (2) More immediately, it's a reminder that EVERY tunable knob we expose in the bake-off (SMA windows, RSI thresholds, etc.) multiplies N and the overfitting risk; fewer, theory-justified parameters beat many searched ones. Conceptually reinforces our thesis: if a strategy's "edge" only appears after threshold tuning, it's likely fitting noise.

### [19] The Three Types of Backtests
- **Authors / Venue:** Jacques Joubert, Dragan Sestovic, Illya Barziy, Walter Distaso, Marcos López de Prado / SSRN working paper (ADIA Lab / Hudson & Thames)
- **Year:** 2024
- **Source:** SSRN:4897573 (https://papers.ssrn.com/sol3/Delivery.cfm/4897573.pdf); PDF https://www.hillsdaleinv.com/uploads/The_Three_Types_of_Backtests.pdf
- **% read:** 50% (full taxonomy + pros/cons + recommendations via PDF text extraction; skipped detailed examples)
- **Summary:** Organizes backtesting into three families and their failure modes. (1) WALK-FORWARD: the single historical path; benefits = realistic, no need to choose a resampling/data-generating model; drawbacks = PATH-DEPENDENT (one realization), small effective sample, and re-optimization across windows still risks overfitting (cites Bailey–LdP 2014). (2) RESAMPLING (K-fold, combinatorial purged CV, bootstrap): resamples observed data to construct MANY paths, giving a distribution for risk/drawdown and being LESS prone to overfitting (cites LdP 2018); CPCV is the recommended variant; drawback = some path-dependent data structure can be broken by reshuffling. (3) MONTE CARLO: simulate paths from a FITTED data-generating process (incl. modern generative models, Wiese et al. 2020); most paths/flexibility, but quality hinges entirely on the model — a wrong/overfit DGP gives confidently wrong answers. They recommend building a CAUSAL GRAPH before any backtest, and note all three still leave overfitting risk. Costs split into brokerage/slippage/impact.
- **Relevance to our system:** This taxonomy maps our app exactly: our bootstrap gate is a RESAMPLING backtest (type 2 — the one the authors RECOMMEND as least overfit), while our forward paper-trade is a WALK-FORWARD (type 1 — path-dependent, weakest). That ordering validates treating the bootstrap as the authoritative gate and the forward run as a secondary confidence check. Two action items: (a) consider adding a MONTE CARLO leg (type 3) — fit a simple DGP to the coin (e.g. block-bootstrap is already a non-parametric MC; a GARCH/OU sim would be parametric) and confirm the crown survives there too, giving cross-method agreement; (b) the "causal graph before backtest" advice is a discipline we could adopt to force explicit hypotheses about WHY a strategy might work before crowning it (anti-storytelling, ties to [17] sin #3). Reinforces that no single backtest type is sufficient — agreement across types is the real robustness signal.

### [20] Automatic Block-Length Selection for the Dependent Bootstrap
- **Authors / Venue:** Dimitris N. Politis, Halbert White / Econometric Reviews 23(1), pp. 53–70 (+ Patton–Politis–White 2009 correction)
- **Year:** 2004 (corr. 2009)
- **Source:** DOI:10.1081/ETC-120028836; PDF https://public.econ.duke.edu/~ap172/Politis_White_2004.pdf (read via text extraction); correction PDF https://public.econ.duke.edu/~ap172/Patton_Politis_White_2009.pdf
- **% read:** 35% (abstract, intro, optimal-block-size theorem statements via text extraction; skipped full proofs)
- **Summary:** Solves the single most important practical knob for block/stationary bootstrap: how long should the blocks be? Too-short blocks destroy the series' serial dependence (bootstrap understates true risk); too-long blocks leave too few distinct resamples (high variance). They derive a DATA-DRIVEN estimator of the optimal block length that minimizes the bootstrap's mean-squared error and is ADAPTIVE to the strength of serial correlation as measured by the CORRELOGRAM — stronger autocorrelation ⇒ longer optimal blocks. The optimal block size grows on the order of N^(1/3). They unify and compare the moving-block, circular, and stationary bootstraps (noting circular/SB have favorable accuracy), and report their automatic selector lands within ~90–110% of the true optimal block size in simulations. The 2009 correction (Patton–Politis–White) fixes constants after Nordman's correction of Lahiri (1999) and ships corrected MATLAB code.
- **Relevance to our system:** This is the missing rigor for OUR gate's most important hidden parameter — the moving-block length. If we set block length by intuition, we may be silently under- or over-stating crypto's volatility-clustering risk, biasing the gate's verdicts (too-short blocks would make strategies look MORE robust than they are — the dangerous direction). Action: compute the data-driven optimal block length per (coin,window) from the series' autocorrelation using the Politis–White selector (corrected version) instead of a fixed constant, and document it in the report so the gate's robustness claim is reproducible and defensible. Also informs a possible switch to the stationary (random-length) bootstrap [10], for which this paper gives the matching optimal expected-block-length formula. Highest-leverage methodological fix to make our FROZEN gate's internals principled.

### [21] Deep Reinforcement Learning for Cryptocurrency Trading: Practical Approach to Address Backtest Overfitting
- **Authors / Venue:** Berend Gort, Xiao-Yang Liu, Xinghang Sun, Jiechao Gao, Shuaiyu Chen, Christina Dan Wang / arXiv (q-fin.TR)
- **Year:** 2022 (v4 2023)
- **Source:** arXiv:2209.05559 (https://arxiv.org/abs/2209.05559); PDF https://arxiv.org/pdf/2209.05559
- **% read:** 45% (abstract, intro, three-category taxonomy, method via PDF text extraction; skipped DRL architecture details)
- **Summary:** Applies the overfitting-detection machinery directly to CRYPTO. Frames detection of backtest overfitting as a HYPOTHESIS TEST: train many DRL agents, ESTIMATE each agent's probability of backtest overfitting (PBO-style), and REJECT any agent whose estimated overfit probability exceeds a preset threshold before deployment. Classifies prior work into three backtest schemes — (1) walk-forward (single validation window, overfits easily), (2) K-fold cross-validation (leakage risk from IID assumption), (3) hyperparameter tuning. Empirical result on 10 cryptocurrencies during the 05–06/2022 period (market crashed twice): LESS-overfitted agents earned higher returns than more-overfitted agents AND beat both an equal-weight strategy and the S&P crypto market benchmark. I.e. explicitly filtering for low overfitting improved out-of-sample/crash performance.
- **Relevance to our system:** The closest paper to our exact setting — crypto, multiple candidate models, select by an overfitting-aware criterion, benchmark against the market. It operationalizes the [2] PBO idea as a SELECTION FILTER (reject overfit candidates) rather than just a diagnostic — directly portable to our bake-off ranking: compute PBO per candidate and DOWN-RANK or DISQUALIFY high-PBO strategies before crowning, instead of crowning purely on in-sample Sharpe. The crash-period result is encouraging evidence that overfitting filters help precisely when it matters (regime breaks), which is the scenario our forward paper-trade most fears. Also independently corroborates that walk-forward (our forward run's shape) is the weakest scheme. Strong support for adding a PBO gate to ranking.

### [22] Implementation Risk in Portfolio Backtesting: A Previously Unquantified Source of Error
- **Authors / Venue:** (arXiv q-fin authors, 2026) / arXiv preprint
- **Year:** 2026
- **Source:** arXiv:2603.20319 (https://arxiv.org/abs/2603.20319); HTML https://arxiv.org/html/2603.20319v1
- **% read:** 40% (abstract + definitions + quantitative findings + recommendations from full-text HTML)
- **Summary:** Identifies and measures a NON-statistical backtest error: "implementation risk" = the variability in backtest results attributable SOLELY to the choice of simulation ENGINE, with strategy logic, input data, and cost spec held fixed. Running the same strategy through five different engines: at ZERO cost all engines agree exactly (so divergence is entirely the COST MODEL's doing); with costs, divergence scales with turnover — under 0.18% total-return spread for simple strategies, 0.28–0.49% for signal-driven/ML strategies, up to 3.71% for high-turnover rotation. Cost-intensity vs engine-spread correlation is 0.93. Reassuringly, across 15 strategies engine choice NEVER reversed the sign of the Sharpe ratio. Recommends: validate cost-intensive strategies across ≥2 independent engines (ideally one event-driven + one vectorized), audit each engine's cost implementation against a reference algorithm, and report the implied ambiguity.
- **Relevance to our system:** A subtle test-data/engine-discipline point our single-engine app should heed. Because divergence is driven entirely by the COST MODEL and grows with TURNOVER, our churny candidate strategies (the ones most likely to look good in-sample) are exactly the ones whose backtested edge is most sensitive to cost-implementation details — so a high-turnover crown deserves extra skepticism and an explicit, audited cost spec. The "zero-cost engines agree" result is a useful internal sanity check: we could verify our backtest engine against a second simple reference at zero cost (must match to the penny) and then with costs (must agree within a documented tolerance). The sign-of-Sharpe-never-reverses finding is mildly reassuring that our verdict direction (beat vs not-beat hold) is robust to engine quirks, but the magnitude of net return — which feeds our buy-and-hold comparison — is not. Low-cost, high-value: add a zero-cost cross-check and an audited cost algorithm.

### [23] Correctness of Backtest Engines
- **Authors / Venue:** Robert Löw, Stanislaus Maier-Paape, Andreas Platen / arXiv (q-fin.TR), later in applied-math finance
- **Year:** 2015
- **Source:** arXiv:1509.08248 (https://arxiv.org/abs/1509.08248); PDF https://arxiv.org/pdf/1509.08248
- **% read:** 35% (abstract, intro, model-candles concept via PDF text extraction; skipped formal proofs)
- **Summary:** Tackles a rarely-discussed correctness problem: many trading-platform backtest engines silently compute WRONG historic performance because OHLC candle/bar data loses information. With only open/high/low/close per period, the intra-period PATH is unknown, so for combined order setups (e.g. a limit/stop ENTRY with an intra-period stop-loss and profit-target EXIT in the SAME candle) it is not uniquely decidable which order fills first — the engine must adopt a deterministic "decision mode," and different reasonable choices give different P&L. They formalize "model candles" + an intra-period price model and prove that if an engine passes a finite battery of tests on these model candles, it is correct in general; they also give algorithmic shortcuts for fast verification. Core message: backtest correctness is subtle and must be VERIFIED, not assumed.
- **Relevance to our system:** Directly relevant if our engine simulates intra-bar stop-loss / take-profit / limit fills on candle data — the exact ambiguity they flag. If a crowned strategy relies on intra-candle exits, our fill assumptions (does high or low come first?) can flip results, and an optimistic assumption manufactures phantom edge. Two actions: (1) document our engine's intra-candle decision mode explicitly (e.g. "assume worst-case ordering: stop-loss before target") so results are conservative and reproducible; (2) build a small set of "model candle" unit tests asserting our engine fills orders deterministically and conservatively. Pairs with [22] (implementation risk) — both say: VERIFY the engine, prefer conservative/worst-case assumptions, because over-optimistic mechanics are an alpha-inflation source independent of strategy logic.

### [24] Technical Trading Revisited: False Discoveries, Persistence Tests, and Transaction Costs
- **Authors / Venue:** Pierre Bajgrowicz, Olivier Scaillet / Journal of Financial Economics 106(3), pp. 473–491
- **Year:** 2012
- **Source:** SSRN:1095202 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=1095202); also Swiss Finance Inst. RP 08-05
- **% read:** 30% (abstract + headline findings via multiple search/secondary sources; full text gated)
- **Summary:** Revisits the famous apparent profitability of technical trading rules on the DJIA (1897–2011) using False Discovery Rate (FDR) control — a more powerful data-snooping correction than the FWER methods of White's RC [6]. The FDR approach selects MORE genuinely-outperforming rules than RC/SPA (enabling diversification against model uncertainty), but the economic conclusions are damning: (1) PERSISTENCE TESTS show that even with the better FDR technique, an investor could NEVER have selected the future-best rules ex ante — past winners did not predict future winners. (2) Even IN-SAMPLE, the apparent performance is COMPLETELY OFFSET once modest TRANSACTION COSTS are introduced. Overall, the long-claimed economic value of technical trading rules in early periods is seriously called into question.
- **Relevance to our system:** Possibly the single most on-thesis empirical paper for our product. It is exactly our experiment (a huge set of technical rules, data-snooping-corrected, on real prices) and reaches exactly our validated conclusion: technical rules don't robustly beat the alternative net of costs, and crucially you CAN'T pick tomorrow's winner from yesterday's. The persistence finding is a direct warning about our forward paper-trade: crowning the in-sample best does NOT mean it will be the forward best — which is precisely why our gate must judge robustness, not in-sample rank, and why buy-and-hold is the right benchmark. The transaction-cost-offsets-everything result reinforces [13]/[17]-sin#5: cost realism is decisive, and any crowned edge that evaporates under realistic fees is not an edge. Strongest external validation that our skeptical design is correct.

### [25] Lucky Factors
- **Authors / Venue:** Campbell R. Harvey, Yan Liu / Journal of Financial Economics 141(2), pp. 413–435
- **Year:** 2021 (working paper 2014–2019)
- **Source:** SSRN:2528780 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2528780); PDF https://people.duke.edu/~charvey/Research/Published_Papers/P146_Lucky_factors.pdf
- **% read:** 40% (abstract + full method/orthogonalization + bootstrap + sequential-selection sections via PDF text extraction; skipped per-factor empirical tables)
- **Summary:** Tackles the "factor zoo" with a multiple-testing method tailored to TIME-SERIES REGRESSIONS rather than the cross-sectional t-stat catalog of [5]. Goal: from a large set of candidate factors, identify which genuinely explain returns while controlling the Family-Wise Error Rate (FWER) under heavy test multiplicity and factor correlation. Core mechanics: (1) ORTHOGONALIZATION — transform the data so the null hypothesis (a factor has NO explanatory power for the cross-section / test assets) holds EXACTLY in-sample, which is what makes the bootstrap distribution valid under the null. (2) BOOTSTRAP — resample under the imposed null to get the joint distribution of the test statistic (they use the improvement in fit, e.g. an increase in test-asset R² or a scaled-intercept reduction) across ALL candidates simultaneously, naturally accounting for the search. (3) SEQUENTIAL SELECTION — pick the single best factor by bootstrapped significance, then ORTHOGONALIZE all remaining candidates with respect to the already-selected factor(s) and repeat, so each new factor must add INCREMENTAL explanatory power beyond those already chosen. Explicitly built on White's Reality Check [6] and Romano–Wolf StepM [8] (the "reality bootstrap"), extending them to a model-selection / panel setting. Conclusion: very few factors survive once multiplicity and redundancy are handled.
- **Relevance to our system:** The cleanest template for a multiple-testing-correct, dependence-aware ranking of CORRELATED candidates — exactly our bake-off, where neighboring SMA/MACD configs produce near-identical equity curves so naive Bonferroni [5] would massively over-penalize. The sequential orthogonalize-then-reselect loop is directly portable: crown the best strategy by bootstrapped excess-return-vs-hold, then orthogonalize the remaining candidates' return streams against the crowned one and ask whether ANY adds incremental robust edge — this both controls FWER and exposes that most "good" configs are just the crown in disguise (redundant, not independent discoveries). It also shows how to make a bootstrap null EXACT via orthogonalization, a refinement over a naive resample of raw excess returns. Pairs with [8] StepM as the rigorous way to report "the SET of strategies that beat hold" without the correlation-blind over-penalty. Strong candidate methodology if we ever upgrade ranking beyond top-1.

### [26] Replicating Anomalies
- **Authors / Venue:** Kewei Hou, Chen Xue, Lu Zhang / Review of Financial Studies 33(5), pp. 2019–2133; NBER WP 23394
- **Year:** 2020 (WP 2017)
- **Source:** SSRN:3275496 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3275496); PDF https://www.nber.org/system/files/working_papers/w23394/w23394.pdf
- **% read:** 35% (abstract + methodology + headline failure-rate results via PDF text extraction + corroborating lecture-note slides; skipped per-category anomaly tables)
- **Summary:** The largest single replication study of the asset-pricing "anomaly" literature and a centerpiece of finance's replication crisis. The authors compile a library of 452 anomalies (later ~447–452) and re-test every one with a uniform, microcap-robust protocol: NYSE breakpoints for portfolio sorts and VALUE-WEIGHTED returns (so tiny illiquid stocks — which dominate equal-weighted results and are largely untradeable — can't manufacture significance). Headline: with this protocol, 65% of the 452 anomalies fail to clear even the conventional single-test hurdle |t| > 1.96; for the "trading frictions" category the failure rate is 96%. Raising the bar to the multiple-testing hurdle |t| > 2.78 (≈ Harvey–Liu–Zhu [5]) pushes overall failure to 82%. They attribute the gap between original publications and replication primarily to microcaps + equal-weighting in the originals, plus p-hacking incentives (purely empirical literature, weak theory, publication bias, trillions invested). Note a live debate: Chen–Zimmermann (2022) and Jensen–Kelly–Pedersen (2023) report much HIGHER reproduction rates when methods are applied consistently, so the exact failure fraction is contested — but the direction (many anomalies are fragile to method choices) is widely accepted.
- **Relevance to our system:** The empirical embodiment of why our skeptical gate exists: most published "edges" evaporate under a uniform, conservative re-test — the same fate we expect for crowned bake-off strategies under our bootstrap-vs-hold gate. Two concrete imports. (1) METHOD-CHOICE FRAGILITY is itself a robustness axis: HXZ show results flip on weighting/breakpoint choices alone, mirroring [22]'s engine/cost sensitivity — so we should confirm a crown survives reasonable variations in our own evaluation choices (block length, cost spec, window), not just one configuration. (2) The "microcaps/equal-weighting inflate significance" mechanism is the equity analogue of our cost/turnover trap: apparent edge that lives in untradeable corners (here illiquid stocks; for us, high-turnover churn or thin-liquidity periods) is not real edge. The contested failure fraction (HXZ vs Chen–Zimmermann) is a useful caution against over-claiming from a single study — and a reminder that reproducibility depends on protocol discipline, which is exactly what a FROZEN gate enforces.

### [27] Optimal Execution of Portfolio Transactions
- **Authors / Venue:** Robert Almgren, Neil Chriss / Journal of Risk 3(2), pp. 5–39
- **Year:** 2000
- **Source:** DOI:10.21314/JOR.2001.041; PDF https://www.smallake.kr/wp-content/uploads/2016/03/optliq.pdf (PDF not text-extractable; read via search abstract + Anboto Labs deep-dive explainer + prior knowledge)
- **% read:** 35% (abstract + full model/impact-function/efficient-frontier mechanics from explainer; primary PDF binary-unreadable)
- **Summary:** The foundational optimal-execution paper. A trader must liquidate X shares over a fixed horizon; trading fast incurs market-IMPACT cost, trading slow incurs price-RISK (volatility exposure). Model: price follows arithmetic Brownian motion with a LINEAR PERMANENT impact g(v)=γ·(shares) (a lasting price shift proportional to total traded) plus a TEMPORARY impact h(v)=ε+η·(trade rate) (a transient liquidity cost paid only on the slice traded each period, ε a fixed/spread component). The cost of a trajectory (the "implementation shortfall" — pre-trade vs post-trade book value) has an EXPECTATION (driven by impact) and a VARIANCE (driven by volatility on the still-held position). They minimize E[shortfall] + λ·Var[shortfall], yielding an EFFICIENT FRONTIER of execution trajectories: λ=0 (risk-neutral) gives the minimum-impact straight-line/TWAP schedule; λ→∞ (very risk-averse) gives near-immediate execution. Interior optima are EXPONENTIAL/hyperbolic decay curves with a characteristic trading rate κ that INCREASES with risk aversion λ and volatility σ and DECREASES with temporary-impact η — i.e. more urgency/risk ⇒ trade faster up front.
- **Relevance to our system:** Background/boundary-setting for our cost model rather than a feature to build. For a €200 retail budget on a liquid coin, permanent + temporary impact are negligible (order size ≪ market depth), so our fixed-fee+spread cost model is justified — but Almgren–Chriss is the reference that JUSTIFIES that simplification explicitly, and the ε (fixed/spread) term of h(v) IS the part that bites us. The substantive lesson for the gate: a strategy's net edge is the difference of two costs (impact vs timing risk), and ANY backtest that executes "at the close, frictionlessly" ignores the E[shortfall] term — the same phantom-alpha source flagged in [13][22]. It also frames the future "what if I scaled to $X" guard: as size grows the temporary-impact η·(rate) term turns small apparent edges negative, the execution-cost analogue of [13]'s √-law. We do NOT need optimal scheduling, but we should keep a non-zero per-trade cost floor (spread + fee) so churny crowns can't fake a win at zero friction.

### [28] Portfolio Performance Manipulation and Manipulation-Proof Performance Measures (MPPM)
- **Authors / Venue:** William N. Goetzmann, Jonathan E. Ingersoll, Matthew I. Spiegel, Ivo Welch / Review of Financial Studies 20(5), pp. 1503–1546
- **Year:** 2007
- **Source:** SSRN:1151564 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=1151564); PDF https://ivo-welch.org/research/journalcopy/2007-rfs.pdf (PDF binary-unreadable; formula + axioms read via Breaking Down Finance explainer + abstract)
- **% read:** 30% (abstract + explicit MPPM formula + manipulation mechanism + axioms via secondary explainer; primary PDF not extractable)
- **Summary:** Shows that virtually every popular performance metric — Sharpe ratio, Sortino, Jensen's alpha, Treynor, Value-at-Risk-based scores — can be GAMED by a manager with no real skill, even under high transaction costs, by trading dynamically or writing options. Classic example: selling out-of-the-money puts/calls collects steady premium that RAISES the Sharpe ratio most of the time, hiding a fat negative tail that only shows up rarely — so a high Sharpe can be manufactured by selling insurance, not by skill. They ask what properties a NON-manipulable score must have and prove a manipulation-proof measure exists and is essentially unique: it must (1) produce a single valued score, (2) be increasing in returns, (3) be CONCAVE (penalize taking on risk for its own sake / reward diversification), and (4) be time-separable / consistent with an arbitrage-free economy so it can't be improved by mere dynamic repackaging. The resulting MPPM is the average of a POWER-UTILITY function over the return history: Θ = (1/((1−γ)·Δt))·ln( (1/T)·Σ_t [ (1+r_t)/(1+r_ft) ]^(1−γ) ), where γ is a risk-aversion parameter, Δt the period length in years, r_t the fund return, r_ft the risk-free rate, T the number of periods. Θ is interpretable as the certainty-equivalent (risk-free-equivalent) annual growth rate of the strategy for an investor with CRRA preferences.
- **Relevance to our system:** A direct upgrade candidate for our ranking SCORE. If our bake-off ranks/crowns by Sharpe (or any ratio), a strategy can win by harvesting premium-like, negatively-skewed payoffs — e.g. trend/mean-reversion rules that "win small often, lose big rarely," which crypto's fat left tail makes especially dangerous and which our bootstrap might under-sample. The MPPM Θ is gaming-resistant BY CONSTRUCTION (concave, so it docks exactly the negative-skew/fat-tail strategies a Sharpe rewards) and conveniently yields an interpretable number — a certainty-equivalent growth rate — that is directly comparable to buy-and-hold's Θ. Concrete imports: (a) add MPPM as a companion (or primary) ranking metric alongside Sharpe/DSR so we don't crown an insurance-seller; (b) compare the crowned strategy's Θ vs buy-and-hold's Θ as a manipulation-proof "does it really beat holding?" check; (c) the concavity penalty makes MPPM a natural pairing with the higher-moment-aware DSR/PSR [1][4][16]. Low-cost, high-value: it's just a power-mean over the return series we already store.

### [29] Backtesting (the Sharpe-ratio "haircut")
- **Authors / Venue:** Campbell R. Harvey, Yan Liu / Journal of Portfolio Management 42(1), pp. 13–28 (Special 40th Anniversary Issue)
- **Year:** 2015
- **Source:** SSRN:2345489 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2345489); PDF https://people.duke.edu/~charvey/Research/Published_Papers/P120_Backtesting.PDF
- **% read:** 45% (abstract + full method mapping p-value corrections → Sharpe haircuts + nonlinear-haircut result + recommendations via PDF text extraction; skipped simulation appendix)
- **Summary:** The practitioner companion to [5]: a concrete recipe for discounting a REPORTED Sharpe ratio for the number of strategies tried. They first convert an observed Sharpe (over T years) into a t-statistic and p-value, then apply the three standard multiple-testing adjustments — Bonferroni and Holm (control Family-Wise Error Rate) and Benjamini–Hochberg–Yekutieli (control False Discovery Rate) — to get an ADJUSTED p-value, then map that adjusted p-value BACK into an implied "haircut Sharpe ratio." Central result: the haircut is strongly NONLINEAR in the original Sharpe. A high Sharpe is only moderately penalized (it's likely a true discovery), while a MARGINAL Sharpe is penalized severely (it's likely luck) — so the common "just cut everything by 50%" rule of thumb is wrong (it over-penalizes the best and under-penalizes the worst). They stress that the relevant number of trials is almost always MUCH larger than what's reported (file-drawer / unpublished tests) and provide guidance for plausible trial counts; the haircut grows with the number of tests. Ships as the `SharpeRatio.haircut` / `haircutSharpe` function in R's quantstrat.
- **Relevance to our system:** This is the most directly IMPLEMENTABLE multiple-testing tool for our bake-off — it takes exactly what we have (a crowned Sharpe, a track length T, and a count of configs tried N) and returns a defensible, discounted Sharpe to report next to the raw one. The nonlinear-haircut insight reshapes how we'd present results: rather than a flat penalty, a config that barely edges out buy-and-hold gets gutted by the haircut (correctly flagging it as probable noise), while only a dramatically superior Sharpe would survive — which operationally reinforces our "nothing robustly beats holding" finding. It also gives the operator an intuitive single number. Pairs with [1] DSR (closed-form max-Sharpe deflation) and [25] Lucky Factors / [8] StepM (dependence-aware) — the haircut is the FWER/FDR-via-p-value route, DSR the extreme-value route; computing both and showing the more conservative is a strong default. Key caveat carried from the paper: count ALL configs in the sweep as trials, not just survivors.

### [30] False Discoveries in Mutual Fund Performance: Measuring Luck in Estimated Alphas
- **Authors / Venue:** Laurent Barras, Olivier Scaillet, Russ Wermers / Journal of Finance 65(1), pp. 179–216
- **Year:** 2010
- **Source:** SSRN:869748 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=869748); PDF https://web.mit.edu/econometrics/fdr2version.pdf (primary PDF not text-extractable; headline results read via search abstracts + reply paper + secondary sources)
- **% read:** 25% (abstract + headline proportions + FDR method outline via secondary sources; primary PDF binary-unreadable)
- **Summary:** Applies the False Discovery Rate (FDR) framework of Storey/Benjamini–Hochberg to the cross-section of U.S. equity mutual-fund alphas to separate genuine SKILL from LUCK. Rather than testing each fund and counting "significant" ones (which a multiple-testing-naive approach inflates), they model the cross-section of fund t-statistics as a MIXTURE of three populations — zero-alpha, truly positive-alpha (skilled), and truly negative-alpha (unskilled) funds — and use the FDR to estimate each population's PROPORTION and to control the expected fraction of false positives among funds flagged as skilled. Headline: ≈76.6% of funds have essentially ZERO alpha, ≈21.3% have NEGATIVE alpha (the unskilled, dispersed in the left tail), and only ≈2.1% have genuinely POSITIVE alpha (extreme right tail) — and that tiny skilled fraction has shrunk over time. A later debate (Andrikogiannopoulou–Papakonstantinou critique; BSW 2019 reply over 1975–2018) argues the FDR estimator can be conservative/low-power for small alphas but that performance improves over longer samples.
- **Relevance to our system:** The mutual-fund analogue of our bake-off, and a useful FRAMING upgrade: instead of "did the crowned strategy beat hold?", model the WHOLE field of swept configs as a mixture of {no-edge, real-positive-edge, real-negative-edge} and estimate the proportion that genuinely beat buy-and-hold via FDR. For our setting we'd almost certainly find the "real positive edge" proportion is ~0 (consistent with our thesis and with the ~2% skilled funds here) — a quantitative, multiple-testing-correct way to state "essentially none of these strategies have real edge" rather than only reporting the single crown. The negative-alpha mass (21%) is a direct parallel to [3]'s "overfit strategies actively lose" result — many configs aren't just no-edge, they're worse than holding net of costs. The conservative-power caveat is a reminder that FDR needs adequate T (track length) to detect small edges — ties to our MinBTL [3] concern. Mostly a reframing/reporting idea rather than a new gate component.

### [31] Open Source Cross-Sectional Asset Pricing (publication-bias-adjusted returns)
- **Authors / Venue:** Andrew Y. Chen, Tom Zimmermann / Critical Finance Review 11(2), pp. 207–264; Fed FEDS WP 2021-037
- **Year:** 2022 (WP 2020)
- **Source:** SSRN:3604626 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3604626); PDF https://www.federalreserve.gov/econres/feds/files/2021-037pap.pdf (PDF not text-extractable; results via search abstracts + project site openassetpricing.com)
- **% read:** 25% (abstract + headline reproduction/publication-bias numbers via secondary sources; primary PDF binary-unreadable)
- **Summary:** The DISSENTING data point in the finance replication debate, and the source of a fully open dataset (319 firm characteristics, signals + portfolios, openassetpricing.com / GitHub OpenSourceAP). They reproduce nearly all published cross-sectional return predictors and find HIGH reproducibility: for 161 characteristics clearly significant in their originals, 98% of reproduced long-short portfolios have |t|>1.96; a regression of reproduced t-stats on original t-stats has slope 0.88 and R²=82% (i.e. the originals were largely real, not typos or fraud). Crucially, they build a PUBLICATION-BIAS-adjusted return estimator (an empirical-Bayes shrinkage that accounts for the selection of which predictors get published) and find bias-adjusted returns are only ~12.3% smaller than naive in-sample returns (SE ~1.7pp) — a SMALL haircut. Their argument against the strong data-mining view: the DISPERSION of returns across predictors is far too large to be explained by data-mined noise (pure mining would give returns clustered near zero; the observed spread implies most signals carry real, if modest, information). This directly contrasts [26] Hou–Xue–Zhang (65–82% fail) and tempers [5] Harvey–Liu–Zhu — the disagreement turns on method choices (value- vs equal-weighting, microcaps, breakpoints) and on the model of the null.
- **Relevance to our system:** A vital COUNTERWEIGHT that keeps our skepticism honest and non-dogmatic. It does NOT undermine our thesis (it's about cross-sectional EQUITY anomalies with economic priors, not technical rules on a single crypto series — and [24] shows technical rules specifically fail net of costs), but it teaches two disciplines. (1) The size of a multiple-testing haircut DEPENDS ON THE NULL MODEL and the cross-section's return dispersion — a publication-bias / empirical-Bayes shrinkage can be much gentler than a worst-case Bonferroni [29]. For our bake-off, if we ever estimate "how much to shrink the crowned Sharpe," the honest answer is method-dependent, and we should report a RANGE (gentle empirical-Bayes vs conservative DSR/haircut) rather than one number. (2) The "dispersion too large for noise" test is a clever diagnostic we could borrow: if our swept configs' Sharpes are tightly clustered near buy-and-hold's, that itself is evidence of NO real edge (pure selection noise) — whereas a genuine alpha would show separation. Mostly a reasoning/reporting refinement; reinforces showing both optimistic and pessimistic corrections.

### [32] Interpretable Hypothesis-Driven Trading: A Rigorous Walk-Forward Validation Framework for Market Microstructure Signals
- **Authors / Venue:** (arXiv q-fin authors, 2025) / arXiv preprint
- **Year:** 2025
- **Source:** arXiv:2512.12924 (https://arxiv.org/abs/2512.12924); HTML https://arxiv.org/html/2512.12924v1
- **% read:** 45% (abstract + full validation-framework + multiple-testing + power-analysis sections from full-text HTML; skipped per-signal microstructure detail)
- **Summary:** A model paper for HONEST backtest methodology. Frames the reproducibility crisis bluntly ("over 90% of academic strategies fail with real capital") and enforces four principles: (1) information-set discipline — features/signals/decisions use only data available up to that timestamp (no look-ahead); (2) walk-forward across 34 independent test periods (252-day train / 63-day test / 63-day step) spanning multiple regimes; (3) every trade derives from a human-interpretable, natural-language hypothesis (auditable, anti-storytelling-by-construction); (4) realistic execution (commissions, slippage, position limits, stop-loss). Statistical safeguards: Deflated Sharpe Ratios + multiple-testing adjustments (Bailey/LdP), two-sided t-tests with NO selective p-value reporting, bootstrap CIs (10,000 resamples) and permutation tests. The headline is its INTELLECTUAL HONESTY: aggregate returns are NOT statistically significant (p=0.34) and they report it rather than p-hacking. Key methodological contribution — a POWER analysis: with 34 folds they had only ~12% statistical power; ~540 independent test periods would be needed for 80% power at the observed effect size, so "modest sample sizes provide limited inference." They find daily microstructure signals work in high-volatility regimes but fail in calm markets, and design the framework to be agnostic to the hypothesis source (hand-crafted, genetic programming, or LLM).
- **Relevance to our system:** Almost a manifesto for what our app already aspires to, and a checklist of upgrades. (1) The explicit STATISTICAL-POWER analysis is something our gate currently lacks: a bootstrap that returns "does not beat hold" could mean "real no-edge" OR "too few data / too little power to tell" — we should report the power / the effect size detectable at our window length and N (their 12%-power confession is exactly the honesty we want). This complements MinBTL [3] from the power side. (2) Their honest p=0.34 reporting is the literal embodiment of our "nothing robustly beats holding" stance — strong external precedent that publishing a NULL result rigorously is the correct output, not a failure. (3) The "agnostic to hypothesis source" design (same validation pipeline for hand-crafted / GA / LLM strategies) is exactly our bake-off philosophy — the gate, not the generator, is the arbiter. (4) Their regime-conditional finding (signals work only in high-vol) argues for REGIME-STRATIFIED reporting of our gate verdict. Directly actionable: add a power/effect-size line and regime-split verdicts to our ranking report.

### [33] Downside Risk Reduction Using Regime-Switching Signals: A Statistical Jump Model Approach
- **Authors / Venue:** Yizhan Shu, Chenyu Yu, John M. Mulvey / arXiv (q-fin.PM); later Quantitative Finance
- **Year:** 2024
- **Source:** arXiv:2402.05272 (https://arxiv.org/abs/2402.05272); PDF https://arxiv.org/pdf/2402.05272 (PDF not text-extractable; read via abstract page)
- **% read:** 30% (abstract + method/backtest summary from abs page; full PDF binary-unreadable)
- **Summary:** A regime-aware backtesting case study with disciplined out-of-sample evaluation. The "jump model" (JM) detects market regimes from the return series alone, differing from a hidden-Markov model by adding a JUMP PENALTY on each state transition that ENHANCES regime persistence (fewer spurious switches). Hyper-parameters (including the jump penalty) are chosen by TIME-SERIES cross-validation. The strategy cuts market exposure during regimes flagged as unfavorable, aiming to reduce downside risk. Backtest spans US/Germany/Japan equity indices 1990–2023 with transaction costs AND trading delays explicitly modeled (e.g. apply day-t regime to trade at end of t+1). Result: the JM-guided strategy reduces volatility and maximum drawdown and improves Sharpe vs both an HMM strategy and buy-and-hold, and is reported as relatively robust to trading-delay assumptions.
- **Relevance to our system:** Two methodology imports, plus a caution. (1) ROBUSTNESS TO EXECUTION-TIMING: they explicitly stress the result against a one-bar trading delay (signal at t, trade at t+1) — a cheap, high-value robustness check we should run on any crowned strategy, since edges that vanish under a one-bar delay are look-ahead artifacts ([15][17]-sin#2). (2) TIME-SERIES CV for hyper-parameters (not plain K-fold) is the leakage-aware way to tune the jump penalty — aligns with our purge/embargo concern [9]. (3) Caution: this is a single-path backtest (the weakest scheme per [11][19]) on equity indices, and "beats buy-and-hold on drawdown/Sharpe" is a DRAWDOWN-reduction claim, not a total-return claim — a defensive overlay can improve Sharpe while underperforming hold in total return, which is exactly the distinction our gate must keep straight (our benchmark is buy-and-hold on the relevant metric). Useful as a regime-overlay idea to TEST in our app, but its evidence standard (single path, no DSR/PBO) is below our gate's bar — we'd re-evaluate any such overlay under the bootstrap.

### [34] Comparing Sharpe Ratios: PSR & Minimum Track Record Length for the DIFFERENCE of Sharpe Ratios
- **Authors / Venue:** Methodology synthesis — J.D. Opdyke (J. Asset Mgmt 2007) + Bailey–López de Prado (2012) [4]; documented via Portfolio Optimizer (Roman Rubsamen) technical write-up
- **Year:** 2007 (Opdyke) / 2012 (BLdP) / write-up 2022
- **Source:** https://portfoliooptimizer.io/blog/the-probabilistic-sharpe-ratio-hypothesis-testing-and-minimum-track-record-length-for-the-difference-of-sharpe-ratios/ ; Opdyke "Comparing Sharpe ratios: so where are the p-values?" J. Asset Management 8(5)
- **% read:** 40% (full method + formulas from the technical write-up; not Opdyke's primary paper verbatim)
- **Summary:** Generalizes the single-strategy Probabilistic Sharpe Ratio / Minimum Track Record Length [4] to the TWO-strategy comparison — exactly "is strategy A's Sharpe statistically greater than strategy B's?". The difference estimator SR_diff = SR_a − SR_b is asymptotically normal with a standard error that depends on the skewness, kurtosis, and CROSS-moments (and correlation) of the two return series (Opdyke's distribution-free asymptotics, valid for non-normal, non-IID returns). The generalized PSR is PSR_a(SR_b) = Φ( SR_diff / SE(SR_diff) ) — the probability A truly out-Sharpes B. The corresponding Minimum Track Record Length is MinTRL_a(SR_b) = (λ_a + λ_b + λ_{a,b})·(z_{1−α} / SR_diff)², where λ_{a,b} captures the joint/correlation moments — so HIGH correlation between the two streams REDUCES the SE and the required sample length (it's easier to tell apart two highly-correlated series than two independent ones). Key reference is Opdyke (2007) for the asymptotics and BLdP (2012) for the probabilistic framing.
- **Relevance to our system:** This is the single most ON-POINT statistical test for our core question, which is literally a difference of Sharpes: does the crowned strategy's Sharpe exceed BUY-AND-HOLD's Sharpe by a statistically significant margin? Three concrete imports. (1) Replace/augment any "strategy SR > hold SR" eyeballing with the generalized PSR_strategy(SR_hold) — a probability the operator can read directly, higher-moment- and correlation-aware. (2) The correlation term is crucial and FAVORABLE for us: a long/flat strategy is highly correlated with buy-and-hold (it IS the asset most of the time), so the SE of the Sharpe DIFFERENCE is smaller than a naive independent-samples test would suggest — we can detect a real difference with less data than we'd fear, AND it correctly shows that a strategy that's "hold 95% of the time" has almost no distinguishable edge. (3) MinTRL_a(SR_b) answers "how long must the forward paper-trade run before we could even tell the crown apart from holding?" — a principled stopping/confidence rule. Pairs with [16] (Sharpe SE) and [35] (Ledoit–Wolf bootstrap version). Highly actionable, cheap (closed-form from the two return series).

### [35] Robust Performance Hypothesis Testing with the Sharpe Ratio
- **Authors / Venue:** Olivier Ledoit, Michael Wolf / Journal of Empirical Finance 15(5), pp. 850–859
- **Year:** 2008
- **Source:** DOI:10.1016/j.jempfin.2008.03.002; PDF http://www.ledoit.net/jef_2008pdf.pdf (self-signed cert, not fetchable); method via search abstracts + R `PeerPerformance::sharpeTesting` docs + IDEAS/RePEc
- **% read:** 30% (abstract + full method outline + decision rule via multiple secondary sources; primary PDF blocked by self-signed cert)
- **Summary:** The bootstrap counterpart to [34]'s closed-form test, and the most-cited robust method for comparing two Sharpe ratios. Problem: standard tests for the difference of Sharpe ratios assume IID normal returns; real returns are heavy-tailed, skewed, and autocorrelated, so those tests have wrong size/coverage. Ledoit–Wolf propose a STUDENTIZED TIME-SERIES (circular block) BOOTSTRAP: resample blocks of the bivariate return series to preserve dependence, recompute the studentized Sharpe-difference statistic on each resample (studentization uses a HAC — heteroskedasticity-and-autocorrelation-consistent — kernel variance estimator), and build a confidence interval for SR_diff. Decision rule: the two Sharpes differ significantly iff the bootstrap CI does NOT contain zero. A simulation study shows materially better finite-sample size/power than the normal-theory (Jobson–Korkie/Memmel) test under realistic return distributions. Ships as `PeerPerformance::sharpeTesting` and in `PerformanceAnalytics` in R.
- **Relevance to our system:** The method whose machinery is CLOSEST to ours (block bootstrap) applied to the exact comparison we care about (strategy Sharpe vs buy-and-hold Sharpe). We already run a 1000-path block bootstrap; adopting Ledoit–Wolf's STUDENTIZED Sharpe-difference statistic inside that same loop would give a rigorous, distribution-free p-value/CI for "does the crown beat hold on Sharpe?" with essentially no new infrastructure — just compute the studentized difference per resample and check whether the CI excludes zero. The HAC studentization is the autocorrelation-correct version of the Sharpe SE we flagged in [16], important for crypto's volatility clustering. This is the bootstrap sibling of [34]'s closed-form PSR-difference; running both and requiring agreement is a strong, frozen-gate-compatible upgrade. Among the highest-leverage, lowest-cost methodological imports for our specific gate.

### [36] Tactical Investment Algorithms
- **Authors / Venue:** Marcos López de Prado / SSRN working paper (ADIA / True Positive Technologies)
- **Year:** 2019
- **Source:** SSRN:3459866 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3459866 — 403 gated); read via search abstracts + Scribd copy + Portfolio123/Intalcon write-ups
- **% read:** 30% (abstract + the three-methods argument + tactical-vs-all-weather thesis via secondary sources; primary gated)
- **Summary:** Lays out three ways to test an investment algorithm against history and argues for the strongest. (1) WALK-FORWARD — the single historical path; its fatal flaws are that it assumes history repeats exactly, uses just ONE realized path, and NEVER states the data-generating process (DGP), so when the pattern breaks you can't understand WHY it worked and therefore can't decommission the model before it bleeds money. (2) RESAMPLING (bootstrap/CV) — simulates many paths from the observed data; hard to overfit, but a finite historical sample may not contain paths representative of the future. (3) MONTE CARLO — simulate future paths from a DGP estimated via statistical analysis + economic theory; LdP favors this because an explicit DGP lets you evaluate the algorithm under conditions not seen in history and KNOW when its assumptions no longer hold. Second thesis: prefer "TACTICAL" algorithms each optimal for a SPECIFIC market regime (deploy/retire by regime) over an "all-weather"/"strategic" algorithm assumed to work in every regime — the latter is usually a backtest-overfit illusion.
- **Relevance to our system:** Sharpens the three-types taxonomy [19] into a clear ranking that maps onto our app: our bootstrap gate is RESAMPLING (good, hard to overfit) and our forward run is WALK-FORWARD (weakest, no DGP). The strongest actionable idea is the DGP-DECOMMISSION argument: because our forward paper-trade never states WHY a crowned strategy should work, we cannot know when to stop trusting it — a reason to (a) require an explicit hypothesis/causal story before crowning (anti-storytelling, [17]-sin#3, [19]) and (b) consider a Monte-Carlo leg that simulates crypto paths from a fitted DGP (GARCH/jump) to stress the crown beyond the one observed history. The TACTICAL-vs-all-weather point reframes our search: rather than hunting one strategy that beats hold always, we could test whether ANY strategy beats hold within a specific REGIME (and accept it only there) — but with the strict caveat that regime-conditioning multiplies trials and overfitting risk, so it must be DSR/PBO-penalized. Note: LdP's preference for parametric MC is contestable (a wrong DGP gives confidently wrong answers, per [19]); our non-parametric block bootstrap is a defensible middle ground.

### [37] Stock Portfolio Design and Backtest Overfitting
- **Authors / Venue:** David H. Bailey, Jonathan M. Borwein, Marcos López de Prado / Journal of Investment Management 15(1), pp. 1–13
- **Year:** 2017
- **Source:** SSRN:2739335 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2739335); PDF https://carmamaths.org/jon/stockfund.pdf (PDF not text-extractable; read via abstract + JOIM listing + search summary)
- **% read:** 30% (abstract + thesis + headline result via secondary sources; primary PDF binary-unreadable)
- **Summary:** Extends the Bailey–Borwein–LdP overfitting line [3] from single trading rules to PORTFOLIO CONSTRUCTION — designing a fund/portfolio as a weighted basket of stocks. Core point: backtest overfitting occurs whenever too many strategy/weighting VARIATIONS are tried relative to the available data, and this is a primary reason portfolios that look great in-sample disappoint live. They show how the same selection-bias mathematics (expected max performance over N trials, MinBTL [3]) applies to choosing portfolio WEIGHTS / membership, not just rule parameters — the optimizer that picks the "best" weights on history is itself a multiple-testing engine, so an apparently optimal in-sample portfolio is largely fitted to noise. The remedy is the same family: limit the number of effective trials, lengthen the data, and use overfitting-aware evaluation (PBO/DSR).
- **Relevance to our system:** Mostly REINFORCING and boundary-marking. Our advisor is single-coin (no cross-sectional weight optimization), so the portfolio-weight overfitting it targets isn't a direct surface — BUT the principle "any optimizer that selects 'best' parameters on history is a multiple-testing engine" applies squarely to our bake-off's parameter sweep AND to our newer param-TUNING feature (tuning SMA/RSI thresholds on a window). It's a caution that adding tunable knobs (composed families, threshold tuning) silently inflates N and overfitting risk, so each new knob must be counted as trials and DSR/PBO-penalized — exactly the discipline [18] argued for from the model-based side. Confirms the anti-overfitting toolkit (MinBTL/DSR/PBO) generalizes to ANY in-sample optimization, weights or rules alike.

### [38] AlgoXpert Alpha Research Framework: A Rigorous IS-WFA-OOS Protocol for Mitigating Overfitting
- **Authors / Venue:** (arXiv q-fin authors, 2026) / arXiv preprint
- **Year:** 2026
- **Source:** arXiv:2603.09219 (https://arxiv.org/abs/2603.09219); PDF https://arxiv.org/pdf/2603.09219
- **% read:** 35% (abstract + three-stage protocol + statistical-adjustment + guardrail sections from full-text PDF; skipped implementation specifics)
- **Summary:** A practitioner protocol that chains THREE separated stages to fight overfitting: (1) IN-SAMPLE — strategy development + parameter optimization on historical data; (2) WALK-FORWARD ANALYSIS — repeatedly re-optimize on rolling windows then test on the next period without refit, simulating periodic live re-tuning; (3) OUT-OF-SAMPLE — final validation on data held out from BOTH IS and WFA. Statistical discipline: applies Deflated-Sharpe-Ratio corrections [1] for the number of trials/rule combinations, explicitly LIMITS the number of parameter combinations tested to bound the multiple-comparison problem, requires OOS windows large enough for significance, and compares IS-vs-WFA-vs-OOS metrics to detect degradation. Core recommendation: a robust strategy must show CONSISTENCY across all three stages with minimal IS→OOS deterioration.
- **Relevance to our system:** A clean blueprint for hardening our pipeline's stage separation. We already have an IS bake-off and a forward (≈OOS) paper-trade; the missing middle is a disciplined WFA leg and, crucially, the EXPLICIT TRIAL-COUNT GUARDRAIL — they don't just deflate after the fact, they CAP the number of parameter combinations up front, which is a direct, cheap policy for our sweep/tuning features (bound N so DSR stays meaningful). The "consistency across all three stages, minimal IS→OOS degradation" criterion is a concrete pass/fail to add next to our bootstrap verdict (it's the Walk-Forward-Efficiency idea [12] formalized into a gate). Confirms (again) DSR as the standard trial-count correction. Mostly an engineering/discipline import rather than new statistics; pairs with [11][19] (resampling > walk-forward) by treating walk-forward as a degradation DETECTOR, not the robustness verdict.

### [39] An Evaluation of Alternative Multiple Testing Methods for Finance Applications
- **Authors / Venue:** Campbell R. Harvey, Yan Liu, Alessio Saretto / Review of Asset Pricing Studies 10(2), pp. 199–248
- **Year:** 2020
- **Source:** SSRN:3480087 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3480087); DOI:10.1093/rapstu/raaa003; primary PDFs (FDP Institute / RAPS) 404/gated — read via search abstracts + Oxford listing + Scholars@Duke
- **% read:** 30% (abstract + the five-method comparison + dependence/power conclusion via secondary sources; primary PDF unavailable)
- **Summary:** A horse-race of multiple-testing corrections SPECIFICALLY for finance, where test statistics are heavily CORRELATED (factors/strategies overlap). Compares five Family-Wise-Error-Rate controllers: (i) Bonferroni (1936), (ii) Holm (1979), (iii) White's (2000) bootstrap Reality Check [6], (iv) Romano–Wolf StepM (2005) [8], and (v) Harvey–Liu's (2019) Stepwise Model Selection (SMS, the engine behind [25] Lucky Factors). Key message: Bonferroni and Holm assume INDEPENDENCE (or use union bounds) and therefore MASSIVELY OVER-CORRECT when tests are positively correlated — they spend the error budget as if every test were a fresh independent shot, when correlated tests carry far fewer than N independent pieces of information (the "effective number of tests" m_eff ≪ N). The bootstrap methods (BRC/StepM/SMS) RESAMPLE the joint data and thus learn the actual dependence structure, recovering power while still controlling FWER. Recommendation: in finance, use a dependence-aware bootstrap/stepwise method, not Bonferroni/Holm, and account for the effective (not nominal) number of tests.
- **Relevance to our system:** Settles a design question our gate faces directly: our bake-off's configs are HIGHLY correlated (neighboring SMA windows ⇒ near-identical equity curves), so a Bonferroni haircut on the raw config count N would be far too harsh — it would over-penalize a genuine winner and is the WRONG tool. This paper is the authority for using a dependence-aware BOOTSTRAP instead, which is exactly the machinery we already own (1000-path block bootstrap). Concrete imports: (1) when correcting for multiple testing, resample the JOINT performance of all configs (not per-config independent p-values) so the correlation is captured automatically — this is the RC/StepM [6][8] route and our gate is the right substrate for it; (2) report the EFFECTIVE number of trials (m_eff via the eigenvalue/correlation structure of the config return matrix) rather than the raw config count when computing DSR [1] / haircut [29] — likely m_eff ≪ N, which actually makes our gate FAIRER to a real winner while still honest. Resolves the open question in knowledge.md about the right N for DSR.

### [40] False (and Missed) Discoveries in Financial Economics
- **Authors / Venue:** Campbell R. Harvey, Yan Liu / Journal of Finance 75(5), pp. 2503–2553
- **Year:** 2020
- **Source:** DOI:10.1111/jofi.12951; PDF https://people.duke.edu/~charvey/Research/Published_Papers/P143_False_and_missed.pdf
- **% read:** 45% (abstract + full Type-I/Type-II framing + loss-function-threshold argument from PDF text extraction; skipped formal estimator derivation)
- **Summary:** The crucial CORRECTIVE to the "just raise the t-stat hurdle" message of [5]. Multiple-testing corrections reduce FALSE discoveries (Type I) but, by raising the bar, mechanically INCREASE MISSED discoveries (Type II — real effects you fail to detect). Treating the problem as one-sided (only fear false positives) is itself a mistake: an excessively high hurdle throws away genuine signals. They reframe threshold selection as a LOSS-FUNCTION / decision problem that explicitly weighs the cost of a false discovery against the cost of a missed one, noting different stakeholders rationally prefer different hurdles. They also stress the DOUBLE-COUNTING / redundancy issue — many "factors" are overlapping phenomena, not independent tests, so the right correction depends on the effective (dependence-adjusted) number of tests, not the raw count. Key contribution beyond HLZ 2016: SYMMETRY — both error types matter, and the optimal cutoff comes from an explicit loss function, not a universal magic number like t=3.0.
- **Relevance to our system:** A vital balance-check on our deliberately skeptical design. Our gate is tuned to avoid Type-I (crowning a fake winner) — correct given our "nothing beats hold" prior and the asymmetric cost to a retail user of being told a noise strategy works. But [40] reminds us this has a COST: if a genuinely-edge strategy ever existed, an over-conservative gate would MISS it (Type II). Two imports. (1) Make the gate's strictness an EXPLICIT, documented loss-function choice ("we weight a false 'beat' as far more costly than a missed real edge, because the product's job is to protect a paper-trading retail user"), rather than an unexamined default — this is honest and defensible. (2) The effective-number-of-tests / double-counting point reinforces [39]: penalize by m_eff, not raw N, so we don't ADD unnecessary Type-II error by over-counting correlated configs. Together [39][40] say: be strict, but be strict CORRECTLY (dependence-aware) so the strictness buys real Type-I protection instead of just blunt power loss. Strong framing for documenting WHY our FROZEN gate is calibrated the way it is.

### [41] Diffusive in Plain Sight: An Inconspicuous Law of Market Impact
- **Authors / Venue:** Julius F. Bonart / arXiv (q-fin.TR)
- **Year:** 2026
- **Source:** arXiv:2606.07059 (https://arxiv.org/abs/2606.07059); HTML https://arxiv.org/html/2606.07059
- **% read:** 35% (abstract + core diffusivity argument + impact-form + crossover sections from full-text HTML; skipped formal derivation)
- **Summary:** A fresh theoretical explanation for WHY market impact follows the square-root law [13]. Rather than treating the √-law as a microstructure puzzle, Bonart argues it is a direct, almost inevitable consequence of price DIFFUSIVITY (returns being approximately uncorrelated). The argument: if both the realized and the counterfactual (no-trade) price are diffusive, then their difference — the impact of an information-neutral trader — is constrained to grow as the square root of traded volume. Functional form recovered without microstructure assumptions: impact ∝ sign(Q)·σ·√(|Q|/V) (Q total metaorder volume, V daily volume, σ volatility), and it is approximately INDEPENDENT of how the order is sliced (number of child orders N) or the execution time T — only total volume Q matters. Important nuance: when trades are INFORMED (correlated with future returns), impact crosses over from √ to LINEAR (I ∝ participation rate) — mechanical execution gets √-impact, informed execution gets linear impact. Practical diagnostic: an impact model that violates the diffusivity/whitening requirement leaves autocorrelation in the "de-impacted" price — a detectable inconsistency.
- **Relevance to our system:** Deepens the cost-model background from [13][27] with two usable ideas. (1) The √-law's near-independence from execution schedule reassures us that for a small €200 order (participation rate ≈ 0) a simple per-trade cost (fee+spread) is the right approximation — schedule/impact subtleties only matter at size. (2) The √→LINEAR crossover for INFORMED trades is a subtle warning: if a strategy genuinely had predictive edge (informed), its real-world impact would be LINEAR, not √ — i.e. the more "alpha" a strategy claims, the WORSE its execution cost scaling, which further erodes apparent edge at any non-trivial size (the [13][3] phantom-alpha theme made sharper). Still background-only for our current retail-scale app, but the "adaptive markets offset historically calibrated impact" point is a good caveat for any future capital-scaling feature. Confirms the √-law is robust enough to rely on as our impact reference.

### [42] AutoQuant: An Auditable Expert-System Framework for Execution-Constrained Auto-Tuning in Cryptocurrency Perpetual Futures
- **Authors / Venue:** (arXiv q-fin authors, 2025) / arXiv preprint
- **Year:** 2025
- **Source:** arXiv:2512.22476 (https://arxiv.org/abs/2512.22476); PDF https://arxiv.org/pdf/2512.22476
- **% read:** 40% (abstract + execution-constraint + auto-tuning + overfitting-control sections from full-text PDF; skipped expert-system implementation + result tables)
- **Summary:** A crypto-specific auto-tuning framework whose central thesis is that most backtesting tools ignore real execution FRICTIONS that erode profit. It explicitly models perpetual-futures costs — FUNDING RATES (periodic settlement unique to perps), slippage and fees (size/condition-dependent), and leverage/margin/liquidation constraints — rather than assuming instant mid-price fills. The auto-tuner runs many optimizers (grid, random, Bayesian/Optuna, differential evolution, CMA-ES) over an "execution-centric" objective that PENALIZES strategies generating excessive slippage or funding cost. Overfitting discipline: Deflated Sharpe Ratio [1] to discount for the number of trials, walk-forward analysis (rolling re-optimize → OOS test), out-of-sample validation, a White-Reality-Check-style [6] multiple-testing adjustment, and a "DOUBLE SCREENING" via DSR to filter spurious strategies before deployment. The design is AUDITABLE — parameter choices and optimization decisions are logged for reproducibility.
- **Relevance to our system:** The closest paper to a "responsible auto-tuner for crypto" and a near-checklist for our param-tuning feature. (1) FUNDING-RATE modeling is a crypto-specific cost we must not ignore if any strategy touches perps/leverage — for our spot, long/flat retail advisor it's mostly N/A, but it flags that crypto cost realism ≠ equity cost realism (our core/funding_rate.rs already models this — good). (2) The "auto-tuner is a multiple-testing engine, so deflate by trial count + walk-forward + DSR double-screen" pattern is EXACTLY the guardrail our SMA/RSI/composed-family tuning needs (ties to [37][38][39]) — every optimizer call is a trial, and Bayesian/evolutionary searches can rack up enormous effective N invisibly. (3) The AUDITABILITY principle (log every parameter/decision) matches our FROZEN-gate reproducibility ethos and our spec-driven discipline. (4) Their execution-centric objective (penalize slippage/funding in the objective itself, not just post-hoc) is a cleaner design than gate-after-the-fact: we could fold realistic costs into the bake-off ranking score directly. Strong, directly-applicable guidance for keeping the tuning feature honest.

### [43] On the Maximum Drawdown of a Brownian Motion
- **Authors / Venue:** Malik Magdon-Ismail, Amir F. Atiya, Amrit Pratap, Yaser S. Abu-Mostafa / Journal of Applied Probability 41(1), pp. 147–161
- **Year:** 2004
- **Source:** DOI:10.1239/jap/1077134674; PDF https://www.cs.rpi.edu/~magdon/ps/journal/drawdown_journal.pdf (PDF not text-extractable; results via search abstracts + RPI talk slides)
- **% read:** 30% (abstract + main scaling results + Calmar relationship via secondary sources; primary PDF binary-unreadable)
- **Summary:** Derives the distribution and EXPECTED VALUE of the maximum drawdown (largest peak-to-trough drop) of a Brownian motion with drift μ and volatility σ over horizon T. For ZERO drift, an analytic closed form; for nonzero drift, an infinite-series representation. The headline is the LIMITING (large-T) SCALING of expected max drawdown: LOGARITHMIC in T for positive drift (a winning process barely draws down more as time goes on), SQUARE-ROOT in T for zero drift (E[MDD] ∝ σ√T), and LINEAR in T for negative drift (a losing process bleeds proportionally). They build scaling laws for the Calmar / Sterling-type ratios (return ÷ max drawdown) and note that under these dynamics optimizing the Sharpe ratio also optimizes the Calmar ratio (Calmar is monotonic in Sharpe), giving a theoretical basis for drawdown-based objectives.
- **Relevance to our system:** Gives us a NULL MODEL for drawdown — the missing benchmark for judging whether a strategy's observed max drawdown is impressive or expected. Just as DSR [1] compares an observed Sharpe to E[max Sharpe under noise], we can compare a strategy's observed max drawdown to E[MDD] for a drift-σ Brownian null calibrated to the coin: a drawdown NOT meaningfully smaller than the √T-scaled no-edge expectation is no evidence of skill. The drift-sign scaling is intuitively useful for crypto's regimes (bull = log-scaling shallow drawdowns; bear = linear bleed). The Calmar-monotonic-in-Sharpe result is a caution: reporting BOTH Sharpe and Calmar adds little independent information under these assumptions, so for a manipulation-resistant view prefer MPPM [28] over piling up correlated ratios. Cheap to compute (closed-form E[MDD] from the coin's μ,σ,T) and a natural companion to our bootstrap's empirical drawdown distribution — gives the analytic benchmark the bootstrap's percentile can be checked against.

### [44] Non-Standard Errors in Asset Pricing: Mind Your Sorts
- **Authors / Venue:** Amar Soebhag, Bart van Vliet, Patrick Verwijmeren / Journal of Empirical Finance (2024); building on Menkveld et al. "Non-Standard Errors" (J. Finance 2024)
- **Year:** 2024
- **Source:** DOI:10.1016/j.jempfin.2024.101517; SSRN:4338122; method via ScienceDirect abstract + Alpha Architect explainer + Menkveld et al. lineage
- **% read:** 30% (abstract + non-standard-error concept + magnitude + key design-choices via secondary sources; primary gated)
- **Summary:** Formalizes a source of uncertainty BEYOND the usual sampling-based "standard error": the NON-STANDARD ERROR (NSE), defined as the dispersion in a result that arises purely from defensible RESEARCH-DESIGN choices on the SAME data. Following Menkveld et al. (where 164 research teams analyzing one dataset produced widely scattered estimates), Soebhag et al. data-mine 250+ defensible construction variants of each asset-pricing factor (choices about excluding microcaps, industry-adjusting, value- vs equal-weighting, rebalancing frequency, breakpoints) and measure how much the factor's Sharpe / estimate moves. Finding: the variation is large — the average ratio of NON-standard error to standard error EXCEEDS ONE, meaning design-choice ambiguity contributes MORE uncertainty than sampling noise. Implication: a single reported number is misleadingly precise; researchers must disclose and justify design choices, and ideally report the DISTRIBUTION of results across reasonable specifications (a "specification curve").
- **Relevance to our system:** The rigorous, quantified version of the method-fragility theme from [22] (engine implementation risk) and [26] (HXZ weighting choices) — and a direct mandate for how our app should report a crown's robustness. Our evaluation has its own design knobs (bootstrap block length [20], cost spec, window start/length, bar frequency, indicator lookback), each a "researcher degree of freedom." NSE says the honest output is NOT one verdict but the DISTRIBUTION of verdicts across reasonable knob settings — a specification curve for "does the crown beat hold?". Concrete imports: (1) run the gate over a small grid of defensible evaluation settings and report whether the "beats hold" verdict is STABLE (if it flips on block length or cost spec, it's not robust — exactly the fragility we must surface); (2) treat our FROZEN gate's fixed choices as the headline but disclose the NSE around them rather than implying false precision. This is the strongest argument yet for a "specification-curve" robustness panel, and it dovetails with our existing multi-window stability idea [12]. High-value reporting discipline.

### [45] Data-Snooping, Technical Trading Rule Performance, and the Bootstrap
- **Authors / Venue:** Ryan Sullivan, Allan Timmermann, Halbert White / Journal of Finance 54(5), pp. 1647–1691
- **Year:** 1999
- **Source:** SSRN:160330 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=160330); PDF https://www.kevinsheppard.com/files/teaching/mfe/advanced-econometrics/Sullivan_Timmermann_White.pdf (cert error, not fetchable); read via search abstracts + EconPapers + data-snooping.martinsewell.com
- **% read:** 30% (abstract + universe size + headline data-snooping-corrected conclusion + later-period result via secondary sources; primary PDF cert-blocked)
- **Summary:** The landmark application of White's Reality Check [6] to technical trading. Brock–Lakonishok–LeBaron (1992) famously found simple MA/filter rules profitable on 100 years of DJIA; STW ask whether that survives once you account for the FULL UNIVERSE of rules a researcher could have tried. They build a universe of ~7,846 technical trading rules (parameterized filters, moving averages, support/resistance, channel breakouts, on-balance volume) and apply RC's stationary-bootstrap to compute a data-snooping-adjusted p-value for the BEST rule's performance. Result: in the original 1897–1986 sample the best rule's superiority does still clear the RC bar (BLL's finding is somewhat robust in-sample), BUT extending to 1987–1996 the best rules' OUT-OF-SAMPLE performance DETERIORATES sharply and is no longer significant — consistent with the early profitability being a now-arbitraged or partly snooped artifact. The methodological contribution is enduring: the RIGHT null tests the best rule against the entire universe searched, via a dependence-preserving bootstrap.
- **Relevance to our system:** The historical proof-of-concept for our exact gate design — a bootstrap test of the BEST of a large rule universe against a benchmark, accounting for the full search. Two lessons. (1) METHOD: STW operationalize "best-vs-benchmark over ALL rules tried" [6] on real price data with a huge N — validating that our bootstrap-vs-hold gate is the academically correct frame, and that the universe (every config swept) must define the null, not the survivor alone. (2) SUBSTANCE: even rules that pass IN-SAMPLE (their 1897–1986 best) decay OUT-OF-SAMPLE (1987–1996) — a direct precedent for our forward paper-trade underperformance and a reminder that an in-sample RC pass is necessary but NOT sufficient; out-of-sample/forward confirmation matters, and edges arbitrage away over time. Pairs with [24] (Bajgrowicz–Scaillet, FDR + costs kill the edge) and [40] (Type-II caveat). Strong anchor that our skeptical bootstrap gate is the established right tool.

### [46] Estimating the Effective Number of Independent Trials for the Deflated Sharpe Ratio (clustering method)
- **Authors / Venue:** Marcos López de Prado et al. (DSR implementation literature); synthesized via Wikipedia "Deflated Sharpe ratio" + DSR practitioner sources
- **Year:** 2014 (DSR) / clustering refinement ~2019–2020
- **Source:** https://en.wikipedia.org/wiki/Deflated_Sharpe_ratio ; builds on [1] SSRN:2460551
- **% read:** 40% (full effective-N clustering procedure + DSR formula recap via Wikipedia; the original derivations are in [1] and LdP's later ML book)
- **Summary:** Addresses the single hardest input to making the Deflated Sharpe Ratio [1] usable in practice: what is N, the number of trials? Naively N = number of configs tested, but configs are CORRELATED (overlapping features / neighboring parameters), so the raw count grossly overstates the independent search and over-deflates. The proposed fix estimates the EFFECTIVE number of INDEPENDENT trials by clustering: (1) compute the correlation matrix of the candidate strategies' return series and convert correlations to an angular DISTANCE metric; (2) apply a clustering algorithm (e.g. ONC — Optimal Number of Clusters, hierarchical, or spectral); (3) take the NUMBER OF CLUSTERS as the effective N for the DSR's E[max Sharpe] term. The cross-cluster variance of Sharpe ratios feeds the expected-max-Sharpe threshold SR₀. The literature stresses planning the multiple-testing exercise in advance and RECORDING every backtest's returns (burdensome but necessary) so the correlation/clustering can be computed honestly.
- **Relevance to our system:** This is the missing operational piece for porting DSR [1] to our bake-off, and it directly answers the open question from [39]: how to get m_eff. Our swept configs (SMA windows, RSI thresholds, composed families) produce highly correlated equity curves, so the right N for DSR is the NUMBER OF CLUSTERS of distinct strategy behavior, not the raw config count — likely far smaller, which makes the deflation FAIR rather than punitive. Concrete plan: we already produce a return series per config; build their correlation matrix → angular distance → cluster → effective N → feed DSR. This both hardens the gate (honest selection-bias correction) and is interpretable ("you really tried ~k distinct ideas, not 200"). It also imposes a useful discipline we should adopt regardless: RECORD every config's return series in the bake-off output (not just the winner's) so effective-N, PBO [2], and FDR [30] are all computable after the fact. Among the most directly actionable methodology refinements for our ranking.

### [47] Realistic Cost Modeling for Crypto Backtests (bid-ask spread, slippage, fills) — practitioner synthesis
- **Authors / Venue:** Practitioner/methodology synthesis (Binance Academy; Coinbase Help; Paybis; Blockchain-Council backtesting guides)
- **Year:** 2024–2025 (synthesis)
- **Source:** https://academy.binance.com/en/articles/bid-ask-spread-and-slippage-explained ; https://paybis.com/blog/how-to-backtest-crypto-bot/ ; https://www.blockchain-council.org/cryptocurrency/backtesting-ai-crypto-trading-strategies-avoiding-overfitting-lookahead-bias-data-leakage/
- **% read:** 45% (multiple full methodology articles; not a single primary paper — a practitioner-consensus synthesis)
- **Summary:** Pulls together the working numbers and rules for cost-realistic crypto backtesting, complementing the academic impact theory [13][27][41]. Bid-ask spreads for LIQUID majors (BTC/ETH on top venues) are tiny in calm markets (sub-bp to ~0.1–0.3 bp / a few bp) but BLOW OUT during volatility/stress (model spreads at 2–3× their historical average in volatile periods; banking-crisis spikes seen). Slippage depends on available liquidity and order size — practitioner guidance is to assume ~0.1%–1%+ on non-trivial orders, higher in volatile/thin conditions. Headline discipline: a crypto backtest is only credible if it includes (a) realistic taker FEES, (b) bid-ask SPREAD, (c) SLIPPAGE scaled to liquidity/volatility, (d) processing/order DELAY and conservative/partial FILL assumptions, and (e) strict out-of-sample windows. Ignoring any of these inflates net returns — the same phantom-alpha mechanism the academic literature [13][24] formalizes.
- **Relevance to our system:** The concrete, crypto-specific cost checklist for our engine and gate. For our €200 spot retail advisor, market IMPACT is negligible (tiny order vs depth, per [13][41]), so the dominant costs are FEES + SPREAD + (small) SLIPPAGE — exactly the regime where a fixed fee + spread model is defensible, but only if the spread/slippage parameters are REALISTIC and ideally REGIME-DEPENDENT (wider in high-vol). Two imports: (1) make our cost spec STATE-AWARE — a constant calm-market spread understates costs precisely in the volatile periods where churny strategies trade most and where their backtested edge is most fragile [22]; using a volatility-scaled spread (2–3× in stress) is a cheap realism upgrade that disproportionately penalizes the over-trading crowns we most distrust. (2) The "processing delay + conservative fills" point reinforces the one-bar-delay robustness check [33] and conservative intra-candle fill ordering [23]. Directly supports keeping a non-trivial, volatility-aware per-trade cost floor so no strategy can fake a win at unrealistically cheap friction.

### [48] Techniques for Verifying the Accuracy of Risk Measurement Models (the Kupiec POF / TUFF tests)
- **Authors / Venue:** Paul H. Kupiec / Journal of Derivatives 3(2), pp. 73–84 (also FEDS working paper)
- **Year:** 1995
- **Source:** DOI:10.3905/jod.1995.407942; method read via MathWorks `varbacktest.pof` docs + ResearchGate + the Federal Reserve "A Review of Backtesting and Backtesting Procedures" (FEDS 2005-21) which restates the test in full
- **% read:** 35% (abstract + full POF/TUFF likelihood-ratio mechanics + power critique from secondary restatements; original JoD paywalled)
- **Summary:** The foundational VaR backtest. A VaR model at coverage level p (e.g. 99%, so expected exception rate 1−p=1%) should be VIOLATED (realized loss exceeds the forecast VaR) only ~(1−p)·100% of the time. Kupiec's PROPORTION-OF-FAILURES (POF) test treats the violation sequence as i.i.d. Bernoulli and forms a likelihood-ratio statistic LR_POF = −2·ln[ ((1−p)^(N−x)·p^x) / ((1−x/N)^(N−x)·(x/N)^x) ], where N = total observations and x = observed violations; under the null (true rate = 1−p) LR_POF is asymptotically χ²(1). The TIME-UNTIL-FIRST-FAILURE (TUFF) test instead checks whether the first violation arrives too early. The paper's enduring caveat: both tests are UNCONDITIONAL (they check only the count/timing, not whether violations cluster) and have LOW POWER for the small samples (≈250 days) regulators use — distinguishing a correct 1% model from a wrong 2-3% model needs years of data, and TUFF is especially weak.
- **Relevance to our system:** This is the canonical way to validate a TAIL-RISK forecast, and it maps cleanly onto a possible upgrade to our gate. Our bootstrap produces a distribution of outcomes from which we can read percentile VaR / worst-case drawdown for the crowned strategy AND for buy-and-hold; the POF test is the principled check that those forecasts are CALIBRATED on the forward paper-trade (do realized losses breach the predicted 5%/1% band at the right rate?). The low-power lesson is doubly important for us: with only a short forward window, a "VaR looks fine" pass is weak evidence — exactly the MinBTL/MinTRL power discipline [3][4][32] restated for tail risk. POF is cheap (just count breaches) and gives the operator an honest "is the downside forecast trustworthy yet?" badge. Pairs with [49] (clustering) and [50] (ES) for a full tail-validation suite.

### [49] Evaluating Interval Forecasts (Christoffersen conditional-coverage / independence tests)
- **Authors / Venue:** Peter F. Christoffersen / International Economic Review 39(4), pp. 841–862
- **Year:** 1998
- **Source:** DOI:10.2307/2527341; JSTOR https://www.jstor.org/stable/2527341; method read via MathWorks `varbacktest.cc`/`cci` docs + the FEDS 2005-21 review restatement
- **% read:** 35% (abstract + full LR_uc / LR_ind / LR_cc Markov-chain construction from secondary restatements; original IER paywalled)
- **Summary:** Fixes the blind spot in Kupiec [48]: a VaR model can have the RIGHT NUMBER of violations but cluster them all in one volatile stretch (a fatal model failure POF can't see). Christoffersen frames a violation indicator I_t ∈ {0,1} and tests THREE nested nulls via likelihood ratios. (1) UNCONDITIONAL COVERAGE LR_uc — same idea as Kupiec's POF (right average rate). (2) INDEPENDENCE LR_ind — models I_t as a first-order MARKOV CHAIN with transition probabilities π_01 (violation after non-violation) and π_11 (violation after violation); the null is π_01 = π_11 (today's violation is independent of yesterday's), so clustering (π_11 > π_01) is detected. (3) CONDITIONAL COVERAGE LR_cc = LR_uc + LR_ind, asymptotically χ²(2), tests correct rate AND independence jointly. This made "are the exceptions both the right frequency and well-spread-out in time?" a single testable hypothesis and became the dominant interval-forecast evaluation method.
- **Relevance to our system:** The independence/clustering dimension is the part most relevant to crypto and our gate. Crypto's volatility CLUSTERS hard, so a downside-risk forecast that passes on average can still fail catastrophically by bunching all its breaches in one crash — precisely the scenario our forward paper-trade most fears and the reason our bootstrap uses BLOCKS (to preserve that clustering [10][20]). If we add a tail-calibration check to the forward run, it must be the CONDITIONAL-coverage version (LR_cc), not just a breach count, or we will bless a model that is correct-on-average but clustered-in-crisis. The Markov-chain independence test is also a clean template for a leakage/serial-dependence diagnostic on our own bootstrap residuals. Together with [48] it is the standard, cheap, well-understood toolkit for validating any risk band we surface.

### [50] Back-Testing Expected Shortfall (Acerbi–Székely three tests)
- **Authors / Venue:** Carlo Acerbi, Balázs Székely / Risk Magazine 27 (Nov), pp. 76–81
- **Year:** 2014
- **Source:** Risk Nov-2014; method read in full via the Zeliade Systems whitepaper "Backtesting Expected Shortfall" (zwp-011) + Acerbi–Székely follow-ups
- **% read:** 40% (abstract + all three test definitions + elicitability discussion via the Zeliade whitepaper; original Risk article paywalled)
- **Summary:** Tackles the regulator-driven shift from VaR to EXPECTED SHORTFALL (ES = average loss GIVEN a VaR breach; Basel FRTB moved the capital measure to 97.5% ES), against the theoretical objection that ES is NOT ELICITABLE (Gneiting 2011 — there is no scoring function it uniquely minimizes, unlike VaR/quantiles), which was widely (mis)read as "ES cannot be backtested." Acerbi–Székely show it CAN, proposing three non-parametric tests. TEST 1 (conditional, "tail-severity"): given the days that breached VaR, check whether the average breach magnitude matches the predicted ES — tests the tail shape conditional on VaR being right. TEST 2 (unconditional): a single statistic Z = (1/N)·Σ [X_t·I_t / (p·ES_t)] + 1 that tests both frequency and severity jointly without first validating VaR. TEST 3 (quantile/rank-based): estimates ES from the realized ranks of observed losses in the forecast distribution, the most powerful and fully distribution-checking variant. All three are simulated under the null (MC the predictive distribution) for critical values; the headline message is that elicitability is NOT required for backtesting — ES is "backtestable" / jointly identifiable with VaR.
- **Relevance to our system:** Directly upgrades how we'd report DOWNSIDE risk. VaR/POF [48] only counts whether the loss band is breached; ES asks the more useful question "WHEN it goes wrong, HOW BAD on average?" — which is exactly what a retail operator deciding between a crowned strategy and buy-and-hold cares about (tail severity, not just tail frequency). Our bootstrap already yields a full loss distribution per strategy, so ES at 95%/97.5% and an Acerbi–Székely Test-2 calibration check on the forward run are cheap additions that make the risk side of the comparison honest. The elicitability nuance is a useful caution for any RANKING/scoring metric we adopt: scoring/ranking strategies by a non-elicitable measure can be inconsistent, so prefer elicitable scores (mean return, quantile/VaR) for the LEAGUE TABLE and use ES for reporting/validation. Strengthens the "compare the whole downside distribution, not one number" discipline [44][43].

### [51] Testing Density Forecasts, With Applications to Risk Management (Berkowitz PIT / tail test)
- **Authors / Venue:** Jeremy Berkowitz / Journal of Business & Economic Statistics 19(4), pp. 465–474
- **Year:** 2001
- **Source:** DOI:10.1198/07350010152596718; method read via the rugarch `BerkowitzTest` docs + risk.net technical-brief + UCR/Lee density-comparison lecture notes
- **% read:** 30% (abstract + full PIT / inverse-normal / LR-test + censored-tail-test mechanics from secondary restatements; original JBES paywalled)
- **Summary:** Generalizes VaR backtesting from a single quantile to the WHOLE predictive DENSITY. Core machinery: the PROBABILITY INTEGRAL TRANSFORM (PIT) — if a model's forecast density is correct, then plugging each realized return into that density's CDF, p_t = F_t(x_t), yields i.i.d. Uniform(0,1) values. Berkowitz's twist: apply the inverse standard-normal Φ⁻¹ to get z_t = Φ⁻¹(p_t), which under the null are i.i.d. N(0,1) — letting you use POWERFUL Gaussian likelihood-ratio tests for mean=0, variance=1, and zero autocorrelation (an AR(1) on z_t) instead of weak uniformity tests. For risk management specifically he adds a TAIL TEST: a CENSORED-normal likelihood that only evaluates z_t below the VaR threshold (censoring the bulk), so the test focuses power exactly on the tail region that VaR/ES care about. Result: a unified, higher-power framework that subsumes coverage and independence testing and checks the tail shape, not just the breach count.
- **Relevance to our system:** The most complete tail-validation tool of the set [48][49][50][51] and a clean fit for our bootstrap, which produces a full predictive DISTRIBUTION (not just a quantile) per strategy and for buy-and-hold. The PIT idea gives us a single, principled calibration check for the forward paper-trade: transform each realized forward return through the strategy's bootstrap-implied CDF and test that the PITs are uniform/independent — if they aren't, the model's risk picture is miscalibrated and any "beats hold on risk-adjusted terms" claim is suspect. The censored TAIL test is the right tool when we only care about downside calibration (the operator's loss). It also doubles as a leakage/serial-dependence diagnostic (the AR(1)-on-z_t independence test). Higher-power than a bare breach count, and the Gaussian-LR reframing is cheap to implement. Together [48][49][50][51] form a complete "is our downside forecast honest?" suite.

### [52] Multinomial VaR Backtests: A Simple Implicit Approach to Backtesting Expected Shortfall
- **Authors / Venue:** Marie Kratz, Yen H. Lok, Alexander J. McNeil / Journal of Banking & Finance 88, pp. 393–407
- **Year:** 2018 (arXiv 2016)
- **Source:** arXiv:1611.04851 (https://arxiv.org/abs/1611.04851); DOI:10.1016/j.jbankfin.2018.01.002; read via ar5iv full text
- **% read:** 45% (abstract + multinomial-vs-binomial construction + power results + traffic-light recommendation via ar5iv HTML; skipped some asymptotic detail)
- **Summary:** A pragmatic answer to "how do you backtest ES given the elicitability worry [50]?": don't test ES directly — test VaR EXCEPTIONS AT SEVERAL probability levels SIMULTANEOUSLY. Because ES is (approximately) the average of VaR quantiles above the level, validating the model's exception rates across N levels (e.g. 4–8 quantiles spanning the tail) IMPLICITLY validates ES. The violation counts across levels follow a MULTINOMIAL distribution under the null; they build Pearson-χ², Nass, and likelihood-ratio multinomial tests. Key empirical finding: a multinomial test with N≥4 levels has SUBSTANTIALLY MORE POWER than the single-level binomial/Kupiec [48] test at detecting misspecified tails — especially models that underestimate heavy tails / kurtosis (the dangerous direction for capital). They propose extending the Basel traffic-light from one level to the multinomial setting and illustrate on three datasets spanning the 2008 crisis.
- **Relevance to our system:** A cheap, higher-power upgrade path for validating the downside our bootstrap forecasts, and philosophically aligned with our "look at the whole distribution" ethos [44]. Instead of checking only one VaR band on the forward run, check breaches at several tail quantiles at once via a multinomial test — far more sensitive to the heavy-tail underestimation that is THE failure mode for crypto risk forecasts. It sidesteps the ES-elicitability debate entirely (test multiple VaRs, get ES validation for free), which makes it more robust to implement than Acerbi–Székely Test 2/3 [50]. The N≥4 power result tells us how many quantile levels to monitor. Strong candidate if we add a calibration panel: "does the crown's (and hold's) predicted tail hold up out-of-sample, across the whole tail, not just at 95%?"

### [53] A Simple Traffic Light Approach to Backtesting Expected Shortfall
- **Authors / Venue:** Nick Costanzino, Michael Curran / Risks (MDPI) 6(1):2; building on Costanzino & Curran (2015)
- **Year:** 2018
- **Source:** DOI:10.3390/risks6010002; open access https://www.mdpi.com/2227-9091/6/1/2; abstract+framework via repec/MDPI
- **% read:** 30% (abstract + framework + traffic-light mapping via repec listing; full MDPI text only skimmed)
- **Summary:** Ports the Basel Committee's familiar GREEN/YELLOW/RED traffic-light supervisory zoning — originally defined for VaR exception counts (Basel 1996: 0–4 exceptions/250 days = green, 5–9 = yellow with rising capital multiplier, 10+ = red) — onto EXPECTED SHORTFALL, "completely consistent with, and analogous to" the VaR traffic light. The engine is the authors' earlier GENERALIZED COVERAGE TEST for ES (Costanzino–Curran 2015), which extends the unconditional-coverage idea to a spectral/ES setting; the new paper maps that statistic's distribution onto three supervisory zones so a regulator (or operator) gets the same intuitive green/amber/red verdict for an ES model that the Basel framework gives for VaR. Practical, regulator-facing, deliberately simple.
- **Relevance to our system:** Less a new test than a REPORTING/UX pattern we can borrow directly: collapse a tail-calibration statistic into a green/yellow/red badge. Our app's whole value is making a robustness verdict legible to a retail operator; a "downside-forecast traffic light" (is the crown's predicted ES holding up on the forward run? green/amber/red) is exactly the kind of honest, glanceable summary we want, and the Basel zoning gives defensible, well-known thresholds rather than ad-hoc ones. It also reinforces that ES — not just VaR — is the regulator-blessed tail measure to surface [50][52]. Background-to-actionable: the test math we'd take from [50]/[52], but the traffic-light presentation idea is the import.

### [54] Backtesting Value-at-Risk: A Duration-Based Approach
- **Authors / Venue:** Peter F. Christoffersen, Denis Pelletier / Journal of Financial Econometrics 2(1), pp. 84–108
- **Year:** 2004
- **Source:** SSRN:418762 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=821715); DOI:10.1093/jjfinec/nbh004; CIRANO WP 2003s-05; method via repec + abstract
- **% read:** 30% (abstract + duration/Weibull/i.i.d.-null construction + Dufour Monte-Carlo power result via repec restatement; primary paywalled)
- **Summary:** Attacks the LOW POWER of count-based VaR backtests [48][49] in the small (≈250–500 day) samples regulators and practitioners actually have. Insight: instead of looking at the violation INDICATOR series, look at the DURATIONS (number of days) BETWEEN consecutive violations. Under a correctly-specified VaR at coverage p, violations are a Bernoulli process so the between-violation durations are i.i.d. GEOMETRIC (continuous analogue: EXPONENTIAL) with mean 1/p — crucially MEMORYLESS (no duration dependence). They nest this in a WEIBULL distribution whose shape parameter b=1 recovers the memoryless exponential; b≠1 means duration dependence (clustering ⇒ b<1, hazard decreasing in time-since-last-violation). The LR test of b=1 detects clustering that count tests miss, and tests both conditional and unconditional coverage against general alternatives. Because asymptotics are poor in small samples, they use Dufour's MONTE-CARLO test to control size exactly. Result: duration-based tests have CONSIDERABLY better finite-sample power than Kupiec/Christoffersen LR tests.
- **Relevance to our system:** The power-upgrade member of the tail-validation suite [48–54], and the small-sample angle is exactly our situation — a forward paper-trade is short, so the weak count tests will rarely reject even a bad downside model. The duration/Weibull framing tests the SAME crypto-critical property as Christoffersen's independence test [49] (do breaches cluster?) but with more power, and the Dufour Monte-Carlo-test trick to get exact size in small samples is directly reusable for ANY of our bootstrap-based statistics where asymptotic χ² is untrustworthy on short windows. If we add a tail-calibration check, the duration-based version is the one most likely to actually catch a miscalibrated model on a realistic forward horizon. Reinforces the recurring "short window ⇒ low power ⇒ a clean pass is weak evidence" caution [3][32][48].

### [55] Optimal Trading Strategy and Supply/Demand Dynamics (transient / resilient market impact)
- **Authors / Venue:** Anna A. Obizhaeva, Jiang Wang / Journal of Financial Markets 16(1), pp. 1–32; NBER WP 11444
- **Year:** 2013 (circulated 2005)
- **Source:** NBER w11444 (https://www.nber.org/papers/w11444); SSRN:686168; PDF https://web.mit.edu/wangj/www/pap/ObizhaevaWang13.pdf
- **% read:** 35% (abstract + LOB-resilience impact model + optimal-execution-shape result + Almgren-Chriss contrast from full-text PDF; skipped HJB derivation)
- **Summary:** Generalizes Almgren–Chriss [27] by making temporary impact TRANSIENT rather than instantaneous. They model the limit order book explicitly: a trade walks the book and pushes the price away from its steady state, but the book then REPLENISHES at a finite RESILIENCE rate ρ, so the impact of a past trade DECAYS over time (it is neither purely temporary — gone next instant — nor permanent). This intertemporal supply/demand picture changes the optimal execution schedule qualitatively: instead of Almgren–Chriss's smooth, near-linear participation, the optimum is DISCRETE block trades at the START and END of the window plus continuous trading in between — the first block pushes the book to attract fresh liquidity, the interior picks off incoming orders, the final block clears the remainder. Almgren–Chriss emerges as a special case (instantaneous resilience). The key parameter is resilience (how fast depth recovers), which governs how much execution cost depends on TIMING/SPACING of child orders, not just total size.
- **Relevance to our system:** The most realistic of the cost-model references for ANY future "what does it cost to actually trade this" feature, and a useful sharpening of why our retail assumption is safe. (1) For a €200 spot order the participation rate is ≈0 and the book replenishes essentially instantly at that scale, so transient-impact subtleties are negligible — a fixed fee+spread is fine, NOW with a third independent paper [13][41][55] confirming impact only bites at size. (2) The resilience concept is the right mental model if we ever simulate a strategy that trades large or fast: cost depends on order SPACING relative to book recovery, so a high-frequency churny strategy in a thin coin would pay far more than a naive constant-spread backtest charges — exactly the phantom-alpha direction our gate must guard against. (3) It cements that "impact decays" — so charging a permanent impact per trade would OVER-state cost, while charging zero impact UNDER-states it; the honest middle for at-size sims is a transient kernel. Mostly background for our current scale, but the canonical model to reach for if capacity/size ever enters scope.

### [56] How Markets Slowly Digest Changes in Supply and Demand (the propagator / transient-impact model)
- **Authors / Venue:** Jean-Philippe Bouchaud, J. Doyne Farmer, Fabrizio Lillo / in "Handbook of Financial Markets: Dynamics and Evolution"; arXiv:0809.0822 (lineage: Bouchaud et al. 2004, QF)
- **Year:** 2008 (handbook chapter); core propagator model Bouchaud et al. 2004
- **Source:** arXiv:0809.0822 (https://arxiv.org/abs/0809.0822); core: Bouchaud, Gefen, Potters, Wyart (2004) Quantitative Finance 4, 176–190; mechanics via search restatements
- **% read:** 30% (abstract + propagator/decay-kernel formulation + square-root-law reconciliation via secondary restatements; full handbook chapter skimmed)
- **Summary:** Resolves a deep puzzle: order flow is strongly AUTOCORRELATED (persistent — big buyers split orders over days), yet prices are nearly DIFFUSIVE (uncorrelated returns). If each trade had a fixed permanent impact, persistent order flow would make prices trend, contradicting near-efficiency. The PROPAGATOR MODEL fixes this: observed price = convolution of past signed order flow with a DECAYING impact kernel G(ℓ) (a "propagator"). Impact is TRANSIENT — strongest immediately after a trade, then decaying as a power law G(ℓ)∼ℓ^(−β). The decay exponent is precisely tuned to order-flow persistence: if the order-sign autocorrelation decays as ℓ^(−γ) with γ<1, then β=(1−γ)/2 makes the two effects cancel to yield diffusive prices (a true permanent component is only consistent if γ>1). Non-linear instantaneous impact + this decay kernel reproduces the empirical square-root impact law [13][41]. This made transient/history-dependent impact (vs Almgren-Chriss's permanent+temporary split) the microstructure-standard description.
- **Relevance to our system:** Background-to-conceptual for us, deepening [13][41][55]: it's the microstructure mechanism behind WHY impact decays and why the √-law holds, unifying the cost-model papers. The actionable kernel for us is small: at €200 retail scale, propagator subtleties vanish (we're not moving the book). But two durable takeaways: (1) the order-flow-persistence ↔ price-diffusivity tension is a reminder that apparent "momentum" in price can be the mechanical footprint of split institutional orders, not a tradable signal — relevant skepticism for any momentum strategy our bake-off crowns. (2) It reinforces (with [41]) that impact is fundamentally a DYNAMIC, decaying quantity, so any at-size cost simulation we ever build should use a decay kernel, not a static per-trade charge. Confirms our fixed-cost retail model is a defensible small-order limit of the correct dynamic theory.

### [57] Does Academic Research Destroy Stock Return Predictability?
- **Authors / Venue:** R. David McLean, Jeffrey Pontiff / Journal of Finance 71(1), pp. 5–32
- **Year:** 2016
- **Source:** SSRN:2156623 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2156623); DOI:10.1111/jofi.12365; figures via search restatements
- **% read:** 35% (abstract + the three decay numbers + the data-mining-vs-arbitrage decomposition + cross-sectional drivers via secondary restatements; primary paywalled)
- **Summary:** The landmark LIVE-vs-BACKTEST decay study. They take 97 published cross-sectional return predictors and measure each one's portfolio return in three regimes: the original IN-SAMPLE period, the OUT-OF-SAMPLE period AFTER the original sample but BEFORE publication, and the POST-PUBLICATION period. Findings: returns are 26% lower out-of-sample (in-sample → post-sample/pre-publication) and 58% lower POST-publication (vs in-sample). They interpret the 26% OOS drop as an UPPER BOUND on the data-mining/statistical-bias component (the edge that was never real), and the additional 32% (58−26) as the effect of PUBLICATION-INFORMED ARBITRAGE — sophisticated investors read the papers and trade the anomaly away. Corroborating evidence: post-publication decay is LARGER for predictors with higher in-sample returns (more attractive to arbitrageurs) and correlated stocks show increased trading/correlation after publication. So roughly HALF of typical anomaly alpha is real-but-arbitraged and half (or less) is data-mining illusion.
- **Relevance to our system:** A quantified, authoritative justification for two of our core design choices. (1) The 26% pure-OOS haircut is independent empirical evidence that backtest edges are systematically OVERSTATED even before any arbitrage — exactly why our gate must be skeptical and why we never crown on in-sample score alone [1][2][45]. (2) The publication-arbitrage 32% is why a STRATEGY THAT WAS REAL can still die: any well-known technical rule (SMA crossover, RSI bands — all textbook) has been public for decades, so its expected live edge should be discounted HEAVILY toward zero — strong support for our validated "nothing robustly beats holding" thesis specifically for the well-known indicators our bake-off uses. Concretely: we could apply an explicit "well-known signal" decay prior — published/standard indicators get an extra haircut vs a hypothetically-novel one — and we should EXPECT our forward paper-trade to show ≥26% degradation vs the in-sample crown as the NORMAL case, not a bug. The cross-sectional driver (high-IS-return decays most) also warns that the more spectacular a crowned backtest, the more its forward return should be discounted.

### [58] Why and How Systematic Strategies Decay
- **Authors / Venue:** Antoine Falck, Adam Rej, David Thesmar / arXiv (q-fin.PM); CFM / MIT
- **Year:** 2021
- **Source:** arXiv:2105.01380 (https://arxiv.org/abs/2105.01380)
- **% read:** 35% (abstract + the decay-driver decomposition + overfitting-vs-arbitrage conclusion via HTML abstract + search restatement; full PDF not text-extractable)
- **Summary:** Asks WHICH characteristics of a published anomaly PREDICT how fast its risk-adjusted (Sharpe) performance will decay out-of-sample, to adjudicate between the two competing decay theories: (a) ARBITRAGE — real edges erode as capital crowds in (McLean–Pontiff [57]'s publication channel); vs (b) OVERFITTING — the in-sample Sharpe was inflated by multiple testing and was never fully real [1][3]. Headline result: the YEAR OF PUBLICATION alone explains ~30% of the variance in Sharpe decay across factors — every later year, newly-published factors decay ~5 percentage points MORE — consistent with an increasingly data-mined literature. OVERFITTING proxies (signal-construction COMPLEXITY and sensitivity to OUTLIERS) add ~15% explanatory power. ARBITRAGE-capital proxies are statistically significant but contribute little. Conclusion: OVERFITTING-related features DOMINATE arbitrage in explaining backtest-to-live degradation — i.e. most decay is the edge never having been as real as the backtest claimed, not competition eroding a genuine edge.
- **Relevance to our system:** Directly actionable and strongly aligned with our thesis. The two practical signals it identifies as PREDICTING decay — signal-construction COMPLEXITY and OUTLIER-SENSITIVITY — are knobs we can literally measure on a crowned strategy and use to DOWN-RANK fragile winners: a strategy whose edge needs many tuned parameters (complex) or whose backtest return hangs on a few extreme days (outlier-sensitive, our drop-top-k-days stress test [17]) should be expected to decay most and trusted least. That overfitting beats arbitrage as the decay driver reinforces that our gate's anti-overfitting machinery [1][2][20][46] targets the RIGHT failure mode — it's not mainly that edges get competed away, it's that they were overfit. Concretely: add a "complexity penalty" (fewer parameters preferred [18]) and an outlier-sensitivity metric to the ranking, and frame the expected forward decay as predictable from these. The complement to [57]: McLean–Pontiff quantifies the decay, Falck–Rej–Thesmar tells us which strategies will suffer it most.

### [59] A Real-Time Data Set for Macroeconomists: Does Data Vintage Matter for Forecasting?
- **Authors / Venue:** Dean Croushore, Tom Stark / Journal of Econometrics 105(1), pp. 111–130 (Fed Phila real-time dataset); related JBES forecast-evaluation work
- **Year:** 2001 (dataset) / 2003 (vintage-matters forecasting study)
- **Source:** SSRN:244554 (https://papers.ssrn.com/sol3/papers.cfm?abstract_id=244554); Fed Phila Working Paper 99-15; PDF https://bfi.uchicago.edu/wp-content/uploads/1.pdf
- **% read:** 35% (abstract + vintage/PIT definition + revision-significance example + forecast-evaluation recommendation from full-text PDF; skipped per-series revision tables)
- **Summary:** The foundational POINT-IN-TIME (PIT) methodology paper. A data "VINTAGE" is the complete snapshot of a time series as it existed on a particular date; because macro series are REVISED repeatedly (new source data, benchmark/conceptual changes), the value you see for, say, Q1-1974 GDP differs dramatically depending on whether you look in 1974, 1975, or today. Croushore–Stark build a database of quarterly vintages and show the revisions are economically LARGE — sometimes flipping the sign or magnitude of growth rates — so a forecaster facing PRELIMINARY data saw a genuinely different signal than one using FINAL data. The core methodological result (Stark–Croushore): evaluating a model on FINAL/revised data OVERSTATES its real-time skill, because the model implicitly "knows" the revised truth. The mandate: out-of-sample forecast comparisons (new model vs benchmark) MUST use real-time vintages — the data actually available at each forecast date — not the latest revised series.
- **Relevance to our system:** This is the METHODOLOGY backbone for honest point-in-time backtesting (the `data` topic owns the crypto data plumbing; we own the EVALUATION discipline). The principle transfers exactly even though raw crypto OHLCV is barely revised: ANY input to our signals that gets restated or arrives with a lag must be used AS-OF its real availability, or the backtest leaks. Crypto-specific vintage hazards to audit: exchange data CORRECTIONS/gap-fills after the fact, on-chain metrics and "circulating supply" that get re-estimated, index/price aggregator methodology changes, and survivorship in the coin universe [14]. The deeper, durable lesson is the "evaluate on what was knowable then" rule — the same rule behind our one-bar-delay check [33], no full-sample normalization [15], and no parameters chosen on the full series. Concretely: if we ever add a fundamental/on-chain feature, store its vintage (value + as-of timestamp) so the backtest reads the value KNOWN at the bar, not today's revised value. Canonical citation for the PIT requirement.

### [60] Point-in-Time Data Discipline for Bias-Free Backtesting — practitioner synthesis
- **Authors / Venue:** Practitioner/curriculum synthesis (CFA Level-II "Problems in Backtesting"; Refinitiv/LSEG and Eagle Alpha point-in-time guides; portfoliooptimizationbook §8.2)
- **Year:** 2023–2024 (synthesis)
- **Source:** https://analystprep.com/study-notes/cfa-level-2/problems-in-backtesting/ ; https://www.eaglealpha.com/2024/05/06/point-in-time-alternative-data/ ; https://portfoliooptimizationbook.com/book/8.2-seven-sins.html
- **% read:** 40% (multiple full methodology articles; a practitioner-consensus synthesis, not one primary paper)
- **Summary:** Operationalizes the Croushore–Stark [59] PIT principle for SECURITY backtesting and enumerates the concrete look-ahead traps a naive backtest hits. Three manifestations of look-ahead from non-PIT data: (1) REPORTING LAG — using a fundamental on the period-END date when it was only ANNOUNCED weeks/months later (e.g. acting on Q1 EPS on Mar-31 when it's reported in April/May); fix = lag every fundamental to its announcement/availability date. (2) DATA REVISIONS/RESTATEMENTS — vendors overwrite originally-reported figures; a true PIT database RETAINS the original value plus a separate restated-value field with its restatement date, so the backtest reads the originally-known number. (3) INDEX ADDITIONS / RETROACTIVE BACKFILL — when a name is added to a universe, vendors back-populate its history, injecting both look-ahead and survivorship [14]; fix = PIT index membership (who was in the index ON that date). Recommended mitigations across sources: source PIT data, lag to availability, retain vintages with as-of timestamps, and pair with higher statistical hurdles / cross-validation for the snooping side.
- **Relevance to our system:** The concrete checklist version of [59] for our pipeline, and a clean delineation of where look-ahead can sneak in beyond the price series itself. For our current spot-price-only advisor the reporting-lag and restatement traps are largely dormant (OHLCV isn't "announced late"), but THREE items apply right now: (a) any AGGREGATE/derived input (a coin "market cap," a volatility index, an aggregator's price) can be revised — store its vintage; (b) UNIVERSE/listing membership is point-in-time — a coin's listing date and any delisting must gate when it's tradable in a backtest [14]; (c) the as-of-timestamp discipline is the generalizable rule we should bake into the data layer's contract so future features (on-chain, funding, sentiment) inherit it for free. It also re-states the seven-sins look-ahead audit [15][17] in checklist form. Cheap, high-value discipline: make "what was knowable at bar t" an explicit, tested invariant of the engine rather than an assumption.

### [61] The Model Confidence Set
- **Authors / Venue:** Peter R. Hansen, Asger Lunde, James M. Nason / Econometrica 79(2), pp. 453–497
- **Year:** 2011
- **Source:** DOI:10.3982/ECTA5771; SSRN/Aarhus pure; method via scirp + repec + ECTA listing + rubetron/MCS reference implementation
- **% read:** 35% (abstract + MCS equivalence-test/elimination procedure + bootstrap + CI-analogy + applications via secondary restatements + reference-code README; primary paywalled)
- **Summary:** Solves "which of these models are statistically indistinguishable from the best?" The MODEL CONFIDENCE SET (MCS) is a data-determined SUBSET of models that contains the best one(s) with a chosen confidence level (e.g. 90%) — directly analogous to a confidence INTERVAL, but over models. Procedure: start with the full set; run an EQUIVALENCE test of "all remaining models have equal predictive ability"; if rejected, an ELIMINATION rule drops the single worst model; repeat until the equivalence test no longer rejects — the survivors are the MCS. Critically it uses the BOOTSTRAP to capture the JOINT distribution of the pairwise loss differentials (so correlated models are handled correctly), and it does NOT assume any model is "true." Key property: the MCS adapts to information content — UNINFORMATIVE data leave MANY models in the set (you can't tell them apart), INFORMATIVE data shrink it to a few. Generalizes White's RC [6]/SPA [7] from "is the best better than a benchmark?" to "which models are in the top tier?"
- **Relevance to our system:** The most principled way to convert our bake-off from "crown a single winner" to "report the SET of strategies that are statistically indistinguishable from the best — and from buy-and-hold." This is exactly the right framing for our thesis: if buy-and-hold is IN the MCS alongside the crowned strategy, then the strategy does NOT robustly beat holding (they're statistically tied), which is the honest verdict our gate should surface. It reuses our existing bootstrap machinery (joint resampling of per-strategy return differentials) and handles config correlation correctly, unlike Bonferroni [5]. The "uninformative data → large set" property is a built-in honesty mechanism: on a short/noisy window the MCS will correctly be large (we can't distinguish strategies), which is the truthful answer rather than a falsely-confident crown. Complements StepM [8] (which finds all models beating a benchmark) — MCS finds the top-tier EQUIVALENCE class. Strong candidate for the ranking output: "MCS at 90% = {buy-and-hold, SMA-50/200, ...}" tells the operator far more than a single crowned name.

### [62] On Over-fitting in Model Selection and Subsequent Selection Bias in Performance Evaluation
- **Authors / Venue:** Gavin C. Cawley, Nicola L. C. Talbot / Journal of Machine Learning Research 11, pp. 2079–2107
- **Year:** 2010
- **Source:** JMLR open access https://www.jmlr.org/papers/v11/cawley10a.html (PDF https://www.jmlr.org/papers/volume11/cawley10a/cawley10a.pdf)
- **% read:** 40% (abstract + model-selection-overfitting argument + nested-CV construction + variance-of-criterion point via JMLR HTML; skipped per-dataset experiment tables)
- **Summary:** The canonical ML statement of a bias our bake-off is exposed to: OVER-FITTING IN MODEL SELECTION (distinct from over-fitting in training). When you tune hyperparameters by optimizing a cross-validation score and then REPORT that same CV score as the model's performance, the estimate is OPTIMISTICALLY BIASED — you selected the configuration that best fit the random fluctuations of those particular folds, so its CV score reflects luck, not just skill. The degradation can be "surprisingly large," rivaling the true differences between learning algorithms. The fix is NESTED (double) cross-validation: an INNER CV loop selects hyperparameters, an OUTER CV loop — which NEVER participates in selection — gives an unbiased estimate of the whole select-then-fit procedure. Second key insight: a selection criterion's VARIANCE matters as much as its bias — a low-bias but high-variance CV score gives the search more noise to exploit, worsening selection over-fitting; sometimes a more regularized/lower-variance criterion selects better even if slightly biased.
- **Relevance to our system:** Names precisely the trap in crowning a bake-off winner by its in-sample/CV score: that score is selection-biased upward and must NOT be reported as the strategy's expected performance — the same lesson as DSR/PBO [1][2] from the ML side. The NESTED-CV prescription maps to our architecture: the bake-off sweep is the INNER selection loop; the OUTER, never-used-for-selection evaluation is exactly what our forward paper-trade (and an outer CPCV fold [9]) should be — so we must report the crown's performance from the OUTER/forward leg, never from the sweep score that picked it. The variance point is a subtle, actionable warning: selecting strategies by a high-variance metric (e.g. raw Sharpe on a short window) invites selection over-fitting; a lower-variance/regularized selection criterion (e.g. a bootstrap-percentile or DSR-adjusted score) will pick more robust crowns. Reinforces that our weakest-link bootstrap percentile is a SOUND selection criterion precisely because it's lower-variance than a point Sharpe. Bridges the finance [1][2] and ML literatures on the identical bias.

### [63] Comparing Predictive Accuracy (the Diebold–Mariano test)
- **Authors / Venue:** Francis X. Diebold, Roberto S. Mariano / Journal of Business & Economic Statistics 13(3), pp. 253–263; + Diebold "20 Years Later" JBES 2015 retrospective
- **Year:** 1995 (orig. circulated 1991); retrospective 2015
- **Source:** DOI:10.1080/07350015.1995.10524599; retrospective https://public.econ.duke.edu/~ap172/Patton_Diebold_JBES_discussion_21apr14.pdf; method via dm.test docs + LEM lecture notes
- **% read:** 40% (abstract + DM statistic + HAC variance + the forecasts-vs-models misuse caveat from the retrospective PDF + secondary restatements; original JBES paywalled)
- **Summary:** The standard test for "do two forecasts have equal accuracy?" Form the LOSS DIFFERENTIAL d_t = L(e₁,t) − L(e₂,t) between two forecasts' errors under ANY loss L (need not be quadratic, symmetric, or Gaussian-error), and test H₀: E[d_t]=0. The DM statistic is the sample-mean differential divided by a HAC (heteroskedasticity-and-autocorrelation-consistent) standard error — crucial because loss differentials are serially correlated for multi-step forecasts. Asymptotically N(0,1). The 2015 retrospective draws a sharp, often-ignored line: the DM test is valid for comparing TWO GIVEN FORECASTS (the canonical, correct use), but is widely MISUSED to compare MODELS in pseudo-out-of-sample exercises where parameters were estimated on overlapping data — there West (1996) and Clark–McCracken corrections to the critical values are needed because estimation error contaminates the comparison, and naive DM can favor overfit specifications.
- **Relevance to our system:** The arbitrary-loss, HAC-robust pairwise comparison test underlying the whole "is A better than B?" family our gate lives in. Two imports. (1) When we compare a crowned strategy's per-bar P&L (or forecast errors) to buy-and-hold's, a DM-style test with a HAC SE on the serially-correlated differential is the textbook way to attach significance — and our block bootstrap is the resampling analogue of the HAC correction, so we're already in the right family [10][35]. (2) The forecasts-vs-models caveat is a direct warning for us: our crowned strategy was SELECTED on the same window we'd test it on, so a naive significance test (DM or otherwise) on the in-sample period is contaminated and will favor overfit crowns — significance must come from the OUTER/forward leg or a properly purged resample [9][62], with the multiple-selection penalty [1][2] on top. Also lets us use an ASYMMETRIC loss (downside misses hurt the retail operator more than upside) rather than only squared error — a more honest objective.

### [64] Bootstrapping the Out-of-Sample Predictions for Efficient and Accurate Cross-Validation (BBC-CV)
- **Authors / Venue:** Ioannis Tsamardinos, Elissavet Greasidou, Giorgos Borboudakis / Machine Learning 107(12), pp. 1895–1922
- **Year:** 2018
- **Source:** DOI:10.1007/s10994-018-5714-4; open via PMC6191021 (https://pmc.ncbi.nlm.nih.gov/articles/PMC6191021/)
- **% read:** 45% (abstract + BBC-CV pooled-prediction bootstrap procedure + nested-CV cost comparison + BBCD early-dropping via PMC full text; skipped some experiment tables)
- **Summary:** A cheaper, near-unbiased alternative to nested CV [62] for the "select-best-config-AND-report-honest-performance" problem. After running ONE pass of K-fold CV over all C configurations, you have a matrix Π of every configuration's OUT-OF-SAMPLE predictions (each instance was held out exactly once). BBC-CV then BOOTSTRAPS the rows (instances) of Π: in each bootstrap sample, RE-RUN the selection rule (pick the config with best score on the in-bag instances) and measure that winner's performance on the OUT-OF-BAG instances; averaging over ~1000 bootstraps gives an almost-unbiased, slightly CONSERVATIVE estimate of the selected pipeline's true performance — capturing the "optimism of choosing the winner" without ANY model retraining (unlike nested CV's O(K²·C) cost). A variant (BBCD-CV) additionally DROPS clearly-inferior configs early for 2–5× speedups. Validated as comparable in accuracy to nested CV at a fraction of the cost.
- **Relevance to our system:** Possibly the single most directly portable estimator in this batch — it is nested-CV's honesty at bootstrap cost, and we ALREADY HAVE the required input (each config's return/equity series across the bake-off is our analogue of the OOS prediction matrix Π). Concrete plan: treat per-config bar-returns as Π, bootstrap the bars (with BLOCKS to preserve crypto autocorrelation [10][20]), in each resample re-select the bake-off winner by our ranking criterion, and record that winner's out-of-bag performance vs buy-and-hold; the distribution's center is a SELECTION-BIAS-CORRECTED estimate of "what the crown will really do," and its spread is the uncertainty. This operationalizes the DSR/PBO intent [1][2] using machinery we already run, gives the honest (deflated) expected performance directly rather than via a closed-form approximation, and the "re-select in each bootstrap" step is exactly what captures our multiple-testing optimism. Strong candidate to BE our deflation mechanism. Pairs with the m_eff/clustering idea [46] (which feeds closed-form DSR) as the resampling alternative.

### [65] On Market Timing and Investment Performance II: Statistical Procedures for Evaluating Forecasting Skills (Henriksson–Merton)
- **Authors / Venue:** Roy D. Henriksson, Robert C. Merton / Journal of Business 54(4), pp. 513–533
- **Year:** 1981
- **Source:** DOI:10.1086/296144; method via scirp + repec + the "2×2 contingency-table / exact test of independence" restatement
- **% read:** 35% (abstract + parametric down-market-dummy regression + non-parametric 2×2 contingency-table test + p1+p2>1 skill condition via secondary restatements; primary paywalled)
- **Summary:** The canonical test for MARKET-TIMING skill — does a strategy correctly call the market's direction more than chance? Model: a timer forecasts whether the market will beat the risk-free rate and shifts beta accordingly, which makes the portfolio return a NON-LINEAR (option-like, kinked-at-r_f) function of the market return. PARAMETRIC test: regress portfolio excess return on market excess return PLUS a term max(0, −(R_m−r_f)) (a "down-market" protective-put dummy); a significant positive coefficient on that term = genuine timing ability (the manager systematically reduces exposure in down markets). NON-PARAMETRIC test: build a 2×2 contingency table of FORECAST (up/down) vs OUTCOME (up/down); let p1 = P(forecast up | market up) and p2 = P(forecast down | market down); under the null of NO timing ability p1 + p2 = 1 (forecasts independent of outcomes), and genuine skill requires p1 + p2 > 1. This is exactly an exact (Fisher-style) test of independence in the 2×2 table with fixed margins.
- **Relevance to our system:** A cheap, interpretable, directly-applicable skill test for any crowned TIMING strategy (our SMA-crossover / MACD / regime strategies ARE market-timing rules — long when bullish, flat when bearish). The non-parametric 2×2 version is the gem: tabulate the crown's position signal (in-market / flat) against the next-bar market direction and test p1+p2 > 1 — if a crowned strategy can't beat the independence null, its "edge" is just luck, regardless of its backtested return. This is a fundamentally DIFFERENT and complementary lens to our return-based bootstrap: it asks "is the directional CALL skillful?" rather than "did the equity curve beat hold?", and a strategy can pass the latter by luck while failing the former. The parametric down-market-dummy test also formalizes the most plausible way an active rule COULD beat hold (downside protection), which is exactly worth checking against buy-and-hold. Strong candidate for a "timing-skill" badge in the ranking, and a sharper way to expose lucky crowns.

### [66] Determinants of Portfolio Performance (Brinson–Hood–Beebower) + II Update
- **Authors / Venue:** Gary P. Brinson, L. Randolph Hood, Gilbert L. Beebower / Financial Analysts Journal 42(4), pp. 39–44; update Brinson–Singer–Beebower FAJ 1991
- **Year:** 1986 (update 1991)
- **Source:** DOI:10.2469/faj.v42.n4.39; update PDF https://indexacapital.com/bundles/unaiadvisor/docs/papers/1991-Brinson%20-Determinants-of-Portfolio-Performance-II.pdf; + French (2003) "Another Look" critique SSRN:706603
- **% read:** 30% (abstract + the attribution decomposition + the 93.6%/95.6% variance result + the well-known variance-vs-level misinterpretation critique via secondary sources)
- **Summary:** The foundational performance-ATTRIBUTION study and source of the famous "asset allocation explains ~94% of returns" claim. Decomposes a portfolio's return relative to a passive policy benchmark into three pieces: INVESTMENT POLICY (strategic asset allocation — the long-run passive weights), MARKET TIMING (tactical over/under-weighting vs policy), and SECURITY SELECTION (picking specific holdings within a class). Across 91 large pension plans (1974–83) they find investment POLICY explains on average 93.6% (95.6% in the data) of the VARIANCE of total return across time, while active timing and selection add little on average and are frequently NEGATIVE net of costs. The widely-cited critique (Ibbotson–Kaplan 2000; French 2003) clarifies a crucial nuance: the 90%+ figure is the variance of returns OVER TIME explained by policy, NOT the share of return LEVEL or cross-sectional dispersion — a distinction routinely mangled into "allocation is 90% of performance."
- **Relevance to our system:** A strong, classic prior in favor of our core thesis, with an important methodological caveat to avoid mis-quoting. The substantive lesson: ACTIVE decisions (timing, selection) add little on average and often subtract net of costs, while the passive baseline dominates — the portfolio-management analogue of "no active strategy robustly beats holding." For our single-coin advisor, "asset allocation" collapses to the in-market-vs-flat decision, so the Brinson lesson sharpens to: the timing overlay our strategies apply is the small, easily-negative active component on top of a buy-and-hold baseline — exactly what our gate keeps finding. The caveat is a reporting-discipline import: be precise about whether we mean variance-explained vs return-level when attributing our crown's performance, lest we repeat the field's famous misinterpretation. Background-to-conceptual, but a citable pillar for the product thesis and a reminder that attribution numbers are easy to mis-state.

### [67] Portfolio Performance Attribution via Shapley Value
- **Authors / Venue:** Nicholas Moehle, Stephen Boyd, Andrew Ang / arXiv (q-fin.PM); Journal of Investment Management
- **Year:** 2021
- **Source:** arXiv:2102.05799 (https://arxiv.org/abs/2102.05799)
- **% read:** 30% (abstract + Shapley-vs-Brinson rationale + efficiency/symmetry/additivity properties via HTML abstract; skipped computation sections)
- **Summary:** Fixes a fundamental flaw in classic attribution [66]: the contribution assigned to each decision/feature DEPENDS ON THE ORDER you peel them off (sequential attribution is path-dependent and thus arbitrary). They recast attribution as a COOPERATIVE GAME — each "feature" (an active tilt, a factor exposure, a baseline) is a player, the portfolio's performance is the value to be divided — and use the SHAPLEY VALUE, the unique allocation satisfying EFFICIENCY (attributions sum to total performance), SYMMETRY (equal contributors get equal credit), NULL-PLAYER (a feature that never helps gets zero), and ADDITIVITY (attributions combine linearly across sub-portfolios). The Shapley value averages a feature's marginal contribution over ALL possible orderings of the other features, eliminating order-dependence. They give exact and approximate (sampling-based) computation for realistic portfolios.
- **Relevance to our system:** The principled, order-independent way to answer "WHERE did the crown's performance come from?" if we ever decompose a composed/multi-signal strategy. Our ComposedStrategy families stack signals (e.g. SMA filter + RSI trigger), and a naive "turn each off in sequence" attribution would give arbitrary, order-dependent credit; the Shapley value gives a fair decomposition of how much each component contributes to the edge over buy-and-hold. This is directly useful for the anti-storytelling discipline [17][19]: instead of narrating why a strategy "should" work, Shapley-attribute its actual realized edge to components and check whether the contribution is real or noise. The efficiency/additivity properties also make it a clean basis for explaining a crown to the operator. Niche but high-quality: reach for it when explaining COMPOSED crowns; overkill for single-indicator strategies.

### [68] Robustness Testing for Algorithmic Trading Strategies — practitioner synthesis
- **Authors / Venue:** Practitioner synthesis (BuildAlpha robustness-testing guide; bagging/ensemble-for-trading write-ups; Pardo/StrategyQuant robustness lineage)
- **Year:** 2024–2025 (synthesis)
- **Source:** https://www.buildalpha.com/robustness-testing-guide/ ; bagging refs https://www.datacamp.com/tutorial/what-bagging-in-machine-learning-a-guide-with-examples
- **% read:** 40% (full robustness-test taxonomy + philosophy + pass/fail heuristics via the guide; a practitioner-consensus synthesis, not a primary paper)
- **Summary:** A practitioner taxonomy of how to stress a candidate strategy until an overfit one breaks. Core philosophy: a real edge SURVIVES small perturbations; a strategy that collapses under tiny changes was fitting noise. The test families: (1) MONTE-CARLO on trades — RESHUFFLE trade order (path-dependence), RESAMPLE trades with replacement (return-distribution variance), and PERMUTATION of price changes to make synthetic series that preserve marginal statistics but destroy the specific pattern; (2) DATA PERTURBATION — add/subtract NOISE to each bar repeatedly and require the edge to persist; SHIFT bar open/close times; (3) PARAMETER analysis — SENSITIVITY / NEIGHBORHOOD STABILITY: require neighboring parameter values (±10%) to perform similarly, avoiding "fragile peaks" (a lone spike in the parameter surface = overfit); (4) BENCHMARKS — out-of-sample/walk-forward, and beat the BEST RANDOM strategy (a Monte-Carlo significance test); (5) STRESS — delayed entries/exits (fill-sensitivity), realistic slippage/liquidity, synthetic shocks. Also notes the BAGGING diagnostic: average many models on bootstrapped data — if bagging HURTS performance, the original was overfit to idiosyncratic patterns. Few hard pass/fail rules; surviving MANY independent tests is the signal.
- **Relevance to our system:** A ready-made menu of cheap robustness checks, several of which directly extend our gate. (1) The PARAMETER NEIGHBORHOOD-STABILITY test is a high-value, currently-missing check for us: a crowned config sitting on a lone spike in the Sharpe-vs-parameter surface is overfit — we should require the crown's neighbors (±1-2 parameter steps) to also beat hold, or flag it. (2) "Beat the best RANDOM strategy" is conceptually our bootstrap-vs-hold gate but with a different null (random trading vs holding) — a useful SECOND null: a crown should beat BOTH buy-and-hold AND a randomized-entry strategy of equal trade frequency. (3) The NOISE-perturbation and one-bar-SHIFT tests are concrete implementations of the look-ahead/fragility audits [15][33][17]. (4) The BAGGING diagnostic ("if averaging hurts, you overfit") is a clever, cheap overfit detector we could run on the bake-off's neighboring configs. Practitioner-level (not peer-reviewed), but a concrete, directly-portable robustness checklist that complements the academic gate with parameter-surface and perturbation tests we don't yet have.

### [69] Permutation and Randomization Tests for Trading System Development (Monte-Carlo permutation testing)
- **Authors / Venue:** Timothy Masters / self-published book ("Algorithms in C++"); same method in his "Testing and Tuning Market Trading Systems"
- **Year:** 2020
- **Source:** ISBN 9798607808105; method via book description + author's market-trading page (cert-blocked) + practitioner summaries
- **% read:** 25% (detailed method overview from publisher/author descriptions + secondary summaries; not the book verbatim)
- **Summary:** A practitioner-rigorous treatment of MONTE-CARLO PERMUTATION testing as the tool for separating LUCK from SKILL in trading-system development. Core idea: PERMUTE (shuffle) the price changes / returns to destroy any genuine predictive structure while preserving the marginal return distribution, then re-run the SAME system on each permuted series to build a NULL distribution of performance "a worthless but lucky system" could achieve; the real system's p-value is its rank in that null. Key applications: (1) test a single finished system for luck-vs-skill; (2) test for overfitting at the EARLIEST stage; (3) remove SELECTION BIAS when screening many indicators — a "permutation training" procedure that, like White's Reality Check [6], accounts for the whole search by permuting before selecting (so the selected best is compared to the best-under-permutation); (4) bound future MEAN returns and typical/CATASTROPHIC future DRAWDOWNS via the permutation distribution; (5) test a strategy "factory" (the whole development process) for reliability. The unifying principle is the same as our gate: compare observed performance to a resampled/permuted null that preserves nuisance structure but removes the alleged edge.
- **Relevance to our system:** A direct, implementation-oriented companion to the academic data-snooping tests [6][7][8][45], framed exactly for our use case (a strategy "factory" that screens many configs). Two concrete imports. (1) The PERMUTATION null is a useful ALTERNATIVE/complement to our block-bootstrap-vs-hold null: shuffling returns destroys serial structure (the opposite of our block bootstrap, which PRESERVES it), so the two nulls answer different questions — permutation tests "is there ANY exploitable time-structure?", our block bootstrap tests "does the edge survive realistic path resampling?". Running BOTH brackets the robustness claim. (2) The "permutation training removes selection bias" procedure is precisely the multiple-testing correction our bake-off needs, and it's the same family as BBC-CV [64] / Reality Check [6]: permute, RE-SELECT the best, compare. The drawdown-bounding via permutation also complements the Brownian E[MDD] null [43] with a non-parametric version. Practitioner source (not peer-reviewed), but the most application-focused statement of the luck-vs-skill permutation method, and its "test the whole factory" framing matches our pipeline exactly.

### [70] Illusory Profitability of Technical Analysis in Emerging Foreign Exchange Markets
- **Authors / Venue:** Pei Kuang, Michael Schröder, Qingwei Wang / International Journal of Forecasting 30(2), pp. 192–205
- **Year:** 2014
- **Source:** DOI:10.1016/j.ijforecast.2013.07.015; SSRN:1586278; Birmingham WP 13-09 (https://ideas.repec.org/p/bir/birmec/13-09.html); abstract via repec/ScienceDirect
- **% read:** 30% (abstract + universe size + data-snooping-corrected headline result via repec/ScienceDirect; primary paywalled)
- **Summary:** A large-scale, data-snooping-corrected replication of the technical-analysis-profitability question [45] in TEN emerging FX markets. They evaluate 25,988 trading strategies (the full searchable universe of TA rules), and under STANDARD (uncorrected) tests find hundreds to thousands of "significant" profitable strategies — with the best rules sometimes posting >30% annual mean excess return. But once DATA-SNOOPING BIAS is corrected (White's Reality Check [6] / Hansen's SPA [7] family, accounting for the full 25,988-rule search), ALMOST ALL of those profits VANISH. Their blunt conclusion: the profitability of technical analysis in these markets is ILLUSORY — an artifact of mining ~26k rules and reporting the luckiest, not genuine predictability. (Companion to the broader Hsu–Hsu–Kuan stepwise-test literature, which finds some surviving edge in the YOUNGEST/least-liquid markets but not mature ones.)
- **Relevance to our system:** Among the strongest single pieces of evidence for our entire product thesis, in a market (FX) and rule-class (technical analysis) very close to ours. The mechanism is IDENTICAL to our bake-off: search a huge universe of TA configs, find spectacular-looking winners, then watch them evaporate under a search-aware (Reality-Check) correction — which is exactly why our gate must be search-aware and skeptical [1][2][6][46]. The >30% headline that becomes nothing after correction is a vivid, quotable illustration of how large the selection-bias haircut can be and why a raw bake-off Sharpe is meaningless without it. The crypto relevance is direct: crypto is a young, sometimes-thin market, and the Hsu-lineage finding that surviving edge appears (if anywhere) only in the LEAST mature/liquid markets is a nuanced caveat — any apparent crypto TA edge should be cross-checked for whether it's just an illiquidity/microstructure artifact that real costs [13][47] would erase. Reinforces: standard significance is worthless under heavy search; only the corrected verdict counts.

### [71] Testing and Dating of Structural Changes in Practice (Bai–Perron breakpoints + fluctuation tests)
- **Authors / Venue:** Achim Zeileis, Christian Kleiber, Walter Krämer / Computational Statistics & Data Analysis 44(1-2), pp. 109–123; methods of Bai & Perron (1998, 2003)
- **Year:** 2003 (Bai–Perron 1998/2003)
- **Source:** PDF https://www.zeileis.org/papers/Zeileis+Kleiber+Kraemer-2003.pdf; Bai–Perron DOI:10.1002/jae.659 (J. Applied Econometrics)
- **% read:** 35% (abstract + Bai–Perron dynamic-programming breakpoint estimation + CUSUM/MOSUM fluctuation tests + BIC break-count selection + the parameter-stability rationale from full-text PDF; skipped R-package code)
- **Summary:** The practical toolkit for detecting and DATING structural breaks (regime changes) in a time series, where model parameters shift at unknown points. Two complementary approaches. (1) FLUCTUATION TESTS (OLS-CUSUM / MOSUM): track the cumulative or moving sum of residuals from a constant-parameter fit; large excursions signal that parameters aren't stable — a generic "is there a break anywhere?" detector. (2) BAI–PERRON multiple-breakpoint estimation: simultaneously estimate the NUMBER and LOCATIONS of m breaks by DYNAMIC PROGRAMMING that minimizes residual sum of squares across the m+1 regimes (O(T²) cost for any number of breaks), with sup-F tests for "0 vs k breaks" and a double-max (UDmax) test for "0 vs an unknown number," plus BIC / sequential selection of the break count and confidence intervals for the break DATES. The core warning: with unmodeled breaks, constant-parameter inference and forecasts are invalid — you must either model regime-specifically or restrict to a single regime.
- **Relevance to our system:** Directly relevant to two backtest-validity questions for our gate. (1) WINDOW REPRESENTATIVENESS: if the bake-off window straddles a structural break (crypto is full of them — halvings, regime flips, 2021 bull → 2022 bear), a single in-sample crown is fit to a MIXTURE of regimes and won't generalize; running a Bai–Perron / CUSUM check on the (coin,window) would flag when the window is non-stationary so we can warn the operator or split the analysis. (2) PARAMETER STABILITY of the crown itself: re-estimating a strategy's edge in each Bai–Perron regime tests whether the apparent edge is regime-specific (consistent with [36][33]'s regime-conditioning theme) or stable — a stable-across-regimes crown is far more trustworthy. This also sharpens our block-bootstrap: the bootstrap assumes (approximate) stationarity, so a detected break is a caveat on the gate's own validity, arguing for regime-aware resampling or a stationarity pre-check. Cheap, well-implemented (R strucchange), and a missing stationarity guard for our pipeline.

### [72] AlphaEval: A Comprehensive and Efficient Evaluation Framework for Formula Alpha Mining
- **Authors / Venue:** Ding, Chen, Huang, et al. / arXiv (q-fin / cs.LG)
- **Year:** 2025
- **Source:** arXiv:2508.13174 (https://arxiv.org/abs/2508.13174); HTML https://arxiv.org/html/2508.13174v1
- **% read:** 35% (abstract + five-dimension evaluation framework + cross-method ranking results via HTML; skipped per-metric formal definitions and full tables)
- **Summary:** Argues that the explosion of alpha-MINING methods (genetic programming, RL, GANs, LLMs) is evaluated INCONSISTENTLY — typically by in-sample Information Coefficient (IC) or a single backtest return, which ignores overfitting, stability, robustness, and redundancy, and is expensive/parameter-sensitive. AlphaEval proposes a standardized, backtest-light evaluation across FIVE complementary dimensions: (1) PREDICTIVE POWER (IC + RankIC); (2) TEMPORAL STABILITY (a "relative rank entropy" measuring how consistent the signal's ranking is over time); (3) ROBUSTNESS to market perturbations (a "perturbation fidelity score" — does the signal survive added Gaussian AND heavy-tailed noise?); (4) FINANCIAL LOGIC / interpretability (LLM-scored economic coherence); (5) DIVERSITY (an entropy measure over the covariance structure detecting multicollinearity/redundancy among signals). Evaluating eight mining algorithms, they find the COMBINED multi-dimensional score selects better alphas than any single metric, with a ~25%+ speedup vs full backtesting; RL methods scored high on stability/robustness, LLM methods best on overall trade-off and interpretability.
- **Relevance to our system:** A modern, directly-relevant template for how to SCORE candidate strategies on more than one axis — exactly the multi-metric ranking discipline our bake-off should adopt. Three of its five dimensions map cleanly onto checks we want: ROBUSTNESS-to-noise is the perturbation test [68] in a quantified score; TEMPORAL STABILITY is our multi-window/regime-stability idea [12][71]; DIVERSITY/redundancy is the config-correlation / effective-N clustering insight [46][39] (don't reward many copies of the same idea). The headline lesson — single-metric (IC or one backtest return) selection is inferior to a composite that includes overfitting/robustness/diversity — validates moving our crown selection AWAY from raw in-sample Sharpe toward a composite that bakes in robustness and a selection-bias penalty. The "backtest-light" angle (evaluate cheaply on these proxies before expensive full backtests) is also a useful efficiency pattern for a big sweep. Caveat: it's a cross-sectional equity-alpha framework (IC-based), so the predictive-power axis doesn't transfer 1:1 to our single-coin long/flat setting, but the multi-dimensional philosophy and the robustness/stability/diversity axes do.

### [73] Online Control of the False Discovery Rate with Decaying Memory (alpha-investing / LORD family)
- **Authors / Venue:** Aaditya Ramdas, Fanny Yang, Martin J. Wainwright, Michael I. Jordan / NeurIPS 2017; building on Foster–Stine (2008) alpha-investing & Javanmard–Montanari LORD
- **Year:** 2017
- **Source:** arXiv:1710.00499 (https://arxiv.org/abs/1710.00499); NeurIPS 2017 proceedings
- **% read:** 35% (abstract + alpha-investing/alpha-wealth mechanism + decaying-memory mem-FDR motivation + GAI++ improvements via HTML abstract; skipped proofs)
- **Summary:** Solves multiple-testing when hypotheses arrive in a STREAM and decisions must be made online without knowing future tests — unlike Benjamini–Hochberg/Bonferroni [5], which need the whole batch up front. The ALPHA-INVESTING framework (Foster–Stine) uses an "alpha-WEALTH" metaphor: you start with an error budget; each test SPENDS some alpha to be conducted, and a REJECTION (discovery) EARNS some alpha back, so a run of true discoveries replenishes the budget and sustains power, while a drought of rejections conserves it — all while controlling FDR over the growing sequence. This paper generalizes the family (GAI++): higher power via better alpha allocation, PRIOR WEIGHTS for hypotheses likely to be non-null, DIFFERENTIAL PENALTIES when some false discoveries are costlier, and crucially DECAYING-MEMORY FDR (mem-FDR) that down-weights OLD tests so the procedure stays responsive to recent data rather than being dominated by (or starved by) the distant past ("piggybacking"/"alpha-death").
- **Relevance to our system:** The right multiple-testing frame for a CONTINUOUSLY-RUNNING or repeatedly-re-run advisor, which the batch corrections [1][5][39] don't cover. Every time we re-run the bake-off (new coin, new window, a later date) we conduct ANOTHER family of tests; over months these accumulate and the naive per-run significance silently inflates the lifetime false-discovery rate. Online-FDR/alpha-investing is exactly how to control the false "this strategy beats hold" discovery rate ACROSS our entire sequence of runs, not just within one. The DECAYING-MEMORY variant fits crypto's non-stationarity: a discovery from a 2021 regime shouldn't keep spending our error budget in 2026 — recent runs should dominate. The "rejection earns alpha" mechanic also formalizes a sensible behavior: after genuinely finding edges, we can afford to keep testing; after a long "nothing beats hold" streak (our usual outcome), the budget tightens, raising the bar — which is the correct, self-skeptical response. Forward-looking: most relevant if/when the advisor becomes a standing service issuing many verdicts over time.

### [74] Bootstrap Methods for Strategy Robustness (drawdown / Calmar / Sharpe confidence intervals) — practitioner synthesis
- **Authors / Venue:** Susan Potter (quant practitioner) + PyBroker bootstrap-metrics docs / CME drawdown-measures note
- **Year:** 2024 (synthesis)
- **Source:** https://www.susanpotter.net/quant/bootstrap-methods-strategy-robustness/ ; https://www.pybroker.com/en/latest/notebooks/3.%20Evaluating%20with%20Bootstrap%20Metrics.html
- **% read:** 45% (full methodology articles incl. block/stationary choice + drawdown/Calmar CI procedure + block-length sensitivity discipline; practitioner synthesis, not a primary paper)
- **Summary:** A clear, implementation-level statement of EXACTLY our gate's method, applied to risk metrics. The framing matches ours precisely: "you get one sample path... every metric is a point estimate from a sample of one," so resample to turn point estimates into DISTRIBUTIONS. Procedure for any metric (Sharpe, MAX DRAWDOWN, CALMAR = CAGR/MaxDD): generate B bootstrap return series, recompute the metric on each, read percentiles for a CI / full distribution. Three resampling choices are weighed: IID bootstrap (REJECTED — destroys autocorrelation and volatility clustering), MOVING-BLOCK (preserves within-block structure but has boundary discontinuities), and STATIONARY bootstrap (RECOMMENDED — geometric random block lengths smooth boundaries; mean block length ~7–20 daily bars is the lone knob). Key practical findings: bootstrapped MAX DRAWDOWN at high confidence is much WORSE than observed (e.g. 15% observed → 30%+ at the 95th percentile) — the observed drawdown is just one lucky realization; and a critical discipline — NEVER report bootstrap results at a single block length, test several, and if conclusions change the finding isn't robust. It explicitly ties in DSR as a fast pre-filter and RC/SPA for multiple-strategy snooping.
- **Relevance to our system:** This is essentially a how-to for the exact gate we already run, so it both VALIDATES our design and supplies concrete upgrades. (1) DRAWDOWN/CALMAR distributions: we should report the crowned strategy's and buy-and-hold's bootstrap MAX-DRAWDOWN distribution (not just a point MaxDD), since the observed worst-case is one lucky path and the operator cares most about downside — pairs with the analytic Brownian E[MDD] null [43] (analytic vs empirical cross-check). (2) The block-length SENSITIVITY discipline ("never one block length; if the verdict flips, it's not robust") is a direct, cheap hardening of our FROZEN gate and a concrete instance of the specification-curve idea [44] — re-run the gate over a small grid of block lengths and require the "beats hold?" verdict to be STABLE; this also operationalizes the data-driven block-length selector [20] as a SENSITIVITY band rather than a single value. (3) Endorses the stationary (random-length) bootstrap [10] over fixed-length to remove boundary artifacts. (4) The "DSR pre-filter, then bootstrap survivors, then RC/SPA for snooping" pipeline is a sensible end-to-end ordering we could adopt wholesale. Among the most directly actionable entries for our gate's internals.

### [75] Deep Learning for Financial Time Series: A Large-Scale Benchmark of Risk-Adjusted Performance
- **Authors / Venue:** Saly-Kaufmann, Wood, Calliess, Zohren (Oxford-MAN lineage) / arXiv (q-fin.TR / cs.LG)
- **Year:** 2026
- **Source:** arXiv:2603.01820 (https://arxiv.org/abs/2603.01820); PDF https://arxiv.org/pdf/2603.01820
- **% read:** 35% (abstract + evaluation-methodology section + honest-baselines design + headline finding from full-text PDF; skipped per-architecture result tables)
- **Summary:** A large-scale, methodologically-careful benchmark of modern DEEP-LEARNING architectures (Transformers, iTransformer, PatchTST, Temporal Fusion Transformer, Mamba variants, LSTMs/RNNs) for financial time-series prediction/trading across multiple datasets. The contribution is as much about EVALUATION DISCIPLINE as about models: it scores on RISK-ADJUSTED metrics (Sharpe etc., not raw prediction accuracy), runs MULTIPLE-SEED experiments to test consistency/generalization (one lucky seed isn't evidence), enforces strict train/test separation against look-ahead, and — crucially — compares every fancy model against HONEST SIMPLE BASELINES (buy-and-hold, AR(1)/linear models). Headline finding: complex DL models FREQUENTLY FAIL to beat the simple baselines on a risk-adjusted, out-of-sample basis, and the authors suggest many published DL-beats-market results reflect overfitting / favorable data mining rather than genuine predictive advantage.
- **Relevance to our system:** A fresh (2026), large-scale, independent restatement of our core thesis on the ML/DL frontier: after honest, risk-adjusted, multiple-seed, baseline-anchored evaluation, sophisticated models do NOT reliably beat simple holds/linear baselines — exactly the conclusion our gate keeps reaching for technical strategies, now shown for state-of-the-art DL. Three methodology imports directly relevant to us: (1) MULTIPLE-SEED robustness is the ML analogue of our many-bootstrap-paths idea — never trust a single training run / single backtest path; we already embody this for strategies and should keep it if we ever add any learned component. (2) Insisting on buy-and-hold AND linear baselines as the comparison floor is precisely our "buy-and-hold is always the benchmark" rule, validated as best practice. (3) Risk-ADJUSTED, out-of-sample scoring over raw accuracy/return mirrors our preference for robustness-percentile selection over in-sample point returns. Strong, current ammunition for resisting any "add a deep model, it'll beat holding" pressure, and a template for how we'd HAVE to evaluate such a model honestly (multiple seeds, risk-adjusted, vs hold) if we ever tried.

### [76] Efficient Market Hypothesis and Forecasting
- **Authors / Venue:** Allan Timmermann, Clive W. J. Granger / International Journal of Forecasting 20(1), pp. 15–27
- **Year:** 2004
- **Source:** DOI:10.1016/S0169-2070(03)00012-8; SSRN:353300; CEPR DP3593; abstract via repec/sciepub
- **% read:** 30% (abstract + self-destruction/nonstationarity argument + forecasting-test implications via repec restatement; primary paywalled)
- **Summary:** A conceptual cornerstone linking market efficiency to the LIMITS of forecasting. The argument: a genuinely predictable pattern is a profit opportunity; once forecasters discover and trade it, their trading moves prices and ELIMINATES the pattern — so stable predictable structure "SELF-DESTRUCTS when discovered by a large number of investors." This has three consequences. (1) Market-efficiency tests are harder than ordinary forecast-optimality tests because they must establish profitable opportunities in REAL TIME (not with hindsight). (2) The continuous search-and-arbitrage cycle induces NONSTATIONARITY in returns — the data-generating process keeps changing as edges are found and competed away. (3) Because the model space and information set evolve, any fixed forecasting model has a limited shelf life; persistent out-of-sample predictability is the exception, not the rule. They frame data-mining and forecasting as inherently self-limiting in efficient-ish markets.
- **Relevance to our system:** The theoretical "why" behind our validated thesis and behind the decay results [57][58]: edges that exist get arbitraged away, so a backtest edge is unlikely to persist out-of-sample. For our advisor this argues that (a) any crowned strategy on a well-known indicator should be expected to have ALREADY self-destructed (textbook signals are maximally "discovered") — heavy decay prior; (b) the NONSTATIONARITY point is a direct caution on our backtest window choice and on the bootstrap's stationarity assumption — it predicts the structural breaks [71] we should test for; and (c) "establish profit in REAL TIME" is precisely the discipline of our forward paper-trade vs the in-sample crown. It also tempers over-claiming: the same logic says a real edge, if found, won't last, so even a gate-passing crown deserves a "this will likely decay" caveat. Background-to-conceptual, but the canonical citation for WHY no active strategy robustly beats holding once everyone can see the same signals.

### [77] Technical Analysis Meets Machine Learning: Bitcoin Evidence
- **Authors / Venue:** José Ángel Islas Anguiano, Andrés García-Medina / arXiv (q-fin.ST / cs.LG)
- **Year:** 2025
- **Source:** arXiv:2511.00665 (https://arxiv.org/abs/2511.00665); HTML https://arxiv.org/html/2511.00665v1
- **% read:** 35% (abstract + experimental design + cost treatment + headline net-of-cost results + overfitting-treatment note via HTML full text)
- **Summary:** A head-to-head crypto study: two ML models (LSTM, LightGBM) vs two technical-analysis strategies (EMA crossover, MACD+ADX) vs BUY-AND-HOLD on Bitcoin. Methodology: train 2021-01 to 2024-01, evaluate out-of-sample 2024-01 to 2024-12, with a 0.1% per-trade fee in the revised results and a fair-comparison alignment (TA delayed 95 days to match ML lookback). Net-of-cost result: only the LSTM beat buy-and-hold (53.23% vs 42.31%), while LightGBM (39.78%), MACD+ADX (34.45%), and EMA (25.27%) all UNDERPERFORMED hold. Importantly, the authors acknowledge MINIMAL overfitting/data-snooping treatment: parameters are grid-searched in-sample and applied out-of-sample with no cross-validation or multiple-testing correction discussed.
- **Relevance to our system:** A near-exact analogue of our app's question (TA + ML vs buy-and-hold on a single coin, net of costs, out-of-sample) and a CASE STUDY in why our gate's extra rigor matters. Two lessons. (1) SUBSTANCE: 3 of 4 active strategies (including both classic TA rules our bake-off uses) LOST to buy-and-hold net of a realistic 0.1% fee — direct corroboration of our thesis for BTC. (2) METHODOLOGY CAUTION: the lone "winner" (LSTM, +11pp) was selected via in-sample grid search with NO multiple-testing correction, NO cross-validation, and evaluated on a SINGLE out-of-sample window that happened to be a 2024 bull year — exactly the setup our DSR/PBO/bootstrap machinery [1][2][64] exists to discount, and exactly the single-path walk-forward weakness [11][19] we treat as a confidence check, not proof. Read through our lens, this is NOT credible evidence that LSTM beats holding BTC — it's one lucky config on one window with no selection-bias correction. A perfect teaching example for the operator of why a raw "X beat hold last year" claim is insufficient and why our gate demands path-robustness + a selection-bias haircut + a skill test [65] before crowning. High-relevance crypto anchor.
