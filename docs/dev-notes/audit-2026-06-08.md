---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-06-08
---

# Spec Audit — 2026-06-08

## Headline

The spec tree is **structurally flat-to-healthier** vs the audit-2026-06-01
baseline (mechanical lint 94/2-cat, down from 95/3-cat; the lone
missing-frontmatter regression is RESOLVED; anchors 119/119 PASS, up +32),
**but the recurring "tester-PASS handoff frontmatter-flip" drift has
compounded into its worst instance yet: FIVE feature.md files now carry a
status that lags their actual pipeline state** — four robustness features
that reached presenter `PRESENTATION → READY` (and one fully retired
program) still read `arch-done`/`tester-done`. None are stale by the 30-day
rule, but all five are semantically wrong. This is the 4th consecutive
audit flagging this drift class; the recommended pre-commit enforcement
hook is now overdue.

## Run metadata

- Date: 2026-06-08 (Monday, scheduled weekly cadence; 7 days since
  `audit-2026-06-01.md`, the most recent `docs/dev-notes/audit-*.md` baseline).
- Auditor: spec-auditor (scheduled, autonomous run — operator not present).
- Baseline: `docs/dev-notes/audit-2026-06-01.md` (per `audit-*.md` glob
  convention). No off-cadence `spec-audit-*.md` interim notes since.
- Python: 3.10.12 in sandbox; `tomllib` missing. `uv` cannot bootstrap
  Python 3.11+ (egress to GitHub python-build-standalone blocked).
  Ran `scripts/spec_lint.py --all` via a `tomli` shim
  (`sys.modules['tomllib'] = tomli` + `runpy.run_path` to preserve
  `__file__`). Carry-over P3 from five prior audits.
- Commits since 2026-06-01: **45** (`git log --since='2026-06-01' --oneline
  | wc -l`). Major arcs: carry-strategy full build → M-TEST PASS → retired
  (anchors #88/89); time-series-momentum-robustness PASS (#90/91, active-
  trading thesis CLOSED); horizon-retest-robustness PASS (#92–99, OHLCV
  thesis closed across method AND horizon); perp-basis-signal-robustness
  (long-only FAMILY-UNIFORM-FRAGILE, #99→107); perp-basis-mn-spread v0.2.0
  (MN 3-arm sweep all FRAGILE, basis carries no orthogonal alpha,
  derivatives-positioning domain closes, #107→119); universe-method-
  diagnosis + on-chain-vs-conclude fork dev-notes.

## Mechanical lint

`scripts/spec_lint.py --all` — **FAIL (94 violations in 2 categories).**
Exit code = 2 (= number of triggered categories).

| Category             | 2026-06-08 | 2026-06-01 | Δ   |
|----------------------|-----------:|-----------:|----:|
| dead-link            | 87         | 87         | 0   |
| missing-frontmatter  | 0          | 1          | −1  |
| trace-broken-path    | 7          | 7          | 0   |
| shipped-no-tests     | 0          | 0          | 0   |
| **TOTAL**            | **94**     | **95**     | **−1** |

The single delta is **missing-frontmatter −1**: last week's lone violation
(`cross-sectional-mean-reversion-strategy/feature.md` `status: tested`,
non-enum) was actioned — it now reads `status: tester-done` (commit
`400a09e`, "action the 2026-05-31 audit"). **Last audit's P1 RESOLVED.**

### Sample violations (unchanged clusters — no new entries this week)

**dead-link (87 — all pre-existing):**
- `_bmad-output/planning-artifacts/architecture/decisions/0027-kronos-onnx-tract-integration.md` — 5 links to
  archived `../../v25-kronos-forecast-overlay/feature.md`.
- `spec/chart-canvas-overhaul/feature.md` — 7 links to ephemeral
  `/tmp/orch-diag/cockpit-*.png`.
- `spec/v0-paper-sma/reports/screenshots/README.md` — 6 links to pre-flat-
  layout `../../features/` + `../../tasks/` paths.
- `spec/cockpit-activity-status-bar/presentations/...-2026-05-26.md` — 4
  uncommitted `artifacts/.../*.png`.
- `spec/journal-transactions-metadata/feature.md` + `tasks.md` — 4 links to
  a missing test report.
- `spec/lumen-design-adoption/phase-5-humancontrol-agentfeed/{feature,tasks}.md`
  — 5 links to post-crate-split `tape.rs`/`state.rs`/`runtime.rs`.
- `spec/ui-rethink-phase-{d,e}-*/presentations/*` — 7 links to uncommitted
  `crates/ui/tests/visual-baselines/*.png`.
- `spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-20260522.md`
  — 1 dead link, **byte-immutable per ADR-0038 § D6 — do NOT edit directly.**

**trace-broken-path (7 — all pre-existing carry-over):**
- `REQ-LAB-YAHOO-REALDATA-V0-1-4-001` arch → missing
  `spec/lab-yahoo-realdata-v0.1.2-per-ticker-scaling-and-aggregate-cache-state/feature.md`.
- `REQ-VISUAL-FAIL-HTML-REPORTER-001` arch → archived
  `dev-notes/archive/2026-Q2/ui-testability-deep-dive-2026-05-15.md`;
  tests (2) → bare fn names without `crates/...rs::` prefix.
- `REQ-UI-CONTRAST-ASSERTER-001` arch → same archived path.
- `REQ-QUEUE-STALENESS-RECONCILIATION-001` tests →
  `scripts/queue_staleness_check.py --self-test` (script, not Rust test).
- `REQ-OPERATOR-LEDGER-SCHEMA-LINT-001` tests →
  `scripts/operator_ledger_check.py --self-test` (same shape).

### Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119)**, verified live.

Anchor count trajectory:

| Date       | Count | Net | Driver                                                                 |
|------------|------:|----:|-----------------------------------------------------------------------|
| 2026-05-31 |    87 | +1  | MR θ-surface #87.                                                      |
| 2026-06-01 |    87 |  0  | audit-2026-06-01 baseline.                                             |
| 2026-06-08 |   119 | +32 | carry #88/89; TS-momentum #90/91; horizon-retest #92–99 (8); perp-basis 99→107 (8); perp-basis-mn 107→119 (12). |

## Feature inventory (98 feature.md files)

Counted via the task glob `spec/*/feature.md` excluding
`design/ dev-notes/ runbooks/ archive/ architecture/` (= `find -maxdepth 2`).
**Methodology note:** audit-2026-06-01 reported 100 files; that count
included depth-3 `lumen-design-adoption/phase-*/feature.md` sub-features,
which fall outside the `spec/*/feature.md` glob. 98 is the correct count
for this task's specification.

### Status histogram

| Status          | Count | Δ vs 06-01 | Members / notes                                                              |
|-----------------|------:|-----------:|------------------------------------------------------------------------------|
| shipped         |    69 | —          | (06-01 count of 74 included depth-3 lumen phases; not comparable)            |
| tester-done     |     5 | —          | cross-sectional-mean-reversion-strategy, momentum-parameter-robustness-sweep, monte-carlo-bootstrap-path-generator, **time-series-momentum-robustness**, v2-1-tracing-layer-redactor |
| dev-done        |     5 | 0          | carry-funding-data-backfill, strategy-robustness-harness, ui-contrast-asserter, ui-test-harness-viewport-matrix, visual-fail-html-reporter |
| arch-done       |     5 | +3         | **carry-strategy, horizon-retest-robustness, perp-basis-signal-robustness, perp-basis-mn-spread**, lab-yahoo-realdata-v0.1.4-bulk-ticker-re-emit |
| deprecated      |     4 | 0          |                                                                              |
| draft           |     3 | 0          | subscription-pipe-server-time-template, ui-gallery-table-cell, v3-xgboost-cheap-classifier |
| retired         |     2 | 0          | v3-volatility-forecaster, v3-volatility-forecaster-rebaseline                |
| candidate       |     2 | 0          | cockpit-app-bundle, iced-ecosystem-evaluation                                |
| shipped-partial |     1 | 0          | v3-llm-forecaster                                                            |
| roadmap         |     1 | 0          | lumen-design-adoption                                                        |
| proposed        |     1 | 0          | lab-polish-round-2                                                           |
| **TOTAL**       |  **98** |          |                                                                              |

The `tested` (non-enum) bucket from last week is GONE — flipped to
`tester-done`. No invalid-enum statuses this run.

### Stale work (>30 days, status proposed | in-progress) — ZERO hits

- `lab-polish-round-2` — `proposed`, updated 2026-05-25 (**14 days**) — under threshold.
- No `in-progress` features exist.
- Watch (non-proposed/in-progress, but aging): `cockpit-app-bundle`
  (`candidate`, 2026-05-11, **28 d**) and `iced-ecosystem-evaluation`
  (`candidate`, 2026-05-13, **26 d**) — both still inside 30 d but will
  cross it within two audits if untouched.

### ⚠ Status drift — feature.md status lags actual pipeline state (HEADLINE)

Not caught by the >30-day staleness rule (all five updated within 7 days)
and not caught by spec-lint (all five carry valid-enum statuses). Surfaced
by cross-referencing feature.md frontmatter against `git log` +
`presentations/` + `reports/`. **Every one of these reached presenter
`PRESENTATION → READY` (or full retirement) yet the feature.md status was
never advanced past the developer/tester handoff:**

| Feature                          | feature.md status | Actual state (git + presentations/ + reports/)                                                                  | Should be       |
|----------------------------------|-------------------|-----------------------------------------------------------------------------------------------------------------|-----------------|
| carry-strategy                   | `arch-done`       | M-TEST VERDICT PASS (`72d711c`); presenter sprint-review deck `PRESENTATION → READY`, **carry retired, program closed** (`25591cc`); anchors #88/89 locked. | presenter-done / retired |
| horizon-retest-robustness        | `arch-done`       | M-TEST VERDICT PASS (`2c2d949`); test report `test-2026-06-05-...`; presenter program-retrospective deck `PRESENTATION → READY` (`94b26fc`); anchors #92–99. | presenter-done  |
| time-series-momentum-robustness  | `tester-done`     | presenter program-retrospective deck `PRESENTATION → READY` (`25c848a`); anchors #90/91, active-trading thesis CLOSED. | presenter-done  |
| perp-basis-signal-robustness     | `arch-done`       | M-TEST VERDICT PASS (`0a36cdf`); presenter release-retrospective deck, long-only close-out (`f9e3302`); anchors 99→107. | presenter-done  |
| perp-basis-mn-spread             | `arch-done`       | M-TEST VERDICT PASS (`8c2e6c4`); presenter domain-close retrospective deck (`05f5824`); anchors 107→119, domain closes. | presenter-done  |

This is the **4th consecutive audit** to flag the tester-PASS handoff
frontmatter-flip drift (audit-2026-05-29: lab-yahoo owner stuck at
presenter; spec-audit-2026-05-31: MC-bootstrap stuck at dev-done;
audit-2026-06-01: MR flipped to non-enum `tested`). This week it
**compounded to 5 features simultaneously** across the carry / TS-momentum /
horizon / perp-basis program wave — the workflow processed five full
analyst→…→presenter cycles and advanced the trace state and the anchors
correctly each time, but the feature.md status was left at the
architect/tester handoff in all five. The trace.toml rows are the source of
truth and were maintained; feature.md is the lagging mirror. Process signal
is now unambiguous: **automate the flip or gate it.**

## Decay markers (TODO / FIXME / TBD / XXX / ???)

Raw match counts across `spec/` excluding `spec/archive/`:

| Folder                                          | Matches | Δ vs 06-01 | Flag                                   |
|-------------------------------------------------|--------:|-----------:|----------------------------------------|
| dev-notes (cross-cutting incl. audit notes)     |      80 | +27        | informational (audit notes carry TBDs by nature) |
| lumen-design-adoption                           |      11 | +3 (was 8) | **DECAY-HEAVY** (carry-over, regressed) |
| cockpit-performance-and-input-responsiveness    |       6 | 0          | **DECAY-HEAVY** (carry-over, flat)     |
| (all other folders ≤ 2 matches each)            |     ≤ 2 | —          | —                                      |

Only two non-dev-notes folders exceed the >5 threshold:
`lumen-design-adoption` (11, regressed +3 — it had improved to 8 at the
interim, now back up) and `cockpit-performance-and-input-responsiveness`
(6, flat). Both pre-existing carry-overs. No new decay-heavy folders.

## Soft contradictions (LLM-judged — verify before acting)

Fresh sweep over the five CLAUDE.md non-negotiable cross-cutting topics.
**Three of five checked CONSISTENT; two carry-over contradictions persist;
one new documentation-staleness surfaced.**

### CONSISTENT (no contradiction found)

- **Money math (Decimal, never f64):** `spec/architecture.md:33-36` §2 and
  ADR-0003 both mandate `rust_decimal::Decimal`, exact-cent, zero tolerance.
  The new perp-basis-mn trace row explicitly commits to "Decimal money
  throughout". Aligned. ✓
- **Timestamps / no-clocks-in-UI-tests:** `chart-x-axis-local-time/feature.md`
  documents a deterministic `UtcOffset::UTC` return in **both production AND
  test** (`crates/ui/src/widgets/chart.rs#L175`) — consistent with the
  anchor-determinism invariant; no opposing wall-clock claim found. ✓
- **Sub-agent capability boundary:** `AGENT.md:28` "Sub-agents do NOT
  commit. The orchestrator owns git add/commit/push." Consistent with
  CLAUDE.md orchestration contract. ✓

### SC-A — ADR-0043 § D2 RNG: ChaCha20 (body) vs Murmur3 (changelog) (MEDIUM, carry-over)

- `_bmad-output/planning-artifacts/architecture/decisions/0043-simulated-latency-and-slippage.md:74-84` § D2
  body still specifies a `ChaCha20Rng` sub-stream
  (`latency_rng_for_order(scenario_seed, order_id)`).
- The same ADR Changelog `:272-278` (2026-05-27, M-FINAL) records the
  **implemented** form is a **Murmur3-style bit mixer** (ChaCha20 init was
  ~400-600 ns/order, 8-12× over the ≤50 ns R7 target; Murmur3 achieves
  2.28-2.50 ns/call).
- Body never updated to reflect the changelog deviation. A reader of § D2
  alone is misled. Carry-over from audit-2026-06-01 SC-5 / audit-2026-05-29
  P2 — **not actioned in 7 days.** Owner: architect (one-line body note
  pointing at the § Changelog deviation closes it). Confidence: MEDIUM
  (deviation is operator-acknowledged in the changelog, so not a live
  functional contradiction — only a body-vs-changelog documentation split).

### SC-B — `11-regression-gate.md` live-count caveat already stale (HIGH, partial-regression)

- audit-2026-06-01 P1 ("anchor table frozen at Phase-1A / 11 scenarios")
  was actioned: `spec/architecture/11-regression-gate.md:62` now reads "At
  Phase 1A close, the set was 11 scenarios … The LIVE set has since grown
  to **87**". The caveat fix landed (good).
- But the embedded live number (**87**) is itself **already stale** — the
  live count is **119** as of today (+32 in one week). The caveat
  hard-codes a count instead of deferring purely to `verify_anchors.sh`,
  so it re-rots every anchor wave.
- Recommendation: replace the inline number with a pointer only
  ("the LIVE count is whatever `verify_anchors.sh` reports; do not
  hard-code it here"). Owner: architect. Confidence: HIGH (the live count
  was verified at 119/119 this run).

### SC-C — `product.md` § Week 2 `bps: 2` vs v5 canonical `bps: 8` (MEDIUM, carry-over)

- `spec/product.md:553-554` — Week-2 paper-fill MatchingEngine uses
  `bps: 2` slippage + `0.04%` taker fee.
- ADR-0043 / ADR-0045 define the v5 canonical friction profile as
  `slippage_bps: 8` (medium-friction) with sqrt-impact in v0.5.0.
- Week-2 product narrative is the original 2026-04 ship history; the ADRs
  are the current model. Not a live contradiction, but a one-line
  cross-reference would prevent confusion. Carry-over from audit-2026-06-01
  SC-4 / audit-2026-05-29 P2 — not actioned. Owner: analyst. Confidence:
  MEDIUM.

## Anchor coverage

`spec/anchors.toml` declares **119 locked scenarios** (77 distinct scenario
names across the dual `noop-baseline` / `v5-realdata-medium-2026-05` +
`mc-robustness-2026-06` namespaces). `spec/trace.toml` carries **92
`[[req]]` rows**.

### Unreferenced anchors — ZERO

Every scenario name in `anchors.toml` is cited by at least one trace row
(`comm -23` of anchor names vs trace tokens = empty).

### Shipped strategy features with no anchor coverage

| Feature                          | State    | Disposition                                                                                          |
|----------------------------------|----------|------------------------------------------------------------------------------------------------------|
| v2-llm-strategy                  | shipped  | Carry-over: zero anchors, no "not-anchorable" disposition note. LLM non-determinism is the likely (acceptable) reason but it is undocumented. Owner: tester → architect. |
| v3-llm-forecaster                | shipped-partial | API-key blocked by-design per feature.md § 14. Acceptable.                                     |
| v25-tcn-horizon-bump-or-retire   | shipped  | Decision/retire-judgement feature, not an alpha surface — anchorless by intent, but no disposition note. Cosmetic carry-over. |

**RESOLVED since 06-01:** `v15a-mean-reversion-pairs` (last week's P2 anchor
gap) is now covered — `REQ-V15A-PAIRS-001` cites `pairs-2023-zscore-mr` +
`pairs-2024-h1-zscore-mr`, both present in `anchors.toml`. No longer a gap.

The remaining shipped trace rows with empty `anchors` arrays are UI /
cockpit / lab-infrastructure features with no backtest scenario to lock —
not strategy gaps.

### Trace coverage for the new program wave

- `carry-strategy`, `time-series-momentum-robustness`,
  `horizon-retest-robustness`, `perp-basis-signal-robustness`,
  `perp-basis-mn-spread` — **all five now have `[[req]]` rows**
  (`REQ-CARRY-STRATEGY-001`, `REQ-TIME-SERIES-MOMENTUM-ROBUSTNESS-001`,
  `REQ-HORIZON-RETEST-ROBUSTNESS-001`, `REQ-PERP-BASIS-SIGNAL-ROBUSTNESS-001`,
  `REQ-PERP-BASIS-MN-SPREAD-001`). Trace hygiene for the wave is GOOD —
  it is only the feature.md status mirror that lagged (see § Status drift).
- `carry-funding-data-backfill` (`dev-done`) — **still no `[[req]]` row.**
  Carry-over P2 from audit-2026-06-01. Owner: orchestrator (add
  `REQ-CARRY-FUNDING-DATA-BACKFILL-001`, 14 unit-test paths).

## Orphan check

- `spec/_probe_lint_test/` — orphan probe folder, **now 26 days old**
  (dir mtime 2026-05-13; was 20 d at audit-2026-06-01). Pending delete.
  Carry-over P3. Owner: orchestrator.
- `spec/cockpit-app-bundle/` — missing `tasks.md` (candidate / pre-developer
  — expected, not a defect).
- `spec/lumen-design-adoption/` — missing `tasks.md` (roadmap umbrella —
  expected).
- Test reports for missing slugs: none found.
- Crates not mentioned in any spec: not re-verified this run (carry-over;
  never flagged as a problem in any interim audit).

## Resolved / changed since audit-2026-06-01

| 06-01 finding                                                       | Status now    | Evidence                                                                      |
|---------------------------------------------------------------------|---------------|-------------------------------------------------------------------------------|
| P1 — MR `feature.md` status `tested` → `tester-done`                | **RESOLVED**  | commit `400a09e`; missing-frontmatter lint count 1 → 0.                        |
| P1 — `11-regression-gate.md` Phase-1A anchor table stale            | **PARTIAL**   | Caveat added (live count noted) BUT hard-codes 87; live is 119. See SC-B.      |
| P2 — `v15a-mean-reversion-pairs` anchor disposition                 | **RESOLVED**  | Anchored via `REQ-V15A-PAIRS-001` (2 scenarios in anchors.toml).               |
| P2 — `carry-strategy` trace row missing                             | **RESOLVED**  | `REQ-CARRY-STRATEGY-001` present.                                              |
| P1 — `backlog.md` `updated:` stale                                  | **OPEN — REGRESSED** | Still `updated: 2026-05-29`; now 10 days + 45 commits since last audit alone. |
| P2 — `carry-funding-data-backfill` trace row missing                | OPEN          | Still 0 rows; status now `dev-done`.                                           |
| P2 — `v2-llm-strategy` anchor disposition undocumented              | OPEN          | Carry-over.                                                                    |
| P2 — `product.md` Week-2 `bps:2` vs v5 canonical                    | OPEN          | Carry-over. See SC-C.                                                          |
| P2 — ADR-0043 § D2 RNG ChaCha20 vs Murmur3                          | OPEN          | Carry-over. See SC-A.                                                          |
| P3 — `_probe_lint_test/` delete                                     | OPEN          | Now 26 days.                                                                   |
| P3 — Pin Python 3.11+ for weekly lint                               | OPEN          | `tomli` shim still required.                                                   |
| P3 — Residual dead-link clusters (87)                               | OPEN          | Unchanged.                                                                     |
| P3 — `architecture.md` inline ADR table gaps                        | OPEN          | Not re-verified; carry-over.                                                   |

## Recommended triage

- **P1 — Advance the 5 drifted feature.md statuses AND finally land the
  enforcement hook.** Flip `carry-strategy`, `horizon-retest-robustness`,
  `time-series-momentum-robustness`, `perp-basis-signal-robustness`,
  `perp-basis-mn-spread` from their lagging `arch-done`/`tester-done` to
  `presenter-done` (carry-strategy → retired/closed). This is the 4th
  consecutive recurrence and the first to hit five features at once. The
  trace rows and anchors were maintained correctly every cycle — only the
  feature.md mirror lagged — which is exactly the signature that should be
  mechanically enforced. **Add a pre-commit / CI check** (sibling to
  `adr_registry_check.py` / `queue_staleness_check.py`) that asserts: if a
  feature has a `presentations/*.md` deck AND a PASS `reports/test-*.md`,
  its `feature.md` status must be ≥ `presenter-done`. Owner: orchestrator.

- **P1 — `backlog.md` frontmatter `updated:` 10 days stale.**
  `spec/backlog.md` still `updated: 2026-05-29` despite 45 commits since
  the last audit alone (and the full carry/TS/horizon/perp-basis wave +
  ~5 ships unrecorded). Flagged P1 in the last THREE audits and getting
  worse (2 d → 3 d → 10 d). Same recurring "every-ship-should-bump"
  workflow miss as the status drift above; fold both into the enforcement
  hook. Owner: orchestrator.

- **P1 — `11-regression-gate.md` live-count caveat already stale (87 vs
  live 119).** `spec/architecture/11-regression-gate.md:62`. The caveat
  added last week hard-codes a number that re-rots on every anchor wave;
  +32 anchors landed this week and the doc now understates by 27%. Replace
  the inline count with a pure pointer to `verify_anchors.sh`. Owner:
  architect. (See SC-B.)

- **P2 — `carry-funding-data-backfill` trace row missing** (status
  `dev-done`, 0 `[[req]]` rows). Add `REQ-CARRY-FUNDING-DATA-BACKFILL-001`
  with the 14 unit-test paths. Owner: orchestrator.

- **P2 — `v2-llm-strategy` shipped without anchor disposition note**
  (carry-over). Add a one-paragraph "not-anchorable: LLM non-determinism"
  note in `feature.md`, or lock a fixture-replay anchor. Owner: tester →
  architect.

- **P2 — ADR-0043 § D2 RNG body (ChaCha20) vs Changelog (Murmur3)**
  (carry-over SC-A). One-line body note. Owner: architect.

- **P2 — `product.md` Week-2 `bps:2` vs v5 canonical `bps:8`**
  (carry-over SC-C). One-line cross-reference. Owner: analyst.

- **P3 — `_probe_lint_test/` orphan, 26 days.** Delete. Owner: orchestrator.

- **P3 — `lumen-design-adoption` decay markers regressed 8 → 11**
  (DECAY-HEAVY). `cockpit-performance-and-input-responsiveness` flat at 6.
  Owner: phase owners.

- **P3 — Residual dead-link clusters (87, unchanged)** and **Python 3.11+
  pin for the lint** — both long-standing carry-overs; neither blocks the
  audit (shim works). Owner: developer (link sweep, excluding anchored
  report files per ADR-0038 § D6) / ops (Python).

## Changelog

- 2026-06-08 (spec-auditor): Weekly scheduled audit. 45 commits since
  audit-2026-06-01. Anchors 119/119 PASS (+32: carry #88/89, TS-momentum
  #90/91, horizon-retest #92–99, perp-basis 99→107, perp-basis-mn
  107→119). Mechanical lint 94/2-cat (−1 vs 95/3-cat): missing-frontmatter
  cleared (MR status flipped `tested`→`tester-done`, last audit's P1
  RESOLVED). **HEADLINE: 5 feature.md files status-drifted** — carry-strategy,
  horizon-retest-robustness, time-series-momentum-robustness,
  perp-basis-signal-robustness, perp-basis-mn-spread all reached presenter
  `PRESENTATION → READY` but feature.md still reads arch-done/tester-done
  (4th consecutive recurrence of the tester-PASS handoff drift; trace rows
  + anchors were maintained, only the feature.md mirror lagged). backlog.md
  `updated:` 10 days stale (P1, 3rd consecutive, regressing). 11-regression-
  gate.md caveat added last week but already stale (hard-codes 87, live
  119 — SC-B). Soft sweep: money-math / timestamps / sub-agent boundary
  CONSISTENT; RNG (SC-A) + bps (SC-C) carry-over contradictions persist.
  v15a + carry-strategy anchor/trace gaps RESOLVED; carry-funding-data-
  backfill trace row still missing. _probe_lint_test orphan now 26 days.
  Recommended a pre-commit enforcement hook for status-flip + backlog-bump.
