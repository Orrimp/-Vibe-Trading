---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-06-29
---

# Spec Audit — 2026-06-29 (weekly cadence)

## Headline

The spec tree is **mechanically clean but carries fresh trace drift**: `spec_lint.py`
is PASS (0 violations) and anchors hold 119/119, but **three features that closed to
`status: shipped` in `feature.md` this week still carry pre-ship `state=` values in
`trace.toml`** (`advisor-param-tuning` → `in-progress`, `advisor-param-promotion` →
`design-complete`, `advisor-signal-library-expansion` → `implemented`). This is the
classic auditor catch the lint cannot see — `feature.md` and `trace.toml` disagree on
lifecycle state. No hard contradictions, no stale (proposed/in-progress >30d) features,
no decay-heavy feature folders. The repo also reorganized into `spec/v1` (shipped) +
`spec/v2` (research-driven) and stood up a 900-paper research knowledge base since the
last audit.

## Run metadata

- Date: 2026-06-29 (scheduled Monday cadence).
- Baseline: `docs/dev-notes/audit-2026-06-22-post-b1.md` (most recent audit on disk,
  written 2026-06-22 19:02). That was an on-demand post-B1 focused run; this is the
  first full scheduled run since `audit-2026-06-22.md` (07:12) and the post-B1 note.
- Commits since baseline: ~40 (`git log --since='2026-06-22 19:02'`). Dominant themes:
  (1) the `spec/v1`+`spec/v2` reorg (`1395520`); (2) the research-driven v2 phase
  (`5152202`, `1f4cd3a`, 900/900-paper knowledge base across 9 topics — `b7c18d1`);
  (3) new advisor features: `advisor-param-tuning` (#3), `advisor-param-promotion`,
  `advisor-signal-library-expansion`, `advisor-short-selling`, `advisor-combination-search`,
  `advisor-crossasset-macro-regime`, `advisor-options-impliedvol-probe`,
  `advisor-overfitting-scorecard` (v2, report-only).
- Tooling: `spec_lint.py` requires Python 3.11+ (`tomllib`); the workspace ships 3.10
  and `uv` could not fetch a managed interpreter (network-restricted). Ran the lint
  under a `tomli`-as-`tomllib` shim — result is identical to the script's own logic.
  This is a run-environment note, not a finding.

## Mechanical lint

`scripts/spec_lint.py --all` — **PASS (0 violations).**

| Category            | This run | Previous (post-b1, reconciled) | Δ  |
|---------------------|----------|--------------------------------|----|
| dead-link           | 0        | 0                              | 0  |
| trace-broken-path   | 0        | 0                              | 0  |
| missing-frontmatter | 0        | 0                              | 0  |
| shipped-no-tests    | 0        | 0                              | 0  |
| orphan-feature      | 0        | 0                              | 0  |
| **TOTAL**           | **0**    | **0**                          | 0  |

The byte-immutable ADR-0038 dead-link floor that headlined every audit from 2026-06-08
remains closed — it was resolved in `691e2aa` via the `KNOWN_FROZEN_DEAD_LINKS`
allowlist (the post-b1 baseline's snapshot still showed FAIL(1) because it predated the
fix; the orchestrator triage at the bottom of that note confirms the resolution). No
new lint violation introduced by the ~40 commits this week.

## Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged from baseline.
Every new advisor feature this week runs the bake-off path with `write_report=false`
(ADR-0066/0067/0068/0071 anchor-safety-by-construction), so no new anchored report
body was emitted and the SHA set is byte-stable. `anchors.toml` still holds 119
`[[anchors]]` entries (count confirmed).

## Feature inventory

131 `feature.md` files in scope (excluding `design/`, `dev-notes/`, `runbooks/`,
`archive/`, `architecture/`). Up from the 127 reported at the last audit (Δ +4: the
new advisor features, less any reorg churn).

Status distribution:

| Status          | Count |
|-----------------|-------|
| shipped         | 100   |
| presenter-done  | 7     |
| tester-done     | 5     |
| retired         | 5     |
| deprecated      | 4     |
| dev-done        | 3     |
| candidate       | 2     |
| arch-done       | 1     |
| in-progress     | 1     |
| roadmap         | 1     |
| draft           | 1     |
| shipped-partial | 1     |

The repo reorganized features into `spec/v1/<slug>/` (shipped product, archive) and
`spec/v2/<slug>/` (new research-driven phase). Only one v2 feature exists so far:
`advisor-overfitting-scorecard` (status `dev-done`, report-only P0-1).

## Stale features

**Zero** features match the strict staleness rule (`status: proposed` OR `in-progress`,
not updated in 30+ days). The single `in-progress` feature is `cockpit-cross-platform`
(updated 2026-06-15, 14 days) — under the 30-day bar.

### Aging watch items (NOT strict-stale, carry-over — unchanged this week)

These are `candidate` / `draft` / `roadmap` states the strict rule does not flag, but
they have not moved in 6–8 weeks:

| Slug                       | Status    | Updated    | Age  | Notes                              |
|----------------------------|-----------|------------|------|------------------------------------|
| lumen-design-adoption      | roadmap   | 2026-05-04 | 56d  | umbrella roadmap; expected lag     |
| cockpit-app-bundle         | candidate | 2026-05-11 | 49d  | no movement (was 42d last audit)   |
| iced-ecosystem-evaluation  | candidate | 2026-05-13 | 47d  | no movement (was 40d last audit)   |
| ui-gallery-table-cell      | draft     | 2026-05-16 | 44d  | no movement (was 37d last audit)   |

All four aged +7d since the last audit with no state change. Carry-over; park or advance.

## Spec/code status divergence (NEW — primary finding this week)

`feature.md status:` and `trace.toml state=` **disagree** for three features that
closed this week. In every case the `feature.md` advanced to `shipped` but the
`trace.toml` row was never flipped forward from its mid-pipeline value. The lint does
not compare these two fields, so it stays green.

| Feature                          | feature.md status | trace.toml state   | Lag        | Test report on disk |
|----------------------------------|-------------------|--------------------|------------|---------------------|
| advisor-param-promotion          | shipped           | design-complete    | ~3 steps   | YES (test-2026-06-26-0900) |
| advisor-param-tuning             | shipped           | in-progress        | ~1 step    | YES (test-2026-06-26-0900) |
| advisor-signal-library-expansion | shipped           | implemented        | ~1 step    | YES (test-2026-06-26-1856) |

Evidence:
- `advisor-param-promotion` — `feature.md:status: shipped`; closed via `2080b21`
  (status → presenter-done) + ship. `trace.toml:3207 state="design-complete"`
  (architect 2026-06-25 handoff, ADR-0070). Three lifecycle steps behind.
- `advisor-param-tuning` — `feature.md:status: shipped`; closed via `dd1daed`
  (T7b composed-families ON + CHANGELOG close). `trace.toml:3185 state="in-progress"`
  (developer 2026-06-25, "phase 1 T1–T5 complete … HANDOFF → tester").
- `advisor-signal-library-expansion` — `feature.md:status: shipped`; closed via
  `795cdd2` ("proper close — tester report + ship + compress"). `trace.toml:3218
  state="implemented"` (developer 2026-06-26, "HANDOFF → tester").

Severity: medium. All three are genuinely shipped (test reports present, lint
`shipped-no-tests=0`), so the *code* is sound — the gap is purely that `trace.toml`
was not advanced when the features closed. This is exactly the cross-file drift the
auditor exists to surface. Route to the owning agent (tester/orchestrator) to flip the
three `state=` values to `shipped` (and populate `tests=`/`anchors=` per the post-close
reality). Do NOT touch `feature.md`.

## Anchor / test-wiring coverage gaps

Per the procedure, shipped strategy features with no anchor citation in `trace.toml`
are flagged. 11 strategy-class features carry `anchors=[]`. **Most are legitimate** —
they are bake-off arms that run the `write_report=false` advisor path against existing
anchored scenarios (anchor-safe by construction; ADR-0066/0067/0068/0071) and carry a
CLAUDE.md-mandated day-1 baseline-equity-divergence e2e test *instead of* a new anchor.
Verified the e2e wiring for the new arm-class features:

| Feature                          | trace anchors | trace #tests | day-1 divergence e2e |
|----------------------------------|---------------|--------------|----------------------|
| advisor-combination-search       | []            | 6            | YES (wired)          |
| advisor-signal-library-expansion | []            | 5            | YES (wired)          |
| advisor-options-impliedvol-probe | []            | 5            | YES (wired)          |
| advisor-crossasset-macro-regime  | []            | 3            | YES (wired)          |
| advisor-param-tuning             | []            | 3            | YES (wired)          |
| **advisor-short-selling**        | **[]**        | **0**        | **on disk, NOT wired in trace** |
| advisor-param-promotion          | []            | 0            | none (engine seam; relies on param-tuning's test) |
| advisor-overfitting-scorecard    | [] (v2)       | 0            | none (report-only P0-1; no equity surface) |

### SS-WIRE — `advisor-short-selling` e2e exists on disk but `trace.tests=[]` (NEW, P2)

`REQ-ADVISOR-SHORT-SELLING-001` has `tests=[]` and `anchors=[]`, yet the day-1
divergence harness **exists on disk**: `crates/backtest/tests/short_selling_divergence_e2e.rs`
(plus `short_enabled_byte_identity.rs`, `short_bakeoff_bear_bull.rs`, and three UI
render tests). `feature.md` § Day-1 e2e (R-SS.5) documents it as "LOAD-BEARING,
CLAUDE.md NON-NEGOTIABLE". So the verification floor is satisfied — the gap is that
the trace row's `tests=` array was never populated. Same pattern as the historic F8
`tests=[]` P1 (resolved in a prior audit). Severity: low-medium — the test is real and
passing; the trace just doesn't cite it. Note also the close commit (`fd819bd`) flags
"`always_short` = gate blind spot" as a known honest limitation, not a defect.

Confirmed there are **no orphan trace rows** (every `trace.toml feature=` slug maps to
an existing `spec/<slug>/feature.md`).

## Trace coverage gaps (carry-over, low severity)

13 in-scope `feature.md` slugs have no `[[req]]` row in `trace.toml` (same count as
the last audit; composition shifted as the reorg moved files into `v1/`):

```
chart-fixture-line-clipping (shipped)        cockpit-toast-queue (shipped)
cockpit-toast-queue-v0.2.0-cleanup (shipped) leaderboard-timeframe-capital (shipped)
paper-soak-longevity (shipped)               ui-drop-iced-aw (shipped)
ui-gallery-bin (shipped)                     ui-gallery-table-cell (draft)
ui-headless-emulator (shipped)               ui-session-journal-iced-tester (shipped)
v5-latency-slippage-sim (shipped)            v5-latency-slippage-sim-v0.2.0-anchor-migration (shipped)
v5-latency-slippage-sim-v0.3.0-full-path-wiring (shipped)
```

Mostly UI-infrastructure features the spec-compression convention intentionally left
un-traced, plus the `v5-latency-slippage-sim` version-bump folders. The most material
new entrant is `leaderboard-timeframe-capital` (shipped 2026-06-26, a tuning-knob
feature) — worth a one-line REQ if the operator wants the cross-reference. No lint
violation (`orphan-feature=0`). Low severity.

## Decay markers (TODO / FIXME / TBD / XXX / ???)

**No feature folder exceeds the 5-marker threshold.** Counts by folder (excluding
`spec/archive/`):

| Folder                         | Count | Flag                                          |
|--------------------------------|-------|-----------------------------------------------|
| dev-notes (NOT a feature)      | 43    | self-reference: audit notes quote the marker patterns literally; exclude |
| lumen-design-adoption          | 4     | below threshold (was 4 last audit)            |
| architecture (section files)   | 2     | phantom-`models` carry-over context           |
| v1/ui-rethink-phase-a-lab      | 2     | benign                                        |
| ui-design-principles.md        | 1     | benign                                        |
| v1/advisor-llm-narration       | 1     | single TBD                                    |
| all others                     | 0–1   | benign                                        |

The 43 in `dev-notes` are an artifact of the audit notes themselves spelling out the
`TODO/FIXME/TBD/XXX/???` grep pattern — not real decay. No feature is decay-heavy.

## Soft contradictions (LLM-judged — verify before acting)

Swept 5 cross-cutting topics from CLAUDE.md non-negotiables against `product.md` and
`architecture.md`. **No hard contradiction found.** Confidence notes below.

### SC-1 — Money math (`Decimal`, never `f64`) — CONSISTENT (CONFIDENCE HIGH)

`product.md:285` "Money math uses `Decimal`, never `f64` (every monetary value is
`Money<C>`)"; `product.md:220` "all sizing are `Money<Usdt>`; no `Eur` currency type
exists". The shipped `advisor-eur-fx` feature does NOT contradict this — `product.md:594`
reconciles it explicitly ("`Money::<Usdt>::from_decimal` stamps euros as USDT 1:1
today"). No opposing claim in `architecture.md`. No action.

### SC-2 — Determinism / seeded reproducibility — CONSISTENT (CONFIDENCE HIGH)

`product.md:117` "the bake-off seed … all reproducible from the recorded seed + window";
`product.md:277` "byte-identical (the bake-off *reads* the engine; it must not perturb
anchored …)". Aligns with the 119/119 anchor gate and `architecture.md` render/anchor
language. No contradiction.

### SC-3 — UI render-pixel verification — CONSISTENT (CONFIDENCE HIGH)

`architecture.md:189/236/269` consistently require "render-pixel guards" / "FAIL-before
render-pixel proofs" for new UI surfaces, matching the CLAUDE.md Iced-UI non-negotiable
(verify at the rendered-PIXEL layer, not no-panic boot). No opposing claim.

### SC-4 — `anchors.toml` header comment says `87/87` — actual 119/119 — STALE COMMENT (CONFIDENCE HIGH, carry-over, severity low)

`spec/anchors.toml:23` still reads "expects 87/87 PASS — 2026-05-31". Actual entry
count is 119 and `verify_anchors.sh` reports `119 / 119`. This is the same SC-E the
post-b1 audit flagged and the orchestrator **DEFERRED** ("fix on the next anchor-touching
commit; the comment is a dated 2026-05-31 historical narrative"). Still open. Cosmetic —
`verify_anchors.sh` does not read the comment. No functional impact.

### SC-5 — Phantom `crates/models` in architecture section files — CARRY-OVER (CONFIDENCE HIGH, severity low)

`architecture.md` self-flags (line ~39/243) that the stale `models` placeholder lingers
in `00-overview.md`, `01-data-flow.md`, `06-ui-and-cockpit.md`, `12-forecast-overlay.md`.
`crates/models` does not exist. Pre-registered architect doc-sweep item; DEFERRED at the
last audit. Documentation-only, no code discrepancy. No new movement.

## Resolved / improved since baseline

| Finding (baseline)                                | Status now      | Evidence                              |
|---------------------------------------------------|-----------------|---------------------------------------|
| dead-link FAIL(1) ADR-0038 floor                  | **RESOLVED**    | `691e2aa` allowlist; lint PASS(0)     |
| ADR-0066 absent from architecture.md Changelog (SC-C) | **RESOLVED** | fixed in baseline's own triage commit |
| F8/F9 tester-loop traceability (older P1s)        | RESOLVED (prior)| confirmed still shipped+traced        |

## Recommended triage

- **P1 — Flip the 3 lagging `trace.toml` rows to `shipped`.** `advisor-param-promotion`
  (`design-complete`→shipped), `advisor-param-tuning` (`in-progress`→shipped),
  `advisor-signal-library-expansion` (`implemented`→shipped). All three closed in
  `feature.md` this week with test reports on disk; the trace row is the only stale
  surface. Populate `tests=`/`anchors=` to match the closed state. Owner: tester /
  orchestrator. (Audit is read-only — this note does not touch trace.toml.)

- **P1 — `cockpit-cross-platform` CI gate (carry-over, unchanged).** Still `in-progress`
  (14d), still operator-deferred. Cross-platform claim unverified off-macOS for a 4th
  consecutive audit (2026-06-08/15/22/29). Owner: operator → tester.

- **P2 — Wire `advisor-short-selling` e2e into trace (`SS-WIRE`).** `tests=[]` despite
  `crates/backtest/tests/short_selling_divergence_e2e.rs` existing and passing. Populate
  `REQ-ADVISOR-SHORT-SELLING-001 tests=`. Owner: tester. Re-confirm the `always_short`
  "gate blind spot" noted at close is an accepted honest limitation, not a TODO.

- **P2 — Approve or return the 7 `presenter-done` features.** Carry-over; all carry PASS
  verdicts and are accumulating un-ticked approvals. Owner: operator.

- **P3 — `leaderboard-timeframe-capital` trace gap.** Shipped 2026-06-26, no `[[req]]`
  row. Add a one-line REQ if the operator wants the cross-reference. Owner: analyst.

- **P3 — `anchors.toml` header comment stale (`87/87`→`119/119`, SC-4).** Carry-over,
  DEFERRED. Cosmetic; update on the next anchor-touching commit. Owner: next tester/dev.

- **P3 — Phantom `crates/models` in 4 architecture section files (SC-5).** Carry-over,
  DEFERRED. Documentation-only sweep. Owner: architect/developer.

- **P3 — Aging watch items.** `lumen-design-adoption` (56d), `cockpit-app-bundle` (49d),
  `iced-ecosystem-evaluation` (47d), `ui-gallery-table-cell` (44d). All +7d, no movement.
  Park or advance. Owner: analyst / operator.

- **P3 — 13 trace coverage gaps (UI infra + `v5-latency-slippage-sim` version folders +
  `paper-soak-longevity`).** Mostly intentional spec-compression convention. No action
  unless the operator wants explicit REQ rows for every shipped feature. Owner: analyst.

## Deltas vs baseline (numerical)

| Metric                              | This run | Baseline (post-b1) | Δ        |
|-------------------------------------|----------|--------------------|----------|
| Lint violations (total)             | 0        | 0 (reconciled)     | 0        |
| Anchors PASS                        | 119/119  | 119/119            | 0        |
| Features in scope                   | 131      | 127                | +4       |
| Strict-stale (proposed/in-prog >30d)| 0        | 0                  | 0        |
| Aging watch items                   | 4        | 4                  | 0 (+7d each) |
| Trace coverage gaps                 | 13       | 13                 | 0        |
| Decay-heavy feature folders (>5)    | 0        | 0                  | 0        |
| NEW: feature.md↔trace status drift  | 3        | 0                  | +3       |
| Hard contradictions                 | 0        | 0                  | 0        |

## Changelog

- 2026-06-29 (spec-auditor): Weekly scheduled audit. ~40 commits scoped since the
  2026-06-22 post-b1 baseline (spec/v1+v2 reorg, 900-paper v2 research base, 8 new
  advisor features). HEADLINE: lint PASS(0), anchors 119/119, but NEW finding — 3
  features (`advisor-param-promotion`, `advisor-param-tuning`,
  `advisor-signal-library-expansion`) closed to `status: shipped` in feature.md while
  their trace.toml `state=` rows still read design-complete/in-progress/implemented
  (P1 flip). NEW P2: `advisor-short-selling` e2e divergence test exists on disk but
  `trace.tests=[]`. Zero strict-stale features; no decay-heavy folder; no hard
  contradiction. Carry-overs unchanged: cockpit-cross-platform CI (P1, 4th audit),
  7 presenter-done awaiting approval (P2), anchors.toml 87/87 header comment (P3,
  deferred), phantom crates/models in 4 section files (P3, deferred), 4 aging watch
  items (+7d), 13 trace coverage gaps. RESOLVED since baseline: ADR-0038 dead-link
  floor (691e2aa allowlist → lint PASS), ADR-0066 architecture.md Changelog entry.
