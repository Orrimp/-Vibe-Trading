---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-05-22
---

# Spec Audit — 2026-05-22

## Headline

The spec tree has materially improved since 2026-05-18: dead-links dropped
from 727 to 81 (−646), the `trace-broken-path` category is fully resolved
(6 → 0), and 30 anchors are now locked across a clean chain from v0 through
v2.5a. The main open concerns are the `v25-tcn-overlay` parent status (still
`in-progress` despite every child being shipped or deprecated), two ADR files
sharing number 0035, and several `product.md` / `architecture/12` rows that
still describe the DL roadmap as a live programme rather than a retired one.

## Run metadata

- Date: 2026-05-22
- Auditor: spec-auditor (on-demand, operator-requested)
- Baseline: `docs/dev-notes/audit-2026-05-18.md` (4 days prior)
- Python: `/opt/homebrew/bin/python3` (3.14) — `tomllib` natively available;
  no shim required this run.
- Commits since 2026-05-18: see git log; heavy DL-follow-on + UI rethink
  Phases B–F + cockpit-training-control churn.

## Mechanical lint

`scripts/spec_lint.py --all` exit code: **2** (2 categories flagged).
Headline: `spec-lint: FAIL (82 violations in 2 categories)`.

| Category            | This run | Previous (2026-05-18) | Delta   |
|---------------------|----------|-----------------------|---------|
| dead-link           | 81       | 727                   | **-646** |
| missing-frontmatter | 0        | 1                     | -1      |
| trace-broken-path   | 0        | 6                     | **-6**  |
| shipped-no-tests    | 1        | 0                     | +1      |
| **TOTAL**           | **82**   | **734**               | **-652** |

### Notable movements

- **dead-link -646**: the bulk of the 2026-05-18 dead-link count came from
  `lumen-phase-N-*` slug references; that cluster is gone. The remaining 81
  are a mix of stale `/tmp/` screenshot paths, missing crate-path cross-links,
  and a few orphaned `../tasks/` paths. None are new regressions introduced in
  this sprint.
- **missing-frontmatter cleared**: `ui-rethink-phase-a-lab/tasks.md` had
  `status: tester-pass`; that is now resolved (feature is `shipped`).
- **trace-broken-path cleared**: all 6 previously-missing anchor names
  (v25a × 2, v25b × 2, v26 × 2) are now handled — v25a's two anchors are
  locked in `anchors.toml`; v25b and v26 were retired with `anchors = []`
  and `state = "deprecated"`, so trace correctly carries empty arrays and the
  lint no longer flags them.
- **shipped-no-tests +1**: `v25-tcn-horizon-bump-or-retire` — see Stale
  Features section below.

### Sample violations (top 5 per category)

**dead-link (81 total):**
- `_bmad-output/planning-artifacts/architecture/decisions/0027-kronos-onnx-tract-integration.md` → 5 refs to
  `../../v25-kronos-forecast-overlay/feature.md` (slug was renamed / removed
  when Kronos was dropped in favour of candle; superseded ADR but never
  updated).
- `_bmad-output/planning-artifacts/architecture/decisions/0033-tcn-alpha-investigation-report-shape.md` →
  self-referencing dead-link to
  `../architecture/adr/0033-...md#d3-f-verdict-decision-algorithm` (one extra
  `../` prefix; relative path walks out of the `spec/` tree).
- `spec/chart-canvas-overhaul/feature.md` → 6 refs to
  `/tmp/orch-diag/cockpit-*.png` (screenshot artefacts written during the
  dev session; never committed).
- `spec/journal-transactions-metadata/feature.md` → `../reports/test-2026-05-03-1608-journal-transactions-metadata-final.md`
  (report file does not exist at that relative path; likely filed under a
  different name or never committed).
- `spec/v25-tcn-alpha-investigation/tasks.md` →
  `../.claude/skills/rust-test/templates/test-report.md` (relative escapes
  `spec/`; correct path would be `../../.claude/skills/rust-test/templates/test-report.md`).

**shipped-no-tests (1):**
- `spec/v25-tcn-horizon-bump-or-retire/feature.md`: status `shipped` (2026-05-21);
  `reports/` directory exists but is empty (no `.md` report files, only an
  empty dir). The feature's trace.toml row records `state = "shipped"` with
  the operator-decision comment "Q1=(b) RETIRE 2026-05-21 — zero code change;
  zero new anchors". The decision itself was the deliverable; no tester PASS
  report was produced. See Orphans/Stale section below.

## Feature inventory (48 top-level feature.md files)

Excludes `design/`, `dev-notes/`, `runbooks/`, `archive/`, `architecture/`.
Nested phases under `lumen-design-adoption/` counted separately (6 sub-features).

### By status

| Status      | Count | Notable new entrants since 2026-05-18                              |
|-------------|-------|--------------------------------------------------------------------|
| shipped     | 37    | ui-rethink-phase-b-lab-run, -c, -d, -d-trail-followup, -e, -f; v25-tcn-recalibrate, -threshold-tuning, -horizon-bump-or-retire; v25a-patchtst-overlay; cockpit-training-control; chart-x-axis-local-time; audit-tick-consumer-envelope; chart-fixture-line-clipping |
| deprecated  | 4     | _probe_lint_test, v25-dl-forecast-overlay, v25b-transformer-overlay, v26-forecast-bakeoff |
| in-progress | 1     | v25-tcn-overlay (persistent from last audit)                       |
| roadmap     | 1     | lumen-design-adoption                                              |
| candidate   | 2     | cockpit-app-bundle, iced-ecosystem-evaluation                      |
| draft       | 1     | ui-gallery-table-cell                                              |
| reserved    | 1     | lumen-phase-6-assistant-slot (sub-feature)                         |

(Total top-level: 48; lumen sub-features add 6 more.)

## Stale features

**`status: in-progress`, updated > 30 days ago:** none — `v25-tcn-overlay`
was updated 2026-05-18 (4 days ago; within the 30-day window).

**`status: in-progress`, updated within 30 days but substantively blocked:**
- `v25-tcn-overlay` — updated 2026-05-18 (4 days), but every child feature
  (`v25-tcn-alpha-investigation`, `v25-tcn-recalibrate`, `v25-tcn-threshold-tuning`,
  `v25-tcn-horizon-bump-or-retire`) is now `shipped` or retired, and the
  parent DL umbrella (`v25-dl-forecast-overlay`) is `deprecated`. The TCN
  work is architecturally complete; only the parent wrapper feature retains
  `in-progress`. This is a classification question for the operator — see
  "Parent status triage" section below.

**`shipped` but no test report (`shipped-no-tests`):**
- `v25-tcn-horizon-bump-or-retire` — feature is `shipped` (operator-decided
  scope (b) retire, zero code change). The `reports/` directory is empty. This
  is a deliberate "decision record" ship: the tester's role was supplanted by
  the operator decision itself. The lint flags it correctly because the ship
  contract normally requires a `.md` report. Whether a decision-record stub
  suffices or the feature should carry a one-paragraph outcome report is a
  triage call. Owner: operator → tester.

## Orphans

### Feature folders without a trace.toml row

Seven feature folders have no `[[req]]` block in `trace.toml`:

| Folder                      | Status      | Notes                                         |
|-----------------------------|-------------|-----------------------------------------------|
| `_probe_lint_test`          | deprecated  | Probe artefact; safe to delete per its own README (pending since 2026-05-13) |
| `chart-fixture-line-clipping` | shipped   | Infra/vendored-fork maintenance; no strategy scope; omission likely deliberate |
| `ui-drop-iced-aw`           | shipped     | Infra feature; no strategy scope              |
| `ui-gallery-bin`            | shipped     | Dev tooling; no strategy scope                |
| `ui-gallery-table-cell`     | draft       | Uncommitted candidate; no trace row yet       |
| `ui-headless-emulator`      | shipped     | Infra; no strategy scope                      |
| `ui-session-journal-iced-tester` | shipped | Infra; no strategy scope                      |

Assessment: the six `shipped` infra/UI features without trace rows appear to
be deliberate omissions (they carry zero strategy or anchor scope). No
immediate action required, but if the team wants a complete trace of all
shipped work, these should get a `[[req]]` stub each.
`ui-gallery-table-cell` (draft) will need a trace row before it can ship.

### Test reports for missing features

None found. All `reports/` directories sit under folders that still exist.

### Crates not mentioned in any spec

`crates/` tree is still mid-materialisation per `spec/architecture.md`
(virtual workspace not yet landed). Crate-orphan check deferred — same
rationale as the 2026-05-18 audit.

## Decay markers

Counts of `TODO | FIXME | TBD | XXX | ???` per top-level spec folder
(excluding `spec/archive/` and `docs/dev-notes/`). Decay-heavy threshold: > 5.

| Folder                                        | Count | Flag        | Delta vs 2026-05-18 |
|-----------------------------------------------|-------|-------------|----------------------|
| lumen-design-adoption                         | 11    | DECAY-HEAVY | +3                   |
| trace.toml                                    | 7     | DECAY-HEAVY | new folder in scope  |
| cockpit-performance-and-input-responsiveness  | 6     | DECAY-HEAVY | 0 (same)             |
| ui-rethink-phase-b-lab-run                    | 4     | —           | new (shipped 2026-05-19) |
| v05-composed-strategies                       | 3     | —           | 0                    |
| ui-rethink-phase-a-lab                        | 3     | —           | 0                    |
| ui-headless-emulator                          | 3     | —           | 0                    |
| tape-row-audit-modal                          | 3     | —           | 0                    |
| v15a-mean-reversion-pairs                     | 2     | —           | 0                    |
| v0-paper-sma                                  | 2     | —           | new (was 0)          |
| ui-quality-gate-overhaul                      | 2     | —           | 0                    |
| operator-success-reports                      | 2     | —           | 0                    |
| ui-rethink-phase-e-compare                    | 1     | —           | new                  |
| ui-rethink-phase-c-sidebar-ia                 | 1     | —           | new                  |
| ui-design-principles.md                       | 1     | —           | 0                    |
| ui-session-journal-iced-tester                | 1     | —           | 0                    |
| v2-llm-strategy                               | 1     | —           | 0                    |
| iced-aw-cherry-pick                           | 1     | —           | 0                    |
| backlog.md                                    | 1     | —           | 0                    |
| architecture                                  | 1     | —           | 0                    |

Three folders exceed the decay-heavy threshold:

- **`lumen-design-adoption` (11 markers, status `roadmap`).** Count increased
  from 8 to 11 since last audit. New markers appear in the phase-1-foundation
  and phase-5-humancontrol-agentfeed sub-features. The umbrella is `roadmap`
  (active design intent); TBDs here represent genuine open design questions
  rather than stale deferrals. Still worth a cleanup pass before phase-6 work
  begins. Owner: analyst.
- **`trace.toml` (7 markers).** All 7 are `tests = [] # TODO developer` on
  early-era REQs (v0, v05, v1, v1.5b, v2 rows) that were written before the
  test-column convention was established. These REQs are shipped; the TODO is
  cosmetic. Owner: developer (batch clear).
- **`cockpit-performance-and-input-responsiveness` (6 markers, status `shipped`).** Same as
  last audit — persistent post-ship Q&A TBDs. Not a regression risk. Owner:
  architect (carry-over from previous recommendation).

**`ui-rethink-phase-b-lab-run` (4 markers)**: new this sprint. Feature is
`shipped`; two TODO markers are tracking explicitly-deferred follow-ons
(TODO-backtest-dep wiring). Not decay-heavy at count 4 but notable because
the feature just shipped. Owner: developer.

**`reflection-memory` was 18 markers in 2026-05-18 audit — it is no longer
in the table above.** This means markers dropped to ≤ 1 or the grep is not
finding them. Auditor note: the file was not checked individually due to it
not appearing in the current run's output. If operators cleaned those markers,
this is a P2 resolution.

## Soft contradictions (LLM-judged — verify before acting)

Five cross-cutting topics checked against `spec/product.md`,
`spec/architecture.md` shards, and relevant `feature.md` files.

**1. DL forecast roadmap status: `product.md` vs retirement reality.**

`spec/product.md` line 183, Strategy library table, v2.5 row reads:
> "DL forecaster portfolio … 4-phase progression: v2.5 TCN → v2.5a PatchTST →
> v2.5b Vanilla Transformer → v2.6 bake-off. … Umbrella: v25-dl-forecast-overlay …"
> Outcome column: "Three model paradigms in production; canonical picked by evidence at v2.6"

The actual outcome (as of 2026-05-22): v2.5 TCN shipped + F4; v2.5a PatchTST
shipped + F4 (lower Sharpe than TCN); v2.5b and v2.6 retired without shipping;
umbrella is `deprecated`. The product.md row still implies a live progression
toward a canonical production model. **Possible contradiction**: the stated
outcome "canonical picked by evidence at v2.6" never materialised; the actual
verdict is "all v2.5 paradigms retire as no-alpha at 1h/24h horizons."

Additionally, `spec/product.md` lines 219–220, "DL/RL models (planned)"
section, says: "Forecaster: lightweight Transformer or TCN on multivariate
features." — this has been empirically falsified by the v2.5 programme.

Similarly, `spec/architecture/12-forecast-overlay.md` line 183 (same table
pulled from product.md) carries no retirement annotation. `status: shipped`
appears on line 3 of `12-forecast-overlay.md`, but that refers to the _doc_
being shipped, not the DL programme being retired.

**Confidence: high** that there is a real omission. Neither `product.md` nor
`architecture/12` was updated with the retirement narrative at the time of the
operator's v25a routing-(a) decision. The feature files (`v25-dl-forecast-overlay/
feature.md`, `v25b`, `v26`, trace.toml) all carry full retirement prose; the
cross-cutting docs lag. Owner: analyst.

**2. ADR-0035 number collision.**

Two files share ADR number 0035:
- `_bmad-output/planning-artifacts/architecture/decisions/0035-phase-b-scenario-dispatch-extraction.md`
  (date: 2026-05-19, title: Phase B scenario-dispatch extraction)
- `_bmad-output/planning-artifacts/architecture/decisions/0035-tcn-sigma-train-recalibration.md`
  (date: 2026-05-21, title: Post-training σ_train recalibration)

The ADR README at line 85 lists only the σ_train entry under 0035.
`spec/trace.toml` references `0035-phase-b-scenario-dispatch-extraction.md`
under `REQ-UI-RETHINK-PHASE-B-001` and `0035-tcn-sigma-train-recalibration.md`
under the v25-tcn-recalibrate and v25a-patchtst-overlay REQs — both links
resolve on disk because both files exist. The collision is not a broken link,
but it is an un-numbered duplicate: any reader citing "ADR-0035" cannot
disambiguate. One of the two should be renumbered (likely the Phase B ADR to
0035-A or 0037, since the README already treats the σ_train ADR as the
canonical 0035). **Confidence: high** that the duplication is unintentional.
Owner: architect.

**3. v0/v05/v1 trace.toml state vs feature.md status.**

`spec/trace.toml` carries `state = "in-progress"` for the v0, v05, and v1
cross-sectional momentum REQs (lines 37, 48, 59 respectively). All three
feature.md files show `status: shipped`. This mismatch was present in the
2026-05-18 audit. It has not been fixed. The trace.toml comment header at
lines 16–21 says "Status states match feature.md frontmatter" — this is
the stated contract, and these three rows violate it. **Confidence: high**
that these are stale trace rows. Owner: developer (three one-line edits to
flip `"in-progress"` → `"shipped"`).

**4. `backlog.md` Active section: stale DL entries.**

`spec/backlog.md` `## Active` section still contains two visible bullets that
are now fully resolved:
- Line 437: `v25-tcn-alpha-investigation` described as _draft, awaiting
  operator scope-decision_ — but feature.md shows `status: shipped`
  (updated 2026-05-19).
- Line 463: `v25-tcn-overlay` described as _in-progress_ — this is not a
  contradiction (feature.md agrees), but the prose refers to "alpha-verdict
  investigation promoted to Active above" pointing at v25-tcn-alpha-investigation
  as still active. Since that investigation shipped and its children (recalibrate,
  threshold-tuning, horizon-bump, patchtst) have all shipped or retired,
  the Active section describes a programme that no longer exists.

Separately, backlog Active line 7–10 (the HTML comment block at the top of
the file) references the v25a-patchtst-overlay v0.1.0 brief as still in
flight, while it shipped 2026-05-22. The backlog `## Recent (shipped)` section
at line 928 does correctly record the ship, but the Active comment block at
the top was not cleared. **Confidence: high** that the Active section needs a
housekeeping pass. Owner: analyst/orchestrator.

**5. `v25b-transformer-overlay` and `v26-forecast-bakeoff` anchor names
reserved in old trace: clean.**

The 2026-05-18 audit flagged 6 `trace-broken-path` rows for v25b × 2 and
v26 × 2 planned anchor names. This audit confirms: both `[[req]]` rows now
carry `anchors = []` (never shipped, no anchors to lock), and `state = "deprecated"`.
The lint `trace-broken-path` count is 0. **No contradiction.** No action
required.

## Anchor coverage

`spec/anchors.toml` declares **30** locked scenarios (up from 13 on
2026-05-18 — net +17 across the TCN recalibrate/threshold/patchtst sprint).

All 30 declared anchors are referenced by at least one `[[req]]` row in
`trace.toml` (confirmed by cross-check: zero items in the diff of trace
anchor references vs anchors.toml scenario names).

### Shipped strategy features vs anchor coverage

| Feature                     | Status      | Anchors in anchors.toml                                                                   |
|-----------------------------|-------------|-------------------------------------------------------------------------------------------|
| v0-paper-sma                | shipped     | btc-2023-1m-sma-cross, btc-2023-1m-sma-baseline-refresh                                  |
| v05-composed-strategies     | shipped     | btc-2023-1m-macd-trend, btc-2023-1m-rsi-reversion, btc-2023-1m-bbands-mean-revert        |
| v1-cross-sectional-momentum | shipped     | top10-2023-1h-momentum, top10-2024-h1-momentum                                            |
| v15a-mean-reversion-pairs   | shipped     | pairs-2023-zscore-mr, pairs-2024-h1-zscore-mr                                             |
| v2-llm-strategy             | shipped     | report-sample-7d, report-sample-90d                                                       |
| v1-5b-multi-venue           | shipped     | (none — infra/routing feature, not a strategy)                                            |
| v25-tcn-overlay             | in-progress | top10-2023-fy-tcn-overlay, top10-2024-fy-tcn-overlay, top10-2023-fy-tcn-overlay-weights, top10-2024-fy-tcn-overlay-weights, top10-2023-fy-tcn-overlay-realdata, top10-2024-fy-tcn-overlay-realdata, top10-2023-fy-tcn-overlay-weights-realdata, top10-2024-fy-tcn-overlay-weights-realdata |
| v25-tcn-recalibrate         | shipped     | forecast-distribution-bs1-realdata-recalibrated, forecast-distribution-bs2-realdata-recalibrated, recalibrate-sigma-train-bs1, recalibrate-sigma-train-bs2 |
| v25-tcn-threshold-tuning    | shipped     | threshold-sweep-bs1-realdata-recalibrated, threshold-sweep-bs2-realdata-recalibrated      |
| v25-tcn-alpha-investigation | shipped     | forecast-distribution-bs1-realdata, forecast-distribution-bs2-realdata, sharpe-comparison-realdata |
| backtest-real-binance-data  | shipped     | top10-2023-fy-tcn-overlay-realdata … (shared with v25-tcn-overlay)                        |
| v25a-patchtst-overlay       | shipped     | forecast-distribution-patchtst-bs1-realdata, top10-2023-fy-patchtst-overlay-realdata      |
| v25-tcn-horizon-bump-or-retire | shipped  | (none — scope (b) retire; zero code change; no new anchors by design)                    |

**No shipped strategy feature is missing anchor coverage.**

`top10-2024-fy-patchtst-overlay-realdata` is NOT in `anchors.toml` — the
`v25a-patchtst-overlay` decomp.md notes this was explicitly deferred ("sharpe-comparison-patchtst-bs1-realdata is NOT anchored at v0.1.0 per
decomp.md § Anchor gate: defer to v2.6 bake-off"). With v2.6 retired, this
anchor will never be locked unless a new bake-off is initiated. The trace.toml
REQ-V25A-PATCHTST-001 only references the two locked anchors
(`forecast-distribution-patchtst-bs1-realdata` and
`top10-2023-fy-patchtst-overlay-realdata`), so there is no trace-broken-path
violation. Flagging as a loose-end: if the `sharpe-comparison-patchtst-bs1-realdata`
body was generated and is byte-stable, it may be worth locking as an
archeological record before the programme fully closes. Owner: tester.

## Newly-retired bookkeeping consistency check

Four features retired in this sprint: `v25-dl-forecast-overlay` (umbrella),
`v25b-transformer-overlay`, `v26-forecast-bakeoff`, plus `v25a-patchtst-overlay`
(shipped + programme retired).

| Document                           | v25-dl-forecast-overlay | v25b-transformer-overlay | v26-forecast-bakeoff | Notes |
|------------------------------------|-------------------------|--------------------------|----------------------|-------|
| `feature.md` status field          | `deprecated`            | `deprecated`             | `deprecated`         | Consistent |
| `trace.toml` state field           | `deprecated`            | `deprecated`             | `deprecated`         | Consistent |
| `backlog.md` Queue § Strategy      | listed as RETIRED 2026-05-22 | RETIRED 2026-05-22  | RETIRED 2026-05-22   | Consistent |
| `backlog.md` Recent (shipped)      | not listed (deprecated, not shipped) | not listed | not listed         | Appropriate |
| `product.md` strategy table        | row not updated         | row not present          | row not present      | **Gap — see Soft Contradictions §1** |
| `architecture/12-forecast-overlay.md` | not updated         | not updated              | not updated          | **Gap** |
| ADR cross-references               | ADR-0027 (Kronos) links to deleted v25-kronos slug (unrelated) | n/a | n/a | Carry-over dead-link |

**Verdict:** retirement bookkeeping is consistent within the feature/trace/backlog
triangle. The gap is in the cross-cutting documents (`product.md` strategy
table, `architecture/12-forecast-overlay.md`) that still describe the 4-phase
DL programme as a forward-looking roadmap. These were not updated at the time
of the operator retirement decision — expected pattern (cross-cutting docs are
analyst-owned and trail the feature-level bookkeeping). Flag for analyst on
next pass.

## Parent status triage: `v25-tcn-overlay v2.5.0`

**Current state:** `feature.md` — `status: in-progress`, updated 2026-05-18.
`trace.toml` REQ-V25-TCN-OVERLAY-001 — `state = "in-progress"`.

**Evidence for a status change:**
- All child features spawned from this parent have closed: `v25-tcn-alpha-investigation`
  (shipped 2026-05-19), `v25-tcn-recalibrate` (shipped 2026-05-21),
  `v25-tcn-threshold-tuning` (shipped 2026-05-21), `v25-tcn-horizon-bump-or-retire`
  (shipped/retired 2026-05-21), `v25a-patchtst-overlay` (shipped 2026-05-22,
  programme retired).
- Parent `v25-dl-forecast-overlay` is `deprecated` (2026-05-22).
- 8 anchors locked under the v25-tcn-overlay family; all verify PASS.
- Backlog Active section still lists `v25-tcn-overlay` as `in-progress` at
  line 463, but the prose there is stale (it refers to "alpha-verdict investigation
  promoted to Active above" — that investigation has since shipped).

**Two plausible end-states (operator decides):**

Option A — `shipped`: TCN phase 1 work is complete. Anchors locked. Tester
PASS recorded (v25-tcn-alpha-investigation final report exists). The F4 verdict
means no live deployment, but the engineering deliverable (trained TCN checkpoints,
backtest framework, anchor suite) shipped. Precedent: `v25-tcn-alpha-investigation`
itself was closed as `shipped` even though its F4 verdict ended the programme.

Option B — `deprecated`: The 1h-horizon TCN hypothesis was falsified. The code
and checkpoints exist but will not go to production. Retiring the parent as
`deprecated` creates a clean narrative: the entire DL forecast overlay family
is deprecated at every level (umbrella, phases 1-2, children, follow-ons),
with only the shipped infra features (backtest-real-binance-data,
cockpit-training-control) remaining as `shipped`.

**Auditor stance:** this is a naming/narrative decision, not a technical one.
Both options are defensible. Surface to operator for one-line decision before
next audit. If operator chooses Option A, also flip trace.toml
REQ-V25-TCN-OVERLAY-001 `state` to `"shipped"`. If Option B, flip both to
`"deprecated"`.

## Recommended triage

- **P1 — ADR-0035 number collision.** Two ADR files share the same number.
  The ADR README only lists one (the σ_train recalibration). The Phase B
  scenario-dispatch ADR (`0035-phase-b-scenario-dispatch-extraction.md`)
  should be renumbered (suggest 0037, since 0036 is PatchTST training
  contract). Update filename, frontmatter `adr:` field, README table, and all
  cross-references in `trace.toml` and `spec/ui-rethink-phase-b-lab-run/`.
  Owner: architect.

- **P1 — `v25-tcn-overlay` parent status decision.** Feature and trace row
  both stuck at `in-progress` with all children closed. Operator decides
  `shipped` vs `deprecated`; single one-line flip in feature.md + trace.toml.
  Backlog Active section needs a corresponding housekeeping pass (see P2 below).
  Owner: operator → architect.

- **P1 — `v0` / `v05` / `v1-cross-sectional-momentum` trace state mismatch.**
  Three `[[req]]` rows in `trace.toml` carry `state = "in-progress"` while
  their feature.md shows `status: shipped`. The trace.toml contract
  (header comment lines 16–21) explicitly says "Status states match feature.md
  frontmatter." Three one-line edits. Owner: developer.

- **P2 — Update `product.md` and `architecture/12-forecast-overlay.md` to
  reflect DL retirement.** The v2.5 strategy table row and the
  "DL/RL models (planned)" sub-section in `product.md` still describe the
  4-phase DL programme as a live forward-looking roadmap. Same gap in
  `architecture/12-forecast-overlay.md`. The retirement narrative belongs
  here. Owner: analyst.

- **P2 — `backlog.md` Active section housekeeping.** Lines 437–488 contain
  stale entries (`v25-tcn-alpha-investigation` as active, `v25-tcn-overlay`
  paragraph pointing at shipped children as still in-flight). The Active
  section no longer reflects reality. Also: the HTML comment block at lines
  7–10 references v25a-patchtst-overlay as still in flight. Owner: analyst /
  orchestrator.

- **P2 — `v25-tcn-horizon-bump-or-retire` missing test report.**
  Feature is `shipped`; `reports/` is empty. If the operator's written
  decision (scope (b) retire) suffices as the test artefact, a one-paragraph
  decision-record stub in `reports/decision-2026-05-21-retire.md` would
  silence the lint and complete the paper trail. Owner: tester.

- **P2 — Residual dead-links cleanup (81):** Three clusters dominate:
  (a) ADR-0027 links to the removed `v25-kronos-forecast-overlay/` slug (5
  links; that ADR is superseded and should carry a superseded-by note or the
  links should be removed); (b) `/tmp/` screenshot artefacts in
  `chart-canvas-overhaul/feature.md` and reports (inert; but noisy); (c) the
  self-referencing dead-link in ADR-0033. None are regressions from this
  sprint; all pre-exist. Owner: developer.

- **P3 — `_probe_lint_test/` still not deleted.** Pending since 2026-05-13
  per its own README ("Safe to delete: `rm -rf spec/_probe_lint_test`").
  Operator clears the sandbox-blocked delete. Owner: orchestrator.

- **P3 — Decide on `sharpe-comparison-patchtst-bs1-realdata` loose-end anchor.**
  Generated during v25a but not locked. With v2.6 retired, this body will
  never be wired unless a new bake-off commences. Either lock it as an
  archeological anchor or explicitly document "not-anchored by design."
  Owner: tester.

- **P3 — `trace.toml` `tests = [] # TODO developer` on 7 early-era REQs.**
  Cosmetic; the features are shipped. A batch-clear pass would remove them
  from the decay count. Owner: developer.

## Changelog

- 2026-05-22 (spec-auditor): second scheduled audit; 82 lint violations across
  2 categories (down from 734); trace-broken-path fully resolved (-6); ADR-0035
  number collision found; v25-tcn-overlay parent status flagged for operator
  triage; product.md + architecture/12 retirement gap identified; 3 trace
  state mismatches (v0/v05/v1); backlog Active section stale.
