---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-06-12
---

# Spec Audit — 2026-06-12

## Headline

The spec tree is **net-healthier** since 2026-06-08 (lint dropped from 94 to
71 violations; anchors 119/119 PASS; _probe_lint_test orphan deleted; five
last-audit status-drift items resolved to `presenter-done`; ADR-0052 +
ADR-0053 registered and both new features land with `status: shipped`), but
the cleanup sweep of 2026-06-11 introduced **two new status-drift instances**
(cockpit-baseline-panel still `in-progress` despite a live presenter deck;
cockpit-live-dashboard-wiring still `tester-done` with its presentation deck
now archived but never reflected in the feature.md), and the recurring
`backlog.md` updated-date staleness remains open.

## Run metadata

- Date: 2026-06-12 (Thursday; 4 days since `audit-2026-06-08.md` — short
  cycle triggered by the 2026-06-11 cleanup sweep + two feature ships).
- Auditor: spec-auditor (on-demand).
- Baseline: `docs/dev-notes/audit-2026-06-08.md`.
- Python: 3.10 with `tomllib` shim (carry-over; same workaround as all
  prior audits); `spec_lint.py --all` completed successfully.
- Commits since 2026-06-08: **7** (`7390fcb`, `737d7bf`, `2ec06c6`,
  `24c6213`, `7fdb684`, `abdb5dc`, `2c4a59f` from the git log plus the
  live-equity-* wave and cockpit-live fixes visible in the log preview).

## Mechanical lint

`scripts/spec_lint.py --all` — **FAIL (71 violations in 2 categories).**
Exit code = 2.

| Category            | This run | Previous (06-08) | Δ    |
|---------------------|----------:|-----------------:|-----:|
| dead-link           |        66 |               87 | −21  |
| missing-frontmatter |         0 |                0 |    0 |
| trace-broken-path   |         5 |                7 | −2   |
| shipped-no-tests    |         0 |                0 |    0 |
| **TOTAL**           |    **71** |           **94** | **−23** |

**Improvement driver:** the cleanup sweep (`1405042`, 2026-06-11) removed
or rewrote 122 files, retiring 23 dead-link and 2 trace-broken-path
violations. This is the lowest lint count in recorded audit history.

**Note on delta discrepancy vs the operator brief** ("baseline is 71 in 2
categories, 66 dead-link + 5 trace-broken-path"): confirmed — the baseline
referenced by the operator IS this run's result (the sweep reduced from 94
to 71 before this audit ran). No further sweep effect to attribute.

### Top violation clusters (representative sample)

**dead-link (66 — all pre-existing, no new entries this run):**
- `_bmad-output/planning-artifacts/architecture/decisions/0027-kronos-onnx-tract-integration.md` — 5 links
  to removed `../../v25-kronos-forecast-overlay/feature.md`.
- `_bmad-output/planning-artifacts/architecture/decisions/0045-v5-canonical-config-and-noop-baseline-namespace.md`
  — 1 link to `../../.claude/skills/spec-update/SKILL.md` (path resolves
  relative to `_bmad-output/planning-artifacts/architecture/decisions/` → `spec/.claude/skills/…`, which
  does not exist; actual file lives at repo-root `.claude/skills/spec-update/SKILL.md`).
  **NEW finding** relative to the 06-08 audit — this was introduced by the
  sweep or surfaced for the first time in this category pass; operator
  should add `../../../.claude/` correction or use an absolute reference.
- `spec/chart-canvas-overhaul/feature.md` — 7 links to `/tmp/orch-diag/*.png`.
- `spec/journal-transactions-metadata/feature.md` + `tasks.md` — 4 links
  to a missing test report (archived in sweep tar).
- `spec/v0-paper-sma/reports/screenshots/README.md` — 6 links to pre-flat-
  layout paths.
- `spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-20260522.md`
  — 1 dead link; **byte-immutable per ADR-0038 § D6 — do NOT edit directly.**

**trace-broken-path (5 — reduced from 7):**
- `REQ-LAB-YAHOO-REALDATA-V0-1-4-001` arch → missing
  `spec/lab-yahoo-realdata-v0.1.2-per-ticker-scaling-and-aggregate-cache-state/feature.md`.
- `REQ-VISUAL-FAIL-HTML-REPORTER-001` tests → two bare function names
  without `crates/…rs::` prefix.
- `REQ-QUEUE-STALENESS-RECONCILIATION-001` tests →
  `scripts/queue_staleness_check.py --self-test`.
- `REQ-OPERATOR-LEDGER-SCHEMA-LINT-001` tests →
  `scripts/operator_ledger_check.py --self-test`.

The two `REQ-UI-CONTRAST-ASSERTER-001` trace-broken-path violations from
the 06-08 audit are **RESOLVED** — that row was repaired in the sweep.

### Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged from
06-08. No new anchors were added this cycle (the two new feature ships are
UI/wiring features, not backtest strategy features).

## Feature inventory (103 feature.md files)

Count is 103 at maxdepth-2 (up from 98 in the 06-08 audit — the delta is
the lumen sub-phase features now included at depth-2 plus `paper-mode-equity-
wiring` and `live-equity-history-durable` added this cycle).

### Status histogram

| Status          | Count | Δ vs 06-08 | Notes                                                                    |
|-----------------|------:|-----------:|--------------------------------------------------------------------------|
| shipped         |    71 | +2         | `live-equity-history-durable`, `paper-mode-equity-wiring` newly shipped  |
| tester-done     |     5 |  0         | see list below                                                           |
| dev-done        |     5 |  0         | unchanged                                                                |
| presenter-done  |     4 | −1         | five last-audit items flipped to presenter-done; `cockpit-baseline-panel` missed |
| deprecated      |     4 |  0         |                                                                          |
| retired         |     4 | +2         | `carry-strategy` flipped; +1 from new count methodology                  |
| candidate       |     3 |  0         |                                                                          |
| draft           |     2 | −1         | one draft consumed                                                       |
| shipped-partial |     1 |  0         |                                                                          |
| roadmap         |     1 |  0         |                                                                          |
| proposed        |     1 |  0         |                                                                          |
| in-progress     |     1 |  0         | cockpit-baseline-panel (status drift — see below)                        |
| arch-done       |     1 |  0         | lab-yahoo-realdata-v0.1.4-bulk-ticker-re-emit                            |
| **TOTAL**       | **103** |          |                                                                          |

## Stale features (>30 days, status proposed | in-progress)

By the strict proposed/in-progress stale rule, **one feature crosses the
30-day threshold this run:**

- `cockpit-app-bundle` — `candidate`, updated 2026-05-11 (**32 days**).
  Crossed the threshold since the last audit. Not proposed/in-progress, so
  not a formal stale-rule hit, but flagged as watch.

Strict proposed/in-progress stale rule hits:
- **ZERO.** `lab-polish-round-2` (`proposed`, 2026-05-25, 18 days) is still
  under threshold. `cockpit-baseline-panel` is `in-progress` updated
  2026-06-08 (4 days) — but see Status Drift section below.

Watch items aging toward 30 days:
- `iced-ecosystem-evaluation` — `candidate`, updated 2026-05-13 (**30 days
  exactly**). Will be stale at next audit if not advanced.

## Status drift — feature.md status lags actual pipeline state

**Last audit's P1 RESOLVED:** the five features flagged on 2026-06-08
(`carry-strategy`, `horizon-retest-robustness`, `time-series-momentum-
robustness`, `perp-basis-signal-robustness`, `perp-basis-mn-spread`) have
all been advanced to `presenter-done` (or `retired` for `carry-strategy`)
as of `updated: 2026-06-08`. This is the first audit since 2026-05-22 that
does NOT carry that specific cluster forward. **Status-drift class partially
broken.**

**New status-drift instances introduced since 06-08:**

| Feature                        | feature.md status | Actual state (git + reports/ + presentations/)                                                                                                   | Should be      |
|--------------------------------|-------------------|--------------------------------------------------------------------------------------------------------------------------------------------------|----------------|
| `cockpit-live-dashboard-wiring` | `tester-done`    | Presenter commit `c46928f` (2026-06-09): release deck + cockpit-smoke log (`PRESENTATION → READY`). The deck was **archived** by the cleanup sweep (`1405042`, 2026-06-11) into `spec/archive/presentations-2026-Q2.tar.gz`. feature.md was never advanced past `tester-done` either before or after the sweep. | `presenter-done` |
| `cockpit-baseline-panel`       | `in-progress`     | Presenter commit `dbe6e60`: release deck present at `spec/cockpit-baseline-panel/presentations/cockpit-baseline-panel-2026-06-08.md` (not archived — postdates the sweep cutoff). feature.md still reads `in-progress` despite a fully completed presenter cycle. | `presenter-done` |

**Pattern analysis:** the cleanup sweep archived `cockpit-live-dashboard-
wiring`'s presentation deck on 2026-06-11 (2 days after it was created), and
the feature.md had not yet been flipped to `presenter-done` in that window.
The `cockpit-baseline-panel` missed the flip entirely — its deck was created
the same day as the last audit (2026-06-08) and was not acted on.

This is now the **5th consecutive audit** that must record a status-drift
finding. The enforcement hook (pre-commit check: if `presentations/*.md`
exists AND `reports/test-*.md` is PASS, `feature.md` status must be ≥
`presenter-done`) is overdue per the P1 from audit-2026-06-08.

**Additionally:** `trace.toml` inline comment for `REQ-COCKPIT-LIVE-
DASHBOARD-001` still reads `arch-done 2026-06-09` — the comment was written
at architect phase and was never updated to reflect tester-done or
presenter-done. This is documentation rot inside the trace row (the row
itself is structurally correct).

## Orphans

**`spec/_probe_lint_test/`** — **RESOLVED.** Deleted in the cleanup sweep
(`1405042`). Was flagged P3 for 26+ days; now gone.

**Folders missing `feature.md` or `tasks.md`** (from spec-lint — unchanged
carry-overs):
- `spec/cockpit-app-bundle/` — missing `tasks.md` (candidate,
  pre-developer — expected).
- `spec/lumen-design-adoption/` — missing `tasks.md` (roadmap umbrella —
  expected).

**Test reports for missing slugs:** none found.

**Crates without spec mention:**
- All crates under `crates/` are referenced in `spec/` (architecture.md,
  trace.toml, or feature.md files). Lowest coverage: `crates/trader` (16
  hits) and `crates/features` (20 hits). These are internal infra crates;
  low mention count is not a defect, but operator may want to verify
  `crates/trader` is described in `spec/architecture.md`.

## Decay markers (TODO / FIXME / TBD / XXX / ???)

Counts across `spec/` excluding `spec/archive/` and `docs/dev-notes/archive/`:

| Folder                                       | Matches | Δ vs 06-08 | Flag                                    |
|----------------------------------------------|--------:|-----------:|-----------------------------------------|
| dev-notes (active, excl. archive)            |      13 | −67        | informational (49 superseded notes archived) |
| lumen-design-adoption                        |       8 | −3         | **DECAY-HEAVY** (improved from 11, still above threshold) |
| cockpit-performance-and-input-responsiveness |       6 |  0         | **DECAY-HEAVY** (flat, carry-over)      |
| (all other folders ≤ 4 each)                 |    ≤ 4  | —          | —                                       |

The `dev-notes` count dropped sharply (80 → 13) because 49 superseded notes
were moved to `docs/dev-notes/archive/2026-Q2/` by the sweep — those notes
carry audit-era TBDs by nature and are now correctly out of scope.
`lumen-design-adoption` improved from 11 to 8 but remains DECAY-HEAVY.
`cockpit-performance-and-input-responsiveness` is flat at 6 (5th consecutive
audit with this finding). No new decay-heavy folders.

## Soft contradictions (LLM-judged — verify before acting)

### SC-A — ADR-0043 § D2 RNG: ChaCha20 (body) vs Murmur3 (changelog) (MEDIUM, carry-over)

Not resolved since audit-2026-06-01 (5 consecutive audits). Body text at
`_bmad-output/planning-artifacts/architecture/decisions/0043-simulated-latency-and-slippage.md § D2` still
specifies `ChaCha20Rng`; the changelog records the shipped implementation is
a Murmur3-style bit mixer. One-line body note would close it. Owner:
architect.

### SC-B — `product.md` § Week 2 `bps: 2` vs v5 canonical `bps: 8` (MEDIUM, carry-over)

Not resolved since audit-2026-05-29 (5 consecutive audits). `spec/product.md:
553-554` uses `bps: 2`; ADR-0043 / ADR-0045 canonical is `slippage_bps: 8`.
One-line cross-reference. Owner: analyst.

### SC-C — `trace.toml` `REQ-COCKPIT-LIVE-DASHBOARD-001` inline comment stale (LOW, new)

The inline comment on `REQ-COCKPIT-LIVE-DASHBOARD-001` reads `arch-done
2026-06-09`. The feature completed a full presenter cycle on 2026-06-09 and
the feature.md is `tester-done`. The comment is stale by at least two phases.
Not a functional contradiction (the row's `arch`, `tests`, and `feature`
fields are correct), but it misleads a reader scanning trace.toml for
pipeline state. Confidence: HIGH (status progression is deterministic from
git log).

### SC-D — CONSISTENT: ADR-0052 + ADR-0053 architecture coherence (new, PASS)

Both new ADRs (`_bmad-output/planning-artifacts/architecture/decisions/0052-durable-live-equity-series.md`
and `_bmad-output/planning-artifacts/architecture/decisions/0053-unified-per-bar-trading-loop.md`) are
registered in `_bmad-output/planning-artifacts/architecture/decisions/README.md`. The `LiveEquityStore` trait
mode-gate pattern (`mode != Research` → caller passes `Some(store)`,
Research passes `None`) is consistently described in both ADRs and in both
feature.md files. No contradictions found between the two ADRs. CONSISTENT.

### SC-E — CONSISTENT: `11-regression-gate.md` live-count staleness (last audit P1, RESOLVED)

The prior P1 (SC-B in audit-2026-06-08) is **RESOLVED.** The inline hard-
coded `87` was replaced with a pure pointer: `spec/architecture/11-
regression-gate.md:63` now reads `scripts/verify_anchors.sh is the SOLE
source of truth for the current count — do not hard-code it here` (commit in
the 2026-06-12 window). CONSISTENT with the anchor verification result of
119/119.

## .claude/ reference rot (cleanup-sweep aftermath)

The cleanup sweep (`1405042`) moved 49 dev-notes to
`docs/dev-notes/archive/2026-Q2/` and archived tester reports to tar. The
sweep explicitly covered `spec/` + root `.md` files, but **did NOT update
`.claude/skills/`**. Two skills reference moved files:

| Skill | Reference | Actual location | Severity |
|-------|-----------|-----------------|----------|
| `.claude/skills/rust-coverage/SKILL.md` | `docs/dev-notes/testing-framework-audit-2026-05-25.md` (×2) | `docs/dev-notes/archive/2026-Q2/testing-framework-audit-2026-05-25.md` | MEDIUM |
| `.claude/skills/rust-coverage/SKILL.md` | `docs/dev-notes/testing-strategy-review-2026-05-25.md` (×2) | `docs/dev-notes/archive/2026-Q2/testing-strategy-review-2026-05-25.md` | MEDIUM |
| `.claude/skills/rust-mutants/SKILL.md` | `docs/dev-notes/testing-framework-audit-2026-05-25.md` (×3) | `docs/dev-notes/archive/2026-Q2/testing-framework-audit-2026-05-25.md` | MEDIUM |
| `.claude/skills/rust-mutants/SKILL.md` | `docs/dev-notes/testing-strategy-review-2026-05-25.md` (×3) | `docs/dev-notes/archive/2026-Q2/testing-strategy-review-2026-05-25.md` | MEDIUM |

The `v3-vol-overlay-noop-discovery-2026-05-22.md` reference in
`rust-mutants/SKILL.md` is NOT affected — that file is still at
`docs/dev-notes/v3-vol-overlay-noop-discovery-2026-05-22.md`.

These stale paths will silently mislead agents invoking `rust-coverage` or
`rust-mutants` skills when they attempt to read the rationale documents.
Owner: developer (update the two skill files to the archive path, or add an
inline redirect note).

**ADR-0045 dead link (new lint hit):** `_bmad-output/planning-artifacts/architecture/decisions/0045-v5-
canonical-config-and-noop-baseline-namespace.md:159` links to
`../../.claude/skills/spec-update/SKILL.md`, which resolves to
`spec/.claude/skills/spec-update/SKILL.md` (relative to the ADR's directory).
The file actually lives at `.claude/skills/spec-update/SKILL.md` (repo root).
The path needs one additional `../` to escape `spec/`. Owner: developer or
architect (one-character fix).

## Anchor coverage

`spec/anchors.toml` — **119 scenarios, PASS 119/119** (unchanged from
2026-06-08; confirmed via `scripts/verify_anchors.sh`).

### Shipped strategy features with no anchor coverage (carry-over, unchanged)

| Feature              | State    | Disposition                                                                                          |
|----------------------|----------|------------------------------------------------------------------------------------------------------|
| `v2-llm-strategy`    | shipped  | Zero anchors, no "not-anchorable" disposition note. LLM non-determinism is likely acceptable but undocumented. Owner: tester → architect. Carry-over P2 (5th consecutive audit). |
| `v25-tcn-horizon-bump-or-retire` | shipped | Decision/retire feature — anchorless by intent, but no disposition note. Cosmetic. Carry-over. |

The two new shipped features (`live-equity-history-durable`,
`paper-mode-equity-wiring`) are UI/wiring features without backtest
scenarios — no anchors expected, and no gap here.

### New trace rows

Both new features have `[[req]]` rows in `trace.toml`:
- `REQ-LIVE-EQUITY-HISTORY-001` — `live-equity-history-durable`, references
  `spec/live-equity-history-durable/feature.md#architecture` and
  ADR-0052. GOOD.
- `REQ-LIVE-EQUITY-PAPER-001` — `paper-mode-equity-wiring`, references
  ADR-0053. GOOD.
- `REQ-COCKPIT-LIVE-DASHBOARD-001` — structurally correct, inline comment
  stale (see SC-C above).

**`carry-funding-data-backfill` trace row** (`REQ-CARRY-FUNDING-DATA-
BACKFILL-001`) — EXISTS in `trace.toml` (carry-over P2 from 06-08 is
**RESOLVED** — the row is present). Status still `dev-done` (14 days);
not yet stale by the 30-day rule.

## Resolved since audit-2026-06-08

| 06-08 finding                                                                 | Status now       | Evidence                                                                          |
|-------------------------------------------------------------------------------|------------------|-----------------------------------------------------------------------------------|
| P1 — 5 feature.md status-drift items (arch-done/tester-done)                 | **RESOLVED**     | All 5 advanced: carry-strategy→retired, others→presenter-done. Updated 2026-06-08. |
| P1 — `11-regression-gate.md` hard-codes 87 (live was 119)                    | **RESOLVED**     | Inline count replaced with pointer-only to `verify_anchors.sh` (line 63).        |
| P3 — `spec/_probe_lint_test/` orphan, 26 days                                | **RESOLVED**     | Deleted in cleanup sweep `1405042`.                                               |
| P2 — `carry-funding-data-backfill` trace row missing                         | **RESOLVED**     | `REQ-CARRY-FUNDING-DATA-BACKFILL-001` is present in trace.toml.                  |
| Lint count 94/2-cat                                                           | **IMPROVED**     | 71/2-cat (−23 violations from sweep).                                             |
| SC-B (06-08) — `11-regression-gate.md` inline count                          | **RESOLVED**     | See above.                                                                        |

| 06-08 finding still open                                                      | Status           | Notes                                                                             |
|-------------------------------------------------------------------------------|------------------|-----------------------------------------------------------------------------------|
| P1 — `backlog.md` frontmatter `updated:` stale                               | **OPEN**         | Still `updated: 2026-06-08` (4 days; was 10 days at last audit; no regression but unresolved). |
| P2 — `v2-llm-strategy` anchor disposition undocumented                        | OPEN             | 5th consecutive audit.                                                            |
| P2 — `product.md` Week-2 `bps:2` vs v5 canonical `bps:8` (SC-C)             | OPEN             | 5th consecutive audit.                                                            |
| P2 — ADR-0043 § D2 RNG ChaCha20 vs Murmur3 (SC-A)                           | OPEN             | 5th consecutive audit.                                                            |
| P3 — Residual dead-link clusters (66, reduced from 87)                       | OPEN (improved)  | −21 from sweep.                                                                   |
| P3 — Python 3.11+ pin for lint                                                | OPEN             | tomllib shim still required.                                                      |

## Recommended triage

- **P1 — Add the status-enforcement hook NOW.** Two new status-drift
  instances appeared in a 4-day window. cockpit-baseline-panel has a live
  presentation deck at `spec/cockpit-baseline-panel/presentations/cockpit-
  baseline-panel-2026-06-08.md` but `status: in-progress`. cockpit-live-
  dashboard-wiring has a presenter commit in git and a deck in the archive
  tar but `status: tester-done`. Both should read `presenter-done`. Flip
  both manually (P1), then wire the pre-commit check (sibling to
  `adr_registry_check.py`): if `presentations/*.md` exists AND a PASS
  `reports/test-*.md` exists, `status` must be ≥ `presenter-done`. Owner:
  orchestrator.

- **P1 — `backlog.md` `updated:` staleness.** `spec/backlog.md` reads
  `updated: 2026-06-08` — unchanged from the last audit despite two feature
  ships and 7 commits. Flagged P1 for 4 consecutive audits. A bump to
  `updated: 2026-06-12` and a brief `## Recent` entry for the live-equity
  and paper-mode ships is overdue. Owner: orchestrator.

- **P2 — Fix `.claude/skills/rust-coverage/SKILL.md` and
  `.claude/skills/rust-mutants/SKILL.md` stale dev-notes paths.** Both
  skills reference `docs/dev-notes/testing-framework-audit-2026-05-25.md`
  and `docs/dev-notes/testing-strategy-review-2026-05-25.md` which moved
  to `docs/dev-notes/archive/2026-Q2/` in the cleanup sweep. These are
  active agent skill files — agents invoking these skills will fail to
  load the rationale documents. Owner: developer.

- **P2 — Fix ADR-0045 dead link to `spec-update/SKILL.md`.** Correct path
  in `_bmad-output/planning-artifacts/architecture/decisions/0045-v5-canonical-config-and-noop-baseline-
  namespace.md:159` from `../../.claude/skills/spec-update/SKILL.md` to
  `../../../.claude/skills/spec-update/SKILL.md`. One character. Owner:
  developer or architect.

- **P2 — `trace.toml` `REQ-COCKPIT-LIVE-DASHBOARD-001` inline comment
  stale** (still reads `arch-done 2026-06-09`). Update to `presenter-done
  2026-06-09`. Owner: orchestrator.

- **P2 — `v2-llm-strategy` shipped without anchor disposition note**
  (5th consecutive carry-over). One paragraph. Owner: tester → architect.

- **P2 — ADR-0043 § D2 RNG body (ChaCha20) vs Changelog (Murmur3)**
  (5th consecutive carry-over). One-line body note. Owner: architect.

- **P2 — `product.md` Week-2 `bps:2` vs v5 canonical `bps:8`** (5th
  consecutive carry-over). One-line cross-reference. Owner: analyst.

- **P3 — `iced-ecosystem-evaluation`** — `candidate`, exactly 30 days old
  as of this audit. Will cross the stale threshold at the next audit if
  not advanced or explicitly parked. Owner: operator / analyst.

- **P3 — `lumen-design-adoption` decay markers** (8, improved from 11 but
  still DECAY-HEAVY). `cockpit-performance-and-input-responsiveness` flat
  at 6. Owner: phase owners.

- **P3 — Residual dead-link clusters (66, unchanged in character)**
  and **Python 3.11+ pin** — both long-standing carry-overs. The
  `spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-
  20260522.md` dead link must NOT be edited per ADR-0038 § D6 byte-
  immutability contract. Owner: developer (non-anchored links) / ops
  (Python).

## Changelog

- 2026-06-12 (spec-auditor): Off-cadence audit (4-day cycle). 7 commits
  since audit-2026-06-08. MAJOR CONTEXT: cleanup sweep `1405042`
  (367 files, −51.8k lines, 2026-06-11): lint improved 94→71 (−23);
  _probe_lint_test orphan DELETED; 49 dev-notes archived; 47 tester
  reports tarred. Two features shipped: `live-equity-history-durable`
  (ADR-0052 + T6b purge scheduling) + `paper-mode-equity-wiring`
  (ADR-0053 unified trading loop). Both `status: shipped`, ADRs
  registered, trace rows present, anchors 119/119 PASS (unchanged —
  no new strategy scenarios). P1s from 06-08 RESOLVED: 5 status-drift
  items flipped to presenter-done/retired; 11-regression-gate inline
  count replaced with pointer-only; carry-funding-data-backfill trace
  row added; _probe_lint_test deleted. NEW HEADLINE: 2 new status-
  drift items (cockpit-baseline-panel in-progress despite live
  presenter deck; cockpit-live-dashboard-wiring tester-done with
  presentation in sweep archive) — 5th consecutive status-drift audit.
  NEW FINDING: cleanup sweep left .claude/skills/ referencing archived
  dev-notes paths (rust-coverage + rust-mutants reference 2 moved
  files each). ADR-0045 dead link (spec-relative path off by one
  directory level). Soft contradictions: ADR-0052/0053 CONSISTENT;
  SC-A (RNG) + SC-B (bps) carry-over (5th each); SC-C (trace comment
  stale, LOW). backlog.md updated: still 2026-06-08 (P1 unresolved).
