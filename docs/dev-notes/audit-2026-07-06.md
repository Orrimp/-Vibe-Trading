---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-07-06
---

# Spec Audit — 2026-07-06 (weekly cadence)

## Headline

The spec tree is **mechanically green and structurally sound, but the feature.md↔trace.toml
lifecycle-state drift the last audit flagged has not been triaged and has grown**:
`spec_lint.py` is PASS (0 violations) and anchors hold 119/119, but the count of
`feature.md status: shipped` features whose `trace.toml state=` still reads something
other than `shipped` rose from **3 → 25**. Most of that jump (22 of 25) is the v2 Phase
2A–2D build landing this week under a **tester-terminal vocabulary** (`verified` / `passed`)
that the `trace.toml` legend never enumerated — a schema-vs-practice mismatch, not 25
separate defects. But the **3 genuine mid-pipeline lag rows from last week
(`advisor-param-promotion` = design-complete, `advisor-param-tuning` = in-progress,
`advisor-signal-library-expansion` = implemented) are UNCHANGED** — last week's P1 was
not actioned. On the positive side: the two new v2 overlays (`advisor-drawdown-control-overlay`,
`advisor-vol-overlay-reposition`) both ship with the CLAUDE.md-mandated day-1
baseline-equity-divergence e2e test — that non-negotiable held under a 12-feature build.
Zero strict-stale features, no decay-heavy folder, no hard contradiction.

## Run metadata

- Date: 2026-07-06 (scheduled Monday cadence, 09:xx local).
- Baseline: `docs/dev-notes/audit-2026-06-29.md` (most recent audit on disk).
- Commits since baseline: **23** (`git log --since='2026-06-29 09:12'`). Dominant theme:
  the **v2 Phase 2A→2B→2C→2D build shipped to feature-complete** — operator-approved
  (`4ad8b23`, `3904b11`). New v2 features this week: `advisor-turnover-and-tail-metrics`,
  `advisor-confidence-not-verdict`, `advisor-overfitting-scorecard` (P0-1 UI),
  `advisor-forward-fidelity-coverage`, `advisor-vol-estimator`, `advisor-vol-overlay-reposition`,
  `advisor-drawdown-control-overlay`, `advisor-cost-model-opt-in`,
  `advisor-narration-faithfulness`, `advisor-no-alpha-gate-ci`, plus the `phase-2c-overlays`
  and `phase-2d` rollup folders. ADRs 0075–0081 registered.
- Tooling: `spec_lint.py` requires Python 3.11+ (`tomllib`); this workspace ships 3.10.
  Ran the lint under a `tomli`-as-`tomllib` shim (`PYTHONPATH` module alias) — identical
  logic, result unchanged. Same run-environment note as the last four audits; not a finding.

## Mechanical lint

`scripts/spec_lint.py --all` — **PASS (0 violations).** Exit code 0.

| Category                    | This run | Previous (2026-06-29) | Δ  |
|-----------------------------|----------|-----------------------|----|
| dead-link                   | 0        | 0                     | 0  |
| missing-frontmatter         | 0        | 0                     | 0  |
| orphan-feature              | 0        | 0                     | 0  |
| anchor-coverage             | 0        | 0                     | 0  |
| trace-row-validity          | 0        | 0                     | 0  |
| pipeline-status-drift       | 0        | 0                     | 0  |
| **TOTAL**                   | **0**    | **0**                 | 0  |

The lint's `pipeline-status-drift` rule (deck + PASS report ⇒ `feature.md status ≥
presenter-done`) stays green — but that rule only reads `feature.md`, never cross-checks
`trace.toml state=`. The drift finding below lives in exactly that blind spot, which is
why it is an auditor catch, not a lint catch.

## Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged from baseline.
Every new v2 feature this week runs the bake-off / advisor path with `write_report=false`
(ADR-0066/0067/0068/0071 anchor-safety-by-construction), so no anchored report body was
re-emitted and the SHA set is byte-stable. `anchors.toml` still holds 119 `[[anchors]]`
entries (count confirmed). The 9 backtest scenario families and their SHA-256 bodies are
untouched — audit-only, per instruction.

## Feature inventory

**148** `feature.md` files in scope (excluding `design/`, `dev-notes/`, `runbooks/`,
`archive/`, `architecture/`) — up from 131 last audit (**Δ +17**, all attributable to the
v2 Phase 2A–2D build plus a few v1 landings). Layout: 119 in `spec/v1/`, 12 in `spec/v2/`
(was 1 last week), 17 top-level (not-yet-migrated / roadmap). No duplicate slugs.

Status distribution:

| Status          | This run | Previous | Δ    |
|-----------------|----------|----------|------|
| shipped         | 117      | 100      | +17  |
| presenter-done  | 7        | 7        | 0    |
| retired         | 5        | 5        | 0    |
| tester-done     | 5        | 5        | 0    |
| deprecated      | 4        | 4        | 0    |
| candidate       | 2        | 2        | 0    |
| dev-done        | 2        | 3        | −1   |
| shipped-partial | 1        | 1        | 0    |
| in-progress     | 1        | 1        | 0    |
| roadmap         | 1        | 1        | 0    |
| draft           | 1        | 1        | 0    |
| arch-done       | 1        | 1        | 0    |
| reserved        | 1        | 0        | +1   |

`reserved` is new: `lumen-phase-6-assistant-slot` (a placeholder slot in the lumen phase
sequence). `dev-done` dropped by one as `advisor-overfitting-scorecard` advanced to shipped.

## Stale features

**Zero** features match the strict staleness rule (`status: proposed` OR `in-progress`,
not updated in 30+ days). The single `in-progress` feature is `cockpit-cross-platform`
(updated 2026-06-15 → 21 days) — still under the 30-day bar, but see carry-over P1.

### Aging watch items (NOT strict-stale — carry-over, all +7d)

`candidate` / `draft` / `roadmap` states the strict rule does not flag, unmoved for 7–9 weeks:

| Slug                       | Status    | Updated    | Age  | Δ vs last audit |
|----------------------------|-----------|------------|------|-----------------|
| lumen-design-adoption      | roadmap   | 2026-05-04 | 63d  | +7d, no change  |
| cockpit-app-bundle         | candidate | 2026-05-11 | 56d  | +7d, no change  |
| iced-ecosystem-evaluation  | candidate | 2026-05-13 | 54d  | +7d, no change  |
| ui-gallery-table-cell      | draft     | 2026-05-16 | 51d  | +7d, no change  |

All four aged +7d with no state change (4th consecutive audit). Park or advance.

## PRIMARY FINDING — feature.md ↔ trace.toml lifecycle-state drift (3 → 25)

25 features carry `feature.md status: shipped` while their `trace.toml state=` reads
something other than `shipped`. The `trace.toml` header legend is explicit: *"Status
states match feature.md frontmatter: draft | proposed | candidate | in-progress | roadmap
| shipped | deprecated"* — terminal = `shipped`. The 25 split into two distinct classes.

### Class A — genuine mid-pipeline lag (3 rows) — **CARRY-OVER, UNRESOLVED, P1**

These three were the entire P1 headline of the 2026-06-29 audit. **None has moved.**

| Feature                          | feature.md | trace.toml state | Lag       | Test report on disk |
|----------------------------------|------------|------------------|-----------|---------------------|
| advisor-param-promotion          | shipped    | design-complete  | ~3 steps  | YES                 |
| advisor-param-tuning             | shipped    | in-progress      | ~1 step   | YES                 |
| advisor-signal-library-expansion | shipped    | implemented      | ~1 step   | YES                 |

Evidence (line numbers in `spec/trace.toml`): `advisor-param-promotion` feature row at
:3190, `state="design-complete"`; `advisor-param-tuning` at :3167, `state="in-progress"`;
`advisor-signal-library-expansion` at :3212, `state="implemented"`. All three are
genuinely shipped (test reports present, lint `shipped-no-tests=0`) — the code is sound;
the trace row is simply behind. This is the exact cross-file drift the auditor exists to
surface, and it survived a full week untriaged. **Escalate: same P1 as last week, now
1 audit-cycle stale.** Owner: tester / orchestrator. (Audit is read-only — this note does
not touch trace.toml.)

### Class B — tester-terminal vocabulary not in the legend (22 rows) — **schema question, P2**

The remaining 22 sit in `state=` values that are *plausibly terminal* but absent from the
legend's enumerated set: **10× `verified`** (all v2 Phase 2A–2D advisor features shipped
this week) and **11× `passed`** (`cockpit-activity-audit-ledger-producer`,
`cockpit-activity-llm-producer`, `cockpit-activity-status-bar`,
`cockpit-training-pressed-wiring`, `lab-recipe-test-harness`,
`lab-yahoo-realdata-v0.1.2-…`, `lab-yahoo-realdata-v0.1.3-…`,
`reflection-memory-trader-wiring`, `v5-latency-slippage-sim-v0.4.0-…`,
`v5-latency-slippage-sim-v0.5.0-…`, `vol-killswitch-overlay-noop-fix`), **plus 1×
`paper-mode-equity-wiring`** whose row has no `state=` field at all.

Zooming out, `trace.toml` in aggregate uses a wider state vocabulary than its legend
documents: `shipped` ×83, `passed` ×16, `verified` ×10, `tester-done` ×7, `tested` ×6,
alongside the enumerated `retired`/`deprecated`/`candidate`/`design-complete`/`roadmap`/
`in-progress`/`architected`/`shipped-partial`. So `verified`/`passed`/`tested`/`tester-done`
(39 rows total) are an established de-facto tester-terminal convention.

**LLM-judged, uncertainty flagged:** I cannot tell from the files alone whether
`verified`/`passed` are *intended* to be terminal (in which case the fix is a one-line
legend update, not 22 row edits) or are the same one-step-short lag as Class A at scale.
Given the pattern is systematic (every Phase 2A–2D feature landed at `verified`, matching
the tester's `VERDICT → PASS` step), I read Class B as **an established convention that
diverges from the documented legend** — a schema hygiene item for the architect, *not* 22
P1s. Recommendation: architect decides — either (a) add `verified`/`passed`/`tested`/
`tester-done` to the legend as blessed post-tester states, or (b) codify a lint rule that
flips them to `shipped` on feature close. Owner: architect. Do NOT bulk-edit trace rows
without that decision.

## SS-WIRE — `advisor-short-selling` e2e on disk but `trace.tests=[]` — CARRY-OVER, UNRESOLVED, P2

`REQ-ADVISOR-SHORT-SELLING-001` (trace :3156) is now `state="shipped"` but still carries
`tests=[]` and `anchors=[]`, while the day-1 divergence harness
`crates/backtest/tests/short_selling_divergence_e2e.rs` **exists on disk** (confirmed this
run). Same finding as last audit's P2; the trace `tests=` array was never populated. The
verification floor is satisfied — the test is real and passing — only the citation is
missing. Owner: tester. Unchanged since last week.

## Anchor / test-wiring coverage — new v2 overlays PASS the CLAUDE.md non-negotiable

CLAUDE.md mandates every strategy overlay / sizing-modifier ship with a day-1
baseline-equity-divergence e2e test (the `v3-volatility-forecaster-noop-fix` precedent).
The v2 build added two overlays; **both cite a real day-1 divergence e2e in `trace.tests`**:

| New overlay feature              | trace.tests cites day-1 divergence e2e                         |
|----------------------------------|----------------------------------------------------------------|
| advisor-drawdown-control-overlay | `crates/strategy/tests/drawdown_control_overlay_end_to_end.rs` ✓ |
| advisor-vol-overlay-reposition   | `crates/strategy/tests/vol_targeting_overlay_end_to_end.rs` ✓   |

Both e2e files exist on disk. The non-negotiable held under a 12-feature build — good
hygiene, explicitly called out here as the positive delta this week. (`advisor-vol-estimator`
is a shared σ̂ estimator, not an overlay, so the divergence rule does not apply.)

## Trace coverage gaps (13 → 21)

21 in-scope `feature.md` slugs have no `[[req]]` row in `trace.toml` (was 13). Δ +8, all
from features that landed this week or migrated into the lumen phase sequence:

```
NEW this audit (+8):
  lumen-phase-1-foundation          lumen-phase-2-shell-ia-charts
  lumen-phase-3-detail-screens      lumen-phase-4-backtest-panel
  lumen-phase-5-humancontrol-agentfeed  lumen-phase-6-assistant-slot (reserved)
  phase-2c-overlays (rollup)        phase-2d (rollup)

CARRY-OVER (13, unchanged):
  chart-fixture-line-clipping       cockpit-toast-queue
  cockpit-toast-queue-v0.2.0-cleanup  leaderboard-timeframe-capital
  paper-soak-longevity              ui-drop-iced-aw
  ui-gallery-bin                    ui-gallery-table-cell (draft)
  ui-headless-emulator              ui-session-journal-iced-tester
  v5-latency-slippage-sim           v5-latency-slippage-sim-v0.2.0-anchor-migration
  v5-latency-slippage-sim-v0.3.0-full-path-wiring
```

`phase-2c-overlays` and `phase-2d` are rollup/umbrella folders (their constituent features
each have their own trace rows), so their absence is likely intentional. The 6 `lumen-phase-*`
folders are UI-infrastructure the spec-compression convention has historically left un-traced.
No lint violation (`orphan-feature=0`). Low severity — add one-line REQ rows only if the
operator wants full cross-reference. Owner: analyst.

Confirmed **no orphan trace rows** — every `trace.toml feature=` slug maps to an existing
`spec/**/feature.md`.

## Decay markers (TODO / FIXME / TBD / XXX / ???)

**No feature folder exceeds the 5-marker threshold** (unchanged). Counts by folder
(excluding `spec/archive/`):

| Folder                     | Count | Flag                                             |
|----------------------------|-------|--------------------------------------------------|
| dev-notes (NOT a feature)  | 63    | self-reference: audit notes quote the grep patterns literally; exclude |
| lumen-design-adoption      | 4     | below threshold (was 4 last audit)               |
| v1/ui-rethink-phase-a-lab  | 2     | benign                                           |
| architecture (section)     | 2     | phantom-`models` carry-over context              |
| v1/ui-headless-emulator    | 1     | benign                                           |
| v1/operator-ledger-schema-lint | 1 | benign                                           |
| v1/live-equity-history-durable | 1 | benign                                           |
| v1/advisor-llm-narration   | 1     | single TBD                                        |
| ui-design-principles.md    | 1     | FALSE POSITIVE — `XXXL` spacing token matched `XXX` |

The 63 in `dev-notes` are the audit notes themselves spelling out the marker patterns —
not real decay (rose from 43 as more audit notes accrued). No feature is decay-heavy.

## Soft contradictions (LLM-judged — verify before acting)

Swept 5 cross-cutting topics from CLAUDE.md non-negotiables against `product.md` and
`spec/architecture/`. **No hard contradiction found.**

### SC-1 — Money math (`Decimal`, never `f64`) — CONSISTENT (HIGH)

`product.md:285` "Money math uses `Decimal`, never `f64` (every monetary value is
`Money<C>`)"; `product.md:220` "all sizing are `Money<Usdt>`; no `Eur` currency type
exists". The shipped `advisor-cost-model-opt-in` (VolScaledSpread fee model, this week)
introduces no `f64` money surface — `product.md:711` confirms `BacktestKpis.final_equity:
Money<Usdt>`. No opposing claim. No action.

### SC-2 — Determinism / seeded reproducibility — CONSISTENT (HIGH)

`product.md:29/45/78` describe "deterministic signal-vote mixes", "a deterministic rule
engine with no forecast", and "an honest, measured, reproducible bake-off". Aligns with
the 119/119 anchor gate. The new `advisor-no-alpha-gate-ci` (null-falsification CI) and
`advisor-narration-faithfulness` (verbatim-number match) reinforce, not contradict, the
determinism posture. No contradiction.

### SC-3 — UI render-pixel verification — CONSISTENT (HIGH)

CLAUDE.md requires Iced/cockpit UI verified at the rendered-PIXEL layer. The v2 UI
features this week (`advisor-overfitting-scorecard` P0-1 block, `advisor-turnover-and-tail-metrics`
Churn column) land alongside existing render-snapshot harnesses; no opposing claim in
architecture. Not exhaustively re-verified this run — flagged as an area, not a finding.

### SC-4 — `anchors.toml` header comment says `87/87` — actual 119/119 — STALE COMMENT (HIGH, carry-over, low severity)

`spec/anchors.toml` header still reads "expects 87/87 PASS — 2026-05-31". Actual entry
count is 119 and `verify_anchors.sh` reports `119 / 119`. Same SC-4 the last three audits
flagged and the orchestrator DEFERRED ("fix on the next anchor-touching commit — dated
historical narrative"). Still open. Cosmetic — `verify_anchors.sh` does not read the
comment. No functional impact.

### SC-5 — Phantom `crates/models` in architecture files — CARRY-OVER (HIGH, low severity)

`crates/models` does not exist, yet it is referenced in `spec/architecture/12-forecast-overlay.md`,
`spec/architecture/06-ui-and-cockpit.md`, and ADRs 0021/0023/0064. Pre-registered architect
doc-sweep item, DEFERRED at prior audits. Documentation-only, no code discrepancy. No new
movement.

## Resolved / improved since baseline

| Finding (baseline 2026-06-29)                         | Status now       | Evidence                                  |
|-------------------------------------------------------|------------------|-------------------------------------------|
| CLAUDE.md day-1 divergence e2e for new overlays       | **HELD**         | drawdown-control + vol-overlay-reposition both cite e2e |
| Lint PASS(0) / anchors 119/119                         | HELD             | unchanged, green                          |
| No hard contradictions                                 | HELD             | SC-1..SC-3 consistent                     |

**No baseline finding was closed by triage this week** — the two P1s and the P2 (SS-WIRE)
carried straight through.

## Recommended triage

- **P1 — Flip the 3 lagging `trace.toml` rows to `shipped` (CARRY-OVER, now 1 cycle stale).**
  `advisor-param-promotion` (design-complete→shipped), `advisor-param-tuning`
  (in-progress→shipped), `advisor-signal-library-expansion` (implemented→shipped). All
  shipped in `feature.md` last week with test reports on disk; untriaged for a full audit
  cycle. Populate `tests=`/`anchors=` to match. Owner: tester / orchestrator.

- **P1 — `cockpit-cross-platform` CI gate (carry-over, unchanged).** Still `in-progress`
  (21d), still operator-deferred. Cross-platform claim unverified off-macOS for a 5th
  consecutive audit (06-08/15/22/29, now 07-06). Owner: operator → tester.

- **P2 — Decide the `verified`/`passed` trace-state vocabulary (NEW).** 22 shipped features
  sit at tester-terminal states (`verified` ×10, `passed` ×11, `paper-mode-equity-wiring`
  missing `state=`) not in the `trace.toml` legend. Architect: either bless them in the
  legend or add a close-time flip-to-`shipped` rule. Do NOT bulk-edit rows first. Owner:
  architect.

- **P2 — Wire `advisor-short-selling` e2e into trace (SS-WIRE, carry-over).** `tests=[]`
  despite `crates/backtest/tests/short_selling_divergence_e2e.rs` passing on disk. Populate
  `REQ-ADVISOR-SHORT-SELLING-001 tests=`. Owner: tester.

- **P2 — Approve or return the 7 `presenter-done` features (carry-over).** All carry PASS
  verdicts, accumulating un-ticked operator approvals. Owner: operator.

- **P3 — Trace coverage gaps grew 13→21.** 6 `lumen-phase-*` + 2 rollup folders
  (`phase-2c-overlays`, `phase-2d`) + carry-overs. Add one-line REQ rows only if full
  cross-reference is wanted; rollups likely intentional. Owner: analyst.

- **P3 — `anchors.toml` header comment stale (`87/87`→`119/119`, SC-4, carry-over).**
  Cosmetic; update on the next anchor-touching commit. Owner: next tester/dev.

- **P3 — Phantom `crates/models` in architecture files (SC-5, carry-over).**
  Documentation-only sweep. Owner: architect/developer.

- **P3 — Aging watch items.** `lumen-design-adoption` (63d), `cockpit-app-bundle` (56d),
  `iced-ecosystem-evaluation` (54d), `ui-gallery-table-cell` (51d). All +7d, no movement,
  4th cycle. Park or advance. Owner: analyst / operator.

## Deltas vs baseline (numerical)

| Metric                                | This run | Baseline (2026-06-29) | Δ        |
|---------------------------------------|----------|-----------------------|----------|
| Lint violations (total)               | 0        | 0                     | 0        |
| Anchors PASS                          | 119/119  | 119/119               | 0        |
| Features in scope                     | 148      | 131                   | +17      |
| Shipped features                      | 117      | 100                   | +17      |
| Strict-stale (proposed/in-prog >30d)  | 0        | 0                     | 0        |
| Aging watch items                     | 4        | 4                     | 0 (+7d each) |
| feature.md↔trace status drift (total) | 25       | 3                     | +22      |
| — Class A genuine mid-pipeline lag    | 3        | 3                     | 0 (untriaged) |
| — Class B tester-terminal vocabulary  | 22       | (not separated)       | new framing |
| Trace coverage gaps                   | 21       | 13                    | +8       |
| Decay-heavy feature folders (>5)      | 0        | 0                     | 0        |
| Hard contradictions                   | 0        | 0                     | 0        |

## Changelog

- 2026-07-06 (spec-auditor): Weekly scheduled audit. 23 commits scoped since the
  2026-06-29 baseline — dominated by the v2 Phase 2A→2D build shipping to feature-complete
  (10 new advisor features + 2 rollup folders, ADR-0075–0081). HEADLINE: lint PASS(0),
  anchors 119/119, but feature.md↔trace.toml state drift rose 3→25. Class A (3 genuine
  mid-pipeline lag rows: advisor-param-promotion/param-tuning/signal-library-expansion)
  is UNCHANGED from last week — last audit's P1 was not triaged (now 1 cycle stale). Class
  B (22 rows) is the v2 build landing under a `verified`/`passed` tester-terminal vocabulary
  absent from the trace.toml legend — a schema question for the architect, not 22 defects.
  POSITIVE: both new v2 overlays (drawdown-control, vol-overlay-reposition) ship with the
  CLAUDE.md day-1 divergence e2e — non-negotiable held. Carry-overs unchanged:
  cockpit-cross-platform CI (P1, 5th audit), SS-WIRE tests=[] (P2), 7 presenter-done
  awaiting approval (P2), anchors.toml 87/87 header (P3 deferred), phantom crates/models
  (P3 deferred), 4 aging watch items (+7d). Trace coverage gaps 13→21 (+6 lumen-phase-*,
  +2 rollup folders). Zero strict-stale, no decay-heavy folder, no hard contradiction.
