---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-07-29
---

# Spec Audit — 2026-07-29 (weekly cadence — first POST-BMAD-migration audit)

## Headline

**The first audit run against the BMAD v6 layout: the entire audit window (2026-07-20 →
2026-07-29) contains the migration itself — 10 commits, Phases 0-5c — so the previous
audit's whole frame of reference (the `spec/` tree, `.claude/agents/`, `spec-update` skill)
no longer exists and every path in the 2026-07-20 note is now historical.** Despite the
largest structural change in the series, all mechanical gates are GREEN post-migration:
`spec_lint --all` PASS(0) across the re-founded 11 categories, `verify_anchors` 119/119
byte-identical, `adr_registry_check` exit 0, and the ADR-0082 triad (story `Status:` ↔
`trace.toml` state ↔ CHANGELOG) is lint-clean. The migration also **RESOLVED** the
5+-audit carry-over SC-5 (phantom `crates/models`) — the rewritten
`_bmad-output/planning-artifacts/architecture.md` now states the negative explicitly. Two
NEW findings are artifacts of the honest status remap, not new decay: `2-1
lumen-design-adoption` was mapped roadmap→`in-progress` and now trips the strict-stale rule
by letter (86d, but a structural umbrella), and `6-9 cockpit-cross-platform` escalated
35d→44d strict-stale. One residual cosmetic carry-over (SC-4, anchors header `87/87`) and
one new provenance observation (trace rows still cite the archived pre-bmad product/arch
docs) round out the ledger. Operator should read this as **"the machinery survived the
migration intact; two structural staleness flags and one provenance question need a
decision."**

## Run metadata

- Date: 2026-07-29 (scheduled weekly cadence; task ran 2026-07-29, today).
- Baseline: `docs/dev-notes/audit-2026-07-20.md` (most recent audit on disk).
- **Layout migration since baseline.** The task file was written against the pre-BMAD
  `spec/` layout (`spec/dev-notes/`, `spec/*/feature.md`, `.claude/agents/spec-auditor.md`,
  `spec/anchors.toml`, `spec/trace.toml`, `spec/product.md`). **None of those paths exist
  anymore.** The BMAD-METHOD v6 migration (ratified 2026-07-24, completed 2026-07-25, story
  6-10) moved everything. This audit was run against the live equivalents and the mapping is
  recorded below so the series stays comparable:

  | Pre-BMAD path (task file)              | Live path this audit used                                        |
  |----------------------------------------|------------------------------------------------------------------|
  | `.claude/agents/spec-auditor.md`       | `_bmad/custom/spec-auditor-charter.md` (live def) + archived verbatim at `docs/archive/pre-bmad-agents/spec-auditor.md` |
  | `spec/dev-notes/audit-*.md`            | `docs/dev-notes/audit-*.md`                                       |
  | `spec/*/feature.md` (feature records)  | `_bmad-output/implementation-artifacts/{epic}-{story}-{slug}.md` (`Status:` line) + `sprint-status.yaml` |
  | `spec/product.md`                      | `_bmad-output/planning-artifacts/PRD.md`                         |
  | `spec/architecture.md`                 | `_bmad-output/planning-artifacts/architecture.md`               |
  | `spec/anchors.toml`                    | `evidence/anchors.toml`                                          |
  | `spec/trace.toml`                      | `_bmad-output/planning-artifacts/trace.toml`                    |
  | `spec-update` skill (write path)       | RETIRED (ratified D5) — this note written directly              |

- **Commits in window: 10** (`git log --since='2026-07-20'` = 10). HEAD is `ae9b415`
  (`review(1-10): simple-strategies-realdata — first BMAD-native code-review PASS`,
  2026-07-27). The window is: `378e6e3` migration plan → `5582a74` Phase 0 (install BMAD
  v6.10.0) → `4df9ef4` Phase 1 (PRD + architecture spine) → `226d00b` Phase 2 (7 epics, 142
  retro stories, sprint-status) → `452ce02` Phase 3 (evidence/ move + resolver base-swap) →
  `d3a069c` Phase 4 (knowledge→docs/ + ADR corpus→planning-artifacts) → `440c7e4` Phase 5a
  (persona customizations) → `4e67191` Phase 5b (governance lints re-founded on the story
  layout) → `11422f2` Phase 5c (CLAUDE/AGENT/README cutover + legacy agents retired) →
  `ae9b415` first BMAD-native code-review closing story 1-10. **9 of 10 are the migration;
  1 is real product/review work.**
- Tooling: `spec_lint.py` requires Python 3.11+ (`tomllib`); this workspace ships 3.10. Ran
  under the standing `tomli`-as-`tomllib` shim (identical logic) — same run-environment note
  as every prior audit, not a finding.

## Mechanical lint

`PYTHONPATH=<tomllib-shim> python3 scripts/spec_lint.py --all` — **PASS (0 violations).**
Exit code 0. The lint was **re-founded on the story layout in Phase 5b** (commit `4e67191`):
`orphan-feature`→`orphan-story`, `shipped-no-tests`→`story-done-no-tests`,
`feature-shipped-trace-drift`→`story-done-trace-drift`,
`feature-shipped-changelog-missing`→`story-done-changelog-missing`. Same 11 categories,
renamed. All clean.

| Category                          | This run | Previous (2026-07-20) | Δ  | Note                          |
|-----------------------------------|----------|-----------------------|----|-------------------------------|
| dead-link                         | 0        | 0                     | 0  |                               |
| missing-frontmatter               | 0        | 0                     | 0  |                               |
| orphan-story (was orphan-feature) | 0        | 0                     | 0  | renamed Phase 5b              |
| bad-anchor                        | 0        | 0                     | 0  |                               |
| unreferenced-anchor               | 0        | 0                     | 0  |                               |
| story-done-no-tests (was shipped-no-tests) | 0 | 0                  | 0  | renamed Phase 5b              |
| status-drift                      | 0        | 0                     | 0  |                               |
| story-done-trace-drift            | 0        | 0                     | 0  | renamed Phase 5b              |
| story-done-changelog-missing      | 0        | 0                     | 0  | renamed Phase 5b              |
| trace-broken-path                 | 0        | 0                     | 0  | see SC-NEW (paths are archived-but-present) |
| adr-not-registered                | 0        | 0                     | 0  | `adr_registry_check.py` exit 0 |
| **TOTAL**                         | **0**    | **0**                 | 0  |                               |

The triad held byte-for-byte through the migration — the single most important result of
this audit. The status re-founding (Phase 5b) explicitly re-derived `status-drift`,
`story-done-trace-drift`, `story-done-changelog-missing` on the story `Status:` line and
they land at 0.

## Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Exit 0. `evidence/anchors.toml`
holds 119 `[[anchors]]` entries (count confirmed via both `[[anchor` and `^[[` greps → 119).
The corpus moved `spec/ → evidence/` in Phase 3 (commit `452ce02`) but the SHA-256 bodies are
byte-identical — the move was a `git mv`, no re-emission. The 9 backtest scenario families
and their bodies are untouched; audit-only per instruction. **The evidence move is the
migration's highest-risk operation for this gate and it passed clean.**

## Story inventory (was "feature inventory")

**142 stories** in `_bmad-output/implementation-artifacts/*.md` (+ `sprint-status.yaml` and a
non-story `deferred-work.md`, both excluded). Statuses read from each story's authoritative
`Status:` line (AD-4, story-keyed since Phase 5b). Cross-referenced against
`sprint-status.yaml` (`last_updated: 2026-07-27 00:30`).

BMAD status vocabulary is `{backlog, ready-for-dev, in-progress, review, done, retired}` —
the pre-BMAD 13-value vocabulary (`shipped`, `presenter-done`, `tester-done`, `dev-done`,
`candidate`, `draft`, `roadmap`, `reserved`, `arch-done`, `deprecated`, …) was HONESTLY
retro-mapped (never silently promoted), per the mapping embedded in `sprint-status.yaml`:

```
shipped -> done                          retired/deprecated -> done (story file: retired)
presenter/tester/dev-done -> review      (frozen mid-pipeline at the 2026-06-17 compression)
arch-done -> ready-for-dev               candidate/draft/reserved -> backlog
in-progress -> in-progress               roadmap umbrella -> in-progress (see 2-1)
```

| Status         | This run | Maps from (pre-BMAD)                    | Note                                    |
|----------------|----------|-----------------------------------------|-----------------------------------------|
| done           | 113      | shipped (124) − remaps + retired/deprecated | terminal                            |
| review         | 13       | presenter-/tester-/dev-done             | frozen mid-pipeline (was 14; 1-10 closed 07-27) |
| retired        | 9        | retired (5) + deprecated (4)            | terminal (story file says `retired`)    |
| backlog        | 4        | candidate/draft/reserved                | non-strict                              |
| in-progress    | 2        | in-progress (1) + roadmap-umbrella (1)  | **both strict-stale — see below**       |
| ready-for-dev  | 1        | arch-done (1)                           | `3-18` C4 done, not built               |
| **TOTAL**      | **142**  |                                         |                                         |

No story is missing a `Status:` line (only `deferred-work.md`, a non-story backlog list, has
none — expected). No duplicate story ids.

## Stale stories — 2 strict-stale (both structural, +1 vs baseline)

The strict rule (`in-progress` — the BMAD equivalent of the pre-BMAD `proposed`/`in-progress`
set — not substantively updated 30+ days) trips on **both** `in-progress` stories. **Caveat:
the migration reset every story file's git mtime to 2026-07-25 and BMAD stories carry NO
`updated:` frontmatter**, so "days since update" is NOT computable from the story file; it is
taken from the archived pre-bmad `feature.md` `updated:` frontmatter (the last substantive
date), continuing the series' methodology.

- **`6-9 cockpit-cross-platform`** — `in-progress`; underlying `updated: 2026-06-15` → **44
  days** (was 35d at baseline, 28d the audit before). Escalated exactly as the last two audits
  predicted. `sprint-status` records the real activity as "CI matrix ACTIVATED 2026-07-10
  (P7); run-2 shakeout on Linux/Windows lanes is the open work (fix-forward)" — so the true
  last-touch is arguably 2026-07-10 (19d), but the frontmatter date was never bumped and the
  story now carries no date at all. Either way the Linux/Windows verification is still open.
  **Carry-over P1, escalated.**

- **`2-1 lumen-design-adoption`** — `in-progress`; underlying `updated: 2026-05-04` → **86
  days**. This is NEW to the strict-stale set and is a **migration artifact**: it was
  `roadmap` at baseline (a non-strict "aging watch item"), and the Phase 2 remap mapped the
  roadmap umbrella to `in-progress`. It is a **structural umbrella** — its child phases 1-5
  (`2-2`…`2-6`) are all `done`, phase 6 (`2-7`) is separately `backlog`/reserved. The
  "in-progress" reflects an umbrella that stays open until the reserved phase 6 lands, NOT
  stalled work. **New P2: decide — accept as structural (and document it), or re-status to a
  non-strict state so it stops tripping the rule.**

### Aging watch items (NON-strict — `backlog`/`ready-for-dev`, carry-over)

Age from the archived `feature.md` `updated:` frontmatter, relative to 2026-07-29:

| Story                              | Status        | Underlying updated | Age  | Was (2026-07-20)     |
|------------------------------------|---------------|--------------------|------|----------------------|
| `2-7 lumen-phase-6-assistant-slot` | backlog       | 2026-05-04         | 86d  | reserved 77d (+9d)   |
| `2-62 cockpit-app-bundle`          | backlog       | 2026-05-11         | 79d  | candidate 70d (+9d)  |
| `2-61 iced-ecosystem-evaluation`   | backlog       | 2026-05-13         | 77d  | candidate 68d (+9d)  |
| `2-53 ui-gallery-table-cell`       | backlog       | 2026-05-16         | 74d  | draft 65d (+9d)      |
| `2-63 lab-recipe-test-harness…`    | backlog       | 2026-06-17         | 42d  | (not listed) — the one genuinely-open forward build item per sprint-status |
| `3-18 advisor-reflection-decision-loop` | ready-for-dev | 2026-06-26    | 33d  | arch-done — C4 done, not built |

All carry non-strict statuses so they do not trip the strict rule, but the top four are now
9-10 weeks stale (7th consecutive audit for the lumen pair). `2-63` and `3-18` are the two
genuinely-open forward items on an otherwise-done board (per `sprint-status` "honest NON-DONE
exceptions"). "Park or advance" is overdue for the lumen/cockpit-app/iced quartet.

## Orphans

- **Story folders missing required files:** none (`orphan-story = 0`).
- **Test reports for missing stories:** none.
- **Crates unreferenced:** no new orphan crate. 17 workspace members
  (`agent audit backtest core cost data exec features forecast llm reflection replay-cache
  reports risk strategy trader ui`) — all present, all named in `architecture.md`'s Structural
  Seed table. The old SC-5 phantom (a spec-referenced crate that does NOT exist) is now
  **RESOLVED** — see SC-5 below.
- **Orphan trace rows:** 0 genuine (the `live`/`realdata`/`yahoo` cargo-feature-gate false
  positives from prior audits are unchanged and remain false positives).

## Trace coverage gaps (16 → 16, flat; composition shifted by one)

16 in-scope story slugs have no `trace.toml` `[[req]]` row (after normalizing the story
filename `v0-1-2` ↔ trace `feature=` `v0.1.2` dot/dash mismatch — the raw `comm` gives a
misleading 21; the true figure is 16). 140 distinct `feature=` values across 147 `[[req]]`
rows.

```
UI-infra / rollup umbrellas (likely intentional, 8):
  lumen-phase-1-foundation … lumen-phase-6-assistant-slot (6, UI-infra convention)
  phase-2d   bmad-method-migration  (2 umbrella/process stories)

v1 carry-over (8, identical set to baseline):
  chart-fixture-line-clipping   leaderboard-timeframe-capital
  paper-soak-longevity          ui-drop-iced-aw
  ui-gallery-bin                ui-gallery-table-cell
  ui-headless-emulator          ui-session-journal-iced-tester
```

Net flat at 16, but composition changed by one: `phase-2c-overlays` (baseline) dropped out,
`bmad-method-migration` (the new process meta-story `6-10`) entered. `bmad-method-migration`
having no `[[req]]` row is correct — it is process, not product. Low severity; add one-line
REQ rows only if full cross-reference is wanted. Owner: analyst.

## Anchor coverage gaps

`unreferenced-anchor = 0` (lint) — every one of the 119 anchors is cited from `trace.toml`
`anchors = [...]` arrays. `verify_anchors.sh` green at 119/119. No shipped strategy story is
left with zero anchor coverage that the lint would surface. No new gap. (The stale header
comment in `evidence/anchors.toml` — SC-4 — is cosmetic and separate; `verify_anchors.sh`
does not read it.)

## Decay markers (TODO / FIXME / TBD / XXX / ???)

**No story or planning folder exceeds the 5-marker threshold.** Counts across the live BMAD
trees (excluding `docs/archive/`, `.git`, and the byte-frozen `evidence/**/reports/`):

| Area                                    | Count | Flag                                                      |
|-----------------------------------------|-------|-----------------------------------------------------------|
| `_bmad-output/implementation-artifacts` (142 stories) | 0 | clean — no story file carries a decay marker         |
| `_bmad-output/planning-artifacts` (PRD/arch/epics)    | 1 | single TODO, below threshold                          |
| `docs/dev-notes/audit-*.md`             | 6-12 each | SELF-REFERENCE — the audit notes spell out the grep patterns; exclude (same false positive every audit) |
| `docs/ui-design-principles.md`          | 1     | FALSE POSITIVE — `XXXL` spacing token matched `XXX`       |

The story layer is markedly cleaner than the old `spec/*/feature.md` tree on this axis (the
migration did not carry decay markers into the retro-generated stories). No story is
decay-heavy.

## Soft contradictions (LLM-judged — verify before acting)

Swept the 5 cross-cutting non-negotiable topics (RNG seeding, money math, timestamps,
no-clocks-in-UI-tests, sub-agent capability boundary) against `PRD.md`, `architecture.md`
(AD-1..AD-19), and the relevant surfaces. The migration is docs-only for money/RNG/timestamp
code paths (the one code commit, `ae9b415`, was a code-review that fixed a vacuous-UI-guard
test + 3 production bugs — scenario-name collision, unindented-frontmatter, Compare-skip CSV
bug — none touching money/RNG/determinism), so the standing three re-confirm; one carry-over
is now RESOLVED and one NEW provenance item appears.

### SC-1 — Money math (`Decimal`, never `f64`) — CONSISTENT (HIGH)

`PRD.md:423` ("Money math uses `Decimal`, never `f64`; every monetary value is typed
`Money<C>`") and `architecture.md` AD-9 (`:191`, `:195` "money is `Money<C: Currency>` over
`rust_decimal::Decimal` — never `f64`") agree verbatim. No new money surface landed. No
opposing claim. No action.

### SC-2 — Determinism / seeded reproducibility (RNG) — CONSISTENT (HIGH)

No new decision-path code. The PIT-discipline / seed + `last_bar_ts` / no-wall-clock posture
carried into `architecture.md` (AD data-row: `TimestampMs` i64 ms-since-epoch as the PIT join
key, ADR-0058 §D3) unchanged. No contradiction found.

### SC-3 — Timestamps & no-clocks-in-UI-tests — CONSISTENT (HIGH, not re-verified at pixel layer)

The CLAUDE.md non-negotiable (verify Iced UI at the rendered-pixel layer; no wall-clock in
test harnesses) is not contradicted by any live planning text. Not exhaustively re-run at the
pixel layer this migration week — flagged as an area, not a finding.

### SC-4 — `anchors.toml` header says `87/87` — actual 119/119 — STALE COMMENT (carry-over, cosmetic)

`evidence/anchors.toml:22` still reads "Verify with: scripts/verify_anchors.sh (expects 87/87
PASS — 2026-05-31)". Actual count 119; `verify_anchors.sh` reports 119/119. Same SC-4 flagged
the last six audits and DEFERRED ("fix on the next anchor-touching commit"). The Phase 3
`git mv` of the corpus was NOT an anchor-touching (body-re-emitting) commit, so it persisted.
Cosmetic — the script does not read the comment; anchor-additivity is unaffected. No
functional impact. Owner: next tester/dev on an anchor-touching commit.

### SC-5 — Phantom `crates/models` — **RESOLVED by the migration** (was HIGH carry-over, 5+ audits)

Previously flagged every audit since 2026-06-22: `crates/models` did not exist yet was
referenced as if it did in `spec/architecture/00-current-state.md`,
`spec/architecture/12-forecast-overlay.md`, and `spec/architecture.md`. The Phase 1/Phase 4
architecture rewrite **cleared it**: the live `_bmad-output/planning-artifacts/architecture.md:457`
now states the negative explicitly — "**There is no `crates/models`** — ML/DL work lives in
`forecast` + `features`" — and the review rubric
(`architecture/architecture-trading-2026-07-24/reviews/review-rubric-walker.md:103`) confirms
"17/17 members match root Cargo.toml exactly; no `crates/models`; `forecast`+`features` claim
holds." The old phantom-bearing section files (`00-current-state.md`, `12-forecast-overlay.md`)
were NOT carried into the new planning-artifacts architecture tree. **First carry-over the
migration retired outright — mark SC-5 CLOSED.** (Two historical mentions remain in
`docs/archive/pre-bmad-spec/**` — frozen history, out of scope.)

### SC-NEW — trace `[[req]]` rows cite the ARCHIVED pre-bmad product/arch docs (LLM-judged, MEDIUM, flag uncertainty)

124 of 147 `[[req]]` rows set `product = "docs/archive/pre-bmad-spec/product.md"` (i.e., the
FROZEN archived copy, not the live `_bmad-output/planning-artifacts/PRD.md`). The `arch=`
field is mixed: newer ADR-linked rows point at the LIVE
`_bmad-output/planning-artifacts/architecture/decisions/00XX-*.md`, but many still cite
`docs/archive/pre-bmad-spec/architecture.md` and archived `feature.md`/`tasks.md` anchors.
The lint's `trace-broken-path` passes at 0 because the archived files still exist on disk, so
this is NOT a mechanical break. **Two readings, operator/architect to decide:** (a) INTENTIONAL
provenance — each row records the doc that governed the feature at ship time, and history is
correctly frozen (no action); or (b) a migration-sweep MISS — trace should point at the live
governing PRD.md/architecture.md, in which case a targeted `product=`/`arch=` rewrite is owed.
**Flagged with uncertainty** — I lean (a) is defensible for provenance, but the split `arch=`
field (some rows already updated to live ADRs, most not) suggests an incomplete sweep rather
than a deliberate policy. Owner: architect (decide policy) → analyst (execute if (b)).

## Resolved / improved since baseline

| Item                                       | Movement                                                   |
|--------------------------------------------|------------------------------------------------------------|
| SC-5 phantom `crates/models`               | **RESOLVED** by the Phase 1/4 architecture rewrite (5+-audit carry-over CLOSED) |
| Governance lints                           | Re-founded on the story layout (Phase 5b) — still PASS(0)  |
| Anchors through the `spec/→evidence/` move | 119/119 byte-identical — the migration's riskiest op, clean |
| ADR triad through the migration            | status-drift / trace-drift / changelog-missing all 0       |
| Story 1-10 simple-strategies-realdata      | Closed `review → done` by first BMAD-native code-review (07-27); review backlog 14→13 |
| Decay markers in the feature/story layer   | Story files carry ZERO markers (cleaner than old feature.md tree) |

Regressions (staleness sense only): `cockpit-cross-platform` 35d→44d; `2-1
lumen-design-adoption` entered strict-stale via the roadmap→in-progress remap; aging watch
items +9d each.

## Recommended triage

- **P1 — `6-9 cockpit-cross-platform` is strict-stale at 44d `in-progress` (carry-over,
  escalated 35d→44d).** The Linux/Windows CI shakeout (activated 2026-07-10, P7) is still
  open. Either finish the run-2 fix-forward + sign off T-T1..3, or re-status to a non-strict
  state. Bonus hygiene: BMAD stories carry no `updated:` field, so this story's staleness is
  only computable from the archived `feature.md` — consider adding a date/last-touch
  convention to the story template. Owner: operator → tester.

- **P2 — `2-1 lumen-design-adoption` now trips strict-stale (86d) as a migration artifact.**
  The roadmap→`in-progress` remap moved a structural umbrella (phases 1-5 done, phase 6
  reserved) into the strict-eligible set. Decide: accept-as-structural + document it in the
  story, or re-status so it stops tripping. Owner: operator / analyst.

- **P2 — SC-NEW: decide trace `product=`/`arch=` provenance policy.** 124/147 rows cite the
  archived pre-bmad `product.md`; `arch=` is split live-ADR vs archived. Confirm this is
  intended provenance-to-history, or schedule a targeted sweep to point at live governing
  docs. Owner: architect → analyst.

- **P2 — 13 `review`-status stories frozen mid-pipeline (was 14; 1-10 closed).** All are
  presenter-/tester-/dev-done work frozen at the 2026-06-17 compression, now carrying the
  BMAD `review` status. The first BMAD-native code-review just cleared one (1-10) — the same
  path (`bmad-code-review`) can close the rest. Owner: operator (prioritise) → dev/reviewer.

- **P3 — Aging watch items (park or advance).** `2-7 lumen-phase-6` (86d), `2-62
  cockpit-app-bundle` (79d), `2-61 iced-ecosystem-evaluation` (77d), `2-53
  ui-gallery-table-cell` (74d) — 7th consecutive audit for the lumen pair. Plus the two
  genuinely-open forward items `2-63 lab-recipe-test-harness` (42d) and `3-18
  advisor-reflection-decision-loop` (ready-for-dev, 33d, C4 done not built). Owner: analyst /
  operator.

- **P3 — Trace coverage gaps = 16 (flat).** 6 `lumen-phase-*` + 2 umbrella/process
  (`phase-2d`, `bmad-method-migration`) likely intentional; 8 v1 UI/infra carry-overs. Add
  one-line REQ rows only if full cross-reference is wanted. Owner: analyst.

- **P3 — `evidence/anchors.toml` header stale (`87/87` → `119/119`, SC-4, carry-over).**
  Cosmetic; update on the next anchor-touching commit. Owner: next tester/dev.

## Deltas vs baseline (numerical)

| Metric                                | This run (2026-07-29) | Baseline (2026-07-20) | Δ         |
|---------------------------------------|-----------------------|-----------------------|-----------|
| Commits in window                     | **10** (9 migration)  | 0                     | **+10**   |
| Repo layout                           | **BMAD v6**           | pre-BMAD `spec/`      | migrated  |
| Lint violations (total)               | 0                     | 0                     | 0         |
| Lint categories                       | 11 (re-founded)       | 11                    | 0 (renamed) |
| Anchors PASS                          | 119/119               | 119/119               | 0         |
| ADR registry                          | exit 0                | (not run)             | clean     |
| Records in scope                      | 142 stories           | 155 features          | layout change (not comparable 1:1) |
| Terminal (done + retired)             | 122                   | ~140 (shipped/retired/etc) | layout change |
| Strict-stale (`in-progress` >30d)     | **2**                 | 1                     | **+1**    |
| Aging watch items                     | 6                     | 5                     | +1 (+9d each) |
| story ↔ trace status drift            | 0                     | 0                     | 0         |
| Trace coverage gaps                   | 16                    | 16                    | 0 (composition shift ×1) |
| Orphan trace rows (genuine)           | 0                     | 0                     | 0         |
| Decay-heavy folders (>5)              | 0                     | 0                     | 0         |
| Hard contradictions                   | 0                     | 0                     | 0         |
| Open soft contradictions              | 2 (SC-4, SC-NEW)      | 3 (SC-4/5/6)          | **−1** (SC-5 resolved; SC-6 n/a — see note) |

Note on SC-6: the baseline's SC-6 (DSR-retraction propagation to
`advisor-corpus-expansion/feature.md:609` + `tasks.md:281`) was scoped to the pre-bmad
`spec/v3/` tree, now frozen at `docs/archive/pre-bmad-spec/v3/advisor-corpus-expansion/`. As
archived history it is no longer a LIVE editable surface, so it drops off the open-soft-
contradiction ledger (the anchored errata remains byte-immutable and correct). If the operator
wants the retraction reflected in the LIVE PRD/architecture surfaces, that is a separate
analyst task; the live `PRD.md` / `do-not-build-register.md` were spot-checked clean of the
retracted "16/19 clear DSR" phrasing this run.

## Changelog
- 2026-07-29 (spec-auditor): Weekly scheduled audit — **first run against the BMAD v6
  layout**. The audit window (2026-07-20→07-29) contains the entire migration (10 commits,
  Phases 0-5c; HEAD `ae9b415`); the pre-BMAD `spec/` tree, `.claude/agents/`, and
  `spec-update` skill no longer exist and were mapped to their live equivalents
  (`_bmad-output/{planning,implementation}-artifacts/`, `evidence/`, `docs/`,
  `_bmad/custom/spec-auditor-charter.md`). ALL mechanical gates GREEN post-migration:
  `spec_lint --all` PASS(0) across the 11 re-founded categories, `verify_anchors` 119/119
  byte-identical (the `spec/→evidence/` move was a clean `git mv`), `adr_registry_check` exit
  0, ADR-0082 triad drift 0. 142 stories (113 done, 13 review, 9 retired, 4 backlog, 2
  in-progress, 1 ready-for-dev). HEADLINE: the machinery survived intact; two structural
  staleness flags need a decision. SC-5 (phantom `crates/models`) **RESOLVED** by the Phase
  1/4 architecture rewrite (5+-audit carry-over CLOSED). NEW/escalated: `6-9
  cockpit-cross-platform` strict-stale 35d→44d (P1, carry-over); `2-1 lumen-design-adoption`
  entered strict-stale (86d) via the roadmap→in-progress remap — a structural umbrella, not
  stalled work (P2, decide re-status vs accept). NEW soft item SC-NEW: 124/147 trace rows
  cite the archived pre-bmad `product.md` (and split live/archived `arch=`) — provenance vs
  sweep-miss, operator/architect to decide (P2). Carry-overs: 13 review-status stories frozen
  mid-pipeline (was 14; 1-10 closed by the first BMAD-native code-review 07-27), 6 aging watch
  items +9d, anchors.toml `87/87` header (SC-4, P3 cosmetic), 16 trace coverage gaps (flat).
  Open soft contradictions 3→2 (SC-5 resolved; baseline SC-6 dropped as it is now frozen
  archive history).
