---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-08-03
---

# Spec Audit — 2026-08-03 (weekly cadence — 2nd post-BMAD-migration audit)

## Headline

**The machinery is healthy and the window is real product work, not churn: 5 commits (0
migration), all gates GREEN — `spec_lint --all` PASS(0), `verify_anchors` 119/119
byte-identical, `adr_registry_check` exit 0, ADR-0082 triad drift 0.** The window's theme is
*honest closure*: three review-status stories (1-12 carry-funding, 1-13 monte-carlo, 1-14
strategy-robustness-harness) were closed `review → done` by BMAD-native code reviews, and the
reviews did NOT paper over their findings — a CRITICAL fill-arithmetic bug (**bug-log #67**)
was disclosed and handed to a NEW re-lock story (1-25) rather than silently patched, and a
PWSD-fidelity gap went to a NEW story (1-24). The `adversarial-review` hardening commit
(`bcfd13b`) **machine-enforced the constitution** — a `.githooks/pre-commit` + CI now run the
triad lint and anchor gate on every commit (previously discipline-only, bug-log #66) — and
**resolved the bulk of last week's SC-NEW**: all 152 trace `[[req]]` rows now cite the LIVE
`PRD.md` (was 124/147 citing the archived pre-bmad `product.md`). Two carry-overs persist:
both `in-progress` stories remain strict-stale (2-1 lumen-design-adoption 91d, 6-9
cockpit-cross-platform 49d — each +5d, no in-window activity), and the `anchors.toml` `87/87`
header (SC-4) is untouched. Read this as **"gates green, one genuine open CRITICAL now
properly owned (1-25), and two staleness flags still awaiting an operator decision."**

## Run metadata

- Date: 2026-08-03 (scheduled weekly cadence; Monday).
- Baseline: `docs/dev-notes/audit-2026-07-29.md` (most recent audit on disk).
- **Layout note (unchanged since last audit).** The scheduled task file is still written
  against the pre-BMAD `spec/` layout (`spec/dev-notes/`, `spec/*/feature.md`,
  `.claude/agents/spec-auditor.md`, `spec/anchors.toml`, `spec/trace.toml`, `spec/product.md`).
  **None of those paths exist** — the BMAD-METHOD v6 migration (completed 2026-07-25) moved
  everything. This audit ran against the live equivalents, same mapping as the 2026-07-29 note:

  | Pre-BMAD path (task file)             | Live path this audit used                                            |
  |---------------------------------------|----------------------------------------------------------------------|
  | `.claude/agents/spec-auditor.md`      | `_bmad/custom/spec-auditor-charter.md` (live) + archived at `docs/archive/pre-bmad-agents/spec-auditor.md` (template) |
  | `spec/dev-notes/audit-*.md`           | `docs/dev-notes/audit-*.md`                                          |
  | `spec/*/feature.md`                   | `_bmad-output/implementation-artifacts/{epic}-{story}-{slug}.md` (`Status:` line) + `sprint-status.yaml` |
  | `spec/product.md`                     | `_bmad-output/planning-artifacts/PRD.md`                            |
  | `spec/architecture.md`                | `_bmad-output/planning-artifacts/architecture.md`                  |
  | `spec/anchors.toml`                   | `evidence/anchors.toml`                                              |
  | `spec/trace.toml`                     | `_bmad-output/planning-artifacts/trace.toml`                       |
  | `spec-update` skill (write path)      | RETIRED (ratified D5) — this note written directly                  |

- **Commits in window: 5** (`git log --since='2026-07-29'` = 5; **0 migration, all real
  product/governance work**). HEAD is `03f2eb2` (2026-08-02). Window, oldest→newest:
  - `bcfd13b` harden: adversarial-review pass — machine-enforce the constitution (17 findings actioned)
  - `9915f1e` feat(stories)+review(1-12): three decided stories scoped ready-for-dev; 1-12 reviewed
  - `719a58f` review(1-12)-close: carry-funding-data-backfill done — triad flipped (burn-down 2/14)
  - `f4228e0` review(1-13)-close: monte-carlo-bootstrap done — PWSD fidelity owned by new story 1-24 (burn-down 3/14)
  - `03f2eb2` review(1-14)-close: strategy-robustness-harness done — CRITICAL #67 owned by new story 1-25 (burn-down 4/14)
- Tooling: `spec_lint.py` requires Python 3.11+ (`tomllib`); this workspace ships 3.10. Ran
  under the standing `tomli`-as-`tomllib` shim (identical logic) — same run-environment note as
  every prior audit, not a finding.

## Mechanical lint

`PYTHONPATH=<tomllib-shim> python3 scripts/spec_lint.py --all` — **PASS (0 violations), exit 0.**
Same 11 story-layout categories (re-founded Phase 5b). All clean, flat vs baseline.

| Category                       | This run | Previous (2026-07-29) | Δ  | Note                                        |
|--------------------------------|----------|-----------------------|----|---------------------------------------------|
| dead-link                      | 0        | 0                     | 0  |                                             |
| missing-frontmatter            | 0        | 0                     | 0  |                                             |
| orphan-story                   | 0        | 0                     | 0  |                                             |
| bad-anchor                     | 0        | 0                     | 0  |                                             |
| unreferenced-anchor            | 0        | 0                     | 0  | all 119 anchors cited from trace            |
| story-done-no-tests            | 0        | 0                     | 0  |                                             |
| status-drift                   | 0        | 0                     | 0  | 3 review→done flips landed clean            |
| story-done-trace-drift         | 0        | 0                     | 0  |                                             |
| story-done-changelog-missing   | 0        | 0                     | 0  |                                             |
| trace-broken-path              | 0        | 0                     | 0  | archived-but-present paths still resolve    |
| adr-not-registered             | 0        | 0                     | 0  | `adr_registry_check.py` exit 0              |
| **TOTAL**                      | **0**    | **0**                 | 0  |                                             |

The triad held byte-for-byte through 3 story closures. Governance note: as of `bcfd13b` these
checks are no longer discipline-only — `.githooks/pre-commit` + `.github/workflows/ci.yml` run
the triad lint and anchor gate on every commit (see "Resolved / improved").

## Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119), exit 0.** `evidence/anchors.toml`
holds 119 `[[anchors]]` entries; bodies byte-identical. No anchor-touching commit landed this
window (the 3 review closes were "anchor-safe patches" per their commit messages — the burn-down
notes say 4/14 patches applied so far, all anchor-safe). Audit-only per instruction.

## Story inventory

**147 stories** in `_bmad-output/implementation-artifacts/*.md` (+ `sprint-status.yaml` and the
non-story `deferred-work.md`, both excluded) — **+5 vs baseline (142)**, all 5 net-new files
added this window. Statuses read from each story's authoritative `Status:` line; cross-referenced
against `sprint-status.yaml`.

| Status         | This run | Baseline (07-29) | Δ   | Note                                                     |
|----------------|----------|------------------|-----|----------------------------------------------------------|
| done           | 116      | 113              | +3  | 1-12, 1-13, 1-14 closed `review → done`                  |
| review         | 10       | 13               | −3  | the same three left `review`                             |
| retired        | 9        | 9                | 0   | terminal                                                 |
| ready-for-dev  | 6        | 1                | +5  | **all 5 net-new stories** (see below)                    |
| backlog        | 4        | 4                | 0   | 2-7, 2-53, 2-61, 2-62                                     |
| in-progress    | 2        | 2                | 0   | **both strict-stale — see below**                        |
| **TOTAL**      | **147**  | **142**          | +5  |                                                          |

No story is missing a `Status:` line (only `deferred-work.md`, expected). No duplicate ids.

### The 5 net-new stories (all `ready-for-dev`, all trace-linked)

Verified each carries a `- Trace: REQ-... (state=scoped)` citation → trace rows went 147→152
(+5), coverage gaps stayed flat (see below). No new coverage gap introduced.

| Story                                        | Origin commit | Provenance                                                   |
|----------------------------------------------|---------------|--------------------------------------------------------------|
| `1-24 pwsd-fidelity-relock`                  | `f4228e0`     | 1-13 code-review finding (PWSD fidelity) — CRITICAL, re-lock  |
| `1-25 harness-fill-correctness-relock`       | `03f2eb2`     | 1-14 code-review **CRITICAL bug-log #67** (fill arithmetic)  |
| `2-63 lab-recipe-test-harness-v0-3-extension`| `9915f1e`     | one of "three decided stories scoped ready-for-dev"          |
| `3-19 advisor-eur-live-rate`                 | `9915f1e`     | scoped ready-for-dev                                          |
| `4-13 advisor-online-fdr-annex`              | `9915f1e`     | scoped ready-for-dev                                          |

## Stale stories — 2 strict-stale (both carry-over, both +5d, no in-window activity)

The strict rule (`in-progress` not substantively updated 30+ days) trips on both `in-progress`
stories — identical set to baseline. **Caveat (unchanged):** BMAD stories carry no `updated:`
frontmatter and the migration reset git mtimes to 2026-07-25, so "days since update" is taken
from the archived pre-bmad `feature.md` `updated:` date (the last substantive touch), continuing
the series methodology. Confirmed neither story file was touched by any commit this window.

- **`2-1 lumen-design-adoption`** — `in-progress`; underlying `updated: 2026-05-04` → **91 days**
  (was 86d). **Structural umbrella, not stalled work**: child phases 1-5 (`2-2`…`2-6`) all
  `done`; phase 6 (`2-7`) is separately `backlog`/reserved. Entered strict-stale last week via
  the roadmap→`in-progress` remap. **Carry-over P2 — still awaiting the decision flagged 07-29:**
  accept-as-structural + document in the story, or re-status to a non-strict state so it stops
  tripping. No movement this week.

- **`6-9 cockpit-cross-platform`** — `in-progress`; underlying `updated: 2026-06-15` → **49 days**
  (was 44d, 35d the audit before). Escalating on schedule. Real activity per `sprint-status` is
  the Linux/Windows CI matrix shakeout (activated 2026-07-10, P7); the run-2 fix-forward +
  T-T1..3 sign-off is still open. **Carry-over P1, escalated 44d→49d.**

### Aging watch items (NON-strict — `backlog`/`ready-for-dev`, carry-over, each +5d)

Age from the archived `feature.md` `updated:` date, relative to 2026-08-03:

| Story                                     | Status        | Underlying updated | Age  | Was (07-29) |
|-------------------------------------------|---------------|--------------------|------|-------------|
| `2-7 lumen-phase-6-assistant-slot`        | backlog       | 2026-05-04         | 91d  | 86d         |
| `2-62 cockpit-app-bundle`                 | backlog       | 2026-05-11         | 84d  | 79d         |
| `2-61 iced-ecosystem-evaluation`          | backlog       | 2026-05-13         | 82d  | 77d         |
| `2-53 ui-gallery-table-cell`              | backlog       | 2026-05-16         | 79d  | 74d         |
| `2-63 lab-recipe-test-harness-v0-3-*`     | ready-for-dev | 2026-06-17         | 47d  | 42d (was backlog) |
| `3-18 advisor-reflection-decision-loop`   | ready-for-dev | 2026-06-26         | 38d  | 33d         |

The lumen/cockpit-app/iced quartet is now 11-13 weeks stale (8th consecutive audit). "Park or
advance" remains overdue for that quartet. Note `2-63` flipped backlog→ready-for-dev this week
(scoped in `9915f1e`) — it and `3-18` are now genuinely-queued forward items, not parked.

## Orphans

- **Story folders missing required files:** none (`orphan-story = 0`).
- **Test reports for missing stories:** none.
- **Crates unreferenced:** none. All **17** workspace members
  (`agent audit backtest core cost data exec features forecast llm reflection replay-cache
  reports risk strategy trader ui`) are named in `architecture.md`'s Structural Seed table.
- **Orphan trace rows:** 0 genuine (the `live`/`realdata`/`yahoo` cargo-feature-gate false
  positives from prior audits persist and remain false positives).

## Trace coverage gaps (16 → 16, flat)

16 in-scope story slugs carry `- Trace: none — known trace-coverage gap` (the auditable marker).
Composition matches baseline: 6 `lumen-phase-*` (UI-infra convention) + 2 umbrella/process
(`phase-2d`, `bmad-method-migration`) + 8 v1 UI/infra carry-overs. The 5 net-new stories all
have proper REQ rows, so trace rows grew 147→152 (+5) with the gap count unchanged. Low
severity; add one-line REQ rows only if full cross-reference is wanted. Owner: analyst.

## Anchor coverage gaps

`unreferenced-anchor = 0` (lint) — every one of the 119 anchors is cited from `trace.toml`.
`verify_anchors.sh` green 119/119. No shipped strategy story left with zero anchor coverage. No
new gap. (The stale `anchors.toml` header — SC-4 — is cosmetic and separate; `verify_anchors.sh`
does not read it.)

## Decay markers (TODO / FIXME / TBD / XXX / ???)

**No story or planning folder exceeds the 5-marker threshold — the story + planning layer is now
entirely clean.**

| Area                                                  | Count | Flag                                                    |
|-------------------------------------------------------|-------|---------------------------------------------------------|
| `_bmad-output/implementation-artifacts` (147 stories) | 0     | clean — **zero** decay markers across all story files   |
| `_bmad-output/planning-artifacts` (PRD/arch/epics)    | 0     | **improved: was 1 TODO at baseline → 0**                |
| `docs/dev-notes/audit-*.md`                           | 6-12 each | SELF-REFERENCE — audit notes spell out the grep patterns; excluded (same false positive every audit) |
| `docs/ui-design-principles.md`                        | 1     | FALSE POSITIVE — `XXXL` spacing token matched `XXX`     |

The one planning-artifacts TODO flagged at baseline is gone. No decay-heavy folder.

## Soft contradictions (LLM-judged — verify before acting)

Swept the 5 cross-cutting non-negotiable topics (money math, RNG/determinism, timestamps,
no-clocks-in-UI-tests, sub-agent capability boundary) against `PRD.md`, `architecture.md`
(AD-1..AD-19), and the touched surfaces. The window's code delta was 3 review closures + one
`engine.rs` +87-line hardening; no money/RNG/timestamp invariant was contradicted. One carry-over
resolved, one carry-over persists (cosmetic), and the sharpest item this week is a disclosed
CRITICAL, not a contradiction.

### SC-1 — Money math (`Decimal`, never `f64`) — CONSISTENT (HIGH)

`PRD.md:423` ("Money math uses `Decimal`, never `f64`; every monetary value is typed `Money<C>`")
and `architecture.md` AD-9 (`:191`, `:195` "money is `Money<C: Currency>` over
`rust_decimal::Decimal` — never `f64`") agree verbatim. The `bcfd13b` `engine.rs` additions
introduced no `f64` money surface (grep of the `+` diff for `f64`/`Decimal`/`Money` in a
money-math context is empty). No opposing claim. No action.

### SC-2 — Determinism / seeded reproducibility (RNG) — CONSISTENT (HIGH)

No decision-path RNG change landed. Note: story **1-25** explicitly calls out `FILL_SEED domain
separation` as one of its riders (AC3) — i.e. a *known* seed-hygiene item scoped for the re-lock,
not a live contradiction. The PIT-discipline / `last_bar_ts` / no-wall-clock posture in
`architecture.md` is unchanged. No contradiction found; one seed-hygiene item is queued in 1-25.

### SC-3 — Timestamps & no-clocks-in-UI-tests — CONSISTENT (HIGH, not re-verified at pixel layer)

The CLAUDE.md non-negotiable (verify Iced UI at the rendered-pixel layer; no wall-clock in test
harnesses) is not contradicted by any live planning text. Two UI stories (`2-15`, `2-18`) sit in
`review`; not re-run at the pixel layer this window — flagged as an area, not a finding.

### SC-4 — `anchors.toml` header says `87/87` — actual 119/119 — STALE COMMENT (carry-over, cosmetic)

`evidence/anchors.toml:22` still reads "Verify with: scripts/verify_anchors.sh (expects 87/87
PASS — 2026-05-31…)". Actual count 119; `verify_anchors.sh` reports 119/119. (The per-block
incremental annotations further down — `69/69`, `70/70`, … `87/87` — are legitimate historical
provenance for each additive block and are NOT the finding; only the top summary is stale.) Same
SC-4 flagged 7+ audits; DEFERRED "fix on the next anchor-touching commit." No anchor-touching
commit landed this window, so it persisted. Cosmetic — the script does not read the comment.
Owner: next tester/dev on an anchor-touching commit.

### SC-NEW (from 07-29) — trace rows citing archived docs — **LARGELY RESOLVED** (product= swept; arch= residual)

Last week's SC-NEW flagged 124/147 `[[req]]` rows citing the FROZEN archived
`docs/archive/pre-bmad-spec/product.md` (and a split `arch=`). The `bcfd13b` hardening commit
rewrote `trace.toml` (294 lines changed) and **swept the `product=` field completely**: all 152
rows now cite the LIVE `_bmad-output/planning-artifacts/PRD.md` (0 archived). This resolves the
headline of SC-NEW — reading (b) ("point trace at the live governing doc") was executed for
`product=`.

**Residual (downgraded to LOW, LLM-judged):** the `arch=` field is still split — **62 rows cite
archived pre-bmad architecture paths, 32 cite the live planning-artifacts architecture** (152
total arch arrays; some rows carry multiple citations). Most of the 62 point at per-feature
frozen design anchors (`.../v1/<slug>/feature.md#design`) that are legitimately
provenance-to-ship-time and arguably *should* stay archived. A smaller subset cites the old
monolithic `docs/archive/pre-bmad-spec/architecture.md` and could be re-pointed at the live
spine if the operator wants `arch=` to match the `product=` sweep. `trace-broken-path` is still 0
(files exist), so this is NOT a mechanical break. **I lean: the product= sweep closed the real
concern; the arch= residual is defensible provenance — confirm intent, no urgent action.** Owner:
architect (confirm `arch=` provenance policy) → analyst (execute a targeted re-point only if
desired).

### SC-CRIT (NEW this window) — CRITICAL fill-arithmetic bug (#67) disclosed & owned, not silently patched — GOOD DISCIPLINE, but a genuine OPEN critical

Not a contradiction — a *disclosure*, surfaced here because it materially affects the credibility
of shipped evidence. The 1-14 code review found that the research-harness lanes
(`scenarios/montecarlo.rs::run_path` and `threshold_sweep.rs::run_cell`) price orders at the
**trigger bar's symbol** rather than **each order's own symbol's bar** (bug-log #67, CRITICAL).
Rather than patch in place (which would have silently mutated anchored evidence), the review
closed 1-14 as `done` and **handed the fix to a new re-lock story `1-25`** — with the frozen rows
to stay byte-immutable and a formal ADR-0038/0045 §D6 re-lock + errata + loud verdict
re-derivation. This is exactly the AD-18 "honest divergence → handoff, never silent" discipline
working as designed, and it is **the sharpest triage item this week**: until 1-25 lands, the C2
FRAGILE / sweep FAMILY-UNIFORM-FRAGILE research verdicts rest on incorrect fill arithmetic. Story
1-25 AC4 already flags that any verdict flip touching the era-qualified thesis escalates to the
operator BEFORE publication (AD-19 spirit). Owner: operator (prioritise the 1-24/1-25 re-lock
program) → architect (seam decision) → dev.

## Resolved / improved since baseline

| Item                                            | Movement                                                                 |
|-------------------------------------------------|--------------------------------------------------------------------------|
| SC-NEW trace `product=` provenance              | **RESOLVED** — `bcfd13b` swept all 152 rows to live `PRD.md` (was 124/147 archived) |
| Constitution enforcement                        | **NEW GATE** — `.githooks/pre-commit` + CI now run the triad lint + anchor gate every commit (was discipline-only; bug-log #66) |
| Review backlog                                  | 13 → 10 (`review`) — 1-12, 1-13, 1-14 closed by BMAD-native code reviews |
| Code-review honesty                             | 2 CRITICAL findings (#67 fill-arithmetic, PWSD fidelity) disclosed + owned by new stories 1-25/1-24, not silently patched |
| Planning-artifacts decay markers                | 1 → 0 (the single baseline TODO cleared)                                 |
| Forward queue                                   | +5 net-new stories, all trace-linked (`2-63` also flipped backlog→ready-for-dev) |

Regressions (staleness sense only): `6-9 cockpit-cross-platform` 44d→49d; `2-1
lumen-design-adoption` 86d→91d; aging watch quartet +5d each.

## Recommended triage

- **P1 — CRITICAL fill-arithmetic re-lock (bug-log #67) is now owned by `1-25` but OPEN.** Until
  the 1-24/1-25 re-lock program lands, the C2/C3 research verdicts rest on orders priced at the
  wrong symbol's bar. The disclosure discipline is exemplary; the fix is the top substantive
  item. Prioritise the re-lock program (one namespace, one re-run budget, one errata per the
  story). Owner: operator → architect (seam) → dev.

- **P1 — `6-9 cockpit-cross-platform` strict-stale 49d `in-progress` (carry-over, 44d→49d).** The
  Linux/Windows CI shakeout (activated 2026-07-10) is still open. Finish run-2 fix-forward +
  T-T1..3 sign-off, or re-status to non-strict. Owner: operator → tester.

- **P2 — `2-1 lumen-design-adoption` strict-stale 91d as a structural umbrella (no movement).**
  Phases 1-5 done, phase 6 reserved. Decide: accept-as-structural + document in the story, or
  re-status so it stops tripping the rule. Same decision as 07-29, still open. Owner: operator /
  analyst.

- **P2 — 10 `review`-status stories frozen mid-pipeline (was 13; three closed).** The
  `bmad-code-review` path just closed three (1-12/1-13/1-14) and is clearly working. Continue it
  down the queue (1-15…1-21 robustness sweep, 2-15/2-18 cockpit, 3-15/3-16 advisor). Owner:
  operator (prioritise) → dev/reviewer.

- **P3 — SC-NEW residual: confirm `arch=` provenance policy.** `product=` fully swept to live
  `PRD.md`; `arch=` remains 62 archived / 32 live (most legitimately frozen design anchors).
  Confirm intent or schedule a targeted re-point of the old-monolith citations. Owner: architect
  → analyst.

- **P3 — Aging watch quartet (park or advance).** `2-7 lumen-phase-6` (91d), `2-62
  cockpit-app-bundle` (84d), `2-61 iced-ecosystem-evaluation` (82d), `2-53 ui-gallery-table-cell`
  (79d) — 8th consecutive audit. Owner: analyst / operator.

- **P3 — `evidence/anchors.toml` header stale (`87/87` → `119/119`, SC-4, carry-over).** Cosmetic;
  update on the next anchor-touching commit (the 1-25 re-lock is a natural moment). Owner: next
  tester/dev.

- **P3 — Trace coverage gaps = 16 (flat).** 6 `lumen-phase-*` + 2 umbrella/process likely
  intentional; 8 v1 UI/infra carry-overs. Add one-line REQ rows only if full cross-reference is
  wanted. Owner: analyst.

## Deltas vs baseline (numerical)

| Metric                                | This run (2026-08-03) | Baseline (2026-07-29) | Δ          |
|---------------------------------------|-----------------------|-----------------------|------------|
| Commits in window                     | **5** (0 migration)   | 10 (9 migration)      | real-work window |
| Lint violations (total)               | 0                     | 0                     | 0          |
| Lint categories                       | 11                    | 11                    | 0          |
| Anchors PASS                          | 119/119               | 119/119               | 0          |
| ADR registry                          | exit 0 (88 ADRs)      | exit 0                | clean      |
| Stories in scope                      | **147**               | 142                   | **+5**     |
| — done                                | 116                   | 113                   | +3         |
| — review                              | 10                    | 13                    | −3         |
| — ready-for-dev                       | 6                     | 1                     | +5         |
| — in-progress                         | 2                     | 2                     | 0          |
| — backlog / retired                   | 4 / 9                 | 4 / 9                 | 0 / 0      |
| Terminal (done + retired)             | 125                   | 122                   | +3         |
| Strict-stale (`in-progress` >30d)     | 2                     | 2                     | 0 (both +5d) |
| Aging watch items                     | 6                     | 6                     | 0 (+5d each) |
| Trace `[[req]]` rows                   | 152                   | 147                   | +5         |
| Trace coverage gaps                   | 16                    | 16                    | 0          |
| Orphan trace rows (genuine)           | 0                     | 0                     | 0          |
| Decay-heavy folders (>5)              | 0                     | 0                     | 0          |
| Planning-artifacts decay markers      | 0                     | 1                     | **−1**     |
| Hard contradictions                   | 0                     | 0                     | 0          |
| Open soft contradictions              | ~1 (SC-4; SC-NEW residual LOW) | 2 (SC-4, SC-NEW) | **−1** (SC-NEW product= resolved) |
| Open CRITICAL disclosures (owned)     | 1 (#67 → 1-25)        | 0                     | +1 (surfaced + owned) |

## Changelog
- 2026-08-03 (spec-auditor): Weekly scheduled audit — **2nd post-BMAD audit, first with a
  real-product window** (5 commits, 0 migration; HEAD `03f2eb2`). ALL gates GREEN: `spec_lint
  --all` PASS(0) across 11 categories, `verify_anchors` 119/119 byte-identical,
  `adr_registry_check` exit 0 (88 ADRs), ADR-0082 triad drift 0. 147 stories (116 done, 10
  review, 9 retired, 6 ready-for-dev, 4 backlog, 2 in-progress); +5 net-new trace-linked stories.
  HEADLINE: machinery healthy; the window's theme is honest closure — three review stories
  (1-12/1-13/1-14) closed `review → done` by BMAD-native code reviews that DISCLOSED rather than
  buried their findings. **NEW**: a CRITICAL fill-arithmetic bug (bug-log #67 — orders priced at
  the trigger bar's symbol, not their own) was disclosed and owned by new re-lock story `1-25`
  (PWSD fidelity → new `1-24`), frozen rows to stay byte-immutable pending a formal ADR-0038/0045
  §D6 re-lock — exemplary AD-18 discipline but a genuine OPEN critical (P1). **RESOLVED**: last
  week's SC-NEW `product=` provenance — the adversarial-hardening commit `bcfd13b` swept all 152
  trace rows to the live `PRD.md` (was 124/147 archived); residual `arch=` split 62 archived / 32
  live downgraded to LOW (mostly legitimate frozen-design provenance). **IMPROVED**: constitution
  now machine-enforced via `.githooks/pre-commit` + CI (was discipline-only, bug-log #66);
  planning-artifacts decay markers 1→0. CARRY-OVERS: both `in-progress` stories still strict-stale
  and untouched — `6-9 cockpit-cross-platform` 44d→49d (P1), `2-1 lumen-design-adoption` 86d→91d
  structural umbrella (P2, decision still owed); `anchors.toml` `87/87` header (SC-4, P3 cosmetic);
  10 review stories frozen mid-pipeline (P2); aging watch quartet +5d (P3); 16 trace coverage gaps
  (flat). Open soft contradictions 2→~1.
