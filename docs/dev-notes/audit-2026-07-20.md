---
slug: dev-notes
status: in-progress
owner: spec-auditor
updated: 2026-07-20
---

# Spec Audit — 2026-07-20 (weekly cadence)

## Headline

**A frozen week — zero commits since the 2026-07-13 baseline, so every mechanical metric
is flat (lint PASS 0, anchors 119/119, 155 features, trace drift 0), but the freeze pushed
one carry-over past its threshold: `cockpit-cross-platform` crossed the 30-day strict-stale
line exactly as last audit predicted (now 35 days `in-progress`).** No new work means no new
findings — instead the ledger is dominated by carry-overs the freeze failed to action: the
SC-6 DSR-retraction propagation (P1, unchanged), the 5 aging watch items (all +7d again),
and the standing cosmetic contradictions SC-4/SC-5. This is the first audit in the series
with **no commits in the window**; the operator should read this as "nothing moved" rather
than "nothing to do."

## Run metadata

- Date: 2026-07-20 (scheduled Monday cadence).
- Baseline: `docs/dev-notes/audit-2026-07-13.md` (most recent audit on disk).
- **Commits since baseline: 0.** `git log --since='2026-07-13 00:00' --oneline` returns
  nothing; HEAD is `91a968e` (`fix(ci): fetch LFS checkpoints + iced Linux build deps`,
  2026-07-12 16:07). The repository has not moved in the audit window. Every numerical
  finding below is therefore identical-by-construction to the baseline unless a **date-relative**
  computation (age-since-update) crossed a threshold — which is exactly what happened to
  `cockpit-cross-platform`.
- Tooling: `spec_lint.py` requires Python 3.11+ (`tomllib`); this workspace ships 3.10. Ran
  under the standing `tomli`-as-`tomllib` shim (identical logic, result unchanged) — same
  run-environment note as the last six audits, not a finding.

## Mechanical lint

`scripts/spec_lint.py --all` — **PASS (0 violations).** Exit code 0. All 11 categories clean.

| Category                          | This run | Previous (2026-07-13) | Δ  |
|-----------------------------------|----------|-----------------------|----|
| dead-link                         | 0        | 0                     | 0  |
| missing-frontmatter               | 0        | 0                     | 0  |
| orphan-feature                    | 0        | 0                     | 0  |
| bad-anchor                        | 0        | 0                     | 0  |
| unreferenced-anchor               | 0        | 0                     | 0  |
| shipped-no-tests                  | 0        | 0                     | 0  |
| status-drift                      | 0        | 0                     | 0  |
| feature-shipped-trace-drift       | 0        | 0                     | 0  |
| feature-shipped-changelog-missing | 0        | 0                     | 0  |
| trace-broken-path                 | 0        | 0                     | 0  |
| adr-not-registered                | 0        | 0                     | 0  |
| **TOTAL**                         | **0**    | **0**                 | 0  |

Zero-commit window means this is definitionally unchanged; recorded for the series.

## Anchor verification

`scripts/verify_anchors.sh` — **ANCHORS PASS (119 / 119).** Unchanged. `anchors.toml` holds
119 `[[anchors]]` entries (count confirmed). No anchored report body was re-emitted (no
commits), so the SHA set is byte-stable. The 9 backtest scenario families and their SHA-256
bodies are untouched — audit-only, per instruction.

## Feature inventory

**155** `feature.md` files in scope (excluding `design/`, `dev-notes/`, `runbooks/`,
`archive/`, `architecture/`) — flat vs baseline. Layout: 119 in `spec/v1/`, 13 in `spec/v2/`,
6 in `spec/v3/`, 17 top-level. No duplicate slugs.

Status distribution (identical to baseline — no state transitions occurred):

| Status          | This run | Previous | Δ    |
|-----------------|----------|----------|------|
| shipped         | 124      | 124      | 0    |
| presenter-done  | 7        | 7        | 0    |
| retired         | 5        | 5        | 0    |
| tester-done     | 5        | 5        | 0    |
| deprecated      | 4        | 4        | 0    |
| dev-done        | 2        | 2        | 0    |
| candidate       | 2        | 2        | 0    |
| shipped-partial | 1        | 1        | 0    |
| in-progress     | 1        | 1        | 0    |
| roadmap         | 1        | 1        | 0    |
| reserved        | 1        | 1        | 0    |
| arch-done       | 1        | 1        | 0    |
| draft           | 1        | 1        | 0    |

## Stale features — NEW strict-stale hit (the week's escalation)

**One** feature now trips the strict staleness rule (`status: proposed` OR `in-progress`,
not updated 30+ days):

- **`cockpit-cross-platform`** — status `in-progress`, updated 2026-06-15 → **35 days**.
  Last audit flagged this at 28 days with the explicit prediction "will cross it next audit
  if untouched." It was untouched (zero commits) and has now crossed. This is the escalated
  **P1**. Its cross-platform claim remains unverified off-macOS for a 7th consecutive audit.

No `status: proposed` feature exists, so `cockpit-cross-platform` is the sole strict-stale row.

### Aging watch items (NOT strict-stale — carry-over, all +7d again)

| Slug                         | Status    | Updated    | Age  | Δ vs last audit |
|------------------------------|-----------|------------|------|-----------------|
| lumen-design-adoption        | roadmap   | 2026-05-04 | 77d  | +7d, no change  |
| lumen-phase-6-assistant-slot | reserved  | 2026-05-04 | 77d  | +7d, no change  |
| cockpit-app-bundle           | candidate | 2026-05-11 | 70d  | +7d, no change  |
| iced-ecosystem-evaluation    | candidate | 2026-05-13 | 68d  | +7d, no change  |
| ui-gallery-table-cell        | draft     | 2026-05-16 | 65d  | +7d, no change  |

All aged +7d with no state change (6th consecutive audit for the top four; the freeze added a
week to every one). These carry non-strict statuses (roadmap/reserved/candidate/draft) so they
do not trip the strict rule, but the drift is now long enough that "park or advance" is overdue.

## Orphans

- **Feature folders missing feature.md / tasks.md:** none (lint `orphan-feature=0`).
- **Test reports for missing slugs:** none.
- **Crates unreferenced in spec:** no new orphan crates; the standing phantom is the reverse
  case (a spec-referenced crate that does NOT exist) — see SC-5.
- **Orphan trace rows:** 0 genuine (the `live`/`realdata`/`yahoo` cargo-feature-gate false
  positives noted in prior audits are unchanged and remain false positives).

## Trace coverage gaps (16 → 16, flat)

16 in-scope `feature.md` slugs have no `[[req]]` row — identical set to baseline (zero commits,
so no new rows added, none removed). 142 distinct `feature=` values traced across 147 `[[req]]`
rows.

```
UI-infra / rollup (likely intentional, 8):
  lumen-phase-1-foundation … lumen-phase-6-assistant-slot (6, UI-infra convention)
  phase-2c-overlays  phase-2d  (2 v2 rollup/umbrella folders)

v1 carry-over (8):
  chart-fixture-line-clipping   leaderboard-timeframe-capital
  paper-soak-longevity          ui-drop-iced-aw
  ui-gallery-bin                ui-gallery-table-cell (draft)
  ui-headless-emulator          ui-session-journal-iced-tester
```

Low severity; add one-line REQ rows only if full cross-reference is wanted. Owner: analyst.

## Anchor coverage gaps

`unreferenced-anchor = 0` (lint) — every one of the 119 anchors is cited. No shipped strategy
feature is left with zero anchor coverage that the lint would surface. `verify_anchors.sh`
green at 119/119. No new gap. (The stale header comment in `anchors.toml` — SC-4 — is cosmetic
and separate; `verify_anchors.sh` does not read it.)

## Decay markers (TODO / FIXME / TBD / XXX / ???)

**No feature folder exceeds the 5-marker threshold** (unchanged). Per-feature-folder counts
(excluding `spec/archive/`):

| Folder                          | Count | Flag                                              |
|---------------------------------|-------|---------------------------------------------------|
| dev-notes (NOT a feature)       | 69    | self-reference: audit notes quote the grep patterns; exclude (was 64) |
| lumen-design-adoption           | 4     | below threshold (unchanged)                       |
| v1/ui-rethink-phase-a-lab       | 2     | benign                                            |
| architecture / adr              | 2     | phantom-`models` carry-over context (SC-5)        |
| v1/operator-ledger-schema-lint  | 1     | benign                                            |
| v1/live-equity-history-durable  | 1     | benign                                            |
| v1/ui-headless-emulator         | 1     | benign                                            |
| v1/advisor-llm-narration        | 1     | single TBD                                        |
| ui-design-principles.md         | 1     | FALSE POSITIVE — `XXXL` spacing token matched `XXX` |

The 69 in `dev-notes` are the audit notes spelling out the marker patterns (rose 64 → 69 as
this note accrues) — not real decay. No feature is decay-heavy.

## Soft contradictions (LLM-judged — verify before acting)

Swept the 5 cross-cutting topics named in the task (RNG seeding, money math, timestamps,
no-clocks-in-UI-tests, sub-agent capability boundary) against `product.md`,
`spec/architecture/`, and the relevant `feature.md` files. **Zero code/spec changed this week,
so no NEW contradiction could arise.** Re-confirmed the three standing items; all carry over
unchanged.

### SC-1 — Money math (`Decimal`, never `f64`) — CONSISTENT (HIGH)

No new money surface landed (no commits). `advisor-lot-realism` remains the most recent sizing
modifier and stays Decimal-exact (round-DOWN, ADR-0003). No opposing claim. No action.

### SC-2 — Determinism / seeded reproducibility (RNG) — CONSISTENT (HIGH)

No new decision-path code. `advisor-handoff-export` (deterministic, seed + `last_bar_ts`, no
wall-clock) and `advisor-pit-discipline` (look-ahead lint + publication lag, ADR-0086) remain
the current posture. No contradiction found in product.md / architecture / feature.md.

### SC-3 — Timestamps & no-clocks-in-UI-tests — CONSISTENT (HIGH, not re-verified at pixel layer)

The CLAUDE.md non-negotiable (verify Iced UI at the rendered-pixel layer; no wall-clock in test
harnesses) is not contradicted by any spec text. Not exhaustively re-run this frozen week —
flagged as an area, not a finding.

### SC-4 — `anchors.toml` header says `87/87` — actual 119/119 — STALE COMMENT (HIGH, carry-over)

`spec/anchors.toml:22` still reads "expects 87/87 PASS — 2026-05-31". Actual count 119;
`verify_anchors.sh` reports 119/119. Same SC-4 flagged the last five audits and DEFERRED
("fix on the next anchor-touching commit"). No anchor-touching commit occurred this week, so it
persists. Cosmetic — the script does not read the comment. No functional impact.

### SC-5 — Phantom `crates/models` in architecture files — CARRY-OVER (HIGH, low severity)

`crates/models` does not exist (`ls -d` confirms), yet referenced in
`spec/architecture/12-forecast-overlay.md`, `spec/architecture/00-current-state.md`, and
`spec/architecture.md`. Pre-registered architect doc-sweep item, DEFERRED at prior audits.
Documentation-only. No movement (no commits).

### SC-6 — DSR-retraction not fully propagated to editable surfaces — CARRY-OVER (MEDIUM-HIGH, was P1-new)

Last audit's substantive finding is **unactioned** (zero commits). The scorecard NaN-clamp fix
(`9e8cd05`) retracted the flagship P2 verdict "16 of 19 crowns clear DSR" → "0/19", recorded in
the append-only errata `spec/v3/advisor-corpus-expansion/reports/backtest-2026-07-10-p2-verdict-rerun-errata.md`.
The anchored report is correctly byte-immutable. Re-checked the editable surfaces this run:

- **STILL STALE (high-confidence propagation miss):**
  - `spec/v3/advisor-corpus-expansion/feature.md:609` — "symbol-runs, **16 of which clear the
    DSR≥0.95** credibility check) but **HOLDS**". Verbatim the errata's superseded phrasing.
  - `spec/v3/advisor-corpus-expansion/tasks.md:281` — "…2021-22 show materially more
    `ActiveWins` crowns, **16/19**".
- **Now carrying an inline retraction annotation (acceptable — NOT stale):**
  - `CHANGELOG.md:93` quotes the old figure but immediately annotates "…was RETRACTED same-day
    by the scorecard NaN-variance fix (`9e8cd05`): post-fix **0/19 clear DSR** — see the errata".
  - `docs/dev-notes/p2-wobble-thesis-analysis-2026-07-10.md:18` annotates "computed under the
    scorecard NaN bug"; its line :30 still restates "16 of 19" without inline retraction, but as
    a dated append-only decision-support dev-note this is arguably history, not a live claim
    (**flagged with uncertainty** — analyst to decide whether a pointer to the errata is wanted).
- **CLEAN this run (no hit):** `spec/product.md`, `README.md`,
  `docs/dev-notes/do-not-build-register.md`.

**Net:** the propagation gap has narrowed to the two `advisor-corpus-expansion` editable docs
(`feature.md:609`, `tasks.md:281`) plus the optional dev-note pointer. The top-level surfaces
the last audit worried about (product.md / README / do-not-build-register) are clean. Still a
P1 because feature.md is the ADR-0082 single source of truth and currently carries a retracted
number verbatim. Owner: analyst (editable files only — the anchored report stays byte-immutable).

## Resolved / improved since baseline

Nothing resolved and nothing regressed on the mechanical axes — the window had **no commits**,
so the entire "resolved since baseline" ledger is empty by construction. The only movement is
date-relative: `cockpit-cross-platform` aged 28 → 35 days and crossed the strict-stale line
(a regression in the staleness sense), and the aging watch items each +7d.

| Metric                            | Movement                          |
|-----------------------------------|-----------------------------------|
| Lint / anchors / drift / gaps     | HELD (frozen — no commits)         |
| `cockpit-cross-platform` staleness| 28d → **35d, now STRICT-STALE**    |
| SC-6 propagation (feature.md/tasks.md) | UNACTIONED (carry-over P1)     |
| Aging watch items                 | +7d each (6th cycle for top 4)     |

## Recommended triage

- **P1 — `cockpit-cross-platform` is now STRICT-STALE (35d `in-progress`, escalated from last
  audit's prediction).** The 30-day rule is tripped. Either resume + re-verify the cross-platform
  claim off-macOS (unverified for 7 audits), or explicitly re-status it (park to `candidate`/
  `roadmap`) so it stops tripping the rule. Owner: operator → tester.

- **P1 — SC-6: finish propagating the DSR retraction to the two remaining editable docs
  (carry-over, unactioned).** `advisor-corpus-expansion/feature.md:609` and `tasks.md:281` still
  carry the retracted "16/19 clear DSR" verbatim; feature.md is the ADR-0082 source of truth.
  Add the errata's corrected "gate-crowned, cost-robust, NOT DSR-certified" qualifier or a
  pointer. Editable files only — anchored report stays byte-immutable. Owner: analyst.

- **P2 — Approve or return the 7 `presenter-done` features (carry-over).** All carry PASS
  verdicts, accumulating un-ticked operator approvals; a frozen week is a good time to clear
  the approval queue. Owner: operator.

- **P3 — Aging watch items (5 items, all +7d, 6th cycle).** `lumen-design-adoption` (77d),
  `lumen-phase-6-assistant-slot` (77d), `cockpit-app-bundle` (70d), `iced-ecosystem-evaluation`
  (68d), `ui-gallery-table-cell` (65d, `draft`). Park or advance — the drift is now long enough
  to warrant a decision. Owner: analyst / operator.

- **P3 — Trace coverage gaps = 16 (flat).** 6 `lumen-phase-*` + 2 rollup folders likely
  intentional; 8 v1 UI/infra carry-overs. Add one-line REQ rows only if full cross-reference is
  wanted. Owner: analyst.

- **P3 — `anchors.toml` header stale (`87/87` → `119/119`, SC-4, carry-over).** Cosmetic; update
  on the next anchor-touching commit. Owner: next tester/dev.

- **P3 — Phantom `crates/models` in architecture files (SC-5, carry-over).** Documentation-only
  sweep. Owner: architect/developer.

## Deltas vs baseline (numerical)

| Metric                                | This run | Baseline (2026-07-13) | Δ        |
|---------------------------------------|----------|-----------------------|----------|
| Commits in window                     | **0**    | 38                    | **−38**  |
| Lint violations (total)               | 0        | 0                     | 0        |
| Anchors PASS                          | 119/119  | 119/119               | 0        |
| Features in scope                     | 155      | 155                   | 0        |
| Shipped features                      | 124      | 124                   | 0        |
| Strict-stale (proposed/in-prog >30d)  | **1**    | 0                     | **+1**   |
| Aging watch items                     | 5        | 5 (table)             | 0 (+7d each) |
| feature.md↔trace status drift         | 0        | 0                     | 0        |
| Trace coverage gaps                   | 16       | 16                    | 0        |
| Orphan trace rows (genuine)           | 0        | 0                     | 0        |
| Decay-heavy feature folders (>5)      | 0        | 0                     | 0        |
| Hard contradictions                   | 0        | 0                     | 0        |
| Open soft contradictions              | 3 (SC-4/5/6) | 3 (SC-4/5/6)      | 0        |

## Changelog
- 2026-07-20 (spec-auditor): Weekly scheduled audit. **0 commits since the 2026-07-13 baseline
  — first frozen window in the series** (HEAD `91a968e`, 2026-07-12). All mechanical metrics
  flat by construction: lint PASS(0) across 11 categories, anchors 119/119, 155 features (124
  shipped), feature.md↔trace drift 0, trace coverage gaps 16, no decay-heavy folder, no hard
  contradiction. HEADLINE: the freeze pushed `cockpit-cross-platform` past the 30-day
  strict-stale line (28d → 35d `in-progress`) exactly as the last audit predicted — the sole new
  strict-stale row (P1). SC-6 DSR-retraction propagation UNACTIONED (carry-over P1): narrowed to
  `advisor-corpus-expansion/feature.md:609` + `tasks.md:281` still carrying "16/19 clear DSR"
  verbatim (product.md/README/do-not-build-register now clean; CHANGELOG carries an inline
  retraction annotation — acceptable). Carry-overs unchanged: 7 presenter-done awaiting approval
  (P2), 5 aging watch items +7d each (P3), anchors.toml 87/87 header (SC-4, P3), phantom
  crates/models (SC-5, P3). No new work → no new findings; operator should read this as
  "nothing moved."
