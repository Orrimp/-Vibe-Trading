---
title: Test Report
feature: advisor-benchmark-robustness
run_id: 2026-06-22-1800-UTC
commit: ab13407
agent: tester
verdict: PASS
---

# Test Report — advisor-benchmark-robustness (B1) — 2026-06-22

## 1. Scope

- **Feature / change under test:** B1 — Benchmark exemption from the `AllFragile` outcome
  (ADR-0066). Two coordinated edits in `crates/backtest/src/bakeoff/rank.rs` restore the
  honest `BenchmarkWins` recommendation path on the real BTCUSDT H1-2024 7-arm field:
  D1 `all_fragile` → `all_active_fragile` (filter `!is_benchmark` from the `AllFragile`
  determination); D2 `is_eligible` → benchmark always crown-eligible. Classifier
  (`robustness.rs`, `bootstrap.rs`) byte-frozen.
- **Spec refs:** `spec/advisor-benchmark-robustness/feature.md`, ADR-0066
  (`spec/architecture/adr/0066-benchmark-exempt-from-allfragile.md`),
  `spec/advisor-benchmark-robustness/tasks.md`
- **Commit SHA:** `ab13407`
- **Rust toolchain:** rustc 1.94.1 (e408947bf 2026-03-25)
- **OS / arch:** Darwin M022517718D 25.5.0 (arm64, Apple Silicon)

## 2. Static Analysis

| Check               | Result | Notes                                          |
|---------------------|--------|------------------------------------------------|
| `cargo fmt --check` | PASS   | No diff output; workspace clean                |
| `cargo clippy`      | PASS   | `--workspace --all-targets -- -D warnings`; 0 warnings, 0 errors; finished in 1.45 s (cached) |
| `cargo audit`       | n/a    | Not re-run; no new dependencies introduced (`cargo tree -p ui` unchanged per commit diff) |
| `cargo deny`        | n/a    | No new dependencies introduced                 |

### Classifier freeze verification (load-bearing non-goal)

`git show ab13407 --name-only` confirms the fix commit changed exactly:

```
crates/backtest/src/bakeoff/rank.rs
crates/backtest/tests/robustness_bootstrap_bites.rs
crates/ui/src/fixtures.rs
crates/ui/src/screens/leaderboard.rs
crates/ui/src/strings.rs
crates/ui/tests/benchmark_wins_render.rs
spec/advisor-benchmark-robustness/feature.md
spec/advisor-benchmark-robustness/tasks.md
spec/architecture/adr/0066-benchmark-exempt-from-allfragile.md
spec/architecture/adr/README.md
spec/trace.toml
```

`crates/backtest/src/bakeoff/robustness.rs` and `crates/backtest/src/bakeoff/bootstrap.rs`
are **absent from this list** — classifier byte-freeze confirmed. `git diff HEAD~1 HEAD --
crates/backtest/src/bakeoff/robustness.rs crates/backtest/src/bakeoff/bootstrap.rs`
produced empty output.

## 3. Unit & Integration Tests

All tests run independently (not trusting cached state).

| Crate / Suite | Passed | Failed | Ignored | Duration |
|---|---:|---:|---:|---:|
| `backtest --lib bakeoff::rank` | 13 | 0 | 0 | 0.00 s |
| `backtest --test robustness_bootstrap_bites` | 17 | 0 | 0 | 0.52 s |
| `ui --features fixtures --test benchmark_wins_render` | 5 | 0 | 0 | 3.08 s |
| `backtest --features realdata --test bakeoff_e2e t7_1` (ignored, nocapture) | 1 | 0 | 0 | 12.13 s |
| **Total** | **36** | **0** | **0** | |

### bakeoff::rank — 13 tests (all pass)

Including the three tests covering the B1 seam:

- `t65_all_fragile` — fixture: `v0.sma` Fragile @ Sharpe 2.0 + `v0.buyhold` Fragile @ Sharpe
  1.0. After B1: every active arm is Fragile and a benchmark is present, so the benchmark is the
  only crown-eligible arm and wins → `BenchmarkWins` **despite the active arm's higher raw Sharpe**
  (eligibility trumps Sharpe — ADR-0066 § D5 Correction). PASS.
- `t65_all_fragile_no_benchmark` — field with no benchmark arm, all active Fragile → `AllFragile`
  residual dual. PASS.
- `t65_benchmark_wins_when_top_sharpe` — benchmark Fragile @ Sharpe 1.0, active arm Fragile @
  Sharpe 0.5 → `BenchmarkWins`. PASS.

### robustness_bootstrap_bites — 17 tests (all pass)

Including the two day-1 B1 reachability gates (ADR-0063 § D7 / R4.4, finally implemented):

- `benchmark_wins_reachable_when_all_active_fragile_and_benchmark_top_sharpe` — all active arms
  Fragile, benchmark top-Sharpe → `outcome == BenchmarkWins`, `reasons.contains(BenchmarkUndefeated)`,
  `crowned.is_benchmark == true`. FAIL-before / PASS-after gate confirmed. PASS.
- `all_fragile_residual_no_benchmark` — no benchmark arm, all Fragile → `AllFragile`. PASS.

### benchmark_wins_render — 5 tests (all pass)

Render-layer pixel verification per CLAUDE.md iced pixel rule:

- `benchmark_wins_paints_honest_baseline_crown` — crowned baseline row paints `ACCENT` highlight. PASS.
- `benchmark_wins_baseline_note_is_muted_vs_active_badge` — benchmark-note clay (strategy-column
  saturation) strictly less than active-arm Fragile badges. Anti-tautology tie. PASS.
- `benchmark_wins_empty_negative_control` — empty state negative control. PASS.
- `benchmark_wins_active_wins_negative_control` — `ActiveWins` populated state negative control. PASS.
- `benchmark_wins_copy_tokens_are_light_capable` — theme tokens light-capability guard. PASS.

PNG: `/tmp/benchmark_wins_render.png` — read and confirmed: the `BenchmarkWins` leaderboard
renders with the honest recommendation headline "No active strategy cleared the robustness
bar on BTCUSDT — simply holding (buy-and-hold) is the least-bad choice on this window." Row 1
(`v0.buyhold`) shows `baseline (buy & hold)` and a muted `baseline is path-dependent` note —
NOT a saturated Fragile pill. Active arms (rows 2–7) show `fragile` pills. Row 6 (Unanimous
vote 4-of-4) shows `sat in cash — consensus never reached` alongside its `fragile` badge. The
not-financial-advice footer paints. All 7 arms render.

### Failing Tests

_none_

## 4. Property / Fuzz Tests

_n/a_ — B1 is a pure outcome-determination change in `rank_candidates` (a filter + one boolean
disjunct). The existing `determinism_same_input_same_output` test (rank.rs, passes in the 13-test
run above) covers the determinism property. No proptest or fuzz suite is registered for rank.rs.

## 5. Backtest Results

**B1 introduces no new anchored backtest scenario** (ADR-0066 § D4, feature.md § Backtest Scenarios:
"None"). The advisor bake-off path writes no report (`write_report=false`, ADR-0059 § D3); the
classifier is frozen; the fix is a pure `rank_candidates` outcome-determination change.

The CLAUDE.md non-negotiable baseline-equity-divergence e2e is N/A in its literal form (B1
produces no equity / signal / fill). Its intent (a FAIL-before / PASS-after gate proving the
behaviour changed) is fully satisfied by the day-1 reachability e2e gate
(`benchmark_wins_reachable_when_all_active_fragile_and_benchmark_top_sharpe`).

### Real-data end-to-end proof (t7_1) — LOAD-BEARING

The **key proof** that B1 works on real BTCUSDT H1-2024 data. Verbatim output:

```
=== T7.1 FULL WIRED ADVISOR BAKE-OFF — BTCUSDT H1_2024 (Bootstrap 1000) ===
  Seed: [0xC0, 0xFF, 0xEE, 0, …]  Bootstrap seed: 0x0000000000eeffc0
  Field: 4 rule engines + 2 vote ensembles + buy-and-hold (appended)

  Strategy..............   Sharpe    Return%     MaxDD%   Trades  RobustnessFlag
  --------------------------------------------------------------------------------
  v0.buyhold                1.486     +47.78      22.69        0  Fragile    <== CROWN
  v0.5.macd                 0.966      +1.06       0.86      189  Fragile
  v0.5.rsi                  0.478      +0.38       0.97      120  Fragile
  v0.8.vote.majority        0.316      +0.19       0.79      100  Fragile
  v0.8.vote.unanimous       0.000         +0          0        0  Fragile
  v0.sma                   -0.038      -0.07       3.57      115  Fragile
  v0.5.bbands              -1.423      -1.09       1.30      206  Fragile
  --------------------------------------------------------------------------------
  Crowned:     v0.buyhold
  Outcome:     BenchmarkWins
  Reasons:     [BenchmarkUndefeated]
  Recommendation winner: v0.buyhold

  buy-and-hold total_return = 47.78%  (sanity guard: must be > +20%)
```

**Before B1:** `Outcome: AllFragile` (benchmark's own Fragile flag counted toward the determination;
benchmark ineligible for the crown → crown landed on a Fragile active arm → `ActiveWins` on a Fragile
crown, or `AllFragile` short-circuit — the bug B1 corrects).

**After B1:** `Outcome: BenchmarkWins` — benchmark Sharpe 1.486 (+47.78%) crowned as the
top-Sharpe eligible arm; all 7 arms still Fragile (classifier byte-frozen, no band moved).

Duration: 12.13 s.

## 6. Benchmarks

_n/a_ — B1 adds a `filter(!is_benchmark)` and one `||` disjunct to `rank_candidates`, a pure/total
function with O(N) pass at N ≤ 7. No hot path affected. `cargo bench` not run.

## 7. Environment / Infrastructure Issues

_none_ — all gates clean on first run.

## 8. Spec-lint Gate

```
spec-lint: FAIL (1 violations in 1 categories)

dead-link (1):
  [dead-link] spec/v3-volatility-forecaster/reports/vol-verdict-bs1-realdata-20260522.md:
    link target missing: ../architecture/adr/0038-vol-forecast-verdict-shape.md#d1-...
```

**Not a regression.** This is the pre-existing byte-immutable anchored-report dead-link recorded in
`spec/dev-notes/audit-2026-06-22.md` (current baseline: 1 violation in 1 category). B1 introduced
zero new lint violations. The violating file (`vol-verdict-bs1-realdata-20260522.md`) is an ADR-0038
§ D6 anchored report — it cannot be edited without breaking the body-SHA gate, making this a
permanent floor violation. No new regression to route.

**Pre-existing spec debt:** 1 dead-link (byte-immutable anchored report, `vol-verdict-bs1-realdata-20260522.md`);
carried since the 2026-05-22 anchor-commission. Baseline confirmed in `audit-2026-06-22.md`.

## 9. Anchor Verification Gate

```bash
bash scripts/verify_anchors.sh
```

Final output:
```
ANCHORS PASS  (119 / 119)
```

All 9 required anchors for strategy/exec/backtest/audit/report paths: PASS. B1 is anchor-safe by
construction (ADR-0066 § D4): the advisor path writes no report (`write_report=false`); the
classifier is frozen; `RobustnessMode::default() == Skip` keeps every anchored CLI path on `None`
flags; no `anchors.toml` SHA, `data/*/REVISION.toml`, or `spec/*/reports/` body was touched.

Anchors column for `REQ-ADVISOR-BENCHMARK-ROBUSTNESS-001`: `[]` (none — by design per ADR-0066 § D4
and feature.md § Backtest Scenarios "None").

## 10. Verdict

**`PASS`**

All gates cleared independently:

- `cargo fmt --check` PASS; `cargo clippy --workspace --all-targets -- -D warnings` PASS (0 warnings).
- 36/36 tests pass across 4 suites: rank unit (13), robustness_bootstrap_bites integration (17), UI
  render pixel (5), and the real-data end-to-end t7_1 (1).
- The load-bearing proof: `Outcome: BenchmarkWins` on real BTCUSDT H1-2024 (was `AllFragile` pre-B1).
  Crowned arm: `v0.buyhold` Sharpe 1.486 +47.78%; all 7 still Fragile (classifier byte-frozen).
- Classifier freeze confirmed: `robustness.rs` and `bootstrap.rs` absent from the fix commit's
  changed-file list; `git diff HEAD~1 HEAD` against both files produces empty output.
- `verify_anchors.sh` → 119/119.
- Spec-lint: 1 pre-existing violation (byte-immutable anchored-report dead-link floor from
  `audit-2026-06-22.md`); no new regression.
- PNG read and confirmed: the `BenchmarkWins` leaderboard renders the honest copy, muted baseline
  note, active-arm Fragile pills, and the 0-trade unanimous-vote "sat in cash" row.

## 11. Routing

`VERDICT → PASS` — B1 is complete. Benchmark correctly exempted from `AllFragile`; `BenchmarkWins`
reachable and fires on real BTCUSDT data; classifier byte-frozen; 119/119 anchors; no regressions.
Ready for CHANGELOG entry and feature.md status flip.

## 12. Follow-ups (non-blocking, deferred)

**Open item surfaced by ui-designer (v0.3 — do NOT fix here):** a 0-trade ensemble arm
(`v0.8.vote.unanimous`) is flagged `Fragile` by the bootstrap classifier on its degenerate flat curve,
so the leaderboard row renders BOTH "sat in cash — consensus never reached" AND a Fragile badge. The
sat-in-cash note arguably should suppress the vacuous Fragile pill (the 0-trade flat curve is
technically not an overfit risk, it just never traded), but that is an engine-semantics call touching
the frozen classifier region (`classify_verdict` / `verdict_bands` — ADR-0059 § D4 / ADR-0063 § D4).
Deferred to v0.3 as a separate feature; the current dual display is honest (the curve IS flat; the pill
IS technically correct per the frozen rule) and the sat-in-cash copy provides the human-facing context.
Do not open a change against the classifier without a new operator-approved ADR.
