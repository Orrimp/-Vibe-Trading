[package]
name = "exec"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
trading_core = { path = "../core" }
reflection = { path = "../reflection" }
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
async-trait.workspace = true
tokio.workspace = true
# v5-latency-slippage-sim R2: seeded RNG for deterministic latency jitter (ADR-0043 § D2)
rand.workspace = true
rand_chacha.workspace = true
blake3.workspace = true
# live-exec-client-binance-spot F1: authenticated REST client
reqwest = { workspace = true, features = ["json"] }
rust_decimal.workspace = true
rust_decimal_macros.workspace = true
# live-exec-client-binance-spot F1: HMAC-SHA256 signing (new dep, verified in checklist)
hmac = "0.12"
hex = "0.4"
sha2.workspace = true
uuid.workspace = true
time.workspace = true

[dev-dependencies]
rust_decimal_macros.workspace = true
time.workspace = true
criterion.workspace = true
cost = { path = "../cost" }
trading_core = { path = "../core" }
tempfile.workspace = true

[[bench]]
name = "latency_slippage"
harness = false

[[bench]]
name = "throughput_with_sim"
harness = false
